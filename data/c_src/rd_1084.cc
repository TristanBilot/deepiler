#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(g,b))/(cos(c));
h=(fmin(a,g))-(atan(e));
f=fmin(b,f);
if(isgreaterequal(f,d)){
h=fmin(f,h);
c=(pow(c,a))+(tan(a));
d=(pow(c,b))*(fdim(h,b));
}
g=pow(c,a);
h=fdim(c,e);
}