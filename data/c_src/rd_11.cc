#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(d,c);
d=(exp(e))/(fmin(d,g));
d=sin(h);
g=(atan(e))/(acos(d));
while(islessequal(d,d)){
d=asin(d);
h=(cos(d))*(fmax(g,g));
d=(fmax(h,b))*(exp(h));
g=log(b);
}
}