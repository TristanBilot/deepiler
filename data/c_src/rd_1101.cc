#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=pow(a,d);
e=fmax(e,d);
f=fdim(g,a);
c=(exp(d))/(floor(b));
e=ceil(f);
b=(fmin(c,a))*(pow(d,a));
a=sin(b);
while(isless(f,g)){
f=(atan(a))/(fmin(a,e));
g=fmax(g,e);
g=fmax(c,d);
d=(sin(e))/(fdim(b,d));
c=(atan2(c,f))/(atan2(f,e));
}
}