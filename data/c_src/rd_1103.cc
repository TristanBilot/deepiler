#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(exp(d))*(fdim(c,d));
c=pow(c,e);
e=fmin(b,f);
d=atan(b);
e=(fdim(b,d))-(ceil(d));
while(islessequal(b,f)){
f=log(e);
c=floor(a);
}
if(isless(b,c)){
f=(acos(e))/(tan(d));
a=sqrt(a);
f=(fmin(f,e))-(exp(b));
}
}