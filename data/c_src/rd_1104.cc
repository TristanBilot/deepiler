#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(e,a))*(floor(e));
c=sin(e);
a=(atan2(a,c))+(log(a));
while(islessgreater(a,e)){
b=fmax(a,c);
c=fmax(e,e);
c=atan2(a,e);
}
a=(sqrt(e))/(fmin(b,b));
e=exp(b);
a=asin(a);
}