#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=cos(c);
b=fmax(b,b);
c=(log10(e))/(log10(f));
c=(floor(g))/(log(c));
e=(log10(b))/(fdim(d,e));
while(isgreaterequal(b,g)){
d=(fmax(g,g))/(cos(g));
b=fmax(a,g);
c=(atan2(b,c))+(sqrt(a));
b=sqrt(c);
}
e=floor(e);
a=pow(f,d);
d=floor(d);
c=(exp(a))+(cos(a));
}