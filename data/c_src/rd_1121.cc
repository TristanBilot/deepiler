#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(c,b))*(atan2(e,c));
d=sqrt(b);
d=(pow(c,d))/(tan(e));
b=(atan2(d,c))/(pow(a,d));
d=(fmax(d,c))+(atan2(c,c));
c=tan(d);
e=pow(c,a);
c=(fdim(d,a))/(exp(d));
d=fdim(d,b);
e=atan2(e,c);
b=(sqrt(b))-(atan2(d,a));
e=fmax(d,e);
}