#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fdim(e,f))-(exp(b));
b=pow(c,b);
b=(fdim(g,c))/(log(b));
c=ceil(b);
g=(atan2(d,h))+(fmin(a,b));
f=fdim(d,h);
e=asin(a);
b=sin(d);
e=atan(f);
f=log(c);
h=cos(c);
g=(fdim(c,a))*(sin(d));
}