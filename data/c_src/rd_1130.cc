#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(d,g);
d=fmin(a,e);
f=tan(g);
b=fmin(d,b);
e=(fmin(d,f))-(fdim(c,c));
if(islessequal(b,e)){
b=log10(e);
d=(atan2(c,a))-(floor(e));
b=(fdim(g,f))*(fmin(c,b));
b=atan2(b,a);
g=(pow(b,b))+(floor(e));
}
}