#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(d,g);
f=cos(g);
if(islessequal(f,b)){
c=fmax(h,d);
a=acos(h);
e=(log(f))/(fmin(c,h));
e=exp(b);
}
b=(fmax(f,f))+(fmax(e,g));
f=(fdim(e,f))+(log(f));
b=pow(a,a);
}