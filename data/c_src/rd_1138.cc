#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(a,b))+(sqrt(a));
e=(atan2(d,a))-(atan(e));
a=sin(e);
a=atan(b);
c=(ceil(a))/(pow(a,e));
c=(ceil(b))-(log(b));
e=(ceil(d))+(fmin(b,a));
c=fmin(d,b);
c=(cos(b))-(atan2(b,b));
b=fdim(d,b);
a=(sqrt(c))/(atan2(a,b));
e=fmax(a,c);
}