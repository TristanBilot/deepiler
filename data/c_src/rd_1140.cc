#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(d,b);
e=(fdim(d,b))+(cos(a));
c=fdim(e,e);
while(islessequal(a,d)){
e=fmax(e,a);
a=fmax(d,e);
b=cos(b);
}
if(isless(c,d)){
c=sqrt(a);
b=exp(c);
}
}