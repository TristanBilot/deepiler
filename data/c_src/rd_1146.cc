#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=acos(g);
f=(acos(a))-(atan2(b,c));
while(isgreaterequal(b,a)){
g=ceil(e);
e=(asin(d))-(fmin(c,g));
}
while(isless(a,f)){
f=pow(g,d);
g=(pow(b,g))/(atan2(d,c));
d=acos(f);
}
}