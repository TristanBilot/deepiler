#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(a);
c=fdim(a,b);
a=(fmin(a,a))*(log10(a));
d=(fmin(a,c))/(cos(a));
b=(atan(d))+(pow(c,b));
while(islessgreater(e,e)){
a=(sqrt(b))*(cos(d));
e=(exp(c))+(atan2(b,d));
}
e=(cos(d))-(log(b));
a=atan(a);
}