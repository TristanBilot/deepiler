#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log10(a);
c=(acos(f))+(tan(f));
b=fmin(f,c);
while(isgreaterequal(e,e)){
b=(fdim(b,d))-(sin(c));
e=log(d);
d=(fmax(a,c))*(cos(c));
}
if(isless(e,a)){
c=fmax(b,c);
d=atan2(d,b);
f=(fmin(e,c))*(sin(c));
c=atan(c);
}
}