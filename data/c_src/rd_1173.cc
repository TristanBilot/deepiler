#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(c);
d=(sqrt(b))+(log10(b));
if(islessequal(b,b)){
d=(log(b))*(fmax(c,c));
d=(fdim(b,c))*(fmin(e,b));
b=exp(d);
a=(atan2(d,d))*(fmax(b,c));
d=acos(b);
}
else{
c=(floor(c))-(floor(b));
b=fmax(e,b);
a=atan2(e,e);
d=fmin(b,d);
a=(tan(c))*(asin(c));
}
if(isgreaterequal(c,a)){
d=tan(b);
c=tan(d);
}
else{
c=log10(e);
d=(fdim(d,a))+(ceil(e));
b=acos(e);
e=exp(c);
a=fmin(d,c);
}
}