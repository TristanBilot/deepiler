#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(b,e))/(log10(c));
c=floor(e);
b=atan(a);
b=sin(c);
e=(ceil(a))/(fmin(b,c));
while(isless(a,c)){
e=(exp(e))*(acos(a));
e=fdim(c,a);
d=fmax(b,a);
c=fdim(b,c);
}
if(islessequal(b,c)){
a=fdim(d,e);
b=(sin(c))*(atan2(c,b));
c=sin(b);
b=(cos(d))/(atan(c));
a=sqrt(a);
}
else{
a=(fmax(a,c))/(fmin(d,a));
e=log(c);
a=(fmin(e,c))*(log(d));
a=(fdim(e,e))+(fmin(e,a));
}
}