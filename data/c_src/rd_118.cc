#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=cos(e);
g=(cos(f))+(pow(g,b));
g=ceil(g);
b=sin(d);
e=(exp(a))-(fmax(e,f));
a=log(g);
if(isgreaterequal(c,e)){
c=(fdim(e,a))-(log10(c));
c=fdim(g,g);
a=(pow(g,e))/(fmax(b,e));
}
else{
f=pow(b,g);
c=sin(d);
f=(log(a))*(fdim(e,d));
e=atan(a);
}
}