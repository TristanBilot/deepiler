#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sin(b))+(exp(e));
a=exp(b);
a=exp(a);
d=fmax(d,c);
c=pow(c,a);
while(islessequal(a,e)){
d=atan2(c,c);
e=log10(d);
a=atan(b);
a=log(a);
}
c=(exp(e))/(log(d));
e=(sqrt(c))-(atan2(a,a));
a=fmin(c,e);
a=(atan2(c,b))+(fmax(e,e));
e=fdim(e,c);
}