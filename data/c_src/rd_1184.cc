#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,a))+(fmin(b,b));
b=(floor(a))*(cos(a));
a=(acos(c))*(asin(e));
a=log(a);
if(islessequal(c,a)){
d=(fmin(b,b))+(tan(e));
d=(fdim(c,b))-(cos(b));
a=ceil(d);
c=(log(b))-(pow(c,d));
}
else{
c=tan(d);
a=(log(c))+(atan(a));
e=(sqrt(e))*(pow(e,a));
e=ceil(e);
d=(sqrt(d))-(fdim(a,e));
}
if(isgreaterequal(c,a)){
d=pow(e,a);
b=(fdim(a,d))/(floor(c));
b=(fmax(d,a))*(tan(e));
b=sin(a);
a=pow(b,a);
}
}