#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sqrt(d))+(pow(c,c));
e=sqrt(a);
c=atan2(c,c);
a=fmax(a,b);
d=(log10(a))*(pow(b,d));
if(isless(a,a)){
c=(log10(b))*(atan2(d,c));
c=(exp(a))-(fmin(a,d));
d=fmin(e,c);
}
}