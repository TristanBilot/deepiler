#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(e,c);
a=(fdim(c,c))-(atan2(b,e));
while(islessgreater(a,d)){
c=(acos(d))/(fmax(b,c));
e=(fmin(b,d))/(acos(b));
c=fmax(a,c);
b=(cos(b))*(fdim(c,b));
b=pow(b,b);
}
d=fdim(c,c);
d=(cos(c))+(fdim(e,e));
}