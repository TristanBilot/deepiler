#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(c,e);
e=atan2(d,b);
d=(ceil(c))*(log10(c));
a=fmin(b,b);
while(isgreaterequal(e,f)){
f=(exp(b))/(sin(f));
f=fdim(b,c);
}
e=(atan(f))-(floor(d));
e=exp(b);
}