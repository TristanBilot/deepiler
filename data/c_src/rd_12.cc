#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(cos(b))/(exp(a));
a=fmin(b,b);
f=(fmin(b,b))+(fmax(d,c));
e=fmin(a,d);
e=(acos(b))/(sin(a));
b=(atan2(c,a))+(pow(f,f));
b=(log(f))*(sin(d));
e=(pow(d,e))*(atan2(d,e));
if(islessequal(c,e)){
b=fmin(d,e);
c=fmin(c,c);
d=(pow(d,a))-(log10(e));
}
else{
f=sqrt(f);
c=(cos(d))*(fmin(b,d));
d=(fmin(a,f))*(sin(f));
b=fmin(d,c);
e=(tan(e))-(ceil(b));
}
}