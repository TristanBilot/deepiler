#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(e,f))+(pow(d,b));
b=(floor(f))*(atan2(e,e));
b=tan(f);
f=fmin(c,b);
if(islessequal(b,f)){
c=fmax(b,b);
b=(fdim(e,f))/(cos(d));
f=(floor(d))*(cos(d));
}
else{
e=(pow(f,d))*(fmax(e,e));
b=pow(c,d);
b=(tan(a))-(pow(e,a));
e=fmax(f,c);
}
if(isless(b,e)){
b=atan2(a,c);
e=(fmin(b,d))/(atan2(b,d));
d=atan(a);
c=(atan2(a,c))*(cos(e));
a=fdim(b,f);
}
else{
f=(fmax(b,e))+(fmin(d,f));
a=fmax(f,b);
c=pow(a,b);
f=acos(c);
c=acos(d);
}
}