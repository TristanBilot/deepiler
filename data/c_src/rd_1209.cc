#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(d);
e=(tan(d))-(fmin(e,d));
e=fdim(b,d);
f=(log(h))*(pow(f,a));
a=fdim(e,b);
while(islessequal(g,a)){
a=(log(c))+(fmin(b,e));
b=(fmin(c,e))*(sqrt(f));
g=cos(c);
c=pow(h,d);
}
if(isgreaterequal(e,f)){
g=(log(f))*(ceil(a));
b=(fmax(b,h))+(atan(a));
a=(log(g))/(fdim(h,h));
h=(pow(h,b))*(sin(a));
f=(cos(a))*(pow(f,h));
}
else{
f=fmax(d,d);
d=fdim(b,g);
a=ceil(h);
}
}