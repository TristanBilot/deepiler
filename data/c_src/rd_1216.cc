#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(c,f);
f=(fdim(f,d))-(fdim(d,f));
a=floor(d);
c=(ceil(e))-(atan(d));
a=(tan(f))+(fdim(e,e));
if(islessgreater(f,f)){
f=fmax(f,a);
e=fdim(a,b);
e=(exp(f))-(fmax(b,a));
}
else{
c=atan2(b,e);
e=fmin(a,a);
c=fmax(e,f);
}
d=(sqrt(c))-(fdim(a,c));
f=(fdim(a,a))*(fdim(e,a));
}