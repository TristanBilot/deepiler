#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmin(c,c);
c=(log10(d))/(pow(b,f));
f=fmax(f,e);
e=(floor(e))-(atan(c));
f=pow(e,c);
a=acos(a);
c=(log(a))-(exp(b));
while(islessequal(b,a)){
a=acos(a);
a=(ceil(f))-(fmin(c,f));
c=exp(e);
c=(asin(d))+(atan(c));
e=fmax(e,e);
}
}