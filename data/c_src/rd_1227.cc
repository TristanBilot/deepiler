#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fmin(g,f))-(atan2(h,b));
h=(atan(b))*(atan2(d,e));
b=(atan2(b,b))+(tan(g));
e=(tan(f))+(fdim(b,c));
while(isless(a,h)){
e=sin(c);
a=(pow(c,e))-(floor(h));
b=(fdim(f,c))*(fmax(e,e));
a=(log10(e))*(asin(b));
a=(pow(g,h))*(atan2(g,d));
}
if(islessequal(d,f)){
c=(pow(h,e))+(asin(f));
b=(log10(b))*(sqrt(b));
}
else{
g=(fdim(g,a))-(fdim(h,d));
e=fmin(a,a);
b=asin(g);
e=(tan(e))*(fdim(g,h));
}
}