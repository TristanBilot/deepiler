#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(c,h);
c=(floor(b))/(asin(g));
e=tan(f);
h=fmax(e,a);
if(isgreaterequal(a,f)){
h=asin(b);
c=(floor(c))*(fdim(b,c));
d=fdim(e,b);
b=tan(d);
g=cos(c);
}
else{
c=fmax(b,d);
h=(fmin(a,h))+(tan(a));
h=(atan2(d,d))+(fmin(b,e));
f=(cos(a))/(ceil(h));
b=fdim(d,g);
}
while(islessequal(h,g)){
g=atan(g);
g=fmax(g,e);
}
}