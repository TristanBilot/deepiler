#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(asin(c))+(asin(b));
d=(pow(c,a))+(fmax(c,d));
c=pow(b,e);
e=sin(b);
c=(atan(d))/(atan2(d,c));
if(isless(b,e)){
f=(atan2(a,c))+(atan(b));
b=(atan2(b,f))-(fmax(a,c));
f=fmin(b,b);
b=log(d);
}
else{
c=sin(d);
c=(fdim(f,a))/(sin(a));
a=atan2(f,c);
b=atan2(e,f);
b=pow(b,a);
}
e=log(b);
a=(acos(f))*(cos(c));
a=(atan2(c,e))*(floor(e));
}