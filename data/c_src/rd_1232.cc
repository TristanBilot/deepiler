#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan2(e,c);
d=(fmin(c,e))/(pow(d,b));
e=pow(f,c);
if(isless(e,d)){
d=(pow(d,e))+(sqrt(b));
e=(atan(e))-(fmin(c,e));
a=pow(f,d);
}
else{
e=(exp(e))/(sin(d));
e=asin(e);
d=(fmax(c,d))+(fdim(e,c));
}
e=floor(a);
b=exp(d);
b=fmax(e,a);
e=(atan2(f,a))-(tan(c));
}