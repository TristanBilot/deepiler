#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(asin(f))*(cos(d));
d=fmax(f,g);
e=(fmax(g,a))/(cos(g));
d=(pow(e,d))-(log(h));
e=(fmin(e,f))-(sqrt(h));
b=(tan(a))/(sqrt(d));
g=log(c);
h=(asin(b))-(fdim(f,b));
a=(atan2(e,f))*(tan(f));
b=(fmax(e,h))-(atan(d));
h=ceil(e);
}