#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(e,b))-(ceil(e));
e=fmin(a,b);
e=fmax(g,a);
f=(fmax(c,g))*(log(g));
b=(fmin(b,g))+(fmax(b,g));
f=fmin(d,a);
g=(fmax(d,e))-(pow(a,g));
e=(atan2(c,e))-(atan2(e,g));
b=(acos(f))-(tan(c));
e=fmin(g,a);
b=(log10(b))*(floor(b));
}