#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(a);
e=(fdim(f,d))/(exp(c));
h=pow(h,f);
b=sin(h);
h=log10(h);
h=fmin(e,b);
a=(fmin(b,f))+(fdim(h,b));
a=(atan2(c,a))/(fmin(e,h));
if(isless(a,d)){
g=(fmin(g,h))*(cos(a));
a=(cos(b))*(pow(c,a));
e=log10(e);
e=atan2(c,b);
b=sqrt(g);
}
}