#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(c,d);
f=atan(e);
d=(tan(g))*(sqrt(e));
b=log10(g);
h=(tan(a))/(atan2(h,e));
d=(exp(d))/(acos(c));
a=(atan2(d,a))+(sin(c));
b=(floor(c))+(exp(d));
h=(floor(h))/(sqrt(b));
h=(pow(d,e))+(fdim(g,b));
d=fdim(a,a);
}