#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log(d);
c=(atan2(c,b))/(atan2(d,c));
a=(fmin(e,f))/(sqrt(a));
a=(fmin(c,c))-(log(a));
f=(fdim(c,e))-(pow(a,e));
if(isgreaterequal(b,g)){
a=fdim(a,d);
a=(fdim(f,d))/(cos(e));
}
else{
b=exp(e);
g=fdim(b,d);
d=pow(d,f);
b=pow(b,f);
e=(fmax(c,e))+(fdim(d,b));
}
}