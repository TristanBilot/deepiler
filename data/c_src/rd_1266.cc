#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(a,c))/(acos(b));
b=(acos(b))+(fdim(c,a));
c=(tan(b))-(fmax(d,c));
d=(fdim(c,e))/(fmin(d,e));
if(isless(b,a)){
e=(pow(d,d))+(fmin(a,d));
b=(fdim(d,e))-(fmax(a,a));
d=log10(d);
c=atan2(b,d);
a=(log(a))/(tan(d));
}
else{
a=fmin(a,d);
c=(cos(e))*(floor(b));
c=fmax(c,a);
e=(acos(c))/(fmin(c,c));
}
while(isgreaterequal(d,b)){
a=cos(d);
a=cos(b);
b=atan2(c,e);
}
}