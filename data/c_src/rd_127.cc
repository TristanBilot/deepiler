#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=asin(d);
c=pow(c,d);
d=floor(a);
b=(fmax(d,c))+(tan(a));
a=(fdim(a,d))+(cos(b));
while(isgreaterequal(d,b)){
e=(fdim(b,a))*(fmin(c,a));
c=floor(a);
}
d=(sin(c))/(fdim(d,a));
d=fmin(e,c);
d=fdim(a,a);
}