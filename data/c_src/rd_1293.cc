#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(g,a))+(fmax(f,h));
f=pow(h,b);
g=(fmax(b,b))-(fmin(c,c));
g=(fmax(f,a))/(atan(d));
d=floor(f);
while(islessequal(c,b)){
d=(atan2(g,e))/(ceil(b));
g=floor(f);
e=fmax(g,h);
e=(log10(g))-(pow(d,h));
}
if(isgreaterequal(d,g)){
d=atan2(a,d);
c=(atan2(b,b))/(fmin(a,a));
c=(fmax(e,f))*(fmax(e,e));
}
else{
e=sin(d);
e=(log10(b))/(atan(c));
}
}