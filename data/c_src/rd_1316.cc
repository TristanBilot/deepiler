#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log(a))+(exp(b));
b=(atan2(c,b))*(exp(f));
f=(atan2(d,d))+(fmin(c,e));
b=fdim(b,b);
c=(fmax(e,e))-(fmax(d,f));
c=cos(a);
b=tan(b);
while(isgreaterequal(f,d)){
d=(atan2(d,e))/(asin(d));
c=log(b);
d=(tan(d))/(atan2(f,e));
a=(ceil(f))+(fdim(c,c));
a=(fdim(a,b))+(log10(d));
}
}