#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(e,e);
e=(asin(e))/(fmin(d,a));
while(isgreaterequal(c,b)){
d=(cos(c))-(atan2(e,d));
d=fmax(a,b);
}
d=atan2(d,d);
c=(sin(a))-(fmax(b,a));
a=fdim(b,a);
}