#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(e,e);
e=(atan2(a,b))+(floor(d));
c=(log10(d))-(pow(c,d));
a=(sin(a))/(fmin(c,c));
d=(sqrt(d))-(pow(d,d));
d=ceil(b);
if(islessgreater(a,c)){
b=(fmax(e,c))/(fmin(e,c));
c=fmax(a,c);
e=sqrt(c);
}
else{
b=cos(b);
c=(ceil(e))/(atan2(b,b));
d=acos(a);
}
}