#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=pow(b,e);
d=exp(a);
e=(pow(e,c))-(pow(a,c));
while(isless(c,b)){
b=ceil(b);
a=fmin(e,b);
}
while(islessgreater(d,c)){
d=(pow(c,b))*(fmax(e,b));
d=(fmin(d,e))+(atan2(d,e));
}
}