#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(c);
a=floor(e);
c=floor(d);
c=(ceil(d))*(fmax(b,a));
e=fmax(b,d);
c=fmax(e,d);
if(islessgreater(b,d)){
a=(log10(e))/(atan2(d,a));
c=cos(b);
}
else{
a=atan2(e,a);
c=log10(a);
d=(atan2(a,e))-(fdim(c,d));
c=(exp(c))/(log(e));
c=log(e);
}
}