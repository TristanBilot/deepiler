#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan(a);
f=(sin(b))/(sin(g));
c=pow(a,g);
b=fdim(e,g);
b=log(f);
e=(exp(a))/(fmin(b,f));
c=fmax(b,c);
c=atan2(c,g);
g=atan(a);
c=fmax(b,d);
a=asin(a);
b=(fmax(b,g))-(atan(g));
}