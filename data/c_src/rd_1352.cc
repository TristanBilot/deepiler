#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,b))-(acos(e));
b=exp(b);
c=sin(e);
a=(fdim(b,e))+(sin(e));
c=(fmin(d,a))+(ceil(e));
if(isgreaterequal(a,b)){
d=pow(a,e);
e=(fmax(e,d))*(ceil(b));
d=exp(b);
}
if(isgreaterequal(d,b)){
e=pow(c,d);
a=(floor(a))*(atan(d));
}
}