#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=exp(e);
b=log(e);
d=log10(c);
if(islessgreater(a,b)){
b=(fdim(c,b))/(atan(a));
e=atan(e);
d=fdim(a,b);
b=(cos(e))/(fmax(d,a));
}
if(isgreaterequal(b,c)){
a=floor(e);
c=floor(d);
f=(fmax(e,a))*(pow(e,a));
f=(fdim(f,c))/(asin(b));
}
}