#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(cos(b))-(acos(f));
d=fmax(d,d);
b=tan(d);
a=exp(b);
f=(exp(c))-(sqrt(f));
d=pow(e,b);
f=sin(d);
while(isless(a,d)){
c=(acos(c))/(fmax(e,e));
e=(log(a))+(log(a));
f=fmin(c,d);
}
}