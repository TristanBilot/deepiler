#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(g,e);
d=(log(f))+(pow(b,g));
e=fdim(b,a);
d=floor(f);
c=pow(f,f);
g=fmax(c,c);
d=fmax(g,f);
g=ceil(e);
if(islessgreater(g,e)){
f=(fmax(c,b))*(pow(g,c));
a=(asin(f))-(pow(d,c));
a=fdim(b,d);
}
else{
f=fmax(e,d);
f=(asin(g))/(fdim(d,c));
}
}