#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(c,c))*(tan(c));
c=(cos(a))+(fmax(b,e));
c=atan2(c,b);
d=(cos(a))*(sin(e));
a=(atan(b))*(pow(c,b));
if(isgreaterequal(b,d)){
a=atan(b);
d=(log10(c))/(log10(a));
d=exp(d);
d=pow(d,a);
a=(exp(e))/(fmax(d,c));
}
while(isgreaterequal(d,e)){
d=(fmax(e,b))/(pow(d,e));
e=(atan2(d,e))/(fmax(a,e));
b=(acos(b))+(log(c));
a=floor(c);
}
}