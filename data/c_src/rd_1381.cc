#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(b);
b=fdim(f,f);
f=floor(h);
while(isless(c,g)){
g=(floor(g))-(fdim(e,d));
d=(pow(f,f))/(cos(h));
e=fmin(a,d);
f=(fmin(d,f))-(log10(d));
}
while(isgreaterequal(f,d)){
f=(floor(a))+(fmax(g,e));
f=(floor(c))+(fdim(e,c));
f=(asin(b))/(pow(e,h));
b=tan(g);
}
}