#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(e,b))/(log10(f));
e=log(b);
b=(log(f))+(floor(d));
d=fmax(f,e);
d=ceil(c);
if(isless(g,g)){
a=exp(c);
f=(asin(e))+(fdim(g,c));
f=(log10(c))-(fmin(d,g));
c=(fdim(g,b))/(pow(a,a));
}
if(isless(d,c)){
g=(pow(g,d))-(fmin(g,e));
c=fmax(a,c);
e=(tan(f))/(fdim(a,a));
}
}