#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(e,c);
c=sqrt(e);
c=cos(g);
while(islessequal(c,f)){
a=(fdim(a,d))*(log10(a));
f=(atan2(f,f))+(exp(d));
b=fdim(f,b);
g=(fmax(f,d))*(pow(b,b));
f=fmax(f,f);
}
f=(pow(b,c))/(pow(d,b));
a=(acos(b))-(cos(d));
}