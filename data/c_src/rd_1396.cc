#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(h,e);
g=tan(g);
while(isgreaterequal(c,f)){
a=(floor(g))+(atan2(c,a));
c=(fmin(b,h))-(ceil(c));
d=(sqrt(f))*(fmax(a,e));
b=(log10(h))+(atan2(d,a));
d=(cos(a))-(log10(d));
}
while(isgreaterequal(c,h)){
d=fmax(f,g);
a=log10(c);
e=(atan2(b,b))-(fmin(e,d));
g=acos(e);
}
}