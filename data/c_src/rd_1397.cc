#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=floor(e);
a=sin(f);
c=atan2(f,e);
while(islessequal(f,c)){
b=(pow(f,d))/(cos(e));
e=sqrt(b);
b=floor(e);
}
while(isless(c,b)){
d=(asin(a))+(atan2(b,d));
e=(fmax(c,b))/(fmax(e,a));
f=fmax(b,b);
e=log10(e);
}
}