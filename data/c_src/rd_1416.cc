#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(f,b))-(atan(d));
d=(log(e))-(atan2(c,e));
e=fmax(e,c);
f=tan(a);
e=atan2(d,c);
while(isgreaterequal(f,c)){
e=fmax(e,d);
c=(exp(b))/(exp(e));
a=(fmax(b,b))-(asin(b));
}
e=pow(b,b);
a=log10(a);
b=(atan(c))-(floor(a));
}