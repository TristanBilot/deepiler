#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(b,c))-(pow(b,c));
e=fmax(b,c);
c=atan(a);
c=(pow(d,e))+(pow(c,b));
a=(acos(b))/(tan(e));
while(isless(c,d)){
d=fmax(d,c);
c=pow(d,e);
d=atan(a);
d=atan2(d,a);
b=(pow(d,b))+(acos(c));
}
while(isgreaterequal(c,b)){
a=floor(d);
e=pow(b,b);
d=sin(e);
b=(pow(c,a))+(fmax(b,b));
}
}