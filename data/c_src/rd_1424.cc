#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(c,d))-(cos(g));
b=fmin(f,a);
b=(sin(e))+(floor(f));
c=fmin(a,b);
b=exp(f);
if(isgreaterequal(f,e)){
c=fmin(e,d);
f=fmin(b,g);
c=(sqrt(a))/(fmax(b,b));
}
else{
e=(pow(c,b))/(fmin(a,g));
a=fmax(d,c);
}
f=fmin(g,c);
e=(fmax(a,b))/(fmin(f,c));
}