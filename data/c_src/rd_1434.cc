#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(log10(g))+(cos(b));
c=exp(d);
c=(sin(f))/(pow(c,e));
a=fmax(c,b);
a=(cos(b))-(pow(c,b));
e=(fmin(c,c))*(tan(e));
c=(sqrt(b))/(sqrt(e));
a=fmin(a,f);
c=(fdim(b,a))-(fmin(e,c));
if(isgreaterequal(a,f)){
d=(exp(c))*(fmax(d,e));
a=fdim(g,c);
d=(acos(f))/(fmax(e,b));
e=(fdim(a,g))*(atan(b));
}
else{
d=(sqrt(e))-(fmin(e,c));
e=(fdim(a,d))+(exp(e));
f=(fdim(a,a))-(sqrt(f));
d=acos(c);
}
}