#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(h);
c=(pow(e,g))+(cos(b));
c=pow(h,f);
f=(ceil(b))*(asin(h));
g=pow(f,b);
e=(fmin(h,g))/(pow(g,d));
e=atan(d);
c=(tan(b))/(exp(f));
h=(ceil(g))+(fmin(e,g));
f=exp(c);
}