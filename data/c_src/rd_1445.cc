#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(asin(a))+(asin(g));
a=fdim(c,e);
g=(pow(c,g))*(asin(f));
g=(fmin(g,b))-(fdim(c,f));
while(isgreaterequal(b,e)){
e=pow(c,a);
d=ceil(e);
h=(fmax(d,b))+(fmax(c,a));
a=atan(e);
}
while(islessgreater(h,c)){
b=floor(d);
h=(cos(b))-(atan2(g,h));
}
}