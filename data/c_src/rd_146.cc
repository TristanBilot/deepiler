#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(tan(f))/(pow(c,d));
e=(ceil(b))/(sqrt(c));
e=fmin(e,d);
d=sin(e);
d=log10(a);
c=sqrt(a);
c=(exp(b))+(pow(a,d));
b=(floor(f))*(cos(e));
d=pow(c,d);
a=fmin(e,f);
}