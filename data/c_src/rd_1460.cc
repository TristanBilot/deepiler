#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(a,d);
d=(fmax(d,e))-(asin(d));
if(islessequal(d,c)){
b=(atan2(d,d))+(fmin(a,b));
c=fmin(d,c);
d=(tan(e))-(fmin(d,e));
}
while(islessgreater(a,d)){
d=fmax(d,d);
a=tan(d);
}
}