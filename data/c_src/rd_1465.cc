#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(e,c))-(fdim(a,b));
a=(cos(c))-(sqrt(b));
a=cos(e);
e=(fdim(b,d))+(log(a));
d=sqrt(a);
if(isless(b,d)){
e=floor(c);
c=fmax(a,c);
c=atan(e);
}
else{
c=tan(b);
d=fmin(d,d);
e=(floor(e))-(pow(d,d));
c=(atan2(e,a))+(atan2(d,c));
b=asin(b);
}
while(isless(b,e)){
e=(pow(e,d))/(fmin(b,c));
c=fdim(c,c);
b=acos(c);
}
}