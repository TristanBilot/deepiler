#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=sin(e);
d=fdim(c,e);
a=(pow(c,e))/(cos(b));
e=(atan2(b,c))-(atan(c));
a=(fmax(a,b))/(asin(f));
while(islessgreater(c,d)){
f=sin(b);
e=(pow(e,b))/(log10(f));
f=tan(a);
}
while(isless(b,d)){
d=pow(a,b);
f=sqrt(b);
b=(acos(c))/(log(d));
}
}