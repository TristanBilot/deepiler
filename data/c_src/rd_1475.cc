#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(b,f);
e=tan(c);
a=fmax(a,a);
a=fmin(a,e);
f=(fmax(b,b))*(acos(e));
e=log10(b);
f=(fmax(c,a))*(log(f));
e=(fmin(b,a))/(atan(a));
c=(atan(a))+(pow(a,c));
a=fmin(d,a);
f=cos(f);
d=(fmax(a,b))*(fmin(e,d));
a=ceil(a);
d=(exp(a))/(cos(f));
}