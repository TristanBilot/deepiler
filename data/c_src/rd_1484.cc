#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fdim(c,c))*(fmax(a,a));
c=fmin(f,d);
e=(pow(a,d))/(ceil(f));
e=(sqrt(e))/(fdim(c,f));
f=(log(f))/(tan(e));
if(isgreaterequal(b,b)){
e=(fmax(b,c))*(tan(f));
f=(pow(a,d))*(fmax(e,e));
a=(pow(e,c))+(atan2(e,e));
}
}