#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(b,c))+(pow(d,b));
b=(cos(b))/(fmin(a,b));
c=(fdim(b,e))-(fdim(e,a));
e=(pow(b,b))+(log10(c));
e=(pow(d,c))/(sin(e));
c=log10(b);
if(isgreaterequal(d,b)){
e=(atan2(c,a))+(fmax(e,d));
e=(fmax(c,e))/(sqrt(a));
c=(floor(c))/(fdim(b,e));
a=(ceil(c))+(acos(e));
}
else{
c=fmax(a,c);
c=fmin(b,b);
b=fmax(e,e);
}
}