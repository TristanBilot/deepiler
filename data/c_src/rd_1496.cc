#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(b))-(acos(d));
c=(ceil(e))-(sin(b));
e=log10(c);
c=(fmin(d,e))*(ceil(c));
c=(fmax(a,a))/(pow(b,e));
d=(atan(a))+(pow(b,e));
e=(fdim(d,e))/(fdim(c,b));
while(islessequal(d,b)){
c=cos(d);
a=(pow(c,b))+(floor(e));
}
}