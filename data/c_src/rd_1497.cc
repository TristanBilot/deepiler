#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log10(d);
f=cos(b);
a=atan(d);
b=(acos(h))*(sin(h));
a=sqrt(f);
g=atan2(f,f);
f=asin(h);
if(isless(g,e)){
f=sin(b);
e=(pow(h,b))-(fmin(g,f));
d=(pow(g,b))/(log10(h));
e=fmin(h,d);
h=(sin(f))/(tan(f));
}
else{
e=floor(b);
d=fmax(a,a);
}
}