#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=acos(b);
d=(exp(g))-(fdim(b,e));
f=atan2(h,h);
if(isless(a,c)){
a=cos(g);
g=atan(e);
c=(fdim(b,c))+(fdim(h,d));
d=atan(a);
h=acos(f);
}
else{
g=(fmin(e,a))/(log10(c));
c=fmin(e,c);
}
b=(exp(f))*(atan2(g,e));
e=fdim(a,h);
h=(asin(d))/(atan2(d,g));
}