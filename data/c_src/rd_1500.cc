#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(e,d))+(atan2(b,d));
b=fmax(d,a);
b=atan2(d,d);
c=fmin(a,d);
while(islessgreater(e,a)){
b=acos(e);
c=atan2(a,e);
}
a=(pow(b,d))/(floor(a));
b=(fdim(e,b))+(fdim(b,a));
b=fmax(c,b);
e=acos(e);
b=log10(a);
}