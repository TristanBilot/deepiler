#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=pow(a,e);
e=(floor(e))+(tan(f));
if(islessequal(b,f)){
b=fmin(e,a);
e=atan2(e,e);
b=(atan(b))+(atan2(f,b));
f=(exp(b))*(fmin(d,b));
}
else{
c=sqrt(b);
f=log(a);
b=fmin(e,a);
b=(fdim(a,c))+(tan(a));
a=(log(b))+(pow(f,f));
}
if(isless(f,b)){
a=atan2(f,d);
d=(fmin(e,d))/(fdim(b,f));
b=(pow(f,b))-(ceil(f));
b=asin(e);
a=(atan2(b,c))*(fmax(b,c));
}
else{
d=(fdim(d,c))+(sin(e));
b=fdim(e,f);
}
}