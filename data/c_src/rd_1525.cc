#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(b,e))/(acos(e));
e=(pow(e,h))+(log10(h));
e=(floor(e))/(atan2(c,d));
g=(atan(c))/(fmin(g,d));
f=(fmax(e,c))+(pow(c,f));
g=cos(c);
e=(pow(d,g))*(fmax(d,b));
if(isless(h,c)){
h=atan2(f,e);
h=(fdim(f,c))/(atan(e));
g=(fmin(d,b))+(atan2(b,d));
d=(acos(d))/(floor(h));
}
else{
f=acos(d);
c=fmin(d,e);
g=sqrt(b);
}
}