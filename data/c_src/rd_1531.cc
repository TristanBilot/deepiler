#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(e,c);
e=(fdim(c,d))*(log10(a));
a=fmin(a,c);
if(islessequal(b,d)){
a=(fmin(d,d))*(asin(c));
e=tan(c);
d=(tan(d))/(atan(b));
}
else{
e=atan2(c,e);
a=(fdim(c,c))+(fmin(e,a));
d=exp(d);
}
if(islessgreater(c,d)){
c=(fmin(b,b))+(atan2(e,c));
e=pow(a,a);
a=(exp(c))/(fmax(b,e));
}
}