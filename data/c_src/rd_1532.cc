#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(b);
c=fmin(b,e);
while(isless(e,a)){
b=(acos(b))/(atan(e));
b=(tan(a))/(pow(c,a));
}
if(islessequal(b,d)){
b=(atan2(c,a))/(sqrt(d));
d=(acos(a))-(sqrt(a));
b=sin(b);
e=(fdim(a,b))+(fmin(a,e));
}
else{
b=(pow(e,a))*(tan(a));
a=cos(c);
b=log10(c);
d=(pow(c,b))-(tan(e));
}
}