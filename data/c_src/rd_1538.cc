#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(b,c))-(fdim(a,c));
g=log(g);
d=atan(c);
d=(atan(a))+(fmin(d,a));
c=pow(d,e);
while(islessgreater(d,b)){
g=(sin(e))+(asin(f));
a=log(d);
b=(exp(b))*(fdim(e,b));
}
f=(fmin(b,f))*(fmin(d,b));
f=fmax(f,e);
a=(atan2(f,e))+(fdim(g,f));
}