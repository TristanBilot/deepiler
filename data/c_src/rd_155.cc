#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log10(a))*(pow(b,c));
b=acos(e);
d=(acos(b))/(pow(a,e));
if(isgreaterequal(c,d)){
e=log10(b);
a=fmin(e,b);
}
if(islessgreater(c,a)){
d=cos(c);
b=(atan2(b,e))+(fmax(a,a));
a=fmax(b,d);
c=(sqrt(e))+(log10(b));
d=(acos(a))-(acos(d));
}
}