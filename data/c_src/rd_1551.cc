#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(c,d))+(sqrt(c));
d=(fmin(d,a))/(fmin(a,c));
d=sqrt(e);
d=fdim(e,d);
d=fdim(d,d);
d=(fmax(e,b))/(ceil(d));
a=(atan(a))+(atan2(d,b));
b=asin(b);
while(isless(a,a)){
c=fmin(c,a);
b=sin(a);
a=(atan2(d,c))/(fdim(e,c));
e=(log10(d))/(ceil(a));
a=(floor(d))*(fmin(a,d));
}
}