#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(tan(b))*(sqrt(e));
b=(floor(c))+(fmin(e,d));
c=log10(d);
b=fdim(e,d);
a=sin(d);
while(isgreaterequal(a,a)){
a=fmax(a,c);
e=(tan(e))-(fdim(e,a));
}
if(isgreaterequal(b,e)){
d=exp(e);
a=tan(c);
a=fdim(d,c);
c=floor(e);
e=atan(c);
}
else{
e=(ceil(e))+(cos(e));
e=(sqrt(a))+(fmax(b,d));
b=(pow(a,e))/(atan2(c,e));
c=fmin(c,d);
c=tan(c);
}
}