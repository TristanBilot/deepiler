#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=exp(f);
d=(atan2(f,a))+(fmax(f,c));
b=pow(f,b);
b=pow(b,b);
d=sqrt(c);
a=(pow(f,d))+(fmax(e,c));
f=(floor(f))+(pow(a,b));
while(islessequal(e,a)){
a=(floor(c))+(tan(a));
a=atan2(e,e);
}
}