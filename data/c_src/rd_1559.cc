#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(g,a);
a=cos(e);
c=(sin(e))*(log10(a));
if(isgreaterequal(c,a)){
e=(asin(f))-(fdim(b,a));
e=(ceil(g))*(fdim(d,b));
e=fdim(a,c);
e=(log10(b))*(fmax(f,e));
b=(atan(e))/(fmin(b,f));
}
else{
b=(log10(c))/(fmax(d,b));
e=(acos(e))-(ceil(d));
a=pow(e,c);
b=(fmin(a,b))*(atan(e));
}
while(islessequal(e,a)){
b=(cos(f))/(exp(g));
f=floor(a);
e=tan(a);
}
}