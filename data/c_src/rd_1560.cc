#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(d);
b=sin(e);
b=(sin(e))*(log10(d));
if(islessgreater(b,e)){
e=(pow(d,c))*(cos(a));
e=ceil(e);
c=fmax(a,c);
d=(fmin(d,c))*(fdim(e,a));
d=fdim(a,d);
}
else{
e=(floor(c))-(log(a));
a=fmin(b,d);
b=pow(b,a);
}
if(isgreaterequal(e,d)){
d=(atan2(b,d))/(acos(a));
c=sqrt(e);
c=fmin(c,a);
c=(atan2(d,a))/(log10(a));
}
else{
e=(log(a))-(tan(e));
a=fmax(d,e);
}
}