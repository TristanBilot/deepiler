#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(c,d))/(log10(f));
d=fmax(a,d);
b=tan(e);
while(isless(a,e)){
d=(fdim(b,a))+(fdim(d,e));
b=acos(d);
a=(pow(a,c))*(fdim(a,a));
e=fmax(f,d);
d=acos(f);
}
while(isgreaterequal(b,a)){
a=sqrt(d);
a=(cos(e))-(fmax(e,e));
d=(fmax(e,a))*(exp(b));
a=(atan(a))*(sqrt(d));
}
}