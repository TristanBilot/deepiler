#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(e,f);
c=log(g);
while(isless(c,e)){
a=log10(b);
a=acos(f);
b=(tan(a))/(cos(d));
f=fmax(a,b);
e=sqrt(g);
}
c=fmin(d,e);
c=log(g);
e=(fmax(g,c))-(fmin(e,a));
}