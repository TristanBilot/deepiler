#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(c,e);
d=floor(a);
e=(ceil(b))/(fmin(e,f));
f=floor(a);
if(isless(d,a)){
e=log10(e);
b=(acos(f))-(log10(d));
}
while(isless(c,c)){
c=fdim(e,c);
e=exp(e);
}
}