#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmax(c,e);
f=(fmax(f,e))*(log10(d));
f=pow(e,d);
a=(exp(c))-(atan2(b,f));
f=(cos(b))+(atan2(f,b));
a=(acos(b))/(atan2(b,a));
b=tan(c);
a=asin(c);
}