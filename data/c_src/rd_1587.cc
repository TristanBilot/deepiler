#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sin(b))+(atan2(b,e));
a=(fmax(a,f))*(fmin(d,c));
if(isless(a,g)){
c=(fmax(b,c))/(fmax(c,b));
c=(tan(b))*(sin(e));
d=pow(d,d);
}
else{
g=fdim(e,f);
a=(exp(b))-(fmax(c,g));
c=exp(a);
a=(floor(g))-(sin(c));
e=(fdim(g,d))*(sin(e));
}
if(islessgreater(g,g)){
a=(fmin(f,c))*(sqrt(c));
f=(fmax(a,b))-(sin(d));
d=(exp(c))/(fmax(g,a));
}
else{
b=(fdim(c,f))+(log10(b));
g=(pow(c,e))+(pow(b,f));
f=cos(b);
g=(fmax(a,g))+(fmin(g,e));
g=(fmax(c,d))+(pow(b,f));
}
}