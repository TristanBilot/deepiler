#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(e,d);
c=(log10(c))/(sqrt(g));
c=(fmin(b,d))+(log10(b));
while(isless(a,g)){
d=fmin(b,a);
a=(pow(e,h))/(pow(f,d));
b=pow(g,g);
b=(log10(f))*(cos(a));
f=(atan2(e,h))/(asin(e));
}
if(islessgreater(d,a)){
c=(pow(b,b))-(ceil(d));
a=(fmax(b,h))*(pow(g,c));
c=(fmin(g,d))+(fmax(a,g));
h=(pow(h,b))+(fmin(f,b));
}
else{
g=(pow(a,a))+(exp(g));
g=log10(b);
c=fmax(g,h);
}
}