#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(d,e);
b=(acos(a))/(acos(c));
b=(fdim(d,c))-(tan(c));
e=(exp(a))*(fdim(c,e));
b=(fdim(d,b))/(cos(c));
b=(asin(b))+(pow(a,a));
a=floor(a);
b=(floor(b))-(atan2(d,b));
c=ceil(a);
b=sin(c);
c=(log(b))/(fdim(c,e));
}