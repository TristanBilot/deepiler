#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(b,a);
e=(ceil(e))-(fdim(f,c));
while(isless(c,d)){
f=fmin(a,d);
b=fdim(f,a);
}
while(isgreaterequal(b,f)){
e=atan2(c,a);
f=fmax(e,e);
a=(atan2(b,f))-(fdim(d,d));
e=acos(c);
}
}