#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log(b))*(cos(g));
a=(fdim(g,f))*(fdim(d,c));
f=(fmin(e,f))-(tan(b));
a=fdim(c,a);
f=fdim(e,c);
b=(sqrt(h))/(fdim(e,c));
f=(cos(g))*(atan(d));
a=(atan2(h,h))+(log(f));
c=(floor(b))+(pow(c,g));
g=(fmax(g,c))*(ceil(f));
e=(fmax(e,e))/(fmin(g,a));
}