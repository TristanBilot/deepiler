#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(b);
h=fdim(g,c);
h=sin(h);
h=(pow(c,h))*(sin(h));
if(islessequal(e,e)){
f=(sin(a))+(sin(d));
c=(atan2(g,a))-(pow(d,b));
f=(acos(a))-(floor(h));
}
if(isless(b,c)){
f=fmin(g,g);
b=(cos(a))*(exp(b));
g=fmin(c,e);
}
}