#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log10(b))*(fmax(d,d));
d=(cos(d))-(log10(b));
d=(pow(d,a))/(fmax(c,a));
e=atan2(a,e);
b=asin(c);
a=fdim(b,b);
e=pow(b,e);
if(isless(e,c)){
e=pow(c,c);
c=(pow(b,e))/(fmax(c,d));
}
else{
e=(acos(a))*(floor(a));
b=fdim(c,e);
}
}