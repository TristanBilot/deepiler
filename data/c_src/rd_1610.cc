#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=cos(d);
f=exp(a);
b=sin(f);
a=(fdim(a,c))-(fdim(f,d));
f=(sqrt(a))/(fmax(e,e));
while(islessequal(d,d)){
d=sin(b);
b=(sqrt(e))-(atan2(e,e));
}
while(isless(c,c)){
a=(log(e))+(pow(c,c));
e=atan(e);
}
}