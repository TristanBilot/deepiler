#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(d);
e=(pow(e,d))*(cos(b));
if(isgreaterequal(b,d)){
c=fmax(d,d);
c=(pow(d,c))*(cos(b));
b=(fdim(b,c))/(pow(b,e));
}
if(islessequal(b,d)){
b=(fdim(e,a))*(fmin(a,a));
d=sin(d);
}
}