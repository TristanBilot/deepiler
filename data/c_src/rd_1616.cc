#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=ceil(e);
b=(fmax(e,c))+(tan(e));
while(islessequal(b,c)){
c=(sqrt(b))+(fmax(e,d));
a=log10(e);
d=(exp(e))*(pow(e,a));
b=(sqrt(a))+(fdim(b,e));
d=log(c);
}
a=sin(b);
b=(sqrt(a))/(fmin(a,a));
d=(acos(a))*(log(e));
}