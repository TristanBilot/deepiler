#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(e,d))+(floor(g));
g=(fmin(a,b))*(asin(a));
b=fmin(c,d);
a=log(b);
while(islessgreater(b,e)){
f=(fdim(b,d))/(atan2(b,e));
g=(ceil(b))/(fdim(d,a));
b=fmin(b,g);
a=(fdim(e,b))+(fdim(d,f));
g=(cos(e))/(fmin(c,d));
}
if(isgreaterequal(d,d)){
d=(log(a))+(fdim(a,f));
g=(fmin(g,a))*(fdim(e,b));
f=floor(g);
}
else{
b=(log(d))/(atan2(d,a));
c=(pow(d,g))/(log10(b));
}
}