#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,a))+(fmax(b,a));
a=(exp(a))/(ceil(b));
c=sin(b);
c=(exp(d))/(pow(c,d));
a=(acos(b))*(atan(a));
d=pow(d,b);
e=fdim(e,c);
while(islessgreater(c,e)){
d=atan2(b,d);
a=(fmax(b,a))/(log10(c));
b=(fmax(d,b))+(sqrt(c));
}
}