#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(a);
b=atan2(e,c);
a=(fdim(d,d))+(log(c));
d=(fmax(b,e))*(asin(b));
while(islessequal(e,d)){
a=pow(b,d);
d=(cos(c))+(pow(a,e));
}
d=(atan2(d,a))/(cos(b));
a=(log(b))/(floor(b));
}