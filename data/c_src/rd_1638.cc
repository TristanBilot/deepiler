#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=cos(e);
c=(tan(b))/(fdim(g,c));
e=floor(d);
g=atan2(f,c);
b=fmax(d,b);
d=pow(a,g);
f=(atan(b))*(fmin(f,a));
e=(log(g))+(exp(f));
b=(acos(f))*(pow(a,a));
c=pow(d,f);
if(isgreaterequal(a,d)){
e=asin(a);
e=(fdim(f,a))+(sin(g));
b=ceil(c);
}
else{
b=sqrt(b);
d=exp(f);
g=log10(g);
}
}