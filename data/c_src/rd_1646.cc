#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(f);
d=(asin(f))+(sin(e));
a=(sin(b))/(fmax(c,g));
f=(floor(d))*(fdim(e,d));
f=fdim(a,e);
if(isless(a,f)){
b=tan(f);
d=(pow(c,b))*(atan2(e,d));
g=asin(c);
}
else{
f=(fmax(g,f))/(pow(c,b));
b=(pow(a,b))/(fmax(c,e));
g=fmax(g,d);
b=(pow(e,b))-(sqrt(g));
g=(fmax(g,g))/(fdim(c,a));
}
while(isgreaterequal(e,g)){
f=(fdim(f,d))*(atan2(d,e));
c=(fmax(c,e))*(exp(c));
e=fdim(d,d);
g=(log(f))+(sqrt(e));
e=(pow(g,a))/(fdim(d,g));
}
}