#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(b,h))*(floor(g));
e=ceil(d);
a=atan2(e,d);
if(isgreaterequal(a,d)){
g=(log10(e))/(fmin(f,h));
c=exp(c);
b=ceil(f);
d=pow(h,c);
a=exp(g);
}
else{
h=(fmax(c,c))/(sqrt(b));
a=acos(d);
}
e=fmax(b,a);
f=fmin(d,b);
}