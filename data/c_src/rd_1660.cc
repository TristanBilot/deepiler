#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan(b);
a=(fmax(c,g))*(cos(g));
a=fdim(b,g);
f=(fmin(f,e))/(exp(b));
d=cos(c);
g=(exp(f))-(ceil(g));
d=fmin(f,b);
e=(fdim(d,b))*(pow(a,g));
a=(ceil(d))*(pow(g,d));
if(isless(b,c)){
c=(fdim(f,e))*(sin(e));
g=(fmin(g,c))-(tan(g));
}
else{
f=(fmax(b,d))/(fdim(e,b));
e=(pow(b,e))/(pow(c,d));
c=pow(a,d);
}
}