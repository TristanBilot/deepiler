#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(c);
h=fdim(g,h);
g=sqrt(c);
h=acos(h);
h=fmax(a,h);
g=(exp(h))/(acos(e));
e=(fmax(c,b))-(exp(b));
e=(sin(h))+(fmin(b,c));
e=fdim(c,h);
if(isless(g,e)){
h=tan(g);
f=(fmax(g,b))+(floor(g));
h=(fdim(e,c))+(asin(c));
b=(floor(b))*(fmax(b,b));
g=atan2(d,g);
}
else{
g=log(b);
d=fmax(b,e);
e=atan2(d,d);
b=(fdim(b,b))/(floor(c));
d=(asin(e))+(acos(g));
}
}