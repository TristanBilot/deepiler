#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(tan(c))/(acos(a));
c=(atan(b))/(ceil(g));
c=(atan2(h,b))*(sqrt(f));
if(isless(e,g)){
h=fdim(g,f);
d=(cos(a))/(floor(f));
a=(fmin(b,b))*(fmin(e,a));
g=(atan2(c,e))/(fmax(g,a));
e=(fdim(c,e))/(cos(g));
}
else{
e=(pow(e,d))*(atan(f));
g=acos(f);
b=(fdim(b,g))/(sqrt(h));
}
while(islessgreater(e,b)){
e=(sin(c))/(fmax(g,d));
d=pow(d,g);
h=(log10(d))-(fmin(c,g));
d=(exp(h))-(ceil(g));
}
}