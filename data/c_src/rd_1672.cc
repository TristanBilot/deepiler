#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(a,b);
b=(atan2(d,e))*(exp(b));
while(isgreaterequal(d,a)){
e=(pow(e,a))-(cos(d));
b=(atan(b))-(log10(c));
}
c=floor(a);
b=fmax(b,e);
}