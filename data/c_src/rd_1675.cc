#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(b))/(atan(c));
a=sqrt(b);
c=pow(b,e);
c=(sqrt(e))+(ceil(d));
d=fmax(a,b);
c=(log10(d))/(sin(a));
d=cos(b);
c=(atan(b))*(tan(c));
a=(acos(b))+(atan2(b,c));
e=exp(b);
a=sqrt(b);
a=(tan(a))/(fmin(b,c));
}