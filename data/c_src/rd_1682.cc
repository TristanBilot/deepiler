#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(sin(e))/(fmin(f,f));
d=fdim(g,e);
if(islessequal(b,b)){
g=sin(c);
a=log(a);
}
else{
f=floor(g);
e=fmax(g,a);
b=(pow(f,c))+(log10(e));
}
b=(fmax(c,b))/(exp(e));
b=fdim(g,b);
d=atan2(e,g);
g=floor(e);
d=exp(f);
}