#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(exp(b))+(fmin(c,f));
g=asin(g);
c=(sqrt(d))/(fmax(g,a));
f=(ceil(f))+(fmin(g,e));
g=(tan(g))-(fmax(a,d));
g=log(f);
b=fmin(d,c);
c=(atan2(d,c))-(fmin(f,a));
e=(fmax(d,d))+(pow(c,d));
e=log(b);
}