#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(b,d);
e=atan2(b,a);
f=(floor(a))+(fmax(b,d));
if(islessequal(d,b)){
b=(fmax(d,e))-(tan(a));
f=(log10(f))-(ceil(c));
c=(log(d))+(ceil(c));
e=atan(c);
}
b=fmin(f,f);
a=sin(b);
b=(cos(b))+(atan2(f,e));
e=fmax(d,c);
}