#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(h,f))+(atan(d));
f=log(a);
b=(fmax(e,a))-(fmin(h,d));
if(isless(f,f)){
b=(asin(c))-(cos(d));
h=(pow(g,f))-(atan2(g,b));
f=tan(g);
f=cos(e);
d=fmax(c,c);
}
else{
e=pow(g,g);
h=cos(g);
}
d=(fmin(b,c))+(floor(b));
c=fmin(e,h);
}