#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(b);
d=pow(b,c);
d=(fmin(b,a))*(fmin(a,c));
while(isless(c,a)){
b=(atan2(c,d))/(atan2(d,d));
e=(acos(a))+(pow(b,e));
a=(fmin(e,a))-(atan(d));
d=asin(c);
}
if(isless(b,a)){
c=(fmax(c,c))*(fmax(d,c));
b=ceil(a);
d=ceil(d);
}
else{
d=(fdim(b,e))-(exp(c));
a=(fdim(c,d))+(atan2(c,c));
d=fdim(b,d);
c=fdim(c,e);
b=floor(e);
}
}