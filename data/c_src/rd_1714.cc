#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log10(b);
e=(floor(e))+(fmax(a,d));
a=(atan(e))/(pow(c,b));
d=(log10(c))+(tan(e));
b=pow(d,d);
c=(atan2(d,a))+(log(e));
if(islessequal(a,e)){
e=acos(e);
e=(pow(d,e))-(sqrt(b));
}
else{
e=fmin(c,c);
e=sin(c);
a=pow(e,d);
a=ceil(e);
}
}