#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(cos(e))*(cos(b));
f=(fmin(f,a))/(exp(a));
while(isgreaterequal(b,a)){
d=exp(f);
e=atan2(b,a);
d=sin(b);
f=tan(a);
f=(fmax(c,f))+(fmin(a,e));
}
a=(fmin(b,a))*(pow(a,a));
c=fmin(c,f);
g=fmax(f,g);
e=(acos(e))+(fmin(f,a));
}