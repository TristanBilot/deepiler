#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log10(d))*(fdim(a,e));
a=floor(f);
f=fmin(b,d);
b=(acos(e))-(pow(d,b));
d=floor(b);
f=(tan(e))+(log10(b));
d=(sqrt(d))-(sqrt(f));
while(isgreaterequal(b,f)){
f=(fmax(g,c))-(atan(c));
e=(fmax(a,c))-(fmin(e,g));
b=(tan(a))/(sqrt(b));
a=fdim(c,g);
}
}