#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(a,a))+(sqrt(b));
d=asin(c);
b=log10(e);
c=sin(b);
e=(pow(e,b))+(sqrt(a));
while(isgreaterequal(a,a)){
e=(fmin(a,e))+(atan2(c,e));
e=(cos(e))/(fdim(b,c));
a=fdim(e,c);
c=acos(e);
c=(fmin(e,e))*(floor(d));
}
a=(atan2(b,b))/(floor(e));
c=fmax(b,a);
c=(atan2(a,d))-(exp(c));
b=(cos(e))+(exp(a));
e=fmax(a,b);
}