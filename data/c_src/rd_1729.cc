#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=sin(c);
d=tan(d);
f=fmax(b,a);
e=floor(c);
if(islessgreater(d,d)){
e=(tan(c))/(log10(f));
a=(fdim(a,d))-(fmax(b,c));
}
f=asin(d);
a=(atan2(b,d))/(fdim(c,c));
e=atan2(d,d);
}