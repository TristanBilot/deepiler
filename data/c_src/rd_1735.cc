#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(c,e))-(tan(e));
a=(fmin(e,c))-(pow(e,e));
a=(atan(a))-(sin(c));
a=(floor(b))+(sqrt(e));
b=(fmax(b,c))-(fmin(b,d));
while(islessequal(b,e)){
b=(atan2(b,d))-(fdim(b,b));
d=acos(d);
}
while(isless(b,e)){
e=fmax(a,e);
b=fmin(b,d);
}
}