#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=log(a);
f=fmax(f,c);
e=(fmin(c,f))+(sqrt(e));
d=(fmax(c,e))-(fdim(b,b));
b=(acos(a))/(exp(a));
while(isgreaterequal(b,d)){
d=log(b);
e=acos(d);
e=(asin(e))/(fmax(f,c));
b=(fmax(d,d))+(fmax(f,e));
}
while(islessequal(c,f)){
e=fmin(b,f);
f=(sqrt(f))*(exp(e));
c=(fmax(d,a))+(ceil(d));
e=(atan2(d,a))/(pow(a,e));
}
}