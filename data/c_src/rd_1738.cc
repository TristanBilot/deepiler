#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(a,d))+(log(f));
d=pow(e,a);
if(islessequal(a,c)){
a=fmin(a,e);
f=(pow(d,a))-(tan(g));
e=log(b);
}
else{
g=(fdim(f,g))*(asin(g));
g=(fmin(b,a))+(acos(b));
}
while(islessequal(e,g)){
c=asin(b);
d=(tan(d))+(fmin(a,g));
d=(exp(e))-(log10(c));
b=(fdim(f,e))-(fmin(g,g));
f=(pow(b,d))-(sqrt(d));
}
}