#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fdim(d,c);
d=(acos(f))+(floor(d));
e=(sin(b))*(log10(a));
c=asin(a);
f=(pow(c,b))/(sin(e));
if(islessequal(b,e)){
d=fdim(d,c);
f=pow(f,f);
a=fmin(e,e);
f=(floor(d))+(fmax(a,e));
}
else{
b=(fmin(a,c))*(log(d));
a=(pow(e,d))-(sqrt(b));
c=(pow(e,c))-(pow(e,a));
}
}