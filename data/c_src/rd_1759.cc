#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(e,c))*(atan2(c,d));
d=fmax(a,e);
e=(acos(a))+(fmin(b,d));
c=ceil(e);
while(islessequal(b,c)){
e=tan(a);
c=fmax(d,e);
d=sin(c);
e=tan(b);
}
e=(acos(b))-(fmax(b,a));
a=fdim(c,d);
}