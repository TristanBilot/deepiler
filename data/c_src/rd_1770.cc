#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(atan(e))+(fmin(g,b));
e=(atan2(b,a))/(asin(a));
b=(atan2(a,c))/(fmin(f,d));
a=log10(f);
c=atan2(d,a);
if(isless(g,f)){
a=(fmax(a,g))*(floor(b));
b=exp(d);
g=(tan(e))*(exp(c));
e=fdim(e,a);
f=atan2(b,a);
}
else{
b=(fmin(e,f))/(sin(g));
e=(pow(g,b))/(pow(c,f));
}
g=(fmax(a,e))*(fdim(b,c));
c=log10(g);
b=(log(b))+(pow(e,f));
}