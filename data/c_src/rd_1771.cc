#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(c,d))+(ceil(b));
e=ceil(h);
e=tan(e);
f=sin(a);
if(isgreaterequal(g,d)){
a=(log10(c))*(log10(c));
g=asin(h);
b=(pow(d,g))/(fmin(g,d));
g=(acos(d))/(floor(a));
d=(atan2(a,h))-(atan(d));
}
while(isless(e,c)){
e=(atan2(d,b))*(pow(e,f));
d=(floor(d))/(atan2(f,h));
d=(ceil(h))+(pow(d,h));
}
}