#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fdim(d,e);
d=(fdim(f,a))/(cos(h));
while(isgreaterequal(d,a)){
d=fdim(h,d);
c=pow(d,e);
e=(asin(h))-(cos(d));
}
while(islessgreater(b,a)){
g=(asin(g))-(fmax(e,b));
d=sin(f);
h=log(c);
}
}