#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(floor(g))-(fmax(h,d));
e=floor(h);
d=(atan2(e,g))*(atan2(e,g));
c=exp(d);
g=(sin(f))-(sqrt(a));
if(isgreaterequal(e,h)){
d=(log(f))*(cos(b));
d=atan2(a,f);
g=pow(e,a);
}
else{
e=(floor(h))*(log(b));
e=(fdim(a,e))/(pow(h,h));
a=floor(e);
}
e=sqrt(a);
a=(pow(d,g))+(log10(e));
h=(asin(c))/(sqrt(f));
e=asin(a);
}