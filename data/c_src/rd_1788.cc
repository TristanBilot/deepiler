#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(e);
f=(cos(c))+(asin(e));
e=(fmax(b,c))/(floor(b));
d=(sin(d))-(fmin(c,f));
c=(cos(e))+(sin(a));
if(islessgreater(e,b)){
a=acos(e);
a=(sqrt(b))*(atan(d));
f=(atan(e))+(pow(a,e));
e=(fmax(f,d))*(log10(d));
}
c=acos(f);
d=atan2(e,a);
}