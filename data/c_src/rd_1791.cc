#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(asin(c))*(atan2(d,e));
e=(fdim(b,c))+(sqrt(d));
f=(atan(b))/(acos(b));
f=log10(b);
if(islessequal(b,b)){
a=fdim(a,d);
c=(fmax(b,d))-(log(a));
a=log(a);
}
if(islessgreater(d,d)){
a=atan(c);
a=(asin(a))+(pow(e,f));
e=(acos(c))*(log10(d));
f=(cos(f))*(acos(c));
}
else{
d=fdim(c,b);
f=(fdim(a,b))+(pow(e,c));
b=(exp(a))-(log10(e));
c=pow(f,c);
e=(fmax(c,a))*(sqrt(c));
}
}