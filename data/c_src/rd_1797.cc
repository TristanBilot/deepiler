#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(g,d);
g=cos(c);
if(isless(a,f)){
h=(atan(h))/(fmin(g,g));
a=fmin(a,f);
}
f=(sqrt(g))+(fmin(c,b));
h=(fmin(a,a))/(ceil(b));
}