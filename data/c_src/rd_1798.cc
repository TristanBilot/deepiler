#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=tan(e);
e=log10(f);
d=(fmin(c,e))/(log10(e));
if(isless(c,c)){
b=exp(g);
d=fmin(c,c);
}
else{
e=pow(f,d);
b=fmax(d,e);
d=atan(d);
}
while(isless(f,b)){
e=(ceil(d))*(fmin(g,f));
b=ceil(e);
d=fdim(b,b);
b=(atan2(c,e))-(sqrt(c));
}
}