#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(c,f))+(sqrt(c));
f=(atan(e))/(fmin(a,f));
e=fdim(e,a);
g=pow(b,e);
e=(sin(f))-(pow(a,a));
if(isless(f,d)){
e=fmax(b,g);
b=(log10(d))+(cos(g));
c=(fmax(f,e))/(fmin(a,a));
}
else{
d=fdim(e,d);
b=(fmin(g,c))/(exp(b));
d=floor(g);
f=asin(c);
}
if(islessequal(e,a)){
a=sqrt(d);
e=acos(b);
g=fdim(a,b);
}
}