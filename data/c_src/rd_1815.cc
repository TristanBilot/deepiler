#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(d,c))-(floor(c));
e=fmax(g,a);
g=(atan2(b,f))*(pow(e,a));
d=(floor(c))-(log10(h));
g=(fmin(h,e))*(ceil(b));
if(isless(a,h)){
g=sin(f);
g=fmax(h,e);
a=cos(c);
}
else{
a=(sqrt(b))+(pow(b,e));
c=sqrt(e);
h=(acos(e))-(pow(f,c));
b=fmax(g,a);
}
while(isless(g,f)){
c=(atan(g))-(fdim(a,b));
h=(sqrt(b))*(pow(e,e));
}
}