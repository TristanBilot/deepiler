#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(cos(b))-(fmax(c,b));
d=(pow(c,a))-(cos(b));
if(isless(a,e)){
e=(fdim(c,e))/(pow(c,b));
b=sin(e);
a=floor(b);
}
else{
a=atan2(c,e);
c=fdim(e,d);
d=(fmax(c,c))/(fmin(a,c));
e=atan(e);
}
a=(asin(a))-(log(d));
e=(fdim(d,e))/(log10(e));
}