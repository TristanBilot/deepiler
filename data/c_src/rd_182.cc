#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(d,e))-(asin(f));
a=floor(f);
c=fmax(d,e);
b=pow(e,d);
e=(log(f))*(tan(c));
while(isless(d,d)){
d=asin(f);
f=(exp(e))*(fmin(d,f));
}
e=atan2(d,b);
d=fmax(e,b);
c=atan2(a,f);
}