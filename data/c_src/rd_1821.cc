#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(b,e);
c=(exp(c))+(fmin(b,d));
d=asin(c);
f=(sqrt(d))+(exp(g));
c=atan2(d,b);
c=cos(d);
f=ceil(c);
f=(pow(e,d))/(log10(b));
a=(pow(c,g))*(floor(f));
while(islessgreater(a,d)){
f=log(g);
e=(fmax(a,a))*(acos(f));
e=(exp(f))-(fmin(a,b));
b=sin(d);
}
}