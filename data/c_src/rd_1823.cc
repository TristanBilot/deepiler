#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(b))*(cos(b));
d=fmin(d,a);
d=(acos(d))+(exp(c));
d=(log(d))+(atan2(d,d));
a=tan(d);
a=atan(b);
d=fdim(b,d);
b=(ceil(e))*(fmin(a,c));
}