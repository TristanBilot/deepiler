#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=sin(a);
d=(log10(d))+(pow(e,d));
d=(floor(b))/(pow(c,e));
c=(cos(a))+(atan2(d,c));
a=fmin(d,c);
e=pow(c,b);
b=log(d);
d=(sqrt(b))+(fmax(c,d));
while(isless(b,b)){
e=(fmin(b,d))*(floor(d));
e=atan2(d,b);
b=fmin(c,d);
e=ceil(b);
}
}