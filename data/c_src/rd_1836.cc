#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,c))+(ceil(a));
a=(atan2(d,e))+(fmin(c,b));
d=(exp(e))+(atan2(c,a));
a=(log10(b))-(atan2(c,b));
b=(exp(b))+(exp(c));
b=sin(a);
c=atan(b);
b=asin(c);
while(islessgreater(b,d)){
c=(pow(a,c))*(ceil(b));
e=(exp(c))*(fdim(d,c));
d=atan(c);
b=pow(a,e);
e=floor(e);
}
}