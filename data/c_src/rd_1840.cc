#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan2(a,b))-(atan2(e,g));
e=pow(c,b);
c=(fmax(f,h))-(floor(d));
c=sqrt(g);
g=tan(f);
c=fdim(b,e);
d=(fdim(d,d))*(fmin(g,e));
b=(ceil(b))+(sqrt(c));
f=log10(h);
a=(exp(c))-(cos(a));
h=(atan(g))+(fdim(g,h));
d=(atan(e))/(sin(d));
}