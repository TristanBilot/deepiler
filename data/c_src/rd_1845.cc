#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=floor(a);
f=(fmin(b,a))-(fmin(b,g));
f=fmax(f,d);
g=exp(g);
g=(log(c))*(fmin(d,c));
c=(fmax(a,g))/(fmin(b,d));
f=(tan(d))*(acos(f));
d=ceil(e);
e=fmax(e,c);
e=(pow(d,e))+(fmax(f,c));
if(islessequal(c,b)){
f=sqrt(e);
g=(floor(d))*(log(b));
}
else{
b=(ceil(f))+(ceil(g));
a=(fmin(e,d))*(fmin(c,f));
f=(atan(a))-(atan(d));
}
}