#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(e,a))+(atan(d));
c=tan(c);
e=(fmax(b,d))/(pow(b,d));
a=atan2(c,b);
b=(atan2(a,d))*(fmin(d,d));
b=(atan2(c,b))/(sqrt(c));
b=fmin(d,a);
e=(floor(e))+(log10(d));
e=fdim(a,e);
a=(fmax(a,e))/(fdim(d,e));
a=(pow(d,e))+(pow(d,a));
}