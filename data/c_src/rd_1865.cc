#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(f,e);
b=tan(f);
e=fdim(f,e);
d=(atan2(c,a))-(log(b));
while(isgreaterequal(b,b)){
a=(tan(c))*(acos(e));
b=fmin(c,a);
a=pow(e,f);
}
if(isgreaterequal(c,c)){
b=(fdim(e,f))*(atan2(b,d));
c=pow(a,c);
e=sin(e);
}
else{
b=fdim(f,d);
a=(asin(a))*(pow(d,d));
c=sin(a);
e=log10(d);
f=(log(d))+(atan2(c,d));
}
}