#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(b);
c=(pow(a,b))/(atan(c));
b=atan(c);
d=log(e);
e=(fdim(d,a))/(floor(d));
a=ceil(e);
b=(atan2(c,e))+(sin(c));
if(isgreaterequal(d,a)){
a=sin(d);
d=pow(a,c);
a=fmin(d,a);
c=(sqrt(e))*(fdim(c,c));
}
else{
d=log10(b);
c=(acos(d))/(floor(a));
e=(sqrt(b))*(fdim(b,d));
}
}