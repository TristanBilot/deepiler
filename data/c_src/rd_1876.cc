#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmax(c,d))+(cos(f));
h=(fmax(f,e))-(cos(g));
b=(pow(e,a))/(fdim(e,b));
g=(cos(b))-(fmax(d,c));
if(islessgreater(f,f)){
e=(fdim(b,a))/(atan2(h,c));
h=(fmax(g,d))+(fmin(g,h));
f=fdim(a,f);
h=(fmin(c,h))*(fdim(a,e));
}
else{
h=(atan2(f,b))-(atan2(f,a));
b=floor(c);
b=pow(c,d);
}
f=ceil(h);
d=(log(e))*(floor(e));
e=pow(a,f);
a=sqrt(b);
d=(atan(g))*(tan(d));
}