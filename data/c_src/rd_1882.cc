#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
e=fdim(e,e);
f=(fmax(a,g))/(pow(h,f));
if(isless(b,a)){
g=(fdim(b,e))*(cos(f));
f=log10(c);
g=(tan(g))/(log10(d));
}
else{
g=fdim(g,f);
b=(log10(g))*(atan2(b,c));
c=(sqrt(g))*(atan2(d,e));
h=floor(d);
}
if(isgreaterequal(h,g)){
h=log(d);
a=(pow(d,d))*(sin(c));
c=fmin(e,d);
}
else{
e=(fmin(h,c))+(fdim(a,g));
c=fdim(a,f);
a=(fmin(b,e))+(log10(d));
f=(exp(d))+(exp(d));
d=cos(c);
}
}