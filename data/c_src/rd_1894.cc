#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(e,d);
e=cos(d);
a=(fmin(c,c))/(fdim(d,a));
a=(log10(a))*(log(e));
b=(ceil(e))*(pow(c,c));
e=(fmax(c,b))/(log10(e));
c=tan(c);
a=log(d);
while(isless(a,d)){
c=ceil(a);
a=(log(a))/(acos(b));
}
}