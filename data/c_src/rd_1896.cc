#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(sin(b))*(fdim(a,f));
h=pow(c,d);
if(isgreaterequal(e,e)){
f=(fmax(a,g))/(ceil(c));
f=fmax(h,e);
c=(fmax(a,b))-(acos(f));
a=(fdim(f,e))-(log(a));
}
f=(fmin(c,e))+(asin(g));
a=fdim(a,f);
}