#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(d,b))*(atan(d));
b=(acos(b))/(exp(a));
b=(sqrt(c))-(sin(d));
while(isless(e,d)){
b=fmax(d,c);
e=(tan(a))/(fmax(d,b));
d=cos(a);
a=atan2(c,a);
}
while(isgreaterequal(d,e)){
c=floor(c);
b=(sqrt(c))-(sqrt(a));
a=(fmin(a,b))-(pow(c,b));
a=(fmin(d,d))*(atan(f));
}
}