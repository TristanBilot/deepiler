#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=tan(c);
d=(atan2(c,a))/(pow(g,g));
c=tan(c);
a=(cos(f))*(log10(a));
g=(pow(a,e))-(ceil(a));
f=fmax(d,c);
c=(fmax(f,d))/(cos(b));
if(islessgreater(d,a)){
c=(fmax(b,d))-(atan2(b,b));
f=(tan(b))-(tan(d));
}
}