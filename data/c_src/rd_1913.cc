#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(b);
a=(tan(c))/(exp(d));
d=(pow(a,e))-(atan(e));
e=(fmin(e,c))+(asin(c));
a=fmax(e,b);
if(isgreaterequal(b,a)){
c=fdim(b,d);
d=(pow(c,d))-(floor(c));
c=(atan2(c,a))*(floor(d));
a=fmin(a,d);
d=ceil(b);
}
b=fdim(b,a);
c=log(d);
}