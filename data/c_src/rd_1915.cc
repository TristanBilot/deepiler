#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(c,b))*(log10(a));
e=pow(a,d);
b=log(b);
e=sin(a);
if(isless(e,a)){
a=ceil(c);
e=acos(e);
e=(log(a))+(cos(b));
d=(atan2(a,a))*(fmin(e,a));
}
else{
d=(asin(e))*(fmax(c,c));
a=(fmin(a,e))*(exp(c));
c=fmin(a,e);
e=fmax(c,e);
a=(atan2(d,e))*(fmax(c,c));
}
if(isless(b,c)){
b=atan2(e,a);
a=(asin(d))-(fdim(d,d));
d=(exp(b))*(atan(b));
}
}