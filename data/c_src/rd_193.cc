#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(c,e))*(ceil(e));
a=pow(b,g);
b=(atan2(g,a))+(acos(a));
while(isless(a,f)){
b=(atan2(b,d))/(pow(g,c));
c=ceil(b);
d=(floor(g))/(fmax(c,f));
f=fdim(f,a);
}
if(islessgreater(g,a)){
e=fmin(a,c);
f=atan2(d,d);
g=(fmin(b,d))*(fmax(e,b));
}
}