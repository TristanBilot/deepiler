#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan2(g,g))*(atan(e));
e=tan(f);
e=floor(f);
while(islessequal(d,d)){
a=fmax(g,d);
f=fmax(d,d);
c=(pow(d,f))-(tan(f));
b=(atan(a))*(fdim(a,f));
f=tan(a);
}
if(islessgreater(c,f)){
a=fdim(c,b);
b=(atan2(d,e))/(sqrt(g));
c=atan2(c,g);
a=fmax(c,b);
}
else{
a=fmin(g,e);
f=log(g);
}
}