#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(b,a))/(log(d));
d=atan2(c,e);
d=atan2(c,a);
c=sin(b);
b=(ceil(c))*(ceil(d));
b=(cos(f))/(pow(a,c));
e=atan(a);
if(islessequal(a,e)){
b=(fdim(c,b))+(acos(d));
a=(pow(a,a))*(sin(e));
}
}