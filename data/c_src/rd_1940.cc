#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan2(c,e);
c=cos(a);
b=atan2(c,b);
while(islessgreater(b,f)){
f=pow(e,g);
d=ceil(c);
e=exp(f);
g=(atan2(d,a))+(atan2(g,d));
g=asin(d);
}
e=(log10(g))-(tan(e));
d=(fmin(b,d))/(atan(b));
b=(pow(g,f))-(log10(f));
}