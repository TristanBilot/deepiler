#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(d,a))+(fdim(e,c));
a=atan2(a,e);
d=(pow(a,c))+(ceil(e));
d=log10(c);
while(isgreaterequal(b,c)){
c=(acos(a))*(asin(a));
e=floor(b);
c=fmax(b,b);
}
if(isless(a,b)){
c=fmax(b,d);
c=fmin(b,e);
}
}