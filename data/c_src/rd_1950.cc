#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sin(c))-(floor(a));
c=(acos(b))-(fmax(a,e));
e=(fdim(c,a))-(log(b));
d=log10(b);
while(isgreaterequal(a,d)){
d=(fmax(a,e))+(fdim(d,e));
e=(tan(b))*(fmin(c,e));
a=(floor(e))+(asin(a));
}
while(isless(a,e)){
a=sin(b);
c=sin(b);
d=fdim(e,e);
d=sin(c);
e=fmin(c,c);
}
}