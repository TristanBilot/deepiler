#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,a);
e=(fmin(b,c))/(pow(d,d));
e=atan2(c,a);
if(isgreaterequal(b,c)){
c=(fmax(a,b))/(ceil(b));
b=fmin(c,b);
}
else{
c=sin(c);
c=(cos(d))/(sqrt(a));
e=log10(e);
c=exp(b);
e=(pow(e,e))+(tan(a));
}
e=tan(d);
d=(pow(a,a))+(fmax(b,e));
d=(log10(b))+(log10(b));
a=(fmin(e,c))-(fmax(c,b));
}