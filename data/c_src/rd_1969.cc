#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(d,a))-(atan(b));
d=(fmin(b,b))+(fdim(d,d));
d=fmax(d,e);
a=(fmin(c,c))+(asin(e));
d=(fmax(a,d))+(acos(b));
e=(asin(c))+(atan(a));
d=fmin(b,a);
while(isless(c,a)){
e=fmin(a,b);
e=fmax(e,d);
}
}