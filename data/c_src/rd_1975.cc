#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=floor(a);
a=(ceil(a))-(log(a));
if(islessgreater(c,b)){
a=(atan(c))/(ceil(c));
e=atan(c);
}
else{
e=(exp(a))-(atan2(b,a));
c=(fdim(e,a))+(fdim(e,e));
b=fdim(d,b);
e=fmax(a,e);
e=pow(d,a);
}
while(isless(e,e)){
d=(asin(b))/(ceil(d));
e=atan2(c,d);
d=asin(e);
}
}