#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(asin(e))-(fdim(c,e));
h=sqrt(d);
b=(pow(e,e))-(fdim(b,c));
f=log(g);
if(islessgreater(b,a)){
c=exp(g);
g=sqrt(h);
h=pow(d,g);
h=(floor(c))*(exp(h));
}
c=(log10(a))-(floor(f));
d=exp(d);
}