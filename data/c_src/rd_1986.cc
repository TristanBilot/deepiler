#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(c,c);
d=fdim(a,a);
c=(fdim(d,a))/(floor(c));
d=(atan2(a,d))*(atan2(e,c));
b=(ceil(b))+(pow(a,b));
d=fdim(b,b);
c=tan(d);
}