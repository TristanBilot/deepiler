#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(g,c);
c=(log(a))*(tan(e));
f=ceil(f);
while(isgreaterequal(a,f)){
a=fmax(c,b);
b=(pow(a,d))*(fmax(g,a));
}
if(islessgreater(a,e)){
c=fmax(d,b);
f=log10(g);
a=floor(g);
d=(fmax(d,c))-(log10(g));
c=(fdim(a,c))/(fdim(b,e));
}
else{
c=(fmax(b,d))*(sqrt(d));
g=atan(e);
e=(fdim(f,c))/(tan(f));
e=atan(b);
}
}