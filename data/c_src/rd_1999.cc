#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(tan(b))-(fmax(b,c));
a=fdim(c,e);
c=fdim(c,c);
if(islessequal(c,a)){
e=(tan(d))+(sin(c));
c=(log10(a))/(tan(a));
b=(atan2(b,a))-(pow(a,c));
e=fmax(b,d);
a=sqrt(c);
}
else{
a=(atan2(a,a))*(fmin(e,b));
e=acos(a);
d=(fdim(c,a))+(fmin(e,b));
a=pow(a,d);
a=(fmin(d,e))+(atan2(a,b));
}
d=pow(d,d);
c=floor(a);
c=(asin(e))/(ceil(d));
}