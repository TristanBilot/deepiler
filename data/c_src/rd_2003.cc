#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,b);
b=floor(c);
d=cos(a);
h=(fmax(d,h))*(log(f));
e=acos(a);
while(islessequal(a,c)){
c=(log(c))*(fmin(f,c));
d=(fmax(b,h))/(fmax(a,b));
h=fmax(h,g);
}
}