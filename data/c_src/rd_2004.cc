#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(b))-(pow(c,a));
b=exp(e);
e=fmin(c,a);
d=ceil(c);
c=fdim(d,c);
while(islessgreater(d,b)){
d=floor(b);
b=(fmin(b,c))-(fmax(a,e));
c=log(b);
e=ceil(a);
}
if(isgreaterequal(c,c)){
c=(asin(a))*(atan2(d,d));
d=(pow(b,e))/(fmax(c,c));
a=(cos(c))*(exp(d));
d=atan2(a,a);
d=asin(a);
}
else{
c=(floor(d))-(sqrt(e));
c=fmax(a,c);
a=(fmax(d,d))*(sqrt(a));
c=(ceil(b))+(asin(c));
}
}