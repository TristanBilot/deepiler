#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,c))-(fmax(b,d));
d=(cos(d))*(fmax(c,b));
c=fmin(b,c);
e=(fmin(c,e))/(ceil(a));
d=log(d);
while(islessgreater(b,e)){
e=(sqrt(e))*(atan(e));
a=(exp(b))/(atan2(e,a));
a=(pow(e,e))-(fmax(e,c));
d=(log(e))/(atan2(a,a));
}
if(isgreaterequal(e,b)){
e=atan2(e,b);
c=(ceil(b))*(acos(a));
e=ceil(b);
e=sqrt(e);
}
else{
a=(floor(b))*(fmin(a,a));
b=(floor(e))*(acos(d));
}
}