#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=ceil(a);
g=ceil(a);
e=(tan(g))/(fdim(f,a));
g=ceil(c);
while(islessgreater(d,g)){
e=fdim(g,b);
b=(fdim(c,a))*(exp(a));
a=(ceil(a))-(cos(d));
}
while(islessequal(g,e)){
b=sin(c);
g=(cos(e))/(sqrt(a));
}
}