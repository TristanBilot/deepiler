#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=exp(b);
a=(floor(f))-(atan2(a,f));
while(isless(f,f)){
g=(atan2(g,b))+(pow(e,a));
d=atan(b);
}
c=(atan(e))*(fdim(e,d));
d=sqrt(e);
c=(fmin(g,d))*(log10(f));
}