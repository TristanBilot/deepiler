#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(cos(g))*(fmin(e,a));
f=(pow(e,g))-(tan(f));
g=fmax(b,a);
while(isless(a,f)){
f=(atan2(e,c))*(asin(b));
e=pow(g,f);
}
if(isgreaterequal(b,f)){
f=atan(d);
g=fmax(d,f);
}
else{
d=cos(g);
c=(acos(d))-(sqrt(d));
}
}