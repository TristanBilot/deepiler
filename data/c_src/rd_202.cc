#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=ceil(b);
c=fmin(e,e);
e=floor(e);
while(isgreaterequal(a,e)){
a=(asin(b))/(tan(c));
b=(acos(e))-(atan2(b,e));
b=pow(e,b);
e=(fdim(a,d))+(pow(d,d));
}
if(isgreaterequal(c,c)){
d=fdim(b,a);
b=(fmax(a,d))/(atan2(d,c));
}
else{
c=(pow(c,b))+(fdim(e,d));
b=(fmax(b,a))*(tan(a));
a=atan2(c,e);
b=(fmin(b,e))+(fdim(a,e));
b=(atan2(c,b))+(pow(c,e));
}
}