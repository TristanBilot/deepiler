#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,g);
h=sin(c);
d=sin(h);
f=pow(a,e);
h=pow(g,h);
if(isless(e,f)){
c=log(e);
g=acos(c);
}
if(islessequal(b,g)){
e=fmax(a,f);
c=fmin(h,g);
a=fdim(c,f);
}
else{
b=(pow(h,h))+(fmax(d,g));
a=log(g);
e=fmin(c,d);
}
}