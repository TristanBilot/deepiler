#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(ceil(b))*(pow(b,b));
d=(tan(c))*(tan(d));
d=fdim(a,e);
d=exp(d);
while(isgreaterequal(d,c)){
d=fmin(c,c);
a=(asin(e))+(exp(c));
a=(log10(e))*(sin(a));
}
while(islessequal(c,d)){
b=(acos(d))/(floor(a));
c=exp(e);
a=pow(a,a);
c=fdim(b,a);
d=log(c);
}
}