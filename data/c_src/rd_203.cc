#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(b,d))+(pow(d,f));
e=(atan2(f,c))-(pow(c,f));
a=pow(c,d);
f=(atan2(c,b))-(log10(d));
if(isgreaterequal(c,d)){
f=fmin(f,c);
a=acos(c);
}
while(islessequal(e,a)){
d=log10(b);
b=(fmax(d,a))*(fmax(d,e));
f=(exp(e))/(fmin(f,a));
}
}