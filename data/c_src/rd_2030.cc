#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(f,h))*(tan(d));
g=(fdim(e,c))+(tan(a));
h=fmin(d,g);
f=atan2(h,f);
a=(fmin(f,a))-(fdim(c,d));
e=sqrt(f);
e=(fmax(f,g))-(sin(g));
g=(atan2(g,a))-(pow(f,a));
d=fmin(g,e);
if(isless(a,a)){
g=(atan2(a,b))-(atan(h));
a=(fdim(a,d))/(cos(g));
g=(log10(g))-(sin(a));
}
}