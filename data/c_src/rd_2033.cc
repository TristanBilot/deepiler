#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(e);
f=(atan(d))-(fdim(a,c));
b=log(e);
f=(floor(f))-(fmax(b,c));
a=fmin(c,e);
if(islessequal(d,b)){
c=(tan(h))*(asin(d));
g=fmax(f,g);
g=(atan2(f,h))+(fdim(b,c));
c=(fmin(c,h))*(atan2(g,c));
}
else{
f=log10(e);
e=fmin(a,a);
}
while(islessequal(b,e)){
f=log10(e);
a=(atan2(c,g))+(fdim(c,g));
g=(fmax(c,d))-(ceil(d));
d=(fmin(b,a))/(fmax(h,d));
f=fdim(d,a);
}
}