#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(e,c);
a=(ceil(f))+(fdim(a,f));
if(islessgreater(e,d)){
b=tan(e);
e=fdim(f,b);
d=fmax(c,c);
b=exp(f);
}
if(islessgreater(d,f)){
c=(fmax(a,a))/(sin(c));
a=fmax(a,d);
b=atan(e);
c=cos(b);
}
}