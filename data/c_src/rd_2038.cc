#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(e,e);
c=asin(d);
a=fmin(c,c);
while(islessgreater(e,e)){
d=(tan(a))*(sin(c));
e=(fmax(d,d))/(tan(d));
}
if(islessgreater(c,d)){
e=(fdim(d,b))-(fmax(e,a));
b=fmax(b,a);
b=atan2(e,a);
c=log10(d);
e=(fmax(d,a))-(fdim(c,a));
}
else{
d=floor(b);
e=(fdim(e,a))-(atan2(d,d));
d=(fmin(a,c))-(atan(c));
d=(fdim(e,b))/(atan2(d,a));
b=(pow(e,a))/(sin(d));
}
}