#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(b,g))+(tan(c));
c=fmin(f,a);
b=cos(d);
if(isless(a,a)){
b=pow(c,c);
e=(atan(b))/(log10(a));
c=cos(d);
d=(acos(e))+(ceil(b));
}
if(isgreaterequal(g,e)){
d=ceil(h);
a=(ceil(a))+(ceil(f));
g=tan(g);
f=exp(e);
}
}