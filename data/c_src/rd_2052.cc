#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(e,a);
f=(tan(e))*(pow(d,b));
e=(atan(f))*(asin(e));
while(islessgreater(d,b)){
a=(log(c))*(asin(e));
c=fdim(d,d);
}
while(islessgreater(e,b)){
c=pow(c,d);
e=log10(d);
f=log10(e);
e=(fdim(c,e))*(asin(c));
b=atan2(b,a);
}
}