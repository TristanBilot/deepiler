#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=cos(c);
f=fdim(e,a);
b=exp(a);
a=(log10(d))+(fmin(e,f));
f=pow(f,b);
if(islessgreater(e,a)){
b=(pow(a,a))/(pow(d,b));
b=(log10(f))-(cos(c));
}
else{
d=fdim(f,a);
d=log10(a);
c=pow(a,d);
c=(sqrt(f))*(sqrt(e));
}
f=(sin(b))-(fmin(c,f));
c=pow(f,a);
}