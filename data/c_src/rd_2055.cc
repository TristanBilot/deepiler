#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=ceil(b);
b=fmax(d,e);
f=atan2(c,d);
e=sqrt(d);
e=(floor(d))-(fmax(d,f));
c=sin(e);
a=fdim(b,c);
while(isless(e,b)){
d=exp(f);
b=(acos(c))+(fdim(c,e));
}
}