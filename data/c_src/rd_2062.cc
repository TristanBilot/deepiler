#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(asin(e))/(fmin(c,a));
c=(pow(c,e))*(pow(d,b));
a=(exp(e))*(exp(c));
while(isless(a,c)){
a=sin(b);
c=atan2(c,a);
}
while(isgreaterequal(a,a)){
a=(fdim(c,c))/(fmin(d,b));
b=atan(c);
}
}