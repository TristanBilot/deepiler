#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(b,c);
d=atan(c);
e=(fmax(a,b))*(asin(a));
while(isless(b,e)){
c=(pow(d,c))/(fmin(b,a));
b=fmax(b,e);
d=(fmin(c,c))/(tan(a));
b=(fmin(d,c))*(asin(a));
d=fmin(e,e);
}
a=(fmin(d,e))-(asin(d));
d=asin(a);
e=acos(d);
}