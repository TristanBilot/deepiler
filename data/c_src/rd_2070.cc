#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(d,e);
e=(fmax(d,a))/(atan2(d,e));
c=atan(g);
a=log10(b);
while(isless(f,d)){
g=fmin(e,g);
c=sin(f);
d=fmin(c,b);
}
if(islessequal(a,e)){
f=(fmax(d,a))+(cos(c));
a=fdim(a,d);
d=(pow(f,e))-(pow(d,a));
f=atan(e);
g=(sin(f))-(fmin(f,e));
}
}