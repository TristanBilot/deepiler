#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(floor(e))/(fmax(f,c));
f=(pow(a,b))-(exp(f));
e=sin(c);
if(isgreaterequal(a,b)){
b=fmin(a,e);
c=(exp(g))+(sin(d));
f=fmin(d,c);
b=(fmax(b,g))*(atan(a));
e=sqrt(f);
}
if(isless(d,e)){
d=pow(e,e);
e=(fdim(b,c))*(pow(e,c));
d=(fdim(b,a))/(fmin(a,f));
g=log10(a);
b=fmax(e,e);
}
}