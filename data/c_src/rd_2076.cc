#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(a,g);
b=(exp(c))/(atan2(f,e));
e=fmin(a,g);
e=(log10(e))/(exp(h));
h=(exp(g))-(pow(d,e));
if(isgreaterequal(c,h)){
g=fmax(a,b);
h=exp(f);
h=(atan2(c,c))-(ceil(h));
g=(fdim(c,f))*(atan2(d,e));
f=fdim(c,c);
}
b=log(d);
h=(pow(f,h))+(fdim(d,b));
e=(fdim(d,e))/(cos(g));
b=(fdim(h,h))-(pow(e,g));
}