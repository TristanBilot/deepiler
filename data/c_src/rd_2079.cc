#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(exp(c))*(fmin(b,g));
b=tan(f);
if(isless(g,e)){
d=fdim(e,a);
c=acos(b);
d=fdim(h,h);
g=atan(c);
h=atan2(d,h);
}
else{
g=atan2(c,d);
a=(atan(d))+(sqrt(f));
b=fdim(f,c);
b=log10(g);
}
e=fmin(g,e);
f=(fdim(a,e))/(exp(b));
h=asin(a);
}