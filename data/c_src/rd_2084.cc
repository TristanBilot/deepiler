#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(d,g);
c=(fmin(d,b))*(pow(c,b));
d=(fdim(c,e))+(fmin(d,c));
b=fdim(e,c);
if(isless(g,g)){
e=exp(e);
g=fmin(a,b);
c=(atan2(c,a))+(fmin(b,a));
g=(exp(g))*(cos(a));
g=fmin(a,f);
}
else{
g=(log10(b))-(log(e));
g=(fdim(e,d))/(floor(d));
}
g=(asin(f))*(sqrt(e));
a=fmin(g,g);
a=(pow(d,f))+(tan(b));
c=floor(d);
}