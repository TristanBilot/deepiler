#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan(g);
c=(pow(b,d))/(floor(b));
e=fmax(f,b);
a=fmax(c,g);
while(isgreaterequal(f,b)){
a=(fmax(a,c))/(atan(e));
c=(atan2(d,g))*(pow(d,a));
}
e=(fmax(c,c))+(exp(b));
b=(log(e))+(asin(g));
}