#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(floor(a))-(fmax(b,g));
h=(log(h))-(fmin(d,h));
c=(sin(g))/(fmax(b,d));
if(isless(c,a)){
e=fmin(b,g);
b=ceil(h);
d=(sin(b))/(fdim(c,g));
g=(pow(b,b))/(exp(d));
}
else{
h=atan2(e,c);
e=log10(d);
b=(pow(d,d))-(atan(h));
f=fmax(g,a);
h=(fmax(h,h))+(fmin(b,f));
}
g=exp(g);
g=(sqrt(b))/(log10(f));
d=atan(g);
g=ceil(b);
}