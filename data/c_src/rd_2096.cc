#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(cos(e))+(fdim(d,f));
e=(fmax(a,b))*(pow(h,g));
f=(fmax(e,b))-(log(a));
g=fdim(e,c);
while(islessequal(g,e)){
b=fmin(g,f);
b=atan2(a,a);
g=asin(c);
}
if(isless(a,c)){
f=(fmax(d,b))*(fmax(e,b));
c=exp(e);
b=(sqrt(f))+(log10(a));
}
}