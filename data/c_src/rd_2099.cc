#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(c,d);
b=(log(g))-(fdim(f,a));
h=ceil(h);
c=acos(f);
b=(pow(c,e))*(asin(g));
if(islessgreater(b,h)){
d=pow(a,f);
a=cos(d);
}
else{
f=(fmax(g,d))-(fmax(g,a));
g=(fdim(f,g))/(atan(c));
}
if(islessgreater(e,g)){
d=fdim(g,a);
g=(fmax(g,c))/(fmin(b,e));
}
else{
d=(exp(g))+(fmin(e,f));
g=tan(b);
}
}