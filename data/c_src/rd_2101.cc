#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=sin(c);
d=(log(e))-(atan2(a,a));
e=exp(c);
if(isless(e,b)){
b=fmin(b,c);
b=floor(b);
}
else{
a=floor(e);
d=(ceil(c))-(ceil(e));
}
if(isless(c,c)){
a=(pow(c,e))*(fmax(c,c));
d=(pow(a,d))-(fmin(a,e));
}
}