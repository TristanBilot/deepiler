#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan(b))+(sin(b));
c=(exp(f))*(fmin(g,b));
f=(sqrt(g))*(fmin(a,d));
d=fdim(c,b);
b=(log(e))/(pow(c,c));
if(islessequal(b,e)){
d=atan2(g,b);
a=(atan(c))+(atan2(f,c));
e=fdim(a,b);
}
d=(atan2(e,f))/(asin(d));
f=(fmin(b,e))*(pow(e,d));
g=(log(c))/(log10(e));
d=(fdim(g,g))/(fmax(e,b));
}