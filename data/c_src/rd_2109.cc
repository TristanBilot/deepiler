#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(e,g);
h=(fdim(f,g))*(tan(c));
e=(pow(b,d))/(fmax(b,f));
b=(fdim(e,b))*(fmin(a,c));
if(isgreaterequal(a,c)){
g=(sqrt(g))+(fmin(h,d));
c=(atan(a))-(ceil(a));
d=atan(d);
}
h=floor(c);
h=(fdim(h,h))*(sqrt(f));
g=(atan(c))/(fmax(g,b));
h=(pow(d,a))*(atan(b));
b=(log10(c))/(fdim(g,h));
}