#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(c))-(fdim(e,b));
b=floor(d);
d=(ceil(c))*(log(b));
a=sqrt(e);
c=(log(e))*(cos(a));
e=exp(d);
e=sqrt(c);
while(isgreaterequal(a,e)){
d=(fdim(d,b))/(fmax(d,b));
a=log10(b);
c=(fmin(e,b))+(floor(d));
}
}