#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(b);
e=(fmin(a,a))+(cos(b));
d=pow(a,e);
e=(sqrt(e))+(fmax(b,a));
c=(fdim(e,e))*(atan2(c,a));
while(islessequal(c,c)){
e=acos(d);
b=(log(e))+(fmin(d,a));
c=(pow(b,c))-(atan2(a,b));
}
if(islessgreater(d,a)){
c=pow(e,e);
b=(sqrt(e))-(fdim(e,a));
d=floor(c);
a=fmax(a,a);
c=(floor(d))/(fmax(a,d));
}
}