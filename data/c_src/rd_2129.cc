#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(asin(g))/(cos(d));
g=(fdim(a,c))/(log10(f));
f=pow(g,b);
while(isless(c,g)){
f=(atan2(a,f))-(pow(f,f));
g=fdim(e,b);
a=(fdim(g,c))*(fdim(c,d));
a=(fmin(e,e))-(acos(a));
}
if(islessequal(a,e)){
d=asin(d);
e=pow(e,b);
d=ceil(e);
c=pow(c,c);
b=fdim(d,c);
}
}