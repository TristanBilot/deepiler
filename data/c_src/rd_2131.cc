#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(h,c);
c=tan(g);
f=asin(a);
g=(log10(b))-(asin(e));
if(islessequal(b,f)){
d=(asin(h))-(pow(a,e));
b=(exp(b))*(fdim(a,c));
a=tan(h);
f=fmax(e,e);
a=(fdim(c,d))-(atan(e));
}
else{
d=(log10(b))-(sin(f));
b=exp(h);
c=sin(d);
e=ceil(b);
}
while(isless(e,h)){
d=pow(h,g);
b=floor(a);
e=(atan(h))-(fdim(f,h));
g=pow(d,f);
}
}