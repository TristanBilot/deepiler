#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(tan(g))-(fdim(e,a));
a=(log(f))+(floor(c));
e=(pow(g,b))+(exp(b));
e=pow(b,b);
d=sqrt(d);
g=tan(c);
a=asin(c);
g=(asin(e))/(exp(f));
f=pow(c,a);
f=(asin(a))/(acos(c));
}