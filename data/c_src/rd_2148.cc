#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(a,f);
f=(acos(e))/(atan2(c,e));
d=fmax(a,g);
c=atan2(e,c);
if(islessequal(c,f)){
e=(floor(b))*(sin(a));
g=cos(b);
d=(fmin(e,g))*(floor(a));
g=tan(a);
e=asin(f);
}
else{
e=ceil(a);
e=floor(g);
}
while(isless(d,d)){
f=(ceil(a))-(atan2(f,g));
d=(asin(c))*(fdim(d,c));
c=(atan(a))-(pow(d,a));
d=cos(c);
c=(exp(b))+(pow(c,c));
}
}