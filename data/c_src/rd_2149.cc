#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan2(c,c);
e=(fmax(d,d))/(pow(d,b));
a=(acos(c))-(pow(e,d));
if(isgreaterequal(c,b)){
a=(log(e))-(fmax(f,e));
c=(log(a))+(fmax(b,f));
f=asin(f);
g=(pow(c,b))+(log10(e));
g=exp(g);
}
a=(fmax(f,d))/(pow(f,c));
d=(exp(e))-(fmin(d,f));
}