#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmin(d,e);
c=asin(b);
b=sin(b);
b=log10(c);
while(islessgreater(e,a)){
c=(pow(a,f))*(pow(a,a));
b=(fdim(f,c))/(sin(c));
a=(atan(c))/(fmax(e,c));
b=fmax(b,c);
}
while(islessequal(f,b)){
d=atan2(f,d);
a=(atan(b))*(atan(f));
c=(fmax(f,d))+(log(e));
a=fdim(d,b);
b=(pow(c,d))-(log10(d));
}
}