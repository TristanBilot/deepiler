#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=log(a);
f=(fdim(e,e))/(fmin(f,b));
d=(floor(b))*(sin(b));
c=(acos(a))-(pow(d,b));
if(islessgreater(f,d)){
e=(atan2(f,a))/(atan2(b,f));
e=(asin(e))*(fmax(e,d));
b=acos(c);
}
else{
c=(fmin(a,e))+(cos(d));
b=sin(a);
f=(fdim(e,b))*(atan2(f,c));
e=sin(e);
f=fmin(b,c);
}
while(islessgreater(a,f)){
c=fmax(a,e);
e=(tan(e))/(tan(e));
d=(asin(f))/(sin(a));
a=(asin(e))*(fmin(d,c));
}
}