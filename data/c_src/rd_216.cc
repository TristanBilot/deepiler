#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(e,f))+(tan(d));
d=floor(c);
a=fmax(f,e);
a=(asin(f))/(ceil(b));
c=fdim(g,a);
g=fmax(d,c);
f=pow(b,f);
}