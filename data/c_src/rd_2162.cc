#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan2(c,f))/(exp(d));
g=(fdim(f,f))-(log(e));
c=(fdim(f,g))/(asin(e));
if(isless(a,c)){
c=(fmax(a,b))*(fdim(g,f));
b=fmax(h,g);
d=(fmax(g,h))/(pow(b,f));
e=atan2(c,g);
}
if(islessequal(b,d)){
g=(fdim(a,h))+(cos(a));
f=fmin(f,b);
a=(pow(f,e))+(fmax(h,h));
d=(fmax(b,f))-(fdim(d,d));
}
}