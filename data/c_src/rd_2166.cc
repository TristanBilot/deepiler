#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=sqrt(a);
f=cos(b);
f=(floor(d))-(fmax(b,f));
d=atan(e);
if(islessequal(c,b)){
d=log(f);
a=log10(b);
}
while(isgreaterequal(f,e)){
c=(acos(d))-(log10(b));
d=(acos(d))/(acos(f));
f=(fmax(c,d))/(atan(d));
f=cos(c);
f=cos(a);
}
}