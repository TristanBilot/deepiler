#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(b,f);
b=atan2(b,c);
b=(cos(d))*(fmin(f,c));
while(islessequal(d,a)){
b=(fmax(a,e))+(atan2(c,b));
f=fmax(b,e);
a=ceil(d);
e=(exp(f))+(pow(b,b));
}
b=(log10(c))-(acos(a));
d=(pow(d,e))+(fmin(d,d));
}