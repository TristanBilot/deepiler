#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(c,e))/(log(d));
b=atan(d);
c=(fmin(e,c))/(fmax(a,b));
c=acos(e);
e=(acos(e))-(exp(e));
d=(fmax(b,d))-(sin(d));
e=fdim(d,a);
b=(atan2(e,c))-(atan2(a,d));
c=fmax(e,c);
d=(asin(d))/(pow(c,a));
}