#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(e,b))+(fdim(a,f));
f=(fmax(c,e))+(sin(a));
f=floor(h);
g=(fdim(f,a))-(sqrt(d));
h=tan(g);
g=(fdim(g,h))*(ceil(d));
e=acos(g);
}