#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(c,c))/(fdim(c,e));
c=(fmax(e,c))*(asin(d));
a=(atan2(a,b))-(sqrt(d));
if(islessgreater(a,c)){
e=(exp(a))/(sin(c));
c=(asin(e))+(fmax(c,b));
}
else{
e=(pow(a,c))*(fmin(e,b));
d=tan(a);
}
while(isgreaterequal(a,a)){
b=fmin(a,b);
a=fmax(b,c);
b=floor(c);
c=(acos(d))-(fmax(d,b));
d=fmin(b,b);
}
}