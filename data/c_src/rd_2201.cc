#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(e);
e=(fmin(d,d))*(atan(d));
e=(asin(c))-(fmax(b,b));
e=(floor(b))*(pow(c,e));
a=(sin(d))/(pow(a,c));
b=(ceil(e))+(fmin(a,e));
e=atan(c);
b=(fmax(d,d))/(fmax(d,c));
d=fmax(c,a);
e=sqrt(c);
}