#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log10(a);
c=(floor(b))-(exp(a));
b=atan2(f,a);
if(islessgreater(f,c)){
f=fmin(b,b);
c=acos(a);
}
f=(atan2(f,f))/(fdim(d,b));
f=atan2(b,a);
f=(exp(f))*(fmax(e,a));
b=exp(a);
}