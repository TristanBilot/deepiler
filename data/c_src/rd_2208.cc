#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(c);
c=log(e);
g=atan2(f,e);
d=(atan(b))*(fdim(d,a));
e=(log10(d))/(fmax(c,e));
g=(atan2(h,c))/(log(g));
h=(fmin(f,f))-(floor(d));
c=(log10(d))/(atan2(b,f));
if(islessequal(g,g)){
d=fdim(c,g);
c=sqrt(a);
a=sin(g);
}
}