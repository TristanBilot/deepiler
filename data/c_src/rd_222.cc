#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(exp(b))/(fmin(d,c));
d=(acos(c))+(fmin(c,d));
e=(fmax(a,f))+(fdim(f,b));
f=(atan2(c,b))-(sqrt(f));
if(isless(a,d)){
c=atan2(f,f);
d=atan2(c,c);
b=atan(f);
}
else{
a=atan2(e,e);
a=(exp(c))/(sqrt(f));
c=(fdim(b,f))+(fmax(d,b));
}
if(islessgreater(e,f)){
f=fmin(d,d);
c=atan2(c,d);
f=fmax(a,f);
}
}