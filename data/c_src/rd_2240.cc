#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan2(g,c))+(atan2(e,e));
a=(pow(c,b))*(fmax(e,e));
g=(cos(d))+(tan(b));
g=atan2(g,c);
a=ceil(f);
if(isless(g,g)){
f=pow(g,g);
a=pow(g,a);
a=exp(g);
f=cos(a);
}
else{
b=(fmin(f,f))*(ceil(b));
e=(pow(a,f))*(fdim(e,a));
}
}