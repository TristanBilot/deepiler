#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(floor(d))/(ceil(a));
e=pow(c,a);
c=(pow(e,c))+(tan(g));
g=(atan2(e,d))+(atan2(d,e));
if(islessequal(g,b)){
e=(fmin(a,f))+(pow(a,d));
d=log(f);
}
else{
b=(log(b))-(sin(f));
c=(atan(b))*(fdim(a,a));
d=cos(f);
}
if(isgreaterequal(e,a)){
c=(pow(e,e))/(log(e));
a=(tan(e))+(exp(d));
g=sin(g);
c=(fmax(e,d))+(tan(f));
}
}