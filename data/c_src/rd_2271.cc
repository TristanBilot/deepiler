#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(d,e))*(fmin(d,d));
a=(cos(d))+(log(e));
if(islessequal(e,c)){
b=fdim(b,e);
a=fdim(c,d);
e=fdim(d,e);
c=ceil(d);
}
if(islessgreater(e,a)){
b=(tan(d))/(ceil(b));
d=(floor(a))*(fmax(a,a));
c=(floor(e))*(sqrt(e));
e=fmin(e,a);
c=acos(a);
}
else{
d=fdim(a,d);
a=fdim(d,e);
a=ceil(a);
d=fmax(b,c);
}
}