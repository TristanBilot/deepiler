#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(floor(c))-(log10(b));
c=fmin(d,b);
e=(fdim(d,b))/(log10(b));
d=(sqrt(g))+(tan(g));
while(islessequal(e,c)){
h=asin(e);
e=(exp(f))+(atan2(a,a));
}
if(isgreaterequal(d,e)){
h=atan2(b,e);
d=acos(g);
f=(fmax(b,g))/(tan(b));
}
}