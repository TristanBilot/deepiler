#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(tan(g))-(cos(e));
f=(fmax(a,h))+(asin(b));
a=ceil(h);
if(isless(a,h)){
b=(fmin(g,g))/(fmin(c,f));
g=(pow(c,h))-(pow(h,c));
e=(fmin(g,c))*(fmin(e,d));
d=asin(g);
}
if(islessequal(e,h)){
e=(fdim(a,b))+(fdim(f,a));
f=asin(a);
}
}