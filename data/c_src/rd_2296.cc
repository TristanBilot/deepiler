#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(a,e))*(log(c));
c=(atan2(b,a))/(fmax(d,c));
d=(floor(d))-(exp(b));
if(isgreaterequal(b,b)){
d=fmin(c,d);
e=atan2(c,c);
c=pow(d,b);
d=log10(a);
}
else{
c=(atan2(e,e))*(pow(e,e));
b=atan(d);
e=(fmax(e,e))-(log10(a));
}
d=(atan2(a,a))*(fmax(e,e));
a=(fmax(e,a))*(atan2(c,b));
d=(fmin(e,a))*(atan2(d,a));
d=(ceil(b))*(ceil(e));
e=pow(b,c);
}