#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(c,e))*(asin(b));
f=exp(a);
e=fdim(c,c);
c=(fmin(f,c))*(sin(a));
if(islessequal(f,c)){
b=atan2(f,f);
f=(sqrt(b))*(cos(f));
a=atan2(d,d);
f=(asin(d))-(floor(b));
f=(fmax(c,a))*(fdim(f,b));
}
else{
e=fdim(f,f);
b=fdim(e,b);
f=pow(f,c);
b=(pow(b,f))-(sqrt(b));
a=(fdim(a,e))-(exp(a));
}
if(islessgreater(a,d)){
f=(sin(d))/(fmin(b,c));
f=asin(e);
c=(fmin(b,e))*(fdim(f,b));
d=pow(a,c);
}
}