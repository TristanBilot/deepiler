#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(e,a))-(fmax(d,a));
d=(pow(d,g))-(sqrt(b));
g=(sin(f))/(atan2(a,c));
b=(atan2(e,g))*(fmin(c,c));
while(isgreaterequal(e,c)){
d=fmin(g,d);
g=exp(g);
e=(atan2(d,a))/(log(f));
g=(fdim(e,e))*(acos(e));
a=(acos(f))-(fmin(b,c));
}
f=(floor(f))/(sqrt(e));
b=log10(a);
a=pow(d,f);
d=(fmax(e,e))+(fmin(b,c));
d=(fmin(d,b))/(sqrt(a));
}