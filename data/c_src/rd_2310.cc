#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=log(g);
f=(fmax(d,g))/(pow(d,d));
while(islessgreater(b,c)){
d=(fdim(g,d))-(tan(f));
b=(log(d))/(cos(a));
c=(atan2(g,f))+(log(f));
b=(ceil(c))*(pow(b,f));
}
while(islessequal(d,g)){
d=atan2(f,d);
a=(atan(a))*(fmax(d,b));
}
}