#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(a);
g=ceil(h);
e=(log10(f))+(sin(h));
while(islessequal(g,d)){
f=(pow(h,d))*(floor(h));
c=(fmin(f,b))+(acos(h));
b=(asin(d))*(pow(b,c));
a=(pow(b,e))/(log(e));
g=(fmax(e,g))+(exp(d));
}
if(isless(b,f)){
e=(atan2(a,d))+(pow(g,a));
g=tan(b);
}
}