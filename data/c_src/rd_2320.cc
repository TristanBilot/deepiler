#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(c))+(exp(a));
d=log10(a);
b=(sqrt(f))/(ceil(a));
c=(tan(e))+(fmax(a,a));
c=exp(b);
b=fdim(c,d);
while(islessequal(b,c)){
f=fdim(f,b);
c=fmin(d,b);
}
}