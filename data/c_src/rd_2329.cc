#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=cos(a);
a=cos(f);
e=(asin(d))/(fmax(b,d));
d=(fmin(a,e))*(ceil(a));
while(isgreaterequal(f,e)){
c=(fmax(c,b))/(pow(c,b));
f=atan(f);
d=(atan2(f,f))*(fdim(b,f));
c=(floor(b))/(cos(e));
}
if(isgreaterequal(d,e)){
a=floor(d);
a=exp(a);
}
else{
a=(atan2(d,e))-(pow(a,f));
e=fmax(a,c);
f=atan2(a,f);
a=fmax(c,b);
f=(pow(b,c))/(fdim(c,a));
}
}