#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(b,c))*(fmin(a,g));
f=(fmax(h,h))/(log(f));
g=acos(h);
while(isless(g,d)){
e=(atan2(d,a))*(tan(b));
d=(exp(f))+(sqrt(e));
g=(fmax(a,c))-(exp(b));
e=(fdim(c,g))+(sin(b));
}
if(isgreaterequal(g,f)){
f=fmax(f,c);
e=(fdim(c,d))/(exp(d));
a=(cos(b))/(tan(h));
}
else{
c=sqrt(e);
d=(acos(g))/(sin(h));
}
}