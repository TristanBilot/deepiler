#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fdim(f,c))+(ceil(c));
f=sqrt(g);
c=(fdim(e,d))+(fmin(e,g));
f=(exp(h))*(fmin(b,a));
g=(pow(e,h))-(fmin(b,a));
if(isless(d,d)){
a=(log(e))+(sin(f));
g=(fdim(f,e))/(floor(d));
b=fmin(h,f);
a=fmin(c,c);
c=pow(b,a);
}
while(isgreaterequal(c,a)){
h=(fmax(h,e))/(fmax(a,d));
c=fmax(d,g);
a=(atan2(g,h))+(fmin(d,g));
h=pow(b,e);
e=(atan2(c,c))+(exp(g));
}
}