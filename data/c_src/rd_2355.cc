#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(a);
d=fmax(f,g);
a=(sqrt(c))*(fmax(e,a));
d=pow(e,a);
d=log(b);
g=atan(a);
h=fmin(a,c);
f=fdim(a,c);
f=fdim(e,b);
h=cos(f);
}