#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(a,a);
f=(fmax(d,c))*(sqrt(a));
c=(asin(c))/(fdim(c,f));
e=(log(f))*(pow(b,f));
a=(pow(d,e))-(fdim(f,a));
d=exp(f);
a=(atan2(f,c))-(fdim(b,f));
f=(pow(f,e))*(pow(c,d));
b=fmax(e,b);
while(islessgreater(e,f)){
f=cos(d);
c=tan(e);
}
}