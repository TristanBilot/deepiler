#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(e,b))-(atan2(c,g));
a=log(e);
f=(atan2(e,e))+(fdim(f,c));
g=(pow(g,h))*(fmin(a,c));
g=(sin(b))+(log10(g));
h=fdim(b,b);
b=floor(g);
b=sin(b);
a=pow(f,c);
g=fmin(d,d);
f=fdim(d,c);
f=(acos(f))*(ceil(f));
b=fmin(c,b);
}