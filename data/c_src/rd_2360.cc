#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=acos(e);
a=pow(e,c);
c=(pow(d,f))/(fmax(b,c));
while(isgreaterequal(f,f)){
d=atan(a);
b=ceil(a);
f=(log10(a))-(cos(a));
}
while(isless(a,a)){
f=pow(f,a);
f=fmin(e,a);
}
}