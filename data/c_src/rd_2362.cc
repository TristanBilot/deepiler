#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(cos(e))*(log10(a));
b=atan(d);
e=(atan(c))*(exp(c));
c=(fmin(a,b))-(fdim(e,d));
a=(fmax(a,a))+(cos(e));
while(islessgreater(d,e)){
b=fdim(d,b);
a=(fdim(c,e))-(pow(d,d));
}
}