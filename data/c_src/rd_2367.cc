#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=floor(b);
d=(pow(b,c))*(asin(d));
c=atan2(f,a);
c=fdim(b,d);
f=fmin(c,a);
b=pow(f,f);
c=fmin(b,a);
e=(atan(c))*(fmax(e,d));
while(islessgreater(c,e)){
c=(pow(e,e))/(atan2(e,e));
e=(tan(c))*(fmax(d,c));
a=fdim(a,f);
e=atan2(c,f);
c=pow(c,f);
}
}