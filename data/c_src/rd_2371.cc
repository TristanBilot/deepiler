#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(d,a))/(atan2(b,c));
d=(pow(f,f))/(fdim(e,b));
c=log(c);
g=(fmin(d,g))/(log10(b));
while(isless(c,c)){
e=pow(f,b);
d=sin(e);
f=(fdim(d,g))*(fmax(c,c));
g=(fdim(g,d))+(atan2(b,g));
d=exp(g);
}
f=(fmax(f,b))/(asin(e));
b=log10(e);
}