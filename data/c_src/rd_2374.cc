#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(a);
d=(sqrt(d))*(sin(a));
if(islessequal(d,e)){
e=(tan(a))+(cos(e));
d=fmin(c,d);
d=(exp(d))*(log(b));
}
else{
c=acos(a);
d=ceil(e);
e=fmax(a,e);
}
while(islessequal(c,a)){
a=exp(d);
e=(atan2(c,e))*(pow(b,c));
}
}