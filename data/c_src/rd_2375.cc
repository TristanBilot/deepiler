#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(e,e))*(fdim(a,c));
c=pow(f,b);
a=sin(c);
f=tan(a);
while(isgreaterequal(a,c)){
c=(pow(d,g))*(log10(d));
d=sqrt(d);
c=exp(d);
}
if(islessgreater(g,f)){
e=(asin(e))-(acos(a));
g=log10(b);
a=(fmin(a,e))+(atan2(e,g));
a=(pow(c,g))+(fmax(e,a));
}
else{
d=(fmin(g,d))-(atan2(e,e));
e=exp(b);
f=(fdim(c,e))-(fmax(d,c));
}
}