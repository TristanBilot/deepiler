#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(c))*(fmin(b,a));
b=(asin(c))+(log10(b));
b=tan(d);
d=(fmax(a,d))*(fmin(e,e));
e=(pow(e,e))-(atan2(b,a));
d=fmax(d,c);
c=fmax(d,b);
a=fmax(d,c);
e=(fmax(e,a))*(tan(d));
if(isgreaterequal(e,e)){
b=atan2(d,c);
c=(fmax(d,d))-(exp(a));
}
else{
c=fmax(a,b);
c=fmax(d,e);
b=fmin(d,b);
c=cos(b);
}
}