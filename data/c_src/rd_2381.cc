#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(e,d))*(asin(c));
e=(pow(d,a))*(atan2(d,b));
d=fmin(b,d);
b=(atan2(e,b))/(floor(a));
e=(fmin(b,b))*(sqrt(d));
b=(fdim(b,d))/(log(e));
if(isgreaterequal(b,e)){
e=(atan(d))/(sin(b));
d=(ceil(e))-(fdim(e,c));
a=ceil(e);
a=(pow(b,e))*(log10(d));
b=(atan(c))*(atan2(e,d));
}
else{
e=cos(c);
c=log(c);
a=fmin(b,a);
d=(pow(b,d))-(floor(b));
d=(fmax(c,e))-(pow(b,b));
}
}