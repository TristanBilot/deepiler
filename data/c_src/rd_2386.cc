#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,d))+(fdim(e,d));
d=(atan2(a,b))+(atan(c));
a=fdim(b,b);
if(isless(b,d)){
e=(exp(b))-(log10(e));
a=sqrt(d);
}
if(isgreaterequal(a,e)){
a=(pow(c,b))+(fdim(c,d));
c=exp(e);
d=asin(b);
c=pow(c,c);
a=ceil(e);
}
}