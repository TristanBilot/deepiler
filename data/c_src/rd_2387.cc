#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(b,a))*(fmax(e,f));
a=(atan2(g,c))-(pow(b,e));
f=(atan(b))+(cos(g));
c=log(b);
a=(fmin(c,d))*(fmax(d,d));
while(isgreaterequal(f,b)){
f=log(a);
d=(pow(b,e))*(ceil(b));
g=fdim(e,d);
}
b=sin(d);
c=(log10(f))-(fdim(e,c));
b=tan(f);
c=(exp(a))*(fmin(g,d));
}