#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(a,b);
c=fmin(b,e);
b=(fmax(e,c))/(atan2(c,d));
a=log(c);
c=sqrt(c);
while(isless(b,a)){
e=(fmin(e,a))/(fmin(b,e));
e=(pow(a,a))/(fmin(c,e));
d=fdim(a,a);
}
d=pow(e,d);
b=atan(b);
e=(atan2(b,d))-(pow(e,b));
}