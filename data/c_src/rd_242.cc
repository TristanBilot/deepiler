#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(e))+(log(e));
d=(cos(d))*(atan2(d,b));
b=(fdim(e,c))*(fdim(e,e));
d=sin(b);
c=sqrt(c);
while(islessequal(e,d)){
a=(fmax(e,a))/(atan(d));
c=fmax(a,c);
c=log10(b);
}
if(islessgreater(c,b)){
a=log10(e);
b=atan2(b,a);
e=tan(e);
}
}