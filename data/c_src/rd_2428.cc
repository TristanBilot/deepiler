#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(acos(f))*(sin(a));
e=(sin(a))*(floor(d));
e=(exp(b))*(fmax(g,f));
b=(pow(c,g))/(ceil(a));
if(isless(g,a)){
b=(fmax(d,f))/(exp(a));
f=log(c);
b=fdim(d,f);
}
else{
c=pow(c,a);
c=(fmax(a,b))/(acos(e));
d=(fmax(f,d))/(log10(a));
e=(fmax(a,d))-(log(c));
}
while(islessgreater(e,g)){
d=sin(g);
c=(tan(d))+(fdim(g,d));
f=(acos(g))-(fdim(g,c));
g=pow(c,g);
}
}