#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(asin(a))-(exp(c));
c=fmin(a,f);
c=fmin(b,d);
d=fdim(d,c);
c=(pow(d,b))*(fmax(g,c));
if(isless(f,g)){
d=(atan2(b,f))-(cos(e));
a=log10(g);
g=sqrt(d);
a=fmax(f,f);
}
a=log10(c);
e=pow(g,d);
c=(floor(e))/(sin(b));
g=log(f);
}