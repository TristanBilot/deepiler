#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(asin(b))+(fmin(c,a));
b=(sin(a))*(pow(a,c));
d=sin(e);
b=(pow(c,a))/(cos(e));
if(isgreaterequal(e,d)){
c=fdim(c,a);
c=(atan(a))+(fdim(a,d));
}
else{
a=(floor(b))+(fmin(e,d));
b=pow(c,b);
}
c=ceil(c);
e=(acos(b))+(floor(d));
}