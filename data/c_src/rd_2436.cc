#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(d);
d=(pow(c,d))-(fdim(e,d));
if(isgreaterequal(a,c)){
a=(fdim(d,c))/(fdim(e,e));
a=tan(c);
d=ceil(d);
}
e=cos(e);
e=pow(d,d);
}