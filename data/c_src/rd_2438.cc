#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(d,e);
e=(sqrt(e))+(sqrt(e));
b=fmax(c,a);
b=(log(a))/(log10(c));
e=(fmax(b,b))-(log10(b));
a=sqrt(e);
e=fmin(a,b);
b=fmin(a,d);
while(islessequal(b,e)){
e=(pow(c,b))+(fmax(b,a));
e=(pow(b,c))/(exp(c));
}
}