#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(a,f))*(floor(f));
e=(pow(g,c))/(fmax(h,d));
d=(fmin(b,a))*(fmax(c,e));
if(isless(h,d)){
b=log(g);
e=log(c);
f=(acos(c))-(cos(g));
h=acos(d);
}
else{
f=(log10(d))*(sin(d));
f=(atan2(h,f))-(atan2(b,g));
}
while(islessequal(f,h)){
c=(log(c))-(fmax(g,a));
a=(fmin(e,c))/(asin(f));
g=asin(d);
e=pow(d,h);
}
}