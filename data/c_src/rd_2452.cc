#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(cos(a))*(ceil(c));
c=(pow(c,d))/(fmin(d,e));
if(isless(f,f)){
d=(log(d))-(fdim(e,b));
f=(tan(b))-(pow(f,c));
b=fmax(f,e);
}
if(isgreaterequal(a,c)){
c=sin(e);
a=(atan(a))/(cos(d));
c=(tan(b))*(ceil(e));
d=fmax(b,a);
e=(log(a))+(fmin(b,d));
}
else{
c=fdim(a,c);
e=atan2(d,d);
d=atan2(e,b);
f=fdim(c,b);
e=(asin(d))*(atan2(f,d));
}
}