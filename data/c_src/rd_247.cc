#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=acos(c);
c=asin(f);
c=exp(a);
b=(pow(f,c))+(exp(b));
if(isless(e,b)){
b=fmax(f,a);
a=sin(a);
}
if(isgreaterequal(b,c)){
f=asin(a);
b=(exp(d))+(exp(a));
a=(log(e))+(sqrt(f));
f=(fmin(f,a))*(log10(c));
}
else{
d=(tan(f))-(cos(c));
f=atan(f);
f=(fmin(a,a))-(log10(e));
}
}