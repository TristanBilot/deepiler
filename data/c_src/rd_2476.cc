#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fmax(d,f);
b=(fmax(g,d))+(exp(e));
f=(tan(e))*(fmax(e,b));
g=(fmax(g,c))-(fmax(b,f));
f=tan(c);
e=asin(g);
e=(cos(e))*(atan(g));
b=(atan2(a,e))-(fmax(e,g));
d=(sin(e))/(asin(c));
a=(atan2(b,e))*(sin(e));
c=(fdim(e,f))*(atan2(a,d));
e=(tan(c))-(sqrt(a));
}