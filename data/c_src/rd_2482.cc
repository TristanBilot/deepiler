#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(b,f))/(pow(a,d));
c=(exp(a))/(fdim(d,e));
if(islessequal(e,d)){
f=(asin(d))+(pow(e,g));
g=log(g);
g=(fmax(b,f))/(atan(g));
g=log10(f);
}
if(isgreaterequal(c,e)){
a=log10(g);
d=(log10(c))*(atan(c));
}
else{
f=sqrt(c);
d=(asin(e))*(pow(c,c));
c=(log(b))*(sqrt(c));
}
}