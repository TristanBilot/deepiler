#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=tan(e);
e=(fdim(a,e))+(fmax(e,a));
c=atan(a);
d=fmin(b,b);
d=(fmin(e,b))/(log10(b));
if(islessgreater(b,d)){
d=log10(e);
d=fmax(a,d);
a=(asin(a))*(log(c));
c=fmin(b,e);
b=cos(d);
}
if(islessequal(b,d)){
a=(pow(d,d))+(exp(a));
c=(tan(d))-(fmin(c,d));
d=sin(e);
a=fmax(d,a);
}
}