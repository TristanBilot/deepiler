#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(f,a))*(pow(d,b));
c=pow(c,f);
b=fdim(a,c);
b=(cos(c))-(log(c));
while(islessgreater(c,e)){
f=fdim(a,c);
a=fdim(d,b);
c=fmax(c,f);
f=(sin(a))*(pow(b,f));
f=fdim(b,b);
}
while(isless(b,f)){
b=pow(d,b);
f=(fmin(d,d))-(atan(b));
b=(sin(c))*(fdim(e,c));
f=(fmax(c,a))*(fmin(a,b));
f=atan2(c,b);
}
}