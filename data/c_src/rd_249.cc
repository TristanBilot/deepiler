#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(d,a);
f=sqrt(e);
b=floor(f);
f=atan2(d,a);
d=(ceil(f))*(atan2(c,a));
f=atan2(f,b);
c=(fdim(b,f))*(atan2(c,c));
d=exp(c);
if(isless(e,d)){
e=(asin(d))*(sqrt(a));
f=atan2(b,e);
a=(ceil(a))*(log10(f));
c=ceil(c);
}
}