#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(e,d);
b=ceil(e);
f=atan(e);
e=(sqrt(f))/(log(b));
c=atan2(e,b);
f=fdim(f,a);
e=cos(b);
e=(ceil(a))-(atan2(b,c));
while(isgreaterequal(d,b)){
b=(log10(d))+(atan(b));
b=sin(d);
d=ceil(f);
}
}