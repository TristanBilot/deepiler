#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(sin(b))*(pow(d,a));
c=ceil(e);
if(isless(b,e)){
c=fmax(b,d);
a=atan2(e,d);
d=fdim(a,e);
}
else{
d=(log10(a))/(log10(b));
e=log(b);
}
c=(atan2(c,c))/(pow(d,e));
c=(fmax(e,c))+(log10(b));
b=pow(b,e);
}