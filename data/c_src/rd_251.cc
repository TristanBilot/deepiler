#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=exp(d);
g=fdim(f,g);
if(isgreaterequal(c,g)){
c=(atan(c))-(pow(b,e));
e=(fmax(b,a))+(fdim(c,g));
b=floor(e);
f=atan2(a,a);
}
else{
d=(fmax(f,f))/(log(b));
g=atan2(e,f);
c=atan2(g,a);
f=(atan2(b,c))-(log10(a));
}
while(islessequal(b,f)){
f=atan(f);
a=(ceil(a))*(atan(g));
a=log10(a);
b=(fdim(c,f))+(fmax(d,d));
}
}