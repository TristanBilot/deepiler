#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(e))+(atan2(c,d));
d=fdim(d,e);
while(islessgreater(d,a)){
a=pow(a,b);
c=log10(d);
e=(tan(d))*(ceil(a));
c=floor(d);
}
c=(fmin(e,b))*(sin(c));
a=atan2(c,d);
}