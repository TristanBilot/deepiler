#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(e,d);
e=(atan2(e,c))+(atan2(b,a));
c=(fdim(a,a))+(fmin(c,b));
d=fdim(b,d);
while(isgreaterequal(e,c)){
d=(sin(a))-(fdim(a,a));
d=(atan2(a,b))-(fmin(a,b));
}
if(islessgreater(a,b)){
a=asin(a);
a=tan(d);
e=(tan(e))*(atan2(d,c));
c=(sqrt(c))/(fmax(d,b));
}
else{
c=(exp(c))*(fdim(b,a));
e=fdim(a,b);
e=exp(e);
a=(exp(a))+(tan(c));
}
}