#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(acos(a))/(cos(c));
b=pow(d,f);
c=(tan(f))-(log(b));
if(isgreaterequal(c,c)){
b=fdim(e,c);
e=(fmax(e,e))+(fmax(e,e));
}
else{
f=(log(c))+(acos(e));
c=exp(f);
e=(sqrt(b))/(sqrt(d));
}
while(isgreaterequal(f,a)){
b=(fdim(c,d))+(asin(f));
b=(sin(e))+(exp(c));
c=acos(e);
c=pow(b,b);
f=fmin(f,c);
}
}