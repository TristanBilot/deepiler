#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(cos(h))*(fmax(a,d));
h=(fmax(a,c))*(pow(a,h));
g=fmin(d,g);
a=cos(a);
b=(pow(f,d))/(atan2(h,h));
while(isless(h,b)){
c=exp(g);
b=acos(f);
f=tan(h);
c=exp(h);
e=(pow(a,g))*(atan2(g,g));
}
a=pow(b,f);
f=(pow(c,e))*(fmax(h,f));
a=fdim(d,e);
}