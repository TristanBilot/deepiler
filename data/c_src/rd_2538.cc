#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(a);
b=(fdim(d,b))+(asin(h));
e=atan(c);
h=(fmin(d,c))*(cos(e));
h=(atan2(h,b))*(fmin(d,h));
g=exp(e);
h=(pow(h,h))/(atan(g));
h=(pow(a,a))-(atan2(a,c));
g=atan2(f,e);
a=(fmin(a,d))/(ceil(a));
}