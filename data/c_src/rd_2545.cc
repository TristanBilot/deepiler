#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(d,b))+(sin(b));
b=(fmin(c,a))*(fmin(e,c));
d=(pow(b,b))*(tan(b));
b=cos(e);
e=(ceil(b))*(atan2(d,d));
a=(sin(d))+(tan(e));
e=log10(b);
c=(log(e))/(fdim(b,a));
e=(asin(a))/(fmin(e,c));
d=(floor(a))/(sin(d));
a=(atan(a))*(tan(a));
}