#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,e);
e=(asin(c))-(atan2(a,d));
d=(pow(c,e))*(acos(b));
while(isless(b,b)){
b=(pow(c,a))/(exp(a));
c=(atan(e))-(exp(d));
e=(log(d))/(log(a));
}
while(islessgreater(a,d)){
d=asin(e);
a=(fmax(a,d))*(fmax(e,d));
b=log(d);
}
}