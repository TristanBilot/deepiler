#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(d,c))-(fdim(c,b));
b=(ceil(c))+(fmax(a,a));
d=cos(c);
while(islessequal(e,d)){
b=fmax(d,c);
c=(sin(b))*(log10(d));
a=fmax(a,d);
}
if(isless(c,f)){
f=tan(f);
f=pow(c,e);
}
else{
e=(atan(b))-(fmax(b,e));
f=(atan(f))/(sqrt(e));
}
}