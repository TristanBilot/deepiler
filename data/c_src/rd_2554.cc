#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(g,b))/(log(a));
g=(cos(b))+(pow(b,b));
d=acos(b);
if(islessequal(g,g)){
e=log10(b);
b=(asin(c))+(fmax(a,e));
a=(ceil(c))-(log(d));
a=(fmin(g,b))+(ceil(e));
b=(fmin(a,d))-(tan(d));
}
else{
f=atan2(a,a);
d=floor(b);
}
while(islessgreater(d,a)){
a=pow(g,b);
d=(atan(a))*(sqrt(f));
}
}