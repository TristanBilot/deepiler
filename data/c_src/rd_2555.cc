#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmin(g,e);
d=(log(b))+(fmin(g,c));
g=(fdim(b,a))+(fmax(f,e));
d=log(c);
c=(fmax(c,f))+(cos(g));
f=(pow(e,f))+(pow(f,f));
while(islessequal(d,a)){
f=fmax(f,d);
f=(acos(b))*(ceil(b));
}
}