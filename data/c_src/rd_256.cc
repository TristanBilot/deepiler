#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(pow(f,b))*(exp(d));
c=pow(c,g);
h=(floor(a))*(acos(a));
d=(fmin(h,d))*(tan(h));
e=pow(e,h);
a=(fmin(c,b))*(ceil(e));
a=(exp(b))+(exp(g));
a=(acos(e))*(sin(c));
if(isgreaterequal(a,d)){
g=(pow(d,c))+(fmin(g,c));
a=fmax(f,d);
c=(atan(g))*(fdim(e,d));
}
else{
e=(exp(d))+(floor(b));
g=log(b);
f=(fdim(d,a))+(ceil(g));
b=cos(b);
}
}