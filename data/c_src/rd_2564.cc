#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(b,c);
a=floor(f);
b=(acos(d))+(atan2(c,e));
f=atan2(c,f);
d=sqrt(b);
if(islessequal(b,f)){
f=(fdim(d,c))*(sin(e));
c=(exp(f))+(asin(d));
e=fmax(c,f);
a=fmin(f,f);
}
else{
b=log(d);
d=log10(b);
d=(exp(f))*(log(e));
}
c=sqrt(a);
d=(fmax(b,b))/(log(c));
a=fdim(e,a);
c=fmax(d,a);
f=(asin(e))/(acos(d));
}