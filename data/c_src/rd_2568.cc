#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(a,d);
c=(fdim(b,a))/(fmin(b,a));
while(islessequal(b,c)){
a=(pow(b,b))+(fmin(c,d));
d=acos(c);
}
if(isgreaterequal(c,e)){
c=pow(a,d);
d=pow(d,c);
b=atan2(d,e);
c=(asin(e))*(asin(b));
e=(asin(d))*(tan(a));
}
}