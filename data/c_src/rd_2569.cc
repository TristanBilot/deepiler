#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=pow(a,c);
g=sin(c);
d=fmax(g,f);
b=atan2(a,c);
b=pow(d,g);
f=(atan2(e,a))-(acos(a));
while(islessgreater(f,c)){
d=atan2(g,d);
a=(cos(d))*(cos(e));
e=(log10(b))+(fmax(g,f));
a=(floor(f))+(fmax(e,d));
}
}