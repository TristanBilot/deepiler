#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(acos(d))/(tan(e));
c=(fmax(e,c))-(floor(a));
if(isgreaterequal(a,a)){
b=ceil(c);
e=(log10(e))*(log(c));
}
else{
d=(floor(c))*(acos(e));
e=pow(b,d);
c=fmax(d,e);
e=log(c);
}
b=(atan2(a,c))-(floor(e));
d=fmin(a,e);
}