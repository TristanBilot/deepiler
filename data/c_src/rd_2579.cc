#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(pow(e,b))/(fmax(g,d));
g=(pow(g,e))*(floor(f));
d=log(c);
a=floor(f);
d=(fmin(d,d))/(cos(g));
while(isgreaterequal(e,a)){
d=(fmax(c,c))-(atan(e));
d=(pow(e,c))/(sqrt(f));
c=pow(d,b);
a=atan2(e,a);
}
while(isless(a,b)){
e=fdim(d,a);
c=(atan2(f,d))+(fdim(d,g));
a=(fdim(e,a))+(fdim(e,b));
b=fmax(e,d);
}
}