#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=ceil(a);
b=(pow(b,e))-(sin(b));
d=(tan(b))-(fmax(e,d));
d=(fmax(b,e))-(exp(e));
while(islessequal(a,d)){
c=(atan(c))+(log(d));
c=log10(c);
b=log(a);
c=(acos(e))*(pow(e,d));
}
if(isgreaterequal(c,a)){
d=(fdim(a,b))/(fmin(c,b));
c=ceil(b);
c=fmax(a,e);
}
}