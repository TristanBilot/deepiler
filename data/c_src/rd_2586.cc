#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=tan(b);
d=(fdim(a,b))-(exp(c));
e=(exp(e))-(pow(b,f));
while(isless(f,f)){
d=(cos(e))+(atan2(f,d));
a=(fmax(f,f))+(pow(a,f));
f=(floor(c))/(fdim(a,e));
d=sqrt(d);
a=ceil(d);
}
b=tan(e);
f=asin(e);
d=(atan2(a,b))*(fmax(c,f));
b=(fmax(b,d))+(fdim(f,d));
}