#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(a,e))/(cos(d));
e=(pow(d,e))+(ceil(b));
d=fmax(a,a);
e=(atan(a))*(fmax(e,b));
a=sin(b);
b=pow(c,d);
c=(atan2(a,a))-(ceil(d));
e=pow(a,c);
while(islessgreater(c,e)){
c=(log10(a))+(fmin(b,c));
b=(cos(a))-(atan(b));
c=atan2(e,c);
a=(log(b))-(fdim(c,a));
b=(sin(d))+(fdim(e,b));
}
}