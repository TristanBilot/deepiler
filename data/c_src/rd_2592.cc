#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log10(c))*(fmin(c,a));
a=(atan(a))+(fdim(c,c));
while(isgreaterequal(b,e)){
a=(sin(d))+(atan2(d,a));
e=(pow(e,b))+(fmin(d,b));
c=pow(b,b);
}
d=(fmax(b,e))*(floor(d));
e=fdim(d,a);
c=exp(d);
c=fmin(b,b);
}