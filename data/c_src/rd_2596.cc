#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(b);
c=(atan2(a,a))-(tan(a));
a=log(e);
b=(fdim(b,a))*(atan(e));
e=(fdim(e,c))*(atan(a));
if(isgreaterequal(e,b)){
a=(sqrt(c))-(pow(a,a));
b=atan2(e,d);
}
else{
b=exp(d);
b=exp(b);
d=(fmin(d,a))+(pow(c,d));
c=log10(a);
}
while(isless(d,b)){
c=tan(c);
c=exp(c);
}
}