#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(sin(c))/(acos(e));
g=pow(g,d);
e=fmax(a,d);
b=(tan(g))+(ceil(a));
f=(sqrt(a))*(cos(c));
c=(asin(d))+(fmax(f,d));
a=floor(b);
a=(pow(e,a))-(acos(a));
g=tan(b);
if(islessgreater(d,b)){
f=atan2(a,a);
c=sqrt(c);
}
else{
d=fmax(d,a);
g=(pow(c,d))/(atan(g));
e=(atan2(b,f))-(log10(b));
g=acos(g);
}
}