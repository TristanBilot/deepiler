#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(a,e))*(fmin(c,g));
a=(log10(c))+(atan2(e,c));
while(islessequal(e,c)){
e=ceil(d);
g=(atan2(a,a))-(tan(a));
}
d=tan(b);
c=sqrt(f);
c=(acos(g))-(fmax(f,b));
e=exp(e);
f=log10(b);
}