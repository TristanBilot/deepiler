#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(e,b))-(atan2(a,c));
a=ceil(c);
b=fmax(d,b);
if(islessgreater(d,c)){
a=fmin(e,b);
e=(fmin(b,c))-(fmin(a,d));
}
if(isless(b,a)){
b=ceil(d);
b=fmin(a,b);
d=fmax(b,e);
}
}