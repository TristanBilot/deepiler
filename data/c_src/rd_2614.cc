#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(a,b))+(fmax(d,b));
a=sqrt(c);
a=pow(b,b);
a=(atan2(a,d))/(asin(e));
e=fmin(b,a);
b=(acos(b))/(fmax(b,e));
b=fmax(d,a);
e=(fmax(a,b))+(ceil(c));
d=pow(e,b);
c=(pow(c,e))-(log(d));
e=atan2(c,c);
}