#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=acos(c);
b=pow(b,e);
a=fdim(d,d);
if(islessgreater(d,b)){
d=(acos(a))/(sin(c));
d=(cos(d))-(sin(a));
d=(sin(b))+(pow(c,b));
}
else{
c=pow(e,d);
c=fdim(a,d);
a=acos(c);
c=(tan(b))/(tan(a));
}
if(islessequal(b,d)){
a=pow(d,d);
a=(fmin(a,b))-(fmin(a,c));
c=fmax(b,e);
}
else{
a=(acos(b))*(log10(a));
e=(floor(b))*(sqrt(c));
e=fmin(b,a);
}
}