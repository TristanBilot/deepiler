#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=exp(g);
g=(sin(d))/(fmin(f,b));
a=fmin(e,d);
f=atan(c);
a=(floor(g))/(tan(b));
a=fmax(g,c);
b=log(d);
while(isless(f,a)){
b=(sin(e))-(fmax(a,f));
b=(atan2(d,b))/(fdim(g,g));
c=(pow(g,c))+(fdim(c,b));
}
}