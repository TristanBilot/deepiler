#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log(a);
a=(fdim(a,b))*(sin(c));
if(islessequal(e,d)){
d=(exp(e))-(fmin(c,c));
a=(floor(e))*(fmin(d,c));
e=fmax(a,c);
d=cos(e);
e=(fmin(e,b))-(floor(a));
}
else{
a=acos(b);
c=fmax(e,e);
b=pow(b,b);
d=fdim(c,d);
e=atan2(a,c);
}
if(islessgreater(d,b)){
d=sin(c);
a=fdim(d,d);
}
else{
c=(atan2(e,b))+(fdim(c,a));
b=fdim(a,a);
d=floor(a);
a=(acos(a))*(fmin(b,d));
}
}