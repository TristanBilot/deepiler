#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(sin(f))+(cos(g));
e=(pow(h,c))/(fmax(b,d));
d=(ceil(c))-(exp(e));
g=sin(h);
c=(atan(f))*(exp(f));
c=(pow(d,f))*(acos(a));
c=fmin(b,a);
b=(fmin(c,f))+(ceil(g));
g=(fmax(b,h))-(ceil(d));
}