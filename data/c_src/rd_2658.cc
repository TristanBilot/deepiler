#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(f,h);
f=(fdim(c,b))-(fdim(c,f));
h=fmin(c,g);
g=(sin(e))+(fmin(a,d));
if(islessequal(a,b)){
c=log10(a);
d=(fdim(c,g))*(fmin(h,e));
a=(cos(b))*(atan2(g,a));
e=(log(a))*(atan(a));
}
else{
e=(log(a))*(fdim(e,f));
a=sqrt(c);
c=(fmax(b,f))*(asin(f));
d=fmin(g,e);
}
}