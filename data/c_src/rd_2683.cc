#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(a,a))+(fmin(c,a));
c=floor(c);
a=exp(c);
while(isless(b,f)){
e=acos(d);
f=(pow(b,e))-(exp(e));
f=(log(b))*(pow(f,b));
d=(ceil(e))+(tan(c));
e=(pow(e,f))/(sqrt(d));
}
if(isgreaterequal(a,c)){
b=(pow(a,c))*(pow(d,d));
d=fmax(f,c);
d=atan(e);
}
else{
c=asin(b);
b=(fdim(e,a))/(atan2(a,e));
a=(fmax(c,c))+(pow(a,a));
f=(pow(d,d))*(acos(e));
}
}