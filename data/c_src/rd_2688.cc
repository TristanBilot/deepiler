#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(a);
d=fmin(a,a);
if(isless(c,c)){
b=(cos(e))*(fmin(a,b));
b=tan(d);
a=(exp(b))/(atan2(e,c));
d=fmax(e,c);
a=(pow(b,c))-(cos(c));
}
else{
d=(ceil(d))*(log10(e));
c=(fmax(e,d))-(asin(a));
e=(fmax(a,b))+(fmax(b,a));
e=atan2(b,c);
d=(ceil(e))-(sin(d));
}
d=fdim(d,c);
d=(fmin(b,e))*(cos(a));
d=atan2(e,d);
c=sin(c);
d=(cos(a))*(fmin(d,c));
}