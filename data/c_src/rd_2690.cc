#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(c,b);
a=log10(e);
c=exp(b);
b=atan2(a,b);
f=fdim(b,b);
c=(atan2(b,b))-(fdim(d,f));
c=(sin(c))+(fdim(b,c));
e=(atan(f))-(sqrt(a));
f=exp(f);
c=exp(a);
e=(fmin(d,f))*(cos(a));
}