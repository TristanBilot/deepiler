#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(e);
a=(atan2(a,c))-(fmax(d,d));
d=(fmax(a,e))/(atan2(a,b));
b=(fdim(c,d))/(floor(b));
if(islessequal(a,a)){
d=(atan2(d,b))-(acos(c));
b=(log10(b))*(pow(b,e));
c=sin(c);
}
if(isgreaterequal(c,b)){
b=(sin(e))*(sqrt(d));
e=(fdim(e,e))/(exp(b));
c=atan2(e,a);
}
}