#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(c,b))+(log10(c));
d=sin(c);
d=atan2(d,d);
c=(log(c))+(exp(c));
d=(cos(e))-(log(b));
if(isgreaterequal(c,e)){
b=(acos(e))/(tan(e));
b=(fmax(c,b))+(fmax(a,a));
a=(fdim(a,b))/(pow(b,a));
d=pow(c,a);
}
else{
c=fmin(c,e);
d=(asin(d))/(pow(b,d));
e=asin(c);
}
if(islessequal(a,e)){
b=(asin(e))*(exp(a));
a=(ceil(e))/(fmax(e,d));
}
else{
e=sin(b);
e=atan2(c,e);
e=sin(c);
c=(fmin(a,a))+(sqrt(a));
e=(exp(c))/(acos(c));
}
}