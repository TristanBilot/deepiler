#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(e,b))-(fdim(b,e));
c=cos(b);
c=(fdim(e,e))+(pow(e,e));
c=pow(c,d);
a=cos(b);
e=pow(e,d);
c=(pow(d,e))+(fdim(d,c));
a=(fdim(e,c))*(fdim(e,a));
c=sin(a);
}