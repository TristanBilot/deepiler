#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fmax(e,b))-(sin(b));
a=(atan2(b,h))-(exp(a));
d=(atan2(c,c))/(pow(d,d));
h=(fdim(b,e))+(pow(h,g));
g=fmax(c,e);
h=(floor(g))-(tan(a));
if(islessgreater(h,c)){
c=(ceil(e))/(atan2(g,a));
h=(asin(h))/(fmin(d,b));
b=(fdim(g,c))+(fdim(h,f));
e=fmax(g,g);
}
}