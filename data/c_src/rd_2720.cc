#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(a);
b=(atan2(e,g))-(atan2(a,h));
e=atan2(g,a);
a=asin(b);
while(isless(h,h)){
b=fdim(f,a);
g=(atan(f))-(atan(b));
}
while(isless(b,b)){
h=fmax(a,e);
e=atan2(e,f);
g=atan2(e,c);
}
}