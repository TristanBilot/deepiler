#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log10(e);
c=(floor(a))-(fmax(c,a));
d=(fdim(c,c))+(cos(e));
d=cos(d);
while(islessgreater(c,d)){
e=(cos(d))*(tan(a));
e=sqrt(d);
c=(pow(c,c))*(pow(b,d));
e=(fmin(c,a))+(atan(c));
}
if(islessgreater(c,b)){
c=ceil(c);
c=(fdim(c,c))-(log(a));
a=(fdim(e,a))*(atan(e));
c=(floor(c))*(log10(d));
}
}