#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(d))-(cos(e));
a=acos(c);
while(islessgreater(e,b)){
c=(fdim(e,b))-(fdim(a,c));
b=cos(b);
a=(pow(a,b))/(asin(c));
}
if(isgreaterequal(c,b)){
c=(fmax(b,c))+(cos(e));
a=(pow(c,e))-(cos(a));
d=fdim(e,a);
d=fmax(a,a);
}
else{
b=(ceil(e))+(asin(a));
d=(pow(a,c))*(log(c));
}
}