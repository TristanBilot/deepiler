#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan(a);
b=log10(a);
d=sqrt(e);
if(isless(d,e)){
a=atan2(e,c);
e=(fdim(b,b))*(sin(b));
c=(log10(b))/(asin(b));
}
if(islessequal(e,c)){
a=(atan2(c,d))/(pow(a,a));
e=(floor(a))*(fmax(a,a));
b=fdim(d,e);
b=fmax(c,b);
}
}