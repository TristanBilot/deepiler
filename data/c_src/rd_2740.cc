#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(a,d))-(atan(e));
c=(fmin(d,e))+(pow(e,a));
a=(pow(a,b))/(ceil(c));
a=fmax(a,b);
b=(fmax(a,e))+(atan2(e,b));
if(isless(e,b)){
c=(fdim(c,d))+(fmin(a,c));
e=(pow(e,b))+(atan2(c,b));
d=atan2(b,c);
b=(fdim(e,a))/(cos(c));
}
else{
e=(cos(c))/(exp(d));
a=(exp(a))+(log(d));
e=(pow(d,a))-(pow(c,a));
}
if(islessequal(c,e)){
c=(sqrt(c))-(fmin(c,e));
c=fmax(c,d);
e=(fmin(c,d))*(ceil(a));
a=(pow(d,c))*(fdim(d,a));
}
else{
b=(atan(b))/(sqrt(c));
c=cos(a);
e=asin(d);
d=(sin(b))+(ceil(c));
c=fmax(a,c);
}
}