#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(c,a))*(tan(f));
e=(pow(b,b))+(acos(b));
f=(atan2(a,a))+(fdim(e,b));
if(isgreaterequal(a,c)){
f=(sin(a))*(fmax(e,f));
f=fmin(e,d);
}
else{
f=log(f);
b=(fdim(f,b))+(asin(a));
a=pow(f,f);
}
e=(fdim(e,b))+(asin(c));
b=asin(a);
e=fmax(e,e);
f=fmin(e,f);
}