#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log(c);
f=pow(b,e);
a=log10(c);
b=fmin(e,c);
b=(pow(b,b))*(fmin(c,a));
if(islessequal(a,a)){
a=asin(d);
b=(ceil(c))*(asin(b));
a=(log(f))+(floor(b));
a=(tan(d))*(acos(e));
}
}