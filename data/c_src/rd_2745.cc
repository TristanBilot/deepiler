#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(c,b))*(floor(a));
c=tan(f);
b=(tan(d))*(acos(e));
d=(sqrt(b))/(ceil(c));
a=fmin(a,d);
c=asin(d);
a=(log(a))/(fmax(e,a));
if(islessequal(a,a)){
a=atan(d);
b=(cos(b))-(fdim(f,f));
f=atan2(c,d);
a=(pow(a,c))/(pow(c,c));
}
else{
f=log10(e);
f=tan(b);
}
}