#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sqrt(d))*(fmax(e,c));
d=ceil(c);
e=tan(d);
d=fdim(e,b);
e=tan(c);
while(isless(a,d)){
b=(pow(c,a))/(log(b));
d=(acos(e))-(fmin(d,a));
e=(log(a))+(sin(e));
d=atan(c);
}
c=fdim(e,c);
b=atan2(a,e);
b=atan2(b,b);
}