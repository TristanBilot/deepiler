#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(c,f);
b=(pow(c,b))+(exp(e));
e=(asin(b))-(exp(a));
d=floor(b);
while(islessequal(b,c)){
c=atan2(e,c);
c=(fmin(e,a))+(fmin(c,d));
b=(fmin(d,d))+(tan(e));
f=sqrt(b);
}
c=ceil(c);
a=acos(f);
}