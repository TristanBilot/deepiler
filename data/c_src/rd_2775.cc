#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(c);
b=acos(a);
c=(pow(a,e))+(sin(e));
b=(fmin(b,c))/(atan2(a,c));
if(islessgreater(d,d)){
d=atan2(b,d);
e=fdim(d,d);
a=(asin(c))/(sqrt(b));
d=log10(e);
}
while(isless(e,d)){
d=(log(e))-(floor(b));
b=fdim(a,d);
d=pow(a,b);
a=fdim(a,e);
c=(fdim(a,c))*(fmax(d,e));
}
}