#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=floor(c);
c=(sin(a))/(fmin(a,c));
c=(sin(d))/(fmin(a,b));
while(isgreaterequal(a,b)){
a=(fmin(e,b))*(asin(e));
a=log(e);
b=atan(c);
}
if(islessequal(d,c)){
a=(log(d))*(sin(a));
b=atan2(a,c);
a=fmax(d,a);
}
else{
d=log(d);
e=(fmax(e,b))*(fmin(b,e));
e=atan2(a,d);
}
}