#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(e,b))+(cos(b));
b=(fmax(a,b))-(pow(d,b));
while(isless(d,a)){
b=cos(b);
e=(sin(a))/(sin(c));
}
while(isgreaterequal(e,b)){
a=sin(a);
c=(log(a))-(acos(e));
}
}