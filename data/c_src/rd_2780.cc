#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(a,a))*(acos(b));
c=sqrt(c);
c=(fdim(e,c))/(sin(d));
d=fdim(b,d);
if(islessgreater(c,e)){
d=fdim(e,c);
a=exp(e);
}
else{
c=tan(e);
e=(fdim(b,b))/(atan(c));
c=(log(d))/(sin(c));
}
if(isless(b,b)){
e=asin(e);
c=floor(e);
a=fmax(b,b);
}
}