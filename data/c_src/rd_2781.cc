#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=floor(d);
e=(sqrt(e))*(floor(b));
d=fmax(a,e);
b=log10(e);
e=atan2(b,b);
if(islessequal(e,f)){
g=log10(c);
d=(fdim(f,c))*(fmax(d,c));
}
}