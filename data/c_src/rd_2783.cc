#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,e))-(ceil(a));
b=pow(d,c);
if(isgreaterequal(e,e)){
a=fmin(d,c);
e=(cos(d))*(floor(a));
}
else{
d=(atan(e))/(tan(c));
c=(sin(a))+(fmin(a,c));
e=fmin(a,e);
d=fmax(c,d);
}
while(isless(b,e)){
a=(tan(c))*(atan2(b,a));
e=(sin(b))-(ceil(d));
a=exp(d);
c=pow(d,d);
c=tan(a);
}
}