#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=floor(c);
d=sqrt(c);
c=(fmin(b,d))/(fmin(a,e));
a=sin(e);
d=atan2(b,a);
while(isless(d,c)){
c=fmin(d,e);
b=(exp(a))+(ceil(a));
d=(sqrt(c))-(tan(c));
e=(pow(d,a))+(fdim(e,c));
d=cos(b);
}
if(islessequal(e,d)){
d=(exp(c))*(pow(a,a));
c=atan2(d,b);
d=pow(a,a);
e=(log(d))-(atan2(a,d));
}
}