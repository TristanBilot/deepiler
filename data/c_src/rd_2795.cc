#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=tan(c);
d=log(c);
e=pow(c,f);
e=fmin(f,d);
if(islessequal(d,a)){
a=(fmax(a,d))*(sin(b));
a=(exp(e))/(fmin(c,c));
}
else{
d=(floor(e))*(atan(d));
d=pow(c,e);
c=floor(d);
d=pow(d,b);
c=log10(f);
}
b=(pow(d,f))/(cos(c));
e=fmax(f,a);
}