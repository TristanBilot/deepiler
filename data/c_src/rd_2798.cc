#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(d);
b=(atan2(d,b))-(exp(e));
c=(fmin(d,b))/(pow(e,d));
b=(asin(b))/(sqrt(a));
d=(fmax(e,a))-(sin(b));
while(isless(b,e)){
e=log(c);
c=(atan(b))/(atan2(a,d));
c=floor(c);
c=log(b);
}
while(isgreaterequal(a,a)){
d=(acos(d))*(atan2(b,b));
a=(fdim(b,e))-(log10(e));
e=(floor(b))*(atan2(b,e));
e=fdim(c,e);
}
}