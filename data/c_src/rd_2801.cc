#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=acos(a);
e=(cos(b))+(fmax(e,d));
c=(atan2(e,b))-(pow(c,b));
if(islessequal(e,e)){
a=fmin(c,a);
b=asin(d);
d=exp(b);
}
else{
d=(tan(c))-(fmin(b,d));
b=(sqrt(c))+(acos(d));
b=(sqrt(d))+(fmin(d,a));
d=atan(b);
}
while(islessgreater(e,d)){
b=(floor(b))+(atan2(c,b));
a=pow(a,d);
b=(pow(a,c))-(atan2(c,d));
d=atan2(b,e);
c=log10(a);
}
}