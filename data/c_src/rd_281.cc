#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log10(d);
a=(log10(c))/(sin(a));
b=(pow(h,d))-(fmax(a,g));
a=(atan2(e,b))+(pow(h,c));
f=(sqrt(d))*(asin(e));
if(isgreaterequal(c,h)){
g=fmin(e,c);
c=(exp(d))-(tan(a));
}
else{
c=(atan2(g,c))-(fdim(e,b));
c=pow(a,d);
c=(atan2(d,b))*(exp(f));
b=(fmin(c,e))-(pow(f,b));
h=fmin(f,f);
}
}