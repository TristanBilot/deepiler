#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(a);
c=(fmin(a,e))-(ceil(d));
a=(atan2(e,c))*(acos(a));
if(isgreaterequal(a,b)){
b=floor(a);
a=(exp(a))-(log10(a));
}
else{
d=(floor(a))/(atan2(c,b));
e=(fmax(c,e))+(fdim(d,e));
}
a=fmin(a,d);
d=atan2(a,b);
d=(atan2(a,a))+(log(c));
e=exp(a);
e=(pow(c,e))+(sqrt(d));
}