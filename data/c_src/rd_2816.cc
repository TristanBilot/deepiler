#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(f);
a=log(g);
f=(fdim(e,d))/(log(b));
d=fmin(e,a);
while(isless(f,f)){
a=(fmax(e,h))+(cos(c));
h=pow(e,b);
e=atan2(e,c);
}
f=(fmin(e,a))/(pow(f,g));
f=log10(b);
e=(atan2(a,h))+(fdim(d,c));
d=ceil(e);
c=(exp(e))-(acos(h));
}