#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(asin(c))+(log(f));
d=fmax(c,b);
g=pow(g,d);
h=fmin(d,b);
c=exp(g);
while(isgreaterequal(d,g)){
b=sin(f);
d=(sqrt(b))-(exp(g));
}
g=atan(g);
b=pow(d,c);
d=(pow(h,d))-(cos(e));
}