#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(sin(a))*(tan(c));
a=(atan2(c,a))/(pow(e,f));
b=(floor(a))*(tan(a));
d=fdim(e,a);
f=(log10(b))*(log10(b));
a=(atan2(e,a))+(log10(c));
f=(fmax(d,d))*(fdim(d,c));
d=(pow(b,b))+(asin(f));
c=tan(b);
c=(tan(c))/(exp(f));
d=ceil(d);
}