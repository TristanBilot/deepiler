#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(floor(d))*(exp(c));
a=tan(a);
c=fmax(b,b);
c=acos(e);
d=sqrt(b);
while(isless(b,b)){
c=(sin(b))-(atan(b));
d=fdim(c,c);
c=sin(e);
}
while(isgreaterequal(c,a)){
e=pow(d,a);
b=(cos(b))/(sin(e));
e=asin(b);
}
}