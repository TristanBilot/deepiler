#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(b,d))-(fdim(d,b));
c=ceil(e);
f=(atan2(e,c))/(fdim(b,f));
f=(fdim(b,e))/(log10(c));
a=(sin(c))*(sin(b));
f=floor(c);
a=exp(a);
e=(fdim(c,d))+(pow(a,d));
}