#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(a,a);
c=(atan2(a,e))*(pow(a,d));
e=(cos(e))/(fmax(a,a));
e=fmax(d,d);
if(islessgreater(e,d)){
a=(ceil(c))-(log10(a));
a=atan2(e,c);
}
if(isgreaterequal(c,b)){
b=(pow(b,b))-(exp(b));
b=(fmin(c,e))-(log(c));
c=(floor(b))+(atan2(c,e));
b=(sqrt(d))-(fmin(a,b));
e=(atan2(e,d))-(log10(b));
}
}