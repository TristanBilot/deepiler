#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(tan(b))+(floor(a));
c=fmax(d,e);
a=(tan(c))-(log(a));
e=pow(c,b);
d=log10(a);
a=(sqrt(e))+(floor(c));
e=(fmax(a,d))*(acos(b));
a=(cos(b))/(pow(c,c));
if(isgreaterequal(a,b)){
c=(tan(e))/(asin(a));
e=log(c);
c=pow(d,c);
}
}