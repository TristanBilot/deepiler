#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(e);
d=acos(d);
d=(fmin(d,d))-(asin(b));
e=fdim(c,a);
if(isless(e,c)){
c=(fmin(e,e))/(ceil(a));
e=fmax(e,c);
d=(asin(b))*(fmin(c,a));
}
else{
c=(fmax(a,e))+(fmax(b,e));
c=atan(d);
}
c=pow(c,b);
d=(pow(c,d))*(ceil(c));
d=fmax(b,e);
d=(asin(b))-(exp(a));
b=(log10(c))*(atan2(b,d));
}