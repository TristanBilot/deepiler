#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=tan(a);
f=(fdim(e,f))-(sin(c));
a=(atan(g))-(acos(g));
e=fmax(g,g);
f=(sqrt(f))+(atan2(a,b));
c=log(c);
b=(fdim(f,c))+(sin(b));
b=log10(f);
g=cos(f);
a=(cos(b))-(fdim(b,e));
}