#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(d,a);
c=fdim(d,a);
b=(atan2(a,e))/(sqrt(c));
d=fmin(a,e);
a=sqrt(c);
d=(acos(e))+(pow(e,b));
b=atan2(d,b);
e=fmax(d,d);
b=(fmax(d,e))-(floor(d));
c=exp(a);
e=fdim(c,a);
c=(floor(c))+(fmax(d,c));
}