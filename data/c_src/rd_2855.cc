#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(d,a))*(atan(d));
b=floor(a);
b=fmax(f,a);
if(isless(a,d)){
c=(sqrt(c))/(fmax(b,e));
e=(atan(a))/(atan2(c,a));
c=log(e);
b=fmin(c,c);
}
if(isgreaterequal(a,f)){
f=fmax(g,f);
d=exp(f);
e=(sin(c))*(asin(a));
f=floor(e);
}
else{
g=(fmax(g,g))-(pow(c,f));
b=cos(g);
c=(fmin(a,e))*(atan2(a,a));
f=ceil(b);
e=(cos(c))-(fmax(e,e));
}
}