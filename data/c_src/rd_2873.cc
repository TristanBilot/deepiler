#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fdim(a,d))-(fmin(b,a));
c=fmax(b,e);
g=atan(g);
a=atan2(f,d);
f=(pow(e,f))/(fmax(g,a));
if(isless(f,b)){
d=(fmax(c,d))/(fmin(d,a));
g=(pow(g,b))*(sqrt(g));
a=log10(a);
d=(pow(g,f))+(fdim(b,e));
e=(fmax(g,d))+(floor(f));
}
}