#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(c,h))/(cos(h));
g=(atan2(h,d))/(log(d));
e=sin(c);
c=pow(a,e);
d=fdim(f,b);
c=(cos(b))/(pow(b,f));
d=(atan2(g,f))-(fdim(e,b));
e=atan2(h,f);
d=(fmin(f,d))-(tan(g));
d=fmax(a,b);
a=ceil(h);
b=fmax(a,e);
h=fdim(d,h);
}