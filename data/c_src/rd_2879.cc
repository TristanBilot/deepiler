#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(log(g))+(exp(a));
e=(fmin(f,f))+(log(a));
d=(ceil(f))+(fmax(g,g));
f=(asin(g))-(log(d));
c=(log10(b))-(atan2(d,f));
a=(fdim(d,c))*(atan(g));
b=fmax(c,c);
b=(fmin(c,d))+(pow(c,g));
a=(atan2(d,e))+(pow(f,g));
if(isless(f,a)){
e=pow(f,d);
d=fdim(e,g);
}
}