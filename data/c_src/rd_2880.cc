#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,d);
e=fmax(c,d);
while(islessgreater(b,a)){
a=(tan(b))-(log(b));
a=fdim(c,c);
c=(sin(a))*(fmax(d,b));
}
while(islessgreater(c,b)){
e=(fmin(b,c))*(atan2(c,e));
a=pow(c,a);
e=sin(e);
}
}