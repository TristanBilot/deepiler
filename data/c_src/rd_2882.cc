#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(c,a);
a=cos(a);
while(islessequal(a,d)){
a=acos(d);
a=atan(e);
b=(fmax(e,e))+(atan(c));
b=(atan(a))/(exp(b));
e=(fmax(c,b))-(atan2(a,d));
}
while(isgreaterequal(c,b)){
b=fdim(c,b);
d=(exp(a))/(pow(d,e));
c=tan(b);
b=(fdim(b,a))/(sqrt(c));
}
}