#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=tan(c);
b=log10(a);
e=fmax(d,f);
f=fmin(a,b);
a=(tan(d))-(tan(b));
while(islessgreater(f,e)){
b=(fmin(e,f))-(fdim(a,d));
d=(atan2(c,b))*(log(c));
d=fmax(f,a);
c=(atan2(e,f))/(atan2(a,e));
}
d=(sin(b))/(log(c));
b=fdim(b,f);
}