#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log(g))/(exp(h));
b=(asin(f))*(fmax(c,g));
f=(fdim(c,c))/(pow(g,h));
d=cos(e);
b=acos(b);
d=exp(b);
d=pow(c,a);
a=fmax(b,g);
if(isgreaterequal(b,a)){
e=exp(a);
g=(fdim(c,g))-(fmax(g,c));
d=fmax(f,e);
f=(fdim(h,d))*(fmin(a,d));
}
else{
d=fmin(c,h);
b=(fmin(e,g))-(atan2(g,f));
g=(fmin(g,c))+(pow(d,f));
e=fdim(a,e);
a=(fmin(c,e))/(fmax(g,e));
}
}