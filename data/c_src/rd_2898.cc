#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(f);
c=sin(d);
c=(atan2(a,c))+(acos(g));
f=floor(a);
b=(fmax(h,b))*(fmax(d,g));
while(isless(f,g)){
d=atan2(h,f);
b=fmax(d,f);
b=(log(f))*(fdim(g,g));
c=floor(e);
}
if(islessgreater(h,c)){
b=(fdim(d,a))+(fmax(a,g));
g=(floor(e))-(fdim(h,e));
a=fmax(g,f);
e=(cos(d))-(floor(c));
}
}