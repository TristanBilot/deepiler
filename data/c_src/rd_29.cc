#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(floor(d))/(fmin(b,g));
g=(log10(d))/(acos(f));
e=(log10(a))/(fmin(e,a));
d=(pow(e,g))/(pow(c,d));
d=fmax(c,e);
e=log10(f);
g=fdim(a,a);
b=(fmax(b,e))+(exp(g));
g=(tan(c))+(cos(f));
if(islessgreater(d,b)){
f=(sin(a))+(atan2(e,b));
f=fdim(d,b);
c=atan(b);
b=atan2(b,f);
g=(log10(b))+(atan2(b,d));
}
}