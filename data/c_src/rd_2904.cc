#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=acos(b);
a=(fmax(e,b))-(exp(a));
while(islessequal(a,b)){
d=(floor(b))*(fdim(d,e));
c=(fdim(a,a))+(log(a));
b=(fdim(d,e))-(fmax(d,b));
}
if(islessgreater(e,b)){
b=fmin(a,e);
a=(floor(d))-(ceil(a));
c=(log(b))+(fmax(c,d));
}
else{
e=sqrt(a);
e=(atan(b))+(fmin(c,b));
a=(tan(c))*(pow(c,c));
a=fmin(e,d);
b=(asin(b))+(ceil(d));
}
}