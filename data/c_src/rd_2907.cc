#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(c);
c=floor(h);
if(islessequal(h,b)){
d=atan2(e,d);
f=(fdim(e,e))-(log10(c));
d=fmax(a,c);
c=ceil(h);
d=fmin(b,b);
}
else{
d=ceil(a);
c=(fmax(f,b))/(fmin(h,f));
}
a=(fmin(g,h))*(fmin(b,a));
e=(fmax(b,c))*(ceil(f));
}