#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(f);
b=tan(g);
b=(ceil(h))*(log10(d));
e=fdim(c,f);
c=atan(a);
if(isgreaterequal(c,g)){
b=atan2(a,c);
d=atan(e);
g=(pow(b,h))*(sqrt(c));
f=fdim(f,a);
a=fmax(h,c);
}
d=fmax(e,g);
d=atan2(d,g);
b=pow(b,f);
}