#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(b,g);
c=(fdim(g,h))+(tan(d));
e=(sqrt(g))+(exp(f));
while(isless(f,g)){
g=(floor(e))+(atan2(a,b));
f=atan(c);
c=(fmax(c,a))*(floor(f));
}
f=atan2(e,c);
h=sqrt(b);
}