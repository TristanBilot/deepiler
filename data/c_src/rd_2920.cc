#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan(b);
c=fmax(f,b);
if(islessequal(e,d)){
b=asin(f);
c=(ceil(d))+(atan2(a,e));
}
else{
d=log10(d);
d=(fmin(d,b))/(fmin(b,d));
f=(fmin(e,g))/(tan(b));
}
if(islessgreater(a,c)){
b=asin(c);
e=pow(a,b);
}
else{
g=(pow(g,e))/(fmin(g,d));
c=(ceil(b))*(exp(e));
}
}