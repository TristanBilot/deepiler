#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(tan(a))-(log10(e));
c=(floor(d))*(exp(d));
d=(fmax(b,g))/(atan(d));
c=(fmax(g,f))-(fmax(f,b));
b=atan(b);
e=fmax(b,b);
g=floor(b);
d=floor(c);
g=(atan2(f,e))/(floor(d));
b=(atan2(b,g))/(fdim(c,b));
f=fmin(a,c);
c=log10(a);
a=(sqrt(f))/(fmax(g,g));
}