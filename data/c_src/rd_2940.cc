#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(asin(g))+(tan(f));
b=(fmin(g,c))*(tan(c));
f=(log(d))+(sin(f));
d=atan2(a,e);
h=(atan2(h,h))-(pow(f,e));
if(isgreaterequal(a,e)){
a=fmax(a,g);
a=exp(b);
g=(pow(a,b))/(fmin(e,e));
b=pow(c,a);
c=acos(g);
}
else{
f=(asin(f))/(atan2(d,g));
h=atan2(f,g);
f=(tan(b))/(exp(f));
}
if(isless(h,e)){
e=(acos(f))/(acos(e));
e=(pow(h,g))+(log(d));
b=pow(b,e);
}
else{
c=(fdim(c,b))*(fmin(b,g));
f=fmax(b,a);
h=exp(f);
e=atan2(a,d);
}
}