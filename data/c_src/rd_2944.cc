#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(d,g))*(atan2(b,g));
g=(tan(b))+(exp(e));
if(islessgreater(a,g)){
c=fdim(b,f);
c=atan2(d,f);
f=(fmax(c,b))-(asin(d));
a=(pow(e,f))+(fdim(d,f));
}
else{
c=asin(a);
c=(sqrt(f))+(fmax(e,e));
b=(pow(f,g))-(tan(d));
e=fmin(f,d);
b=pow(f,b);
}
if(islessequal(e,g)){
a=tan(b);
f=(fmin(g,a))+(fmax(a,d));
a=(sin(d))*(tan(f));
e=pow(a,b);
d=ceil(f);
}
else{
f=(fmax(f,g))+(log(e));
a=(fmin(f,f))-(exp(d));
c=fmax(c,b);
}
}