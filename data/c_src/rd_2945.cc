#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(b,e))*(fmin(b,b));
b=floor(c);
c=(sin(c))*(fmin(c,e));
e=(pow(a,d))*(fdim(d,d));
e=(fmax(e,b))*(fdim(d,e));
if(islessgreater(c,a)){
d=(log10(b))/(fdim(e,c));
c=fmax(e,e);
a=fmin(b,e);
}
else{
c=cos(c);
e=fdim(e,c);
e=(exp(a))*(fmin(e,a));
b=fdim(b,c);
}
while(islessequal(e,d)){
a=(ceil(b))-(fmax(e,e));
a=(fmin(b,d))+(exp(a));
}
}