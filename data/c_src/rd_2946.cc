#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(exp(b))/(pow(d,e));
c=fmax(c,b);
c=ceil(a);
b=ceil(e);
c=fdim(c,c);
while(islessequal(d,a)){
c=(atan2(d,c))+(fmax(a,c));
c=(sqrt(c))*(log(b));
}
if(islessequal(e,e)){
b=(sqrt(c))-(floor(a));
b=fmax(a,d);
}
}