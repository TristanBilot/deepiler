#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=sin(c);
f=(log(d))*(ceil(c));
if(islessequal(a,d)){
e=(asin(e))-(fmax(a,a));
e=(fdim(a,e))+(cos(a));
}
else{
c=(fdim(a,b))/(fmax(c,f));
f=(log(f))*(fdim(d,c));
f=(asin(e))-(log(a));
c=(fmin(a,f))*(ceil(a));
c=(sqrt(c))*(atan2(e,d));
}
if(islessequal(f,b)){
f=fdim(b,a);
f=fdim(c,a);
a=(exp(c))*(log10(d));
}
else{
d=(fmax(f,b))-(atan2(b,c));
d=atan(b);
a=fmax(d,d);
e=asin(f);
d=(log10(a))+(sin(f));
}
}