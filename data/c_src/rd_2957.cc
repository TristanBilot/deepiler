#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(pow(c,f))+(sin(f));
c=pow(b,e);
c=(exp(b))+(atan(c));
if(islessequal(g,d)){
a=acos(b);
c=(atan2(c,g))-(exp(d));
c=fmax(a,e);
g=exp(b);
f=floor(c);
}
else{
b=fmin(c,b);
d=(fmax(e,f))-(pow(c,e));
a=(asin(c))*(atan2(e,d));
c=(fmin(c,e))*(floor(c));
f=(tan(c))-(floor(f));
}
g=pow(d,d);
b=(fmax(f,f))-(fmax(a,e));
}