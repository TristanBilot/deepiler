#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(b,c);
d=fmax(e,f);
e=fdim(f,e);
e=pow(f,a);
while(isgreaterequal(d,b)){
d=floor(c);
d=fmin(c,f);
c=(fmax(b,e))+(pow(c,e));
a=(log(c))/(log10(f));
b=(fdim(d,f))*(pow(b,a));
}
if(islessequal(a,c)){
e=(log10(f))+(tan(b));
a=(fmin(a,a))+(log(a));
b=(fdim(c,a))+(pow(b,e));
}
}