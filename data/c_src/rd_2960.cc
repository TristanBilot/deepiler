#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(c,b))+(pow(f,c));
c=fdim(d,e);
d=atan2(e,d);
if(islessgreater(b,f)){
d=(sin(b))*(floor(d));
b=log10(c);
f=(ceil(d))-(cos(e));
f=(sqrt(f))-(atan2(b,b));
}
else{
b=(exp(c))+(log10(a));
c=(atan2(e,e))-(fdim(f,d));
c=fdim(f,b);
f=(tan(d))+(acos(a));
}
if(isless(e,a)){
f=(exp(f))-(fdim(b,b));
b=pow(c,c);
e=(fdim(d,d))+(sqrt(c));
}
else{
d=cos(b);
b=(fdim(a,a))/(pow(d,b));
f=(atan2(a,e))/(floor(d));
}
}