#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(b);
b=(asin(a))/(fmax(b,a));
e=(exp(e))+(pow(b,c));
e=log10(d);
if(islessequal(b,a)){
c=exp(b);
d=(ceil(d))+(fmax(e,b));
}
}