#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(acos(f))/(log(f));
a=atan(g);
a=(fmin(a,c))/(fdim(e,a));
a=atan(f);
f=ceil(a);
if(isless(c,a)){
c=sqrt(b);
a=floor(a);
}
while(islessequal(d,d)){
a=(fmax(g,b))*(floor(d));
g=fmax(c,g);
}
}