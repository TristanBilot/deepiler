#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(c,b);
d=fmin(h,f);
h=(fdim(b,c))*(exp(f));
g=sqrt(d);
b=(sin(a))*(log(e));
if(isless(d,f)){
h=(acos(b))*(fmin(b,d));
b=(fmin(e,f))*(fmin(b,e));
a=fmax(f,h);
a=(sin(e))-(atan(a));
d=(atan2(e,e))*(exp(h));
}
else{
a=fmin(g,b);
c=ceil(b);
a=tan(h);
}
c=(cos(h))-(acos(f));
c=(ceil(d))/(ceil(b));
g=(fmin(e,g))-(pow(g,b));
e=atan(h);
d=(fdim(a,e))-(atan2(h,b));
}