#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(acos(d))/(floor(b));
e=fmax(e,c);
d=(pow(h,h))/(atan2(a,h));
h=(fmax(f,d))*(atan2(f,f));
d=ceil(h);
if(islessequal(d,h)){
h=(atan2(g,h))-(pow(g,b));
f=(cos(f))*(log10(c));
f=pow(g,b);
}
h=log(c);
d=(floor(b))/(log10(b));
a=(log10(c))*(log10(b));
d=(log(e))*(sin(c));
}