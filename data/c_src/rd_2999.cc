#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(floor(d))*(tan(d));
d=(fmax(e,g))*(fmax(e,g));
d=exp(c);
h=exp(c);
c=(atan2(e,d))+(exp(g));
c=(atan2(g,d))/(pow(g,a));
a=floor(f);
h=log10(d);
d=sqrt(e);
if(islessequal(f,d)){
a=(cos(f))/(fdim(g,g));
h=asin(b);
b=(fmin(b,a))/(pow(g,d));
}
else{
f=(pow(a,g))+(pow(d,c));
c=(fmin(a,e))/(fdim(g,g));
a=atan2(h,b);
f=atan2(a,d);
a=(atan2(d,a))+(log10(a));
}
}