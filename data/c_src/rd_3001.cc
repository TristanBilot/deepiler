#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(exp(d))-(atan2(c,a));
a=(pow(d,a))*(fmax(g,g));
f=(exp(f))+(atan2(a,e));
f=(tan(f))*(atan2(d,d));
while(isless(f,c)){
b=(fmax(f,d))+(ceil(e));
f=fdim(a,a);
}
if(isless(b,a)){
c=fmax(b,a);
d=(pow(c,f))/(fmax(g,a));
}
}