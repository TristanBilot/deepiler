#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(a,g))/(atan(f));
g=(floor(c))+(fmax(e,c));
c=(pow(f,d))*(tan(c));
c=tan(b);
e=sin(d);
if(islessgreater(d,f)){
d=fdim(b,g);
f=fmax(g,b);
g=fmax(a,a);
d=(fdim(c,c))/(fmin(g,c));
g=pow(d,a);
}
if(islessgreater(e,g)){
a=fmin(d,e);
b=(atan(a))-(exp(c));
b=sqrt(e);
g=(log(g))+(fmin(g,f));
}
else{
c=asin(e);
f=(fdim(e,e))/(fdim(d,c));
}
}