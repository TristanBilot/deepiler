#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=floor(d);
e=(fmin(d,d))-(atan(d));
b=(atan(e))/(asin(a));
b=(fmin(d,d))*(asin(d));
c=floor(d);
a=cos(d);
c=pow(c,a);
d=(acos(a))*(fmax(b,a));
e=tan(b);
if(isless(a,e)){
a=(fdim(c,e))+(pow(d,a));
a=(atan(a))*(fmax(b,b));
c=(fdim(b,c))+(log10(b));
}
else{
a=(pow(a,c))-(pow(e,c));
e=fmax(c,a);
}
}