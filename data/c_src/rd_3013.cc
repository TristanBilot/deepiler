#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(a,e);
a=fdim(d,f);
b=cos(a);
d=cos(f);
if(islessequal(e,c)){
f=(asin(e))/(fmin(a,f));
c=fmin(c,b);
a=atan2(c,a);
}
if(islessequal(e,c)){
e=fmin(e,b);
f=(pow(a,a))/(exp(b));
}
else{
b=(atan2(d,c))/(asin(c));
d=cos(f);
}
}