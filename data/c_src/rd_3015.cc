#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan(b);
d=pow(a,a);
if(islessgreater(b,c)){
c=tan(a);
d=(sqrt(a))-(fmin(e,d));
a=pow(b,c);
c=ceil(e);
c=sqrt(c);
}
while(islessgreater(b,c)){
b=(atan2(c,c))/(fmax(a,b));
e=atan2(e,d);
a=(sqrt(c))+(atan2(c,a));
}
}