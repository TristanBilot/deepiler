#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log(d))*(cos(b));
e=asin(c);
if(isgreaterequal(d,b)){
a=(tan(c))+(exp(e));
c=fmin(e,c);
e=(pow(e,b))*(ceil(b));
}
else{
e=pow(d,a);
b=(sqrt(b))/(fmin(a,b));
d=fdim(a,a);
}
a=(floor(a))+(tan(c));
b=(exp(c))-(fmax(e,a));
a=pow(d,a);
}