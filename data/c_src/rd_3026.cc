#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(d,c);
f=cos(h);
b=atan2(a,f);
d=floor(f);
e=floor(a);
a=sin(a);
a=pow(d,c);
e=(fmax(d,b))/(fmax(e,b));
while(islessequal(e,a)){
b=(fmin(e,d))+(fdim(c,h));
b=(pow(d,g))*(pow(a,f));
d=(exp(e))/(atan(h));
a=fmax(d,d);
}
}