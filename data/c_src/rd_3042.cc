#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(cos(e))*(tan(e));
d=(pow(d,b))+(fmax(f,f));
c=cos(b);
while(isgreaterequal(b,c)){
b=(fmax(c,d))/(fmax(a,h));
a=(sqrt(b))-(cos(h));
g=atan2(c,b);
d=fdim(h,h);
f=pow(f,e);
}
if(isgreaterequal(g,a)){
d=(fmin(e,d))-(floor(e));
e=tan(h);
e=(cos(c))*(fdim(d,c));
c=atan2(c,c);
}
}