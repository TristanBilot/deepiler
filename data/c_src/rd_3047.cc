#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=cos(d);
c=ceil(b);
d=sqrt(b);
while(isless(d,c)){
e=ceil(a);
a=(atan(c))+(tan(c));
a=ceil(e);
}
if(isless(b,d)){
e=ceil(e);
d=fmin(d,d);
}
else{
a=fmax(e,c);
e=(ceil(b))-(sin(d));
b=(log(d))+(acos(b));
}
}