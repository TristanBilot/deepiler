#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(d,h))-(pow(b,c));
a=atan2(a,f);
c=(fmax(h,d))*(asin(b));
if(isless(g,b)){
e=(log(b))*(sin(c));
c=fmin(f,a);
c=pow(d,f);
}
else{
b=(fmax(a,h))+(fdim(d,c));
b=pow(a,e);
g=fmin(g,e);
g=atan2(e,h);
e=fmax(c,a);
}
if(islessgreater(f,e)){
b=log(d);
g=log(g);
h=fmin(d,d);
e=(fmax(f,a))/(log10(c));
f=fmin(h,c);
}
else{
g=pow(g,a);
b=(cos(a))+(fmax(b,f));
h=pow(d,d);
g=cos(e);
}
}