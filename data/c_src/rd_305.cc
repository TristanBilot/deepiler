#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(asin(g))/(log(d));
a=fdim(h,d);
while(islessequal(g,b)){
d=acos(b);
g=(fdim(c,g))*(fdim(d,d));
b=acos(f);
}
if(islessgreater(d,h)){
h=(fdim(f,f))*(floor(g));
e=(fdim(a,g))+(atan2(b,a));
g=(fmax(e,d))/(atan(c));
d=(tan(a))-(floor(b));
}
else{
h=pow(f,f);
d=(fmin(d,d))*(asin(g));
b=(ceil(b))-(atan2(b,h));
g=(floor(f))+(exp(c));
g=(fmax(e,e))*(tan(f));
}
}