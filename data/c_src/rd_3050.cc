#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(a,d);
c=(fmin(a,c))*(floor(c));
e=cos(a);
d=exp(c);
if(islessequal(e,a)){
e=(acos(e))*(log(e));
a=(floor(c))*(fmax(c,a));
}
else{
c=(tan(f))-(exp(c));
b=(ceil(f))/(sin(e));
e=(fmax(a,f))/(atan2(f,f));
}
while(isgreaterequal(b,d)){
f=log(f);
d=(sin(a))/(cos(d));
d=(exp(f))-(floor(f));
b=(atan(b))+(cos(f));
}
}