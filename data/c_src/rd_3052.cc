#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(atan(c))/(tan(e));
f=exp(b);
c=(fdim(e,f))-(acos(h));
c=(log(c))-(log10(d));
e=pow(c,h);
d=(sqrt(g))-(atan2(b,d));
if(isless(b,e)){
h=fmin(f,f);
f=(log(e))-(fmin(a,e));
g=fdim(f,b);
f=(ceil(d))*(log(c));
}
else{
a=ceil(f);
e=sin(a);
h=fmax(a,e);
g=fmax(a,b);
g=ceil(b);
}
}