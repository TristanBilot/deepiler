#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(d,c))-(ceil(a));
b=sin(f);
d=log(e);
d=(sqrt(f))+(sqrt(a));
while(isgreaterequal(a,d)){
b=exp(a);
f=(fmin(f,e))*(ceil(f));
a=atan2(f,a);
b=(ceil(b))*(log10(c));
f=fdim(a,b);
}
while(islessgreater(b,b)){
d=(exp(a))-(fdim(a,c));
c=sin(a);
a=(fdim(b,c))*(pow(c,b));
e=(exp(f))+(pow(e,f));
d=sin(e);
}
}