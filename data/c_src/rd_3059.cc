#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(e,c))*(cos(e));
a=fmax(c,a);
d=fdim(e,a);
f=(fmin(d,c))+(fmax(f,a));
while(islessgreater(b,a)){
f=(asin(c))-(log10(e));
f=ceil(d);
}
e=(tan(c))*(fmax(f,b));
f=floor(c);
c=acos(f);
a=(fmin(a,c))-(acos(b));
a=log10(d);
}