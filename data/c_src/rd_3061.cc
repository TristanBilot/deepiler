#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(c,c))+(fmax(d,e));
a=atan2(a,e);
e=cos(a);
b=(fdim(c,e))/(pow(c,e));
a=acos(c);
if(isgreaterequal(c,c)){
c=(fdim(a,b))+(pow(d,d));
c=fmin(e,d);
a=atan2(d,c);
b=(fdim(b,d))*(fdim(c,b));
c=log10(d);
}
if(isgreaterequal(c,c)){
d=(fdim(e,b))*(fmax(e,d));
c=(cos(c))+(pow(a,d));
b=(fdim(e,a))*(atan2(e,e));
a=pow(a,c);
}
}