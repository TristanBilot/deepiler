#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log10(d))*(fdim(b,b));
e=(fmax(a,d))/(exp(d));
d=(fdim(b,e))+(tan(d));
while(islessequal(e,d)){
b=atan2(d,d);
b=pow(c,b);
}
while(isless(d,d)){
a=(pow(c,d))-(atan(b));
c=(acos(b))/(fdim(e,e));
b=ceil(e);
}
}