#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(e,b))-(atan(g));
b=(log10(e))*(fmax(h,a));
b=fmax(b,c);
h=sqrt(a);
a=fmax(g,f);
h=atan2(g,e);
g=fmax(g,g);
a=(pow(b,c))+(acos(d));
h=(fmax(f,d))/(fdim(d,a));
while(islessgreater(f,d)){
d=(fmin(d,a))+(atan2(g,e));
d=(atan(c))-(pow(c,b));
e=(tan(f))*(atan2(c,d));
}
}