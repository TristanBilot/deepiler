#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(atan2(f,c))-(log(b));
g=cos(d);
d=cos(b);
if(isless(g,f)){
g=(atan2(c,b))*(atan2(a,e));
g=(pow(d,e))+(pow(g,e));
g=tan(b);
a=atan2(g,g);
a=(log10(d))-(asin(g));
}
else{
c=(fmax(b,e))*(fmin(d,g));
e=(atan2(e,g))-(fmax(e,e));
g=cos(g);
}
while(isless(c,e)){
f=(floor(g))/(fmin(b,b));
f=fmin(f,e);
a=pow(c,e);
c=(fmin(b,a))-(log(b));
b=(sqrt(e))-(fdim(f,c));
}
}