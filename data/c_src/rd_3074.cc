#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(a);
d=(log10(c))-(log(a));
h=(cos(b))-(cos(d));
f=cos(h);
while(islessequal(c,f)){
g=(fmin(h,b))/(acos(e));
a=(fmin(e,a))-(fmin(a,a));
e=(asin(b))+(tan(f));
e=(fmin(f,a))-(floor(g));
}
while(islessequal(f,h)){
f=(pow(f,d))-(fmin(a,f));
f=exp(g);
h=log(b);
}
}