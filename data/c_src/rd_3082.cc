#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(e,b);
e=(atan2(b,d))+(sqrt(b));
c=log(c);
a=(cos(a))+(fmin(a,c));
e=fmax(c,e);
if(isless(e,e)){
d=(acos(c))+(fdim(d,e));
c=(pow(d,c))+(exp(a));
d=(atan2(b,c))/(atan(c));
}
b=fmax(a,e);
b=tan(c);
c=fmax(a,b);
c=(fmin(c,c))-(fdim(d,b));
}