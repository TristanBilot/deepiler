#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(g,d);
g=atan2(a,d);
a=atan2(g,e);
c=(exp(a))/(fmax(e,h));
e=log(c);
g=atan2(b,h);
f=exp(f);
g=(tan(e))-(log10(h));
c=(fdim(h,e))-(log(g));
a=fmax(h,c);
g=(atan2(e,h))+(fmax(c,g));
c=(log10(g))*(fdim(h,e));
}