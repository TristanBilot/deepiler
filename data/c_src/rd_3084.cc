#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(b))/(fmax(b,d));
b=sin(b);
e=acos(b);
while(islessequal(c,b)){
b=acos(c);
e=pow(e,a);
a=pow(d,c);
}
while(islessgreater(e,a)){
e=(fmin(b,e))-(fdim(d,e));
d=(pow(d,e))/(atan(b));
}
}