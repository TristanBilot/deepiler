#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(c,g))*(sin(f));
b=fdim(g,c);
a=pow(g,f);
if(isgreaterequal(d,f)){
e=fdim(c,a);
e=(fdim(f,b))-(fmax(d,d));
e=floor(b);
}
else{
d=(atan2(g,b))-(fdim(a,e));
a=(fmax(d,a))/(pow(f,f));
}
if(isless(b,a)){
c=(exp(e))+(atan(f));
b=tan(a);
g=floor(a);
f=floor(d);
b=(cos(c))*(tan(d));
}
else{
b=sin(f);
a=fmax(f,g);
b=(exp(f))/(pow(f,c));
}
}