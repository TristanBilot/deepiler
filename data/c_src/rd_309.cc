#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(c);
f=asin(f);
a=fmin(f,h);
g=(fmax(c,b))-(exp(h));
h=(acos(c))*(log10(e));
f=fdim(b,a);
d=(asin(e))/(tan(c));
}