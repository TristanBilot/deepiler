#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan(d))+(exp(f));
e=atan2(a,b);
c=atan2(h,f);
if(isgreaterequal(a,f)){
b=(fmax(c,h))-(atan2(e,a));
b=pow(a,e);
a=(pow(f,h))-(atan2(c,h));
}
e=atan2(b,h);
f=(atan2(e,f))/(atan(f));
h=fmax(a,h);
g=floor(b);
}