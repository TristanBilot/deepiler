#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(d,a);
c=ceil(e);
d=fmin(a,b);
a=(cos(a))+(sin(b));
while(islessequal(b,a)){
e=pow(c,e);
b=(atan2(a,d))/(acos(c));
a=fmin(a,e);
c=(floor(d))-(fmax(b,d));
e=sqrt(d);
}
while(islessequal(b,e)){
c=(ceil(c))+(acos(a));
a=(floor(a))-(fmax(e,b));
b=acos(a);
a=pow(e,e);
d=pow(c,b);
}
}