#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(cos(b))-(atan2(d,e));
e=(sin(g))*(fmax(g,a));
c=(fdim(e,g))+(atan2(c,f));
d=(log(g))-(tan(b));
g=pow(e,f);
g=(log10(a))-(fmin(g,c));
c=(atan(e))*(tan(e));
b=(pow(e,e))*(atan2(a,g));
f=(fmax(e,e))+(tan(e));
f=pow(e,d);
while(islessequal(b,f)){
f=(sin(g))-(tan(g));
a=(tan(d))-(sin(c));
}
}