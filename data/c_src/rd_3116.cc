#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=cos(d);
d=pow(e,f);
f=pow(f,d);
while(isless(a,b)){
d=fmin(b,a);
f=pow(e,f);
}
if(islessequal(d,d)){
f=(floor(c))/(cos(e));
e=fmin(f,e);
a=sqrt(c);
f=(asin(d))+(atan2(d,c));
}
else{
d=log(d);
f=ceil(a);
f=fmin(d,b);
e=atan2(b,c);
e=fmax(e,c);
}
}