#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(a,a))+(fmax(d,a));
d=(fmax(d,b))+(fmax(a,a));
if(islessgreater(a,e)){
b=(pow(c,b))+(exp(d));
d=(acos(c))+(log(a));
e=(exp(b))-(asin(c));
b=(atan2(e,c))+(sqrt(d));
a=(fdim(a,d))+(exp(c));
}
else{
a=(log(b))+(sin(c));
a=fmax(d,b);
}
while(islessgreater(d,e)){
d=(fdim(d,c))/(pow(a,b));
d=(pow(b,c))-(atan2(c,e));
}
}