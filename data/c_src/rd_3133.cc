#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan(e);
a=(atan(d))*(sqrt(b));
a=asin(e);
if(isgreaterequal(b,f)){
b=pow(d,b);
e=acos(d);
d=log10(d);
e=fmin(a,b);
f=fmax(f,d);
}
f=(log10(a))*(fmax(f,b));
c=sin(f);
}