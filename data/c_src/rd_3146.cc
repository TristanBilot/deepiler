#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(ceil(c))*(asin(c));
f=(tan(f))/(atan2(f,b));
f=(pow(d,d))*(atan2(c,b));
if(isless(d,c)){
a=fmax(c,d);
f=(acos(f))/(fmax(a,e));
}
else{
e=(pow(d,d))*(sin(d));
d=(fmax(d,d))+(sqrt(b));
c=(asin(c))+(sqrt(d));
d=(pow(b,a))-(atan2(d,b));
f=(acos(f))*(sqrt(f));
}
d=(acos(b))-(atan(b));
b=(log10(b))-(atan(b));
d=(pow(f,e))-(fdim(a,f));
c=fmin(d,d);
}