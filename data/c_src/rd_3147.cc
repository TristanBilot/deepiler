#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log(c);
a=pow(d,d);
d=(fmax(d,a))+(atan2(c,c));
d=(fdim(e,b))*(acos(c));
e=(fmin(d,a))*(ceil(e));
c=(exp(b))-(exp(d));
b=ceil(b);
b=(fmin(c,a))-(fmin(a,e));
if(islessequal(b,e)){
d=(log(a))+(exp(b));
c=fmin(b,a);
}
}