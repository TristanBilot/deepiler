#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sqrt(b))+(tan(c));
c=log(b);
c=(atan(e))*(fmax(d,c));
b=(atan2(d,d))*(fmin(e,a));
a=(cos(a))+(fdim(b,c));
d=pow(a,b);
c=(atan(d))/(fmin(c,c));
b=log10(a);
d=(exp(e))/(fmin(c,d));
c=fdim(d,d);
}