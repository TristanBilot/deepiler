#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan(c))-(fmin(e,b));
d=fdim(c,c);
a=cos(e);
b=(sin(e))/(fmax(a,e));
while(islessgreater(d,b)){
e=(fdim(a,d))/(tan(e));
d=fdim(c,d);
}
while(isless(c,e)){
a=(acos(b))-(sqrt(c));
d=atan2(a,e);
d=(log(e))/(fmax(e,c));
c=(fdim(c,c))+(log(c));
}
}