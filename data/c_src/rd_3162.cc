#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(b,f);
d=(fmin(f,a))/(atan2(g,d));
b=(fmin(e,b))/(acos(b));
while(islessgreater(d,c)){
d=fmax(g,a);
a=(pow(f,b))+(atan2(e,g));
b=(acos(f))-(atan2(e,a));
a=(sqrt(g))-(atan(e));
b=(fmax(f,c))+(fmin(g,c));
}
d=log10(a);
e=pow(d,f);
a=(acos(e))-(atan2(b,b));
}