#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fmax(g,b);
d=pow(f,g);
a=log(g);
c=(fmin(g,b))-(log(c));
e=log(d);
d=floor(f);
a=(tan(g))*(exp(g));
a=sqrt(g);
g=fmax(c,c);
c=(atan2(b,d))/(cos(g));
e=atan2(b,d);
g=(log10(g))-(floor(b));
}