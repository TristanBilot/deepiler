#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(d);
d=fdim(g,f);
h=(fdim(e,c))/(sqrt(h));
a=pow(c,f);
e=(fdim(f,c))/(fmax(e,g));
if(islessequal(e,a)){
c=(sqrt(c))/(pow(c,h));
a=acos(d);
}
d=(fmax(c,d))/(pow(d,c));
d=sin(f);
h=(atan2(d,a))/(fmax(h,f));
}