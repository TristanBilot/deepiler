#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(d,b))*(sqrt(c));
e=fmin(f,e);
b=(log10(a))-(cos(g));
c=(fmin(d,h))*(fdim(c,g));
c=fdim(b,h);
e=tan(h);
g=(fdim(a,g))/(fdim(c,d));
h=fdim(a,d);
h=sqrt(h);
a=(floor(c))+(ceil(h));
e=(fmax(e,c))/(fmin(h,a));
f=(atan2(g,d))-(exp(b));
}