#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(b))+(atan2(b,b));
e=sin(d);
c=fmax(d,a);
d=atan2(a,d);
c=(log10(d))*(sin(a));
d=(asin(a))/(log(a));
b=(acos(e))+(cos(d));
a=fmin(b,a);
c=fmin(c,e);
d=atan2(d,c);
c=(atan2(d,b))-(fdim(b,b));
b=fmin(a,a);
e=(exp(c))-(atan(e));
}