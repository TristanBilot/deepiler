#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(floor(g))*(fdim(f,b));
d=(log(c))-(sqrt(f));
a=(pow(b,g))*(atan(e));
d=(sqrt(b))/(tan(c));
c=exp(f);
a=(ceil(a))*(atan2(g,c));
d=(atan(g))*(pow(b,b));
g=asin(c);
f=(pow(b,a))/(atan2(e,f));
c=(fmax(c,e))*(fmax(d,a));
}