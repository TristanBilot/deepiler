#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(h,d);
a=asin(f);
b=(pow(f,a))+(pow(f,c));
f=log10(h);
d=tan(g);
while(isless(a,f)){
f=acos(d);
g=(fdim(e,c))*(fmax(g,g));
d=(sqrt(c))+(pow(h,g));
g=(pow(g,f))-(exp(b));
}
d=fmax(f,b);
c=(fdim(h,b))-(tan(e));
g=(sqrt(a))/(pow(e,b));
}