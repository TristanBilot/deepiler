#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(d,b))/(ceil(f));
f=(atan2(b,b))+(ceil(b));
c=cos(f);
d=fmin(e,a);
a=(fmin(b,f))/(fmin(b,b));
f=(fmax(a,d))*(fmin(e,e));
f=asin(a);
b=(ceil(e))/(asin(c));
c=atan2(d,f);
e=fdim(f,f);
d=log(d);
}