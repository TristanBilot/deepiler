#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(f,b);
a=(fmax(c,a))/(fmin(f,d));
d=(asin(c))/(pow(b,e));
a=fmin(a,f);
d=fmin(b,e);
if(isgreaterequal(e,d)){
d=log10(d);
d=exp(a);
f=atan(e);
}
else{
a=ceil(b);
e=fdim(f,b);
}
if(islessgreater(d,c)){
a=(sin(a))*(log10(d));
d=(atan2(d,e))*(fmin(e,e));
c=pow(b,b);
}
}