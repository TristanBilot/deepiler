#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(e,e))/(fmax(e,a));
c=fmin(e,a);
d=(fdim(b,a))+(exp(a));
c=fmin(d,d);
e=fmax(b,d);
d=log(e);
e=(fmin(c,c))+(fmin(c,d));
b=acos(c);
}