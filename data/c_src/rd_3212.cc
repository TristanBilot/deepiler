#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log10(c))*(fmin(e,a));
b=(atan2(a,b))*(atan2(c,c));
b=pow(a,b);
a=(fmax(b,d))+(log10(b));
a=fmin(d,d);
c=(floor(e))-(pow(c,e));
while(islessgreater(a,d)){
d=(tan(b))+(tan(a));
a=(atan(e))-(asin(b));
d=fmin(e,a);
a=(pow(c,c))*(acos(e));
e=(pow(e,a))+(pow(e,d));
}
}