#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(b,c);
f=pow(f,d);
b=acos(f);
a=floor(d);
d=(pow(d,d))+(atan2(b,c));
if(isless(a,e)){
e=(pow(a,c))+(atan(c));
c=(atan2(a,b))*(fdim(a,f));
a=acos(b);
}
else{
c=fmax(e,b);
d=(pow(a,e))*(log(a));
}
while(islessgreater(e,a)){
d=acos(e);
c=fmax(a,d);
d=floor(a);
e=(fdim(f,b))+(floor(a));
a=fmin(d,b);
}
}