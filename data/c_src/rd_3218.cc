#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(c,e))/(fmin(d,b));
a=(atan2(c,b))*(fmax(a,b));
f=(fmin(d,d))/(tan(d));
e=fdim(e,e);
a=fmax(c,d);
e=(tan(e))+(fmin(f,b));
f=(acos(b))*(floor(d));
c=(fmin(a,e))-(fmax(d,c));
while(islessequal(d,e)){
d=(fdim(f,a))/(tan(f));
b=cos(a);
d=cos(f);
a=(sqrt(e))-(fmax(f,c));
c=(fmin(e,d))/(fdim(f,a));
}
}