#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(e,d))/(floor(c));
d=(cos(c))+(pow(b,d));
d=(tan(a))+(fdim(d,e));
d=(cos(d))/(sqrt(b));
while(isless(c,e)){
a=(log10(b))-(acos(b));
b=(fmax(a,e))-(fmax(b,c));
e=(sqrt(d))+(sin(d));
}
}