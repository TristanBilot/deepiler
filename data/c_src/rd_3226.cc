#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(d,d);
h=fmax(b,f);
if(isgreaterequal(d,a)){
e=log(h);
e=(exp(f))+(atan2(d,c));
a=(fmax(a,d))*(fmin(h,h));
g=(fdim(e,a))/(fmin(h,h));
}
else{
h=(fmin(a,f))*(fmax(g,c));
d=(fdim(c,e))/(pow(d,e));
a=(log(d))-(log(c));
c=(exp(h))+(sqrt(h));
f=acos(c);
}
b=pow(h,c);
a=(sin(f))+(fdim(h,f));
h=atan2(e,f);
e=pow(f,h);
a=(ceil(e))+(tan(d));
}