#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,a))*(asin(d));
d=(fmax(a,a))/(tan(c));
d=fmin(b,a);
b=(atan(e))*(atan2(b,e));
if(islessequal(a,e)){
b=(fmin(a,a))*(sqrt(a));
c=asin(a);
b=(sin(d))*(fmin(c,d));
}
if(islessequal(b,c)){
c=(atan(e))/(fmin(e,e));
a=fdim(a,d);
}
}