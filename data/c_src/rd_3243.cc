#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=tan(e);
f=tan(d);
d=fdim(d,d);
d=(pow(d,e))/(sin(a));
c=(acos(f))+(fmin(f,c));
d=fdim(d,c);
while(islessequal(b,a)){
d=(floor(b))*(acos(c));
f=(fmin(c,c))*(acos(b));
c=fmin(d,a);
}
}