#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(e);
b=(cos(g))+(cos(a));
h=(exp(d))/(fmax(h,h));
e=cos(b);
c=fdim(d,e);
e=exp(c);
c=(exp(c))/(atan2(b,e));
f=(pow(d,d))+(log10(d));
if(isgreaterequal(h,f)){
f=(pow(d,f))/(fmax(a,f));
g=(atan2(b,d))-(sin(b));
b=asin(c);
h=fmin(c,d);
c=(fmax(g,g))+(atan2(f,f));
}
}