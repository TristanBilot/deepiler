#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(d,a);
c=(log(d))-(atan2(e,e));
d=(atan2(b,c))*(ceil(e));
e=ceil(d);
d=(acos(d))-(atan2(a,a));
c=(asin(b))*(atan(b));
c=(atan(e))*(atan(b));
e=exp(b);
if(islessgreater(d,e)){
e=(fdim(d,a))*(fdim(e,d));
a=(exp(c))-(fdim(a,a));
e=acos(b);
}
else{
d=(atan2(b,c))-(log(c));
b=(log(c))*(fmax(a,d));
d=tan(c);
}
}