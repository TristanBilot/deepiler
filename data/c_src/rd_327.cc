#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(b,e);
e=atan2(d,b);
d=(sqrt(a))-(atan2(d,a));
b=(fdim(b,a))*(tan(e));
if(islessequal(d,a)){
b=(acos(a))-(ceil(d));
d=pow(d,c);
}
else{
e=floor(d);
a=sin(c);
b=exp(c);
d=(fmin(b,d))/(pow(d,b));
}
if(isgreaterequal(b,e)){
d=(fmax(e,d))+(atan2(d,b));
b=(tan(c))-(tan(b));
c=(log10(a))-(fdim(a,d));
a=atan2(b,b);
a=(exp(e))*(fdim(c,b));
}
else{
c=fmax(a,c);
b=(exp(d))-(fmin(e,b));
}
}