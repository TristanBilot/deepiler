#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=asin(c);
e=(sqrt(c))-(fmin(c,a));
d=(ceil(b))/(atan2(a,e));
e=fdim(d,c);
d=pow(c,c);
if(islessgreater(e,d)){
c=cos(b);
c=tan(e);
}
else{
d=atan2(e,e);
d=ceil(b);
}
if(islessgreater(c,d)){
a=(sqrt(c))/(log10(e));
a=atan(d);
e=(fdim(a,a))/(fdim(b,e));
}
else{
c=(ceil(d))*(log10(d));
a=fmin(c,a);
b=log10(d);
a=(exp(e))*(floor(c));
}
}