#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(e,b))-(tan(e));
g=(fdim(d,c))/(floor(e));
e=(ceil(c))-(fmax(a,c));
b=asin(c);
if(isless(e,f)){
g=(asin(g))-(fmax(c,g));
e=(sin(b))+(asin(e));
f=fmax(d,b);
}
else{
f=tan(a);
e=fmax(f,c);
}
while(islessgreater(d,g)){
f=(cos(f))-(pow(g,h));
g=(sqrt(b))-(ceil(c));
d=cos(g);
h=(fdim(b,f))+(exp(h));
}
}