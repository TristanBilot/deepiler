#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=floor(e);
a=(fdim(c,d))-(fmin(g,a));
e=(fdim(b,g))+(fmax(g,a));
a=sqrt(c);
if(isgreaterequal(b,e)){
b=fmax(f,c);
b=fmax(d,b);
a=cos(c);
b=fmin(c,e);
}
else{
g=(cos(d))/(pow(e,g));
g=(pow(a,g))*(sqrt(e));
d=(fdim(f,f))*(atan2(d,d));
b=pow(g,b);
}
while(isgreaterequal(b,a)){
c=(floor(a))+(acos(e));
a=atan2(c,e);
b=fmax(e,b);
f=tan(g);
a=(tan(b))-(tan(f));
}
}