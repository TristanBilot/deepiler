#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(d);
d=(fmin(b,d))/(fmax(c,e));
c=ceil(e);
b=fmin(b,d);
e=(exp(b))-(pow(d,b));
while(islessequal(b,d)){
c=(pow(a,a))-(pow(c,d));
a=(pow(c,e))*(fmax(e,c));
b=sin(c);
}
if(islessequal(d,a)){
d=log10(d);
c=acos(c);
}
}