#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(d,e))/(cos(d));
a=(floor(b))*(cos(e));
d=tan(d);
d=(log10(e))*(atan2(b,d));
b=(log(e))*(fdim(d,a));
c=(fdim(d,e))-(fdim(e,d));
while(islessgreater(a,d)){
a=atan2(a,d);
e=(atan(d))/(cos(a));
a=atan2(d,d);
d=fdim(a,a);
}
}