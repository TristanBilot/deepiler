#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(asin(a))/(fmin(b,b));
f=acos(a);
c=(fmax(d,e))/(fdim(e,c));
g=(fmax(c,b))-(pow(e,b));
e=atan2(a,a);
if(isgreaterequal(g,b)){
c=(sin(c))-(exp(d));
e=atan2(g,f);
b=sin(g);
g=(pow(a,a))*(fmin(e,a));
}
else{
g=fmax(f,d);
f=atan2(a,g);
d=sin(a);
f=fmax(e,c);
}
if(isgreaterequal(a,a)){
b=pow(a,g);
c=floor(g);
a=sqrt(f);
}
else{
b=(fdim(a,f))/(log10(a));
e=atan2(b,b);
b=pow(f,f);
b=(floor(d))/(log(c));
}
}