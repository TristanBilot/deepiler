#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(b,c);
e=ceil(e);
a=log10(b);
e=(fdim(e,e))-(tan(d));
f=atan2(b,d);
if(isless(d,a)){
a=pow(f,f);
f=fdim(d,c);
e=sin(c);
}
else{
d=sin(b);
f=(fmax(d,f))/(pow(a,c));
b=pow(d,a);
f=fmax(f,b);
d=(fdim(f,f))-(fmax(d,f));
}
while(islessgreater(f,f)){
a=floor(d);
a=atan(b);
c=sin(c);
b=(pow(d,c))/(pow(d,b));
}
}