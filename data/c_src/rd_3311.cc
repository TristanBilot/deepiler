#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(ceil(c))-(fmax(e,d));
d=fdim(e,d);
c=(fdim(f,b))*(asin(a));
if(islessgreater(h,e)){
d=cos(b);
h=(sin(g))+(floor(b));
d=pow(f,a);
}
if(islessequal(g,e)){
h=(pow(f,a))*(log(a));
d=pow(d,f);
d=(fmin(h,a))*(ceil(a));
}
else{
d=fmax(f,h);
a=pow(b,a);
d=fmax(h,c);
d=(acos(b))/(fmax(g,c));
g=log10(h);
}
}