#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=tan(b);
a=(pow(a,a))/(ceil(g));
g=fmax(d,a);
c=pow(a,b);
while(isgreaterequal(d,a)){
a=fmax(e,g);
f=(pow(b,f))*(pow(d,f));
c=(fmax(d,c))-(fdim(f,c));
a=fmax(d,d);
}
f=(fdim(a,c))*(fdim(d,g));
c=log10(g);
}