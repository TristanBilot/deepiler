#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=sin(f);
a=fdim(f,c);
f=(asin(g))*(asin(g));
a=fmax(c,e);
g=(fmax(b,d))/(ceil(f));
while(islessequal(g,b)){
c=(atan(f))+(exp(g));
e=pow(g,d);
g=(exp(f))/(exp(g));
e=(fmax(e,c))*(pow(c,f));
g=(atan(d))/(pow(e,g));
}
if(isless(c,a)){
b=(cos(a))+(fmax(f,d));
c=(fdim(g,e))+(fdim(f,d));
}
else{
b=(fdim(a,a))*(log10(c));
c=log(c);
b=log(e);
}
}