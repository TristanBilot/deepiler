#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(acos(e))*(fdim(c,a));
b=log10(a);
while(islessgreater(a,e)){
d=pow(d,a);
a=(log(b))+(tan(c));
c=(cos(a))*(pow(b,c));
}
while(isless(b,c)){
e=(pow(e,d))/(fmax(e,a));
d=pow(a,a);
d=atan2(d,e);
b=atan(d);
c=(pow(e,d))-(acos(d));
}
}