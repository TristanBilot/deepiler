#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sin(e))-(fmax(h,c));
e=(asin(f))*(floor(e));
f=tan(e);
f=(floor(e))*(asin(b));
e=(fmin(g,b))+(exp(d));
d=atan(h);
h=(atan(h))*(tan(f));
a=(floor(f))*(fmin(a,d));
e=(atan(a))*(fdim(a,e));
}