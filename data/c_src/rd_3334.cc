#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(d,e);
c=fdim(a,e);
e=ceil(c);
while(islessequal(f,f)){
f=ceil(a);
d=fmin(d,e);
f=sin(c);
}
while(islessequal(c,d)){
a=cos(c);
b=sin(a);
f=sqrt(b);
c=(atan2(b,f))*(fdim(d,b));
}
}