#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(e))/(atan(a));
d=(fmin(b,e))+(exp(d));
while(islessequal(c,c)){
a=acos(c);
a=(fdim(b,a))/(asin(e));
a=acos(e);
e=fmax(d,d);
e=log(e);
}
if(islessequal(a,d)){
d=fmax(e,e);
b=fdim(a,a);
e=(atan(a))-(pow(c,e));
e=sin(a);
}
else{
c=ceil(c);
b=(fdim(b,b))+(sin(e));
a=(pow(c,d))-(pow(b,e));
}
}