#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(d,f))*(cos(e));
f=(sin(d))/(floor(g));
a=(fdim(b,b))/(fmax(a,g));
d=fmin(f,a);
e=cos(d);
e=(fmax(a,f))/(atan2(b,d));
f=fmin(c,g);
f=(cos(e))+(fdim(f,a));
g=fmax(g,b);
b=(pow(b,d))+(log(b));
g=fmax(b,c);
b=(asin(a))+(sin(d));
f=pow(f,g);
c=(fmin(b,e))*(fmin(f,c));
a=log(f);
}