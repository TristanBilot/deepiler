#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(c,d);
b=(fmin(c,b))/(pow(e,e));
b=(sqrt(e))*(ceil(c));
c=atan2(e,b);
d=floor(f);
while(islessequal(e,f)){
f=acos(d);
f=(fmax(e,f))/(atan2(b,b));
d=(log10(d))/(pow(a,a));
b=(asin(e))-(fmax(d,a));
d=(sqrt(f))*(fmax(b,d));
}
}