#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(d);
h=asin(f);
e=fdim(b,e);
d=asin(e);
a=fmax(h,e);
while(isless(e,e)){
c=fmax(g,h);
a=(fdim(h,c))-(fmax(c,c));
a=cos(c);
c=pow(d,e);
h=asin(f);
}
c=pow(f,d);
d=asin(e);
}