#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(c,e);
b=(fdim(c,d))/(fmax(b,a));
d=log10(c);
if(isless(e,c)){
c=ceil(d);
a=(sin(e))/(acos(b));
a=log10(a);
e=fmax(e,d);
e=(floor(e))-(sqrt(b));
}
if(islessequal(e,c)){
a=exp(e);
b=(fdim(b,d))/(pow(e,a));
}
}