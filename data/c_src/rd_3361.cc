#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(b,b))*(fmin(e,d));
c=tan(g);
while(islessequal(a,f)){
a=(sqrt(b))/(exp(f));
f=(fmax(f,c))-(fdim(f,f));
g=acos(c);
}
while(isless(g,d)){
f=tan(d);
b=ceil(c);
}
}