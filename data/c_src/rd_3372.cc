#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(c,e);
b=(fmax(d,e))+(fdim(c,c));
e=(pow(d,b))+(log(e));
if(isgreaterequal(c,d)){
e=fmax(c,c);
c=(sqrt(d))-(acos(b));
c=(fmax(e,e))+(fmin(b,a));
d=cos(e);
c=cos(a);
}
else{
a=exp(e);
c=(exp(e))/(log(c));
e=exp(e);
}
e=fmin(a,e);
d=(log(a))-(floor(e));
c=(atan2(c,a))/(ceil(c));
c=(log10(e))/(fmin(b,d));
d=log10(e);
}