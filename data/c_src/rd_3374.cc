#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(b,c);
a=tan(c);
c=(ceil(a))-(log10(b));
b=(log10(b))-(fmax(d,c));
b=atan(d);
while(islessequal(e,d)){
a=(atan(a))/(atan(b));
e=log10(c);
d=exp(d);
a=(atan2(d,d))/(atan(b));
d=(atan2(e,c))+(asin(b));
}
while(islessequal(d,e)){
c=pow(d,a);
e=floor(a);
c=(fdim(c,e))/(sqrt(b));
b=fdim(d,d);
}
}