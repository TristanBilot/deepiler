#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(c))-(exp(c));
c=(tan(a))/(fdim(b,b));
e=sin(d);
a=fmax(e,e);
d=(fmin(c,e))-(asin(b));
e=atan2(c,e);
while(isless(e,b)){
d=(sin(e))/(floor(a));
e=atan2(b,a);
c=fmin(b,d);
}
}