#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(g);
b=(fdim(g,f))-(fdim(f,e));
f=fmin(b,c);
b=cos(c);
while(islessgreater(g,a)){
b=pow(e,d);
c=(fdim(f,b))-(fmax(b,f));
f=fmax(f,b);
}
if(isless(g,g)){
g=fmax(c,d);
b=sqrt(a);
c=exp(e);
f=cos(b);
}
else{
c=pow(c,d);
e=(fmin(g,e))/(fmax(b,e));
e=(log(e))-(fmax(f,f));
}
}