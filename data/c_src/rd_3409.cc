#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(e);
h=pow(g,d);
c=fmax(g,a);
f=asin(c);
if(islessequal(g,f)){
c=(asin(d))+(pow(d,a));
h=log(d);
a=(log(g))*(acos(c));
d=pow(e,a);
}
else{
g=(cos(g))*(fmax(b,f));
c=(sin(c))-(pow(b,g));
g=(pow(b,a))/(pow(c,e));
b=(fmax(h,g))-(fdim(a,g));
}
}