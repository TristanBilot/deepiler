#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(e,c))*(tan(f));
a=(atan(d))+(atan2(g,e));
a=atan2(e,f);
b=log10(f);
h=fmax(f,a);
g=(log(d))*(fdim(c,d));
b=fmax(b,b);
b=(fmax(f,e))/(ceil(h));
if(islessgreater(c,f)){
g=fmin(e,a);
f=fdim(g,f);
}
else{
d=(sqrt(c))-(sin(h));
g=(fdim(e,h))/(fmin(h,e));
c=(cos(a))-(log10(a));
c=(pow(d,c))/(atan2(a,f));
b=(atan(f))+(acos(c));
}
}