#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(c,a);
e=fmax(f,b);
if(isless(e,g)){
f=(pow(b,b))+(fmax(b,e));
g=floor(f);
g=(tan(d))+(log10(c));
}
else{
g=atan2(e,h);
a=(fmax(d,g))+(sin(e));
e=pow(c,h);
h=pow(d,d);
}
e=atan2(h,a);
f=log10(g);
f=exp(c);
}