#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(b,c))-(acos(g));
d=(ceil(c))-(pow(d,e));
f=cos(c);
if(isgreaterequal(b,c)){
d=asin(d);
f=fmin(f,e);
c=(fmin(f,g))*(tan(d));
}
else{
d=(cos(a))*(log(g));
f=atan(g);
}
if(islessequal(c,b)){
a=(tan(g))*(atan(b));
a=pow(d,a);
d=(log(d))*(exp(a));
c=(log10(h))*(pow(b,d));
}
else{
a=(log10(d))+(log(e));
a=(fmax(e,g))+(fmin(e,b));
b=log10(e);
}
}