#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(log10(g))-(fmax(a,g));
g=atan2(h,d);
a=(floor(d))/(pow(g,d));
while(islessequal(g,g)){
f=(fmin(b,a))+(log10(a));
b=fdim(g,f);
b=(pow(c,g))+(tan(a));
c=(pow(c,h))/(sqrt(h));
}
while(islessequal(h,b)){
f=fmax(a,d);
c=fmin(b,a);
g=(sqrt(a))/(atan(g));
h=acos(f);
f=fdim(g,b);
}
}