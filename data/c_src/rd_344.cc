#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=floor(b);
e=(pow(f,b))/(cos(a));
b=tan(c);
d=floor(b);
a=(fdim(c,f))*(log(d));
while(islessequal(d,f)){
f=fdim(e,e);
b=log10(d);
}
while(islessgreater(b,e)){
f=pow(c,d);
f=(sqrt(e))-(pow(f,d));
e=(fmax(d,d))/(fdim(f,e));
c=tan(d);
}
}