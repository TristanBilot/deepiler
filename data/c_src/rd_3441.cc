#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=asin(a);
d=(exp(a))+(log10(e));
f=(pow(f,b))+(floor(a));
e=(pow(e,e))/(fmin(a,d));
e=(fmax(d,c))*(acos(e));
while(isless(a,a)){
f=floor(a);
b=(fdim(a,a))+(sqrt(f));
c=pow(e,a);
b=(fmax(d,e))/(floor(b));
}
while(islessequal(c,b)){
a=atan(c);
d=(cos(c))/(cos(b));
a=(fmax(e,c))+(fmax(c,a));
b=(fdim(a,a))*(pow(f,f));
}
}