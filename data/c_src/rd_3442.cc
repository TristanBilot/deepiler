#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(b,a))/(exp(a));
c=(exp(c))-(atan(d));
a=(cos(c))*(sqrt(e));
if(isless(c,a)){
b=(fdim(b,d))-(pow(e,a));
b=fmax(c,c);
a=(log(d))+(ceil(b));
c=exp(a);
}
if(islessequal(a,a)){
b=(atan2(e,d))-(sqrt(d));
c=(cos(b))-(log(c));
c=fmax(d,b);
}
}