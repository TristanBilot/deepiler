#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(pow(d,e))+(sin(e));
d=(fdim(e,b))-(pow(a,c));
e=(pow(a,d))-(cos(e));
if(isless(b,d)){
a=tan(b);
f=log(c);
b=(fmin(e,a))/(pow(a,a));
c=acos(d);
}
else{
a=(fmax(d,b))*(pow(e,d));
b=(tan(b))+(fmin(f,b));
b=fmax(c,f);
e=tan(e);
d=(atan2(f,b))+(fmax(a,c));
}
b=(tan(c))/(pow(d,d));
f=sqrt(b);
a=(fdim(d,b))*(pow(f,b));
f=(atan2(a,d))/(fdim(d,d));
}