#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(b,g);
f=exp(f);
e=atan(d);
g=(sqrt(h))*(atan2(e,c));
while(isgreaterequal(e,b)){
f=log10(h);
f=(fmin(f,g))-(fmax(d,c));
e=atan2(a,e);
b=(fmax(a,c))+(fmin(d,g));
}
while(isless(b,b)){
f=atan(h);
e=fdim(f,a);
f=log10(b);
c=(sin(d))*(sqrt(c));
}
}