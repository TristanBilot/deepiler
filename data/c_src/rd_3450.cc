#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan(d))/(fmax(d,f));
e=atan2(f,d);
e=fmin(c,b);
b=(acos(a))-(exp(a));
b=fmax(b,c);
a=ceil(e);
c=(pow(d,c))/(sin(e));
d=(pow(f,a))/(pow(a,f));
b=sin(b);
b=(log10(f))*(pow(e,f));
b=fmax(d,d);
}