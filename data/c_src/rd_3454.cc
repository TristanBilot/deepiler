#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(d,a);
c=(pow(b,b))*(log10(e));
d=exp(c);
d=(cos(e))+(fmax(c,a));
e=(log(d))+(sin(b));
e=(log(c))+(atan2(b,d));
while(isless(c,c)){
c=(atan(c))-(fmax(c,d));
b=(tan(d))/(fmin(e,b));
a=(atan2(c,c))*(acos(d));
}
}