#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(c,b))/(asin(a));
d=(ceil(b))-(floor(b));
d=log10(d);
b=(sin(a))+(fdim(b,c));
d=(atan(c))-(floor(c));
d=pow(e,a);
e=fdim(e,a);
e=asin(d);
if(isgreaterequal(e,e)){
c=(ceil(c))/(fmin(e,b));
c=(atan(e))/(fmin(a,c));
d=ceil(a);
e=(log10(d))/(sqrt(b));
}
else{
e=fmax(b,d);
d=(floor(b))-(sqrt(a));
d=(exp(d))+(fmin(e,d));
a=pow(c,c);
}
}