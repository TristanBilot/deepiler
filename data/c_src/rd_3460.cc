#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log(f))-(tan(f));
b=fmax(a,c);
g=asin(c);
if(islessgreater(d,b)){
c=log(e);
d=(floor(c))/(pow(c,d));
f=exp(f);
g=(exp(e))/(pow(a,d));
c=fdim(a,e);
}
if(islessequal(c,g)){
g=pow(c,g);
b=(asin(c))-(fdim(f,b));
a=tan(a);
a=(cos(a))*(fmax(b,a));
}
else{
d=(log(c))+(atan(g));
b=fmin(g,b);
e=(fdim(a,g))/(fdim(f,a));
f=(exp(g))+(sin(d));
a=atan2(e,d);
}
}