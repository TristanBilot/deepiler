#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(c,e);
c=(atan(b))-(exp(c));
e=pow(a,e);
while(isgreaterequal(c,e)){
c=sin(a);
e=(fmin(a,b))+(fdim(d,a));
a=(tan(c))-(asin(b));
c=(fmin(c,b))*(sqrt(b));
d=fmin(e,e);
}
a=ceil(c);
d=(atan2(b,e))-(fmax(b,d));
}