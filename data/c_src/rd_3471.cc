#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(tan(e))/(fdim(b,b));
a=(floor(g))/(pow(e,e));
e=(acos(g))-(fmax(c,g));
c=pow(d,f);
a=cos(b);
d=sqrt(b);
d=(cos(c))+(cos(e));
e=fmin(f,d);
e=(atan2(f,c))/(exp(e));
c=(log10(g))*(fmin(c,g));
}