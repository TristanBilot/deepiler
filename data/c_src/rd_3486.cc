#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=ceil(c);
a=(fmin(c,a))+(fmax(c,e));
e=(log(b))-(exp(c));
while(islessgreater(e,b)){
c=(atan2(b,c))-(fmax(b,c));
a=(pow(d,a))*(pow(c,c));
}
while(isgreaterequal(c,d)){
d=(sin(d))/(atan(d));
b=fmax(e,d);
d=pow(a,d);
}
}