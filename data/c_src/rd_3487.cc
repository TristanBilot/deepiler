#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(a,a))+(atan(b));
b=(fmin(b,e))-(fmax(b,d));
while(isgreaterequal(c,c)){
e=atan2(a,c);
b=cos(c);
d=log(a);
}
if(islessequal(a,a)){
b=asin(c);
d=sin(b);
c=(fdim(c,d))+(acos(c));
}
}