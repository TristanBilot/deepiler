#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=asin(g);
h=(atan(e))+(fmax(f,e));
f=(log10(b))/(log(b));
b=fdim(d,c);
h=(fdim(a,h))+(acos(c));
b=(cos(a))-(log(e));
while(isless(c,g)){
a=(log10(c))/(fmax(a,d));
d=(fdim(g,g))-(asin(b));
a=(log10(f))/(pow(f,d));
g=(fmin(g,d))+(pow(f,h));
f=fmax(b,f);
}
}