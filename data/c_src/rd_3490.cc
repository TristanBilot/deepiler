#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(d);
a=pow(a,e);
a=(tan(b))*(fmin(c,e));
while(islessequal(d,c)){
e=ceil(a);
c=(asin(a))-(fmin(e,e));
d=(atan2(d,d))+(fmin(a,a));
a=(fmax(a,c))/(tan(a));
}
if(isgreaterequal(a,e)){
c=(ceil(d))*(fdim(d,e));
a=(tan(e))+(sin(e));
}
else{
d=(pow(c,b))+(fmax(d,c));
a=atan(c);
}
}