#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(a,e);
b=atan2(a,a);
b=fmin(d,c);
e=atan2(c,d);
e=fmax(c,b);
e=(fmax(a,b))-(fmax(e,c));
d=(fmax(a,d))/(floor(e));
b=(cos(a))-(fdim(e,e));
d=cos(d);
e=(atan(b))-(atan(d));
a=fmax(d,d);
}