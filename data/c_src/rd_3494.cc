#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(a,d))/(log(c));
b=(fmax(e,g))*(floor(e));
d=sqrt(f);
a=(fmin(a,b))/(floor(f));
g=atan2(e,a);
c=sin(e);
f=(exp(g))*(ceil(g));
g=(log(g))*(fmin(e,g));
if(isgreaterequal(a,f)){
c=(ceil(e))-(fmax(b,a));
c=(sin(d))*(fmax(e,a));
f=(atan2(b,f))+(atan2(e,g));
}
else{
a=sqrt(d);
e=(tan(a))/(log(g));
g=sin(b);
a=exp(d);
a=pow(g,a);
}
}