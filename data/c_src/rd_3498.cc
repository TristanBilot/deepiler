#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(asin(c))*(asin(a));
d=(floor(c))/(atan(d));
b=fmax(d,a);
while(isless(b,b)){
c=atan2(b,b);
e=pow(b,c);
c=fmin(a,d);
c=(fmax(c,d))+(atan2(a,d));
c=sin(e);
}
while(isgreaterequal(d,c)){
c=(atan2(d,b))/(atan(d));
a=(tan(c))/(fmax(d,c));
}
}