#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(a);
d=fmax(e,e);
if(islessequal(d,d)){
e=(fdim(d,d))-(pow(d,d));
b=(fmin(d,d))*(atan2(e,a));
a=fmin(e,c);
e=ceil(a);
}
else{
b=(acos(c))*(acos(b));
b=atan2(d,d);
c=sqrt(a);
b=(atan2(a,a))*(asin(b));
b=fmin(c,e);
}
c=(exp(a))-(ceil(a));
a=(acos(e))-(fdim(d,a));
e=(fmin(c,b))-(atan(a));
c=(fmin(d,c))/(acos(e));
c=(atan2(d,c))*(atan2(c,c));
}