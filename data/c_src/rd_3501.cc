#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(g,g);
c=pow(g,c);
a=(log10(a))*(log(f));
e=(fdim(c,c))+(log10(g));
if(isless(e,d)){
g=(sqrt(e))/(atan2(c,d));
a=sin(d);
a=pow(d,b);
}
if(islessequal(g,g)){
d=(pow(a,a))*(atan2(d,g));
c=(sin(e))/(atan2(a,c));
}
}