#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan2(d,b);
c=(pow(b,g))/(asin(d));
b=atan2(b,g);
g=(fdim(e,f))/(pow(e,c));
d=(acos(e))+(fmax(g,b));
g=(asin(e))/(fmax(a,a));
a=exp(f);
g=(fmin(e,f))+(acos(c));
if(islessgreater(g,g)){
f=fdim(c,g);
a=log(b);
e=atan(f);
}
}