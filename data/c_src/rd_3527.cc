#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=sqrt(b);
d=(tan(c))+(log10(b));
a=(atan(e))-(ceil(c));
f=(fmin(c,e))*(floor(f));
if(isgreaterequal(d,b)){
e=sin(d);
f=pow(d,e);
b=(log10(b))/(pow(f,d));
d=asin(e);
}
else{
f=fmax(f,d);
f=(cos(b))-(pow(d,e));
d=(log(a))-(cos(c));
}
while(islessequal(d,d)){
c=(exp(b))*(atan2(c,b));
c=pow(f,a);
a=(fmin(d,d))-(atan2(c,b));
}
}