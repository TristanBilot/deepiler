#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(c,a))+(atan2(e,e));
a=(floor(e))+(fmax(c,d));
a=pow(d,b);
e=atan2(c,d);
d=tan(d);
while(isgreaterequal(c,c)){
b=sin(b);
e=(fmax(a,b))+(log(b));
d=(fmax(b,d))*(atan2(b,b));
b=exp(a);
a=(ceil(c))*(fmax(a,e));
}
while(islessequal(a,a)){
e=cos(e);
b=(fmax(d,b))-(log(e));
b=(fmax(d,e))-(fmax(b,a));
e=(fmax(e,e))+(asin(e));
}
}