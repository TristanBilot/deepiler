#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=cos(a);
f=(pow(e,a))-(fmax(f,e));
while(islessequal(b,g)){
b=(tan(c))-(floor(e));
d=log(e);
f=pow(g,f);
c=fmax(f,e);
}
if(isless(b,b)){
a=(fdim(f,b))+(fmin(f,e));
c=acos(f);
c=fmax(g,b);
c=(fdim(g,e))+(cos(f));
}
else{
f=fmax(e,b);
e=(fmax(a,d))*(cos(a));
d=(fmax(b,b))+(fdim(c,f));
}
}