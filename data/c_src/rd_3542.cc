#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(acos(b))+(atan2(a,c));
d=(log10(c))+(floor(b));
f=fmax(c,d);
e=fmin(b,a);
while(islessgreater(c,e)){
a=fdim(f,d);
c=(acos(f))+(acos(f));
f=(atan(d))-(fdim(g,e));
}
f=(log10(g))*(sin(d));
f=(atan(a))*(fmin(e,g));
}