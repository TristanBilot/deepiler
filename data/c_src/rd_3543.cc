#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(acos(e))/(sin(d));
f=(asin(d))+(atan(b));
h=(cos(d))*(atan2(d,b));
h=atan(g);
h=tan(c);
b=(ceil(g))+(ceil(a));
b=floor(e);
c=(atan2(g,e))+(exp(a));
e=(fmax(f,g))/(atan2(h,e));
b=cos(a);
e=(fmax(g,f))/(cos(e));
}