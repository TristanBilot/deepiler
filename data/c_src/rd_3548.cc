#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=floor(c);
f=(sin(a))+(pow(c,b));
g=(pow(c,b))*(ceil(g));
c=(fmax(a,e))+(atan2(e,d));
while(islessgreater(c,c)){
e=fmax(g,f);
c=atan(e);
}
while(isgreaterequal(a,f)){
c=(ceil(a))/(exp(b));
a=fmin(b,a);
}
}