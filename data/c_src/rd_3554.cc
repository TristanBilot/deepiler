#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan2(b,e);
c=(pow(d,d))/(acos(d));
d=(fmax(d,c))*(acos(a));
c=(tan(c))/(atan2(a,e));
while(islessequal(g,f)){
g=atan2(f,f);
d=atan2(e,e);
c=(asin(d))+(atan2(a,d));
}
if(islessgreater(f,g)){
b=log(e);
a=sin(b);
}
else{
f=(fdim(c,c))+(fmax(g,a));
g=atan2(d,a);
d=fmax(b,d);
b=tan(g);
}
}