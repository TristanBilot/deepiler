#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log(b);
f=(ceil(c))+(ceil(a));
f=fmax(f,c);
c=atan2(b,d);
e=fdim(b,a);
while(islessgreater(c,b)){
d=(pow(c,e))-(atan(b));
b=acos(e);
b=fmin(f,d);
c=(ceil(f))*(atan2(f,d));
a=(atan2(d,b))-(fdim(c,c));
}
if(islessgreater(d,c)){
e=pow(f,e);
c=fmax(f,b);
f=(fmin(b,a))/(atan2(a,e));
d=(fdim(e,e))/(ceil(b));
f=asin(b);
}
else{
b=atan(e);
e=(fdim(d,d))-(pow(f,d));
c=(floor(c))-(ceil(b));
c=atan2(d,e);
a=atan(c);
}
}