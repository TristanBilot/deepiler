#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmax(h,f))+(fmax(b,c));
c=(exp(f))*(asin(h));
b=fmin(g,a);
if(islessgreater(f,a)){
c=asin(e);
e=fmax(f,a);
d=asin(h);
}
else{
g=pow(c,e);
c=(atan2(b,c))/(ceil(g));
d=(sin(b))-(fmin(d,h));
b=(tan(a))/(sqrt(a));
f=(cos(e))/(cos(c));
}
if(islessequal(e,g)){
e=(sqrt(f))+(atan(f));
d=(pow(c,a))*(floor(g));
h=(acos(a))/(sin(g));
f=acos(c);
}
else{
e=exp(c);
f=sqrt(f);
f=(fmax(f,h))*(pow(c,e));
b=log10(f);
}
}