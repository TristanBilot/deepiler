#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(e,c))-(fmax(g,c));
b=fmax(b,a);
a=fdim(d,d);
f=fmin(h,a);
while(islessgreater(f,g)){
f=(fmin(h,f))+(tan(d));
a=sqrt(b);
g=(sin(b))/(fdim(f,c));
}
a=fdim(b,d);
g=fdim(g,a);
}