#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(c))-(fmax(d,e));
c=(cos(d))/(pow(a,a));
c=(cos(d))+(fmax(a,e));
b=fmax(a,e);
d=(fmax(d,e))+(exp(b));
b=(fmax(a,e))/(fmax(a,c));
c=(asin(e))/(fmin(b,d));
e=tan(e);
c=fdim(d,d);
}