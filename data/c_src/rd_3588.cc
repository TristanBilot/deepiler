#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log(e))*(fdim(a,c));
g=(pow(d,h))*(ceil(c));
f=(log10(f))/(log(a));
e=(atan(a))-(pow(b,b));
if(islessequal(d,d)){
g=exp(e);
e=(pow(d,b))+(fmax(a,c));
f=ceil(b);
c=fmax(g,h);
e=(pow(f,g))/(fmax(a,g));
}
else{
e=pow(c,c);
c=fdim(b,h);
a=(atan2(h,g))/(atan2(a,b));
b=atan(g);
}
e=pow(e,d);
c=(exp(b))-(log(e));
a=log(d);
}