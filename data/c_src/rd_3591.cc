#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(b))*(sin(d));
e=log(e);
c=(fdim(a,b))+(fdim(a,e));
e=pow(b,e);
a=(atan2(e,d))/(fdim(e,e));
c=floor(b);
c=exp(a);
if(isless(b,d)){
b=(fmin(c,c))-(fmin(a,c));
b=pow(a,e);
b=pow(d,a);
b=log10(b);
}
}