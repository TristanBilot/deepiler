#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(d,c);
d=pow(g,h);
g=exp(a);
g=atan(g);
h=(fdim(e,h))/(sqrt(h));
while(islessequal(f,b)){
g=ceil(a);
f=(atan2(f,a))*(ceil(f));
a=floor(d);
g=fmax(h,g);
d=fmax(c,a);
}
while(islessequal(a,h)){
d=(fmin(b,g))+(fmin(e,b));
e=fmax(c,a);
}
}