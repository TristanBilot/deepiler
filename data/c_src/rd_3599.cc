#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(e,d))-(acos(c));
c=floor(f);
c=(fmin(a,g))-(tan(g));
e=(fmax(g,g))/(fmin(f,a));
g=(log10(c))-(atan2(b,c));
while(islessequal(c,a)){
b=(exp(f))-(fmin(b,e));
c=log10(e);
e=(tan(c))/(fdim(g,b));
b=(pow(b,a))+(fdim(b,g));
e=exp(a);
}
while(isless(a,c)){
g=(fdim(c,a))/(sqrt(a));
d=(cos(g))-(pow(c,e));
}
}