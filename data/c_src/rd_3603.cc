#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(f,h))+(ceil(g));
d=fmin(g,f);
c=fmax(g,a);
a=atan2(f,c);
b=fmin(e,a);
h=(atan2(b,f))*(exp(f));
while(islessgreater(a,h)){
a=asin(a);
h=exp(d);
g=pow(g,b);
d=asin(e);
a=(log10(a))+(fmin(g,d));
}
}