#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=floor(b);
d=asin(a);
d=pow(a,c);
while(islessequal(c,c)){
f=fdim(f,d);
e=sqrt(b);
c=cos(d);
b=log(e);
d=pow(e,b);
}
if(islessgreater(d,e)){
f=cos(a);
b=fdim(a,c);
d=(exp(d))-(sqrt(a));
}
else{
d=pow(d,b);
f=(fmin(b,b))+(fmax(e,b));
c=pow(c,b);
}
}