#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(a);
d=tan(d);
a=(sin(a))-(fmax(d,f));
b=fmax(f,c);
f=(fdim(a,e))*(atan2(e,b));
f=fdim(d,e);
g=(tan(h))-(ceil(e));
c=(atan(e))-(atan2(h,e));
while(isless(d,c)){
b=(fmax(f,b))/(fmin(c,f));
e=atan(h);
g=(sqrt(b))*(atan2(c,h));
g=(asin(f))-(atan2(d,h));
b=(acos(e))/(pow(f,h));
}
}