#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(c,b);
e=fdim(a,d);
a=(fmin(e,e))+(fmax(e,c));
if(islessequal(e,c)){
d=fmax(d,e);
d=(ceil(d))-(fmax(d,b));
}
while(islessequal(d,a)){
c=(asin(a))*(pow(d,b));
e=(atan2(e,e))/(pow(b,c));
b=atan2(c,e);
d=(atan(d))-(fmax(e,b));
}
}