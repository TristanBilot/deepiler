#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan(h);
g=(fmin(g,a))*(fdim(a,a));
b=fmin(f,f);
while(isless(c,b)){
b=(fmax(b,e))+(log(h));
a=(fmax(b,e))-(atan(c));
c=(pow(a,c))-(pow(g,h));
e=fmin(d,d);
b=(pow(c,g))/(fmax(h,h));
}
if(isgreaterequal(g,c)){
g=pow(g,c);
c=(atan2(d,f))+(log(f));
c=(asin(h))*(fdim(g,d));
h=(log10(c))*(pow(b,a));
}
else{
c=(exp(g))-(fmin(f,a));
b=fmax(h,d);
c=(atan2(b,g))-(sqrt(d));
c=fdim(d,f);
g=(sqrt(c))+(fdim(d,d));
}
}