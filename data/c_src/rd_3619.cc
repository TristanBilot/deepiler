#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=cos(c);
d=pow(b,c);
b=(acos(b))/(fdim(b,c));
b=(fmax(a,c))-(fdim(b,e));
while(isless(c,b)){
d=floor(a);
e=fmax(b,c);
b=floor(a);
d=(asin(d))/(fmin(d,e));
}
while(islessequal(a,a)){
d=floor(b);
b=pow(d,d);
}
}