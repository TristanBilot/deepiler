#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(ceil(b))+(fmin(c,b));
e=floor(a);
d=fmin(d,d);
while(isless(a,e)){
c=(exp(e))+(log(a));
e=asin(b);
c=atan2(d,a);
c=floor(a);
}
if(isgreaterequal(c,b)){
d=fmax(b,e);
a=(fdim(d,d))+(asin(c));
a=atan2(a,d);
e=log10(c);
d=(sin(c))*(atan(d));
}
}