#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(sin(a))+(atan2(e,f));
f=(fmax(b,d))+(sqrt(d));
c=(sin(b))+(fdim(a,a));
c=(fmax(e,f))*(log10(d));
g=(ceil(a))*(floor(e));
while(isgreaterequal(a,g)){
a=(fmax(g,b))-(tan(a));
g=fmin(f,d);
e=sin(e);
f=fmax(b,c);
}
if(isgreaterequal(c,b)){
c=(tan(e))/(exp(f));
g=sqrt(g);
b=asin(d);
e=(exp(g))-(pow(g,e));
}
}