#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=pow(d,g);
g=(fmax(d,b))*(log10(d));
g=sin(g);
g=atan2(c,d);
a=fmin(g,d);
b=sqrt(b);
c=atan2(f,d);
if(isless(g,g)){
b=(fdim(e,d))*(cos(a));
g=cos(g);
c=fmax(f,d);
}
else{
a=fmax(f,b);
c=(log(g))*(pow(f,b));
a=(pow(a,c))+(ceil(g));
}
}