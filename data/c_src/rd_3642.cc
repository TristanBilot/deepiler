#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sqrt(c);
b=fdim(c,d);
f=ceil(f);
c=log10(a);
d=(fmin(d,e))*(fmax(d,h));
g=(sqrt(a))-(asin(f));
a=atan(h);
b=acos(a);
f=fmax(d,e);
f=(fdim(g,a))*(fdim(d,b));
if(isless(d,b)){
h=fmin(c,d);
b=fmax(a,f);
}
}