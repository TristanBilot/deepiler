#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(d))*(pow(c,e));
c=sqrt(a);
d=(fmax(a,a))-(fdim(c,b));
d=floor(d);
c=fmax(d,e);
a=sin(b);
d=(log(a))*(fdim(c,b));
c=(tan(e))-(asin(e));
while(islessgreater(d,a)){
e=(ceil(b))-(fmin(c,e));
c=ceil(e);
a=(floor(e))+(pow(e,a));
}
}