#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(tan(d))*(atan2(a,a));
e=pow(h,b);
f=fmax(g,e);
while(islessequal(c,e)){
f=sin(a);
g=cos(e);
}
while(islessequal(b,b)){
f=(sqrt(b))+(pow(c,a));
d=pow(g,f);
g=(acos(f))/(log10(e));
b=fdim(h,h);
}
}