#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(d);
b=(fmax(b,b))*(pow(c,e));
while(isgreaterequal(e,a)){
a=fmax(a,d);
c=acos(e);
}
while(islessgreater(b,e)){
c=atan2(c,b);
d=floor(c);
}
}