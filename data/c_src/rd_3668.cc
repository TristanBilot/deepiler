#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(b,c);
a=(fmax(a,e))-(log(d));
b=floor(f);
a=(ceil(c))+(acos(b));
a=cos(c);
e=fmin(c,d);
b=fmax(b,d);
b=(atan(e))+(cos(a));
b=(sqrt(c))-(fmax(a,c));
c=(exp(d))-(sin(b));
if(islessequal(d,e)){
c=(atan(f))*(fmin(c,b));
b=fdim(d,d);
a=atan(d);
e=(atan(a))*(log10(e));
}
else{
c=(log(f))-(atan2(a,d));
c=atan2(a,a);
d=(exp(c))+(exp(b));
}
}