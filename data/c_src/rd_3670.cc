#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(a);
d=atan2(c,e);
f=(sin(h))*(atan2(g,e));
f=exp(c);
e=(log10(c))/(asin(c));
e=(atan(c))*(asin(d));
a=fmax(d,f);
b=pow(c,a);
h=pow(a,f);
f=(atan2(d,b))/(log(f));
g=(fdim(g,g))*(fmax(f,d));
}