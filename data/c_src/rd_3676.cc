#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(a,a);
b=asin(c);
b=fdim(c,b);
a=acos(d);
d=atan2(d,a);
if(islessequal(d,d)){
a=pow(e,d);
a=(fdim(e,b))/(log10(a));
}
if(isgreaterequal(d,d)){
a=pow(e,a);
e=fmax(d,e);
}
}