#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(e,e))*(fmax(b,f));
g=(exp(f))*(fmax(a,f));
while(isgreaterequal(a,g)){
c=(ceil(e))+(tan(g));
g=exp(b);
e=tan(c);
}
h=atan(h);
h=(sqrt(g))+(pow(c,d));
a=(fmax(h,e))*(exp(g));
h=(fdim(c,f))-(asin(c));
d=(exp(b))-(sin(d));
}