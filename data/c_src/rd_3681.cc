#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(a,b);
e=(floor(e))+(fmin(d,a));
d=(fmax(c,a))+(fdim(a,a));
c=fdim(b,e);
while(islessequal(d,b)){
d=(pow(b,d))/(log10(b));
d=(exp(b))/(fmin(a,c));
d=atan2(e,e);
a=fmax(a,a);
e=fdim(c,b);
}
a=sin(a);
e=(sin(e))*(pow(a,c));
a=(fdim(a,c))/(fmax(a,c));
}