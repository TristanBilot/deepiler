#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=cos(a);
a=fmax(e,a);
c=fmin(c,e);
d=fmax(d,c);
b=(log10(f))+(sin(f));
f=(sin(e))+(fdim(b,e));
a=(ceil(a))/(fmax(a,e));
e=log10(d);
a=exp(f);
}