#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(g,f))*(fmin(d,f));
f=(log10(e))+(log(c));
f=(fmax(f,b))/(fmin(b,f));
f=(pow(b,c))/(tan(d));
e=(log10(c))/(fdim(g,a));
a=(cos(b))+(atan(f));
a=fdim(e,c);
if(islessgreater(d,g)){
d=sin(c);
b=(floor(e))-(acos(g));
g=(tan(b))*(fmax(g,d));
a=(fmin(d,e))/(floor(f));
}
}