#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,c);
a=(floor(b))/(asin(a));
while(isless(d,b)){
e=fmax(c,a);
d=(floor(e))-(tan(b));
}
while(islessgreater(a,a)){
e=(fmin(a,a))-(acos(c));
e=(fmin(a,a))/(log10(c));
a=acos(c);
a=ceil(a);
}
}