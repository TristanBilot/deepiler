#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(a))*(atan(c));
b=(ceil(c))+(fmin(c,b));
a=pow(c,e);
b=(cos(a))+(pow(e,c));
a=log(c);
a=fmin(d,d);
d=atan(c);
while(islessgreater(e,d)){
d=fmin(a,d);
e=(pow(d,c))*(asin(d));
c=(asin(a))-(fmin(b,b));
d=acos(b);
}
}