#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(tan(d))/(floor(c));
g=ceil(e);
a=(pow(f,g))/(atan2(h,d));
e=sqrt(h);
b=(atan2(g,e))+(fdim(a,c));
f=(fdim(f,e))+(cos(g));
a=(pow(a,e))-(pow(d,g));
a=(floor(h))-(pow(c,e));
if(islessequal(e,e)){
a=atan2(c,e);
h=(pow(a,b))+(atan(h));
d=pow(h,f);
a=atan2(g,c);
}
else{
c=(floor(d))-(atan2(a,b));
h=(fmin(h,e))-(pow(a,e));
d=(asin(c))*(sqrt(g));
}
}