#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(h);
c=ceil(h);
e=log10(c);
d=floor(b);
while(islessgreater(c,d)){
g=(ceil(d))*(pow(c,d));
d=sqrt(c);
a=(atan2(d,g))+(asin(g));
b=(pow(b,g))/(fmax(a,a));
e=atan2(d,b);
}
if(isgreaterequal(h,d)){
d=sqrt(h);
a=(exp(c))/(atan(h));
b=(exp(f))+(sqrt(f));
g=sin(e);
}
else{
c=(pow(d,e))+(atan(a));
e=(log(h))/(fmax(e,c));
c=pow(e,g);
h=log10(b);
}
}