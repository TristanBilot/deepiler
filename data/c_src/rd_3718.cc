#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(e,a);
b=(cos(b))/(pow(e,a));
while(islessequal(e,d)){
e=tan(d);
d=(acos(c))*(acos(d));
d=(fmax(b,a))*(pow(a,e));
c=(atan2(b,e))*(fmax(b,b));
e=(pow(c,b))/(atan2(d,b));
}
while(isgreaterequal(d,a)){
d=(tan(a))-(fmax(d,e));
c=(exp(a))-(fmax(d,c));
a=fdim(e,a);
}
}