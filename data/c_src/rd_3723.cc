#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(d,d))*(tan(b));
a=atan(e);
e=fmax(c,b);
b=(fdim(d,e))/(fmin(a,c));
b=fdim(d,d);
d=fmin(a,c);
b=atan2(e,c);
e=(cos(e))-(sqrt(d));
a=sqrt(b);
b=(atan2(b,d))+(fmin(a,a));
}