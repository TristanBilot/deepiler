#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log(d);
e=(sqrt(f))-(fmax(f,e));
c=(pow(f,d))-(fmax(f,d));
a=(sqrt(b))-(pow(e,d));
while(islessgreater(d,f)){
d=tan(d);
c=pow(c,e);
c=(fmax(e,b))/(atan2(a,b));
a=(fmin(c,e))*(log10(e));
d=(atan2(d,e))-(sqrt(b));
}
while(islessgreater(b,a)){
b=acos(b);
c=asin(e);
d=(atan(d))-(fmax(a,f));
}
}