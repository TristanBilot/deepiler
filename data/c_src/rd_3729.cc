#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=sin(a);
d=fmax(d,a);
a=(fdim(f,e))*(atan(b));
while(isgreaterequal(d,f)){
b=atan2(b,c);
e=sin(g);
}
if(islessequal(d,e)){
g=acos(d);
f=(fmax(c,c))+(atan2(g,c));
d=fdim(c,d);
}
}