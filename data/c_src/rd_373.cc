#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(c);
c=(fdim(e,e))*(ceil(e));
a=(acos(d))-(log10(c));
d=(fdim(c,d))-(pow(d,c));
a=(fdim(e,c))*(log10(b));
d=(asin(e))-(pow(a,d));
c=(atan(c))+(fmax(b,e));
b=(pow(c,e))-(sin(a));
a=floor(a);
a=(fdim(a,e))/(cos(a));
}