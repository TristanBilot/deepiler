#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(exp(f))-(atan2(b,c));
a=cos(h);
d=sin(h);
f=(fmax(g,d))-(log(f));
g=log(a);
g=(cos(c))/(fmin(f,f));
d=(pow(f,e))-(fdim(e,a));
while(isless(b,b)){
e=pow(b,d);
e=(pow(c,f))+(tan(c));
h=ceil(e);
d=fmin(h,f);
e=(fmin(c,d))/(log10(f));
}
}