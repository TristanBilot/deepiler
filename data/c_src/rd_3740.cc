#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(exp(g))+(fmin(c,a));
g=log10(d);
c=fmin(a,e);
f=fdim(b,a);
f=(atan(a))/(pow(a,e));
while(islessequal(g,g)){
b=pow(f,b);
c=sin(f);
}
while(islessequal(f,b)){
a=(sin(d))-(acos(f));
f=fmax(b,e);
b=pow(d,c);
}
}