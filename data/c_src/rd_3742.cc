#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(c,d))/(pow(a,e));
h=atan2(f,h);
while(islessequal(e,e)){
d=asin(c);
g=log(b);
a=(fdim(g,b))+(exp(d));
b=atan2(d,f);
e=ceil(f);
}
f=(pow(b,a))*(fmin(g,g));
g=(sin(f))/(fdim(d,g));
a=(sqrt(h))/(sqrt(b));
c=fmax(c,c);
h=(fmin(e,d))+(pow(a,h));
}