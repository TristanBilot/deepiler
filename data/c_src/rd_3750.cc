#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=tan(d);
d=fmax(d,b);
d=fdim(a,d);
e=tan(e);
while(islessequal(c,b)){
e=pow(c,d);
c=log(d);
c=atan2(e,a);
c=(fdim(e,e))*(ceil(c));
a=sin(e);
}
a=fmax(a,d);
d=floor(a);
c=atan(b);
d=(ceil(a))/(atan(b));
d=sqrt(d);
}