#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(c,a);
c=exp(d);
a=(atan2(c,e))+(log(b));
f=cos(d);
e=fmax(c,b);
e=(fmax(a,f))+(fmax(f,b));
f=fdim(c,f);
if(isgreaterequal(a,f)){
d=ceil(e);
a=(log(e))/(fdim(e,b));
a=(fmin(a,b))+(atan2(b,c));
d=(log10(f))/(exp(a));
c=(atan(b))*(fdim(b,d));
}
}