#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(e,d);
b=(pow(c,c))*(pow(b,b));
a=(cos(a))-(sin(b));
a=(pow(d,e))+(pow(e,b));
a=pow(e,a);
if(isless(d,c)){
e=fmax(b,c);
e=(cos(c))/(pow(a,c));
a=(fmax(a,b))-(fmin(a,d));
c=sqrt(c);
d=(atan2(c,d))*(tan(d));
}
while(isless(d,a)){
d=fmin(a,e);
d=pow(c,c);
b=(pow(b,b))*(fmin(e,d));
e=(log10(c))+(pow(d,e));
}
}