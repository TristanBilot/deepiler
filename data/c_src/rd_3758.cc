#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(b,d);
d=(floor(b))*(acos(d));
d=atan2(e,b);
b=fdim(a,d);
if(islessequal(e,c)){
c=(pow(b,a))/(sin(e));
a=(log(c))/(log10(c));
c=(sqrt(a))+(fmax(c,a));
c=sqrt(a);
}
else{
c=(sqrt(b))-(acos(d));
c=(exp(d))+(tan(a));
a=(tan(e))/(asin(e));
c=(log(c))+(pow(d,e));
}
c=pow(b,d);
c=fmin(d,d);
}