#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(e,d))-(tan(e));
b=(fmax(f,e))-(asin(h));
if(islessequal(a,h)){
h=acos(d);
b=(asin(e))-(fmax(g,a));
}
if(islessgreater(c,b)){
b=(cos(f))*(atan(d));
h=acos(b);
d=acos(f);
}
}