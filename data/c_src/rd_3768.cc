#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(e);
b=sqrt(g);
e=(fdim(b,b))+(fmax(g,c));
if(islessequal(h,e)){
e=atan2(c,e);
d=acos(h);
f=(fmin(g,e))-(log10(a));
}
while(islessgreater(a,d)){
f=(sqrt(f))-(fdim(d,a));
e=ceil(h);
a=(atan2(g,h))*(ceil(b));
g=atan2(c,c);
}
}