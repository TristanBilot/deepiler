#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(pow(d,c))/(fmin(d,c));
f=log10(a);
a=(acos(d))-(acos(g));
b=(asin(f))/(log10(c));
if(isless(f,f)){
g=(sqrt(b))-(exp(d));
d=(atan2(c,g))-(acos(b));
b=(atan2(b,c))+(tan(b));
b=fmax(c,f);
f=fdim(f,a);
}
while(islessgreater(c,e)){
f=(acos(e))-(sqrt(e));
c=fmin(b,f);
}
}