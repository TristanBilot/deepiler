#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(log10(b))/(sqrt(b));
c=fmax(f,b);
f=(fmin(c,d))+(log(c));
c=atan2(d,b);
while(isless(d,c)){
e=(fmin(a,d))+(fmax(g,e));
e=sin(e);
b=atan(e);
f=tan(d);
a=ceil(e);
}
if(islessequal(b,f)){
d=log(a);
g=fmin(a,f);
}
}