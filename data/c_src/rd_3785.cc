#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan(a);
d=pow(f,a);
d=log10(d);
d=(fmax(d,d))*(cos(e));
c=(floor(d))*(exp(b));
a=(atan(e))+(pow(d,b));
d=pow(c,f);
g=(pow(e,a))+(cos(g));
e=(pow(c,d))/(log10(g));
b=(asin(d))*(fmin(d,c));
a=(atan2(g,f))-(ceil(a));
e=atan(b);
a=(pow(c,d))-(atan(b));
f=fdim(a,e);
}