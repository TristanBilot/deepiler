#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(c,a))/(fmin(b,e));
d=(pow(a,c))*(fdim(b,c));
d=log(b);
b=(atan2(c,e))-(fmin(a,b));
c=(asin(d))/(pow(d,c));
b=(log(b))-(atan(b));
b=(atan(b))*(log(b));
d=fmax(d,e);
c=(atan2(a,b))-(pow(b,c));
a=(atan2(e,b))-(fmin(a,d));
}