#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(a,c);
c=fdim(c,e);
d=(fdim(a,e))*(pow(c,a));
c=(fmin(a,b))-(pow(d,a));
c=fmin(c,d);
b=(sin(e))*(pow(b,b));
while(isless(b,d)){
d=fmax(b,a);
d=pow(a,b);
b=(atan2(a,c))*(pow(e,c));
b=(tan(c))*(acos(e));
}
}