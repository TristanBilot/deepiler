#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(c,g);
e=(fmax(a,d))+(exp(b));
a=(pow(b,h))*(pow(e,a));
while(isgreaterequal(b,g)){
g=fdim(b,c);
h=acos(g);
g=atan2(d,h);
c=(fmin(b,b))-(sin(e));
b=fmin(f,d);
}
while(islessequal(e,g)){
g=(exp(b))*(fmax(e,h));
e=fdim(d,f);
a=pow(e,h);
}
}