#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(sin(d))+(sin(c));
a=(fdim(a,a))+(acos(a));
c=fmin(e,c);
a=asin(e);
while(isless(c,c)){
c=(fdim(e,d))*(fmin(b,c));
e=(pow(d,c))/(sqrt(b));
}
a=(tan(a))-(fmax(b,d));
c=(exp(b))*(fmax(b,a));
a=(log(e))+(fdim(c,a));
}