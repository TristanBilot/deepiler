#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(cos(c))/(atan2(e,e));
d=atan(d);
d=(ceil(b))/(atan2(f,d));
a=cos(e);
c=(exp(h))-(atan2(f,d));
h=asin(a);
c=(asin(b))/(atan2(g,c));
b=log(f);
c=(log10(a))-(atan2(d,c));
f=(atan2(e,e))/(sqrt(g));
}