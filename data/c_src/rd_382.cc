#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(a,c))-(sin(g));
g=log(c);
while(isless(g,f)){
f=sin(c);
e=(tan(c))-(atan(e));
g=pow(g,b);
e=(fdim(a,f))*(pow(f,g));
}
if(isgreaterequal(b,e)){
c=(fmax(a,g))+(asin(b));
b=(fdim(d,c))/(exp(g));
a=(exp(a))+(fmax(a,b));
h=acos(f);
b=acos(b);
}
}