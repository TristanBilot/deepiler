#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(e,b);
a=(log10(a))-(atan2(g,c));
f=(ceil(a))*(ceil(c));
b=(sqrt(g))*(tan(a));
c=(fmax(f,a))-(atan2(f,g));
d=(floor(f))/(floor(b));
g=atan2(c,b);
h=exp(b);
d=pow(b,a);
e=(acos(f))/(pow(e,a));
b=(atan2(h,c))/(exp(f));
e=fdim(h,h);
b=fmin(d,f);
g=ceil(e);
}