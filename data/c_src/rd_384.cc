#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(e,a))-(acos(c));
c=fmin(b,c);
b=log(a);
d=fmax(e,a);
d=log(a);
a=log10(a);
while(isless(c,a)){
d=fmax(d,e);
c=fmax(d,d);
a=(sqrt(c))+(sin(d));
}
}