#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(pow(f,d))/(tan(f));
g=(log(c))*(cos(f));
f=fmax(d,d);
f=(asin(e))-(fmin(d,e));
b=acos(g);
g=(exp(e))/(fmax(d,d));
b=(pow(b,f))/(exp(f));
c=(atan2(e,g))+(atan2(c,d));
e=floor(f);
while(isless(b,a)){
e=(exp(a))-(fmin(f,f));
g=(fmin(f,c))-(fdim(c,d));
f=(fmin(c,a))/(log10(g));
f=fdim(c,d);
}
}