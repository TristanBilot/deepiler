#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(b,e))/(fmax(e,c));
e=(atan(d))/(atan2(e,c));
while(islessgreater(b,a)){
b=(fdim(a,d))/(asin(e));
c=pow(a,a);
}
if(isless(a,a)){
d=(log(d))/(floor(e));
a=(pow(c,e))/(fmin(b,c));
d=(pow(d,c))/(pow(e,b));
}
else{
d=(fmax(e,d))-(fdim(e,e));
b=atan2(b,b);
}
}