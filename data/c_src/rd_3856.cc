#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(c,e);
c=(fmin(b,a))+(atan2(a,d));
while(islessequal(a,d)){
e=log(c);
a=(exp(b))*(fmin(a,e));
b=(cos(e))*(pow(d,b));
}
c=(tan(c))*(atan2(d,a));
b=fmax(e,e);
a=sin(d);
c=atan(d);
e=atan(e);
}