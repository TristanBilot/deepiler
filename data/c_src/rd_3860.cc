#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(c))*(pow(g,b));
a=(log(c))+(sqrt(e));
g=(sqrt(d))/(log10(b));
while(islessgreater(d,b)){
a=sin(g);
c=(fmax(e,c))-(tan(g));
b=(log10(g))-(fmax(a,e));
}
while(islessgreater(d,e)){
e=(fmin(b,f))/(cos(c));
f=cos(a);
f=(pow(g,c))-(fmax(e,f));
e=asin(e);
b=(atan(d))*(floor(c));
}
}