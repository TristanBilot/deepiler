#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log10(c);
c=(atan2(e,b))+(asin(d));
c=fmin(g,c);
while(isgreaterequal(a,g)){
f=atan2(g,c);
a=(floor(a))-(atan2(f,b));
e=atan2(f,g);
}
e=pow(g,c);
a=(exp(b))*(pow(f,b));
c=(fmin(a,e))*(fmin(b,b));
a=pow(f,b);
}