#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(sin(g))*(pow(g,f));
h=asin(d);
while(isgreaterequal(a,g)){
g=(floor(a))-(sin(c));
e=(pow(f,h))+(fmax(a,f));
e=fmax(f,a);
b=log(e);
a=log10(f);
}
d=(pow(b,f))-(fmin(b,g));
h=fmin(d,c);
f=(pow(g,g))+(pow(c,c));
a=(fmax(e,b))*(exp(c));
h=(fmax(e,f))/(atan(f));
}