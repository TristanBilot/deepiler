#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(a);
h=pow(a,e);
e=(pow(h,g))-(atan2(g,a));
f=pow(h,b);
if(isless(b,h)){
c=(atan(e))*(fmin(a,c));
e=(cos(g))/(exp(f));
e=log(h);
c=(log10(g))*(fmax(g,g));
}
while(islessequal(a,c)){
f=asin(b);
c=asin(e);
e=fmax(d,f);
b=sin(e);
a=(ceil(h))-(log(e));
}
}