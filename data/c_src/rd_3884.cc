#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(g,a))/(sqrt(d));
f=sin(c);
f=fmax(g,d);
f=pow(c,g);
a=(log(f))+(ceil(d));
f=log10(a);
b=(fmin(f,c))+(fdim(f,c));
g=exp(c);
if(isless(e,f)){
c=exp(c);
g=(pow(b,f))/(asin(e));
b=(pow(c,b))-(exp(f));
e=fmax(e,a);
}
}