#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(b,c))/(log(c));
b=sin(h);
a=(exp(d))*(fmax(e,c));
h=floor(a);
a=(fdim(g,a))+(fdim(h,f));
e=(log(d))*(exp(c));
e=(fmax(b,f))+(log(d));
g=fmax(b,e);
if(isless(f,a)){
g=fmin(h,d);
b=(fdim(g,g))-(sqrt(g));
}
else{
f=atan2(d,f);
e=(atan(b))+(atan(e));
a=(fdim(e,c))-(atan2(c,e));
a=pow(e,a);
}
}