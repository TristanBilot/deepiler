#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(asin(e))*(fmax(a,d));
e=fmax(g,g);
f=(fdim(a,b))/(fmax(e,c));
e=(exp(g))-(log(c));
while(islessequal(c,a)){
d=(fdim(c,b))/(cos(e));
b=fmin(a,f);
f=(ceil(g))+(cos(c));
d=tan(b);
f=(pow(f,a))*(pow(e,c));
}
if(isless(g,g)){
c=pow(e,d);
c=pow(e,c);
f=sqrt(b);
}
else{
a=(fdim(g,d))*(pow(b,f));
d=(ceil(g))*(atan2(f,f));
g=fdim(a,b);
b=atan(e);
b=atan2(g,b);
}
}