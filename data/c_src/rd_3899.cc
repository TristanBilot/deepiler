#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(b);
d=sqrt(e);
e=(atan(g))-(sqrt(b));
f=cos(a);
if(islessequal(g,b)){
a=atan2(d,d);
a=(fdim(d,f))+(pow(b,e));
f=log(c);
c=(floor(f))*(fmin(b,f));
f=cos(b);
}
else{
d=ceil(b);
b=sin(a);
e=(ceil(e))*(acos(c));
d=(atan(d))*(tan(f));
}
while(isgreaterequal(a,b)){
a=fmax(g,e);
g=(pow(g,e))*(floor(g));
d=exp(e);
c=(fmax(c,b))+(fmax(e,d));
b=ceil(d);
}
}