#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(g,b))+(atan(c));
d=(fdim(c,a))+(pow(a,e));
c=(fdim(e,g))+(pow(c,g));
b=(exp(c))/(sqrt(c));
if(islessgreater(a,d)){
f=fmin(c,a);
g=fmin(d,a);
g=ceil(a);
f=asin(f);
e=(atan2(a,c))*(acos(c));
}
else{
g=fdim(c,a);
e=fmin(e,d);
g=(fdim(a,d))+(sqrt(c));
a=(pow(a,g))*(asin(f));
}
if(islessgreater(d,e)){
b=fmin(d,f);
f=tan(f);
a=fdim(b,f);
f=fmin(c,b);
}
else{
b=(fdim(b,g))*(fmin(f,b));
g=(atan2(a,g))-(fdim(b,f));
}
}