#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fdim(e,a);
a=cos(a);
c=fdim(c,e);
e=fmax(a,c);
while(islessequal(f,f)){
b=(cos(a))/(exp(e));
a=fmax(b,d);
}
e=(pow(e,c))/(fmin(d,b));
b=(fmax(d,b))+(fdim(d,e));
}