#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log(a))/(fmin(a,c));
c=acos(d);
a=sqrt(e);
b=fdim(e,b);
if(isless(e,c)){
d=floor(c);
d=pow(d,b);
a=(asin(a))*(ceil(c));
c=log(a);
b=(fdim(e,c))-(exp(a));
}
else{
d=(sin(a))/(sqrt(c));
a=(exp(c))-(tan(a));
c=asin(a);
d=cos(c);
b=fmin(d,c);
}
while(isless(a,a)){
b=(atan2(e,a))*(atan(e));
b=(atan2(e,d))/(fdim(a,c));
e=atan2(d,c);
a=(log(d))+(log(e));
}
}