#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sin(b);
b=(asin(d))*(log10(g));
g=exp(f);
b=(fmin(d,a))/(pow(e,g));
a=fdim(c,d);
if(islessequal(b,e)){
c=log10(a);
a=atan2(c,g);
e=acos(a);
e=pow(e,c);
}
h=fmin(c,g);
f=fmin(e,g);
c=(atan2(c,c))/(pow(g,b));
}