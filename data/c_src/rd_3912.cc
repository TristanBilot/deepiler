#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(asin(a))+(fmax(a,e));
c=log10(b);
while(isless(d,f)){
e=(ceil(b))*(exp(c));
c=ceil(a);
d=(pow(f,f))/(floor(f));
a=pow(a,e);
}
a=(exp(a))/(floor(f));
f=fmin(b,a);
a=(log(a))-(fmax(d,b));
f=(log(d))*(atan(e));
}