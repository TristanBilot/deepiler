#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(b,c))*(fdim(e,c));
a=exp(a);
e=fdim(c,d);
h=(tan(b))-(fmax(a,e));
f=(log(a))/(atan2(g,d));
if(islessgreater(d,h)){
d=pow(c,h);
e=asin(c);
d=(exp(c))/(fmax(c,e));
}
while(islessequal(a,g)){
g=fmin(c,g);
a=(pow(b,f))*(log(a));
e=atan2(d,d);
f=(log10(f))+(fmin(h,h));
}
}