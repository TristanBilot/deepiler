#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(d);
b=log10(e);
g=floor(g);
f=asin(b);
g=log(e);
while(isgreaterequal(b,f)){
g=fmax(e,h);
h=log10(d);
a=fdim(a,f);
b=pow(c,e);
}
while(islessgreater(d,a)){
f=atan(h);
f=(cos(d))/(fmin(h,h));
e=(fmin(g,d))-(pow(a,b));
d=acos(f);
}
}