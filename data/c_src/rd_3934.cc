#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(d);
a=(floor(h))-(cos(g));
c=(ceil(e))*(sin(f));
d=log10(h);
d=asin(e);
if(isgreaterequal(f,f)){
d=fdim(h,d);
a=ceil(e);
c=(fmax(f,b))+(acos(d));
e=fdim(h,f);
c=atan2(b,g);
}
while(isgreaterequal(h,c)){
f=(fdim(h,d))*(fdim(c,b));
e=(atan2(d,g))/(fmin(f,b));
c=(floor(e))+(ceil(a));
}
}