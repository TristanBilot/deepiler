#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sqrt(c))/(log(f));
e=(fmin(a,b))/(fmax(d,f));
d=(sqrt(f))+(log(b));
if(isless(e,f)){
e=fdim(f,b);
d=(fmin(f,c))+(log10(c));
}
while(islessgreater(c,a)){
d=pow(c,f);
e=exp(e);
f=fdim(c,b);
a=(cos(b))*(pow(c,d));
}
}