#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log(b);
c=(fmax(a,b))+(atan(a));
d=(log(e))/(fmax(e,b));
a=fdim(e,a);
while(isless(d,b)){
d=(atan(b))+(sin(c));
c=(fmin(c,b))-(atan2(b,a));
c=fdim(a,b);
b=fdim(b,a);
a=pow(d,c);
}
while(islessgreater(d,a)){
e=atan2(b,e);
b=(fdim(d,e))+(log(b));
c=fdim(b,b);
d=sqrt(b);
}
}