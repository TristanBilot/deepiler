#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(e,a))/(atan2(d,a));
e=(sqrt(b))-(pow(d,c));
a=fmin(a,e);
e=(atan2(b,b))/(fdim(d,e));
if(islessequal(b,b)){
e=fmax(b,e);
d=(atan2(c,c))-(fmax(e,e));
}
else{
c=(pow(b,c))*(ceil(a));
c=fdim(b,a);
a=exp(c);
e=(atan(d))*(log10(b));
e=(fmax(d,c))/(asin(c));
}
d=fmax(c,e);
b=exp(b);
}