#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(c))+(sin(a));
c=fmax(c,e);
d=(pow(e,d))/(exp(e));
d=(atan2(c,e))/(pow(a,d));
c=(fmax(e,d))/(fmin(c,d));
a=fmax(d,c);
d=fmax(c,e);
a=(pow(a,a))/(atan2(a,c));
d=fdim(e,b);
e=cos(b);
d=(fmax(a,e))-(floor(a));
}