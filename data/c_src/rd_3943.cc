#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(b);
f=tan(g);
d=ceil(g);
e=fmin(g,d);
if(islessequal(b,e)){
f=fmax(g,f);
f=(fdim(f,a))-(log(b));
}
while(isgreaterequal(a,g)){
c=(pow(d,b))/(fmin(d,a));
g=fmin(e,g);
}
}