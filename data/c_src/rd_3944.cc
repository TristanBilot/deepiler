#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(c,a);
f=cos(g);
a=(fmin(a,f))/(pow(e,c));
a=(floor(b))+(log10(d));
f=floor(c);
d=(log10(b))*(log10(a));
f=fdim(g,d);
f=(atan2(a,c))-(log(d));
}