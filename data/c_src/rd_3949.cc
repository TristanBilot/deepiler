#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=sin(d);
b=fmax(c,b);
a=(acos(a))*(pow(e,c));
c=(sin(a))/(fmin(b,b));
while(isgreaterequal(e,a)){
e=(fdim(a,b))-(atan2(d,b));
c=(acos(b))-(pow(d,d));
e=(fmax(d,c))+(pow(e,a));
e=fdim(b,c);
}
while(islessgreater(e,e)){
e=fmin(d,b);
a=(fmax(d,e))-(cos(b));
}
}