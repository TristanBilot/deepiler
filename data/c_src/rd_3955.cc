#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(exp(a))+(cos(b));
e=sqrt(e);
a=(pow(d,e))-(fmin(a,g));
b=(sqrt(g))/(fmin(e,d));
if(islessequal(c,e)){
f=(fmin(c,g))/(fmax(b,b));
e=cos(b);
f=pow(a,g);
f=atan(b);
c=(fmax(f,d))+(fmax(d,c));
}
else{
f=fmin(a,g);
g=(atan(d))-(exp(a));
g=fdim(a,c);
b=(fmin(c,d))/(tan(b));
}
c=(tan(e))/(acos(e));
c=log10(b);
b=pow(c,a);
d=log(f);
}