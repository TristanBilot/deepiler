#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmin(c,a))*(asin(e));
b=fmin(e,d);
while(islessequal(c,a)){
d=(exp(c))*(fmax(a,a));
a=(log(f))+(pow(b,b));
}
if(islessgreater(e,b)){
c=fmax(f,d);
a=asin(b);
}
else{
c=(pow(d,c))/(pow(b,b));
c=floor(b);
b=(pow(c,d))+(acos(c));
d=atan2(c,d);
a=fmin(a,d);
}
}