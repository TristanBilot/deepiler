#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(e,c))/(log(c));
f=sin(b);
e=exp(g);
g=(cos(a))*(floor(g));
d=(fmax(a,a))+(sin(g));
c=ceil(h);
f=(fdim(e,b))-(asin(f));
c=cos(d);
h=exp(b);
a=fdim(g,h);
}