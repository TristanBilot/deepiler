#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=tan(g);
d=(sqrt(b))+(exp(c));
while(islessequal(c,b)){
h=(fdim(f,b))-(atan2(d,a));
f=(exp(c))-(fdim(e,g));
e=(fmax(d,b))*(sin(b));
}
d=(log(f))/(atan2(d,g));
g=(fdim(d,b))/(atan2(e,d));
b=(exp(b))+(fdim(b,b));
b=(atan2(e,b))/(tan(g));
g=(atan2(c,d))-(fmax(d,c));
}