#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(g);
c=(atan(b))-(floor(a));
b=(tan(h))/(sin(g));
f=exp(h);
d=pow(c,c);
while(isless(g,g)){
a=asin(c);
e=(fmin(d,h))-(pow(f,f));
f=(fdim(f,e))*(exp(c));
a=floor(f);
a=fmin(d,c);
}
}