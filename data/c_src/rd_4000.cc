#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(acos(c))-(pow(c,e));
b=(log(d))-(fmax(e,a));
d=atan2(b,a);
c=(fdim(d,e))-(pow(d,a));
a=(fmin(d,a))/(exp(a));
e=(pow(b,d))+(atan(e));
c=(asin(e))/(fmax(e,e));
b=pow(a,b);
}