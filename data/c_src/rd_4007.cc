#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(d);
f=log(e);
if(islessequal(c,d)){
b=(asin(d))*(log10(e));
c=(asin(e))-(fdim(e,c));
}
if(islessequal(g,f)){
g=(atan2(a,c))/(fdim(e,a));
e=(atan2(a,d))+(atan2(a,c));
c=(fmin(d,c))-(log10(f));
d=fmin(b,a);
c=atan(a);
}
else{
a=ceil(a);
b=sqrt(b);
g=(fmin(g,d))+(fmax(e,c));
b=(log10(f))+(pow(f,e));
e=fdim(f,a);
}
}