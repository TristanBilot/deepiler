#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,b))/(fmax(d,d));
b=(atan2(b,a))/(fmax(a,c));
if(isless(b,a)){
d=fdim(e,a);
a=fmax(d,d);
}
e=fdim(b,b);
b=(fmax(d,e))-(fmin(a,a));
}