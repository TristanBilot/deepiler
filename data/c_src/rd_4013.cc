#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fdim(a,d);
a=(fmin(e,e))+(ceil(b));
c=fmin(e,b);
g=atan(b);
d=atan2(g,a);
if(islessequal(f,f)){
e=floor(c);
e=acos(e);
}
if(islessgreater(f,g)){
g=atan2(a,b);
g=(atan(e))/(tan(e));
}
else{
e=tan(g);
d=(pow(b,a))/(atan2(g,c));
f=(atan2(d,d))/(cos(g));
a=fmax(g,b);
b=(atan2(e,c))/(sin(g));
}
}