#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(b,c);
b=fmin(c,e);
e=fmin(a,b);
b=(fdim(a,a))*(floor(b));
c=fmin(c,a);
d=ceil(e);
d=atan2(e,a);
a=atan2(e,a);
e=(ceil(c))*(fmax(b,c));
c=asin(c);
c=(asin(b))-(acos(c));
d=(sqrt(b))*(asin(e));
d=cos(c);
}