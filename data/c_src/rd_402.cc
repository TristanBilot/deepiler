#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(a);
e=(ceil(a))-(pow(e,e));
a=(pow(c,d))*(exp(a));
b=(fmax(a,d))*(asin(b));
d=(fmin(c,e))-(fmin(b,c));
while(islessequal(a,a)){
b=(fdim(b,e))*(exp(a));
d=(fdim(b,c))+(atan(a));
b=acos(a);
c=(fmax(e,b))*(sin(c));
}
if(islessequal(e,e)){
b=(cos(c))/(atan2(d,a));
a=ceil(d);
e=(atan(a))+(log(b));
}
}