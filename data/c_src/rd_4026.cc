#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(d,b);
e=(asin(h))-(cos(e));
h=(ceil(e))/(exp(g));
g=(exp(c))/(cos(g));
h=(fmin(d,c))-(fdim(b,f));
c=fdim(a,g);
while(isgreaterequal(b,b)){
f=(asin(b))-(fmin(h,h));
d=(atan(g))-(fdim(a,a));
f=(pow(g,b))*(tan(h));
g=(exp(h))-(fmin(d,g));
}
}