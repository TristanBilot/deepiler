#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,d))-(exp(d));
d=fmin(c,e);
d=(fmax(b,c))+(exp(e));
b=atan(b);
a=log(b);
a=atan2(e,e);
d=(fmin(c,d))+(fmin(e,c));
a=(atan2(e,a))/(sqrt(d));
while(isless(c,e)){
c=(fdim(d,d))-(atan2(d,d));
a=(fmin(c,b))/(fmax(a,d));
e=(cos(a))-(atan2(a,d));
b=(fmin(a,a))/(log10(e));
d=pow(e,e);
}
}