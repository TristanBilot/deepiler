#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=atan(b);
a=pow(f,a);
e=(asin(b))+(floor(c));
f=(pow(d,f))/(fmin(f,e));
d=sqrt(a);
if(islessgreater(f,d)){
a=fmin(e,a);
f=atan2(e,b);
}
}