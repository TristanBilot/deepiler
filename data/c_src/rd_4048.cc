#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(d,e))-(floor(d));
b=(cos(d))*(log10(a));
d=fmax(a,c);
while(islessequal(d,b)){
b=floor(c);
c=acos(c);
d=(fmax(d,d))-(floor(e));
e=(floor(c))*(sin(b));
}
if(islessequal(a,c)){
c=(asin(d))*(ceil(c));
e=exp(a);
b=atan2(d,d);
}
else{
a=(fmax(e,c))*(fmin(c,a));
d=(fmin(d,b))*(acos(d));
e=atan2(d,a);
c=(fmax(d,a))*(floor(c));
e=(sin(e))/(log(d));
}
}