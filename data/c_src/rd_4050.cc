#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(ceil(h))*(log10(c));
c=floor(a);
d=(fmax(h,b))/(log(h));
c=floor(c);
e=fmax(f,h);
g=ceil(b);
while(isless(h,e)){
a=fdim(c,c);
b=pow(d,c);
c=(tan(f))/(pow(g,a));
a=(fdim(d,c))*(atan2(h,a));
b=(log10(g))/(acos(b));
}
}