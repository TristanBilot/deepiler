#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(g,g))-(fmax(d,c));
d=(pow(f,d))*(fmin(d,a));
e=(sqrt(d))*(fdim(c,e));
g=atan(c);
a=(tan(f))*(fdim(g,a));
c=(atan2(b,g))/(cos(g));
f=(log10(b))+(cos(c));
}