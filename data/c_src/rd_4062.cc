#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(e,d);
a=(fmin(b,f))-(log10(c));
g=(ceil(d))*(sqrt(d));
e=cos(b);
if(isless(h,g)){
a=fdim(a,e);
c=tan(d);
f=(ceil(b))/(cos(a));
}
else{
d=fmin(c,d);
a=acos(d);
b=(pow(f,e))/(ceil(h));
d=fdim(b,f);
}
c=tan(g);
e=sqrt(e);
h=log(a);
g=(fdim(a,d))*(pow(g,c));
c=log(h);
}