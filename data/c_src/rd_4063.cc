#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(b,d))+(exp(d));
e=(fmin(a,c))-(asin(c));
c=(atan(c))/(exp(a));
if(islessequal(a,c)){
b=(floor(c))*(fdim(b,c));
d=(log10(c))-(fmax(a,b));
}
if(islessequal(d,c)){
c=sqrt(e);
a=(exp(d))/(pow(d,e));
a=log(a);
}
else{
e=(tan(c))-(sqrt(b));
a=fdim(c,c);
e=asin(a);
}
}