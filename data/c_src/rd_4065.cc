#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan(d))-(fmax(d,c));
d=(log10(d))*(fdim(a,e));
if(isless(b,c)){
d=(atan2(c,b))-(exp(c));
b=(atan2(e,d))/(cos(d));
}
while(isgreaterequal(b,b)){
c=(sin(b))+(fdim(e,b));
b=(atan2(e,e))+(cos(c));
d=(fmax(c,e))-(tan(e));
b=fdim(e,e);
}
}