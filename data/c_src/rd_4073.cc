#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(a,c))/(exp(e));
b=fmax(b,c);
b=pow(d,c);
b=pow(a,a);
b=(fmax(a,d))+(fmin(c,b));
e=(log(b))-(floor(a));
while(isgreaterequal(b,a)){
d=(asin(c))/(sin(d));
d=atan2(c,a);
b=pow(e,e);
c=log10(b);
c=(pow(b,c))+(log10(b));
}
}