#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(log10(e))/(fmin(f,c));
c=cos(e);
f=fdim(e,a);
e=(floor(a))/(acos(c));
b=acos(b);
f=fmax(g,d);
a=(fdim(a,f))/(fmax(f,e));
if(islessequal(g,e)){
c=(pow(d,b))/(fmax(f,e));
f=(cos(h))-(floor(d));
b=(pow(a,e))/(fmin(h,d));
h=(fmax(h,d))/(atan2(h,h));
h=(pow(b,c))/(fmin(h,c));
}
else{
f=(fmin(d,e))-(fmin(h,g));
g=(fmax(g,b))*(sqrt(c));
c=(acos(h))*(fdim(b,b));
c=(pow(b,f))-(sqrt(b));
}
}