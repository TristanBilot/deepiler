#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(floor(d))/(fmax(f,a));
d=(pow(e,b))*(fmin(a,a));
f=fmin(e,g);
if(isless(f,a)){
c=(fmin(b,a))+(ceil(b));
d=(log(f))/(fmax(d,b));
e=atan(a);
e=(pow(g,e))-(ceil(b));
}
else{
a=fmin(d,e);
a=fmin(d,c);
a=pow(g,a);
b=log(b);
}
if(isless(g,g)){
f=ceil(f);
a=(cos(g))*(fmax(d,f));
c=fmax(d,f);
c=(sin(e))/(pow(f,c));
}
}