#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(a,c))/(atan2(c,c));
e=(ceil(c))-(pow(d,c));
d=pow(a,e);
while(isgreaterequal(c,e)){
d=(fmax(b,e))/(log10(b));
e=exp(d);
}
e=sin(c);
c=(tan(e))*(fmax(a,b));
c=(atan2(e,a))/(ceil(a));
a=(tan(b))-(cos(e));
c=asin(b);
}