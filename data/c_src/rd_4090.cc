#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(a,d))+(fdim(c,d));
a=fdim(f,c);
f=(ceil(f))-(pow(f,a));
if(isless(a,d)){
c=(asin(g))+(pow(e,a));
f=(fmin(e,d))+(pow(d,c));
}
if(islessequal(e,e)){
a=pow(f,a);
b=fdim(h,e);
c=(cos(c))/(atan(g));
g=(log10(d))/(fmin(g,f));
}
}