#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(atan2(e,a))-(cos(e));
d=(tan(c))+(cos(c));
b=log(c);
e=(cos(a))+(atan2(b,b));
while(isless(d,e)){
b=fmax(d,d);
d=acos(e);
}
if(isless(a,e)){
b=(floor(c))+(fmax(e,c));
b=(asin(e))-(floor(e));
a=(cos(d))/(atan2(b,e));
a=atan2(b,a);
a=fmin(b,c);
}
}