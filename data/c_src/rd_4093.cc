#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(c,b))+(sqrt(d));
a=log10(e);
e=fmin(b,c);
c=atan(c);
while(isgreaterequal(b,a)){
c=log10(a);
a=(fdim(e,d))*(pow(e,d));
c=fdim(d,a);
}
if(isless(b,b)){
a=pow(e,b);
a=(exp(c))-(fmax(c,d));
a=(atan(b))/(sqrt(b));
c=(acos(d))*(atan2(a,c));
}
}