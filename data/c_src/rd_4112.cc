#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(b,e);
b=(pow(b,a))/(tan(e));
d=(log10(b))/(log10(c));
a=pow(b,e);
d=(fdim(d,b))-(exp(e));
if(isless(a,a)){
a=cos(c);
c=pow(c,b);
b=fmax(e,d);
}
else{
b=(tan(b))*(fdim(e,e));
b=(floor(e))/(tan(c));
}
b=atan2(e,a);
d=fdim(d,c);
d=fdim(d,c);
}