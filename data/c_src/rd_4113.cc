#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(g,d);
b=(fmax(c,d))/(fdim(g,e));
d=fmax(f,f);
while(isgreaterequal(a,e)){
b=(fmin(e,a))-(pow(b,d));
a=fdim(f,f);
d=fmin(c,e);
e=cos(f);
c=(fmax(f,e))*(pow(b,g));
}
if(islessequal(f,a)){
e=(atan(d))-(atan(g));
a=fdim(a,c);
}
else{
e=exp(g);
a=(exp(a))-(log10(a));
}
}