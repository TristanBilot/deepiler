#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(log10(b))*(log10(a));
a=(log(e))-(fmax(f,f));
d=atan2(f,a);
e=(ceil(e))/(tan(d));
while(islessgreater(b,c)){
e=(pow(b,c))/(log10(b));
f=fdim(a,f);
d=(atan2(b,e))+(cos(c));
}
while(islessgreater(e,e)){
f=(fdim(b,c))*(asin(a));
f=(floor(f))+(floor(c));
d=fmin(d,c);
}
}