#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(e,b))*(log10(b));
c=(atan2(b,a))/(atan2(a,e));
a=(sin(a))/(log10(e));
while(isless(a,e)){
e=(fdim(c,b))*(floor(d));
e=(pow(d,e))/(fdim(c,d));
e=(fdim(c,e))/(asin(e));
}
d=cos(c);
a=fmin(d,e);
a=fdim(c,a);
b=(floor(d))/(fmin(e,b));
a=(cos(d))/(tan(c));
}