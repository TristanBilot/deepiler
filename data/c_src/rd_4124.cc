#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(a,e))/(tan(f));
a=asin(d);
f=fdim(e,f);
a=(fmin(f,c))*(sin(f));
if(isgreaterequal(a,f)){
a=fmax(f,f);
c=fmin(a,e);
}
if(islessequal(b,c)){
b=exp(d);
e=(fmin(a,d))-(sqrt(e));
f=(cos(a))*(fdim(b,f));
c=pow(c,f);
f=asin(e);
}
}