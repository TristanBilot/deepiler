#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(d);
g=fmin(f,e);
f=(exp(b))/(fmax(c,a));
b=(fdim(g,e))+(sin(f));
d=fmax(h,b);
h=pow(h,f);
d=fmax(h,h);
c=(ceil(a))+(fmin(c,c));
}