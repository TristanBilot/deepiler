#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(e,h);
g=(log(d))*(fmin(e,c));
f=(fdim(e,b))-(asin(c));
b=fmin(b,e);
g=(pow(a,a))+(pow(g,d));
a=pow(b,h);
a=fmin(h,h);
h=fmin(f,a);
f=(cos(d))*(log10(h));
b=(exp(f))/(atan(c));
b=pow(f,c);
c=sqrt(a);
}