#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(f))-(pow(e,e));
d=fdim(b,d);
if(isgreaterequal(e,f)){
f=fmin(d,a);
b=acos(d);
b=(tan(e))/(atan2(d,b));
a=(fmax(c,c))+(fmax(f,d));
}
else{
d=fmin(d,d);
e=(atan2(e,c))/(fmax(c,d));
}
if(isless(c,e)){
c=(floor(c))+(log10(a));
a=atan2(b,d);
}
else{
b=pow(d,b);
c=pow(a,b);
c=log(a);
}
}