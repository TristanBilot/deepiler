#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(e);
d=(exp(a))-(asin(b));
while(isgreaterequal(a,d)){
h=log(f);
b=(fmax(d,b))/(tan(c));
c=floor(h);
}
e=fdim(d,h);
f=floor(c);
}