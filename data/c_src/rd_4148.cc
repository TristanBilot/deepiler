#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(d);
b=(fmax(a,b))-(sqrt(d));
d=(fmax(e,c))*(fmin(a,e));
a=ceil(e);
a=fmax(c,d);
a=log(e);
c=sin(a);
if(isless(e,c)){
b=(acos(e))*(ceil(d));
a=pow(e,a);
}
}