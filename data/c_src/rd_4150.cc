#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(sqrt(b))-(pow(e,d));
c=fdim(e,c);
e=(fdim(e,d))+(sin(b));
a=(pow(d,d))*(pow(b,a));
if(isgreaterequal(b,c)){
a=log10(a);
b=(atan2(d,a))*(fmax(b,e));
}
d=atan2(d,e);
a=(sqrt(d))*(log(d));
a=fmax(c,d);
}