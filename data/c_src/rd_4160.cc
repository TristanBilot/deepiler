#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(c);
c=(tan(f))/(fdim(d,d));
c=(cos(f))-(ceil(d));
d=(pow(g,f))*(log(b));
h=fmax(h,e);
while(isgreaterequal(c,b)){
h=(atan2(f,g))+(ceil(a));
h=(exp(g))/(fmax(a,d));
}
h=(fmax(d,g))/(acos(h));
g=cos(g);
b=fmax(e,b);
g=(floor(h))/(log10(c));
a=(atan(h))+(fmax(d,d));
}