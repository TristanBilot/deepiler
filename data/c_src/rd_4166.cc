#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(a,e);
c=floor(a);
b=exp(b);
while(islessequal(c,d)){
d=(tan(c))-(pow(d,e));
c=log10(d);
b=(fdim(e,a))-(floor(c));
b=fmax(b,d);
c=fmax(d,b);
}
a=pow(a,a);
c=tan(d);
c=(ceil(d))*(log10(c));
c=(atan2(a,b))/(fmin(a,a));
}