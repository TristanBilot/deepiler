#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fdim(b,b);
e=(sin(d))*(atan2(g,d));
f=(exp(a))-(fmax(f,e));
e=atan2(a,e);
b=asin(g);
f=sin(a);
a=floor(g);
b=asin(f);
while(islessequal(g,c)){
c=(fmax(e,d))-(tan(a));
f=fmin(a,d);
}
}