#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan(d);
d=ceil(c);
c=(asin(e))+(asin(c));
c=(tan(e))/(asin(d));
e=fmax(a,c);
if(isless(e,b)){
e=log10(a);
b=(log(e))*(fmin(a,c));
b=atan2(a,b);
b=(cos(b))*(fdim(e,b));
e=(fdim(a,e))*(fmax(d,b));
}
else{
a=fdim(a,d);
b=(atan2(d,a))/(log(b));
c=(pow(d,d))/(tan(a));
}
if(isgreaterequal(a,a)){
b=(atan(d))*(fmin(d,b));
d=(log10(d))-(fmin(e,c));
}
else{
a=floor(c);
c=atan2(a,d);
}
}