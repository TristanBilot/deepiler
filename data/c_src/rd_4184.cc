#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmax(f,g);
e=(tan(a))-(atan(f));
e=(fdim(e,c))/(atan2(e,b));
if(islessequal(b,f)){
f=(atan2(c,e))*(log10(e));
b=(log10(e))/(log10(f));
b=(fmin(d,a))-(cos(c));
f=atan2(c,a);
d=log10(b);
}
if(isless(e,e)){
c=log10(e);
c=atan2(d,f);
f=atan(a);
d=(pow(c,f))-(fdim(a,e));
}
else{
d=pow(b,a);
d=(log(a))+(atan(d));
d=(fmax(g,g))/(fmax(c,e));
c=fmax(c,f);
g=fmax(b,d);
}
}