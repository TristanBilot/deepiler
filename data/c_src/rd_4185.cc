#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan2(f,h))/(log(d));
d=fdim(a,f);
while(islessequal(c,d)){
h=(fdim(a,g))*(exp(f));
f=fdim(f,g);
c=fdim(g,a);
}
a=(fmax(h,g))/(fdim(c,d));
b=fmax(h,h);
e=(atan2(g,c))/(fmax(h,b));
g=pow(e,b);
}