#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(e,a))/(atan(a));
a=fmax(d,e);
e=(log10(b))*(atan(d));
c=sin(a);
d=fdim(b,e);
while(islessgreater(b,e)){
a=(fmax(c,d))*(pow(b,b));
d=(fdim(e,d))*(ceil(d));
e=atan(e);
}
while(isless(a,b)){
d=fdim(a,c);
a=(pow(e,a))-(fmin(c,b));
d=(log(e))+(asin(a));
}
}