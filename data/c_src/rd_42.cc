#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(sin(d))-(fmin(a,d));
b=(fmax(e,d))-(log(c));
f=(ceil(b))+(fmin(b,b));
f=fmax(b,c);
b=atan2(a,a);
d=sqrt(b);
e=(ceil(f))+(fmax(c,a));
f=atan2(f,d);
c=atan2(f,c);
a=pow(d,e);
while(isless(c,e)){
d=floor(a);
e=(atan2(c,c))*(atan(e));
}
}