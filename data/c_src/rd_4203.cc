#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(e,d))+(fdim(c,a));
a=fmin(e,c);
b=(ceil(b))*(atan2(d,a));
e=fmax(c,b);
c=fdim(a,e);
d=(asin(a))/(fdim(b,c));
while(isgreaterequal(d,a)){
b=ceil(c);
a=tan(e);
e=sqrt(e);
c=(pow(a,c))/(fmax(d,a));
}
}