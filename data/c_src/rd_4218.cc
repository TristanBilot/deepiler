#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(d,b);
f=(ceil(f))-(ceil(g));
f=sin(f);
e=(pow(f,h))/(pow(h,g));
c=log(h);
f=(log(a))/(acos(f));
h=pow(c,a);
g=(fmin(e,g))*(pow(c,g));
h=(pow(g,g))/(pow(e,c));
}