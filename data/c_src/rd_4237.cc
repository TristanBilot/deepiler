#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(tan(f))*(sin(c));
d=(fmax(c,f))+(pow(a,e));
while(isless(c,d)){
g=fmax(f,d);
d=(fdim(g,b))*(fmin(c,c));
a=(sqrt(c))*(acos(a));
}
if(isgreaterequal(a,g)){
f=(fdim(c,d))/(log10(e));
d=pow(g,e);
}
else{
f=(log10(f))*(floor(b));
f=cos(e);
f=fmin(e,b);
d=(pow(e,d))/(fmax(a,d));
}
}