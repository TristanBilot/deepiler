#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(b,a))/(exp(b));
d=(fmax(d,b))*(asin(f));
e=tan(c);
b=(log(g))*(pow(g,b));
if(islessequal(c,e)){
d=(pow(a,e))*(atan2(b,g));
b=(exp(e))/(asin(f));
}
while(islessgreater(g,d)){
b=(sqrt(a))*(atan2(f,f));
f=exp(e);
f=fmax(d,e);
a=(exp(g))+(atan2(g,c));
g=fmax(e,d);
}
}