#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(b,g))*(atan2(e,d));
f=(tan(a))/(fmin(b,f));
d=(fmin(h,f))/(atan2(e,a));
while(islessgreater(f,h)){
g=(fmax(h,c))/(pow(c,h));
d=(fmin(d,h))/(fmin(f,b));
g=atan2(h,g);
g=exp(b);
b=(atan2(h,g))*(fdim(h,d));
}
if(isless(b,c)){
b=cos(f);
a=fmax(c,a);
c=sin(h);
a=(cos(b))*(sin(h));
}
}