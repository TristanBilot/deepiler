#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log(a);
d=(atan(d))/(asin(a));
c=atan2(e,f);
a=asin(e);
while(isgreaterequal(a,b)){
b=(acos(f))+(fdim(e,b));
e=atan2(a,b);
e=log10(c);
b=(acos(d))/(pow(a,b));
a=floor(a);
}
if(isless(c,d)){
e=(fmin(f,c))+(log(a));
a=(sin(a))/(fdim(c,b));
f=log10(d);
}
}