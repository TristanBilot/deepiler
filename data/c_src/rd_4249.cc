#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(b,d);
c=(log10(e))+(pow(d,c));
e=(atan(b))/(fmin(d,d));
if(isgreaterequal(b,a)){
b=fmax(c,c);
e=(fdim(a,c))*(fdim(b,b));
}
else{
c=fdim(c,c);
d=(log10(c))/(cos(a));
c=(fmin(d,a))-(fdim(e,d));
}
while(isgreaterequal(d,a)){
a=(acos(b))/(exp(a));
b=(ceil(e))+(pow(b,c));
c=(log10(c))*(asin(b));
b=(fmax(b,d))/(sin(a));
}
}