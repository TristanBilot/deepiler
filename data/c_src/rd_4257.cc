#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan(a))*(tan(c));
d=(atan2(f,c))+(fmin(b,e));
b=(fdim(d,f))*(fdim(b,a));
d=(ceil(b))+(cos(e));
a=atan2(b,c);
d=fdim(a,e);
while(isgreaterequal(c,f)){
a=(exp(d))*(floor(d));
e=pow(b,c);
f=atan(e);
}
}