#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(b,e);
g=(floor(a))-(pow(a,f));
b=atan2(d,h);
e=pow(b,e);
while(islessgreater(a,h)){
h=fmin(b,b);
f=atan2(g,f);
f=ceil(h);
}
b=pow(g,g);
e=(sin(f))+(pow(h,a));
g=(exp(e))/(fmax(a,e));
h=(ceil(b))/(sqrt(c));
e=log(f);
}