#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fmin(e,b))*(fdim(f,f));
f=fdim(c,f);
while(isless(f,e)){
c=pow(e,c);
a=(atan2(c,f))*(pow(b,e));
b=atan(b);
b=(ceil(b))/(pow(d,a));
f=fmin(c,b);
}
if(islessgreater(d,f)){
e=(atan2(e,d))+(sqrt(f));
e=atan(a);
d=ceil(a);
b=asin(e);
}
}