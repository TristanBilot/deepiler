#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(atan(e))-(asin(e));
f=pow(c,c);
f=(atan(b))-(tan(e));
b=(tan(e))/(exp(b));
d=tan(e);
while(isless(f,b)){
d=atan(d);
c=asin(c);
b=(fmax(b,c))*(atan2(b,a));
a=pow(e,f);
}
}