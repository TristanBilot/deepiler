#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=ceil(e);
e=(fmin(e,d))-(sin(d));
if(isgreaterequal(d,a)){
c=(asin(d))-(log(e));
a=(fmax(e,c))*(floor(c));
}
else{
c=fmax(a,c);
b=(acos(b))/(cos(d));
}
b=(sqrt(e))-(fmax(b,e));
b=(fmin(b,e))*(log(a));
c=atan2(c,b);
}