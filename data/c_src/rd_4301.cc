#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=acos(a);
f=(pow(b,c))+(cos(e));
f=(log10(c))/(atan2(d,f));
c=fmax(f,c);
if(isless(b,e)){
b=fdim(c,a);
f=(fmax(a,a))*(exp(e));
c=(fmin(c,c))-(fdim(b,c));
a=(fdim(b,f))*(pow(d,f));
}
e=(tan(f))-(atan2(f,c));
b=(fmin(f,d))*(fmax(e,a));
c=(cos(a))+(fdim(a,a));
f=pow(b,b);
}