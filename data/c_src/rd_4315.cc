#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(cos(a))-(ceil(c));
f=fdim(e,c);
e=(tan(b))-(tan(d));
if(isless(f,f)){
f=(log(e))*(exp(c));
a=sqrt(b);
c=log10(d);
d=(ceil(e))-(tan(b));
b=(fmax(f,c))-(fdim(c,b));
}
if(islessgreater(b,d)){
d=atan2(e,b);
c=atan2(d,f);
f=tan(e);
}
}