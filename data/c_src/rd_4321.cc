#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(c,a);
b=exp(a);
b=(fmax(c,b))+(fdim(e,b));
d=atan2(e,d);
b=(ceil(c))-(asin(d));
d=fdim(c,a);
a=(sqrt(b))/(asin(e));
a=fmax(b,a);
e=floor(a);
while(isgreaterequal(c,d)){
a=(pow(d,e))*(fdim(b,b));
d=(fmax(d,e))-(fmin(d,a));
d=(pow(c,c))+(sqrt(c));
d=floor(d);
}
}