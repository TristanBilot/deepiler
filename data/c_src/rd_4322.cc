#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log(a))-(pow(b,b));
d=pow(c,c);
a=(log10(a))/(atan2(g,d));
c=pow(c,e);
a=(fmax(c,f))-(atan2(g,c));
while(isless(b,d)){
g=tan(e);
d=(exp(c))-(pow(e,c));
f=atan(g);
g=(atan(f))-(ceil(b));
}
while(islessequal(f,g)){
b=(ceil(b))-(cos(f));
c=(cos(d))*(exp(b));
a=(fmax(f,e))-(atan(f));
c=log(e);
}
}