#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(a,b);
d=(log10(b))*(asin(c));
f=fmax(d,b);
c=(ceil(d))-(ceil(c));
while(islessequal(e,b)){
c=(fdim(d,c))/(pow(f,d));
b=(fmax(b,f))-(atan2(b,d));
b=fmin(f,a);
}
if(isless(b,f)){
e=exp(c);
c=(log(c))*(fmax(b,a));
e=fdim(d,a);
}
}