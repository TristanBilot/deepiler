#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(e,e))*(atan2(a,e));
e=pow(g,a);
a=(pow(d,a))-(fmax(e,e));
f=cos(b);
while(isless(a,b)){
b=(tan(c))+(fdim(f,a));
g=fmax(a,e);
b=(atan2(a,e))/(cos(f));
a=log10(c);
e=(atan2(f,e))-(atan2(c,f));
}
g=fmax(g,e);
f=(fmax(b,c))-(atan2(f,a));
b=fmin(b,a);
e=sqrt(d);
a=atan2(f,g);
}