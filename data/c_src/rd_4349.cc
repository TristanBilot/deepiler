#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(d,d);
b=pow(c,e);
d=(exp(b))-(sqrt(e));
a=(log(c))/(fmax(b,a));
e=(ceil(b))-(acos(d));
if(islessgreater(d,c)){
b=(fdim(c,b))*(log(a));
a=(acos(e))/(ceil(e));
c=sqrt(d);
}
else{
e=floor(a);
c=(pow(e,d))-(fmax(d,d));
}
b=(pow(a,e))/(acos(e));
e=(atan2(c,a))*(pow(b,b));
c=(exp(e))/(tan(c));
e=(atan2(e,d))-(fmin(b,b));
c=(sqrt(c))/(fdim(e,e));
}