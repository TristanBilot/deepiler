#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=sin(e);
g=(exp(e))-(sqrt(d));
g=atan2(g,g);
b=tan(g);
while(isless(a,a)){
c=fdim(b,f);
g=fmax(f,g);
}
if(isless(b,d)){
g=fmax(d,d);
e=acos(f);
}
}