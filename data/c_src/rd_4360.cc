#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(b,e))-(fmin(d,h));
f=(atan2(d,d))*(atan(b));
e=(fmin(g,g))*(fmin(c,f));
f=(atan2(b,d))/(sin(a));
a=floor(c);
if(isgreaterequal(g,b)){
c=(atan(g))/(sin(c));
c=asin(d);
f=(sin(h))-(atan(g));
}
if(islessgreater(b,e)){
c=(ceil(g))-(fmax(g,e));
c=(pow(f,d))-(atan2(c,d));
e=sin(d);
d=(sin(f))/(pow(b,h));
}
else{
g=(fdim(d,h))+(asin(h));
a=pow(h,a);
c=(fmin(c,g))*(floor(f));
a=(fmin(a,c))-(sqrt(f));
}
}