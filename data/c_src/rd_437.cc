#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(d))*(atan(a));
d=(acos(a))-(asin(b));
a=(atan2(d,d))*(sqrt(c));
a=(fdim(c,d))/(fdim(e,a));
e=sqrt(b);
if(isless(a,c)){
b=(fdim(d,e))-(fmin(e,e));
c=pow(b,d);
}
else{
c=(tan(e))/(tan(b));
d=fdim(e,a);
}
while(islessgreater(e,a)){
e=log10(d);
c=(exp(c))+(fmin(c,c));
b=(fmin(c,c))-(pow(e,a));
}
}