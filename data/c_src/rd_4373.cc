#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(a,e);
d=(sin(e))*(fmax(a,a));
b=pow(d,e);
e=(fdim(d,b))+(floor(b));
while(isless(a,b)){
d=tan(b);
d=(fmin(c,c))*(fdim(b,a));
}
d=floor(b);
e=(pow(d,e))-(ceil(d));
c=(ceil(b))/(fmax(b,b));
b=ceil(d);
d=fdim(e,b);
}