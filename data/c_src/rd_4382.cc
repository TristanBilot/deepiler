#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=acos(c);
a=fmax(e,c);
if(islessgreater(e,d)){
b=sin(a);
c=log(d);
f=(fmax(b,c))/(sin(c));
}
e=(atan2(a,c))-(ceil(c));
c=(atan2(e,a))+(atan2(b,a));
d=(fmax(d,e))/(fmax(b,b));
a=pow(c,b);
}