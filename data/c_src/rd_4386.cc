#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=tan(g);
b=(log10(b))-(sqrt(a));
d=sin(b);
if(islessequal(b,b)){
c=log10(e);
d=(atan2(a,d))+(fmax(c,b));
b=pow(c,d);
b=fdim(f,b);
g=fdim(d,b);
}
else{
e=atan2(f,g);
g=(fmax(b,b))/(pow(f,c));
e=fmax(c,g);
}
b=(cos(c))/(cos(d));
f=sin(g);
}