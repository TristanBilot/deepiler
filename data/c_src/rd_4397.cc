#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(sin(d))-(fmax(b,c));
c=fmin(a,a);
c=fmin(a,d);
e=(log(e))*(sin(e));
if(islessequal(a,c)){
e=(pow(e,a))-(pow(a,e));
a=(sin(e))+(fmin(e,a));
d=fmin(a,d);
a=(sqrt(d))/(cos(d));
d=atan2(b,b);
}
else{
e=(log(a))-(fmax(b,e));
a=(fmin(a,c))+(sin(d));
c=asin(c);
}
if(islessequal(d,a)){
c=log(e);
d=pow(d,a);
e=(floor(a))*(pow(c,a));
b=(atan2(b,e))-(pow(a,b));
b=exp(e);
}
else{
d=(floor(b))/(log(d));
c=fdim(d,a);
a=(fmax(c,e))/(sqrt(c));
}
}