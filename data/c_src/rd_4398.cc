#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(a,b);
c=(fmax(e,d))+(ceil(e));
if(isless(e,a)){
e=(fdim(b,d))*(sin(b));
d=fmax(b,b);
c=(floor(d))+(fdim(d,c));
}
while(islessequal(e,c)){
d=acos(e);
c=pow(e,c);
a=(sin(a))+(cos(b));
}
}