#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(tan(e))+(ceil(a));
d=(pow(b,c))*(floor(c));
if(isless(c,a)){
b=(fmin(b,a))*(tan(f));
e=exp(c);
c=fmin(d,d);
}
else{
a=fmax(c,c);
b=atan(b);
c=atan(a);
b=(atan2(a,d))*(fmax(e,c));
}
e=fmin(f,c);
d=(fmax(d,a))/(log(f));
}