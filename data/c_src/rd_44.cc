#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(ceil(d))/(log(c));
b=(asin(e))*(pow(b,d));
c=(fmax(a,d))*(acos(c));
while(islessgreater(d,b)){
e=(sqrt(f))/(floor(a));
e=atan2(f,b);
c=floor(a);
d=fmax(e,a);
d=(asin(f))-(asin(c));
}
if(isgreaterequal(d,d)){
a=fdim(c,b);
e=(fmin(a,f))*(acos(d));
}
}