#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=pow(c,a);
f=pow(b,f);
while(islessequal(a,d)){
b=fmin(a,f);
d=atan2(d,f);
d=pow(b,e);
}
c=(atan2(d,e))+(pow(d,a));
a=fmin(b,b);
a=atan2(f,g);
c=asin(f);
g=pow(g,f);
}