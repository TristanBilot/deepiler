#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(b,d))/(atan2(f,a));
f=ceil(d);
e=pow(e,b);
g=pow(f,g);
c=(atan2(e,f))+(exp(c));
if(isless(f,c)){
e=(floor(e))/(fmin(e,d));
g=(sqrt(c))*(fmax(e,b));
g=fmin(f,g);
c=asin(g);
}
while(islessgreater(b,d)){
f=atan2(c,a);
d=(tan(d))/(acos(b));
g=atan2(f,d);
d=log(a);
a=fmax(g,d);
}
}