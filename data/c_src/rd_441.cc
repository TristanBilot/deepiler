#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmin(c,b))*(fdim(c,a));
f=sin(d);
while(isgreaterequal(d,a)){
a=(cos(c))-(atan2(f,c));
c=(pow(b,e))/(pow(c,e));
}
while(isgreaterequal(a,a)){
e=(fmax(d,e))*(atan2(d,f));
d=fdim(d,a);
a=fmax(e,a);
d=(fmax(d,b))-(atan2(d,d));
}
}