#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(b,g);
f=exp(c);
b=fmin(b,c);
f=(pow(f,a))-(fdim(g,b));
if(isless(a,c)){
g=(pow(b,a))+(cos(c));
g=fdim(g,g);
a=(log(d))+(atan2(f,e));
}
else{
d=(fmax(d,c))/(exp(a));
f=(log(f))/(atan(a));
c=floor(a);
f=(atan(g))/(atan2(e,c));
}
while(isless(a,f)){
b=(exp(d))*(fdim(f,f));
f=pow(d,g);
}
}