#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(cos(d))-(fdim(a,d));
b=(atan2(a,e))*(tan(b));
b=(sqrt(c))-(log10(a));
d=(pow(b,e))*(exp(c));
c=(fdim(e,e))+(exp(a));
c=floor(e);
e=(fmax(c,e))*(atan2(d,b));
b=fmax(d,b);
if(islessequal(c,b)){
d=fmax(a,a);
c=sin(b);
e=(fmax(c,a))+(fmax(c,e));
}
}