#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(d,b))-(ceil(e));
b=(pow(e,c))+(pow(e,d));
a=(sin(c))-(exp(e));
while(isless(c,c)){
f=pow(a,b);
a=(sin(b))+(pow(b,c));
d=log10(f);
}
while(islessequal(b,e)){
f=log(e);
d=asin(b);
}
}