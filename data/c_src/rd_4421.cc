#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(tan(b))/(pow(e,c));
g=fmax(g,f);
e=fmax(e,c);
b=(pow(d,b))/(floor(c));
c=log10(f);
d=atan(e);
a=log(c);
g=pow(g,f);
g=(floor(e))/(fmax(g,e));
e=(floor(c))-(acos(d));
if(islessgreater(c,e)){
c=(asin(e))-(asin(d));
f=(acos(f))*(sqrt(d));
g=fmin(a,b);
}
}