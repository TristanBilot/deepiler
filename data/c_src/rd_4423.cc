#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(sqrt(b))-(atan(a));
a=fmin(c,e);
if(isgreaterequal(c,d)){
b=(log10(d))*(floor(d));
a=(asin(b))-(floor(c));
}
else{
e=(pow(c,c))*(floor(a));
e=pow(a,b);
}
d=atan(d);
e=pow(b,c);
c=(pow(d,e))/(fmax(b,d));
a=acos(c);
}