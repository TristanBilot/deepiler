#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(b,b))-(pow(f,e));
g=(pow(d,h))*(floor(g));
a=(sin(f))-(sqrt(g));
g=(pow(h,f))/(acos(d));
e=(acos(e))/(ceil(f));
if(islessgreater(g,b)){
d=(atan2(h,d))*(pow(h,g));
f=pow(d,a);
}
else{
f=(cos(e))*(fdim(e,h));
c=(fmin(c,e))*(floor(h));
}
if(isgreaterequal(h,e)){
f=fmax(f,h);
f=(log(f))/(fmax(g,c));
e=fdim(g,d);
g=(fmin(c,a))*(fdim(f,g));
c=(cos(e))-(tan(c));
}
else{
a=(pow(g,e))-(log10(h));
a=tan(a);
g=atan(h);
f=cos(c);
c=tan(c);
}
}