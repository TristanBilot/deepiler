#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log10(d);
c=(asin(f))+(tan(e));
while(isgreaterequal(a,a)){
d=(fdim(d,f))*(log10(b));
e=(pow(e,d))-(pow(b,c));
g=(fdim(c,c))-(fmax(a,a));
a=(pow(g,b))/(fdim(a,d));
g=fdim(g,g);
}
g=atan2(f,d);
a=fmin(d,c);
d=(fmax(f,b))+(cos(c));
c=(fdim(f,c))*(fmax(g,e));
d=cos(b);
}