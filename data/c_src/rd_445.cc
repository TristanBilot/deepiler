#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(a))+(cos(c));
e=(fmax(a,b))-(sin(c));
while(islessequal(d,d)){
e=acos(e);
e=fmax(a,d);
}
while(islessequal(b,d)){
d=fmax(a,e);
e=(sqrt(b))+(fmin(b,e));
b=(fdim(c,d))+(cos(e));
b=atan2(c,a);
b=acos(a);
}
}