#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(h);
c=fdim(a,a);
f=fdim(a,e);
d=floor(c);
if(isgreaterequal(a,b)){
h=(cos(g))*(fdim(f,c));
e=(cos(a))-(asin(f));
e=fmax(d,g);
c=(exp(g))+(floor(g));
}
else{
d=(ceil(h))/(cos(d));
f=(fdim(b,b))*(ceil(c));
f=(sqrt(c))*(fdim(e,e));
}
}