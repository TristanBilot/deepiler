#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan2(e,a))-(exp(g));
g=(fmax(f,d))*(fmin(b,c));
c=pow(d,c);
b=(fdim(h,e))/(log(f));
while(islessgreater(c,b)){
b=atan(e);
e=(atan2(f,b))+(cos(e));
h=fdim(h,g);
g=(fdim(h,f))*(sqrt(g));
d=(fmin(b,f))*(atan2(c,h));
}
c=(atan(e))-(fmin(f,a));
h=(pow(c,c))/(atan2(d,d));
h=(asin(g))*(fdim(f,c));
}