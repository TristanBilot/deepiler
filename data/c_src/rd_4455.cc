#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,d);
h=exp(h);
e=pow(c,e);
while(islessequal(a,g)){
f=fdim(h,h);
d=acos(b);
g=acos(d);
e=sin(h);
a=(log(e))+(sin(g));
}
if(islessgreater(h,h)){
d=asin(g);
f=fmax(g,g);
c=(fmin(g,c))*(pow(d,d));
g=(atan2(a,e))-(exp(d));
}
else{
h=fdim(a,h);
a=(sqrt(c))-(fmax(c,d));
a=atan(d);
b=fdim(g,f);
}
}