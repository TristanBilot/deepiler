#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(c,a))*(sin(e));
f=(pow(c,e))+(fmin(a,e));
b=fmax(a,c);
d=(sqrt(b))/(log10(d));
f=(atan2(e,c))-(acos(d));
a=floor(c);
if(islessgreater(f,c)){
c=fmin(e,f);
a=(acos(d))+(atan2(c,c));
b=(sqrt(f))*(log10(e));
}
else{
b=(ceil(e))*(asin(c));
d=acos(b);
a=(fmin(f,b))-(pow(c,c));
f=acos(e);
}
}