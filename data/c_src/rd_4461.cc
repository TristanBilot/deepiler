#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(b,b))*(fdim(g,a));
g=pow(a,e);
f=(log10(a))+(fdim(f,c));
c=log(d);
b=(floor(a))*(atan(g));
if(islessequal(f,g)){
a=exp(d);
a=log10(g);
g=(acos(c))-(log(d));
g=(fdim(b,c))/(sin(c));
}
else{
c=(fmin(g,e))+(sqrt(g));
e=log(g);
c=fmax(b,b);
}
if(isless(a,b)){
d=pow(e,a);
b=(fmin(f,d))+(fdim(e,a));
f=floor(f);
c=log(f);
d=(exp(b))-(fmax(g,c));
}
else{
a=(fdim(e,d))*(fmax(g,c));
c=tan(c);
}
}