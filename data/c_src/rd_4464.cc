#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(pow(a,g))*(sin(e));
d=(fmax(d,d))*(fmin(f,f));
d=(atan2(g,b))*(pow(a,e));
f=(pow(g,a))/(tan(d));
d=(log10(c))+(sin(g));
c=(cos(f))+(pow(f,e));
b=(fdim(f,b))+(fdim(f,g));
f=(cos(d))/(exp(b));
}