#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(c,h);
d=atan2(f,f);
h=atan(b);
g=fmin(e,c);
c=(fdim(e,e))/(fmin(a,e));
while(islessgreater(a,c)){
e=fmin(a,b);
a=(atan(b))/(tan(h));
c=fmin(a,f);
}
while(isless(b,c)){
e=(fmin(f,c))*(fdim(e,f));
h=sqrt(a);
f=floor(a);
f=sqrt(f);
b=(exp(a))*(log10(e));
}
}