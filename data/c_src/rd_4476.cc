#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(a,a);
a=atan2(a,b);
a=exp(a);
b=(exp(c))*(pow(d,a));
if(islessequal(b,b)){
d=(fmax(a,e))+(fmin(a,c));
c=(acos(a))*(log10(b));
}
else{
e=(atan2(b,c))-(fmin(d,c));
b=(exp(b))-(pow(d,e));
b=(atan2(d,a))+(pow(c,b));
d=(log(e))-(fdim(d,e));
d=(fmin(a,c))-(fmax(e,a));
}
}