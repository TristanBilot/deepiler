#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log(d);
d=(fmax(b,d))*(atan2(b,d));
b=fdim(e,e);
while(isgreaterequal(a,b)){
c=pow(a,e);
e=atan2(d,c);
a=(fdim(b,b))/(fmin(b,b));
}
if(isless(a,b)){
b=(fmin(d,e))-(fdim(e,d));
d=fmax(e,e);
c=asin(a);
}
else{
c=fdim(c,c);
e=atan2(a,c);
b=fdim(b,e);
a=(asin(a))/(sqrt(b));
}
}