#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(acos(d))+(exp(c));
a=(atan2(b,c))+(pow(c,a));
c=(acos(a))*(cos(e));
e=(cos(a))+(fmax(d,d));
a=(log(b))-(atan2(d,a));
b=(floor(a))+(pow(e,e));
while(islessequal(b,d)){
d=atan(e);
d=(floor(a))/(sin(c));
a=(atan2(c,a))+(fmin(b,c));
b=fmin(a,b);
}
}