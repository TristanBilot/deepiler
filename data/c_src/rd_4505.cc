#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(d,h))-(atan2(h,h));
e=fmin(d,d);
f=atan2(h,b);
while(isgreaterequal(d,d)){
f=fdim(d,f);
b=ceil(f);
a=(fmax(f,c))-(acos(c));
f=(acos(a))+(fdim(h,c));
c=sqrt(h);
}
if(isgreaterequal(a,d)){
b=acos(h);
d=(sin(c))+(pow(b,h));
}
}