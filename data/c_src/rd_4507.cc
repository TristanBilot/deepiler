#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(f);
b=acos(e);
b=(asin(c))+(fmax(b,a));
b=(pow(f,c))-(pow(e,c));
e=fmin(b,b);
while(isgreaterequal(b,g)){
g=log10(g);
a=(fmin(f,c))/(cos(b));
g=(sqrt(d))+(fmin(f,h));
}
if(islessequal(a,h)){
g=fmin(a,f);
h=atan2(a,h);
b=sqrt(c);
}
else{
g=exp(e);
d=(fdim(a,g))+(pow(d,g));
d=log10(e);
}
}