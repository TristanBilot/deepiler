#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(f,d);
e=(pow(d,d))/(fmin(f,d));
b=(log10(e))/(fmin(e,b));
if(islessgreater(d,c)){
e=fmax(e,a);
f=fmin(d,a);
}
while(islessgreater(f,d)){
f=(pow(b,d))/(exp(d));
f=fdim(a,a);
d=(sqrt(b))-(fmin(d,e));
e=pow(f,f);
}
}