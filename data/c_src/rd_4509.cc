#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log10(b);
c=(sqrt(f))+(atan2(f,a));
d=(sin(f))*(exp(d));
d=(atan2(f,f))*(log10(d));
a=(fdim(e,a))/(atan(c));
while(isgreaterequal(f,f)){
f=pow(c,e);
e=(atan(b))+(fmin(f,b));
c=fmax(f,d);
f=log(d);
c=(sqrt(f))+(fmax(e,e));
}
}