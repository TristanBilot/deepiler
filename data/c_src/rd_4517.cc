#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(a))*(atan2(b,c));
a=(cos(d))*(fmin(c,c));
b=atan2(a,a);
a=(fdim(d,e))-(fmax(d,d));
while(islessgreater(d,b)){
e=log10(d);
d=(asin(b))*(fdim(e,c));
}
while(isgreaterequal(b,a)){
a=(fmax(c,b))-(cos(c));
c=fmax(c,a);
e=(pow(d,c))+(log(b));
}
}