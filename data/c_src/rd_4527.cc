#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(e,a);
f=(fmax(b,d))/(fdim(b,f));
a=fdim(c,f);
while(isless(b,e)){
d=(fmax(e,d))/(sin(f));
f=(fmin(b,d))*(atan2(b,e));
e=acos(f);
e=fmin(b,d);
d=fmin(c,d);
}
d=(cos(f))-(sin(f));
f=fmin(c,b);
f=(atan(b))-(fmax(f,a));
c=fmin(f,c);
c=atan2(d,c);
}