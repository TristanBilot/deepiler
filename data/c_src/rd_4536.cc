#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan(a);
d=(fdim(c,c))*(atan2(d,c));
a=asin(e);
while(isless(a,d)){
e=acos(c);
e=fdim(d,a);
c=atan(d);
e=(pow(c,b))/(fmin(a,d));
}
if(isgreaterequal(c,b)){
e=(asin(d))+(sqrt(e));
d=(fmin(c,c))-(pow(c,d));
c=(pow(a,b))/(fdim(e,b));
}
}