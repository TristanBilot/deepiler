#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(d,a);
g=(fdim(b,h))/(fmin(c,f));
a=exp(a);
if(isgreaterequal(h,f)){
h=floor(h);
f=(sqrt(e))+(floor(d));
d=(fmin(b,b))*(sqrt(g));
}
else{
a=(fmax(a,a))*(acos(d));
g=fmax(b,f);
a=(sin(c))-(pow(f,d));
e=(exp(d))-(cos(g));
g=(fmax(e,b))/(fmin(b,b));
}
a=atan(g);
f=(log10(c))+(fmin(d,g));
d=exp(b);
g=pow(c,c);
c=(asin(h))+(atan2(a,b));
}