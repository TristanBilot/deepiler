#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sin(a);
b=(atan2(b,g))+(floor(a));
a=(fdim(e,b))/(fmin(e,f));
g=tan(g);
a=(cos(g))*(fmax(g,f));
if(isgreaterequal(e,b)){
g=(cos(a))*(fdim(f,a));
a=(pow(f,b))+(atan2(c,d));
}
else{
d=(fmax(f,c))-(atan(b));
c=log(b);
}
e=(cos(c))/(atan2(c,a));
c=log10(e);
f=acos(d);
}