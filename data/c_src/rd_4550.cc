#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log(d))/(fmax(c,b));
b=atan(b);
e=(fmin(b,a))*(pow(a,d));
b=(exp(c))-(tan(a));
d=fmax(e,a);
while(isgreaterequal(b,d)){
d=asin(e);
b=tan(a);
}
b=fmin(b,c);
a=acos(b);
a=(pow(d,a))-(pow(d,a));
b=(ceil(b))/(atan2(c,d));
d=fmin(e,a);
}