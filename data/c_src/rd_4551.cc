#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan2(d,a))/(acos(a));
b=(tan(c))/(atan2(a,b));
if(islessequal(d,a)){
b=(fmax(c,c))-(atan2(c,d));
d=(pow(b,e))/(atan2(a,c));
c=(atan2(d,c))/(atan2(e,d));
}
if(islessequal(c,e)){
b=fdim(b,c);
e=(fdim(e,c))-(fmin(c,c));
f=acos(a);
}
else{
f=fmax(d,a);
e=floor(f);
}
}