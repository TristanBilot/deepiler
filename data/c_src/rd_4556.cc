#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(d,c);
a=fdim(b,f);
e=(pow(b,b))*(atan(f));
e=fmax(e,f);
a=(floor(g))+(tan(d));
g=sin(b);
c=floor(c);
a=(fdim(e,b))-(pow(e,a));
d=(exp(d))+(log(f));
g=(fdim(c,c))*(fmin(g,e));
f=sin(c);
}