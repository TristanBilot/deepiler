#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(d,c))*(fmin(c,e));
b=(fmax(g,b))/(sin(d));
g=fmin(c,a);
c=(sqrt(a))/(atan2(g,c));
e=(exp(d))/(fmin(f,a));
g=(atan2(c,a))/(ceil(c));
if(isless(g,f)){
e=(ceil(c))*(log(e));
e=log(b);
b=atan2(d,g);
d=pow(a,b);
}
}