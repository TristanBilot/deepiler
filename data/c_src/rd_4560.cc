#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(b,d))*(atan2(f,d));
f=(fmin(c,d))/(exp(e));
f=(ceil(f))-(tan(b));
if(islessgreater(c,b)){
c=sqrt(c);
f=(atan2(e,c))-(log10(e));
e=(fmin(d,b))/(fdim(e,c));
e=ceil(b);
}
else{
f=(atan2(e,f))+(sqrt(b));
e=asin(b);
d=(log(e))-(fmax(b,e));
f=exp(b);
}
if(isless(e,f)){
e=acos(c);
b=floor(f);
e=(fdim(e,e))*(fmax(d,f));
d=(atan(e))*(sin(a));
}
else{
d=pow(d,a);
e=(fdim(f,c))/(exp(a));
a=(fdim(d,f))-(exp(a));
}
}