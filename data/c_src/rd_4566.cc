#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(c,b))/(fmax(e,d));
c=log10(d);
d=ceil(b);
while(islessequal(e,e)){
c=(log(a))*(atan2(b,e));
c=(atan(e))+(pow(e,d));
e=fdim(e,b);
a=(fdim(c,b))*(fmin(e,a));
d=(fmax(a,b))*(fmin(d,a));
}
a=log(d);
a=(fmin(b,c))*(atan2(d,a));
c=(atan2(a,c))/(pow(a,e));
a=(floor(a))*(atan2(d,c));
c=fmax(c,c);
}