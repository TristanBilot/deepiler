#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=cos(a);
a=fmin(e,c);
e=(log(e))-(atan2(e,d));
if(islessequal(d,a)){
b=(pow(e,e))/(fmin(a,b));
b=(fmin(c,c))+(atan2(c,c));
c=(atan(c))*(atan2(b,c));
e=(acos(a))*(acos(b));
}
if(isless(d,b)){
b=fdim(e,d);
e=(asin(a))/(fmin(d,b));
}
else{
c=(log10(e))*(pow(c,c));
c=(fdim(a,b))-(acos(d));
d=(fdim(e,d))/(cos(e));
d=tan(b);
a=fmax(d,d);
}
}