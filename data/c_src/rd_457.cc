#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(d,e);
a=(fdim(e,h))/(tan(g));
while(islessequal(d,e)){
b=(tan(a))*(atan2(h,c));
c=(fdim(h,f))*(log(c));
g=fdim(g,g);
f=(log(h))-(ceil(f));
}
h=pow(f,g);
f=(fmax(d,f))+(fmax(e,b));
}