#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(d,a))+(ceil(d));
d=(acos(e))+(sin(c));
c=(fmax(e,b))+(fdim(d,b));
a=exp(b);
b=(fdim(e,d))+(pow(c,b));
a=exp(c);
d=fmin(c,b);
c=(log10(a))*(fmin(b,d));
c=atan(e);
a=fmin(c,a);
}