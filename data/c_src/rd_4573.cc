#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(pow(g,c))*(fmax(g,g));
d=(atan2(e,b))/(asin(a));
e=(exp(b))/(atan2(e,f));
if(islessequal(g,a)){
f=ceil(d);
e=atan2(d,c);
a=fmin(a,d);
b=exp(g);
e=(fmax(g,f))/(atan2(a,d));
}
while(isless(a,f)){
b=(pow(d,f))-(pow(b,a));
c=fmax(c,d);
d=(sqrt(a))+(ceil(e));
e=cos(f);
d=fmax(a,d);
}
}