#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(b);
e=(acos(d))/(fmax(f,c));
a=pow(h,d);
while(islessgreater(a,g)){
f=atan(c);
g=pow(d,f);
c=fmin(a,d);
f=(atan2(c,a))*(pow(g,h));
}
if(isless(a,h)){
b=(fmax(d,d))-(cos(c));
c=exp(a);
a=(fmax(c,b))*(fdim(e,a));
}
}