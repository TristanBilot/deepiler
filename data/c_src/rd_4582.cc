#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log10(b))/(fmin(d,c));
b=(tan(a))-(fmax(e,d));
c=(pow(c,d))*(log10(f));
e=fmax(b,f);
if(isgreaterequal(a,a)){
f=(fdim(f,b))+(acos(f));
d=fmin(f,c);
}
else{
b=(sin(a))*(fmax(a,a));
c=(ceil(e))+(fdim(f,a));
e=fmax(a,a);
}
if(isgreaterequal(a,c)){
f=pow(e,f);
d=pow(a,c);
d=(fdim(a,a))+(pow(c,b));
d=(ceil(e))+(atan2(e,c));
a=pow(f,e);
}
else{
a=(sin(f))+(fmax(b,e));
a=(fdim(f,f))/(sqrt(c));
a=floor(a);
}
}