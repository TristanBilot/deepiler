#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(d,c);
d=(ceil(a))-(floor(a));
f=pow(e,c);
d=sqrt(c);
b=(asin(d))-(sqrt(a));
f=(asin(h))*(ceil(g));
if(isless(a,g)){
h=(fmin(a,d))-(fmin(f,g));
a=floor(g);
}
}