#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(e))*(pow(c,b));
d=(pow(a,d))+(fdim(f,a));
f=(acos(f))-(asin(b));
c=log10(e);
if(islessequal(d,b)){
c=fmax(e,e);
b=cos(b);
}
else{
f=exp(c);
f=acos(e);
c=atan2(f,d);
}
a=(pow(c,a))*(exp(d));
c=(log10(d))/(sin(a));
a=fmax(e,c);
}