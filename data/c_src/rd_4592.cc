#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=ceil(c);
f=(fmin(c,a))*(fdim(g,g));
a=exp(g);
c=floor(c);
if(isgreaterequal(d,f)){
d=(acos(b))*(acos(d));
b=cos(c);
e=fdim(g,b);
}
else{
b=ceil(f);
a=(atan2(b,g))/(log(f));
b=(exp(a))-(log(g));
a=ceil(a);
}
while(isgreaterequal(e,a)){
g=(fmin(f,e))*(pow(c,b));
g=floor(e);
}
}