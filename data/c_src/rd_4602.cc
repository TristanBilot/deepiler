#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(d,c))+(atan2(d,e));
b=acos(a);
c=tan(a);
d=floor(a);
e=(pow(c,a))-(fmax(e,e));
f=fmax(a,a);
if(islessequal(b,d)){
e=fdim(c,f);
e=(fmax(a,c))*(fmin(e,e));
f=(tan(f))+(atan2(c,c));
a=(sin(c))*(exp(b));
}
else{
d=sin(f);
c=(ceil(d))/(fmin(e,e));
e=(atan2(c,b))-(cos(b));
d=(log(b))/(fmax(e,a));
d=exp(f);
}
}