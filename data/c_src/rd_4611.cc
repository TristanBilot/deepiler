#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(c,e);
d=(fmax(a,b))+(tan(d));
d=exp(b);
c=(atan2(f,f))-(atan2(c,d));
c=(sqrt(b))+(sin(d));
while(isless(f,e)){
d=(fmax(c,a))*(atan(e));
c=(atan2(b,a))*(sqrt(b));
f=log10(d);
f=log(c);
}
if(islessgreater(a,f)){
d=(fmax(d,a))-(fmin(f,f));
c=(atan2(f,c))*(fdim(c,a));
d=(atan2(a,f))+(fdim(a,f));
d=tan(d);
}
else{
b=pow(d,a);
c=fmax(a,d);
}
}