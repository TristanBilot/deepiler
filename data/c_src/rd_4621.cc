#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(b))-(fdim(c,e));
e=fdim(d,b);
if(isless(e,b)){
a=(pow(a,d))+(fmax(c,b));
b=atan2(b,b);
b=acos(c);
}
b=exp(e);
d=fdim(b,c);
e=fmin(d,a);
d=(asin(c))/(fmax(d,a));
}