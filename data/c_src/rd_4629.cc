#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(c,c))/(floor(f));
h=(floor(g))*(sin(d));
c=atan2(a,d);
d=log10(f);
if(isless(h,g)){
f=(floor(b))/(fmin(c,b));
b=(fmin(c,f))*(asin(f));
}
h=(fmax(f,h))/(acos(e));
g=exp(d);
d=asin(d);
g=fmin(f,e);
}