#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(e,b);
b=pow(c,c);
d=(log(e))+(fmax(d,d));
a=(floor(a))*(exp(d));
e=(fmax(a,a))-(sqrt(d));
while(isgreaterequal(c,d)){
e=floor(c);
a=(fmin(c,c))-(exp(c));
a=(log10(a))/(atan2(d,b));
b=(atan2(d,d))+(pow(a,c));
}
if(islessgreater(d,e)){
d=cos(a);
c=(fmax(a,e))*(floor(e));
d=(fmax(c,e))-(pow(c,b));
}
else{
c=(log(c))+(fmax(a,c));
b=(cos(e))+(log(b));
}
}