#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(atan2(d,b))-(fmin(a,b));
e=(fdim(c,e))+(fdim(a,c));
g=(fmax(a,c))-(exp(d));
e=log10(g);
while(isgreaterequal(g,b)){
f=(atan2(c,b))-(tan(c));
b=(fdim(f,e))*(cos(b));
}
f=pow(b,g);
e=atan2(a,c);
a=(sqrt(b))/(sqrt(c));
f=(pow(b,g))/(exp(a));
g=(cos(f))*(fmax(e,e));
}