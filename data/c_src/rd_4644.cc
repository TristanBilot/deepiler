#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(c);
c=(ceil(g))/(fmin(f,b));
f=(exp(h))/(fdim(c,e));
if(islessequal(h,f)){
c=(log(a))+(fdim(d,b));
d=(exp(b))-(fdim(f,d));
e=asin(c);
f=(atan2(b,d))/(sqrt(f));
}
while(islessgreater(f,d)){
f=fmax(a,h);
a=atan2(b,c);
f=(atan2(g,a))-(tan(c));
h=fmax(e,e);
a=fmin(f,c);
}
}