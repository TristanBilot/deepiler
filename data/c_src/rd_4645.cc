#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(a,c);
a=(atan2(a,f))+(fmax(a,c));
e=tan(d);
a=log10(d);
e=floor(e);
f=(atan(f))/(fmin(e,e));
c=fmax(d,c);
e=(log10(c))/(fmax(b,b));
f=pow(f,a);
if(isless(f,f)){
a=(fdim(f,b))-(pow(b,e));
d=(acos(f))-(floor(a));
b=fmax(d,c);
a=(exp(a))+(fmax(a,f));
}
else{
b=log(d);
f=(pow(b,c))-(log(c));
e=fmin(e,f);
e=cos(c);
c=atan2(d,a);
}
}