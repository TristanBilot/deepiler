#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(b,h);
d=acos(c);
h=(atan2(a,d))*(atan(b));
a=tan(a);
d=(fmin(g,e))/(asin(e));
a=(tan(g))-(sqrt(g));
b=fmin(g,a);
c=log10(h);
e=(log10(a))+(fmax(a,e));
c=atan2(a,h);
if(islessgreater(d,d)){
e=atan2(b,c);
g=(fmax(a,b))*(sqrt(b));
f=fmax(e,e);
}
else{
h=fdim(h,d);
h=(fdim(f,d))+(fdim(g,a));
f=tan(f);
d=sqrt(b);
f=atan2(g,a);
}
}