#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(c,e);
b=fdim(g,d);
b=(asin(e))*(fmax(c,f));
f=fmin(b,c);
f=(log(f))/(fdim(g,a));
while(isless(g,f)){
e=(sqrt(d))*(atan2(e,f));
g=(fdim(f,f))*(tan(a));
}
if(islessgreater(g,c)){
g=(atan2(a,f))*(atan2(b,c));
f=fmax(c,g);
c=(log(f))-(fdim(f,c));
g=(asin(d))/(tan(g));
b=(floor(c))-(pow(b,b));
}
}