#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fdim(b,d);
f=asin(c);
c=(pow(c,e))-(acos(f));
d=(fmax(e,f))*(sqrt(c));
e=(pow(f,c))+(tan(a));
b=(fdim(a,d))+(fdim(d,a));
c=(atan2(a,c))/(fmin(d,b));
if(islessequal(f,d)){
c=(fdim(f,b))+(cos(e));
f=fmin(d,d);
c=(atan(f))*(asin(d));
e=(cos(f))*(atan2(a,a));
}
}