#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(c);
e=fmax(a,c);
while(islessequal(c,c)){
d=(fmax(c,a))+(fdim(e,c));
a=(sqrt(e))-(fmin(d,a));
a=pow(d,e);
d=pow(b,a);
c=tan(b);
}
while(isgreaterequal(e,a)){
c=(sqrt(d))+(atan2(d,b));
c=(fmax(e,d))+(fmax(e,c));
a=(fmin(a,e))+(pow(b,b));
b=(log10(e))*(log10(a));
e=atan2(c,d);
}
}