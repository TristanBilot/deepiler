#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan(d))+(sin(h));
c=(fdim(c,a))/(atan(c));
f=(fdim(a,a))/(exp(d));
f=log(g);
while(isless(g,h)){
f=log10(b);
h=pow(e,b);
}
b=fmin(a,h);
a=(fmin(f,a))/(fdim(d,h));
g=floor(b);
}