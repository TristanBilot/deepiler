#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(d);
d=acos(e);
c=pow(e,a);
d=(exp(e))+(acos(e));
e=log10(a);
while(islessequal(b,a)){
b=tan(c);
e=(pow(e,b))*(fmax(d,a));
b=(atan2(c,e))/(atan2(d,b));
}
while(islessgreater(c,a)){
c=asin(b);
d=(fmax(d,e))*(asin(e));
}
}