#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=pow(f,a);
e=(floor(a))+(sin(e));
d=pow(g,b);
g=(tan(c))/(sqrt(g));
while(isless(c,a)){
f=atan2(f,d);
g=log10(g);
f=(atan(g))+(atan2(f,c));
a=(pow(b,c))-(exp(f));
c=(fmax(f,a))-(pow(c,c));
}
if(islessequal(b,e)){
f=asin(d);
b=(fdim(d,e))/(atan2(b,e));
}
else{
b=(atan2(d,c))/(fmin(c,f));
e=(fdim(c,d))+(fdim(d,d));
}
}