#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmin(d,e))-(cos(a));
a=(exp(b))*(fmin(d,e));
f=atan2(d,f);
while(isless(b,a)){
d=fmax(f,e);
c=log10(b);
a=tan(c);
e=(log10(c))+(fmin(b,b));
}
if(isless(d,d)){
f=exp(c);
e=ceil(c);
a=cos(f);
}
}