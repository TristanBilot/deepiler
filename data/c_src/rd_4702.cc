#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmax(e,d))/(fmin(f,c));
a=acos(f);
if(islessequal(f,c)){
e=pow(d,b);
a=cos(f);
}
g=cos(b);
a=(atan(a))*(atan(d));
f=(fmin(d,g))+(fmin(b,e));
f=pow(a,e);
f=(atan2(e,f))-(log10(e));
}