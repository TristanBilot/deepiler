#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(c))/(sqrt(e));
b=(log10(e))*(sqrt(d));
b=(fdim(b,a))-(cos(b));
b=(cos(b))*(fmin(d,c));
c=fmin(a,a);
while(isgreaterequal(a,a)){
b=(pow(e,d))/(cos(a));
a=atan2(b,d);
c=fmin(a,e);
c=log(a);
}
}