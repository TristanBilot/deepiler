#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(a,a))+(ceil(e));
c=exp(c);
while(isgreaterequal(c,d)){
a=fmax(d,e);
b=(sqrt(c))-(atan(c));
b=pow(e,b);
}
while(islessgreater(d,c)){
e=asin(c);
a=(fmax(a,c))+(sin(d));
}
}