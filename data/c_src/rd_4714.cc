#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmin(b,d);
f=(atan2(d,e))-(fmin(a,f));
g=log(a);
if(isless(c,a)){
e=pow(d,e);
b=(fmax(g,b))*(atan(e));
a=(tan(e))/(asin(a));
e=fmax(g,e);
}
g=(fdim(e,b))*(pow(a,e));
g=log10(f);
e=(acos(b))-(floor(e));
g=log(c);
}