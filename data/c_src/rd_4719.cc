#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sin(a);
a=fmax(e,b);
while(isgreaterequal(a,g)){
b=(exp(f))-(atan(b));
a=pow(b,f);
}
while(islessgreater(d,b)){
a=(cos(e))+(floor(e));
d=(cos(c))/(asin(f));
b=atan2(a,b);
c=pow(a,c);
d=atan2(a,d);
}
}