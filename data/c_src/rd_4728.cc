#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmax(a,f))/(pow(e,a));
e=exp(a);
f=(fdim(g,d))-(sqrt(e));
b=(atan2(c,e))/(atan2(d,c));
f=tan(f);
d=fmin(g,d);
f=sin(c);
b=fdim(b,c);
while(isless(e,f)){
g=(fdim(g,f))-(fmax(e,d));
f=(fdim(a,f))-(acos(d));
d=(fmax(g,f))*(fmax(d,g));
f=(asin(d))+(cos(c));
d=(sqrt(f))/(pow(f,c));
}
}