#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log(a);
a=fmin(a,e);
if(islessgreater(a,e)){
g=(ceil(b))/(fmin(c,f));
b=atan(a);
d=(ceil(f))*(acos(c));
g=atan(d);
}
else{
g=sqrt(e);
g=ceil(c);
d=(fmax(f,e))-(sqrt(g));
e=(floor(e))/(fdim(f,f));
f=atan(f);
}
if(isless(a,g)){
b=(fmin(b,a))+(pow(g,b));
f=log10(b);
}
}