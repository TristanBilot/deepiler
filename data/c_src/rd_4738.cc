#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(f);
f=fdim(f,a);
c=fdim(g,c);
g=sin(g);
c=(atan2(f,g))*(atan2(a,h));
a=fmax(a,e);
d=fmax(f,f);
d=sin(g);
d=ceil(g);
a=(atan2(g,b))+(cos(a));
g=fmax(h,a);
f=(fmax(g,c))*(ceil(d));
}