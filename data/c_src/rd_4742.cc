#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log(c);
d=cos(e);
if(islessequal(d,d)){
e=fmax(e,c);
c=(atan2(d,b))/(floor(c));
a=fmax(a,f);
f=exp(a);
}
else{
a=fmin(d,a);
e=(pow(b,d))-(fmin(c,a));
}
if(islessgreater(f,a)){
f=(tan(a))+(pow(d,e));
a=(log10(c))-(fmax(e,c));
a=atan2(f,f);
}
}