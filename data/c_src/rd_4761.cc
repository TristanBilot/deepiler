#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(b,d))/(atan2(e,f));
d=atan2(g,h);
g=fdim(c,d);
f=(fdim(c,g))+(atan2(b,f));
f=ceil(f);
a=(atan2(c,e))*(pow(a,a));
if(isgreaterequal(g,g)){
d=(pow(a,b))/(fmax(f,g));
d=fdim(e,h);
a=atan2(h,e);
a=floor(b);
}
else{
h=tan(f);
g=sqrt(d);
}
}