#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(d,e);
f=(atan(a))-(fmin(c,a));
h=atan2(h,d);
if(islessequal(h,c)){
c=atan2(c,d);
b=atan2(d,c);
}
while(islessgreater(d,h)){
h=fmin(e,b);
b=pow(h,a);
e=atan2(e,d);
}
}