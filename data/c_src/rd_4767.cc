#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(e,a);
a=(atan2(a,e))/(cos(d));
e=log10(a);
b=(exp(c))/(pow(a,e));
c=(cos(e))/(pow(a,b));
b=(fmin(b,b))-(fmax(d,b));
e=fmax(a,e);
c=(tan(c))/(fmin(b,b));
b=(fmax(a,e))+(ceil(c));
e=(fmin(b,e))*(floor(c));
if(islessgreater(e,a)){
b=fmin(a,b);
e=pow(c,d);
e=(atan(e))+(floor(a));
a=(cos(e))/(tan(b));
}
}