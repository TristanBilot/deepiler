#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=exp(g);
g=ceil(b);
if(islessgreater(b,f)){
b=(atan(d))/(fdim(b,d));
e=(cos(d))/(sin(g));
e=(fmin(f,e))*(fdim(b,g));
d=(asin(e))-(fmax(f,f));
a=(fmin(a,d))/(atan2(e,d));
}
else{
f=atan2(e,a);
e=(atan2(g,b))/(log10(a));
f=fmin(d,g);
}
if(isless(c,f)){
e=fmax(c,d);
g=(tan(f))/(fdim(c,f));
b=fmin(g,e);
g=(sin(b))-(fmax(a,g));
}
else{
c=(fmax(a,d))-(pow(d,b));
b=(sin(d))/(atan(d));
g=(fmin(a,f))+(fmin(a,d));
e=atan2(f,c);
}
}