#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(sqrt(g))*(fmax(d,c));
h=fmin(e,f);
a=sin(b);
f=exp(h);
g=(fdim(h,e))/(log10(c));
h=asin(f);
g=(pow(d,e))/(atan2(e,h));
d=(log10(d))-(fmax(a,g));
a=(tan(b))-(cos(e));
c=(asin(g))+(pow(f,e));
}