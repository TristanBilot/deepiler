#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(a);
e=(acos(a))-(fmax(d,d));
g=fmax(e,d);
g=cos(f);
g=(floor(d))-(log10(f));
if(isgreaterequal(c,a)){
e=fdim(c,b);
a=tan(c);
a=pow(e,f);
}
if(islessequal(b,g)){
e=atan2(d,e);
c=(sqrt(b))/(sin(g));
f=pow(e,g);
}
}