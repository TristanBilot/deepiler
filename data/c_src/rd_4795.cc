#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(asin(b))-(atan(d));
c=(fmax(b,e))/(atan2(e,c));
f=pow(a,b);
f=(atan2(d,c))/(asin(b));
c=(pow(f,a))+(pow(d,a));
f=(atan2(f,d))/(cos(b));
while(isless(d,a)){
c=(fmax(c,e))+(fdim(f,b));
a=(acos(e))/(fdim(b,e));
b=fdim(a,e);
}
}