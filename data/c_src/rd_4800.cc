#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(d,b))/(ceil(a));
b=cos(e);
c=log(a);
b=(fdim(a,c))/(ceil(d));
d=pow(e,a);
while(islessgreater(b,a)){
f=(pow(b,f))+(fmax(c,f));
f=floor(b);
a=fdim(a,d);
b=(cos(b))/(fmin(c,a));
}
d=(log(f))+(fmax(a,c));
c=fdim(e,c);
c=(acos(a))-(fmin(d,f));
}