#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(c,e);
a=acos(d);
a=ceil(d);
b=(pow(b,c))*(fdim(d,a));
while(isgreaterequal(c,b)){
b=(log10(d))+(pow(e,d));
a=fmax(a,b);
d=(atan2(b,c))/(fmax(c,d));
a=(fmax(b,a))*(fdim(d,e));
}
if(isgreaterequal(c,d)){
c=cos(b);
a=(fdim(b,e))+(pow(c,d));
c=(fmin(c,c))+(fmin(d,b));
}
else{
c=atan(e);
e=(exp(e))*(exp(c));
a=fmax(c,b);
}
}