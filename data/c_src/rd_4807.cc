#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(g,c))/(exp(f));
e=floor(b);
f=fmax(c,g);
d=(atan2(b,a))+(exp(d));
b=asin(f);
c=atan2(b,a);
a=(log(f))/(pow(g,b));
d=(sin(g))/(atan2(c,d));
if(isgreaterequal(g,c)){
b=atan(g);
c=(fmax(e,e))/(cos(a));
}
else{
f=(fmax(g,f))-(atan(g));
b=(atan2(a,a))*(fmin(f,b));
d=sin(a);
}
}