#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(c);
c=(exp(d))*(fmin(a,d));
b=(fdim(e,e))-(fmin(c,b));
b=(atan(c))/(fdim(a,a));
e=(floor(e))*(tan(b));
c=(sin(c))/(atan2(d,e));
while(islessgreater(e,e)){
a=asin(d);
a=(atan2(a,d))/(fdim(c,c));
c=sin(d);
a=log10(e);
}
}