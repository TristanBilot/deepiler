#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(b);
f=atan2(f,f);
b=fdim(e,b);
if(isless(g,g)){
g=(fdim(e,c))-(tan(g));
d=atan(c);
h=(pow(b,b))/(pow(f,h));
b=(fmin(a,f))-(tan(e));
e=fdim(d,e);
}
f=(atan(f))/(fdim(e,a));
h=(asin(d))-(acos(g));
}