#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(a,d))/(pow(a,f));
d=fdim(e,d);
while(isless(c,e)){
f=pow(e,g);
e=(fdim(b,h))+(fmax(e,a));
b=(fmax(c,f))+(fdim(c,c));
h=(ceil(d))*(fmin(c,f));
}
if(isgreaterequal(e,e)){
e=(fdim(f,a))-(tan(b));
d=(tan(g))-(asin(b));
f=fmin(a,c);
c=(pow(b,e))*(atan2(f,b));
}
}