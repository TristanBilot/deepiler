#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(tan(e))*(asin(e));
d=(pow(c,f))/(ceil(f));
e=(log10(f))/(fmax(d,f));
if(islessequal(c,a)){
b=sqrt(c);
b=(fmin(a,c))*(fdim(a,c));
}
else{
a=pow(f,c);
c=(fdim(d,b))+(acos(d));
c=log(d);
d=(pow(f,e))-(cos(a));
c=(fmax(f,c))-(ceil(c));
}
c=(log10(d))+(fmin(d,b));
d=(sin(f))+(fmax(c,d));
f=exp(c);
}