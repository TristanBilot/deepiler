#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=tan(e);
e=(atan2(f,b))*(ceil(e));
c=(floor(a))-(fdim(b,e));
d=(fmax(e,d))/(tan(b));
f=ceil(a);
d=(fmax(f,e))*(fmin(b,f));
c=(fdim(d,b))*(asin(b));
b=(cos(b))*(atan2(b,d));
e=fmax(d,c);
while(islessequal(d,b)){
a=(cos(b))+(ceil(d));
b=log(f);
e=atan2(d,e);
}
}