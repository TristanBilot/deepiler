#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(log(c))*(sin(d));
d=(fmin(d,d))+(fmax(f,e));
a=(tan(e))+(pow(c,f));
a=pow(b,c);
f=(fdim(c,f))*(asin(b));
while(islessgreater(a,d)){
c=atan2(a,a);
f=(pow(f,b))+(ceil(d));
a=(pow(c,a))-(atan2(d,b));
b=floor(b);
}
d=pow(b,f);
e=log10(a);
}