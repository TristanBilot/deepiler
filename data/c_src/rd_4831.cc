#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(sqrt(e))+(exp(c));
d=(log10(g))/(fmin(c,d));
h=atan(h);
if(isgreaterequal(g,b)){
g=(tan(c))-(exp(a));
b=(fmin(e,f))+(exp(e));
h=(asin(b))*(log(e));
d=atan2(g,c);
}
else{
f=asin(a);
f=fmax(b,c);
g=tan(d);
}
d=(exp(g))/(pow(a,e));
g=(ceil(h))/(asin(h));
a=tan(f);
d=fmin(b,c);
}