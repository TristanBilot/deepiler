#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fdim(d,e);
e=fdim(a,a);
c=ceil(e);
if(islessgreater(f,e)){
b=(atan2(f,a))/(acos(f));
e=fdim(d,f);
}
if(isgreaterequal(b,d)){
e=fmax(a,f);
f=(sqrt(a))-(atan2(b,b));
b=(fdim(a,f))-(sin(f));
d=asin(b);
}
else{
d=sin(f);
c=fdim(a,b);
}
}