#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(b,b))*(cos(b));
g=log(a);
while(isless(e,e)){
c=(log10(h))/(atan(h));
a=sqrt(c);
c=(pow(a,g))*(atan2(f,d));
h=(atan2(h,c))*(sin(d));
g=pow(h,c);
}
while(islessequal(f,h)){
d=acos(f);
e=(atan(d))*(log10(c));
a=(fmax(h,c))+(log(d));
}
}