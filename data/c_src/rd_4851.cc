#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(h,c))-(atan(d));
a=fmax(b,d);
f=(log(h))*(acos(a));
b=pow(c,b);
while(isless(f,d)){
b=(fdim(h,f))+(acos(c));
b=(cos(c))-(fmin(h,b));
g=fmax(h,d);
a=pow(a,f);
}
if(isless(f,c)){
g=log10(c);
f=acos(g);
h=fdim(e,h);
f=(log(g))*(pow(e,d));
g=(log10(e))*(fmin(g,d));
}
}