#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log(f))-(tan(e));
d=log10(f);
e=fmax(e,c);
a=(atan2(g,g))-(log(b));
if(isless(b,e)){
c=atan2(d,d);
b=(atan2(d,g))-(acos(g));
}
else{
b=fmin(c,g);
b=fmax(g,a);
b=(pow(a,c))*(pow(a,e));
b=log10(f);
c=(atan2(g,e))*(log10(d));
}
if(islessequal(d,f)){
d=(fmax(g,f))-(sqrt(d));
b=(acos(c))*(pow(e,e));
g=atan(g);
e=(ceil(b))-(pow(b,e));
}
}