#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(d,c))+(exp(b));
e=(atan2(f,a))/(log(c));
a=fmin(e,e);
b=(floor(a))*(sin(f));
b=(exp(b))*(sin(d));
while(isless(a,b)){
c=cos(d);
a=pow(a,e);
d=(exp(f))/(pow(c,b));
}
if(isgreaterequal(c,a)){
b=fmax(c,e);
b=(sin(e))*(atan2(f,b));
a=(cos(b))+(atan2(b,b));
d=(log(a))*(pow(b,d));
}
}