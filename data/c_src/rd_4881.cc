#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(a,e);
c=floor(c);
c=(log(b))+(fmax(b,a));
f=(exp(b))+(fmin(c,f));
b=(cos(a))*(floor(c));
b=(asin(a))-(pow(e,f));
c=(atan2(f,f))-(cos(f));
c=fmax(b,f);
e=(sqrt(b))/(pow(f,c));
d=atan2(f,d);
d=(sqrt(d))*(fmin(b,b));
}