#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(a,b))*(sin(d));
d=(exp(b))-(ceil(c));
d=(fdim(a,a))*(fmax(d,d));
a=atan2(b,a);
b=floor(d);
d=log10(a);
b=(fdim(b,a))*(exp(e));
b=atan(c);
d=asin(a);
while(isgreaterequal(b,a)){
b=tan(e);
e=(fdim(b,a))+(acos(a));
a=atan(b);
}
}