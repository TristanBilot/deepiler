#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(f,a))/(atan(c));
b=(pow(e,d))-(pow(d,e));
e=cos(a);
c=atan2(e,f);
g=asin(d);
if(islessgreater(b,b)){
g=fmin(b,c);
c=floor(d);
c=atan(d);
f=(fmax(c,f))*(fdim(b,d));
}
else{
d=pow(d,e);
e=pow(e,e);
e=(pow(e,b))-(atan(e));
e=sqrt(c);
}
a=fmax(d,a);
a=(ceil(e))/(atan2(c,b));
e=(fdim(a,g))/(sin(f));
b=(cos(b))*(exp(e));
}