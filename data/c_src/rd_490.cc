#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(d,e))*(fmin(e,e));
a=sqrt(a);
e=(log10(c))/(sqrt(c));
f=(pow(b,d))+(atan2(c,b));
f=(atan(f))/(log(d));
if(isgreaterequal(f,e)){
e=atan2(d,e);
a=tan(c);
c=acos(e);
}
else{
a=pow(e,d);
c=ceil(c);
e=(fmax(c,a))+(ceil(e));
}
if(isless(b,c)){
b=(cos(b))-(sqrt(c));
a=pow(c,a);
d=ceil(e);
d=fmin(e,a);
b=atan(b);
}
else{
d=asin(c);
d=(fdim(c,a))+(pow(c,c));
a=(fmax(a,d))/(acos(f));
b=exp(c);
}
}