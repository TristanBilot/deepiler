#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(cos(f))/(exp(h));
f=atan2(g,h);
a=(fmax(d,g))/(pow(h,g));
a=(tan(d))+(fdim(f,d));
if(isless(e,b)){
b=(fmax(e,b))-(log10(g));
d=tan(b);
h=(fdim(e,e))/(fmax(b,a));
}
if(isless(b,a)){
d=(log(g))*(sin(g));
c=atan2(d,c);
c=(atan2(e,c))+(fmax(e,f));
}
else{
b=asin(f);
b=(fdim(e,g))+(fdim(b,c));
e=(pow(b,c))-(sin(a));
f=(ceil(a))*(fmax(e,b));
}
}