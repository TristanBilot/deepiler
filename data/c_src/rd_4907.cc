#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fdim(g,c))+(tan(b));
e=(sin(f))+(atan(g));
e=(sin(c))-(acos(g));
e=(cos(b))*(floor(a));
g=(log10(f))+(sqrt(b));
a=fmax(f,g);
if(islessgreater(c,e)){
e=(pow(a,d))-(fmax(c,g));
a=(floor(c))*(log10(e));
}
else{
c=(pow(c,g))/(fmin(c,e));
g=atan(d);
g=(fmin(f,a))*(sqrt(g));
}
}