#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(c,b);
e=atan2(a,g);
h=(fmax(f,b))*(fmin(e,h));
f=fmax(g,f);
a=atan2(h,g);
h=acos(d);
b=(atan2(b,c))-(atan2(a,a));
b=ceil(e);
while(isless(h,d)){
e=(fmax(f,a))*(sqrt(e));
e=atan2(g,e);
c=sqrt(d);
b=fmin(c,a);
}
}