#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(log10(b))-(atan2(e,c));
d=atan2(e,f);
d=(atan2(f,g))-(tan(d));
d=(atan2(f,d))-(fmin(b,a));
f=(acos(f))*(pow(b,d));
g=(cos(d))-(sqrt(a));
c=(asin(g))+(acos(b));
f=asin(d);
d=fdim(c,b);
if(isless(b,f)){
g=(fmin(g,a))*(exp(e));
a=(fmax(b,b))+(asin(e));
c=(cos(d))/(acos(a));
}
else{
d=(log10(g))+(sqrt(c));
a=cos(d);
c=fmax(a,c);
}
}