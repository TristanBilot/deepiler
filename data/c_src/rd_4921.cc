#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log(d))/(pow(g,d));
d=log(c);
e=fmax(d,b);
c=fmax(e,e);
d=fmin(g,f);
if(isgreaterequal(a,b)){
a=sin(d);
g=(fdim(d,c))/(floor(d));
g=(fdim(e,b))-(fmax(a,b));
}
else{
d=cos(b);
e=(atan(d))*(pow(f,b));
d=(atan2(e,d))/(fdim(g,a));
f=cos(d);
f=log10(a);
}
c=(fmax(f,c))-(tan(e));
e=(fmax(e,f))-(tan(f));
f=pow(b,d);
f=(fmin(d,b))/(asin(g));
}