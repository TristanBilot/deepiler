#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=sin(b);
a=atan2(b,a);
c=fmax(a,a);
e=(atan2(f,e))-(atan2(c,a));
d=fdim(a,a);
d=(atan2(b,d))/(atan2(a,d));
while(islessequal(d,e)){
d=fmax(a,d);
d=log(d);
e=(pow(b,b))+(pow(f,d));
a=atan(f);
}
}