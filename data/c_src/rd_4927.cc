#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(c,c);
a=sin(a);
e=(fmax(e,d))*(sqrt(c));
b=fmin(a,e);
while(isless(e,a)){
a=log(e);
c=cos(a);
}
if(islessgreater(a,b)){
e=(fdim(e,e))*(cos(e));
d=fmax(a,b);
}
else{
a=cos(a);
e=fmin(e,a);
a=(fdim(b,a))-(cos(e));
e=(ceil(e))/(cos(c));
b=(log(d))*(asin(b));
}
}