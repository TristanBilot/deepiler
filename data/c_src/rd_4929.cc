#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(exp(b))/(pow(f,c));
a=fmax(c,f);
d=(atan2(a,d))/(atan2(d,e));
f=fdim(b,d);
while(isless(e,a)){
b=(fmax(d,c))*(log(f));
e=pow(b,b);
f=(acos(e))*(pow(f,d));
f=pow(a,c);
}
c=sqrt(d);
e=atan2(b,b);
}