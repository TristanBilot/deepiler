#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fdim(b,a))*(pow(f,b));
e=(pow(d,d))*(pow(a,a));
if(islessgreater(d,a)){
a=fmax(e,e);
d=pow(a,d);
e=pow(e,e);
f=fmin(e,e);
d=pow(c,c);
}
else{
f=(fmax(e,d))*(fmin(b,a));
a=(atan2(e,f))+(log(a));
}
while(islessequal(b,d)){
a=asin(b);
b=fdim(e,b);
f=fmin(b,c);
a=fdim(e,f);
e=fmin(c,f);
}
}