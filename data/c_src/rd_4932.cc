#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(d,c);
d=floor(b);
a=(fdim(b,a))*(tan(e));
b=atan2(a,d);
b=(sqrt(e))+(floor(f));
f=fdim(c,c);
f=(fmax(d,b))-(sqrt(f));
d=(fmin(a,c))*(ceil(f));
b=(tan(a))+(fmax(e,c));
if(isgreaterequal(a,c)){
d=(atan2(d,d))*(pow(d,d));
d=fmin(e,a);
a=(exp(f))+(floor(d));
}
}