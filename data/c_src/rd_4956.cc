#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=acos(a);
g=(pow(c,f))+(fdim(c,e));
c=pow(c,e);
g=(fmin(g,b))/(fmin(e,h));
f=(fmax(f,e))/(asin(c));
if(isless(a,e)){
b=(sin(a))/(sin(c));
d=atan(g);
c=fmin(d,c);
f=fmax(c,b);
}
else{
b=(cos(h))/(fdim(e,e));
h=fmax(b,h);
b=fmax(a,c);
h=asin(b);
}
while(isless(f,h)){
a=fmin(h,g);
b=(fmax(g,e))/(fdim(h,g));
f=tan(h);
}
}