#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmin(c,g);
f=(acos(d))-(fmax(c,d));
while(isless(e,f)){
f=fmin(c,a);
c=pow(g,b);
a=(fdim(b,e))+(fmin(d,c));
f=(asin(d))+(sqrt(d));
a=(fdim(g,g))+(log10(d));
}
while(isless(b,b)){
d=asin(g);
f=sqrt(b);
d=fdim(g,a);
e=fmin(c,f);
e=asin(g);
}
}