#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(e,b))*(atan2(d,g));
a=log10(g);
g=(cos(g))/(fdim(b,g));
if(islessgreater(a,g)){
g=tan(c);
g=fdim(a,g);
g=fmax(a,b);
e=(sin(c))/(fmax(a,a));
f=(acos(f))/(log(g));
}
f=(pow(f,d))-(atan(g));
e=atan2(f,f);
g=fmax(a,c);
c=log(a);
e=(atan2(f,f))-(atan2(d,e));
}