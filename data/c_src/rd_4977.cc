#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(c,b))-(tan(a));
e=(fmin(d,a))/(fmax(e,a));
a=(fmin(e,b))-(log(a));
a=(acos(b))/(pow(a,c));
b=(pow(a,e))/(fmin(d,a));
a=(log(b))/(exp(e));
b=pow(c,a);
}