#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(c,d))+(exp(d));
a=(log10(e))/(fdim(d,b));
if(isless(a,a)){
a=pow(b,c);
a=(log(b))+(sqrt(c));
}
a=acos(a);
a=(sin(b))*(pow(c,e));
e=pow(d,c);
d=(atan2(e,c))/(pow(a,e));
c=fmin(d,c);
}