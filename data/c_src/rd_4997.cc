#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan(a);
c=atan2(b,f);
e=acos(g);
if(islessequal(d,c)){
c=fdim(c,g);
b=log(h);
e=log(h);
h=fdim(f,d);
}
d=(cos(c))+(pow(a,a));
c=(fmax(e,g))/(log(e));
}