#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(c);
a=(tan(c))/(fmin(a,a));
e=(floor(b))-(floor(d));
while(islessequal(a,c)){
b=fmax(c,d);
c=exp(c);
}
if(isgreaterequal(d,c)){
c=(log(a))*(ceil(d));
a=(fmin(d,e))/(pow(e,c));
d=pow(b,c);
}
}