#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(exp(d))/(log(c));
c=(fmax(e,b))-(tan(e));
e=fmin(a,b);
while(islessgreater(c,b)){
e=pow(c,c);
a=(fmax(a,e))*(fdim(c,e));
a=fdim(e,d);
a=(atan(b))/(floor(a));
e=(sqrt(c))*(sqrt(a));
}
if(isgreaterequal(b,a)){
a=pow(e,a);
b=fmin(e,e);
b=fmax(e,c);
}
}