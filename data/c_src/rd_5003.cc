#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(floor(b))/(atan2(g,c));
g=(log(g))*(atan(a));
b=(atan2(g,a))/(log(e));
g=(log(a))*(pow(a,e));
b=(fmin(b,f))*(fmin(g,g));
f=(atan(c))-(fmax(e,e));
e=(sin(c))+(floor(c));
b=(fdim(f,g))*(log10(a));
while(isless(a,a)){
d=(asin(g))-(fmax(e,d));
g=acos(f);
a=atan(c);
a=log10(e);
d=(asin(c))-(fmax(b,b));
}
}