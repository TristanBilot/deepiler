#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=atan2(a,f);
c=(fdim(f,b))+(tan(b));
c=atan2(e,c);
f=ceil(d);
e=(fmin(f,a))-(fdim(b,d));
while(islessgreater(e,a)){
c=sqrt(d);
e=(exp(b))-(fmax(f,d));
}
if(islessgreater(e,c)){
f=fmin(a,e);
e=(atan(a))-(atan2(f,c));
e=cos(c);
}
}