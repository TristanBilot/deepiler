#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=exp(f);
b=(acos(b))/(fdim(c,a));
e=(cos(b))*(fdim(c,e));
if(islessgreater(d,c)){
c=fdim(f,a);
d=tan(e);
e=(log(b))*(pow(a,d));
f=(pow(e,f))+(sin(a));
}
else{
f=(sin(d))-(log10(e));
c=(ceil(b))*(log10(b));
}
c=pow(f,e);
f=(sin(d))*(pow(a,a));
a=pow(f,e);
b=fdim(f,c);
}