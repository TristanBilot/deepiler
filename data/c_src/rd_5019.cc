#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(a);
f=(acos(f))-(sin(g));
g=(atan2(h,g))+(fdim(a,d));
b=asin(a);
h=(fdim(c,c))/(tan(c));
b=pow(g,c);
h=(acos(b))-(fmax(d,e));
while(islessequal(c,g)){
g=(pow(g,a))-(fmax(a,c));
a=(atan2(h,a))/(fmax(c,c));
a=atan2(c,a);
f=fmin(g,h);
h=(log10(c))+(fdim(e,e));
}
}