#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(exp(b))+(cos(h));
g=(cos(g))/(atan(d));
b=sin(c);
d=fdim(a,b);
if(islessequal(e,c)){
h=(tan(c))+(fdim(d,d));
a=fmax(a,b);
}
else{
h=fdim(g,a);
b=(floor(d))-(ceil(h));
}
while(isgreaterequal(f,g)){
f=(pow(c,f))-(fmax(g,b));
g=fmin(c,c);
}
}