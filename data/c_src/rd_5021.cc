#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(c);
d=(atan2(e,b))+(pow(e,e));
c=(atan2(b,e))*(log10(d));
c=atan(c);
if(isless(d,d)){
e=(pow(b,c))-(log(d));
e=pow(e,b);
a=fmin(a,a);
}
if(islessgreater(a,c)){
b=(acos(c))*(log(a));
d=(fmin(d,b))/(sin(a));
c=fdim(b,c);
a=sin(c);
}
else{
e=sqrt(e);
e=(fmin(b,d))*(log10(e));
a=atan(a);
d=fmin(d,e);
}
}