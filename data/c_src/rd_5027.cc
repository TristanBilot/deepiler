#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(a,c))/(cos(a));
a=fmin(d,b);
d=fmin(d,d);
e=(exp(a))-(fmax(d,d));
b=(fmax(a,b))-(atan(c));
a=(ceil(a))*(cos(d));
if(islessequal(d,c)){
b=(fdim(d,d))/(fdim(b,a));
a=pow(c,b);
}
}