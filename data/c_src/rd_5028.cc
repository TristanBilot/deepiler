#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(b,g))/(atan2(d,c));
g=cos(h);
a=(atan2(b,g))*(pow(a,a));
f=(fdim(c,g))/(exp(a));
d=pow(g,d);
c=(pow(e,f))/(pow(f,c));
c=(atan2(a,e))+(fmax(g,c));
f=exp(a);
b=fdim(g,e);
}