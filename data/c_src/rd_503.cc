#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log(e))-(pow(b,b));
f=atan(a);
d=(sin(g))+(fdim(g,f));
g=(exp(e))/(atan2(b,b));
f=(cos(a))-(log(a));
e=floor(f);
if(isless(d,b)){
g=(log(f))+(sqrt(d));
e=pow(e,d);
c=(fdim(g,a))/(asin(f));
}
else{
c=pow(f,c);
d=fmin(b,a);
a=atan(a);
g=(atan2(d,c))+(acos(b));
d=(fmin(d,d))/(atan2(b,e));
}
}