#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(acos(f))-(exp(e));
g=(cos(c))+(fmax(a,b));
c=fmax(c,a);
while(isgreaterequal(c,g)){
a=(ceil(f))+(asin(g));
b=(fmin(a,b))/(pow(b,a));
f=(log(a))/(sin(a));
g=fdim(e,b);
}
while(islessequal(f,d)){
g=sin(f);
b=acos(g);
c=asin(f);
a=pow(a,b);
}
}