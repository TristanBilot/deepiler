#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(a,b))+(pow(c,d));
e=(asin(e))/(floor(a));
d=(floor(b))*(ceil(b));
c=(fmax(a,e))*(exp(e));
b=log10(e);
if(isless(a,c)){
d=sin(d);
e=log10(b);
}
else{
e=pow(e,e);
c=(atan(a))*(acos(c));
b=cos(d);
a=(cos(b))+(atan(a));
}
if(islessgreater(e,a)){
d=log(e);
d=(ceil(d))*(log(e));
a=(ceil(e))+(sqrt(b));
}
}