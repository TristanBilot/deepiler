#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(f,c))-(pow(a,f));
d=tan(d);
c=(cos(d))+(ceil(e));
f=sqrt(d);
d=(fmax(d,a))-(fdim(c,b));
c=(pow(d,d))/(ceil(b));
if(islessequal(d,c)){
e=asin(d);
f=log(e);
}
else{
c=(cos(b))*(ceil(d));
f=pow(d,d);
d=(acos(b))+(cos(a));
}
}