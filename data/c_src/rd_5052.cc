#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(d,a))*(log10(g));
f=log(e);
h=pow(d,a);
e=atan(d);
h=(log(a))*(atan(d));
h=fmin(h,d);
h=fmax(f,b);
while(isgreaterequal(e,a)){
b=exp(b);
g=ceil(d);
e=fmax(d,g);
d=(pow(b,d))*(sqrt(d));
}
}