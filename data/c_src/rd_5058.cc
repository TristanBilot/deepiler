#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(d);
b=sin(a);
if(islessgreater(a,a)){
c=(log10(a))/(cos(d));
a=(fmin(a,a))+(atan2(e,c));
e=(exp(c))-(sin(a));
c=(log(e))+(acos(d));
c=exp(d);
}
else{
b=sqrt(a);
b=fmax(c,a);
c=(sqrt(a))*(atan2(b,c));
e=asin(b);
}
a=(fdim(a,b))/(sqrt(a));
d=(sqrt(c))/(sin(d));
}