#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(a,d);
d=(acos(g))-(tan(d));
g=floor(g);
h=fmax(a,a);
b=(log10(b))/(log10(h));
if(islessequal(h,g)){
g=pow(f,f);
a=asin(h);
c=exp(c);
}
if(islessgreater(b,h)){
a=(sqrt(a))+(atan2(g,h));
h=sqrt(b);
g=(fmin(e,e))*(atan(d));
c=pow(h,e);
}
}