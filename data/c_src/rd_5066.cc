#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(d,a))/(pow(e,a));
a=atan2(a,d);
if(isless(e,e)){
c=atan2(d,e);
e=(sqrt(c))/(sqrt(b));
a=floor(d);
b=acos(d);
}
else{
c=ceil(a);
d=(atan2(a,b))*(cos(e));
c=(atan(a))*(asin(e));
c=sqrt(c);
a=fmax(b,c);
}
while(isless(e,c)){
d=fdim(b,b);
c=fmin(c,c);
e=(fmax(d,d))+(asin(e));
d=(pow(a,d))*(fmax(e,b));
}
}