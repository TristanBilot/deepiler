#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(b,d);
e=exp(f);
while(isgreaterequal(g,e)){
f=(fmax(g,g))+(asin(a));
c=fmax(c,f);
}
while(islessgreater(d,f)){
d=fmax(g,g);
c=atan2(d,g);
}
}