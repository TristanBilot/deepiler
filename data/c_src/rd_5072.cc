#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(c))/(fdim(a,e));
a=(tan(a))+(sin(c));
b=(pow(c,c))-(sin(c));
c=(fdim(d,e))/(log10(c));
d=(atan(b))-(fmax(a,a));
if(isgreaterequal(d,e)){
a=fmin(b,c);
e=(fdim(c,b))*(fmax(b,c));
e=(atan2(e,a))+(pow(d,c));
b=pow(c,c);
c=exp(d);
}
e=fmin(e,e);
b=fmax(b,a);
e=fmax(e,c);
}