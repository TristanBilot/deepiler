#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(f))+(asin(d));
f=(acos(e))*(pow(a,e));
a=(cos(b))*(exp(e));
d=fdim(e,b);
while(islessequal(d,f)){
d=fmin(f,c);
d=(fmax(e,b))-(atan(c));
f=(sqrt(f))-(acos(e));
e=fdim(e,b);
b=(log10(a))-(pow(f,f));
}
if(isgreaterequal(c,b)){
e=(fmax(d,f))*(fdim(e,e));
b=log10(f);
}
else{
c=pow(e,a);
d=sqrt(d);
}
}