#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,a);
c=fmax(a,e);
c=acos(d);
d=(atan(b))-(atan(a));
b=(acos(d))/(sin(c));
a=(sin(a))+(acos(e));
d=fdim(c,c);
if(isless(b,d)){
d=(fmax(d,c))*(ceil(e));
c=(asin(c))-(fmax(a,c));
e=(fdim(d,a))*(atan2(d,e));
a=pow(a,d);
c=(fmin(c,e))+(asin(d));
}
else{
b=(cos(d))-(ceil(e));
a=fmax(e,e);
a=floor(b);
d=atan(c);
d=sin(d);
}
}