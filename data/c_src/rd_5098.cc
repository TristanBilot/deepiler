#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(b,b);
b=(asin(c))*(fmin(a,d));
while(islessequal(c,a)){
c=(atan(b))*(log10(e));
a=fmin(d,c);
b=(fmin(d,e))*(log10(c));
}
d=(fdim(a,c))-(atan(a));
c=sin(b);
e=(asin(c))/(pow(c,e));
c=exp(c);
c=log(c);
}