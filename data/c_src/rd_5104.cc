#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,a))-(log(c));
c=pow(c,b);
a=(sin(e))-(fmin(e,a));
a=exp(e);
b=(log10(c))+(sqrt(c));
a=(exp(e))/(log(c));
b=(ceil(e))*(fdim(c,d));
while(isless(a,c)){
c=fmin(b,b);
e=(atan2(e,c))*(pow(d,c));
c=(sqrt(b))+(atan2(c,d));
d=(fmax(c,a))*(pow(e,a));
}
}