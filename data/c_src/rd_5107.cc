#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(asin(e))*(fmin(b,c));
b=(fmin(b,a))*(fmin(c,b));
e=sin(b);
f=sin(e);
d=fmax(f,c);
a=(log(d))+(fmin(b,a));
if(islessgreater(a,f)){
d=pow(c,d);
c=(atan2(a,f))+(fmin(a,b));
d=fdim(b,a);
e=(atan2(c,a))*(fdim(e,c));
e=fmax(b,d);
}
else{
d=atan2(a,e);
f=fmin(b,b);
}
}