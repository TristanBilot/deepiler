#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=cos(d);
b=(pow(c,b))*(atan2(g,d));
while(isgreaterequal(f,b)){
a=pow(f,g);
d=fdim(a,b);
c=log10(c);
}
if(isless(e,b)){
c=log(e);
g=pow(g,f);
b=cos(a);
d=(fmax(e,c))-(pow(a,b));
c=atan(d);
}
else{
f=ceil(a);
b=log10(g);
e=fmin(g,b);
a=cos(b);
g=fdim(f,g);
}
}