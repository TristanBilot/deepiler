#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(d,b);
c=(cos(d))-(pow(a,e));
e=(fmax(c,a))/(atan2(c,b));
a=(fmin(a,e))+(sqrt(c));
b=(fdim(a,b))*(ceil(e));
while(isless(c,b)){
b=atan2(c,b);
a=log(a);
a=(fmax(c,e))*(sqrt(c));
c=log10(b);
b=fmin(c,c);
}
if(isgreaterequal(d,b)){
d=(sqrt(b))*(atan2(c,a));
e=fmax(d,d);
d=(atan2(b,c))+(acos(e));
a=log(b);
d=fmax(d,b);
}
else{
c=(sqrt(d))*(exp(d));
b=(floor(b))*(sin(d));
a=fdim(e,c);
d=fmin(d,a);
d=pow(b,d);
}
}