#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,a);
d=fdim(e,d);
d=sqrt(a);
a=atan2(e,d);
if(islessgreater(a,e)){
d=(fmax(b,e))*(exp(d));
e=(asin(e))/(log(b));
}
if(islessequal(a,a)){
c=log10(c);
b=fmax(e,d);
}
}