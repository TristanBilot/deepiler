#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(d,a);
b=(pow(c,c))/(log10(b));
c=exp(b);
while(isgreaterequal(a,a)){
b=(fdim(b,c))-(atan(d));
b=tan(c);
c=(fmax(d,d))*(pow(b,e));
b=fdim(a,c);
e=cos(d);
}
while(islessgreater(e,e)){
e=(ceil(e))*(atan(e));
d=atan2(e,a);
e=tan(e);
}
}