#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmax(f,g))/(exp(g));
e=(cos(e))+(pow(c,f));
f=(fmin(g,e))-(sin(a));
c=(sin(d))-(fmin(b,a));
while(isgreaterequal(e,f)){
a=(sin(d))*(atan2(e,a));
d=(fmin(c,g))+(sin(f));
f=(fmax(b,f))-(pow(f,f));
}
if(islessequal(f,c)){
d=(exp(e))/(log(c));
f=(cos(b))-(fmin(g,d));
}
else{
f=fmax(e,a);
c=(sqrt(d))+(fdim(b,e));
}
}