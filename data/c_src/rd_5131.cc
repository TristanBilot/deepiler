#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=exp(a);
c=pow(d,d);
if(islessgreater(c,c)){
a=(tan(d))*(acos(b));
a=fmin(b,a);
}
d=sin(b);
e=(acos(e))*(pow(e,b));
}