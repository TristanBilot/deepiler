#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmin(b,a);
a=(atan2(a,f))-(cos(b));
d=(fmax(d,c))/(exp(a));
b=(fmin(d,c))/(fmax(d,a));
b=sin(b);
f=(exp(f))+(fmin(a,a));
c=(fmin(e,d))+(fmax(d,b));
d=(log(c))*(fmin(c,b));
if(isless(c,b)){
d=(log10(c))-(tan(d));
c=atan2(f,e);
b=fmax(c,d);
b=(cos(c))/(fmax(c,c));
a=(fdim(d,a))/(atan2(f,a));
}
}