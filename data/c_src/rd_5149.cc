#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan(c);
f=fdim(g,e);
f=(atan2(a,a))+(ceil(b));
e=(pow(c,c))+(fmax(a,d));
e=(floor(a))+(exp(e));
d=fdim(b,e);
c=acos(a);
a=(asin(a))+(log(e));
while(isless(a,f)){
f=atan2(b,d);
f=fmin(c,f);
b=(fmin(b,f))/(atan(d));
}
}