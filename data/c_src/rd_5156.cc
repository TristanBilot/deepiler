#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=asin(e);
c=(acos(e))*(sin(b));
d=(fdim(b,a))-(floor(b));
d=(fmin(d,d))-(fmin(b,c));
while(isgreaterequal(d,e)){
b=(fmin(e,e))/(atan2(f,d));
d=(fmin(c,a))*(log(c));
}
b=fmax(f,a);
b=log(d);
a=floor(f);
}