#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(e,b);
b=(fmin(c,e))+(fmax(a,b));
a=(sqrt(c))*(asin(g));
c=ceil(a);
while(islessequal(f,c)){
d=fdim(d,f);
f=log10(h);
}
while(islessequal(e,h)){
d=ceil(d);
b=pow(b,d);
}
}