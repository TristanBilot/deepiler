#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(b,c))+(acos(e));
c=cos(e);
if(isless(a,e)){
a=acos(c);
b=(acos(b))-(exp(e));
c=(atan2(a,f))+(floor(c));
f=(log(c))/(log10(b));
}
if(islessequal(c,f)){
a=tan(d);
f=(fmin(a,a))-(exp(a));
e=(sqrt(d))-(exp(d));
a=(fdim(a,d))-(asin(b));
}
}