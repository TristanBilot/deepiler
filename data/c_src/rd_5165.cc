#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sqrt(b))-(atan2(a,c));
b=ceil(a);
if(islessequal(a,e)){
d=(fmax(b,b))/(fmax(a,b));
a=(log10(b))*(log10(b));
b=atan2(a,e);
e=(atan(b))*(atan(e));
}
else{
c=ceil(d);
e=asin(a);
c=(fmax(d,b))+(fmin(e,b));
a=pow(a,d);
}
while(isgreaterequal(c,d)){
e=(asin(c))-(atan2(d,a));
c=atan2(c,c);
b=pow(d,b);
b=ceil(c);
e=log(c);
}
}