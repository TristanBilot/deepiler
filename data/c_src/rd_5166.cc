#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(d);
e=tan(b);
d=pow(a,e);
c=(ceil(b))+(fmin(a,d));
d=atan(c);
c=(ceil(b))*(pow(b,b));
c=sin(e);
c=(fmin(e,b))+(pow(b,d));
c=atan2(a,c);
while(islessgreater(d,e)){
c=(fmax(c,d))/(atan(c));
e=atan(e);
c=pow(c,d);
}
}