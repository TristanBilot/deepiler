#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=cos(b);
a=fmin(c,c);
d=cos(a);
d=(atan2(c,c))*(sin(d));
a=fmax(a,d);
while(isless(c,d)){
a=fdim(e,b);
a=fmin(c,c);
b=(fmin(a,a))-(exp(a));
e=(pow(a,d))+(fmin(e,c));
b=acos(e);
}
while(isgreaterequal(e,c)){
b=(cos(a))*(acos(b));
c=(fdim(b,a))-(tan(b));
e=cos(c);
}
}