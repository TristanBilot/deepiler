#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(a,e);
h=(sin(d))+(fmax(c,c));
a=log(h);
c=fmax(b,a);
if(islessgreater(e,e)){
a=sqrt(b);
e=(log(h))*(asin(e));
e=floor(a);
}
a=(atan(b))*(ceil(e));
a=(floor(f))-(fmax(a,d));
f=floor(c);
}