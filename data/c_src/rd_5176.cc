#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(e,e))+(exp(c));
e=fdim(e,c);
a=fmax(e,a);
while(islessgreater(a,a)){
a=fmax(c,c);
a=atan2(b,c);
b=(tan(a))/(acos(c));
c=(pow(e,e))+(sqrt(d));
d=(log10(d))+(tan(b));
}
d=(atan2(b,a))/(fmax(e,e));
d=(fmin(b,e))/(fmin(b,a));
a=(tan(e))+(pow(c,d));
}