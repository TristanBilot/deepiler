#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(b);
b=fdim(c,d);
e=pow(d,e);
while(isgreaterequal(c,d)){
b=floor(c);
a=(acos(c))/(exp(e));
b=(fdim(c,b))-(log(e));
}
while(islessequal(b,e)){
e=(cos(e))-(fmax(c,c));
b=(exp(e))+(floor(c));
d=(atan2(c,a))-(log10(e));
e=fdim(e,b);
}
}