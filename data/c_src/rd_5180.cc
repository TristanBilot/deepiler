#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(a,d))-(ceil(a));
b=(atan2(b,d))*(atan2(c,f));
b=(acos(d))+(asin(b));
c=(cos(d))/(ceil(d));
f=(exp(b))-(pow(d,c));
if(isless(b,e)){
e=(ceil(f))+(asin(c));
c=(log10(b))/(acos(e));
}
else{
e=(exp(f))*(fmax(e,c));
c=(acos(b))-(exp(b));
}
if(isgreaterequal(c,e)){
c=log10(c);
e=fdim(d,a);
a=(pow(b,e))*(cos(b));
f=(pow(e,f))+(atan2(b,d));
}
}