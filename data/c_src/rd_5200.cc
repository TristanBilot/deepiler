#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(d);
d=atan2(c,h);
e=fmin(c,b);
b=floor(b);
g=fmin(g,g);
g=atan2(a,g);
b=(fmax(h,a))-(exp(h));
h=cos(h);
}