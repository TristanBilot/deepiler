#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(tan(a))/(floor(g));
c=(atan2(c,g))+(log(g));
f=cos(f);
g=fmax(g,c);
a=sin(f);
while(isgreaterequal(a,e)){
g=(exp(f))-(log10(e));
d=(fdim(a,e))-(fmax(g,f));
e=(sin(g))/(fmax(d,f));
g=(fmax(a,e))-(fdim(b,e));
f=(fdim(f,b))-(pow(c,b));
}
}