#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(b);
f=(fdim(c,d))-(sin(b));
a=exp(e);
if(islessequal(a,c)){
a=(fmax(a,a))*(log(e));
d=(fmax(e,a))*(atan(c));
}
else{
a=fmax(e,b);
d=pow(e,b);
c=log10(b);
f=(floor(a))*(log(d));
}
c=(floor(e))/(pow(e,e));
f=atan(b);
}