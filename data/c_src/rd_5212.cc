#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=exp(b);
b=fdim(d,c);
a=fdim(a,f);
g=(fmin(d,a))*(fmax(a,d));
c=fmin(d,g);
if(isless(c,e)){
c=(log(a))/(atan2(a,b));
c=(log(e))/(atan2(e,b));
g=fmax(d,d);
d=log(a);
a=pow(d,b);
}
e=exp(c);
g=asin(c);
}