#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(h,d))+(atan2(b,c));
a=fdim(f,c);
e=(log10(c))+(sin(a));
b=(sin(f))+(atan(e));
h=fmax(a,h);
g=(floor(g))-(fdim(b,a));
a=atan2(f,d);
b=(floor(h))-(sqrt(a));
h=(fdim(c,g))-(pow(d,c));
}