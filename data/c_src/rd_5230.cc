#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(e,a))*(fmin(b,a));
d=(fmax(a,a))-(log10(d));
d=pow(b,a);
c=(sqrt(c))/(asin(e));
a=(ceil(d))*(fmin(c,a));
if(isgreaterequal(c,e)){
e=(log10(b))+(atan(e));
b=log10(b);
d=(fmin(b,d))*(atan(a));
b=(sqrt(d))/(fdim(b,a));
c=(log10(d))+(tan(d));
}
if(islessequal(a,a)){
d=(floor(d))/(cos(e));
c=fdim(e,b);
c=exp(e);
e=(fdim(d,c))*(log10(c));
a=fmax(e,d);
}
}