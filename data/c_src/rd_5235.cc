#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(c,c);
a=(atan2(d,e))/(fdim(a,c));
while(isless(b,a)){
c=(fmin(b,b))-(asin(b));
c=acos(a);
e=(fmax(d,b))/(fmin(c,a));
d=fdim(b,a);
c=(sin(b))+(fdim(d,d));
}
while(islessgreater(a,b)){
c=sin(e);
b=atan(c);
d=acos(a);
d=(fdim(e,e))/(sqrt(c));
e=(fmax(c,c))-(fdim(a,a));
}
}