#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(c,a))*(fmax(e,a));
d=(fmax(c,e))/(pow(c,b));
a=(pow(c,a))-(fdim(e,d));
e=sqrt(b);
e=sin(a);
d=(tan(d))*(ceil(c));
a=(atan(b))/(ceil(a));
d=(fdim(a,c))*(log(e));
a=atan2(b,b);
c=atan2(e,c);
b=fmin(d,a);
d=(log(e))-(tan(e));
}