#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(sin(f))*(atan2(d,e));
b=(exp(b))+(fdim(a,e));
f=(fmax(f,a))*(fmin(e,g));
c=(fmin(g,b))/(fmin(e,d));
e=(tan(c))*(sin(f));
if(isgreaterequal(f,a)){
b=(cos(a))*(fmax(f,c));
c=cos(c);
}
else{
a=(log10(d))*(atan2(g,f));
a=(ceil(g))/(pow(a,d));
c=fdim(f,d);
b=(floor(g))*(atan(g));
}
if(islessgreater(e,c)){
g=(cos(b))/(log10(a));
f=pow(c,a);
a=log10(e);
g=fdim(c,e);
}
}