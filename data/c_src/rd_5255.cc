#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(a,b);
b=(log(a))+(fdim(d,f));
d=floor(f);
e=fdim(f,e);
e=pow(d,d);
while(islessgreater(d,c)){
d=floor(b);
c=cos(a);
a=(log(d))-(fdim(b,b));
c=(atan2(f,e))+(atan(a));
}
while(isgreaterequal(a,a)){
e=(exp(a))-(pow(f,e));
b=(fdim(f,e))/(sin(e));
}
}