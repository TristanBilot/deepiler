#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(e,e))+(floor(d));
c=(cos(d))-(pow(b,e));
e=(atan2(d,c))/(fdim(a,b));
a=fmax(c,c);
if(islessequal(a,d)){
b=fmax(d,b);
c=(ceil(b))*(log(d));
d=(fmax(e,a))+(fmin(d,a));
d=pow(d,a);
d=atan(e);
}
else{
e=ceil(d);
c=(log10(c))-(fmax(d,e));
d=(exp(a))*(cos(e));
a=(fdim(e,c))-(sqrt(c));
}
a=(atan(d))+(ceil(c));
a=cos(a);
}