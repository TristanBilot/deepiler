#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmin(e,d);
c=acos(f);
f=(log10(a))/(fdim(c,c));
a=sin(c);
e=fmax(d,b);
f=(tan(a))-(atan(b));
f=(fmax(a,f))+(pow(b,d));
if(islessequal(e,d)){
f=(pow(f,c))/(ceil(d));
b=fmax(c,c);
e=log10(e);
e=ceil(d);
a=ceil(a);
}
else{
d=(floor(e))/(pow(b,c));
b=(atan2(a,b))/(fmin(b,b));
f=(fdim(e,b))*(fdim(d,c));
b=(atan2(b,b))-(pow(b,e));
c=pow(a,c);
}
}