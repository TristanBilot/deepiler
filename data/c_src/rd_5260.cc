#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan(d))*(sin(a));
c=sqrt(c);
a=(fmax(f,d))*(fdim(e,e));
f=cos(d);
d=(log(d))+(fmax(e,b));
while(islessequal(a,b)){
b=tan(c);
b=sin(a);
b=atan2(c,e);
}
d=fmax(f,b);
d=(fmin(b,d))*(sin(a));
a=fmax(b,b);
}