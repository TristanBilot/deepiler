#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(d,e);
c=fmax(d,d);
if(isless(a,c)){
b=fmax(b,a);
b=(pow(e,d))/(pow(e,e));
}
b=(fdim(b,b))-(fmax(b,a));
c=(fmin(a,e))+(fdim(d,d));
e=floor(b);
d=asin(d);
}