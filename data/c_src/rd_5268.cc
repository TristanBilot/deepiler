#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fdim(a,g))*(pow(b,b));
b=fmax(f,e);
c=fmin(a,d);
while(isless(f,b)){
e=ceil(b);
f=acos(f);
e=tan(c);
d=fmin(g,g);
}
while(islessgreater(e,a)){
a=atan2(b,f);
b=(asin(c))/(pow(e,f));
g=fmax(d,c);
}
}