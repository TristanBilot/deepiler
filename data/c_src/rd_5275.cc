#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(a);
a=exp(g);
d=cos(e);
a=fmax(h,h);
if(islessgreater(e,c)){
c=atan2(g,f);
c=sqrt(d);
d=pow(c,f);
e=(log(e))/(fmax(h,b));
d=(tan(c))*(sin(a));
}
else{
g=fmin(a,b);
a=fmax(g,d);
}
while(islessgreater(d,b)){
c=fdim(g,d);
f=(pow(d,d))-(pow(b,f));
h=(log10(d))-(sqrt(a));
}
}