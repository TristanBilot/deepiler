#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(c);
b=(fdim(e,h))*(atan(f));
g=sin(h);
a=atan2(b,b);
d=floor(c);
b=(sqrt(f))*(fmax(g,e));
a=sin(h);
a=(ceil(b))*(fmin(h,g));
a=cos(a);
d=fmax(e,d);
}