#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(c,e))*(pow(b,b));
d=tan(b);
a=log10(b);
b=(fmin(c,a))+(ceil(a));
if(islessequal(c,d)){
a=(fmax(d,b))+(atan2(c,c));
b=(acos(e))*(atan(d));
a=fmin(a,b);
}
else{
d=acos(e);
d=(atan2(a,b))+(log(a));
e=(sin(b))-(floor(c));
b=(atan(a))-(atan(c));
b=(tan(d))+(ceil(e));
}
if(isgreaterequal(b,b)){
b=(fmin(a,a))-(sin(c));
a=fmin(d,d);
c=asin(b);
a=(ceil(c))-(fmax(d,e));
d=(sqrt(e))-(fdim(b,a));
}
}