#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(c,e);
c=fmax(d,d);
while(islessgreater(b,d)){
c=floor(d);
c=atan(d);
c=acos(a);
}
while(isgreaterequal(c,d)){
a=fmax(e,c);
d=(tan(c))*(fmax(e,a));
d=exp(e);
}
}