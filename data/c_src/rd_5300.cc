#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=exp(g);
c=(fmax(b,a))*(acos(b));
g=exp(e);
d=fmin(g,d);
e=(atan2(b,f))-(fdim(a,c));
c=atan2(e,b);
b=(fmin(c,c))/(tan(f));
while(isless(b,e)){
f=(atan2(g,e))+(fdim(e,b));
c=fmin(e,c);
g=(fmax(g,e))-(exp(b));
b=(pow(a,c))+(pow(d,e));
c=fmax(b,d);
}
}