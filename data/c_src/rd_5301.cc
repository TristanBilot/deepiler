#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(log10(e))*(fmax(f,e));
a=fmin(a,e);
a=(fmin(g,g))*(ceil(d));
a=(cos(c))/(fmax(g,g));
if(isless(d,b)){
g=tan(g);
e=fmin(c,c);
g=(sqrt(a))+(atan2(f,a));
a=(atan(c))*(fdim(e,a));
}
else{
e=pow(d,f);
g=(exp(d))*(cos(e));
c=atan2(d,f);
}
b=(pow(b,c))/(fmax(c,d));
d=fmin(a,f);
}