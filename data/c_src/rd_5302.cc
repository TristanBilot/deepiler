#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(c,e);
d=(pow(c,d))-(log(a));
d=(pow(c,b))-(asin(e));
b=(acos(b))+(tan(e));
b=asin(e);
a=pow(a,a);
e=fmax(c,e);
d=(log(c))/(fmin(b,b));
d=log10(b);
e=tan(d);
d=sqrt(d);
c=(acos(a))*(atan2(d,c));
}