#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(b,e))*(cos(e));
b=floor(c);
if(isless(c,c)){
d=sqrt(b);
b=log(c);
c=(exp(a))/(asin(e));
a=(fmin(a,b))/(log(b));
}
else{
c=(atan(d))/(fmax(c,e));
c=(acos(e))-(fdim(c,e));
e=ceil(b);
e=sqrt(b);
a=(fmin(b,b))-(atan2(a,d));
}
if(isgreaterequal(e,a)){
a=(cos(b))/(pow(d,d));
e=sin(d);
c=(fdim(d,c))*(asin(a));
a=fmax(a,a);
}
}