#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(e);
f=(cos(a))+(fmax(a,g));
b=atan2(c,h);
if(islessgreater(f,f)){
g=acos(g);
e=sin(f);
}
else{
b=fmax(c,g);
h=fdim(e,b);
}
e=tan(h);
a=atan2(b,h);
h=fmin(c,b);
e=log10(f);
}