#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmin(f,c);
b=(floor(b))*(fmin(f,e));
c=(fmin(e,c))*(log(f));
f=(atan2(c,d))/(sin(d));
a=atan(b);
if(isgreaterequal(d,e)){
e=(fdim(a,f))-(atan(b));
c=fmax(f,c);
c=(atan(f))-(exp(c));
}
if(islessequal(d,a)){
d=(cos(c))-(atan2(c,e));
a=pow(e,a);
a=(fmin(d,c))/(fmax(d,d));
b=pow(a,c);
}
else{
c=(pow(d,d))+(fmin(f,c));
c=(atan2(e,d))-(atan(e));
b=(sqrt(e))/(tan(b));
e=(acos(a))/(exp(b));
}
}