#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(a);
d=pow(b,a);
if(islessequal(a,c)){
a=(fdim(a,b))*(fmax(e,d));
a=(fmin(e,d))+(atan(d));
d=pow(d,e);
a=(sin(b))-(fdim(c,d));
}
if(islessgreater(b,a)){
b=(fdim(e,c))-(fdim(c,d));
c=(fdim(e,a))*(atan2(a,b));
e=pow(c,b);
e=ceil(b);
c=fmax(a,d);
}
else{
d=tan(e);
d=(acos(d))*(pow(e,e));
d=sin(e);
a=atan2(d,e);
a=(sqrt(a))/(fmin(d,b));
}
}