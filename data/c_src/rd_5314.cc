#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(e,c))+(ceil(c));
e=(fmin(e,c))/(pow(e,e));
e=(fmax(a,d))*(acos(e));
b=sqrt(d);
a=(atan(e))+(fmax(e,d));
b=log(d);
while(isgreaterequal(b,c)){
c=pow(a,e);
e=(fmin(c,a))/(pow(b,d));
e=exp(a);
}
}