#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(h,b))/(acos(c));
g=asin(b);
g=(fmax(c,a))*(sin(g));
b=(atan2(b,f))*(tan(d));
c=fmax(g,d);
if(islessequal(a,c)){
c=fmax(b,c);
d=sin(b);
d=atan2(d,b);
h=ceil(a);
}
}