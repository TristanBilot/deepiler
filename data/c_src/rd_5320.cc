#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=ceil(c);
a=ceil(d);
f=floor(g);
g=(fdim(g,d))*(fmin(g,g));
f=(fmax(a,a))*(fdim(b,a));
while(isgreaterequal(c,e)){
e=(cos(f))-(fmax(b,c));
c=(pow(f,f))*(atan(f));
b=ceil(f);
e=pow(g,d);
}
g=ceil(c);
d=(fmin(g,g))/(floor(b));
g=sqrt(d);
f=sin(c);
g=(exp(e))+(fdim(e,e));
}