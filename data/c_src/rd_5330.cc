#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(sin(a))+(cos(d));
a=fdim(d,e);
d=(fdim(e,e))/(fmax(b,b));
b=fmin(c,a);
c=(asin(e))*(atan2(b,a));
b=sin(a);
c=(fdim(a,b))-(atan2(c,c));
e=(fdim(b,c))/(exp(c));
e=(atan(d))-(atan2(d,b));
}