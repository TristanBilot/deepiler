#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(a))*(fdim(e,a));
a=pow(d,a);
b=exp(b);
d=(ceil(c))*(fmax(a,c));
b=(cos(b))*(atan2(c,c));
c=log10(e);
a=fmax(a,c);
if(isgreaterequal(b,a)){
d=fmin(a,d);
d=(fmax(e,c))/(cos(d));
}
}