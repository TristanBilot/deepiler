#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan(e);
f=atan2(a,d);
f=sin(d);
c=atan2(b,d);
d=(log(e))/(fmax(f,e));
g=log10(c);
g=fdim(d,b);
f=log10(g);
if(islessequal(e,e)){
d=(fdim(e,e))*(atan2(a,c));
a=log(b);
}
else{
c=(log(c))+(log10(g));
d=(sin(g))+(atan2(c,f));
d=fdim(c,a);
a=pow(f,f);
d=tan(e);
}
}