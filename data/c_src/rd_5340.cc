#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(a,c);
a=(log10(e))+(sin(f));
f=exp(c);
d=(sin(f))*(atan2(g,f));
b=(fmin(e,e))-(fmax(a,e));
while(islessgreater(f,f)){
d=pow(a,b);
g=(exp(e))*(tan(e));
f=floor(e);
e=fmin(b,f);
a=log(f);
}
while(islessgreater(g,b)){
g=(fdim(b,c))/(cos(b));
f=(atan2(b,d))*(fdim(g,b));
e=(pow(b,a))/(fdim(c,g));
c=(sqrt(e))-(asin(f));
f=asin(g);
}
}