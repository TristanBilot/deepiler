#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(acos(d))-(pow(b,c));
b=log10(e);
e=fmax(d,b);
c=(pow(a,e))+(acos(e));
a=(fmin(a,c))*(exp(e));
c=(acos(a))/(fmin(c,a));
d=(fmin(c,a))*(floor(a));
c=fmin(d,e);
if(islessgreater(c,c)){
a=(fmin(e,d))/(exp(b));
b=(fmax(d,a))-(cos(a));
a=(pow(a,e))-(fdim(c,c));
}
else{
b=(sin(d))*(fmin(b,d));
a=(fdim(e,d))/(fmax(d,b));
d=pow(c,a);
e=(log(a))/(fmax(a,c));
c=(fdim(b,d))-(sin(b));
}
}