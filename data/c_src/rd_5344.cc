#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=tan(d);
f=atan2(d,e);
b=(fdim(e,e))*(pow(d,e));
f=asin(d);
d=pow(e,f);
if(islessgreater(c,a)){
f=fdim(b,e);
a=(log(e))/(fdim(e,c));
a=(ceil(e))/(fmax(e,c));
b=(tan(a))/(log(c));
a=(pow(d,a))+(fmin(b,c));
}
else{
b=(log10(f))+(pow(f,e));
d=(ceil(f))/(log(e));
b=tan(e);
f=(sin(f))+(sqrt(b));
f=(fdim(f,e))*(ceil(e));
}
while(islessequal(c,f)){
b=(fmax(c,c))+(atan(a));
d=(exp(c))*(fmin(c,d));
}
}