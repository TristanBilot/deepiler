#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(e,h))/(exp(b));
g=(fmax(h,h))/(pow(b,a));
c=(ceil(e))+(fdim(a,f));
d=(log10(d))*(fmin(b,c));
while(islessequal(d,b)){
b=(exp(g))+(pow(a,f));
h=(pow(g,a))+(cos(c));
a=atan(a);
d=atan2(f,d);
}
while(islessequal(g,a)){
f=pow(h,e);
e=fmax(g,e);
g=(fdim(a,a))/(exp(c));
}
}