#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(tan(a))+(sin(b));
a=(sqrt(d))/(exp(a));
b=(atan(c))*(fmax(d,b));
c=fdim(d,e);
d=floor(e);
d=(asin(d))-(pow(c,e));
a=atan2(e,b);
e=atan2(c,d);
if(islessequal(d,d)){
a=(fmax(d,e))*(cos(b));
e=(atan2(b,c))+(fmin(b,b));
d=acos(d);
c=floor(d);
}
}