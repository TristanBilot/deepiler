#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(h,a);
b=(fmin(f,e))/(fmax(a,g));
a=(fdim(g,a))*(atan2(f,h));
h=(floor(a))*(fdim(d,g));
while(isgreaterequal(a,g)){
e=fdim(d,g);
g=(asin(d))/(fmax(e,h));
b=(asin(d))*(ceil(e));
}
while(islessequal(e,f)){
d=pow(b,h);
b=(cos(h))*(fmax(a,e));
f=(tan(b))/(atan2(b,f));
d=(log(a))-(log10(e));
}
}