#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmin(f,e))+(fmin(c,d));
b=fmax(e,h);
h=atan2(e,a);
while(islessequal(c,e)){
a=atan(f);
e=atan2(c,b);
}
while(islessgreater(b,c)){
d=(fmax(h,a))*(atan2(d,g));
b=log(h);
e=atan2(d,b);
a=fmax(g,a);
}
}