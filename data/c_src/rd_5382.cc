#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(e,b);
a=fmin(c,a);
a=fmax(b,d);
c=sin(c);
c=sqrt(f);
f=tan(c);
g=fdim(g,g);
f=exp(c);
a=(fmax(f,a))+(fmax(f,d));
b=fmin(d,c);
b=(atan(f))*(pow(c,f));
b=fdim(f,g);
b=fmin(a,f);
}