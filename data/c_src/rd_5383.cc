#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(e,b);
e=(log(a))/(atan2(a,e));
if(islessgreater(e,d)){
e=(fdim(c,e))-(log10(c));
c=(log10(c))/(atan(e));
}
else{
b=sqrt(d);
e=(fmax(c,b))+(fmin(a,c));
d=(fdim(a,d))/(tan(a));
e=(exp(a))-(sqrt(e));
e=(cos(c))+(fmin(e,d));
}
if(isgreaterequal(a,d)){
a=asin(d);
a=(fmax(e,b))+(fmax(a,a));
b=(pow(d,a))+(pow(b,d));
}
else{
a=cos(b);
a=fmax(e,e);
}
}