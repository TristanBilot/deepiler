#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(d,e);
c=atan(a);
while(islessequal(c,a)){
e=fdim(e,b);
d=(fmin(c,d))+(sqrt(b));
c=atan(b);
b=(fdim(d,e))/(fdim(b,a));
}
if(isless(c,c)){
c=asin(d);
d=(fdim(e,a))*(fmax(b,d));
}
}