#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(log(b))+(fmin(a,c));
b=floor(e);
f=(fdim(b,d))/(ceil(d));
b=(acos(f))*(pow(d,e));
d=fmax(a,f);
if(islessequal(c,b)){
a=(fmax(d,d))/(floor(c));
d=(sqrt(b))-(pow(b,d));
c=(fmax(e,d))/(sin(c));
e=log10(b);
b=(fdim(d,d))-(pow(a,f));
}
b=(fmin(b,b))-(exp(a));
f=fmin(a,b);
}