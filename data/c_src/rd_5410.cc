#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(c))*(cos(a));
e=asin(b);
d=(fdim(b,e))-(exp(a));
a=(pow(d,d))+(fmin(c,c));
if(isless(e,b)){
e=sin(a);
b=(asin(e))-(acos(a));
}
while(isless(d,d)){
a=(fmax(b,c))-(fmin(d,b));
c=(log10(e))+(atan2(c,d));
d=log10(e);
e=(asin(a))/(pow(a,b));
e=pow(a,d);
}
}