#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fdim(c,a);
f=(pow(d,a))-(log(e));
while(islessequal(d,a)){
b=fmax(f,a);
a=(sqrt(c))+(tan(c));
}
while(islessequal(a,g)){
g=(floor(a))+(log(f));
g=cos(d);
}
}