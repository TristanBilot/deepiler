#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(a,f);
g=atan2(h,d);
b=log10(a);
b=(exp(g))-(atan2(f,d));
c=(fmax(e,a))-(tan(c));
d=(fmin(c,g))*(atan2(h,h));
g=(tan(e))/(fmax(c,f));
}