#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fdim(d,c))*(fmax(b,c));
d=(tan(d))-(fmin(d,c));
a=(sqrt(a))/(atan(d));
c=(fmin(d,e))+(exp(b));
b=(fdim(d,f))*(exp(a));
if(islessequal(a,f)){
a=fmax(c,e);
d=acos(f);
}
else{
d=(sin(b))/(pow(d,c));
d=(log10(b))+(asin(b));
e=(sin(d))/(acos(b));
b=(asin(a))*(sin(d));
e=(tan(c))*(atan2(e,e));
}
while(islessequal(c,c)){
d=pow(b,a);
b=(log(d))+(atan2(b,a));
f=sqrt(a);
e=(acos(b))*(log(e));
}
}