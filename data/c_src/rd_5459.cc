#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(d,g))+(pow(e,c));
g=exp(f);
e=pow(c,e);
b=atan2(d,c);
if(isless(g,a)){
e=exp(e);
g=(sin(d))*(atan2(a,c));
f=(floor(e))-(fdim(e,g));
d=fmin(c,g);
d=atan(d);
}
a=floor(f);
b=floor(g);
c=ceil(b);
e=(fmax(c,c))*(log10(b));
}