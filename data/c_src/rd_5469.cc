#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=asin(d);
b=(fmin(g,a))+(pow(c,a));
if(isgreaterequal(g,d)){
g=fmax(c,g);
c=log10(f);
b=fmax(f,c);
f=cos(b);
}
else{
b=atan2(a,d);
a=(atan2(g,e))+(fmax(g,c));
e=(pow(d,d))-(tan(c));
c=log10(e);
b=(pow(g,e))-(sin(a));
}
while(isless(e,b)){
d=(fmin(b,a))+(ceil(e));
d=(fmax(e,g))*(fmax(c,d));
a=(fmin(e,a))/(sqrt(a));
g=(acos(b))*(cos(g));
}
}