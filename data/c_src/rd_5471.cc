#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(b,c);
a=log(e);
b=atan(a);
a=atan2(c,a);
d=(acos(c))-(atan2(c,c));
if(isless(d,e)){
a=(fmax(b,b))+(fmax(d,b));
c=(fdim(e,c))/(pow(a,b));
a=(log10(a))-(floor(e));
e=(pow(e,c))*(pow(c,a));
}
else{
a=atan(e);
a=fdim(c,e);
c=tan(d);
}
if(isless(e,c)){
b=fdim(d,e);
e=(log(a))*(fmax(b,b));
c=(floor(b))/(fmin(e,e));
a=fdim(c,a);
}
}