#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(d,a);
b=(exp(e))/(sin(b));
while(isless(c,e)){
c=exp(d);
a=asin(e);
b=(fmin(e,a))-(pow(d,a));
e=(pow(b,d))/(sqrt(b));
c=acos(d);
}
while(islessgreater(b,e)){
d=(log10(e))-(acos(c));
d=exp(b);
}
}