#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(ceil(e))*(tan(c));
h=fdim(f,h);
e=(log(d))-(sqrt(e));
f=ceil(h);
c=(fdim(h,h))+(atan2(c,a));
a=(pow(c,d))/(exp(d));
e=fmin(a,f);
c=(log(e))/(pow(d,a));
while(isless(g,c)){
c=ceil(g);
a=fmax(h,f);
f=(fmax(f,c))/(sqrt(f));
e=sqrt(g);
}
}