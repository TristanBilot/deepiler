#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=cos(d);
e=(atan2(d,b))/(sqrt(a));
d=fmin(e,e);
while(isgreaterequal(a,d)){
c=exp(b);
d=(pow(c,d))*(cos(c));
e=atan2(b,d);
d=atan2(e,c);
}
d=fmax(e,e);
c=(cos(e))/(sin(e));
a=(fdim(b,d))/(fdim(d,c));
c=(atan(a))+(tan(d));
}