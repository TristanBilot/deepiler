#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmax(a,f);
g=cos(g);
g=(pow(g,d))+(log10(e));
c=fmax(b,e);
b=floor(c);
if(isless(c,g)){
b=log10(b);
e=fmin(d,f);
a=(sin(a))+(fmin(d,f));
d=exp(d);
b=sin(a);
}
while(islessgreater(a,g)){
g=fmax(f,f);
a=(floor(e))/(pow(e,c));
g=pow(b,f);
g=atan2(c,e);
}
}