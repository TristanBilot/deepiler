#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fdim(g,a);
d=ceil(f);
c=pow(e,a);
d=(atan2(a,b))/(atan(c));
if(isless(e,d)){
c=atan2(a,e);
g=fmax(e,g);
}
else{
a=ceil(b);
a=(pow(d,d))+(atan(b));
f=(atan2(a,f))+(fdim(f,d));
a=(cos(g))/(acos(f));
}
if(isgreaterequal(f,g)){
d=(log10(a))*(fmax(f,a));
g=exp(a);
b=(pow(b,g))*(fmax(e,g));
}
}