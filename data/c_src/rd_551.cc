#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(acos(c))-(acos(g));
h=tan(c);
h=exp(b);
a=(exp(e))-(sin(b));
g=(floor(e))/(cos(b));
if(isless(a,c)){
a=cos(b);
a=(pow(d,b))+(fmin(g,a));
a=(sqrt(e))-(fmax(g,a));
f=fmin(e,a);
e=atan2(b,b);
}
while(islessgreater(h,a)){
e=atan2(h,g);
h=(fmax(d,f))-(exp(c));
f=(acos(h))-(atan2(e,d));
h=(fmax(f,e))*(asin(b));
}
}