#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(g,h);
g=log(d);
f=fmax(d,b);
b=(fdim(c,d))-(log10(b));
if(isgreaterequal(e,c)){
d=(ceil(e))/(tan(c));
f=cos(a);
}
else{
g=sqrt(c);
c=(pow(f,g))+(atan(h));
g=sin(f);
b=atan2(e,h);
}
while(islessgreater(f,d)){
h=(fmin(h,g))*(log10(d));
b=fmin(c,c);
h=exp(b);
c=(fmax(c,h))*(exp(h));
}
}