#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fmax(g,d);
g=(fmin(f,b))/(ceil(a));
f=pow(a,f);
b=fmax(d,b);
e=fdim(c,b);
b=(fmax(f,c))/(exp(d));
e=fmax(b,b);
a=exp(b);
if(isless(b,f)){
e=(pow(f,d))+(sin(b));
b=(sin(g))/(pow(g,d));
a=(acos(c))/(sqrt(b));
}
}