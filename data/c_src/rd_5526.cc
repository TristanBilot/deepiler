#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(d))*(atan2(d,e));
a=(log(a))+(cos(d));
while(isgreaterequal(a,e)){
c=(atan2(d,d))-(fmin(c,e));
c=fmin(a,e);
c=(log(b))+(fmin(a,c));
b=fmax(d,a);
d=exp(c);
}
if(islessgreater(b,e)){
e=(pow(d,e))*(atan2(b,a));
c=fmax(a,b);
}
}