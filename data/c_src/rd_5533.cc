#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(e,e);
b=(fmin(f,c))/(fmin(e,c));
f=(fmax(d,e))+(fmin(b,e));
e=fmax(b,b);
e=(atan2(d,a))/(fmax(f,e));
d=(sin(f))/(fdim(f,e));
a=(pow(b,f))/(sin(b));
e=atan(c);
b=ceil(b);
if(islessequal(d,f)){
b=pow(e,d);
d=(acos(c))/(log(f));
a=fdim(f,a);
b=(ceil(b))+(sin(b));
}
else{
e=log10(b);
c=(fdim(e,b))+(fdim(f,d));
}
}