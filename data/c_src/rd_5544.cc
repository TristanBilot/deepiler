#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=asin(a);
e=(pow(d,a))*(fmin(a,f));
e=(sqrt(c))/(fmin(b,b));
c=atan2(d,g);
g=(fmax(f,b))/(fmin(d,c));
while(isless(g,g)){
e=(fdim(e,a))/(log(h));
e=fdim(g,f);
f=tan(e);
d=acos(c);
c=pow(f,g);
}
if(islessequal(d,d)){
a=(fmin(a,g))*(asin(c));
f=fdim(a,h);
g=fmax(e,h);
e=tan(c);
a=acos(g);
}
else{
h=(pow(h,d))/(pow(c,c));
g=(atan2(h,h))/(acos(h));
e=atan(c);
e=cos(a);
}
}