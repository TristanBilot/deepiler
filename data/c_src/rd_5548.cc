#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmin(e,c);
d=(log(a))*(fmin(f,b));
b=(exp(b))*(atan(c));
f=(ceil(a))*(fmax(d,c));
a=fmax(a,b);
e=(tan(b))*(log10(c));
d=fdim(b,d);
e=pow(d,f);
d=(atan2(f,e))/(atan2(c,f));
if(islessgreater(d,d)){
c=(ceil(f))*(fmin(c,f));
e=(ceil(a))-(cos(a));
}
else{
a=sqrt(f);
c=fdim(f,c);
e=(fmin(f,c))-(fmax(c,b));
e=(atan2(b,a))-(asin(b));
}
}