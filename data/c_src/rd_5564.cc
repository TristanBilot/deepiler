#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(f,f))+(sqrt(f));
h=fdim(g,g);
e=(atan2(a,g))+(log(h));
b=fmax(e,g);
g=(fdim(h,f))*(atan(a));
c=pow(a,d);
f=log(g);
e=fmin(b,a);
if(islessgreater(c,d)){
g=pow(b,g);
h=(cos(b))-(log(a));
f=fdim(d,g);
a=(sin(d))/(atan2(c,c));
e=(sin(b))+(atan2(d,f));
}
else{
e=fmax(a,e);
a=(fdim(c,g))/(fdim(h,b));
h=fmax(b,d);
b=fmax(g,g);
}
}