#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(b,b))/(fdim(b,e));
c=asin(c);
c=atan2(c,c);
b=sqrt(b);
d=sqrt(a);
if(isgreaterequal(c,c)){
d=fmax(b,e);
c=fmin(a,b);
}
else{
c=log10(c);
c=(atan2(e,d))/(ceil(a));
a=(fdim(e,c))/(pow(b,c));
}
if(isgreaterequal(e,b)){
b=(atan2(b,b))*(ceil(c));
b=fmax(b,e);
d=fmin(c,a);
}
else{
a=(sin(a))*(exp(d));
e=(fmax(e,b))*(fmax(d,b));
c=cos(a);
b=atan2(b,b);
e=sqrt(b);
}
}