#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(atan(c))+(sin(b));
b=fmin(d,d);
b=(pow(c,c))/(atan2(a,b));
c=(fdim(c,b))+(asin(d));
c=(sqrt(c))*(asin(b));
e=ceil(e);
b=(sin(e))*(log(d));
e=(fmax(b,d))-(fdim(d,b));
c=log(b);
c=(asin(e))+(atan2(e,c));
d=ceil(b);
d=fdim(d,a);
}