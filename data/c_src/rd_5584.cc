#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(c))+(pow(d,c));
e=(ceil(c))+(fmin(d,c));
a=exp(e);
c=fdim(c,d);
c=pow(c,a);
if(islessgreater(e,a)){
e=(atan2(e,a))/(atan(d));
b=cos(a);
c=(tan(c))*(atan2(b,e));
d=(tan(e))+(pow(d,c));
b=cos(b);
}
else{
d=fmin(c,c);
d=(sin(d))*(pow(a,c));
b=fdim(c,c);
}
if(isless(e,c)){
b=floor(c);
a=(atan2(d,d))+(acos(c));
a=(atan(e))/(fmax(e,e));
b=(pow(b,a))-(fdim(c,e));
d=fdim(c,d);
}
else{
b=(pow(d,d))-(exp(b));
b=pow(a,e);
c=(atan2(a,e))/(cos(b));
}
}