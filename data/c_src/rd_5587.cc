#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan(b);
f=(log(f))*(fmax(c,c));
f=pow(c,b);
d=(cos(e))+(asin(f));
e=exp(d);
while(isgreaterequal(d,a)){
e=atan2(c,b);
f=(pow(a,d))*(cos(c));
c=(fmin(d,e))*(fmin(d,e));
f=(fdim(a,a))/(pow(b,f));
}
while(islessequal(e,d)){
b=(fdim(f,a))/(atan2(b,a));
d=acos(e);
d=(atan2(d,a))-(atan2(f,e));
b=atan2(d,f);
c=(fmin(c,b))/(cos(b));
}
}