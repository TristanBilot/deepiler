#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log10(f);
a=(log(f))*(fmin(a,b));
a=(fmax(d,e))/(fdim(d,a));
f=log10(c);
while(isless(c,c)){
f=(cos(b))-(cos(f));
d=tan(f);
}
if(isgreaterequal(d,c)){
b=fmax(b,e);
f=(pow(c,a))*(ceil(f));
e=ceil(a);
}
else{
e=tan(d);
a=(sin(e))-(asin(d));
}
}