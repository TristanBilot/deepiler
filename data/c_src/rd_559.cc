#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=sqrt(d);
d=(log(c))/(pow(f,d));
f=log10(f);
d=fmin(f,a);
b=(fmin(c,b))*(fmax(b,b));
f=(atan2(d,e))-(log10(a));
e=log(a);
while(islessequal(c,d)){
b=fmax(b,e);
f=acos(d);
}
}