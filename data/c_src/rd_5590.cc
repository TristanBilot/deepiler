#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=tan(b);
b=(ceil(e))*(fmax(a,d));
f=fdim(f,a);
a=ceil(c);
while(isgreaterequal(b,d)){
d=tan(b);
e=(atan2(b,f))/(sqrt(f));
d=(fmax(b,e))/(exp(b));
f=(atan(a))-(pow(c,c));
c=(atan(b))/(log10(c));
}
}