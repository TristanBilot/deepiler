#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(f,c);
f=fmax(d,c);
e=log(g);
c=(atan2(h,a))+(floor(g));
f=floor(a);
d=(cos(e))/(log10(a));
c=(pow(d,g))/(fdim(e,a));
while(islessgreater(d,c)){
a=(floor(f))+(fdim(f,a));
g=(fmin(g,h))-(fdim(a,e));
a=atan2(c,e);
a=fmax(h,a);
}
}