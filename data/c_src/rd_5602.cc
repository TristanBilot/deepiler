#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fdim(c,f);
d=(tan(d))-(floor(g));
g=(tan(f))-(fmax(b,a));
c=fmax(e,c);
while(islessequal(e,b)){
d=fmax(d,b);
c=sin(f);
a=(fdim(a,b))/(log10(c));
b=(pow(d,d))/(pow(c,d));
}
if(islessequal(a,g)){
f=sqrt(f);
f=fdim(g,c);
b=(fdim(c,b))*(ceil(b));
}
else{
b=ceil(f);
d=(fdim(c,g))+(pow(b,f));
g=tan(b);
a=(log10(e))-(acos(f));
}
}