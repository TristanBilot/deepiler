#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(ceil(c))*(fmax(e,c));
f=(fdim(d,a))/(atan2(c,b));
a=log(b);
f=(fmax(b,e))*(fmin(e,f));
e=(tan(a))/(atan(d));
if(islessgreater(f,e)){
a=fmax(e,f);
d=(exp(a))/(fmax(c,a));
b=(pow(a,a))-(sin(c));
c=fmin(d,a);
a=(sin(e))+(pow(c,b));
}
else{
b=(floor(c))-(cos(c));
e=log(d);
}
a=cos(a);
e=acos(b);
d=fmin(b,c);
}