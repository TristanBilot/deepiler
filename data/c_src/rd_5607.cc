#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(c);
e=(fmax(c,b))/(sin(a));
b=ceil(b);
d=fmax(a,a);
if(islessgreater(d,a)){
e=(exp(e))*(fmax(a,a));
b=atan(d);
a=exp(c);
e=fmin(e,c);
}
else{
e=log(e);
e=log10(b);
d=fdim(e,c);
e=(sin(b))+(fmax(e,c));
}
b=(atan(b))+(log(b));
d=(pow(a,c))*(log10(e));
a=acos(e);
}