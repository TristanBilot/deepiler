#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=log10(b);
a=(ceil(a))/(fmax(b,a));
d=(atan2(c,c))*(sqrt(f));
b=(atan(f))-(pow(b,d));
c=(pow(d,b))*(atan2(a,a));
a=(atan(e))-(atan2(a,b));
d=(pow(c,b))/(floor(f));
a=(fdim(b,b))/(sin(c));
while(isgreaterequal(a,d)){
f=tan(e);
b=fmax(a,a);
c=(pow(d,f))*(tan(e));
}
}