#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(e,c);
f=pow(f,f);
f=floor(g);
f=fdim(d,f);
f=(atan2(c,b))*(fmin(a,d));
if(isless(b,a)){
f=(fmax(a,g))-(atan2(b,d));
d=(fdim(g,d))*(floor(d));
}
d=(pow(d,g))-(sqrt(f));
a=(fdim(d,b))*(atan2(f,b));
b=fmin(b,g);
e=(atan2(f,f))*(acos(b));
}