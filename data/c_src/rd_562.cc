#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=exp(d);
b=fmin(a,c);
a=atan(a);
e=log10(g);
f=atan2(g,a);
while(islessgreater(e,d)){
c=(floor(d))-(fmax(b,g));
e=(log(g))+(log(g));
}
if(isless(f,c)){
f=(atan2(c,g))/(cos(g));
f=(fmin(g,b))-(fdim(a,a));
d=(fmin(c,f))*(fmax(g,a));
e=ceil(e);
a=cos(c);
}
else{
d=(fdim(b,g))*(fmin(a,g));
c=log10(g);
}
}