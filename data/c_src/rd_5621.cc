#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(a,a);
c=(fdim(a,c))+(fmax(b,d));
d=fdim(e,e);
if(islessgreater(e,c)){
a=fdim(a,a);
d=atan(a);
}
b=fdim(d,c);
a=pow(d,b);
b=(sin(d))-(fdim(a,e));
}