#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log(e))-(tan(e));
d=(atan2(c,e))-(fmin(c,c));
c=ceil(e);
d=fmax(d,b);
if(isgreaterequal(c,b)){
d=atan2(c,c);
b=(ceil(c))*(pow(a,b));
c=(fmin(b,c))/(log(b));
b=(ceil(e))+(log(d));
}
c=ceil(b);
d=(pow(b,d))+(fmax(b,a));
c=fmax(c,d);
e=tan(b);
}