#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmax(b,e))+(log(a));
d=fmin(b,c);
h=(pow(c,c))*(fmin(a,f));
b=(fmax(c,e))*(log10(a));
if(isless(c,a)){
b=(atan2(b,d))*(fdim(c,h));
b=(asin(c))+(atan2(d,f));
c=asin(g);
d=(sin(c))*(atan2(d,e));
}
else{
d=(sqrt(h))+(pow(g,d));
g=(fdim(g,f))+(cos(a));
b=fmin(a,a);
b=(acos(d))*(atan2(c,g));
c=(fdim(e,c))/(fmax(a,c));
}
if(islessequal(e,d)){
b=fmax(f,f);
c=pow(a,g);
}
else{
b=fmin(c,b);
b=(atan2(h,a))+(pow(h,c));
}
}