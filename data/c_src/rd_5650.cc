#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=ceil(c);
d=fmax(b,c);
e=acos(c);
while(islessgreater(c,d)){
d=(atan2(a,b))*(cos(c));
e=asin(b);
c=(exp(c))+(atan2(b,b));
a=atan2(a,c);
}
while(isless(e,b)){
a=(fmin(a,c))*(atan2(d,a));
b=(cos(b))-(fmin(b,b));
}
}