#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=acos(c);
b=(atan2(e,e))/(log(a));
if(isgreaterequal(c,e)){
d=(floor(d))*(floor(f));
b=cos(c);
}
else{
a=atan(b);
b=(fmin(d,e))/(log10(f));
}
e=(pow(b,d))+(log10(e));
b=fmax(c,e);
a=fmax(c,f);
c=pow(c,f);
}