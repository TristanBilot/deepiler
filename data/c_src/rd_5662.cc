#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(floor(f))*(sin(f));
f=(pow(d,e))/(atan2(b,f));
a=(fmin(e,f))-(sqrt(c));
e=fdim(b,f);
while(isless(e,d)){
e=atan2(c,d);
b=(ceil(a))+(exp(b));
d=(pow(c,e))*(log(c));
d=asin(c);
}
if(islessgreater(e,e)){
e=(fmin(e,c))*(atan2(c,e));
f=(exp(f))-(fdim(b,f));
e=sin(e);
c=tan(b);
}
else{
a=(exp(e))/(acos(b));
d=(ceil(e))*(log10(d));
}
}