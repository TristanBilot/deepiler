#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(e))-(fmax(b,e));
c=log(c);
b=atan2(a,e);
c=atan(f);
d=(fmax(b,c))/(floor(c));
if(isgreaterequal(b,b)){
b=(acos(d))-(atan2(f,f));
c=fmax(e,c);
a=(fmax(c,a))-(atan2(e,d));
e=(fmax(e,f))*(floor(b));
}
d=exp(c);
c=(fdim(b,c))+(floor(d));
}