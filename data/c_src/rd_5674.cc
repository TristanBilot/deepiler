#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(g,a);
e=(fmin(b,b))-(atan2(f,g));
f=(atan2(b,f))+(fdim(e,e));
b=(log(g))*(pow(a,c));
a=(atan2(b,c))/(atan(c));
while(islessgreater(f,b)){
e=tan(f);
b=(fmax(c,d))+(fdim(e,d));
a=(pow(a,g))-(fdim(e,d));
}
a=fmin(c,a);
c=(fmax(e,f))+(fdim(c,b));
d=fdim(a,f);
f=(fmin(c,a))+(tan(b));
}