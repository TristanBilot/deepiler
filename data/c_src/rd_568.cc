#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(a,g))+(ceil(b));
e=exp(f);
d=log10(f);
b=(atan(a))+(exp(g));
b=fmax(a,a);
if(isless(d,c)){
f=fdim(b,d);
h=(fdim(h,h))+(log10(h));
c=fmax(a,g);
f=fmax(d,d);
c=atan2(b,c);
}
e=sqrt(g);
f=pow(h,h);
e=(atan2(f,g))*(fmax(e,b));
f=(floor(h))/(fdim(d,b));
}