#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(ceil(c))+(fmin(c,a));
e=pow(e,d);
a=(log10(f))-(cos(a));
a=(asin(d))-(pow(c,d));
b=(tan(a))+(fmax(a,e));
c=(fdim(e,b))*(atan2(b,b));
d=atan2(d,a);
d=(exp(c))/(log10(c));
e=log(d);
b=(atan2(a,d))/(atan2(d,c));
}