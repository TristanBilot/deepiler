#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sqrt(a))/(fmin(e,h));
h=(cos(b))+(fmax(c,g));
f=(atan2(d,f))*(sqrt(d));
if(isless(b,h)){
h=(atan2(d,f))*(floor(b));
g=(tan(b))+(log10(g));
g=fdim(c,b);
}
else{
e=fmax(g,e);
b=fdim(d,a);
b=(fmin(a,g))/(pow(g,f));
}
if(isgreaterequal(e,d)){
b=fdim(h,c);
e=fdim(b,h);
d=(tan(f))+(fmax(b,f));
}
else{
d=tan(h);
h=asin(c);
h=(tan(h))-(sqrt(a));
d=(floor(b))*(log10(e));
}
}