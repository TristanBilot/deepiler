#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(e);
d=pow(b,c);
d=acos(a);
d=(fdim(c,c))-(log(a));
b=log10(d);
if(islessgreater(c,e)){
b=fmin(c,e);
c=asin(b);
c=(log10(d))/(fdim(e,e));
a=fmax(e,a);
}
}