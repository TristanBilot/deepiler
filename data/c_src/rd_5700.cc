#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=log(e);
f=fmax(g,d);
while(islessgreater(c,g)){
b=cos(e);
e=(floor(a))*(fdim(b,g));
d=atan2(a,f);
}
while(islessgreater(f,d)){
e=(fmax(d,c))*(atan2(b,b));
e=tan(b);
}
}