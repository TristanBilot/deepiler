#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(e,c))-(atan2(e,e));
b=(fmin(e,e))*(log10(e));
c=atan2(b,c);
a=fmax(f,c);
b=(fmin(f,d))-(fdim(c,a));
d=(atan(e))/(atan(e));
e=(fmax(e,c))*(tan(e));
e=ceil(b);
c=(cos(c))+(sin(e));
d=floor(c);
f=fdim(d,d);
b=(sin(a))*(fdim(d,b));
e=floor(b);
}