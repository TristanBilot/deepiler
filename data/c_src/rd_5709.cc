#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fdim(a,b);
d=(log(d))+(sin(a));
d=fdim(c,e);
a=(atan2(a,c))+(fmin(d,e));
a=(floor(d))/(atan(b));
while(isless(c,b)){
d=asin(b);
c=pow(f,c);
e=(fdim(f,b))*(exp(a));
f=(fdim(f,c))-(sqrt(f));
b=(atan2(d,a))*(log10(f));
}
}