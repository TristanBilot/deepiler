#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(a);
e=(exp(d))-(asin(a));
if(isgreaterequal(c,c)){
d=(fmin(d,b))*(fmin(e,a));
d=(fmin(b,c))/(fdim(d,a));
d=(exp(d))-(fmax(b,d));
a=cos(a);
}
while(isgreaterequal(a,a)){
a=(ceil(d))/(tan(b));
e=(log10(a))*(acos(c));
e=fmax(d,c);
}
}