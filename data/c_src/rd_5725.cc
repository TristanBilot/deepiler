#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,c))-(floor(c));
e=asin(c);
a=atan2(e,d);
e=log(e);
e=ceil(d);
a=(fmax(e,d))*(exp(a));
e=(floor(c))+(exp(a));
if(islessgreater(e,d)){
a=exp(b);
d=(atan(c))/(floor(b));
}
else{
b=(fdim(d,a))/(atan2(b,b));
c=(fmax(c,c))/(pow(e,e));
a=(fmin(e,a))-(ceil(a));
b=asin(b);
}
}