#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(b,a))/(fmin(g,g));
c=(fmax(d,a))-(sin(g));
g=(log10(a))*(acos(e));
g=sqrt(g);
while(islessgreater(d,g)){
c=(fmin(c,b))+(pow(f,c));
a=(sin(c))*(sqrt(d));
c=(fmin(c,a))-(log(b));
a=log(e);
}
}