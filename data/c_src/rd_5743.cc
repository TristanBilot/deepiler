#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(tan(g))+(log(b));
b=ceil(a);
e=(ceil(d))*(fmin(a,c));
a=sqrt(a);
if(islessequal(b,c)){
c=(atan2(f,g))/(pow(e,h));
d=(asin(c))/(cos(a));
}
while(islessequal(e,d)){
h=floor(f);
c=log10(h);
g=(fmax(h,b))*(fdim(e,d));
e=floor(h);
a=fmax(e,d);
}
}