#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(e);
b=sqrt(e);
h=(tan(a))*(fmin(g,f));
if(isgreaterequal(f,a)){
e=(log10(f))*(log10(b));
d=fmax(f,a);
c=(fmax(f,g))*(fdim(d,b));
f=fmin(e,f);
}
else{
a=asin(e);
b=(fmin(c,d))-(cos(g));
f=fmin(c,h);
g=log(d);
}
b=(fdim(d,f))*(fmax(g,c));
g=fdim(d,d);
}