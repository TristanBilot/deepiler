#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(b,b))-(pow(f,b));
e=pow(g,h);
c=(atan2(f,a))*(tan(f));
e=(floor(c))+(asin(f));
h=log(c);
while(isgreaterequal(b,d)){
d=(fmax(d,c))-(fdim(h,b));
c=fmin(f,a);
f=sin(h);
}
if(islessgreater(g,g)){
c=(acos(a))*(pow(c,e));
b=sqrt(b);
c=(fdim(c,b))-(fmin(c,h));
g=floor(d);
c=(atan(h))/(tan(c));
}
else{
f=(cos(a))+(exp(a));
g=fmax(d,g);
g=tan(f);
b=(pow(b,e))+(fdim(h,h));
}
}