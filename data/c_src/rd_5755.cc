#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(h);
d=(pow(c,c))*(cos(h));
while(isless(a,a)){
d=(atan2(d,e))/(tan(f));
f=sqrt(d);
e=fmax(g,e);
b=fmax(f,h);
e=fmin(c,a);
}
while(isless(b,h)){
c=atan2(e,d);
a=cos(f);
g=pow(f,b);
h=(pow(a,f))+(atan2(c,c));
}
}