#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(b,d))-(sin(b));
f=(pow(b,a))+(cos(a));
f=(fmax(g,b))+(exp(d));
while(islessequal(f,d)){
e=fmin(d,f);
g=fmin(e,d);
f=(log(d))-(fdim(e,e));
g=(asin(d))*(acos(g));
c=fmax(f,f);
}
c=(atan2(d,d))-(sin(g));
e=cos(f);
a=(pow(c,g))+(floor(c));
g=sqrt(g);
b=(atan(c))-(cos(b));
}