#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=exp(f);
d=asin(a);
b=ceil(a);
d=(sin(f))*(atan2(f,d));
while(isless(e,c)){
e=cos(f);
d=atan2(a,d);
}
if(islessgreater(e,b)){
f=(log10(a))*(cos(d));
f=floor(e);
a=(fdim(b,e))*(cos(f));
a=acos(e);
d=(log(b))*(fmax(d,e));
}
else{
e=cos(f);
b=(atan(a))/(sqrt(f));
f=pow(a,d);
}
}