#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fdim(a,d))+(fmax(a,e));
c=(fmax(a,e))+(fdim(d,a));
e=(log(b))*(fdim(a,c));
e=(floor(c))+(pow(e,f));
b=fmax(a,f);
f=(atan2(b,a))+(ceil(b));
while(isgreaterequal(b,e)){
e=sin(a);
d=tan(b);
e=(fdim(c,f))/(atan2(d,a));
e=fdim(a,c);
d=floor(a);
}
}