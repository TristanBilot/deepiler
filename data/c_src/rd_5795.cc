#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(d,d))*(log(b));
c=(acos(b))/(tan(b));
a=fmax(e,a);
while(isgreaterequal(d,b)){
f=fmin(e,a);
d=atan2(c,f);
d=fmax(a,c);
b=(fmax(d,e))/(exp(f));
}
c=(fdim(f,b))*(fmin(a,d));
b=fmin(d,f);
d=(asin(b))/(atan2(b,a));
b=pow(d,f);
b=(pow(d,d))-(sqrt(a));
}