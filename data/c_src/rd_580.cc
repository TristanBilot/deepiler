#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan2(d,a))/(log(b));
d=(fmin(e,e))/(fmin(a,e));
a=pow(b,c);
c=asin(a);
d=fdim(d,b);
while(isless(b,a)){
e=(ceil(d))/(fmin(a,c));
b=(atan2(b,d))/(pow(d,b));
a=pow(e,a);
c=(atan2(a,e))/(acos(e));
e=pow(c,d);
}
if(islessgreater(d,c)){
d=atan(d);
e=(atan2(c,d))/(atan2(f,c));
a=sqrt(d);
c=(fdim(b,b))-(sqrt(c));
d=fmax(a,c);
}
else{
c=pow(c,c);
f=atan(f);
}
}