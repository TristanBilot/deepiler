#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(b,a);
a=(ceil(e))+(fmax(c,b));
b=(sin(e))/(pow(b,e));
while(isgreaterequal(a,b)){
a=(fmin(a,a))*(pow(d,b));
b=fdim(b,e);
d=(pow(d,e))*(pow(a,d));
}
e=sqrt(e);
e=(fmin(a,a))+(cos(a));
}