#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(exp(d))/(fmax(a,a));
c=(fmin(d,b))*(atan2(c,c));
b=(sqrt(a))-(fdim(c,e));
if(isgreaterequal(a,b)){
a=(fdim(e,a))-(sin(a));
c=(fmax(e,e))/(pow(a,a));
d=(pow(a,d))/(tan(e));
}
else{
a=asin(e);
c=(ceil(a))/(tan(d));
c=fmax(c,c);
b=(fmax(a,d))/(atan(e));
}
if(isgreaterequal(d,a)){
d=(fmax(d,d))-(cos(d));
a=(log10(d))+(fmin(b,e));
a=(ceil(e))-(log10(c));
e=fmin(b,a);
}
else{
b=(log(e))*(log(c));
c=log(a);
c=(fmax(e,a))-(fmax(e,c));
}
}