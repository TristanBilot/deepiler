#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,a);
g=floor(e);
g=fmin(d,h);
c=(fdim(b,d))*(fmin(f,c));
h=(sin(b))+(floor(f));
g=(fmax(b,c))*(fmin(a,d));
b=ceil(c);
g=fdim(e,b);
d=(fmax(c,e))/(cos(d));
c=(ceil(h))-(fdim(h,a));
}