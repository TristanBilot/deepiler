#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(f,g))+(sqrt(e));
e=log10(f);
f=fdim(b,f);
f=acos(b);
c=fmax(e,e);
if(isless(g,h)){
f=fmax(a,a);
c=(fmax(c,h))*(pow(g,a));
b=(atan2(b,c))+(pow(e,b));
b=log(c);
}
g=(acos(e))/(log10(d));
h=log10(e);
}