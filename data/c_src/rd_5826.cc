#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(d))*(pow(f,b));
c=(log10(f))+(atan2(a,f));
if(isgreaterequal(b,e)){
f=sin(e);
f=(fmax(f,c))/(fmin(a,b));
e=fmin(f,d);
}
else{
c=(acos(a))*(fmax(d,f));
d=ceil(a);
d=(fmax(c,d))*(fdim(f,e));
}
if(isgreaterequal(c,e)){
f=asin(d);
c=atan(c);
c=(acos(a))+(asin(f));
f=(sqrt(f))-(acos(c));
}
}