#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(c,c))*(sin(a));
b=pow(e,d);
e=(ceil(c))/(atan2(a,b));
c=fmax(a,b);
while(isgreaterequal(e,a)){
d=log(c);
b=sqrt(e);
f=(exp(f))-(log(f));
a=fmin(b,c);
}
while(isless(d,f)){
d=log(a);
c=fmin(c,b);
c=exp(c);
}
}