#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan(e))*(sin(b));
a=(fmax(d,b))-(pow(b,e));
e=fmin(d,a);
a=(atan2(b,b))-(fmax(d,c));
a=(fmax(e,c))-(fdim(e,b));
c=fdim(a,d);
a=(fmin(b,b))*(fmax(a,d));
d=(sin(b))*(asin(d));
if(isless(a,b)){
d=log10(b);
c=(asin(b))-(log(c));
}
}