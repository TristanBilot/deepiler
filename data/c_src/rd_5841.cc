#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(exp(a))*(asin(a));
a=fmax(e,b);
c=fdim(g,g);
d=(sin(e))+(pow(c,c));
d=fdim(g,c);
e=fmax(b,e);
if(islessequal(c,b)){
f=(floor(h))-(floor(g));
e=(acos(h))-(tan(f));
c=exp(a);
b=(ceil(e))*(fdim(b,g));
e=(pow(b,e))*(log10(f));
}
}