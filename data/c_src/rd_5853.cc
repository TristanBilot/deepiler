#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(a,b))*(exp(a));
a=atan2(b,a);
c=atan(a);
if(islessequal(a,c)){
b=atan2(e,d);
e=floor(b);
}
e=(tan(c))-(log10(b));
a=sin(e);
b=(ceil(e))*(pow(b,d));
}