#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(c);
b=(log(b))+(fdim(b,d));
e=floor(d);
a=fdim(d,e);
a=fmax(b,d);
while(islessequal(c,e)){
e=sin(c);
b=(fdim(b,c))+(exp(c));
b=(floor(d))*(fmax(d,e));
a=atan2(c,e);
}
while(isless(d,a)){
e=atan2(a,e);
c=(sin(a))+(fmax(d,a));
d=sqrt(a);
c=ceil(b);
a=fmax(a,b);
}
}