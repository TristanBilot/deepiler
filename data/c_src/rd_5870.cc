#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=floor(b);
a=(atan2(e,b))-(pow(f,b));
a=(fmin(c,c))*(pow(d,d));
f=sin(c);
f=pow(c,f);
a=(log10(f))/(acos(f));
c=(pow(f,d))/(asin(a));
f=(atan2(c,f))*(atan(f));
d=tan(c);
a=(fmin(a,d))+(atan2(b,e));
}