#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(ceil(f))+(atan2(a,e));
b=atan2(a,g);
c=(log10(e))+(fdim(f,g));
e=log10(b);
b=asin(a);
e=fdim(b,d);
a=(pow(c,d))+(exp(a));
f=(atan(a))*(atan2(c,a));
g=exp(d);
if(isless(b,e)){
g=fdim(a,a);
d=(atan2(d,a))/(sqrt(c));
c=fdim(b,d);
f=(floor(b))-(asin(d));
a=(fmin(d,e))/(sin(d));
}
}