#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(c);
b=exp(c);
b=log(a);
b=(tan(c))*(fmax(d,c));
e=(sin(c))+(fmax(b,e));
c=log10(a);
b=(atan2(c,b))+(acos(a));
b=sqrt(e);
while(isless(a,b)){
d=atan2(c,e);
c=(fdim(d,c))-(sqrt(e));
c=(atan2(c,c))*(sqrt(b));
a=(atan2(d,d))-(sin(c));
}
}