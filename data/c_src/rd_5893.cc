#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=ceil(b);
c=(fdim(d,b))*(fmin(c,d));
b=(sin(e))+(fmax(b,a));
c=(atan2(a,d))/(fmin(c,a));
e=(tan(c))-(ceil(c));
d=(tan(b))-(pow(c,a));
e=(fdim(a,b))+(fmax(b,c));
d=fmax(b,c);
d=exp(a);
while(isgreaterequal(d,a)){
a=atan(b);
c=log(e);
}
}