#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmax(f,c))-(fdim(e,d));
e=(fmax(e,e))+(atan2(b,c));
c=(asin(a))+(atan2(d,b));
while(islessgreater(f,a)){
g=pow(e,g);
e=(fmax(g,a))/(atan(c));
}
while(islessgreater(d,a)){
d=atan(d);
c=fdim(g,c);
a=(log10(d))/(asin(f));
d=(fmax(b,g))+(sqrt(a));
}
}