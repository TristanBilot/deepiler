#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(e,g))/(log10(c));
e=(fdim(b,b))*(fmax(g,f));
a=(fdim(a,e))+(atan2(b,f));
c=fmax(f,b);
g=(fmin(f,c))*(fmax(d,d));
c=fdim(g,a);
f=(atan2(e,g))/(atan2(f,f));
d=(fdim(e,f))/(sqrt(a));
c=(pow(d,e))+(fmax(b,g));
b=(cos(d))*(atan2(e,b));
b=atan(a);
f=log(a);
}