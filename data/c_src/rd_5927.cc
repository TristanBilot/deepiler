#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(b,e))/(fdim(b,c));
c=(atan(e))/(tan(f));
d=(tan(e))-(sin(a));
e=(fdim(c,b))/(fmax(f,e));
b=exp(d);
while(isless(d,g)){
d=fmax(e,c);
c=fmin(f,g);
d=(pow(b,e))-(atan2(e,b));
f=(pow(b,e))-(atan2(d,c));
}
if(islessgreater(a,e)){
b=sqrt(e);
b=cos(e);
e=fmin(g,e);
g=atan2(c,f);
}
else{
c=fdim(f,f);
d=(atan2(g,c))*(sin(g));
a=(fmax(g,e))+(fmax(f,a));
e=(fmax(c,d))*(floor(b));
b=(pow(e,f))+(fmin(e,f));
}
}