#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=cos(d);
e=(atan(b))/(pow(a,c));
d=pow(c,e);
b=atan2(b,a);
d=(sqrt(a))*(log(e));
while(islessequal(d,a)){
b=(pow(e,b))+(sin(b));
c=pow(e,e);
c=(log10(b))/(fmin(d,a));
c=fdim(c,d);
a=fmin(d,c);
}
if(isgreaterequal(e,d)){
a=(fmax(c,a))-(cos(b));
b=(pow(e,e))/(fmin(d,e));
}
else{
a=asin(e);
c=floor(a);
a=acos(d);
e=cos(d);
c=acos(b);
}
}