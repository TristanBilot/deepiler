#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(d,d);
d=(pow(e,a))+(atan(b));
if(isless(f,a)){
b=cos(b);
d=atan(e);
a=(tan(g))+(fmax(a,a));
f=(fmax(c,d))*(fdim(h,a));
}
while(islessequal(c,h)){
b=(atan(f))-(tan(g));
a=pow(f,d);
}
}