#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(d,a);
d=(fdim(a,b))*(fmin(b,c));
d=(atan(a))-(fmin(c,b));
e=pow(b,c);
a=fdim(d,e);
if(isless(e,b)){
e=atan2(a,d);
d=(fmax(d,d))/(acos(a));
d=(atan(a))*(sqrt(d));
c=pow(c,d);
d=atan2(c,b);
}
else{
d=(asin(b))*(fmin(d,b));
a=sin(b);
b=(sqrt(c))-(atan2(d,b));
}
if(islessgreater(e,e)){
d=pow(d,e);
e=(fdim(b,e))+(fdim(a,b));
}
else{
c=(log10(d))*(log10(b));
a=(asin(e))-(asin(b));
}
}