#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(pow(e,d))-(fmin(g,b));
f=(atan2(a,h))*(asin(a));
a=fdim(b,f);
d=(tan(d))*(log10(c));
e=pow(d,h);
b=(ceil(f))-(cos(e));
c=(pow(c,b))-(ceil(c));
if(isless(h,b)){
b=(atan(e))*(fdim(g,e));
a=acos(e);
}
}