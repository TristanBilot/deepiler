#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(b,e);
a=fmin(a,e);
if(islessequal(e,e)){
c=fmin(e,d);
b=pow(a,b);
}
else{
c=fmax(c,c);
c=fmin(c,a);
e=floor(a);
}
if(isless(e,e)){
b=fdim(b,d);
e=(log10(d))*(fdim(b,d));
}
else{
d=fmax(c,b);
c=(floor(d))+(sin(b));
c=(atan2(d,c))-(acos(d));
b=(pow(b,a))-(asin(d));
d=(pow(a,d))/(log10(d));
}
}