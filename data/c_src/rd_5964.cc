#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(a,b))+(log(c));
e=(fmin(c,d))*(exp(e));
if(isless(e,d)){
c=log10(c);
d=(atan2(a,c))*(tan(d));
}
e=atan2(e,d);
a=pow(b,c);
}