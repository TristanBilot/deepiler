#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(c,c))/(acos(b));
e=(acos(b))+(log10(e));
b=acos(e);
d=fmax(d,b);
e=(atan2(c,c))/(sqrt(e));
a=pow(c,a);
if(islessequal(a,b)){
b=atan2(d,a);
d=ceil(e);
d=acos(b);
d=(fdim(c,c))/(sqrt(a));
e=exp(b);
}
else{
a=pow(c,a);
b=(tan(b))/(fmin(b,d));
b=ceil(b);
}
}