#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=cos(c);
f=exp(b);
a=sin(e);
e=(floor(c))/(fmax(d,a));
f=(log(d))/(tan(e));
b=acos(b);
d=exp(c);
if(isgreaterequal(e,f)){
b=(fdim(d,b))-(pow(a,f));
d=log10(c);
}
else{
d=(log10(b))+(atan2(a,a));
c=(asin(b))+(tan(a));
d=(atan(d))*(fmax(c,a));
f=sqrt(e);
a=(log10(c))*(sin(f));
}
}