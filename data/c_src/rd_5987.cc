#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(c,c))/(fmin(a,c));
d=sin(c);
d=log10(c);
b=cos(d);
f=(floor(d))+(exp(e));
c=floor(e);
b=(fmax(b,b))+(log10(a));
d=(pow(a,f))+(floor(a));
d=fdim(f,e);
c=(fdim(f,e))+(fmax(e,d));
c=(pow(b,c))*(log(b));
}