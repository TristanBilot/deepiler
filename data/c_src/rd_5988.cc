#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(c))+(fmax(e,c));
d=(log10(d))+(pow(e,b));
a=(asin(b))*(fmax(c,e));
c=fmin(f,c);
f=sqrt(c);
a=floor(d);
b=pow(c,e);
a=fmax(b,e);
e=(acos(a))-(fmin(d,f));
a=exp(c);
c=(pow(a,f))/(fdim(d,a));
d=fmax(b,b);
}