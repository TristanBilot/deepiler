#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log10(e);
e=(fmax(f,d))*(fmax(c,b));
while(isless(b,f)){
f=(tan(c))-(acos(e));
e=(pow(e,c))+(ceil(a));
}
f=(cos(f))*(fmin(d,b));
b=(atan2(a,b))/(fdim(f,c));
}