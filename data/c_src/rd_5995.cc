#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan2(c,a))+(pow(f,e));
h=(atan(h))/(floor(a));
while(islessgreater(h,c)){
e=(fdim(h,b))*(floor(f));
d=atan2(a,b);
a=log10(b);
f=fmax(c,d);
a=(pow(e,c))-(ceil(h));
}
if(islessequal(b,c)){
c=acos(g);
a=(asin(h))*(fdim(e,c));
h=floor(a);
}
}