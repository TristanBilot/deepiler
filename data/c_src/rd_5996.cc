#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fdim(a,b))-(tan(g));
c=(atan2(a,a))*(tan(h));
e=sqrt(d);
d=fdim(d,h);
c=floor(h);
h=pow(f,g);
h=(atan2(a,d))-(atan(f));
f=fmax(e,g);
if(isless(a,h)){
a=(acos(a))-(atan2(g,f));
c=(fdim(g,b))+(pow(b,a));
}
else{
c=(asin(a))-(floor(h));
h=(fmax(b,e))-(pow(e,d));
g=acos(f);
}
}