#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan(b);
b=(atan2(d,a))/(sin(b));
while(islessequal(a,e)){
a=exp(f);
d=fmin(f,d);
}
if(islessgreater(g,b)){
g=(sin(e))*(atan2(g,f));
g=floor(f);
f=(log(g))*(fmax(a,f));
a=(sqrt(a))+(asin(g));
}
}