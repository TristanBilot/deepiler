#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(b,d))+(sqrt(e));
c=(log(b))*(fdim(d,f));
c=(sin(g))*(acos(a));
d=(sqrt(f))+(fmax(f,f));
e=atan2(g,a);
d=(fdim(c,d))/(fdim(d,b));
g=log10(g);
d=floor(f);
c=(floor(d))*(log(e));
e=(pow(e,e))-(cos(g));
b=(atan2(c,f))*(fmin(g,a));
}