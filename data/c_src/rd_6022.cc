#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fdim(b,c))-(fdim(d,f));
e=(ceil(g))*(fmin(d,b));
h=pow(c,c);
c=atan2(c,e);
if(islessgreater(d,d)){
b=exp(a);
d=(log(c))+(fmax(g,h));
}
else{
c=fdim(g,e);
c=ceil(f);
}
g=asin(g);
e=fdim(a,a);
f=(fmin(h,g))*(pow(f,g));
e=ceil(b);
}