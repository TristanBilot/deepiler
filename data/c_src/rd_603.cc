#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log(b);
f=fmin(h,a);
f=(log10(h))*(floor(h));
c=acos(d);
if(islessgreater(d,h)){
c=acos(a);
c=(asin(g))/(fmax(h,e));
}
if(isless(d,c)){
f=atan(f);
g=(log(e))/(fmin(e,f));
a=(atan2(f,c))*(fmin(b,h));
g=(fmin(g,b))*(sqrt(g));
}
}