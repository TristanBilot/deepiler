#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=floor(a);
e=pow(a,f);
d=(fmax(a,b))-(cos(f));
e=cos(b);
d=ceil(d);
while(isgreaterequal(d,d)){
f=(pow(e,f))/(fmax(b,a));
d=(log(d))*(atan2(b,e));
f=(exp(b))*(exp(c));
a=fdim(d,f);
e=(acos(f))-(pow(b,a));
}
while(islessgreater(a,d)){
f=sqrt(e);
e=(ceil(d))+(atan2(e,e));
c=sqrt(c);
}
}