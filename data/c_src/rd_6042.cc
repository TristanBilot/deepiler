#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(e,b);
f=(fmax(f,b))+(floor(a));
a=(log10(c))*(fmax(b,e));
while(islessgreater(a,d)){
e=(exp(f))+(sin(a));
f=(pow(e,d))+(fmin(f,a));
b=(atan(a))/(fdim(a,d));
g=(atan2(f,f))*(atan(c));
b=(fdim(e,f))/(floor(b));
}
while(islessequal(g,e)){
g=acos(c);
d=pow(c,d);
}
}