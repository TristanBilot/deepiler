#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(acos(d))-(ceil(c));
e=(fmax(d,a))+(fdim(d,c));
c=fmin(c,e);
d=fdim(a,b);
while(isless(a,a)){
b=asin(a);
c=(sin(d))*(sin(e));
c=pow(a,e);
b=fmin(c,d);
}
}