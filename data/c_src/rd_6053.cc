#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=asin(f);
d=(fmax(h,c))+(atan(a));
c=(tan(a))+(pow(b,g));
if(isgreaterequal(a,g)){
c=(fmax(c,f))*(fmax(a,d));
b=(log(a))-(atan2(c,f));
b=atan(f);
a=asin(h);
a=sqrt(b);
}
else{
h=(exp(e))+(exp(g));
e=atan2(b,e);
a=(atan2(e,h))+(pow(e,b));
c=(sqrt(f))-(log10(g));
}
b=(fmax(h,a))*(log10(d));
e=log(e);
a=(fmin(b,a))+(fdim(d,c));
f=(pow(b,a))*(pow(a,f));
g=(fdim(e,c))-(fdim(f,f));
}