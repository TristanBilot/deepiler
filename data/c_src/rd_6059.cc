#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sqrt(f))/(pow(e,c));
c=log10(f);
e=(fmin(c,a))+(tan(a));
d=(log10(b))/(cos(f));
b=(log(a))+(sqrt(d));
a=(fmin(b,e))/(fmin(c,c));
f=fmax(e,c);
while(islessgreater(b,d)){
c=fdim(f,c);
e=(sin(f))/(asin(b));
b=sqrt(c);
a=exp(e);
f=(sqrt(e))-(floor(a));
}
}