#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(g,d);
b=fmax(h,b);
a=fmax(g,d);
b=(pow(h,d))/(ceil(e));
f=(fdim(d,c))-(fdim(g,c));
c=fmax(c,a);
g=ceil(b);
e=(ceil(g))-(ceil(f));
h=fdim(b,c);
e=pow(b,a);
a=exp(b);
}