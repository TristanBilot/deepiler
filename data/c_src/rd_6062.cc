#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=acos(d);
e=pow(b,e);
a=atan2(c,d);
b=(atan2(c,c))*(fmax(e,d));
e=(atan(a))/(fmin(b,b));
d=(log(a))/(sin(b));
e=fmin(b,c);
c=tan(c);
if(islessequal(d,b)){
c=(cos(b))*(fmax(c,c));
c=(fmax(d,d))-(pow(c,d));
b=(sin(a))*(pow(b,e));
a=(pow(d,d))-(pow(a,d));
e=(log(c))-(atan2(a,c));
}
}