#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(c);
g=exp(d);
h=floor(a);
g=fmin(f,h);
a=(atan(e))-(tan(h));
e=(floor(a))*(fdim(f,b));
d=sqrt(h);
g=exp(h);
b=(atan2(c,b))*(fmin(d,f));
if(isgreaterequal(g,h)){
d=(asin(g))-(fdim(e,h));
h=(cos(h))*(atan2(c,d));
}
else{
f=(tan(b))/(floor(h));
f=(log(a))/(fdim(e,f));
b=(atan2(b,c))*(log10(a));
}
}