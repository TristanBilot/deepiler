#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(tan(f))-(fmax(f,f));
f=(log10(b))/(sin(c));
f=(sin(d))-(sin(c));
c=atan2(d,g);
b=(exp(e))-(log(d));
while(islessequal(e,b)){
c=(asin(e))*(fdim(f,c));
a=cos(e);
b=(fmin(g,d))-(pow(e,g));
g=atan2(c,a);
}
while(isgreaterequal(b,e)){
f=(fmax(a,g))*(fmax(f,g));
f=fmin(f,e);
c=log(e);
d=atan2(e,f);
}
}