#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(b,d))*(atan2(c,a));
a=(atan2(b,e))-(pow(c,e));
a=(ceil(c))-(fmin(d,c));
d=(fmin(e,a))+(ceil(a));
if(islessgreater(e,c)){
c=(atan(a))+(fmax(c,e));
a=(pow(a,b))/(fmin(e,b));
}
c=(fdim(d,a))*(log(a));
b=(fdim(a,c))-(sqrt(e));
c=tan(e);
}