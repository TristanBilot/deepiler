#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan(b))-(exp(e));
f=(fdim(d,d))-(atan(a));
f=(atan2(e,e))-(log10(c));
b=pow(e,e);
while(isgreaterequal(c,f)){
d=fdim(d,c);
b=atan2(b,e);
e=(asin(f))-(atan2(a,a));
d=(tan(e))/(atan2(f,a));
}
c=(asin(b))/(fdim(f,f));
c=fmin(d,b);
d=(atan2(c,b))/(fdim(d,a));
f=(exp(a))/(sqrt(e));
b=(atan2(b,b))+(atan(d));
}