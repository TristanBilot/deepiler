#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(a,a);
e=fmax(d,h);
while(isless(h,h)){
g=(pow(b,d))*(log10(c));
b=(fmax(g,d))+(ceil(g));
a=(fdim(g,h))-(asin(b));
h=(pow(g,g))-(ceil(b));
a=fdim(g,a);
}
if(islessgreater(e,e)){
a=(fmin(e,e))/(pow(a,f));
d=(fdim(b,f))+(atan2(f,d));
e=fmax(e,f);
}
else{
b=atan(g);
c=(fmax(e,b))-(log(c));
c=(pow(b,b))-(sqrt(f));
}
}