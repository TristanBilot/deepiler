#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(a);
b=(atan2(a,e))/(sin(d));
d=(exp(d))*(log(c));
a=(atan2(a,e))/(pow(e,d));
d=(pow(d,d))*(ceil(a));
if(islessequal(d,d)){
e=fdim(c,d);
d=floor(a);
d=asin(c);
e=fdim(b,e);
a=fmax(a,c);
}
else{
e=(log10(d))*(tan(b));
d=fmin(a,d);
a=atan2(e,a);
}
if(isgreaterequal(e,d)){
e=fmax(d,c);
c=(sin(a))-(log10(c));
}
else{
e=pow(b,b);
e=acos(e);
}
}