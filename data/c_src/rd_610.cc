#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(e);
d=(atan2(e,a))+(tan(c));
d=fmin(e,d);
a=ceil(a);
c=(exp(c))+(fmax(c,c));
if(isless(a,c)){
e=cos(e);
c=(atan2(a,b))*(asin(c));
b=(sin(d))/(sin(c));
b=(ceil(a))/(acos(d));
d=(atan2(b,b))/(fdim(c,a));
}
else{
e=(asin(a))-(atan2(a,e));
b=fmin(c,e);
d=log(b);
a=(floor(a))*(exp(b));
}
d=atan2(a,c);
e=log10(e);
d=fmax(d,b);
}