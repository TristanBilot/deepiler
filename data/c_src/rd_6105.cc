#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(b);
g=(sqrt(d))/(atan(f));
c=(ceil(f))-(fmin(f,f));
while(islessequal(g,c)){
f=exp(c);
h=(ceil(g))+(tan(g));
f=floor(h);
}
b=floor(b);
d=fmin(g,a);
e=ceil(e);
a=atan2(a,h);
}