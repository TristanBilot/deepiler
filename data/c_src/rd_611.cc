#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sin(c))-(acos(d));
d=fmin(a,f);
if(isless(e,f)){
d=log10(b);
b=(fmin(f,d))/(sin(c));
}
else{
c=pow(c,d);
c=(fmax(f,d))-(exp(a));
a=pow(b,d);
b=(pow(a,d))-(asin(a));
d=fdim(b,b);
}
d=ceil(b);
a=(sqrt(d))-(fmin(b,b));
c=(atan2(f,e))*(fdim(b,c));
a=(cos(e))+(pow(f,c));
b=log10(a);
}