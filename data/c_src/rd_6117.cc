#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(acos(e))-(fmax(e,f));
b=fdim(d,c);
c=(tan(f))*(log10(a));
if(isgreaterequal(c,b)){
e=atan2(f,a);
e=(tan(f))/(pow(c,e));
}
else{
f=(sin(a))+(log(d));
d=atan(d);
e=tan(a);
a=asin(b);
}
if(isless(b,d)){
d=sin(c);
d=log10(c);
a=fmax(f,b);
}
else{
b=acos(d);
c=(asin(b))-(floor(a));
}
}