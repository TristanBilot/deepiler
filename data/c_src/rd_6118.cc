#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(asin(d))/(fmin(e,b));
d=fmax(a,f);
g=asin(g);
c=pow(c,g);
a=(fdim(c,g))*(log10(f));
d=(atan2(e,f))+(cos(c));
e=(floor(d))/(atan2(e,d));
if(isgreaterequal(g,f)){
b=cos(f);
e=log10(e);
}
}