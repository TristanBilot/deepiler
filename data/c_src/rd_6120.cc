#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(d);
b=(sin(a))+(fdim(b,c));
b=(fdim(d,b))*(pow(b,d));
b=fdim(b,c);
while(isless(a,d)){
c=(asin(c))*(log10(b));
a=fmax(b,c);
c=(sin(a))*(fdim(c,b));
d=pow(a,c);
}
a=(fmax(b,a))/(atan(b));
a=(fdim(d,e))*(exp(d));
b=(pow(c,b))-(asin(e));
}