#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,c);
c=(log10(f))/(asin(d));
g=(fmax(b,b))/(cos(b));
a=pow(b,a);
a=(pow(a,c))+(exp(b));
b=atan2(f,f);
e=fmax(a,e);
g=(atan2(f,g))/(pow(g,h));
e=fmin(a,f);
d=fmax(a,e);
}