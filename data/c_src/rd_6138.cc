#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(floor(c))/(fmax(e,b));
d=pow(e,a);
d=log10(b);
c=fmax(b,b);
e=pow(a,b);
while(isless(a,c)){
b=log10(e);
a=fdim(d,d);
c=(sqrt(d))/(tan(e));
e=(fmin(c,e))*(fdim(e,c));
d=sin(a);
}
c=(atan2(d,c))/(sin(d));
c=(tan(a))-(pow(d,a));
}