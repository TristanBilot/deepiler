#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(tan(c))+(pow(e,a));
c=acos(a);
c=exp(e);
a=(log10(c))-(fdim(e,f));
if(isless(e,a)){
a=atan2(c,e);
e=(fmax(f,f))-(fdim(c,e));
a=fdim(d,a);
b=(log(b))+(ceil(b));
f=fmax(f,a);
}
else{
e=(fmin(b,d))/(fmin(d,b));
a=pow(a,b);
}
f=atan2(f,b);
b=(atan2(c,e))+(asin(b));
c=ceil(c);
}