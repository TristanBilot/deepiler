#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(b);
d=(pow(c,d))-(acos(a));
if(isgreaterequal(c,b)){
d=exp(e);
a=(atan2(b,b))*(acos(b));
e=atan2(e,b);
c=(atan(b))*(ceil(e));
}
if(isless(c,d)){
b=atan2(b,c);
e=(fmax(e,d))+(sqrt(d));
}
else{
b=fmax(d,a);
b=cos(c);
c=(fdim(b,b))-(fmin(e,a));
c=(asin(e))/(fdim(e,d));
a=asin(c);
}
}