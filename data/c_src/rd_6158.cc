#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(b,e);
e=pow(e,b);
b=(ceil(d))*(sin(c));
if(islessequal(d,d)){
c=pow(e,a);
c=fmin(e,b);
e=fmin(e,e);
d=(exp(b))+(fmax(b,d));
c=(fdim(b,b))*(floor(b));
}
while(islessequal(a,e)){
b=(exp(c))/(cos(c));
c=(fdim(a,b))+(sqrt(a));
e=(fmax(d,e))+(atan2(c,e));
}
}