#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(pow(e,e))/(atan2(e,c));
c=(fmax(d,e))/(sqrt(c));
c=cos(a);
c=(floor(a))-(atan(e));
while(isgreaterequal(f,a)){
f=(log(f))*(cos(c));
e=fdim(b,d);
a=(pow(f,f))-(pow(f,f));
f=(tan(c))+(sqrt(d));
e=(log10(e))/(sqrt(f));
}
if(isgreaterequal(c,b)){
b=asin(b);
b=floor(b);
}
else{
b=(acos(e))/(atan(e));
d=log10(e);
}
}