#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=sqrt(b);
b=(atan(c))*(fmin(b,d));
a=tan(e);
a=fmax(d,b);
d=(fmax(a,a))*(fmax(b,b));
while(isless(e,a)){
d=floor(c);
b=(fdim(b,b))/(pow(a,d));
}
}