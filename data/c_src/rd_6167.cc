#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(c,f))+(fmax(b,c));
g=fmax(f,f);
d=fmin(b,a);
e=(floor(g))+(asin(e));
g=fmin(d,g);
e=(atan2(d,a))+(tan(e));
b=log10(b);
d=atan2(c,g);
e=atan(b);
a=(fmax(e,e))+(fmax(f,e));
g=(fmin(a,g))+(asin(e));
a=(fdim(e,d))*(fdim(f,c));
c=cos(c);
e=atan2(b,b);
}