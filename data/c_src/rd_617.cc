#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(a,d);
b=(sqrt(h))+(pow(a,g));
e=(atan(f))*(sin(h));
a=(pow(d,a))+(acos(e));
f=exp(d);
f=(pow(e,g))*(asin(f));
a=(fdim(c,a))/(fmax(e,b));
a=fdim(b,h);
c=atan2(d,h);
e=(fmax(g,e))/(pow(a,b));
}