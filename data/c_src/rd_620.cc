#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(b,a))-(fmax(c,b));
d=(fmax(b,b))-(tan(b));
while(islessgreater(e,a)){
b=(sin(c))*(acos(d));
a=floor(d);
a=(fmax(b,a))*(fmin(a,b));
c=acos(c);
b=(log10(e))-(asin(b));
}
if(isgreaterequal(b,c)){
d=fmin(a,a);
e=(fmin(d,a))/(log10(b));
e=(asin(c))/(fdim(e,a));
a=(cos(d))-(atan2(e,d));
b=(fdim(b,a))-(log(b));
}
}