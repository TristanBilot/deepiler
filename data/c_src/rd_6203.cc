#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log10(c);
e=(ceil(d))+(fmin(e,f));
a=(asin(f))/(ceil(b));
a=log(e);
if(islessequal(f,a)){
a=(log(c))-(fmax(d,a));
b=(fmax(a,a))/(cos(a));
b=sqrt(e);
}
else{
e=fmax(f,c);
d=(cos(c))/(pow(d,c));
b=atan2(e,a);
a=sin(f);
b=cos(f);
}
}