#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(c,a);
e=fmax(b,h);
while(isless(a,g)){
f=(asin(b))*(fmax(d,f));
c=ceil(c);
f=fdim(c,g);
e=sin(b);
d=(atan2(f,a))*(atan(b));
}
a=atan(c);
g=(fmin(a,b))/(fdim(a,b));
d=(fmax(e,d))*(atan2(c,a));
}