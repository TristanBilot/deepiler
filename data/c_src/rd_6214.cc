#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmax(d,b);
c=sqrt(c);
c=ceil(d);
c=(acos(e))-(asin(e));
d=atan2(d,c);
while(isless(c,b)){
c=(acos(e))/(exp(d));
a=acos(f);
c=(atan(b))-(asin(f));
f=acos(d);
}
d=(log10(d))-(fmax(f,d));
a=atan2(b,b);
}