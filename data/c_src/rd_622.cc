#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(tan(c))+(fdim(b,g));
e=fmax(f,c);
f=(pow(g,b))/(ceil(d));
g=(pow(a,b))-(pow(d,c));
b=(fmax(e,c))+(fdim(c,e));
g=cos(c);
g=(fdim(a,e))-(fdim(c,h));
h=exp(b);
e=(cos(g))+(acos(e));
d=(fdim(d,e))/(fdim(b,c));
}