#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(a))/(log(c));
b=floor(a);
d=(cos(d))-(log10(f));
g=pow(f,a);
b=(atan(c))-(sin(e));
d=(cos(d))/(fmin(c,c));
c=pow(a,f);
c=(fmax(b,a))-(cos(g));
d=(fdim(d,b))+(fmax(c,g));
e=(pow(b,b))/(fmax(d,c));
while(isgreaterequal(e,c)){
g=pow(f,b);
f=fmin(a,a);
g=(fdim(g,f))+(floor(a));
f=ceil(a);
g=pow(c,e);
}
}