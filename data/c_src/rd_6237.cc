#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(floor(a))-(atan(b));
c=tan(b);
b=pow(f,d);
f=atan(a);
c=acos(a);
while(islessequal(c,d)){
f=(fmin(a,d))/(pow(d,e));
e=log(b);
e=(atan2(d,a))/(atan2(d,d));
}
if(isless(c,f)){
c=(fmin(f,b))/(tan(e));
e=(fmax(f,b))-(exp(b));
d=log10(a);
}
}