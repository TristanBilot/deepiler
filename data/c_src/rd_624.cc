#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(c,d);
f=log10(d);
while(isgreaterequal(c,d)){
e=log(b);
b=exp(c);
}
while(islessgreater(d,c)){
b=fdim(c,c);
c=(sin(b))/(cos(a));
b=(fdim(b,f))*(cos(a));
}
}