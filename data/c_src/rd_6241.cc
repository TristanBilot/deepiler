#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=acos(a);
e=(fdim(e,d))-(fmin(a,b));
d=atan2(b,e);
if(isless(d,c)){
c=fdim(e,c);
d=(pow(c,d))*(fdim(e,a));
c=(acos(a))*(sqrt(b));
}
if(isless(b,c)){
a=(pow(e,c))*(pow(d,b));
c=(fdim(a,a))*(sqrt(c));
}
}