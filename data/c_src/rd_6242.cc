#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=asin(c);
d=(exp(g))-(exp(a));
g=(ceil(b))*(acos(a));
d=(log10(g))/(fdim(e,c));
f=(atan2(b,g))*(log(d));
if(islessgreater(a,a)){
f=fmax(g,d);
b=(fdim(f,b))+(fmin(d,a));
e=(tan(a))-(floor(f));
}
else{
f=(cos(a))*(fdim(c,e));
c=fdim(b,f);
a=log(e);
g=(fdim(c,c))*(atan2(b,d));
d=(atan2(d,b))-(fdim(c,b));
}
g=(fdim(b,b))-(fmax(d,c));
f=(fmin(a,f))*(tan(c));
d=log10(c);
a=acos(d);
g=(fmax(g,g))-(fmax(e,f));
}