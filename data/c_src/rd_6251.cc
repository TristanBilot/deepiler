#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(sin(a))/(fmin(d,e));
e=(sqrt(a))+(fmax(a,b));
c=(sqrt(b))*(fdim(c,b));
e=(atan2(d,b))+(log10(b));
if(isless(b,e)){
c=fmax(e,d);
b=log10(d);
c=(tan(d))*(fmax(d,d));
b=(cos(a))*(fdim(e,c));
}
else{
c=(sin(e))*(fdim(b,d));
e=tan(d);
a=acos(b);
e=atan2(e,a);
d=(fmax(a,c))+(pow(c,b));
}
while(isgreaterequal(c,a)){
e=(atan(b))*(fdim(d,a));
c=tan(b);
b=pow(b,d);
}
}