#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(c,e);
e=fmin(a,c);
d=fmax(d,c);
b=acos(b);
b=(fmax(e,e))/(exp(b));
c=fmin(c,c);
c=fmin(a,d);
a=(pow(e,a))*(acos(b));
d=(cos(b))-(fmin(d,c));
if(isless(a,d)){
d=(ceil(c))/(fmax(b,b));
a=(fdim(b,a))-(atan2(d,e));
}
}