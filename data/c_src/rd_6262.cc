#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(d);
a=pow(b,b);
c=ceil(d);
d=(ceil(a))*(pow(b,b));
d=(log10(c))+(cos(e));
while(islessequal(c,d)){
b=(fmax(a,c))-(fmin(d,a));
e=(atan2(b,c))/(atan(d));
a=(ceil(d))-(pow(a,b));
c=exp(d);
b=asin(e);
}
e=(fdim(e,c))-(log(a));
d=atan2(c,c);
c=fmax(d,d);
d=(pow(a,c))*(exp(b));
c=cos(d);
}