#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=acos(c);
f=(sin(d))-(sqrt(b));
d=fmax(a,c);
f=(fdim(e,d))*(atan(f));
b=(atan(b))*(log(e));
c=pow(c,c);
a=(exp(e))-(exp(a));
if(isless(f,b)){
f=(floor(a))*(fmin(e,b));
f=acos(b);
d=(pow(e,e))*(tan(c));
}
else{
b=(tan(d))*(fdim(c,e));
d=(cos(f))+(tan(b));
c=sin(a);
b=fmax(d,f);
}
}