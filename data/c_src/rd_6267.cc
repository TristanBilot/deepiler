#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(b,g);
h=exp(b);
h=fmax(b,e);
g=(fmax(b,c))*(pow(a,g));
f=fmax(b,a);
if(islessequal(d,a)){
d=fmax(a,c);
b=asin(f);
e=(atan2(f,f))*(cos(b));
g=fmin(h,f);
g=fdim(d,h);
}
if(islessequal(e,a)){
g=fdim(c,f);
e=tan(e);
e=(fmin(h,b))-(fmax(g,b));
}
else{
f=log10(d);
f=(sin(d))*(pow(a,a));
}
}