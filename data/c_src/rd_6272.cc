#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sqrt(c))*(fmax(d,b));
a=pow(f,b);
b=atan2(e,f);
if(islessequal(d,b)){
f=cos(e);
c=pow(f,f);
b=(asin(b))*(pow(f,b));
}
e=(pow(g,e))*(atan2(b,e));
c=tan(d);
c=exp(a);
a=(pow(c,f))+(log(c));
d=log10(d);
}