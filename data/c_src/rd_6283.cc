#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(c))/(tan(e));
a=(floor(b))-(fmax(a,e));
if(isgreaterequal(a,d)){
c=(fmax(b,b))-(cos(b));
c=(fdim(a,c))*(floor(b));
a=(cos(b))+(floor(c));
c=pow(a,d);
b=cos(c);
}
b=sqrt(d);
d=fdim(a,c);
d=(floor(d))+(log(c));
}