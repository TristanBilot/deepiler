#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(c,b))+(pow(e,b));
f=(floor(f))-(asin(d));
c=fdim(d,f);
while(isgreaterequal(a,f)){
b=(atan2(d,f))+(fmax(a,b));
f=(sqrt(b))+(sin(a));
b=(fmax(b,c))-(pow(c,e));
}
c=(log10(d))*(atan(d));
b=(exp(b))*(log10(e));
d=fmin(c,f);
a=(cos(d))*(pow(e,d));
b=fmax(d,f);
}