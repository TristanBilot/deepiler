#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(sin(e))+(tan(h));
f=pow(g,a);
h=atan(a);
h=acos(e);
c=(atan(e))/(acos(a));
e=(pow(d,d))+(atan2(f,g));
d=fdim(f,d);
if(islessequal(g,e)){
g=fmax(c,g);
d=(log10(h))-(log(a));
b=log10(e);
g=log10(a);
b=(log10(e))-(sqrt(f));
}
else{
d=fdim(b,c);
d=asin(h);
g=tan(f);
f=(fmax(e,b))+(log(b));
}
}