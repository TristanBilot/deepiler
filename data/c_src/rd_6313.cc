#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(g,a))*(acos(a));
f=(fdim(a,c))/(atan2(c,g));
e=(fmax(a,e))/(fmin(e,f));
while(islessequal(d,b)){
d=fmax(g,d);
a=atan2(g,g);
c=floor(g);
b=sqrt(c);
b=atan(c);
}
while(islessequal(f,c)){
f=fmax(f,e);
b=(acos(g))*(tan(g));
g=(pow(a,g))/(exp(b));
}
}