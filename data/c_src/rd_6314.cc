#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=acos(b);
d=(ceil(c))*(log(b));
f=(ceil(a))*(cos(b));
b=log(h);
if(islessequal(f,f)){
e=(fdim(a,f))+(fdim(c,f));
g=log10(e);
}
else{
h=(fdim(h,e))+(fdim(f,a));
h=cos(a);
b=(fmax(a,a))*(fmin(f,a));
e=(pow(h,g))+(log10(d));
h=(fdim(e,b))/(asin(e));
}
if(islessgreater(d,d)){
c=(atan(c))*(fdim(c,d));
a=pow(g,g);
a=fmin(d,h);
c=log10(e);
d=tan(f);
}
else{
f=(fmin(a,b))*(pow(b,d));
f=acos(f);
g=log10(f);
d=(cos(e))/(fmin(c,d));
}
}