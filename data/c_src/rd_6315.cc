#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=tan(e);
c=(exp(c))*(fmin(a,b));
f=(pow(a,b))+(fmax(d,e));
f=(tan(c))*(exp(d));
f=(fmax(e,d))+(pow(a,d));
if(isgreaterequal(d,f)){
d=(acos(c))+(atan2(a,b));
e=(floor(e))+(log10(c));
b=(sqrt(c))-(tan(b));
f=(fmax(e,f))*(fdim(a,a));
}
else{
b=(asin(a))/(fdim(f,f));
e=(atan2(c,b))+(atan2(a,b));
}
e=(fdim(e,f))+(sqrt(a));
f=(sqrt(a))+(pow(f,c));
a=(pow(f,e))/(pow(d,e));
}