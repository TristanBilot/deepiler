#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fdim(b,b);
e=fdim(d,d);
e=(fmin(c,e))/(fmin(c,f));
c=(acos(b))/(fdim(d,d));
e=(tan(d))-(pow(c,f));
while(isgreaterequal(a,f)){
b=fmax(b,a);
a=fdim(e,f);
}
c=(log10(e))+(acos(d));
f=pow(c,f);
}