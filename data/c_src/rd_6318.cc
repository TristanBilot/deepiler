#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log10(b))+(fmin(c,c));
f=cos(a);
d=pow(f,c);
e=asin(f);
b=(fmin(e,c))-(pow(a,f));
while(isless(a,f)){
c=(atan2(f,b))*(fdim(f,a));
a=atan2(a,d);
c=(fmin(a,b))+(fmax(d,c));
c=(pow(b,a))-(log10(d));
}
while(isless(a,e)){
f=(log(d))-(exp(f));
f=acos(b);
d=log10(c);
e=fdim(e,a);
d=fmax(d,a);
}
}