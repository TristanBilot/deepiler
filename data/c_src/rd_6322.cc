#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(c,d))/(log10(e));
b=fmax(b,b);
a=exp(b);
e=pow(e,c);
d=exp(a);
a=fmin(a,e);
e=ceil(a);
a=(acos(b))-(fmax(b,c));
e=(atan2(c,d))+(sin(b));
c=fmax(c,c);
}