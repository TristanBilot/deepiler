#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(e,c);
b=tan(a);
d=sin(a);
if(isless(a,d)){
d=fmax(b,a);
b=(sqrt(a))/(pow(c,c));
}
else{
a=ceil(b);
a=pow(b,b);
c=(pow(e,e))*(atan2(a,c));
}
e=pow(e,d);
e=atan2(c,e);
e=(cos(a))+(pow(e,d));
e=ceil(a);
}