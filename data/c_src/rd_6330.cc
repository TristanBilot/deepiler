#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(a,e))*(fmin(c,d));
b=(exp(c))+(atan2(d,c));
f=(floor(a))-(tan(e));
if(islessgreater(a,b)){
d=atan(b);
b=fmax(d,a);
e=atan2(e,e);
e=(tan(c))-(fdim(b,f));
}
f=tan(c);
a=(ceil(e))-(sin(d));
a=(fdim(f,d))+(fmin(c,b));
}