#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(g,b);
f=(fmin(d,c))/(log(d));
f=ceil(f);
b=(fmin(d,e))+(fmax(b,a));
a=fmax(d,b);
e=(pow(a,b))-(atan2(c,c));
f=(fmax(e,d))*(sqrt(d));
f=(fdim(c,e))/(acos(d));
if(isgreaterequal(f,g)){
c=asin(g);
e=fdim(d,f);
b=fmax(b,g);
a=(fmax(f,g))*(atan2(e,c));
}
}