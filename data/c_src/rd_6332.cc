#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(c,b);
e=fmax(b,a);
b=ceil(b);
f=(exp(d))*(exp(f));
a=acos(f);
a=(fmax(a,d))*(sin(f));
if(isgreaterequal(c,c)){
b=fmax(b,f);
c=acos(f);
a=(acos(b))-(fmin(b,d));
}
else{
e=(exp(b))+(fmin(f,e));
c=sin(d);
a=log(c);
c=atan2(c,f);
}
}