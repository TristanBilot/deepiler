#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(d,e))+(cos(c));
d=(cos(c))+(fdim(b,b));
e=(fmax(e,f))*(acos(g));
b=(pow(b,a))*(cos(e));
while(islessequal(a,b)){
f=(log(c))+(fmin(f,f));
d=asin(c);
f=(atan(b))-(fmax(c,c));
}
if(isgreaterequal(g,e)){
f=(fdim(f,c))-(sqrt(g));
g=fmax(c,e);
c=fmin(d,e);
e=(ceil(d))+(acos(f));
a=log(g);
}
else{
a=pow(a,g);
c=pow(g,c);
}
}