#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(f,e))-(sin(g));
f=(sqrt(g))+(exp(g));
e=(atan2(c,a))-(pow(b,b));
f=(sin(g))*(floor(e));
if(isgreaterequal(c,b)){
b=pow(d,c);
b=(atan2(g,e))+(fmin(g,b));
a=fmin(a,c);
e=(fdim(b,f))*(pow(a,b));
g=pow(f,d);
}
else{
c=floor(e);
d=(asin(g))-(ceil(b));
a=exp(a);
g=fmin(c,g);
}
if(islessgreater(c,g)){
b=(fmin(a,b))-(fmax(c,c));
g=fmin(e,f);
d=(fdim(a,c))/(atan(b));
c=(atan(b))+(log10(g));
}
else{
a=(atan2(d,d))/(asin(g));
d=cos(g);
g=(atan2(c,g))/(atan2(e,a));
f=log10(d);
e=ceil(d);
}
}