#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(asin(f))+(asin(e));
d=cos(e);
e=pow(d,g);
while(isless(a,g)){
d=(fdim(d,a))+(log10(b));
a=(fmin(e,e))/(fmax(g,a));
e=(atan2(d,e))*(pow(d,b));
e=exp(e);
f=fmax(f,a);
}
while(islessequal(c,a)){
e=pow(f,a);
b=tan(c);
}
}