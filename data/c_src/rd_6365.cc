#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(asin(c))+(cos(e));
e=(fdim(c,a))*(fmax(d,e));
b=(sqrt(d))*(fdim(e,c));
b=(sqrt(c))*(exp(e));
while(isless(c,a)){
c=pow(a,d);
b=(atan2(e,b))*(sin(d));
a=fmax(e,d);
c=asin(c);
b=fmax(e,c);
}
a=fmin(d,b);
a=fmax(d,e);
b=cos(d);
}