#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(a,a);
d=(pow(c,g))/(atan2(d,d));
while(islessgreater(a,b)){
e=tan(b);
c=(fdim(b,e))+(log(d));
f=(fmin(b,b))-(fmin(f,d));
}
if(islessequal(b,d)){
d=(acos(c))-(fmin(a,b));
f=(sin(b))/(fmax(f,a));
a=fdim(g,e);
e=log10(a);
}
else{
f=atan2(f,f);
b=(atan2(b,b))*(atan2(d,d));
d=(exp(a))/(pow(g,c));
}
}