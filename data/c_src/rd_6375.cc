#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(g,d);
d=(fmax(a,h))-(pow(f,f));
b=(pow(a,a))+(log(e));
a=fmax(b,c);
c=(atan2(a,a))+(log10(f));
a=(atan2(d,d))*(atan(d));
e=(sqrt(a))*(atan2(d,h));
while(islessequal(c,e)){
d=(fmin(b,c))+(tan(h));
f=(pow(h,b))*(log(b));
c=fmax(h,h);
d=(exp(g))/(log10(c));
}
}