#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,d);
c=atan2(c,c);
c=fmin(e,b);
a=exp(d);
d=(floor(d))/(pow(b,c));
c=fmin(e,c);
e=fmin(c,e);
while(isless(b,b)){
d=(sqrt(d))*(floor(b));
a=cos(a);
a=(fmax(e,e))+(atan2(a,d));
a=log10(e);
}
}