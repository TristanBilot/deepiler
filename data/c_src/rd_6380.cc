#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(cos(f))-(fmin(g,a));
d=(fmax(d,e))+(fdim(g,d));
e=(fmin(b,c))*(atan2(c,e));
g=fdim(c,e);
c=pow(a,b);
f=(fmax(b,g))-(fmax(g,a));
a=(fmax(d,f))/(atan(f));
b=(cos(e))-(floor(e));
e=(fdim(c,a))+(acos(b));
if(islessequal(e,f)){
b=fmin(f,b);
d=sin(g);
b=atan2(e,b);
f=(sin(g))*(atan2(f,e));
}
else{
g=(fmin(c,g))+(fdim(f,g));
b=(sqrt(e))/(ceil(a));
c=pow(c,e);
c=exp(b);
}
}