#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(f,c))/(log10(b));
b=fmin(d,f);
c=floor(d);
if(islessequal(f,c)){
f=(fmin(e,a))/(floor(e));
e=(atan2(a,d))/(pow(e,e));
c=tan(c);
}
else{
b=(fdim(c,b))-(floor(b));
c=exp(d);
a=(pow(d,f))*(atan(f));
e=pow(c,a);
c=(acos(d))/(tan(a));
}
while(isless(c,b)){
a=atan(e);
f=(acos(e))-(fdim(d,d));
d=(fdim(c,c))-(fmax(f,c));
d=fmin(d,a);
b=exp(d);
}
}