#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(d,c))/(exp(b));
c=(fdim(b,g))/(exp(g));
c=sin(a);
g=(fdim(e,c))/(fmax(d,e));
while(islessequal(f,b)){
a=fmin(c,g);
b=(fmin(c,f))-(fmax(e,f));
c=(sqrt(d))*(acos(f));
}
g=acos(g);
d=(log10(e))-(log(f));
b=(atan(c))*(fmin(g,e));
a=fmin(g,c);
}