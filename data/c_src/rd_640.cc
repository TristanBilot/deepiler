#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmax(d,c);
b=sin(c);
d=ceil(b);
c=(pow(b,d))+(fdim(e,b));
f=atan2(d,e);
b=(log(e))*(fmin(d,f));
b=(pow(b,a))*(fmax(f,a));
c=cos(e);
while(isless(e,e)){
b=(fmin(f,f))+(fmax(e,f));
c=log10(b);
d=cos(b);
}
}