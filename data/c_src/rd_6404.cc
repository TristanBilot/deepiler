#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log10(e))/(pow(c,d));
e=(floor(b))/(acos(c));
g=(cos(d))+(fdim(f,d));
a=(atan2(c,b))/(pow(e,b));
c=(fdim(d,e))-(fmax(f,g));
g=(atan2(d,a))*(fmax(b,g));
a=fmin(e,g);
c=fmin(f,b);
a=fmax(b,g);
g=(fmin(a,b))+(sqrt(d));
if(isgreaterequal(d,a)){
d=(pow(d,a))+(fmax(f,d));
e=(sin(c))/(atan2(e,a));
d=(acos(f))+(exp(a));
}
else{
c=fdim(f,b);
c=atan(g);
f=sqrt(g);
}
}