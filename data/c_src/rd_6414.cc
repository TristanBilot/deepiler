#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(c,c))+(fmax(a,h));
g=(sqrt(f))-(tan(e));
if(isless(h,f)){
c=sin(g);
g=pow(h,f);
}
else{
b=(pow(a,b))/(log10(b));
d=fdim(g,c);
g=(atan2(f,c))*(cos(e));
d=acos(b);
g=(pow(a,g))-(pow(f,e));
}
while(isless(a,a)){
d=exp(h);
b=(exp(e))/(sin(d));
}
}