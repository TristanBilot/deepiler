#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log10(f);
d=atan(f);
f=log10(e);
if(islessequal(c,f)){
d=atan2(c,e);
c=(ceil(e))/(fmax(b,b));
}
c=(atan2(d,e))-(atan2(e,c));
e=fmax(c,f);
}