#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(b);
a=(sqrt(g))+(fmax(b,d));
h=(fmin(h,f))/(pow(c,c));
if(islessequal(h,g)){
b=sin(c);
g=(fmin(c,c))+(fmin(c,d));
a=tan(f);
}
else{
f=(atan2(a,b))/(pow(d,g));
b=(floor(d))+(asin(d));
a=(pow(h,f))/(log10(g));
}
if(islessgreater(h,a)){
b=(fmax(e,e))-(fmax(d,g));
h=(fmin(f,a))-(tan(e));
}
else{
h=(sqrt(f))*(asin(d));
c=(pow(g,b))-(fmax(d,d));
h=(sqrt(e))/(atan2(e,e));
}
}