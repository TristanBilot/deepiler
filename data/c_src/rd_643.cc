#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(c))+(sqrt(a));
e=fmax(f,d);
f=fdim(b,b);
d=(fmin(a,d))/(atan(e));
while(isless(e,d)){
e=log10(a);
f=atan2(a,a);
}
}