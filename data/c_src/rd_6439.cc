#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=floor(f);
e=cos(a);
f=(exp(a))/(floor(c));
while(isless(f,e)){
d=(tan(d))+(fdim(b,a));
b=fdim(b,b);
d=(sqrt(e))*(ceil(a));
a=fmax(d,c);
}
if(isless(b,d)){
e=(sqrt(d))+(pow(a,d));
a=atan2(e,d);
c=(fmin(a,c))+(sin(a));
}
else{
e=sqrt(a);
c=(log10(b))*(log(d));
}
}