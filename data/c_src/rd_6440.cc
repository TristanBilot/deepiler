#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=asin(e);
a=(exp(a))/(fmin(g,h));
d=tan(c);
b=(sqrt(f))*(fdim(b,e));
g=(exp(b))-(sqrt(f));
f=atan(d);
while(islessgreater(b,b)){
b=pow(f,h);
c=atan2(e,c);
g=fmin(b,g);
h=(atan2(f,a))+(sqrt(c));
}
}