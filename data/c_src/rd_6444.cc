#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fdim(c,b);
d=fmin(d,e);
e=(floor(b))+(ceil(g));
b=(cos(b))-(fdim(g,e));
f=(fmax(e,f))*(sin(f));
b=(fmin(a,d))+(fmin(b,a));
g=(fdim(g,d))/(pow(f,b));
c=cos(e);
c=(log10(d))/(floor(e));
c=fmin(g,d);
}