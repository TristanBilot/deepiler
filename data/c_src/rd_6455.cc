#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(d,b);
d=log10(b);
h=atan2(a,b);
a=(atan2(f,e))+(atan2(a,h));
d=floor(a);
while(islessgreater(f,h)){
b=(floor(h))*(atan(f));
e=fmax(c,g);
d=tan(d);
b=fmax(c,c);
}
while(isgreaterequal(b,d)){
d=log(e);
a=pow(g,h);
b=fdim(g,e);
}
}