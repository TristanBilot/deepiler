#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan(f);
c=log10(d);
e=(exp(e))-(fdim(e,e));
f=sqrt(b);
g=(atan2(e,c))*(tan(c));
d=exp(c);
c=pow(e,b);
c=asin(c);
f=(pow(e,c))*(tan(c));
d=fmin(f,a);
c=fdim(c,a);
a=fmax(g,b);
c=(pow(g,g))/(log10(d));
g=fdim(g,g);
}