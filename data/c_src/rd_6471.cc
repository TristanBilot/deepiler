#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(c);
a=tan(h);
e=(exp(g))-(log10(h));
h=(pow(f,e))*(fmax(g,d));
g=log(a);
if(islessgreater(d,c)){
f=fdim(f,d);
c=fmax(h,b);
f=(fmax(e,h))/(cos(b));
}
f=(fdim(e,h))+(exp(f));
c=fdim(g,c);
h=(atan(f))/(fmax(e,h));
a=fmax(e,g);
}