#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sqrt(c);
e=asin(d);
if(isless(f,d)){
g=ceil(d);
g=(log10(e))/(asin(c));
a=(atan(f))+(sin(d));
b=(cos(g))-(sin(d));
b=sin(a);
}
else{
g=(fdim(a,a))/(sqrt(e));
d=atan2(d,c);
d=(tan(f))/(asin(b));
e=(pow(c,g))*(sqrt(a));
}
while(islessequal(b,c)){
g=fmin(d,f);
f=ceil(e);
a=(atan2(g,d))*(sin(e));
}
}