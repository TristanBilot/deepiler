#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(d,b))+(fmax(a,d));
f=sqrt(a);
if(isless(d,c)){
c=tan(d);
d=fmin(g,c);
}
else{
b=pow(g,g);
c=(fmax(d,g))/(sin(a));
g=pow(a,d);
g=exp(e);
c=atan2(d,e);
}
if(islessequal(b,a)){
d=(pow(a,a))*(pow(e,a));
a=(pow(c,g))/(pow(d,b));
}
}