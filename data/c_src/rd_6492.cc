#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log10(b);
a=(log10(c))*(atan2(f,e));
while(islessequal(b,a)){
b=fmax(c,e);
e=(pow(b,e))+(asin(b));
}
a=(atan2(f,a))/(fmin(d,f));
f=(fmax(c,e))-(fdim(a,e));
}