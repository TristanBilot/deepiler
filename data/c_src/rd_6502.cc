#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan(b))*(sqrt(b));
e=fdim(b,d);
while(islessequal(d,a)){
b=fdim(d,d);
e=(fmax(d,a))+(atan(a));
d=fmin(f,e);
}
if(isless(c,e)){
e=(fdim(b,b))-(pow(a,e));
a=(pow(b,b))+(atan(d));
}
}