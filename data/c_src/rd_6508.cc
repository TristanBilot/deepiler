#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(c,c);
c=exp(d);
a=(fdim(a,f))*(fmin(c,b));
f=floor(a);
f=atan(d);
f=(tan(f))*(fmax(c,a));
d=pow(b,e);
e=ceil(a);
a=(fdim(a,f))/(asin(b));
b=sqrt(d);
}