#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(d,h);
h=atan2(g,g);
g=tan(f);
e=cos(d);
h=atan2(h,d);
h=(tan(d))+(fmin(e,a));
d=pow(g,h);
e=fdim(c,h);
h=fmin(c,c);
b=(sqrt(e))+(fmax(e,e));
}