#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=cos(b);
c=pow(f,d);
e=(atan2(f,d))-(fmax(c,f));
a=(cos(a))+(fmax(c,c));
while(islessequal(a,c)){
c=exp(a);
e=(log(d))/(fdim(d,f));
f=(cos(d))/(atan2(d,c));
f=fmax(c,b);
}
}