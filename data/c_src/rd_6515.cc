#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,b);
a=(fmin(a,b))/(atan2(d,c));
if(isgreaterequal(a,a)){
c=fmin(b,e);
e=(fdim(c,c))/(fmin(c,e));
}
else{
e=(atan2(c,d))*(atan2(e,b));
e=(floor(d))+(sin(a));
d=(atan2(d,e))-(tan(a));
a=fdim(c,d);
}
while(islessequal(d,d)){
d=(tan(a))-(log10(e));
d=fmax(b,d);
c=fmin(c,e);
e=(floor(c))*(sqrt(c));
}
}