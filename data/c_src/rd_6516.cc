#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(floor(c))-(fmin(e,c));
f=(fdim(b,d))*(atan(f));
c=(atan2(e,d))+(ceil(e));
e=tan(b);
a=(pow(c,a))*(sqrt(b));
if(isless(f,b)){
f=tan(a);
a=asin(f);
c=(sin(e))*(log(e));
}
else{
d=fmax(f,b);
a=atan(c);
c=(fmin(b,c))+(sqrt(e));
}
while(islessequal(a,e)){
a=(atan(a))*(pow(c,f));
e=fmax(b,d);
}
}