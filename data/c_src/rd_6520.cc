#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(e,a))+(pow(c,a));
f=(pow(a,c))*(fmax(b,d));
if(isgreaterequal(f,f)){
a=(exp(a))*(log10(f));
b=sqrt(e);
b=(ceil(a))/(sqrt(a));
b=ceil(c);
}
e=asin(a);
b=(pow(c,c))*(fmin(f,b));
b=(exp(e))*(atan2(a,f));
b=fmax(d,b);
}