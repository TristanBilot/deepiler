#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=tan(d);
c=floor(a);
if(isless(d,e)){
b=pow(c,e);
d=(sin(e))/(fdim(d,e));
c=atan2(e,d);
e=(log10(b))+(asin(b));
e=ceil(b);
}
else{
d=fmax(a,b);
d=fmax(b,a);
d=(pow(c,a))/(fmax(a,d));
}
while(isgreaterequal(c,e)){
d=floor(a);
d=(ceil(d))-(exp(d));
c=(cos(a))-(fmin(b,a));
d=(fdim(b,d))-(fmin(d,e));
a=log10(a);
}
}