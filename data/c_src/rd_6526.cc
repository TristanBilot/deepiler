#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(c,e);
c=atan2(b,d);
d=fmin(e,d);
a=(atan2(e,b))-(fmax(c,b));
e=(fmin(a,b))*(fmax(c,d));
b=(sqrt(d))-(asin(b));
b=pow(d,d);
c=(fdim(b,c))/(acos(d));
a=fmin(a,d);
a=log10(e);
d=atan2(e,b);
e=log10(d);
}