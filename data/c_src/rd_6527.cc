#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(a);
d=(fdim(e,e))+(atan2(b,e));
d=fmax(d,e);
b=(fmax(c,d))/(fdim(c,e));
while(islessequal(a,b)){
c=log(e);
c=(fmin(e,b))/(fdim(e,c));
d=(exp(d))*(atan(e));
a=atan(b);
}
if(islessequal(e,e)){
e=(tan(c))-(atan(c));
c=fmax(d,d);
d=(fmax(a,b))+(sin(b));
e=atan2(a,a);
}
else{
a=ceil(b);
c=fdim(d,d);
}
}