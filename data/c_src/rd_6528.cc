#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(tan(b))-(acos(f));
g=fmax(b,c);
c=(acos(c))+(log(c));
c=(atan2(d,a))-(fdim(c,e));
b=log(a);
if(islessgreater(b,c)){
g=(atan2(g,g))*(fmax(f,g));
c=fmax(e,f);
g=(floor(c))*(cos(a));
}
if(isless(f,d)){
a=(pow(c,g))-(fmax(a,f));
b=(atan2(d,d))*(pow(f,g));
}
else{
g=exp(b);
e=(ceil(c))*(atan2(a,c));
g=(pow(g,b))*(sin(a));
a=atan(g);
}
}