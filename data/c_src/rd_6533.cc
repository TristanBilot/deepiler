#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(h);
f=(fmax(b,c))*(sqrt(g));
d=(fmin(f,c))*(atan2(b,a));
b=(log10(d))*(sin(e));
a=pow(f,f);
if(isless(b,a)){
f=(fmin(d,g))*(log(h));
d=(fmin(f,a))*(log10(f));
}
else{
d=atan(f);
e=(fdim(h,f))/(fmin(e,a));
}
if(islessgreater(b,b)){
g=log10(e);
d=(asin(h))*(log(g));
}
}