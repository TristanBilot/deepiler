#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log10(b);
e=(atan2(c,b))*(exp(a));
f=log10(c);
f=(log(a))+(log10(e));
f=(fdim(f,a))-(fmax(e,b));
if(isless(b,f)){
b=fdim(f,d);
b=log10(f);
a=pow(c,a);
d=(floor(f))-(tan(e));
b=floor(b);
}
else{
d=(atan2(c,e))/(sqrt(f));
b=(atan(f))/(fmin(d,a));
f=pow(c,c);
b=atan2(e,a);
}
}