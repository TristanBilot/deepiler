#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(c);
f=fmin(a,e);
f=fmax(a,e);
while(isless(h,e)){
e=(fdim(b,d))/(exp(e));
h=(floor(b))/(fdim(h,h));
}
while(islessequal(h,f)){
d=(atan(g))+(pow(c,g));
d=(exp(f))+(pow(c,e));
f=(fmax(d,f))/(pow(f,g));
e=pow(h,f);
d=atan(a);
}
}