#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(d);
c=tan(b);
a=(log10(c))-(fdim(e,d));
d=log10(d);
if(isless(e,a)){
e=exp(d);
c=log10(a);
a=atan2(d,b);
}
c=acos(d);
d=atan2(d,d);
a=atan2(c,a);
e=pow(b,b);
}