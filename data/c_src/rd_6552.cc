#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(b,e);
e=atan(c);
while(isless(d,b)){
d=(atan(a))/(atan(d));
d=exp(b);
}
if(islessequal(b,d)){
d=(sqrt(b))+(fmax(d,a));
d=floor(c);
a=(fmin(c,b))/(atan(d));
c=(pow(c,b))+(floor(b));
}
}