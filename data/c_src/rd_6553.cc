#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmax(d,b))*(fdim(a,b));
f=(exp(c))/(atan2(g,b));
d=floor(g);
c=fmin(f,g);
while(isless(d,b)){
g=(fmax(e,d))+(asin(e));
b=fdim(c,b);
}
while(islessequal(b,f)){
b=(fmin(c,g))/(exp(g));
b=fmin(e,d);
b=(exp(c))+(asin(f));
b=fmax(g,g);
a=fmax(c,c);
}
}