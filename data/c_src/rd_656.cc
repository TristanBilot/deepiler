#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(b);
b=(exp(c))*(atan2(a,b));
b=(fdim(a,b))/(log(c));
if(islessequal(c,d)){
e=(fmax(d,d))-(fdim(e,c));
c=(atan2(a,e))+(fdim(d,d));
b=sin(a);
a=exp(d);
b=fdim(c,b);
}
a=(fmax(e,e))*(log10(e));
a=(tan(c))/(pow(c,d));
}