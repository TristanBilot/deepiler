#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(d);
c=(log10(b))/(sqrt(e));
c=fmax(b,d);
f=(floor(c))+(fdim(d,e));
if(isgreaterequal(f,e)){
d=(fmax(g,g))/(fmax(f,c));
d=(atan(a))/(fmax(e,c));
a=fmax(d,b);
e=pow(d,f);
f=(atan2(h,f))-(ceil(b));
}
else{
g=ceil(h);
d=(atan(h))+(fdim(c,a));
a=tan(b);
e=pow(a,b);
}
if(islessequal(e,d)){
c=(fdim(g,a))-(ceil(e));
g=(fmin(f,g))*(atan2(a,b));
h=(ceil(d))*(fmin(d,f));
h=(fdim(g,c))*(fmax(e,d));
}
else{
a=(exp(c))/(tan(h));
a=acos(e);
c=ceil(a);
}
}