#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fdim(d,a))*(tan(a));
f=(pow(a,e))/(pow(d,b));
b=(exp(e))+(atan2(b,e));
while(isgreaterequal(f,b)){
b=atan(d);
c=fmax(d,a);
a=pow(c,b);
b=(fmax(a,d))/(fdim(b,d));
b=(tan(c))*(fmin(a,d));
}
if(islessequal(f,b)){
b=fmax(e,b);
b=atan(e);
b=(ceil(d))*(asin(a));
}
}