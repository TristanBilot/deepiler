#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(a,a);
f=pow(b,c);
a=floor(f);
if(isless(e,c)){
e=fmax(c,e);
b=(fdim(b,f))+(log(d));
f=(atan(c))/(fmax(f,b));
}
d=(fdim(c,b))/(sqrt(d));
f=(atan2(a,d))*(acos(b));
f=log(f);
}