#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(f);
b=(fmax(e,b))/(pow(c,b));
g=exp(e);
e=(fmin(h,e))*(log10(h));
b=fmin(b,h);
while(isless(h,b)){
e=asin(f);
b=(atan(e))/(ceil(e));
b=(log10(c))-(fmax(g,b));
d=acos(c);
}
while(islessgreater(c,g)){
g=(fmax(a,g))/(atan(f));
b=(fdim(e,h))/(acos(f));
a=(acos(e))-(asin(h));
e=fdim(e,a);
}
}