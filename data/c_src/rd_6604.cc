#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(f,c))+(exp(b));
d=(cos(d))*(exp(c));
a=atan(h);
while(islessequal(g,c)){
g=fmin(h,c);
a=atan2(e,f);
b=(fmax(a,d))-(fmax(b,a));
}
while(islessequal(a,f)){
e=atan2(f,g);
b=(ceil(h))+(cos(f));
a=(sin(c))-(log10(f));
c=pow(f,c);
}
}