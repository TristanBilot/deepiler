#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(h,d))-(atan2(a,f));
e=(log10(h))/(atan2(c,f));
c=floor(e);
a=(floor(e))*(pow(f,b));
a=(atan2(a,d))*(sqrt(c));
d=log(h);
g=(fmax(f,a))*(fmin(g,d));
c=(atan2(g,h))+(exp(c));
e=fmin(d,a);
if(islessequal(c,d)){
a=(log10(b))/(atan(c));
e=atan2(g,g);
e=ceil(e);
f=(log(a))*(exp(g));
h=fmin(e,d);
}
else{
g=(exp(d))/(acos(b));
g=(sqrt(b))-(fmin(c,c));
e=(log10(e))-(pow(b,d));
}
}