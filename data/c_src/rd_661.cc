#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(sqrt(b))+(fdim(b,c));
g=fdim(b,g);
g=(fmax(h,c))-(fdim(b,c));
while(isgreaterequal(e,f)){
e=fmax(d,e);
d=exp(c);
g=fmax(h,f);
b=sqrt(h);
a=sin(e);
}
while(isgreaterequal(f,b)){
e=(acos(a))/(atan2(h,a));
c=(fmax(b,g))-(atan2(h,c));
}
}