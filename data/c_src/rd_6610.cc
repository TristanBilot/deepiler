#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(exp(c))-(log10(b));
a=(floor(e))+(atan2(e,c));
e=(fdim(b,e))-(fdim(e,c));
if(islessequal(b,a)){
c=(cos(a))/(fmax(d,d));
e=tan(b);
}
while(islessequal(d,a)){
d=cos(a);
b=(fdim(a,b))-(fmin(d,b));
b=floor(b);
c=fdim(b,e);
b=(floor(a))-(pow(e,b));
}
}