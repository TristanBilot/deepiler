#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(tan(c))-(fmin(a,e));
e=(atan2(a,b))/(fmin(a,e));
a=fmax(e,d);
a=(log10(e))/(exp(b));
d=pow(d,e);
while(islessgreater(b,a)){
b=fmin(b,b);
b=fmin(d,a);
}
c=pow(b,e);
b=fmin(b,c);
d=(sin(a))+(pow(d,a));
e=(atan(e))/(atan2(e,e));
b=fmin(d,d);
}