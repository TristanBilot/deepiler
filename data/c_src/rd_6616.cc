#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(ceil(c))*(pow(e,c));
d=floor(c);
a=(cos(e))/(ceil(a));
a=tan(d);
while(islessequal(c,a)){
a=atan2(d,c);
e=(cos(e))+(exp(c));
}
a=(pow(c,b))*(log10(e));
b=(floor(e))/(fmin(c,c));
e=pow(e,e);
}