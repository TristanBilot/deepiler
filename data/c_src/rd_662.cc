#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(e,e);
e=(log10(d))+(ceil(a));
c=(exp(e))-(fmax(b,f));
while(islessequal(f,f)){
d=atan2(b,f);
b=atan2(g,c);
e=fdim(g,f);
e=fdim(b,c);
d=(sin(c))/(atan2(b,b));
}
if(islessequal(c,e)){
f=(sqrt(c))/(fmax(f,e));
g=fmin(d,c);
e=(atan2(c,g))-(pow(c,d));
}
}