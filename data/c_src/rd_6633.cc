#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(a,f);
d=ceil(d);
a=(atan2(a,a))*(ceil(c));
d=pow(c,c);
if(islessequal(f,e)){
f=(fmax(c,b))/(pow(d,a));
c=(sin(b))*(fmax(b,b));
b=log(d);
d=(ceil(b))/(fmax(d,c));
}
if(islessequal(b,d)){
e=(fmax(f,a))+(atan(e));
e=fmax(f,a);
}
else{
c=(fmax(f,a))/(sqrt(d));
a=fmax(d,d);
}
}