#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan(e))*(sin(g));
e=pow(h,g);
if(isless(c,g)){
h=cos(h);
a=(fmax(a,e))+(fmax(g,b));
h=(pow(f,e))-(log10(a));
e=(sin(b))+(fmin(f,c));
}
else{
h=(fmin(d,b))*(ceil(b));
g=(log10(h))+(pow(h,a));
h=exp(h);
c=pow(c,f);
}
while(islessgreater(c,d)){
d=log10(c);
b=sqrt(g);
h=(ceil(a))-(acos(e));
}
}