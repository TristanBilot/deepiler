#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(e,a))*(fmin(a,a));
g=(cos(b))-(log10(a));
f=log(b);
c=fmax(g,e);
e=(tan(g))-(fmin(b,d));
while(islessgreater(b,b)){
c=(log10(b))*(fmin(f,c));
g=acos(a);
d=atan(d);
e=(sin(f))*(atan2(a,g));
}
a=(acos(a))*(atan2(e,f));
c=fdim(c,e);
}