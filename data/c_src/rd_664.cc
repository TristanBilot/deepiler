#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmin(b,f);
b=log(g);
if(isgreaterequal(e,d)){
f=(pow(d,e))/(tan(f));
b=(pow(e,d))/(fmin(f,c));
b=fdim(e,e);
}
if(isgreaterequal(d,c)){
c=(fmin(d,f))*(cos(g));
a=(sin(f))+(log(a));
}
else{
d=log(e);
d=sin(a);
b=(pow(b,e))/(log(b));
a=(exp(g))*(acos(f));
}
}