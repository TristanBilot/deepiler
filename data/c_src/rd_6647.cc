#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(e,f))*(fmax(f,f));
f=atan(g);
f=(sqrt(e))+(sqrt(h));
a=asin(d);
if(isgreaterequal(f,b)){
e=sqrt(g);
e=fmax(e,g);
f=acos(b);
}
if(islessgreater(f,g)){
g=fmax(d,e);
g=acos(g);
c=(fmin(b,g))/(pow(h,g));
b=(log10(c))+(fmin(h,h));
a=fdim(b,a);
}
else{
c=log10(c);
d=fdim(e,h);
g=log(a);
b=(atan2(b,h))-(pow(g,b));
h=(log(f))+(pow(a,e));
}
}