#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(acos(b))/(floor(b));
c=log(f);
b=(fmax(b,d))+(pow(b,b));
c=(acos(a))+(sqrt(b));
c=(exp(d))-(log10(d));
if(isless(d,d)){
f=sqrt(c);
b=(pow(f,c))*(fmax(a,b));
e=(fmax(c,b))+(pow(e,c));
d=(fmin(b,b))-(fdim(e,b));
}
else{
a=atan2(f,f);
e=(pow(e,b))/(atan2(e,b));
b=atan(d);
f=exp(d);
}
d=(atan(e))+(asin(b));
e=(cos(e))/(cos(b));
a=atan2(f,d);
}