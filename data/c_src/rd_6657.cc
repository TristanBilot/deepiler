#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(d,b);
b=fdim(c,e);
c=(fmin(a,b))-(log(b));
c=fdim(b,d);
while(isgreaterequal(e,b)){
d=(fmin(c,c))*(fmin(b,c));
c=(pow(e,c))*(fdim(d,c));
c=sin(a);
}
if(islessequal(a,b)){
c=fdim(a,e);
d=pow(c,b);
d=fmin(b,c);
a=fmax(a,b);
c=acos(e);
}
}