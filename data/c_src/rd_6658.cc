#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan(c);
f=(fmax(a,c))+(atan2(d,a));
e=(fmax(e,d))*(exp(b));
a=fmax(c,b);
a=sin(a);
f=atan(c);
e=(sin(b))+(atan2(a,b));
f=(fmax(e,f))*(tan(c));
if(isless(b,e)){
b=(sin(d))/(fmin(a,c));
f=(pow(a,a))-(ceil(b));
}
}