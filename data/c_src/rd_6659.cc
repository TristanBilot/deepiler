#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmin(c,b))/(acos(f));
c=(sqrt(f))+(fdim(e,f));
f=(atan2(a,e))/(pow(b,a));
e=(atan2(d,c))+(tan(c));
while(isless(f,c)){
b=(fmax(a,d))*(fmin(b,f));
f=log(c);
b=(acos(a))-(fdim(a,a));
f=(sqrt(a))-(fdim(a,b));
c=sqrt(b);
}
b=cos(d);
b=atan2(e,d);
}