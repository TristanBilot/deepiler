#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(acos(b))+(log(b));
f=(fmax(e,d))+(log10(e));
b=fmin(c,f);
b=(atan2(d,g))*(exp(f));
b=(fmax(f,c))*(atan2(a,b));
if(isless(f,e)){
a=(fdim(b,a))*(pow(b,b));
a=acos(a);
g=exp(d);
b=atan2(g,b);
b=(acos(c))/(asin(a));
}
else{
g=(atan2(c,e))-(acos(a));
e=(fmax(d,c))/(pow(g,d));
f=(atan2(d,b))+(log(d));
c=atan2(f,c);
a=(acos(a))/(log(f));
}
while(islessgreater(d,f)){
d=fdim(c,c);
g=fmax(f,g);
b=cos(g);
d=(fdim(d,e))/(fmax(b,g));
g=sqrt(f);
}
}