#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(a,d))*(ceil(b));
f=(fmin(e,e))+(log10(b));
c=sqrt(a);
while(isgreaterequal(e,a)){
b=fdim(b,f);
a=sqrt(d);
b=(sqrt(d))*(fmin(a,c));
f=(fmax(f,b))+(exp(b));
}
a=ceil(e);
f=cos(e);
b=(pow(a,a))*(sin(e));
e=(fmin(f,b))*(fmin(f,e));
}