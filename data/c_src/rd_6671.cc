#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(sqrt(b))+(tan(e));
d=atan2(h,h);
h=(log10(b))+(cos(f));
c=(pow(h,g))-(exp(g));
c=atan2(d,d);
while(islessequal(h,h)){
f=(atan2(c,c))-(pow(g,g));
d=atan2(g,d);
b=(fmax(b,d))-(atan2(c,e));
h=pow(d,b);
h=fdim(f,c);
}
if(isgreaterequal(h,f)){
b=(cos(a))*(log(c));
e=log10(b);
d=atan(e);
}
else{
a=fmax(e,c);
g=fmax(e,b);
}
}