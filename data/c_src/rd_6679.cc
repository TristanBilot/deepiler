#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log10(a);
f=(fmax(f,e))-(atan2(a,f));
if(isless(a,c)){
d=(pow(e,c))*(ceil(e));
c=(atan(c))*(fmax(a,b));
e=fdim(b,c);
c=(sqrt(d))-(sqrt(e));
}
else{
f=acos(g);
c=(log(g))+(atan(a));
}
if(isless(c,g)){
f=(ceil(b))-(log(b));
e=fmin(c,g);
e=atan2(e,f);
f=(pow(g,c))-(sqrt(f));
}
}