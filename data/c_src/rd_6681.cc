#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(a,a);
a=ceil(b);
if(islessgreater(e,c)){
e=(atan(d))/(asin(e));
c=(atan2(e,c))*(atan(b));
d=atan2(c,d);
c=pow(d,c);
b=(cos(c))-(floor(d));
}
else{
b=(atan2(c,a))+(fmin(e,c));
c=ceil(e);
c=pow(c,d);
}
while(islessequal(e,c)){
e=(pow(a,d))-(asin(a));
a=fmax(d,a);
}
}