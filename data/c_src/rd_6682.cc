#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=cos(d);
b=(atan2(d,d))*(fdim(e,a));
d=(pow(c,a))*(exp(b));
if(islessequal(c,d)){
b=(fmin(a,b))+(asin(a));
e=log(a);
a=(pow(e,e))/(ceil(b));
d=atan(a);
e=(atan2(e,c))+(log(b));
}
else{
a=(fmax(a,a))-(sin(c));
b=(sqrt(b))+(atan2(d,a));
}
if(isgreaterequal(c,d)){
b=(cos(a))*(sqrt(d));
b=(fmax(b,e))-(atan(c));
e=(sin(a))-(pow(e,a));
}
else{
e=fdim(a,a);
a=(fmin(d,d))*(log10(a));
e=atan(e);
}
}