#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(g,b))+(fmin(f,e));
c=log10(c);
g=(atan2(b,d))*(atan2(b,d));
a=atan2(g,c);
if(isgreaterequal(e,d)){
a=(cos(b))/(fmax(c,g));
d=fdim(f,e);
e=(log10(c))/(pow(b,g));
b=(sqrt(f))/(fmax(b,d));
g=(fmax(g,e))-(fdim(c,c));
}
while(islessequal(e,g)){
f=floor(g);
b=tan(d);
c=asin(a);
f=fmax(b,b);
}
}