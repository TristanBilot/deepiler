#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(c,e))*(exp(e));
d=fdim(f,e);
h=(sin(d))-(fdim(a,b));
h=ceil(a);
a=log10(b);
e=ceil(e);
g=tan(g);
h=fmax(f,a);
if(islessgreater(d,c)){
f=sin(d);
g=(sin(a))+(fmax(f,f));
g=cos(f);
c=atan2(h,e);
d=atan2(b,b);
}
else{
b=ceil(d);
c=atan(g);
e=acos(c);
d=asin(b);
}
}