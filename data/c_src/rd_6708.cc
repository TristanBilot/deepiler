#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan(g))/(fmax(a,c));
a=(pow(b,g))+(pow(c,a));
e=exp(e);
while(islessequal(e,e)){
a=(ceil(b))-(tan(c));
c=(acos(a))/(pow(c,d));
b=(floor(c))/(atan(f));
d=(exp(a))*(log10(b));
g=floor(g);
}
while(islessequal(e,b)){
d=(ceil(f))-(fmax(g,e));
g=log(d);
}
}