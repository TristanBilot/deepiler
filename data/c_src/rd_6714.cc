#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=tan(e);
e=(ceil(d))*(fmax(a,b));
g=(fmax(f,c))+(cos(c));
c=fmax(a,g);
b=(atan2(d,a))*(fdim(e,f));
f=fmax(d,e);
f=(pow(d,c))/(acos(a));
d=(sin(e))-(log10(c));
a=cos(c);
e=(pow(f,a))/(fdim(c,d));
g=fmin(f,b);
}