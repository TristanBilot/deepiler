#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(a,c);
a=(atan2(d,d))+(ceil(c));
e=fmax(b,b);
c=(exp(c))+(fdim(b,a));
if(islessgreater(d,b)){
e=fmin(c,d);
c=log(a);
b=(sin(b))+(asin(b));
e=atan2(b,c);
d=(fmin(d,b))*(acos(e));
}
e=atan2(a,e);
e=(sqrt(e))-(fmin(e,b));
c=atan(c);
a=acos(b);
}