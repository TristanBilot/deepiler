#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(pow(f,g))-(exp(e));
d=(fmax(c,d))-(fmax(e,c));
h=(floor(c))-(cos(f));
a=(tan(a))-(atan(b));
a=ceil(h);
c=asin(f);
e=(log10(d))*(pow(b,b));
a=(atan2(c,f))-(log(d));
b=fmin(f,b);
b=(exp(h))*(asin(a));
e=(floor(a))+(fdim(e,b));
c=(pow(f,e))*(floor(e));
}