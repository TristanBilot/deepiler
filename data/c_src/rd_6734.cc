#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(d);
e=fdim(b,a);
c=(fmax(a,d))+(tan(a));
e=(fdim(b,e))-(floor(d));
e=atan2(e,e);
a=cos(a);
d=exp(a);
b=sqrt(c);
b=(fdim(b,c))+(log(c));
if(isless(b,a)){
c=(sin(b))/(ceil(c));
b=(atan2(a,e))*(atan(a));
}
}