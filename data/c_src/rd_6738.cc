#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(b);
a=fdim(c,a);
e=tan(a);
if(islessequal(e,e)){
d=(fdim(e,a))+(floor(e));
b=ceil(e);
}
if(islessequal(b,a)){
a=floor(a);
a=(fmin(c,c))/(tan(b));
}
else{
a=fmin(e,d);
a=(cos(d))+(fdim(c,b));
d=(tan(b))+(sin(b));
a=(cos(b))-(fdim(c,c));
}
}