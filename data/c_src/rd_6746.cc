#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=ceil(a);
e=fmax(a,c);
e=(fdim(e,b))+(fmax(f,a));
f=(log(a))/(fdim(e,a));
if(isgreaterequal(c,f)){
b=(asin(b))-(fmax(a,c));
e=atan2(d,b);
c=(tan(e))/(pow(c,b));
a=(log(b))-(atan2(c,a));
}
else{
c=(log(b))+(exp(c));
e=(pow(e,d))+(sin(b));
d=(fdim(b,e))*(fmax(c,c));
a=(cos(b))/(fmax(c,a));
d=tan(b);
}
a=pow(e,c);
a=atan2(f,c);
b=pow(d,b);
}