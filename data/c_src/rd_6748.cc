#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=acos(c);
c=ceil(g);
d=fdim(g,a);
e=log10(b);
while(islessgreater(f,b)){
f=(log10(c))+(fdim(g,a));
f=(sin(b))*(cos(a));
a=fmin(d,e);
}
}