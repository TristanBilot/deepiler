#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(acos(c))+(fmax(b,e));
a=asin(a);
c=fmin(d,e);
a=(sqrt(c))-(pow(b,c));
b=(tan(a))*(fmax(d,e));
while(isless(b,c)){
c=(log10(d))/(fmax(e,b));
a=atan(e);
a=(log10(c))+(tan(c));
a=(atan(a))/(log10(a));
}
e=(sqrt(b))*(log10(d));
b=(atan(a))+(log10(a));
d=(log10(c))+(fdim(a,b));
}