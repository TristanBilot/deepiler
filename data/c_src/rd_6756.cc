#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(f);
h=acos(a);
b=sqrt(e);
c=(fmin(g,e))*(asin(e));
g=fmin(g,g);
e=atan2(c,b);
e=cos(g);
c=(sin(f))*(cos(f));
g=(ceil(b))/(sqrt(b));
if(isless(a,a)){
f=(fdim(b,b))/(sin(e));
b=(asin(d))/(pow(f,f));
g=log10(a);
}
else{
a=fmax(f,g);
a=log10(d);
a=fdim(d,c);
d=fmax(g,g);
}
}