#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(g,d);
f=fmin(d,c);
h=(sin(a))*(log10(f));
f=atan2(d,d);
c=log10(d);
while(isgreaterequal(f,g)){
g=(tan(d))+(floor(b));
b=(tan(h))-(cos(d));
f=(atan(g))*(cos(g));
b=(fdim(c,a))+(fdim(c,g));
a=fmax(b,a);
}
h=(sqrt(e))*(fdim(d,a));
c=(atan2(f,b))-(exp(f));
d=atan(f);
}