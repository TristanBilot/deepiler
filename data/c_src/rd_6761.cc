#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(d))+(sin(c));
b=(fmin(a,e))*(asin(c));
a=log(e);
while(isgreaterequal(b,e)){
e=pow(b,b);
a=(fmax(c,c))+(atan2(b,c));
e=(pow(e,e))*(pow(b,c));
}
if(isgreaterequal(b,e)){
a=(fmax(e,b))+(fdim(a,a));
d=(sqrt(a))*(pow(a,b));
}
}