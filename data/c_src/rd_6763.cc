#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,b);
b=(fmin(e,d))/(ceil(c));
b=(fdim(c,b))*(acos(e));
d=fdim(a,a);
b=fmin(c,b);
if(islessgreater(d,e)){
a=atan2(d,e);
e=log10(e);
d=(fmin(c,e))+(fmax(a,a));
}
else{
d=(acos(b))-(asin(b));
c=(fdim(d,a))+(sin(a));
b=cos(d);
c=(exp(d))*(ceil(d));
c=(pow(e,e))+(log10(c));
}
while(islessgreater(a,b)){
e=tan(c);
a=(asin(d))+(pow(c,e));
}
}