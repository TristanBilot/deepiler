#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan2(c,d))+(sin(d));
h=fmin(f,e);
while(isgreaterequal(a,e)){
e=acos(f);
h=atan2(c,h);
c=(fmax(c,g))/(fdim(f,h));
f=fmin(f,b);
}
if(isgreaterequal(b,d)){
b=fmax(h,d);
b=pow(a,e);
e=(fmax(b,h))-(fmax(b,h));
g=(pow(g,e))+(fmin(e,a));
}
}