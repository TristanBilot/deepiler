#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=pow(d,a);
e=(fmax(b,f))+(pow(f,c));
c=(pow(e,c))*(acos(a));
d=(cos(d))*(sqrt(a));
b=(fmin(g,g))-(log(c));
g=(fdim(g,d))/(fmax(g,a));
a=(ceil(b))-(sin(b));
e=(log(b))-(fmin(e,b));
e=(log(g))/(sin(e));
d=fmax(d,d);
}