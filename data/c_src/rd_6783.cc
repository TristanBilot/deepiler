#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(d,a))*(exp(d));
c=(fmax(c,c))+(fmax(a,a));
e=(log(c))*(sqrt(b));
a=(pow(b,e))-(atan2(a,e));
e=atan2(a,e);
c=atan2(a,c);
e=fmin(b,b);
b=(floor(e))*(tan(a));
if(isgreaterequal(c,d)){
b=log10(d);
e=fmax(d,a);
b=fmax(c,d);
c=(fmax(d,a))-(fmin(d,b));
e=floor(a);
}
}