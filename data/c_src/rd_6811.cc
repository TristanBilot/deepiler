#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=exp(e);
f=(fmin(d,g))+(fdim(c,g));
g=(log(d))+(pow(f,e));
d=fmin(e,e);
c=(sin(b))-(atan(g));
f=pow(b,a);
c=(fmax(d,e))+(pow(a,a));
b=fmax(b,e);
if(isless(g,a)){
f=pow(d,d);
c=(tan(f))*(fdim(g,c));
}
}