#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(d,a);
a=atan2(e,e);
e=(acos(e))-(ceil(d));
if(isless(d,d)){
d=pow(e,c);
d=(acos(d))-(acos(d));
}
while(isgreaterequal(b,b)){
b=exp(d);
e=floor(c);
d=(pow(c,b))/(atan(e));
c=(fmin(b,d))*(sin(c));
a=(exp(d))/(exp(d));
}
}