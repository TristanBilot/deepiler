#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(f,d))+(fmax(a,c));
f=fmax(b,a);
c=(exp(a))-(fmax(a,b));
d=cos(f);
while(islessequal(f,c)){
c=fmax(c,f);
e=(pow(a,c))/(sin(b));
c=atan(b);
}
c=sqrt(d);
c=atan2(a,c);
a=exp(a);
b=ceil(d);
}