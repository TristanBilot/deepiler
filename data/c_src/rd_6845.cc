#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(c,e);
g=(floor(d))*(log(c));
b=(fmin(a,e))*(fmin(e,d));
h=sqrt(f);
f=(pow(g,h))-(atan2(a,f));
if(isless(c,c)){
e=fmax(b,b);
f=(asin(h))*(sin(b));
}
while(isgreaterequal(h,g)){
b=(atan2(e,f))+(log10(a));
f=(atan2(d,d))*(asin(h));
c=acos(a);
}
}