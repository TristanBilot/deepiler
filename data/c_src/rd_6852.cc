#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=asin(c);
e=(fdim(a,a))*(log(b));
b=(tan(b))-(cos(a));
a=(floor(a))*(fdim(b,d));
c=(fdim(e,a))+(cos(a));
e=(sin(a))/(fmin(e,e));
a=(log(d))*(fdim(b,c));
if(islessgreater(d,a)){
a=(sin(c))*(pow(e,c));
e=(tan(e))+(log10(d));
d=fmax(c,c);
}
}