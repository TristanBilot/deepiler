#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(exp(c))*(atan(b));
d=(atan2(c,b))/(atan(e));
g=floor(a);
e=cos(e);
d=(fmin(g,d))-(fmax(d,f));
if(islessgreater(g,b)){
e=(atan(a))-(log(d));
a=(ceil(b))/(atan2(c,d));
}
else{
e=pow(a,b);
c=cos(f);
e=fdim(c,b);
a=acos(d);
}
if(islessequal(b,g)){
f=(log10(d))*(fdim(e,g));
c=(exp(g))-(fmin(c,d));
f=(tan(c))-(fmin(g,g));
}
}