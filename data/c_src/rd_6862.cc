#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(d,b);
b=(exp(b))/(pow(e,b));
if(isgreaterequal(a,e)){
a=(fdim(c,b))/(fmin(a,b));
c=(pow(e,c))*(fmin(c,b));
d=sin(d);
a=atan2(b,a);
}
else{
e=fdim(c,a);
a=(ceil(c))*(log(a));
c=(cos(b))*(fmax(e,a));
}
while(isless(e,b)){
a=log10(b);
b=(floor(b))/(pow(c,c));
e=acos(c);
e=log10(c);
}
}