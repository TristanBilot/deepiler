#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(asin(e))/(fmin(g,a));
b=pow(f,h);
e=(asin(c))-(pow(e,g));
f=(tan(g))/(fmin(h,h));
if(isless(b,f)){
e=(atan2(a,b))-(cos(h));
a=atan2(a,f);
a=log10(d);
d=ceil(g);
}
else{
f=log10(e);
h=fdim(d,c);
}
if(islessequal(e,c)){
e=(ceil(h))-(pow(g,g));
h=pow(f,a);
e=fdim(a,d);
}
}