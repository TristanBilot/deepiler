#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(a,b))*(floor(e));
c=log(c);
e=fmax(b,e);
a=sin(c);
if(islessgreater(d,d)){
b=fmax(a,e);
b=fmin(e,c);
d=(atan2(c,d))-(fmax(e,c));
c=(pow(d,e))-(asin(b));
e=(log10(b))*(fmin(a,c));
}
else{
b=tan(c);
b=(fmin(a,c))/(fmin(e,d));
c=fmin(d,a);
}
}