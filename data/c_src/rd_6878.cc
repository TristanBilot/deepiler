#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(d,c))*(atan2(a,b));
d=pow(c,c);
a=(pow(c,e))*(tan(b));
e=(sqrt(f))*(fdim(a,f));
g=fdim(a,d);
f=atan2(d,f);
c=(fdim(e,g))-(atan2(d,g));
if(islessequal(f,c)){
g=tan(b);
f=log10(g);
e=floor(f);
e=acos(e);
e=atan(g);
}
else{
d=atan(b);
f=ceil(g);
a=(sin(a))/(fmax(g,f));
}
}