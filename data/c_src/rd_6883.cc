#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(log(a))*(exp(c));
a=(fmax(d,d))+(pow(f,e));
c=asin(e);
a=sqrt(b);
if(isgreaterequal(a,a)){
d=(atan2(b,d))+(log10(c));
c=exp(a);
b=acos(e);
}
else{
d=log(e);
b=(fmax(e,f))*(tan(a));
}
d=fmax(c,f);
a=(floor(a))/(cos(a));
c=(sqrt(f))*(sin(b));
a=(pow(d,a))*(ceil(e));
}