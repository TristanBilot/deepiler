#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(c);
e=(pow(a,b))*(log10(c));
while(islessgreater(c,a)){
a=sqrt(a);
e=(fdim(e,b))-(log(a));
a=(exp(e))/(cos(c));
c=(sin(d))*(fmin(e,d));
}
if(islessgreater(c,e)){
c=(atan2(b,c))+(sqrt(b));
b=(asin(e))*(asin(c));
}
}