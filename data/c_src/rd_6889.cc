#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(d,a);
d=log10(c);
f=fdim(a,b);
a=pow(d,f);
e=acos(e);
a=tan(b);
while(islessequal(a,f)){
c=(atan(f))/(cos(e));
e=pow(f,b);
f=pow(c,c);
f=(tan(f))+(fmax(c,a));
d=(log10(f))/(pow(b,a));
}
}