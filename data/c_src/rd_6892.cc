#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(g,f))-(exp(e));
f=(fdim(g,e))/(acos(d));
while(isless(f,b)){
e=log10(g);
d=(atan2(b,b))-(fmax(f,a));
g=(fmin(e,d))-(atan(c));
f=log10(d);
d=(pow(a,g))/(fmin(b,e));
}
if(islessequal(a,f)){
c=atan2(b,f);
g=asin(c);
}
}