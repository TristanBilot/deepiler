#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log(c);
d=atan2(e,d);
d=pow(c,c);
if(isless(b,e)){
d=pow(c,b);
f=(sqrt(e))+(fdim(a,e));
e=exp(a);
a=log(f);
f=exp(f);
}
else{
d=ceil(a);
a=(cos(b))*(atan2(d,d));
b=fmax(b,e);
}
while(isless(b,b)){
b=(fdim(a,b))-(log10(d));
f=(floor(b))+(ceil(d));
a=(pow(b,f))-(exp(d));
c=fmin(b,e);
}
}