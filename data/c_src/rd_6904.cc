#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=acos(a);
d=floor(d);
d=sin(b);
b=(tan(b))-(fmin(b,d));
e=fmax(b,b);
b=(ceil(a))-(atan2(d,a));
b=log10(e);
a=(pow(c,e))/(cos(c));
}