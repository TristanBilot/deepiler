#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(d,e))-(pow(a,f));
f=(atan2(f,a))*(ceil(a));
d=fmax(b,a);
e=(atan2(f,a))-(atan2(f,f));
if(islessequal(d,d)){
a=(fmin(b,a))/(fmax(d,d));
f=(fdim(b,d))/(atan(f));
a=atan2(e,d);
}
else{
a=(log10(b))+(log(a));
f=(tan(a))/(fmax(c,f));
e=sqrt(d);
f=(log10(e))-(ceil(a));
}
c=(fdim(e,d))*(cos(d));
b=(fmax(b,d))*(fmin(c,d));
}