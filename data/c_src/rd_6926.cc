#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan(g);
f=(exp(b))/(fmax(c,d));
while(islessequal(b,e)){
b=(cos(g))/(sin(d));
d=floor(d);
e=(log10(d))*(ceil(d));
e=fmin(a,a);
}
if(isgreaterequal(e,b)){
c=fmax(a,g);
e=cos(a);
c=(floor(b))*(asin(b));
a=(sqrt(b))+(fmin(g,d));
f=(sqrt(f))/(floor(c));
}
else{
a=(sqrt(f))*(exp(b));
g=fmin(a,e);
b=(asin(c))*(fmax(d,d));
}
}