#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(c,e))-(fdim(d,c));
c=(atan2(a,b))+(log10(a));
d=fdim(e,c);
b=fmax(a,e);
c=(fmin(d,e))*(fmin(b,d));
if(islessequal(e,c)){
e=fdim(b,d);
d=floor(b);
d=atan2(a,d);
}
else{
c=(fmax(a,c))*(atan(b));
a=cos(d);
a=(pow(c,b))+(cos(b));
}
e=(fdim(e,e))-(fmax(e,d));
c=fmax(b,b);
d=sin(e);
b=(ceil(a))*(atan2(d,a));
a=asin(c);
}