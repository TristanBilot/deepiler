#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(d,a))-(pow(d,d));
b=(pow(c,b))-(log10(e));
a=fmax(a,c);
c=(log(d))*(fmin(e,a));
if(islessequal(d,c)){
c=atan(a);
e=(pow(a,d))*(fdim(c,e));
a=(pow(a,e))*(fdim(d,b));
}
b=(pow(b,c))*(asin(d));
a=(exp(e))+(fdim(a,a));
c=floor(e);
a=(pow(a,b))/(acos(c));
}