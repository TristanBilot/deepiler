#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(asin(d))-(atan2(a,b));
e=fmin(e,d);
d=tan(b);
d=(asin(b))-(fdim(d,a));
if(isless(a,b)){
c=(asin(b))*(sin(a));
b=(ceil(c))/(fmax(a,c));
a=(log10(e))*(fmin(d,e));
c=asin(c);
}
c=floor(b);
c=fmax(d,e);
c=(fmax(b,a))*(ceil(d));
}