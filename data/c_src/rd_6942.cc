#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sqrt(c);
e=fdim(a,h);
b=(fmin(c,f))-(fmin(a,d));
c=pow(f,a);
e=(fmax(e,h))/(ceil(h));
while(isless(g,e)){
d=(pow(g,c))*(sqrt(a));
b=tan(c);
h=(fmin(e,e))+(fdim(b,c));
}
if(islessgreater(a,b)){
b=atan2(d,e);
f=pow(h,a);
h=sqrt(f);
d=(log10(e))+(asin(h));
}
else{
c=(exp(c))*(fmax(h,d));
b=(ceil(f))/(atan2(c,a));
}
}