#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(sin(c))+(exp(e));
b=fdim(a,g);
b=(pow(d,b))-(fdim(d,h));
if(isgreaterequal(h,b)){
a=log(h);
c=fdim(e,h);
b=(sqrt(b))*(fdim(g,h));
}
if(islessequal(a,g)){
c=(sqrt(a))*(fmin(e,c));
c=(fdim(c,f))+(fmax(f,c));
}
else{
b=(fmax(f,c))+(fdim(h,g));
b=sqrt(h);
g=sqrt(e);
}
}