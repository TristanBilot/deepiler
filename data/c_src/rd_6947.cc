#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(c,a);
e=(fmin(b,e))/(atan2(a,c));
while(isgreaterequal(c,e)){
f=fmax(f,c);
a=floor(b);
}
if(isgreaterequal(d,b)){
g=(floor(b))*(fmin(f,g));
b=fdim(f,a);
}
else{
f=fmin(f,c);
c=(acos(b))*(fdim(e,a));
d=(pow(c,f))+(sqrt(a));
}
}