#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(c,e);
c=pow(e,a);
b=floor(e);
if(islessgreater(d,c)){
a=atan2(c,d);
d=sqrt(b);
}
else{
d=log10(b);
d=tan(e);
b=sqrt(e);
c=atan(a);
c=sqrt(e);
}
while(isless(b,d)){
c=fmin(a,d);
c=(asin(a))-(pow(a,e));
b=(exp(c))-(floor(a));
e=sin(c);
}
}