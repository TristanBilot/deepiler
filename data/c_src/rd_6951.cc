#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(d,b))*(fmin(a,d));
d=(fmax(e,e))-(atan2(d,e));
a=fmin(d,b);
e=fmax(a,a);
if(isless(c,d)){
a=tan(a);
d=fmin(d,c);
}
else{
d=(atan(a))+(fmin(c,e));
c=fmin(c,d);
b=fdim(c,b);
}
d=(fdim(a,c))+(exp(a));
b=(fmax(e,d))/(fmin(e,d));
a=fmin(c,c);
e=(fmin(d,b))*(log(c));
d=(log(a))+(pow(a,c));
}