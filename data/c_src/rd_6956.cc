#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fdim(a,a))/(log(b));
b=cos(b);
c=pow(d,e);
g=fmax(e,b);
b=pow(e,e);
g=(ceil(e))/(fmin(f,a));
c=fdim(e,c);
d=pow(e,c);
g=(acos(b))-(exp(f));
}