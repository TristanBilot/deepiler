#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(a);
e=fdim(b,a);
b=(fmax(d,e))*(ceil(b));
while(isgreaterequal(c,c)){
a=(fmax(e,a))/(ceil(c));
b=(pow(e,a))-(log10(b));
c=sin(c);
c=sqrt(b);
}
if(islessequal(a,e)){
d=(asin(c))/(log(e));
d=fmin(e,d);
c=(floor(b))+(fdim(e,e));
}
}