#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(a,b))/(fdim(a,d));
a=atan(b);
b=fmax(c,c);
b=log10(d);
while(isless(e,a)){
e=(ceil(a))/(cos(d));
a=(pow(e,c))/(atan2(c,a));
b=fmin(e,c);
}
while(isless(d,d)){
e=(fmin(e,c))+(atan2(c,d));
e=fmax(b,d);
}
}