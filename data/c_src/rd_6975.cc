#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan(c);
g=(fmin(e,g))+(log(d));
while(islessequal(b,f)){
h=(cos(c))-(acos(h));
c=(acos(b))*(log(c));
d=atan2(e,h);
e=fdim(f,e);
e=(log10(c))*(log10(h));
}
a=(acos(c))+(log(e));
d=(log10(a))/(pow(b,c));
}