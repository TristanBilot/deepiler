#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(a,c);
c=(cos(d))/(atan2(e,e));
c=(fmax(b,d))/(atan(a));
if(isgreaterequal(c,c)){
d=fmin(a,e);
c=sqrt(c);
c=(pow(a,b))-(fmin(c,a));
}
else{
d=(fmin(b,e))+(fdim(e,a));
f=pow(b,f);
}
d=(fmax(f,c))*(atan2(d,a));
a=fmax(b,f);
c=(log(d))/(fdim(b,e));
b=sin(d);
c=pow(b,d);
}