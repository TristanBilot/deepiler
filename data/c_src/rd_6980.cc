#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(c);
d=(exp(b))-(exp(c));
c=(tan(f))*(sqrt(d));
f=acos(c);
b=(fdim(c,e))+(log10(e));
if(islessequal(g,e)){
e=pow(h,b);
d=cos(e);
a=atan2(h,h);
}
else{
d=exp(g);
h=(atan2(c,g))+(sin(a));
a=sin(b);
e=(pow(h,g))*(acos(f));
b=(fmax(e,a))-(sqrt(e));
}
if(islessgreater(e,g)){
e=(fmax(d,c))/(atan(g));
c=(tan(g))/(floor(g));
f=(sqrt(f))-(atan2(d,b));
f=(fmax(a,b))-(sqrt(a));
g=exp(h);
}
else{
d=fmin(c,h);
h=(sin(b))-(fmax(c,d));
h=fdim(a,e);
a=(pow(g,b))*(atan(f));
}
}