#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan2(e,c);
e=(atan2(d,c))+(floor(g));
f=asin(e);
f=(pow(c,b))*(ceil(c));
if(islessequal(b,e)){
g=(sqrt(f))*(fmin(c,g));
e=fmin(a,g);
c=fdim(g,e);
g=(atan(b))*(exp(a));
g=floor(d);
}
else{
b=(fmin(d,a))*(pow(f,b));
g=fmax(f,c);
c=(cos(c))-(pow(e,b));
a=(fmin(c,a))+(tan(a));
}
b=(atan(b))-(pow(e,d));
b=fdim(g,a);
b=floor(a);
a=fmax(f,d);
d=(atan2(a,g))-(fdim(d,e));
}