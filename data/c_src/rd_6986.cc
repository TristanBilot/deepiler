#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmax(f,a))/(asin(d));
f=(log10(g))+(sin(d));
a=tan(g);
e=pow(a,f);
a=(pow(e,a))+(cos(c));
c=(log(b))*(asin(g));
g=pow(a,b);
b=(ceil(c))-(log10(b));
if(isgreaterequal(e,b)){
a=tan(g);
g=(log(d))/(ceil(b));
b=(fdim(b,a))*(fdim(c,f));
e=log10(a);
}
else{
g=(pow(f,b))-(log10(a));
d=atan2(f,c);
e=atan2(e,d);
}
}