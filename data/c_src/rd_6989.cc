#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan(a))+(exp(e));
c=(fmax(a,a))+(fdim(d,e));
a=fdim(d,b);
b=(tan(d))/(asin(a));
a=atan2(e,a);
while(isgreaterequal(e,b)){
d=(log(e))/(fmin(e,e));
e=acos(d);
a=(fmax(e,e))*(pow(b,d));
d=(atan(b))*(fmin(a,d));
b=fmax(c,e);
}
}