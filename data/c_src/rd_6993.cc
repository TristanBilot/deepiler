#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(b);
f=pow(f,g);
a=(fdim(g,c))+(atan2(d,c));
f=sin(g);
h=floor(a);
if(isless(c,f)){
d=(tan(h))+(atan(b));
e=(sqrt(g))+(atan(e));
}
e=fmin(h,h);
a=fmin(f,d);
}