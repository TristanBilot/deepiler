#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(atan2(f,c))-(exp(c));
a=ceil(f);
g=pow(b,f);
e=(tan(a))-(fmax(b,h));
b=(fmax(a,c))*(fdim(b,a));
while(islessequal(e,b)){
g=atan2(d,c);
e=(sin(g))+(sin(a));
}
a=sin(f);
c=(atan2(g,e))/(atan2(a,d));
a=log10(c);
c=atan2(e,g);
}