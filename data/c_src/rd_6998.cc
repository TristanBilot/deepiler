#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(a);
c=fmax(c,d);
b=fdim(c,d);
a=log(c);
b=(fmax(b,b))*(atan2(c,d));
if(isless(b,c)){
a=(fmax(d,b))*(atan2(c,e));
d=atan2(c,d);
d=(sin(b))-(atan2(b,a));
b=(fmax(b,b))*(fmax(c,c));
d=cos(c);
}
b=(fmin(e,e))/(sqrt(c));
d=(cos(e))+(exp(d));
c=atan(b);
a=(fmax(c,a))*(pow(b,a));
b=(acos(e))-(asin(d));
}