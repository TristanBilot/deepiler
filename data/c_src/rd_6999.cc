#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=floor(b);
a=log10(g);
c=log(a);
a=fdim(f,c);
a=acos(b);
f=(floor(c))+(sin(a));
d=sqrt(b);
f=(log(f))/(fmax(f,a));
d=pow(c,d);
a=(fmax(a,d))*(atan2(e,g));
e=fdim(d,g);
d=(log10(e))/(cos(a));
}