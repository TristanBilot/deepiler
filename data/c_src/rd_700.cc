#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,e))/(pow(c,b));
a=fmax(b,c);
c=(fmin(d,d))-(pow(c,e));
if(islessgreater(b,d)){
a=(atan2(b,a))*(fdim(d,a));
c=(ceil(b))*(floor(a));
}
else{
b=(log10(b))-(pow(a,a));
c=(log(e))+(fmax(e,b));
a=(pow(a,e))*(atan2(c,b));
}
while(islessequal(b,b)){
e=log(a);
b=(sqrt(c))+(pow(c,b));
c=fmin(c,e);
b=floor(d);
d=fmin(a,a);
}
}