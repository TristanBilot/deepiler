#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(exp(b))/(log(e));
d=(atan2(g,a))-(atan2(b,b));
if(isless(d,a)){
d=(pow(a,a))/(ceil(g));
f=asin(e);
c=log10(f);
e=(log(c))/(ceil(h));
}
f=(pow(e,c))-(fmin(c,b));
d=pow(a,c);
g=(fmax(f,e))+(fmax(d,g));
f=pow(a,f);
d=fmax(h,f);
}