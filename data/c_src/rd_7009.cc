#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,d))+(cos(e));
e=fmin(d,b);
e=pow(d,b);
a=(floor(c))+(floor(b));
d=(fmax(d,b))*(cos(c));
a=atan2(d,c);
d=log10(c);
d=asin(b);
e=(pow(c,c))/(exp(e));
e=(fdim(a,e))-(floor(e));
d=(asin(e))-(atan2(c,b));
}