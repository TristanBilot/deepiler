#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(d,b))*(exp(c));
a=(tan(d))+(atan2(a,d));
d=(log10(c))-(fmax(a,e));
while(islessgreater(b,c)){
a=fmax(b,d);
c=floor(e);
}
b=atan2(c,d);
a=(sqrt(d))-(fdim(e,a));
a=(exp(a))-(asin(b));
}