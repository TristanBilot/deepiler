#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=acos(f);
b=(fmin(e,f))/(exp(a));
c=(fdim(b,c))/(atan(e));
f=fmax(d,b);
while(islessgreater(a,a)){
d=fmax(b,a);
a=(atan2(d,f))/(fdim(a,d));
}
if(isless(d,e)){
b=(log(a))+(pow(e,f));
e=sin(b);
a=fmin(f,a);
d=pow(e,d);
}
else{
b=(floor(b))/(acos(f));
a=(fmax(c,b))/(atan2(b,b));
b=(fdim(e,d))/(fmax(e,a));
c=(atan2(b,a))/(exp(e));
}
}