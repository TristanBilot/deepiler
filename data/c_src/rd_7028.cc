#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=sin(b);
d=(atan2(a,d))/(fmax(c,a));
e=pow(a,c);
e=(pow(b,e))/(fmax(c,a));
a=exp(a);
e=log(e);
d=(fmax(d,d))+(exp(b));
e=fmax(a,d);
d=exp(e);
a=fdim(a,a);
}