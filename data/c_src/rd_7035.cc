#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=asin(c);
f=cos(a);
d=log10(c);
c=(pow(d,a))-(fmin(a,f));
f=(exp(b))-(exp(f));
f=(fmax(c,f))+(atan(d));
d=(asin(c))/(asin(c));
f=(sqrt(d))/(sqrt(c));
if(isgreaterequal(e,a)){
f=atan2(b,e);
e=(sin(c))*(pow(c,d));
e=fmax(f,a);
d=(sqrt(b))+(fdim(e,c));
}
}