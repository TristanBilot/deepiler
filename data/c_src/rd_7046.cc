#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(b,b);
d=acos(e);
c=(sqrt(d))-(fmin(e,a));
b=ceil(c);
a=(fmax(a,e))*(fdim(c,e));
while(islessequal(d,d)){
e=(atan(b))+(fmax(e,c));
a=(fdim(c,a))/(atan(d));
b=(exp(a))+(asin(b));
}
c=(floor(e))+(pow(a,c));
e=(floor(c))*(fdim(c,b));
}