#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(tan(h))+(sin(c));
b=fmax(d,g);
g=tan(c);
c=tan(a);
f=(log10(a))/(tan(e));
h=fdim(h,h);
b=fmax(a,c);
h=(atan(b))+(asin(a));
}