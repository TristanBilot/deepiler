#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(a);
d=(fdim(d,d))*(sqrt(c));
e=pow(d,a);
b=fmin(a,b);
a=sqrt(c);
a=fdim(b,a);
e=fdim(a,a);
d=(acos(b))/(log(e));
if(islessgreater(e,d)){
c=(floor(a))-(fdim(b,a));
e=log(b);
c=(log10(b))*(fdim(d,d));
}
}