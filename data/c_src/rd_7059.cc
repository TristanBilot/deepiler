#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(b,a);
e=sin(f);
e=exp(h);
h=atan2(f,f);
if(isless(c,e)){
h=pow(e,a);
f=pow(e,c);
e=fdim(g,h);
e=(ceil(g))/(floor(c));
d=floor(h);
}
else{
e=(atan2(g,f))+(atan(a));
b=(fdim(a,b))*(fdim(d,b));
d=(pow(d,d))/(pow(h,b));
b=(acos(e))/(fdim(d,c));
b=pow(a,d);
}
if(islessequal(b,h)){
g=(fmin(g,d))*(atan2(a,c));
a=(acos(g))-(atan2(f,g));
d=(atan2(b,c))-(floor(c));
f=fmax(g,e);
h=(log(e))*(fmin(h,h));
}
}