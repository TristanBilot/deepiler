#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(d))-(pow(c,b));
d=(acos(f))/(pow(e,g));
while(isgreaterequal(c,d)){
d=(fmin(b,b))-(cos(c));
d=(ceil(c))*(fmax(e,c));
d=fmax(c,g);
}
e=(fdim(f,d))-(sin(g));
b=(pow(c,a))+(cos(a));
b=acos(g);
d=(fdim(f,b))/(floor(g));
g=(ceil(b))*(sin(d));
}