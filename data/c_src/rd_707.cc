#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(a,d))/(asin(e));
e=(ceil(b))-(tan(c));
if(islessequal(e,e)){
c=atan2(a,c);
e=sqrt(e);
a=(floor(d))/(ceil(a));
}
if(isgreaterequal(c,d)){
c=fdim(a,c);
d=sqrt(d);
d=floor(b);
a=asin(b);
}
else{
c=fdim(e,b);
b=atan2(e,b);
e=pow(e,e);
}
}