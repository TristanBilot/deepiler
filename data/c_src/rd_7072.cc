#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(a);
c=atan2(c,b);
b=(tan(d))-(atan(d));
d=(sin(b))-(fdim(d,a));
while(islessequal(e,b)){
c=(tan(c))-(sqrt(a));
e=(tan(d))/(acos(e));
c=(fmax(c,a))/(fmin(b,d));
e=fmin(a,e);
}
}