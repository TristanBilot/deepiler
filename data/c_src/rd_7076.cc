#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(c,e))/(floor(e));
b=(tan(b))*(sin(e));
e=(ceil(f))*(fmin(e,f));
while(isless(f,a)){
b=acos(b);
a=pow(a,e);
f=ceil(d);
f=ceil(e);
b=(asin(e))/(sqrt(d));
}
g=fmax(e,b);
c=fdim(g,d);
d=(tan(d))+(atan2(e,e));
}