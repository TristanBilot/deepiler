#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(atan(a))/(fmin(e,b));
b=(fdim(e,b))-(floor(d));
while(isgreaterequal(c,a)){
a=(fmin(e,e))*(fdim(e,a));
c=fdim(d,c);
e=(pow(c,c))*(pow(d,e));
}
while(isgreaterequal(d,a)){
b=exp(c);
a=(log(d))-(fmin(a,a));
c=(sqrt(e))/(atan2(c,e));
a=exp(b);
d=(fmin(c,c))-(log10(d));
}
}