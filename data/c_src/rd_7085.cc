#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(floor(d))*(atan2(f,a));
f=(pow(c,g))/(fmax(e,d));
h=(fmax(h,c))/(atan(c));
g=floor(a);
while(isgreaterequal(a,b)){
c=ceil(b);
d=(fmax(f,g))*(fmin(e,d));
e=(acos(h))-(log10(h));
}
}