#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=cos(e);
e=atan2(c,a);
a=fmax(d,e);
while(isless(a,d)){
d=(cos(a))+(ceil(b));
b=sqrt(c);
a=(fdim(a,b))/(asin(c));
e=fmax(c,c);
}
e=sin(c);
b=ceil(a);
b=(fdim(c,d))-(atan2(b,a));
}