#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=cos(d);
f=(fmin(e,a))-(fmin(c,c));
e=(pow(f,d))/(asin(d));
while(isless(f,e)){
d=fdim(f,d);
f=fmin(b,e);
f=(atan2(f,d))/(pow(d,f));
}
e=floor(c);
b=fdim(f,b);
d=atan2(d,e);
c=(fdim(e,e))-(fmax(e,f));
c=fmax(a,d);
}