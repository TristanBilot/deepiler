#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(d))/(floor(d));
b=fmin(c,c);
b=sin(c);
c=(log(d))+(sin(c));
c=sin(c);
c=(atan(b))*(tan(d));
while(isgreaterequal(c,c)){
e=(floor(b))-(fmax(c,d));
c=atan2(e,e);
d=log10(e);
}
}