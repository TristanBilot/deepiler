#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(b);
a=fmax(d,b);
a=(fdim(c,b))+(fdim(d,a));
d=fmax(b,e);
while(isless(a,e)){
e=fdim(c,e);
b=pow(b,c);
}
while(isless(e,b)){
a=fmax(d,a);
c=(atan2(a,d))*(asin(c));
c=(ceil(c))/(sqrt(a));
d=atan2(c,c);
e=fmin(d,d);
}
}