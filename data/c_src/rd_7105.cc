#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fdim(b,g);
d=(log(d))+(atan(h));
while(isless(b,c)){
a=(atan2(g,b))-(fmax(d,f));
e=sin(a);
h=(atan2(f,d))-(fmin(a,f));
}
if(isless(b,g)){
f=(atan(b))*(fmax(f,f));
g=fmax(d,a);
a=(acos(c))-(log(h));
}
else{
d=(sin(g))+(fdim(e,d));
a=(pow(g,d))-(acos(a));
}
}