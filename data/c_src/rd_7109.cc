#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=pow(c,d);
b=(sin(b))/(asin(a));
f=fmin(c,c);
g=fmin(c,c);
a=(floor(d))+(exp(b));
if(islessequal(f,d)){
d=(log(g))*(fmin(b,g));
a=(sqrt(a))+(fdim(g,b));
f=(fmin(g,e))*(fmax(e,d));
a=(atan2(e,d))-(pow(f,a));
}
else{
b=cos(g);
g=(asin(d))-(ceil(a));
}
b=exp(d);
g=(fmin(f,f))/(fmin(d,d));
g=(ceil(b))/(log10(a));
g=(fmax(a,g))*(fdim(d,f));
g=(fdim(b,e))/(log10(c));
}