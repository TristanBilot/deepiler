#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=floor(e);
e=ceil(d);
f=acos(e);
d=fmin(f,a);
while(islessgreater(b,d)){
c=(atan2(a,d))/(atan2(f,a));
a=(floor(c))*(fdim(d,b));
}
if(isgreaterequal(g,b)){
a=(fmax(d,c))*(fdim(e,f));
f=(fmax(h,f))*(pow(e,a));
c=(fmax(h,d))*(log(a));
e=fdim(b,b);
h=(tan(c))+(ceil(d));
}
else{
d=atan2(c,f);
b=atan2(a,e);
}
}