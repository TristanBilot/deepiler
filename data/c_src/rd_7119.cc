#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(b);
h=(pow(c,a))+(fdim(h,c));
if(islessequal(g,e)){
h=(atan2(h,g))+(sin(d));
c=(fmin(a,d))*(exp(c));
}
else{
d=tan(e);
f=(sin(h))/(exp(f));
}
c=(acos(f))*(ceil(b));
d=(cos(b))*(fmin(c,e));
b=log10(a);
h=(tan(g))*(fdim(g,d));
g=exp(b);
}