#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(e,f))-(fdim(g,c));
g=(asin(c))-(cos(h));
d=(exp(h))/(fdim(b,e));
c=(asin(c))-(atan2(b,d));
e=(fdim(f,c))*(cos(d));
a=tan(a);
g=(fdim(h,b))*(pow(g,f));
while(islessequal(f,h)){
f=acos(c);
f=(atan2(f,h))-(exp(b));
h=(log10(c))/(pow(g,g));
}
}