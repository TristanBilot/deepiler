#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan2(h,e))*(fdim(b,f));
f=(atan2(a,a))/(log10(f));
while(islessgreater(b,d)){
f=fdim(b,a);
c=pow(h,a);
a=fdim(a,d);
d=atan2(b,e);
e=fmax(e,c);
}
h=(fmin(b,f))+(fmax(d,g));
f=(fmax(h,d))*(atan2(d,e));
h=(fmax(d,a))/(atan(h));
}