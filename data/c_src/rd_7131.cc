#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(e,g);
a=fmin(b,e);
e=fmax(e,c);
if(islessgreater(c,d)){
a=(sqrt(b))+(sin(a));
f=(cos(c))*(acos(c));
}
else{
f=fmax(g,f);
c=(pow(c,b))-(fdim(f,a));
b=exp(f);
e=fmin(e,f);
}
while(isgreaterequal(c,g)){
b=asin(f);
a=cos(f);
g=log10(d);
}
}