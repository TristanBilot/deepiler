#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fmax(c,e))-(sin(c));
f=(fmax(d,d))/(sin(e));
a=log(d);
while(isless(a,b)){
b=fmax(b,a);
c=pow(e,c);
b=(pow(c,a))-(floor(e));
f=pow(a,b);
f=(log(c))-(atan(a));
}
while(islessgreater(c,b)){
b=(pow(c,a))*(atan(d));
a=log(f);
}
}