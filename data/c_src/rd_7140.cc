#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(c);
d=fmin(c,b);
a=(sqrt(e))/(sqrt(e));
d=(log10(d))/(tan(c));
c=fmax(a,b);
b=(atan(c))/(atan2(b,e));
a=cos(e);
a=(exp(d))*(cos(d));
e=asin(d);
d=(pow(d,e))/(acos(c));
a=(log10(c))*(sin(d));
}