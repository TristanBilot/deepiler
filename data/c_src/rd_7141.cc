#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fdim(a,c))/(fmin(d,c));
c=cos(d);
e=tan(g);
c=atan2(b,f);
g=(exp(d))+(atan2(e,b));
while(isgreaterequal(g,c)){
d=(fmax(e,d))+(atan2(c,g));
b=acos(g);
a=(atan2(a,c))+(sqrt(d));
f=(atan2(a,c))-(asin(d));
}
if(isless(g,a)){
d=(atan2(e,e))*(pow(b,b));
g=fdim(c,f);
}
}