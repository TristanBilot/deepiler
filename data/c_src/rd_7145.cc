#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(b,c))-(fmin(e,a));
f=pow(c,e);
while(islessequal(f,f)){
a=cos(f);
c=(pow(a,f))/(atan2(c,d));
}
if(isless(f,c)){
d=ceil(a);
e=fmin(c,e);
c=ceil(c);
b=sin(a);
f=fmax(e,c);
}
}