#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(a,e))+(sin(g));
a=(floor(f))*(fmax(g,c));
c=pow(e,g);
f=(log10(f))*(fdim(d,g));
a=(atan2(c,d))+(fmin(c,a));
if(isless(b,e)){
d=sin(g);
h=fmax(h,a);
e=fdim(h,c);
b=fmax(b,b);
g=fdim(c,a);
}
}