#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(b);
d=sqrt(d);
d=(asin(c))+(atan(a));
e=asin(e);
while(isgreaterequal(a,e)){
e=(floor(c))-(ceil(c));
a=atan2(d,a);
c=fmax(e,c);
}
while(isless(b,c)){
c=(tan(c))+(atan(b));
c=(log(c))-(floor(a));
e=atan(b);
b=cos(d);
}
}