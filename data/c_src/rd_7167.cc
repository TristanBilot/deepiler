#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,d);
c=fmin(d,c);
a=asin(a);
e=asin(b);
a=ceil(e);
if(islessequal(a,c)){
b=(exp(a))*(fmin(a,a));
b=atan2(c,b);
e=fmax(c,c);
b=(fmax(a,e))*(log(b));
}
e=fmax(c,b);
e=(tan(a))/(asin(b));
a=atan(d);
e=exp(e);
}