#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(a,a))-(pow(f,g));
c=fdim(b,g);
b=log10(e);
f=fmax(d,g);
f=fdim(a,h);
while(isless(d,h)){
b=(fmax(e,h))/(asin(c));
h=tan(f);
}
}