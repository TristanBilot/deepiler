#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(asin(e))-(fmax(b,a));
c=acos(d);
e=(acos(d))/(atan2(b,a));
if(islessgreater(e,d)){
d=(atan2(d,b))/(atan2(e,d));
e=fmin(d,a);
b=(log(b))/(fdim(a,b));
d=(asin(c))-(acos(c));
}
else{
e=fmin(e,a);
a=acos(d);
}
if(islessgreater(b,a)){
e=(cos(c))-(sin(a));
d=(log10(d))+(pow(d,e));
e=atan2(c,a);
}
else{
e=(fdim(c,a))-(cos(b));
b=pow(a,c);
}
}