#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=exp(e);
d=(floor(c))-(asin(a));
while(islessequal(g,a)){
c=(fmax(e,f))-(tan(g));
a=(floor(e))/(tan(a));
d=asin(e);
d=(fmax(f,b))-(exp(a));
c=(acos(d))/(fdim(d,a));
}
if(islessequal(d,f)){
d=sqrt(g);
b=fmax(c,g);
d=sin(d);
a=(fdim(g,c))/(cos(d));
}
else{
e=(sqrt(f))+(tan(d));
a=(fmax(f,e))+(exp(f));
c=exp(a);
c=fmax(c,g);
g=atan2(a,d);
}
}