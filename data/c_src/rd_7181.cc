#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=exp(b);
a=(atan2(e,c))+(atan(c));
b=(pow(e,d))+(exp(b));
while(isgreaterequal(c,d)){
a=sqrt(a);
c=(pow(e,c))+(atan(e));
b=fdim(e,b);
a=(exp(e))/(fdim(a,c));
e=(atan2(e,e))+(sin(b));
}
if(isless(e,e)){
d=log10(d);
a=(atan2(c,a))*(atan2(d,e));
c=sqrt(d);
b=(acos(a))/(tan(c));
}
else{
e=(atan2(a,a))+(floor(e));
c=cos(e);
c=(cos(d))*(ceil(d));
}
}