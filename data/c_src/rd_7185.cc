#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmax(f,b);
b=fmin(e,c);
f=pow(f,f);
f=(pow(c,b))*(asin(d));
d=(fdim(d,b))-(fmax(d,a));
e=asin(a);
a=cos(d);
if(isless(c,d)){
e=(tan(f))+(tan(a));
b=exp(c);
d=ceil(b);
}
else{
e=(fdim(f,f))-(exp(f));
d=(pow(a,b))-(pow(e,f));
b=(atan(a))*(exp(a));
}
}