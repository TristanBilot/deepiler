#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,d))/(ceil(e));
c=pow(a,d);
a=(fmin(a,d))-(sqrt(a));
c=fdim(a,e);
while(isgreaterequal(e,a)){
b=fmax(d,a);
e=(floor(b))/(fdim(b,a));
e=pow(a,c);
a=fmax(a,d);
}
while(islessgreater(e,b)){
c=(fdim(d,d))/(asin(c));
c=atan(e);
e=acos(c);
a=atan2(b,a);
}
}