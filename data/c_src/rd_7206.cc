#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(g);
e=(pow(g,a))-(atan(b));
a=(atan(c))/(atan2(g,g));
a=pow(a,f);
f=atan(g);
b=(ceil(f))*(exp(f));
if(isless(e,d)){
c=(fmax(b,f))*(log(g));
f=atan2(a,d);
}
}