#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(b,a))-(pow(e,a));
e=(fmax(d,b))/(pow(c,c));
e=(cos(e))*(log(e));
a=fdim(b,e);
b=(cos(e))+(cos(d));
a=(log10(d))-(fmin(b,d));
b=(fmax(b,d))-(fdim(c,e));
b=atan2(d,a);
if(isgreaterequal(b,b)){
e=(log(e))*(fmax(b,b));
a=ceil(c);
}
}