#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(log(b))/(cos(c));
d=fmax(a,d);
e=(fmax(a,e))-(floor(a));
while(isgreaterequal(g,a)){
f=fdim(c,b);
b=(atan2(d,f))+(pow(f,f));
b=(fdim(e,c))/(cos(e));
}
a=(exp(g))+(pow(e,d));
f=log10(a);
b=log10(d);
}