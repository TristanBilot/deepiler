#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan(c);
b=(pow(b,g))+(atan2(c,b));
g=(acos(d))+(pow(b,f));
g=fmin(b,g);
f=log10(c);
e=fmin(a,a);
b=(acos(a))*(sin(f));
c=(log10(e))+(fmax(a,c));
if(isless(b,c)){
f=acos(a);
d=(fmax(e,f))+(fmin(e,c));
b=(atan(b))-(exp(c));
a=sin(f);
c=(atan2(b,a))+(fmax(d,f));
}
}