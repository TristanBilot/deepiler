#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fdim(c,g);
e=exp(g);
e=(fmax(b,c))-(ceil(f));
d=floor(f);
f=atan(g);
f=pow(e,f);
b=(fmin(a,e))+(pow(b,c));
while(isgreaterequal(d,d)){
c=(fmax(d,f))*(tan(b));
a=(fdim(c,g))/(fmin(f,d));
b=pow(c,f);
}
}