#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(b,b))/(cos(d));
b=asin(e);
e=tan(c);
a=(fmax(c,b))/(floor(d));
c=(fdim(d,b))*(fmax(d,e));
a=fmin(e,b);
c=(cos(c))+(sqrt(d));
if(islessgreater(a,c)){
a=ceil(c);
d=(log(c))/(log10(e));
e=(floor(d))/(atan2(e,d));
}
else{
b=(cos(c))-(log10(a));
c=(pow(e,d))-(fmin(b,b));
c=(fmin(e,a))/(pow(e,b));
}
}