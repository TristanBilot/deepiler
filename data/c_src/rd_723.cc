#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(h,d))*(pow(f,d));
h=(exp(f))-(sin(h));
b=(sin(b))*(atan2(g,e));
d=(pow(a,h))-(atan(g));
d=acos(d);
g=cos(f);
d=(atan2(c,b))*(tan(h));
e=(fdim(e,e))*(cos(h));
c=fmin(d,f);
b=fdim(c,e);
d=(atan2(b,h))*(fdim(e,b));
c=(acos(c))/(fmax(d,c));
}