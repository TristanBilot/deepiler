#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(b,c);
f=tan(a);
f=(floor(b))+(tan(f));
f=atan2(b,b);
a=fmin(b,e);
a=(exp(c))/(fdim(d,a));
if(isless(a,c)){
c=fdim(e,a);
b=pow(c,c);
}
else{
c=fmin(f,a);
f=(fmin(f,e))*(log10(d));
f=cos(f);
d=fdim(f,c);
f=(floor(e))-(pow(b,c));
}
}