#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(a,a))*(cos(h));
g=(fmin(c,b))/(acos(e));
g=(atan2(b,c))+(fmax(g,e));
d=(asin(e))-(atan2(b,a));
c=exp(g);
a=pow(b,c);
c=(exp(a))-(asin(d));
b=pow(f,c);
g=fmax(a,b);
a=(sin(b))-(pow(e,h));
e=tan(e);
}