#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(a,d);
g=(atan2(g,f))*(tan(f));
f=pow(f,e);
a=(tan(a))*(atan2(a,b));
while(isgreaterequal(f,a)){
e=(fdim(c,b))*(fmin(c,b));
a=(floor(f))-(floor(e));
c=(sin(g))+(atan2(a,g));
b=(fmax(c,c))+(atan(g));
}
if(isgreaterequal(e,d)){
a=(fmin(f,e))+(exp(f));
e=(fmin(b,d))-(acos(c));
}
else{
a=log10(b);
b=(floor(e))*(atan2(d,f));
f=(fmax(f,e))/(fmin(g,f));
}
}