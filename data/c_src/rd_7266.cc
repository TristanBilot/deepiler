#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(e))*(atan2(e,c));
b=(floor(b))*(acos(a));
while(isgreaterequal(d,b)){
c=cos(d);
d=(atan2(c,c))/(atan(d));
a=(tan(e))+(fdim(b,a));
}
if(isgreaterequal(a,e)){
a=(fmax(c,c))/(atan2(a,b));
b=(pow(b,e))+(fmax(b,a));
d=(asin(b))-(ceil(c));
a=pow(a,c);
a=pow(a,b);
}
}