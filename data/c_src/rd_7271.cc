#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(d))/(fdim(a,c));
c=(exp(b))*(fdim(b,f));
while(islessequal(b,c)){
c=(fmax(a,b))/(asin(b));
e=atan(c);
a=(asin(c))+(atan2(a,c));
a=fdim(b,d);
b=atan(f);
}
a=(fdim(e,d))+(exp(c));
b=(sqrt(a))*(fmax(e,b));
e=(floor(b))-(fmin(b,d));
}