#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(b,c);
d=(sqrt(e))/(tan(a));
c=log(d);
d=(atan(a))/(exp(c));
b=(fmax(b,d))/(sin(b));
if(isless(a,e)){
e=(sqrt(c))-(fmax(d,a));
a=fmax(c,a);
d=atan(b);
}
d=pow(d,a);
a=exp(a);
d=fdim(d,c);
a=(fdim(e,c))/(pow(e,a));
e=acos(a);
}