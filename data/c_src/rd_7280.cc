#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(b,d);
e=(pow(d,d))*(tan(e));
while(isgreaterequal(c,b)){
a=(pow(d,e))*(pow(b,e));
b=(sqrt(c))/(exp(c));
}
while(isgreaterequal(c,d)){
d=(fmax(c,d))*(tan(d));
e=(asin(e))/(acos(e));
b=asin(a);
}
}