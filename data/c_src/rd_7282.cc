#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(a,a))-(log(b));
c=cos(b);
c=floor(d);
d=(pow(f,c))/(log10(e));
f=(pow(a,b))*(pow(c,e));
f=(pow(e,b))-(pow(b,f));
b=(atan2(e,a))+(fmin(e,f));
f=sqrt(c);
a=(fmax(f,f))+(atan2(f,f));
f=pow(a,a);
}