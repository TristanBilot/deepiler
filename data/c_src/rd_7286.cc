#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(d,e))-(atan2(a,a));
c=(sin(c))-(sqrt(c));
e=fdim(a,a);
b=fmax(d,b);
b=atan2(c,c);
while(islessequal(e,a)){
c=(ceil(b))*(fdim(b,e));
a=fmax(d,a);
}
if(islessequal(b,e)){
e=acos(e);
b=fdim(a,a);
e=sqrt(d);
d=cos(a);
}
}