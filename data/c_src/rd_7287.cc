#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(c,d);
a=atan2(e,f);
e=(exp(d))*(fmin(e,g));
f=(log10(g))/(sqrt(e));
a=ceil(g);
b=floor(h);
f=(pow(c,g))/(pow(b,c));
e=atan2(e,d);
e=pow(d,c);
}