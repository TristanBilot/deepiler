#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(a);
c=(pow(d,b))/(fmax(a,c));
e=(exp(a))+(log(b));
if(isgreaterequal(a,b)){
e=(asin(b))-(pow(d,b));
b=(log10(e))*(fdim(d,c));
c=atan(a);
c=(exp(b))+(tan(e));
}
d=tan(a);
e=sqrt(b);
a=sqrt(c);
}