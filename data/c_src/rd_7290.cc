#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fdim(c,c);
f=(fdim(e,c))*(asin(c));
b=(fmax(e,a))-(fmin(f,e));
e=fdim(a,e);
if(islessgreater(e,b)){
c=sqrt(a);
b=(atan2(e,b))/(sin(c));
a=fdim(a,e);
}
a=ceil(b);
b=atan2(a,e);
}