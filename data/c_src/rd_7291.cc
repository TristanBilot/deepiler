#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(d,d);
d=(pow(e,e))/(fmax(e,b));
if(isgreaterequal(b,b)){
c=asin(c);
c=(log(c))*(exp(c));
a=(atan2(d,e))+(sqrt(c));
b=(pow(e,b))*(atan(e));
c=(fmin(e,b))-(floor(e));
}
if(islessequal(a,e)){
a=(fdim(a,e))*(log10(b));
d=(pow(c,c))/(fdim(d,c));
e=tan(d);
}
}