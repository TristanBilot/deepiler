#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(c,c);
c=(ceil(a))*(pow(f,b));
b=fmin(d,d);
e=fdim(e,c);
if(isless(f,d)){
a=sin(e);
b=(ceil(d))*(floor(e));
}
else{
a=tan(c);
f=fmax(d,c);
c=cos(d);
}
f=(tan(e))-(pow(b,a));
b=(exp(b))-(atan(d));
c=sqrt(c);
d=floor(e);
d=log10(a);
}