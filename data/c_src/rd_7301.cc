#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(asin(d))/(sqrt(a));
e=fmin(c,g);
g=(fdim(a,e))*(fmax(b,a));
f=(fmax(f,d))-(tan(e));
if(islessequal(f,f)){
e=(fmin(g,g))+(fmin(a,f));
a=fdim(d,c);
f=tan(a);
a=exp(e);
d=pow(a,b);
}
else{
e=fdim(e,e);
e=(sin(b))*(fmin(f,f));
c=(atan2(f,f))/(fmax(e,g));
a=fmin(e,e);
g=sqrt(a);
}
if(isless(b,e)){
c=(fmin(e,c))*(exp(d));
c=(pow(g,f))/(sin(c));
f=(pow(a,b))+(asin(g));
}
}