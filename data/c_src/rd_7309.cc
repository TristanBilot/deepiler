#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(d,c);
g=(fdim(c,g))+(atan(a));
while(islessequal(f,g)){
c=ceil(b);
a=exp(g);
f=(fmax(g,e))/(sin(d));
d=(asin(d))*(atan(f));
}
d=(exp(e))-(fmin(c,d));
e=fdim(e,g);
d=acos(b);
f=(asin(g))+(fmax(c,c));
}