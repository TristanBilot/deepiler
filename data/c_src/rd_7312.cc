#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(b,e))*(atan2(a,f));
a=(fmax(f,d))*(asin(f));
a=atan2(c,a);
b=(atan2(a,f))/(atan2(e,e));
a=(atan2(c,d))*(asin(f));
if(islessequal(e,e)){
a=(log10(a))*(pow(a,f));
e=ceil(b);
e=fmin(e,e);
d=asin(a);
c=(atan2(a,c))/(ceil(e));
}
else{
a=fmin(f,c);
d=(acos(e))-(atan(c));
e=tan(b);
}
while(isgreaterequal(d,c)){
a=(fmax(f,b))-(pow(d,d));
d=pow(c,d);
}
}