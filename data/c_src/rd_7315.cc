#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(e,e);
g=pow(g,e);
c=(log(f))+(fdim(e,f));
f=(sin(b))*(fmax(c,c));
while(islessgreater(f,e)){
e=(fdim(g,g))*(pow(d,a));
a=(pow(b,f))+(floor(a));
b=floor(d);
}
while(isgreaterequal(a,e)){
g=fmin(a,f);
a=(log(b))/(fmax(e,d));
d=(fdim(b,f))*(fmin(e,g));
f=fmin(e,b);
}
}