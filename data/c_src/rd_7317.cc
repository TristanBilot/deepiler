#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=ceil(c);
c=fmin(b,d);
c=floor(e);
b=fmax(e,d);
b=(sin(b))-(exp(c));
if(isgreaterequal(b,a)){
c=(pow(d,b))-(fmin(c,d));
e=tan(c);
c=(atan(b))*(fdim(e,a));
}
while(isgreaterequal(c,e)){
d=pow(b,e);
b=ceil(a);
a=atan2(d,e);
}
}