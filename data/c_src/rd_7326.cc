#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(b);
d=pow(c,a);
d=(atan(b))/(pow(d,d));
c=(fdim(c,e))/(fdim(b,e));
b=(atan(c))/(sqrt(b));
if(islessequal(a,d)){
b=(fmax(c,e))-(tan(d));
c=(log(c))/(fdim(b,b));
e=(sin(e))+(floor(b));
e=(fdim(e,c))/(log10(c));
e=pow(c,c);
}
else{
b=(exp(b))+(floor(a));
a=(sin(e))+(fmax(b,c));
b=(pow(c,d))/(sin(b));
d=(asin(a))+(atan(d));
}
}