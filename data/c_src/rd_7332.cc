#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=log(g);
c=(log10(d))+(cos(e));
a=(fmax(g,c))-(atan2(f,f));
e=cos(a);
f=(acos(g))/(atan2(e,g));
a=log10(f);
if(isgreaterequal(e,g)){
b=atan2(d,e);
d=(fdim(b,f))/(atan2(g,d));
g=(fmin(f,e))*(fmax(b,e));
b=exp(d);
a=fmax(d,b);
}
}