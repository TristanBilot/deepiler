#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(exp(c))+(pow(c,f));
a=tan(f);
d=fdim(e,g);
d=exp(d);
g=(tan(b))/(sin(d));
b=(fmax(b,a))/(log10(b));
while(isless(g,g)){
f=floor(b);
a=(cos(a))/(fmax(e,h));
}
}