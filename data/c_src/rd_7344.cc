#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(e);
e=floor(d);
while(isgreaterequal(f,b)){
g=fmax(a,b);
g=sqrt(c);
}
if(isgreaterequal(a,g)){
e=pow(f,b);
c=(log(h))-(log10(h));
f=ceil(a);
}
else{
g=(atan2(c,b))*(tan(f));
a=(fdim(d,b))-(fmax(g,h));
c=fmax(c,e);
b=fdim(b,h);
d=(cos(a))/(fdim(a,f));
}
}