#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fdim(d,e);
g=(sqrt(f))-(sin(d));
c=(exp(d))/(log(b));
if(isgreaterequal(c,a)){
c=pow(g,a);
b=(log10(g))/(fmax(d,a));
c=(fmin(a,f))+(floor(b));
e=pow(f,d);
a=(atan2(c,d))+(fdim(a,b));
}
else{
e=(tan(b))/(acos(d));
g=pow(b,b);
a=fmax(d,f);
g=sin(f);
d=pow(d,d);
}
d=(log10(a))-(fmin(c,f));
b=(acos(e))-(log(g));
a=(sqrt(g))+(sin(d));
}