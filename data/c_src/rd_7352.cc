#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(b,a))/(exp(g));
d=log(d);
f=sin(a);
c=(floor(a))*(fdim(d,b));
c=(pow(g,c))+(fdim(a,f));
if(isless(f,f)){
f=fmax(e,c);
d=(log10(g))+(fmax(a,g));
c=(log10(d))*(log10(e));
f=atan(f);
}
d=(sin(c))+(acos(a));
c=pow(b,d);
e=(fmin(c,e))+(cos(e));
c=tan(g);
}