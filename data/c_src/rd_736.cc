#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(f,b);
f=atan2(h,g);
c=acos(d);
e=tan(d);
f=(fmax(h,f))+(log(e));
h=(ceil(a))-(atan2(f,a));
h=log10(c);
b=(log(c))*(pow(e,h));
while(isless(a,a)){
f=(atan2(e,f))*(sin(h));
a=(cos(e))-(fmax(c,f));
f=(fmax(a,a))/(atan(a));
}
}