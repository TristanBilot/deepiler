#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fdim(f,d);
f=cos(d);
b=fmax(c,c);
a=(fmax(g,a))-(cos(e));
d=atan2(f,e);
e=exp(a);
a=fdim(a,f);
if(islessequal(c,e)){
e=(ceil(e))*(atan(f));
g=sin(e);
c=(fdim(b,g))-(fdim(b,e));
f=fmin(g,d);
b=ceil(g);
}
else{
f=(cos(f))*(log(b));
c=(log(e))+(fmax(c,d));
e=pow(a,f);
d=(fdim(g,d))+(fmax(c,d));
g=(fmax(f,c))/(fdim(e,c));
}
}