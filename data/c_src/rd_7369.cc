#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(cos(a))*(sqrt(f));
b=fdim(a,d);
b=(fdim(f,b))-(fdim(c,e));
d=(pow(a,d))+(atan(g));
c=(pow(a,b))/(fdim(e,a));
if(isgreaterequal(g,f)){
f=(fdim(f,f))+(fdim(g,a));
g=pow(a,b);
e=(fdim(f,g))*(fmax(g,e));
}
else{
e=(sin(a))-(sqrt(c));
g=pow(f,f);
d=(pow(a,d))+(exp(c));
e=(asin(e))/(log10(g));
}
if(isgreaterequal(a,d)){
a=sin(b);
d=tan(c);
g=(pow(e,e))*(fmin(e,d));
e=(atan(a))+(floor(d));
g=log10(a);
}
}