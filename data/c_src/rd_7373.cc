#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(e))*(fmin(c,a));
d=cos(c);
e=(pow(b,d))*(log(e));
b=log(d);
d=(atan(d))/(floor(d));
c=(pow(d,a))+(asin(c));
}