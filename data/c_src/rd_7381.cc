#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(c,d);
c=fmin(b,g);
g=fmin(c,g);
g=(ceil(c))/(exp(c));
if(islessequal(g,b)){
d=fmin(a,c);
g=(atan2(f,c))+(fdim(g,e));
c=fmin(g,a);
}
else{
b=(fmax(a,e))-(ceil(f));
f=(acos(f))-(atan2(f,c));
}
if(isgreaterequal(d,g)){
e=(floor(c))/(sin(g));
b=fmax(e,b);
c=fmin(e,d);
e=sin(b);
e=(atan(b))*(exp(e));
}
}