#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(acos(b))/(fmin(e,d));
e=(fdim(a,b))-(asin(d));
a=fdim(e,c);
a=(pow(a,e))/(fdim(b,d));
e=pow(c,b);
b=fmax(b,c);
c=(sqrt(a))+(log10(d));
while(isless(e,a)){
a=sin(d);
d=fmax(b,a);
c=(floor(c))+(fmax(e,e));
}
}