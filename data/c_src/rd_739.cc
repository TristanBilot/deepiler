#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmax(e,d);
f=(exp(c))-(fdim(f,e));
d=ceil(f);
g=pow(f,f);
b=floor(d);
if(isgreaterequal(c,b)){
e=log10(b);
c=fmax(f,b);
g=(floor(c))/(asin(f));
c=(pow(e,d))/(atan2(g,e));
g=log(a);
}
else{
e=fmax(e,f);
a=(atan2(a,g))*(sqrt(b));
d=(log10(d))+(asin(a));
g=asin(e);
}
d=(ceil(c))/(acos(c));
g=(acos(a))/(fdim(g,b));
b=(fmax(a,g))+(sin(f));
f=fdim(e,e);
c=(fdim(e,a))*(pow(a,a));
}