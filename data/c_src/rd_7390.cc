#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=pow(b,b);
f=tan(e);
d=atan2(f,f);
d=sqrt(a);
g=fdim(g,f);
g=cos(c);
d=(pow(g,c))+(log10(a));
e=(ceil(g))/(pow(c,d));
c=(fmax(c,a))*(fdim(e,c));
d=pow(g,b);
g=log10(b);
}