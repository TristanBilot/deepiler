#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=exp(a);
c=(ceil(b))-(log10(c));
if(isgreaterequal(c,d)){
e=fdim(f,e);
c=fmin(d,f);
f=(fdim(d,b))*(sin(b));
d=(tan(f))/(fmax(f,c));
b=(atan2(b,a))*(pow(a,a));
}
while(isless(c,e)){
e=fmax(d,f);
f=sin(f);
}
}