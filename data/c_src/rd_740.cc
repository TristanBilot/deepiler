#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(c,a))*(pow(e,e));
c=log(e);
e=exp(c);
d=(atan(b))+(exp(a));
c=(fdim(c,c))+(pow(e,a));
e=fmin(b,b);
e=(log10(e))-(log10(b));
}