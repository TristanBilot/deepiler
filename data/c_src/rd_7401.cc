#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(asin(d))+(fdim(a,e));
d=(pow(a,e))/(floor(a));
a=(fdim(d,d))-(floor(a));
c=sin(b);
e=(fdim(b,a))*(log(d));
if(isgreaterequal(a,a)){
f=(fdim(f,f))-(fdim(f,d));
d=(cos(c))-(log(c));
}
else{
e=(atan2(d,d))*(tan(e));
c=sqrt(e);
}
if(islessequal(c,c)){
e=(log(e))/(fdim(c,c));
b=(fmin(e,b))-(fdim(c,f));
}
else{
d=pow(c,b);
e=(sin(a))-(pow(d,a));
e=tan(f);
}
}