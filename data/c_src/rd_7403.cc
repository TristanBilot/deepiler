#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(c,c))+(pow(d,a));
g=(asin(a))/(fmin(b,f));
while(islessequal(e,c)){
b=asin(c);
f=atan2(b,e);
}
while(islessgreater(b,g)){
e=(cos(c))+(fmax(a,g));
c=asin(d);
c=(acos(g))+(fmax(d,d));
g=pow(g,d);
g=fmin(g,g);
}
}