#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(d,a))*(fdim(a,c));
c=(log10(a))/(fdim(c,a));
d=(exp(e))-(pow(e,a));
d=fdim(a,e);
a=(atan2(b,b))-(fmax(a,f));
a=(fmin(a,b))-(pow(a,a));
b=(cos(f))-(fmin(f,a));
b=(exp(e))-(fmin(b,d));
c=(fmin(b,b))/(atan2(b,b));
b=fmax(e,a);
b=log10(f);
e=log(e);
d=(exp(e))*(atan2(d,c));
f=(log10(c))/(atan2(d,c));
}