#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(e,a);
e=atan2(a,c);
c=(atan2(c,d))+(fmax(c,d));
while(islessequal(a,e)){
b=(pow(c,c))/(fmax(c,e));
b=atan2(b,b);
d=atan2(c,e);
b=tan(b);
a=fdim(d,d);
}
if(isgreaterequal(b,d)){
b=(pow(b,b))*(exp(c));
a=(sqrt(d))+(fmax(e,e));
}
}