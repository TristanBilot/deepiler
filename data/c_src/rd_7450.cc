#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(cos(a))+(fmax(e,c));
d=fdim(c,e);
while(islessequal(e,b)){
b=exp(c);
b=cos(d);
d=fmin(c,c);
}
if(islessgreater(b,g)){
c=asin(d);
b=atan2(f,a);
h=(fdim(g,a))/(exp(e));
}
else{
d=fmin(b,g);
g=(pow(a,f))/(atan(h));
e=(asin(d))-(fdim(g,c));
f=(floor(b))+(log10(c));
b=fmax(h,a);
}
}