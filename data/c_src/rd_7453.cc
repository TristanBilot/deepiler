#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(c,d);
d=(pow(a,f))/(fmax(d,e));
a=log10(b);
a=fmax(a,d);
a=fdim(b,e);
c=(fmax(g,c))-(fmax(e,c));
b=fmin(a,b);
d=(pow(a,b))-(atan(d));
a=(pow(d,d))+(atan2(f,a));
if(isless(g,g)){
d=(sin(g))/(atan2(f,c));
f=(exp(d))+(pow(c,g));
c=(pow(c,e))*(pow(e,g));
g=fmax(c,f);
}
else{
a=(fdim(a,b))*(ceil(e));
a=(pow(c,a))*(log10(g));
a=cos(b);
a=pow(a,d);
c=(sqrt(e))-(fmax(c,b));
}
}