#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(sin(h))/(sqrt(c));
b=(pow(a,d))/(pow(h,c));
b=log(a);
f=(pow(g,d))+(floor(f));
g=fdim(c,h);
g=(atan2(f,e))+(fmin(c,g));
e=atan2(b,e);
f=(fdim(g,d))/(pow(d,g));
f=(tan(b))/(pow(d,f));
a=atan2(c,d);
f=cos(f);
e=floor(g);
a=pow(a,b);
}