#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(acos(e))-(acos(d));
c=(atan(a))*(fmax(d,a));
while(isless(c,c)){
e=ceil(d);
e=fmin(a,e);
c=fdim(d,a);
a=pow(b,b);
}
if(isless(d,d)){
b=(log10(e))*(cos(e));
b=(pow(d,e))-(pow(d,c));
d=(atan2(e,d))*(sqrt(a));
}
else{
b=pow(a,c);
a=tan(e);
a=fmin(d,b);
}
}