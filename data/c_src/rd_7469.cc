#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(h);
f=(floor(d))+(acos(c));
c=(fmin(g,f))+(atan(f));
d=fdim(b,e);
a=cos(g);
b=(log(h))/(exp(h));
}