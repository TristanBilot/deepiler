#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(c,e);
e=pow(d,b);
e=pow(b,e);
if(isless(d,d)){
d=atan2(b,c);
d=atan(b);
b=(sqrt(a))*(log10(d));
d=pow(a,a);
}
if(islessgreater(e,d)){
a=ceil(a);
c=log10(e);
e=(floor(a))*(cos(e));
}
else{
a=(fdim(b,b))+(cos(a));
d=(exp(d))/(ceil(c));
c=(tan(c))-(sqrt(c));
c=(asin(d))-(atan2(e,d));
e=cos(b);
}
}