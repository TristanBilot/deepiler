#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(d);
a=(ceil(a))+(log(e));
a=(log10(e))+(fmax(c,c));
d=(sqrt(c))+(log(d));
b=(fmax(b,d))*(fmax(c,a));
b=(sin(d))/(fmax(e,b));
if(isless(c,a)){
d=fmax(c,e);
d=log(a);
a=(fmin(c,a))+(acos(b));
}
}