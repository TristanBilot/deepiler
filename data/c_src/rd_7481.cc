#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(b,c);
d=asin(d);
a=atan2(b,b);
a=(atan(c))/(log10(c));
c=fmin(b,c);
while(isless(b,d)){
d=(fdim(d,e))*(acos(b));
e=(sin(a))+(atan(b));
}
a=(exp(b))+(fmin(a,c));
c=pow(a,c);
}