#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(d);
c=exp(a);
c=floor(h);
c=fdim(e,h);
d=ceil(f);
if(islessequal(g,g)){
d=asin(e);
a=fmax(e,b);
g=sin(a);
b=(floor(h))+(fmax(g,c));
h=(acos(g))*(sin(d));
}
else{
e=acos(g);
b=(fdim(b,f))+(fmax(b,c));
c=acos(d);
}
f=(cos(e))/(log(a));
g=cos(f);
d=(asin(g))+(atan2(a,c));
}