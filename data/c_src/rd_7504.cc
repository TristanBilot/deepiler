#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(a,d);
d=pow(b,b);
d=fmax(e,b);
e=(cos(c))-(ceil(c));
c=(asin(e))/(fmax(b,e));
f=(pow(c,f))*(acos(c));
a=fdim(a,f);
e=acos(b);
}