#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=asin(a);
e=atan2(b,a);
d=(log10(a))+(atan2(a,d));
a=(fdim(b,f))-(atan2(f,b));
f=(pow(b,d))-(fdim(d,e));
a=(acos(b))*(fdim(f,b));
c=(asin(e))/(fdim(e,b));
if(isless(a,b)){
a=sin(f);
d=pow(d,c);
f=(atan2(f,d))+(fmax(f,a));
a=tan(b);
}
else{
d=fdim(d,d);
c=(fdim(e,c))*(atan2(d,a));
a=(log(b))+(sin(f));
b=(atan2(d,e))-(acos(d));
}
}