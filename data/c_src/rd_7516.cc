#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(e,a))+(pow(e,b));
c=fmin(a,d);
a=(atan(a))*(sin(b));
a=(floor(a))+(exp(c));
a=atan(e);
while(isless(a,d)){
d=(acos(c))/(pow(d,c));
e=(pow(d,d))-(exp(c));
e=(pow(d,e))-(atan2(c,a));
c=(atan2(a,d))/(fmax(a,c));
}
while(islessequal(d,d)){
d=(tan(b))+(pow(b,e));
c=(fmin(e,b))/(tan(d));
b=tan(e);
a=(fmax(a,b))*(tan(d));
}
}