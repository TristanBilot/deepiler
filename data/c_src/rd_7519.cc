#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(c);
b=fmax(c,d);
d=asin(a);
e=pow(e,c);
e=log10(e);
a=(floor(b))+(fmin(e,e));
while(isgreaterequal(e,a)){
c=pow(d,c);
c=asin(d);
d=(acos(e))/(acos(a));
e=(acos(d))*(fmax(c,b));
}
}