#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(c,d))*(fdim(d,e));
g=(acos(c))/(atan2(a,a));
d=fdim(d,e);
g=(fmax(a,g))-(fmax(b,a));
c=(asin(c))*(exp(h));
h=fdim(g,b);
h=(ceil(h))/(atan2(f,d));
if(islessgreater(h,d)){
h=(fdim(c,e))-(atan(e));
b=(atan(c))*(atan2(e,e));
c=fmax(b,a);
}
}