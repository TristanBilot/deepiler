#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan2(b,e))*(log(d));
e=tan(f);
d=asin(e);
c=(atan(a))*(fdim(c,e));
e=atan2(c,c);
f=atan2(b,b);
while(isless(d,e)){
a=ceil(c);
f=(fmin(c,a))*(fmin(e,d));
b=(cos(c))-(ceil(c));
}
}