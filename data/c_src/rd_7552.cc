#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(b);
c=cos(e);
while(islessgreater(d,d)){
e=(log10(b))+(pow(d,c));
e=(log(e))+(exp(d));
b=(atan2(b,a))+(pow(a,b));
c=(ceil(b))*(log10(d));
}
while(islessequal(b,d)){
b=(log10(a))/(tan(a));
c=asin(d);
}
}