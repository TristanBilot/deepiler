#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(c))-(fmax(b,a));
a=(fmin(a,c))*(exp(a));
f=(log(b))-(fmax(e,c));
b=fmax(b,a);
d=acos(c);
while(islessequal(b,a)){
b=acos(a);
a=atan(f);
}
a=(atan2(d,d))-(log(b));
b=(floor(a))+(atan2(d,d));
d=acos(b);
}