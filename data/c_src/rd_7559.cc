#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmax(b,d))-(cos(f));
b=fmin(f,a);
f=(fmin(c,b))-(pow(e,a));
b=acos(c);
f=fmax(c,b);
e=atan(d);
c=exp(a);
}