#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(h,b))+(fmin(b,d));
a=(sqrt(f))+(fmax(e,c));
while(isgreaterequal(d,e)){
e=log(a);
b=fmax(g,d);
c=(sin(e))+(fmax(h,b));
b=(tan(h))*(pow(d,a));
g=pow(f,h);
}
if(isless(h,g)){
g=asin(e);
f=(acos(a))-(pow(g,g));
d=fdim(e,h);
}
else{
g=asin(c);
a=fmax(h,g);
e=(fdim(e,d))-(floor(d));
f=(pow(g,b))/(fmax(h,e));
b=log(a);
}
}