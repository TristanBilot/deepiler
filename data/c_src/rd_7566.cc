#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sin(c);
h=(fmin(b,h))/(atan(h));
while(isless(e,h)){
h=(atan2(f,f))/(fmin(h,h));
g=(sin(a))/(ceil(b));
e=fdim(b,a);
d=tan(d);
f=sqrt(e);
}
while(isless(d,e)){
f=acos(c);
f=(cos(f))*(fmin(c,c));
e=(fmin(a,f))/(log(g));
}
}