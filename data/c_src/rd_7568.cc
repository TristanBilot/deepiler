#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan(f);
d=log10(b);
e=(fmin(b,c))/(fmin(b,a));
f=ceil(g);
if(isless(f,c)){
f=sin(d);
c=sqrt(f);
f=acos(f);
}
else{
b=fmin(f,b);
e=(atan2(a,a))/(sin(b));
}
if(isless(d,c)){
d=cos(c);
g=atan(e);
d=fdim(d,b);
e=log10(b);
}
}