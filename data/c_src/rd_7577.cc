#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(g,a);
e=(fmin(a,d))*(ceil(g));
g=ceil(g);
h=sin(e);
a=(fdim(g,b))*(pow(h,b));
f=(fdim(e,e))+(atan2(d,d));
a=log(f);
b=fdim(f,d);
d=(sqrt(h))/(pow(f,g));
if(islessgreater(a,g)){
b=log(h);
h=atan2(h,a);
h=pow(b,b);
}
else{
a=(asin(a))+(asin(e));
a=(exp(f))+(atan2(b,f));
}
}