#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(a,d))/(cos(d));
e=atan2(a,f);
c=fmax(e,b);
e=exp(a);
if(islessgreater(b,d)){
a=fmin(a,c);
e=(atan2(e,d))*(tan(a));
c=fmin(d,c);
}
}