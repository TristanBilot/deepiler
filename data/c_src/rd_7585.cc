#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(b))+(exp(c));
f=tan(b);
f=(fmax(c,f))/(fdim(b,b));
while(islessequal(d,a)){
c=(fdim(d,a))/(atan(b));
c=(atan2(f,b))-(asin(c));
}
d=(acos(c))+(log10(b));
e=log(c);
e=(fdim(c,d))+(pow(a,f));
e=(pow(e,d))-(floor(b));
d=fdim(f,e);
}