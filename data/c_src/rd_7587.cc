#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(d,d))+(log10(a));
d=fmax(d,b);
f=(log(d))-(sin(c));
e=cos(c);
b=(ceil(e))*(sqrt(f));
c=(pow(e,d))-(tan(c));
a=floor(d);
if(isgreaterequal(b,c)){
b=(fmax(d,d))+(fdim(e,e));
d=pow(f,e);
c=exp(a);
b=log(f);
}
else{
f=log10(d);
c=fmax(d,c);
}
}