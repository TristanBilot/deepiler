#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fmax(f,a);
g=fmin(d,g);
f=exp(f);
c=pow(g,c);
a=fmax(a,f);
f=(asin(c))-(ceil(d));
if(isless(b,f)){
d=(log(g))-(sin(f));
f=(pow(b,a))/(atan(d));
}
else{
a=(pow(c,g))/(sin(c));
d=pow(g,b);
d=fdim(g,b);
}
}