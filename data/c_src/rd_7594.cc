#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=acos(e);
c=(sqrt(e))/(sqrt(a));
e=fmax(c,b);
while(isless(a,c)){
e=tan(d);
d=fmax(b,e);
e=(sqrt(d))+(acos(b));
}
b=fdim(e,c);
d=(fmax(c,c))/(fmax(c,b));
d=pow(e,e);
e=(tan(a))-(tan(b));
}