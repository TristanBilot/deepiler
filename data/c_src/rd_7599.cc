#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(log10(b))/(fdim(d,a));
b=fmin(f,b);
if(isgreaterequal(c,b)){
a=log(a);
b=(fmax(b,c))-(log(d));
f=(fdim(f,a))/(fmin(d,a));
f=fmax(a,a);
d=(ceil(d))/(fdim(b,b));
}
else{
b=(fdim(f,c))+(atan(b));
d=atan(d);
e=fmax(a,b);
a=(pow(b,d))*(fdim(c,d));
}
while(islessequal(f,b)){
f=atan2(e,a);
a=acos(a);
}
}