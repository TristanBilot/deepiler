#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(exp(b))*(tan(c));
f=(log(e))*(sqrt(c));
b=(fdim(b,c))*(fdim(a,f));
a=(cos(d))*(asin(a));
if(islessgreater(f,f)){
c=(atan(a))/(tan(f));
a=(fmin(c,b))*(fmax(c,a));
b=(fmax(g,g))*(atan2(g,b));
d=atan2(e,a);
a=(sin(e))-(atan(f));
}
else{
d=(pow(e,c))/(sqrt(g));
a=acos(a);
a=fmin(e,d);
}
}