#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(b,d))*(fmin(a,b));
c=(floor(d))+(fdim(e,e));
while(isgreaterequal(a,d)){
e=log(d);
a=(fmin(e,e))-(pow(a,b));
}
while(islessequal(e,a)){
d=(pow(d,c))/(pow(d,a));
a=tan(c);
d=log10(e);
a=(cos(a))/(atan2(a,e));
b=floor(b);
}
}