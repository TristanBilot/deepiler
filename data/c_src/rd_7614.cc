#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(f,g))/(fmax(g,g));
b=log(c);
d=(log10(g))*(exp(c));
c=fmax(a,c);
f=(pow(e,g))*(atan2(c,d));
e=sqrt(c);
b=acos(e);
a=(pow(g,b))/(atan2(c,b));
g=(fmax(d,d))-(pow(a,g));
g=fdim(b,e);
a=(fmin(e,a))-(fmin(f,e));
c=fmax(g,a);
e=fmin(f,b);
a=(cos(c))+(tan(f));
}