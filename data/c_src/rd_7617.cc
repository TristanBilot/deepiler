#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(d,e))/(fmax(f,a));
f=(tan(b))/(tan(c));
e=(fmin(f,e))-(tan(e));
f=(atan2(c,a))/(fdim(a,b));
d=atan(f);
d=(fdim(e,b))*(fdim(c,d));
f=(atan2(a,b))/(pow(b,e));
f=(pow(a,f))+(pow(e,f));
c=fdim(c,c);
a=floor(c);
e=acos(d);
a=(cos(c))*(acos(c));
f=(pow(e,e))*(fmin(a,f));
}