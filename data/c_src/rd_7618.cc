#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(exp(a))*(asin(e));
f=fmax(b,c);
b=log(b);
c=atan2(d,f);
d=fmin(d,c);
if(islessgreater(a,c)){
e=(fmin(e,c))-(pow(f,e));
e=exp(f);
b=log10(e);
}
d=(fmax(e,e))/(fmax(f,e));
a=fdim(d,c);
f=(fdim(f,a))+(atan2(a,a));
c=tan(b);
d=(atan2(b,b))+(fmin(d,a));
}