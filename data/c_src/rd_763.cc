#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fdim(h,a);
h=(log(e))-(pow(b,e));
c=fmax(f,g);
d=(log10(f))+(fmax(d,b));
f=(atan2(b,h))-(fdim(h,e));
e=(exp(b))*(exp(f));
h=sin(e);
if(islessgreater(g,d)){
h=fmin(b,g);
d=atan(b);
f=(fmin(h,b))*(asin(e));
c=(fmin(a,a))-(tan(b));
f=fmax(b,e);
}
}