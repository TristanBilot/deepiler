#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(a,a);
d=fdim(c,d);
if(islessequal(d,d)){
e=sin(b);
e=(log10(b))/(tan(a));
}
c=log(e);
c=fmax(d,c);
a=fdim(c,a);
}