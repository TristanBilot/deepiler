#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(d,e))-(sin(a));
a=(fmax(c,a))*(log10(a));
b=fdim(e,c);
a=(asin(a))-(exp(a));
if(isless(e,c)){
e=exp(e);
a=sqrt(c);
}
if(isless(a,c)){
d=asin(c);
b=(fmax(d,e))*(exp(d));
d=fmax(c,d);
a=fdim(a,a);
}
}