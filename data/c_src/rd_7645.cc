#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(acos(b))+(ceil(d));
b=(ceil(a))*(log10(b));
e=(exp(c))-(atan2(d,e));
b=cos(e);
while(islessgreater(a,e)){
a=(pow(e,e))-(tan(e));
d=(pow(c,e))-(sin(a));
a=fmin(a,c);
a=pow(b,e);
}
}