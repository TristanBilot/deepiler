#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(c,a))+(fmin(b,c));
c=(asin(a))-(exp(a));
if(islessgreater(e,a)){
d=(pow(b,d))-(log10(a));
c=cos(b);
e=cos(d);
e=sqrt(c);
a=(log10(c))-(floor(d));
}
else{
d=(atan2(c,b))*(ceil(a));
a=fmin(b,d);
a=(fmax(e,e))*(fmax(b,b));
}
while(islessgreater(d,a)){
e=pow(d,e);
b=acos(a);
e=(tan(c))/(tan(e));
a=pow(c,b);
b=(asin(b))-(sin(a));
}
}