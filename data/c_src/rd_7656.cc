#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(b,d))-(acos(c));
f=(cos(d))+(asin(a));
d=log10(f);
c=asin(b);
a=(atan2(a,b))*(fmax(c,e));
while(isgreaterequal(d,f)){
b=fmin(e,b);
f=(acos(e))/(fmin(b,f));
}
while(islessequal(f,a)){
e=exp(b);
a=(atan(b))-(pow(f,d));
e=(log10(d))+(fmin(d,b));
c=(atan2(a,a))+(asin(c));
b=(fmin(a,c))-(sqrt(f));
}
}