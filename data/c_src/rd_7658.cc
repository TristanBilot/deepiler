#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(a,d);
a=fdim(b,d);
a=(sqrt(b))*(fmax(b,e));
c=(sqrt(d))+(log10(a));
d=(atan2(e,b))/(log10(e));
while(islessgreater(c,a)){
b=log(e);
c=atan2(b,d);
e=(fmax(a,c))+(log(d));
}
while(islessequal(b,b)){
b=(sin(c))/(acos(d));
d=(cos(a))/(atan(e));
}
}