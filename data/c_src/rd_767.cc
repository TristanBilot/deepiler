#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(acos(d))/(pow(c,c));
d=fdim(a,e);
e=(pow(c,e))-(ceil(a));
c=log(d);
e=pow(e,e);
c=(atan2(b,e))-(atan2(c,a));
c=fdim(a,a);
while(isgreaterequal(b,c)){
b=acos(c);
e=exp(c);
b=(fmin(d,c))/(ceil(b));
}
}