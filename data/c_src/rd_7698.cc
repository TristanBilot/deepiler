#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(f,c))+(pow(d,a));
a=(atan(b))+(fdim(a,a));
f=(fmin(f,c))-(fmax(e,e));
c=(ceil(a))+(fmax(b,f));
c=fmin(d,g);
d=(cos(f))-(pow(a,f));
b=ceil(b);
if(islessequal(b,b)){
e=fdim(e,e);
e=sin(f);
a=(fmax(d,g))*(fmin(a,e));
}
else{
d=(pow(g,c))+(fmax(c,f));
g=(ceil(f))*(atan(e));
}
}