#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(a,g))+(fdim(e,g));
f=(fdim(a,f))-(pow(c,g));
b=(fdim(d,c))+(exp(g));
f=atan2(c,f);
d=(atan2(b,g))*(fmin(f,e));
b=(atan(f))+(cos(e));
f=log(e);
f=atan(a);
b=(pow(e,d))/(pow(c,c));
g=(exp(c))*(fmin(e,f));
if(islessequal(b,d)){
g=atan2(f,b);
f=floor(c);
c=pow(a,g);
a=(pow(e,g))+(acos(g));
}
}