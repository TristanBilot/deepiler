#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(asin(e))*(exp(b));
c=(fmin(d,d))/(atan2(e,g));
e=(fmin(e,a))/(ceil(b));
b=(fmin(a,b))*(atan(g));
d=(exp(f))/(fmax(g,d));
e=(fmax(d,c))-(pow(a,d));
c=sqrt(g);
g=tan(f);
d=log10(f);
b=fdim(f,f);
if(isless(d,f)){
f=(log(d))+(acos(c));
g=pow(a,f);
c=(sin(e))-(fdim(g,b));
}
else{
b=(sqrt(f))/(pow(c,g));
d=fmax(c,g);
b=floor(f);
d=atan(f);
d=fdim(f,g);
}
}