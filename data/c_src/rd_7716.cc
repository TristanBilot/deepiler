#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fdim(f,b))*(cos(a));
b=fdim(a,g);
a=fdim(g,c);
a=fdim(b,e);
if(islessgreater(g,d)){
g=(fmin(f,a))*(fmax(d,d));
b=(exp(d))/(atan2(b,b));
}
else{
e=(atan2(f,b))*(fdim(f,g));
a=(fmin(e,e))/(floor(b));
}
if(isless(f,d)){
d=fmax(c,f);
c=(fdim(c,d))-(fdim(a,e));
a=tan(c);
}
else{
g=(exp(e))*(log10(d));
a=(fmax(d,f))-(fmin(e,g));
a=(acos(b))-(fmin(d,b));
}
}