#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,a))+(atan(a));
c=(fmax(e,e))/(cos(a));
e=exp(a);
b=(atan2(b,d))-(fdim(c,e));
if(islessequal(c,d)){
a=(fmin(a,a))/(pow(e,a));
c=fdim(a,b);
c=(exp(b))-(asin(b));
}
else{
e=(ceil(a))+(tan(e));
c=fmax(a,c);
b=(fdim(c,e))-(sin(b));
a=(acos(d))-(exp(a));
e=(atan2(e,e))-(atan2(e,a));
}
if(isless(d,d)){
e=fmin(a,e);
c=log10(e);
}
else{
d=fmax(a,a);
e=fmin(b,d);
b=sin(d);
}
}