#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(exp(d))*(ceil(c));
g=(floor(e))-(pow(a,c));
h=(pow(a,f))+(fmax(h,d));
d=(fdim(c,a))*(tan(f));
if(isgreaterequal(c,b)){
f=(log10(a))*(asin(d));
f=(fmin(g,h))*(exp(g));
b=fmax(e,d);
b=(tan(e))+(cos(h));
}
else{
a=(pow(h,c))-(fmin(c,h));
b=(ceil(a))*(atan2(h,a));
f=(fmin(g,a))/(ceil(b));
g=atan2(c,g);
}
if(islessgreater(a,d)){
d=fdim(c,g);
c=atan(d);
c=(fdim(e,f))/(fdim(g,c));
a=ceil(c);
g=(pow(f,g))+(cos(d));
}
else{
g=fmin(d,g);
f=sin(e);
a=atan2(a,f);
}
}