#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmin(d,e))+(exp(a));
e=exp(d);
c=(tan(e))-(atan2(c,e));
f=(fmin(c,f))+(exp(c));
if(isless(b,d)){
d=(fmin(c,c))/(fmax(e,c));
f=(pow(b,e))/(atan2(f,b));
}
if(islessequal(e,d)){
e=(acos(c))*(atan2(e,f));
e=(atan(a))-(cos(d));
f=(fmax(d,f))*(fmin(b,e));
b=(ceil(e))-(pow(e,b));
d=(log10(a))*(fdim(d,f));
}
else{
d=floor(a);
f=atan2(a,d);
}
}