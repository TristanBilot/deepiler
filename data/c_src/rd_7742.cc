#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(g,e))*(acos(e));
d=exp(g);
f=fmin(e,a);
a=(log10(a))/(fdim(f,g));
if(islessequal(f,g)){
e=asin(d);
a=(pow(a,d))*(fmin(g,a));
e=atan2(d,g);
a=(atan2(f,e))+(fdim(b,a));
c=(fmin(f,d))-(fmax(c,c));
}
g=(fmax(a,f))/(fmin(g,e));
f=(acos(a))/(fmin(e,f));
e=(log10(d))+(sin(c));
a=(fdim(c,g))-(floor(e));
}