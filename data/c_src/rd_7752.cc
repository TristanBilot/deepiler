#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(h,e))-(fdim(b,c));
g=(floor(a))/(asin(e));
h=log10(c);
g=asin(e);
c=(fmax(c,e))+(sqrt(b));
while(islessgreater(h,g)){
a=(sin(d))*(log10(h));
f=(ceil(g))*(fmin(h,a));
b=floor(b);
}
if(islessgreater(e,c)){
g=(ceil(c))+(sqrt(g));
b=(fmin(g,c))*(atan(h));
g=(cos(c))*(pow(a,c));
d=fmin(b,f);
}
}