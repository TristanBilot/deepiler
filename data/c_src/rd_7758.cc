#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(a,b);
e=fmin(c,d);
while(islessequal(c,c)){
e=asin(a);
e=(fmin(a,c))/(atan2(d,c));
c=(acos(c))+(fmax(e,a));
c=(fmin(a,b))+(fdim(d,e));
}
if(isless(c,d)){
a=(ceil(a))-(tan(d));
c=cos(c);
b=(fdim(b,b))-(fdim(a,c));
}
}