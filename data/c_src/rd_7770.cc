#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=floor(b);
b=(log10(b))+(fmax(b,c));
e=cos(c);
e=fdim(b,b);
while(islessequal(e,c)){
a=fdim(a,a);
d=(sin(e))/(pow(a,c));
c=acos(b);
a=(log(d))-(fmax(a,a));
}
c=asin(a);
d=floor(e);
a=(atan2(a,e))/(atan(e));
}