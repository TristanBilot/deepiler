#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(ceil(d))-(log10(e));
e=(fmin(d,c))-(exp(e));
a=(fmin(a,c))/(fdim(c,a));
b=(fmax(c,d))-(exp(c));
b=floor(a);
while(islessequal(e,a)){
b=asin(e);
c=fdim(c,b);
b=asin(a);
c=(exp(e))+(tan(e));
}
while(isless(e,b)){
b=(fmin(a,d))+(asin(d));
d=(tan(a))*(pow(e,a));
}
}