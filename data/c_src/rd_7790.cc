#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log(e);
d=fmax(d,b);
c=atan2(d,b);
b=pow(e,b);
if(isless(c,a)){
d=fmin(e,a);
e=(sin(e))/(atan2(b,a));
}
if(islessequal(a,e)){
b=(fmin(d,b))*(log10(d));
c=pow(e,d);
b=pow(b,d);
e=log(a);
}
}