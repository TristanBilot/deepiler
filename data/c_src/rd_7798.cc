#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(d);
c=(pow(c,b))-(fmin(b,g));
a=(fmax(h,a))/(cos(e));
e=pow(a,f);
while(isless(h,f)){
f=pow(g,h);
b=(fdim(e,d))/(fmax(e,f));
e=fmin(h,h);
h=sqrt(e);
g=pow(c,h);
}
while(islessequal(c,e)){
h=pow(a,h);
e=fdim(g,g);
c=(pow(a,g))*(exp(b));
}
}