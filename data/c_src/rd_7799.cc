#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,d);
a=(fmin(a,d))*(fmax(a,b));
if(isless(d,c)){
a=atan2(c,a);
b=(fmin(b,c))*(fmax(b,e));
}
if(isgreaterequal(c,d)){
b=exp(a);
b=(atan2(b,b))-(log10(d));
}
else{
a=ceil(e);
b=fmax(b,d);
}
}