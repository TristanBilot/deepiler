#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(d,b);
a=(acos(a))/(pow(b,c));
f=(fmax(a,f))*(fmin(e,e));
d=pow(d,e);
c=fmax(d,b);
f=fdim(d,c);
d=fmin(a,g);
f=(pow(f,a))*(log10(d));
d=acos(b);
b=sqrt(b);
}