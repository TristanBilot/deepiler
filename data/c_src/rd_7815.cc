#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(ceil(c))/(fdim(b,f));
b=(fdim(f,a))*(log10(c));
g=atan2(d,c);
f=pow(f,a);
while(islessequal(e,g)){
a=(atan(c))+(asin(e));
d=fmin(f,f);
g=(fmax(a,c))/(sin(c));
}
if(islessequal(f,d)){
f=floor(f);
b=(fmax(g,d))-(floor(a));
e=(fmin(f,c))/(pow(e,a));
e=pow(b,g);
}
}