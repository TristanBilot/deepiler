#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(tan(e))-(ceil(b));
c=(fdim(b,c))-(exp(a));
if(isless(c,c)){
d=log10(a);
a=(floor(e))-(cos(c));
a=fmax(a,b);
}
else{
b=(sin(a))-(asin(b));
b=fmin(e,b);
e=fmin(d,c);
}
d=fmin(c,e);
c=fmin(c,a);
}