#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(e,g))*(floor(a));
g=exp(a);
e=pow(g,a);
if(isgreaterequal(a,d)){
h=fdim(g,a);
g=(floor(h))-(floor(h));
a=(floor(a))+(acos(g));
d=atan(h);
}
else{
f=(floor(e))*(tan(g));
b=(sqrt(c))+(fdim(e,g));
g=(fmin(g,h))*(fdim(g,f));
}
if(isgreaterequal(e,a)){
a=log(g);
d=(atan2(h,e))+(fmin(a,h));
}
}