#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(a);
b=fdim(e,e);
c=sin(c);
while(isless(b,a)){
a=(pow(c,c))/(fmax(b,e));
c=(log(a))+(fmin(b,a));
e=(fmin(e,b))/(fdim(b,c));
d=(exp(b))+(fmax(e,d));
}
while(islessequal(a,e)){
b=asin(a);
a=(acos(a))/(log(e));
a=(atan(a))+(floor(e));
c=(fdim(e,a))+(fmin(e,e));
d=(fdim(a,e))*(exp(a));
}
}