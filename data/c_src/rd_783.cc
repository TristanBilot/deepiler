#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(c,b))-(log10(h));
g=(pow(d,a))/(cos(h));
g=floor(f);
a=(atan(d))/(atan2(f,d));
d=(atan2(b,f))/(fmax(a,h));
if(isless(d,h)){
b=floor(c);
b=atan2(c,f);
a=sqrt(g);
h=(fmin(d,b))*(pow(b,h));
}
else{
a=floor(b);
b=fmax(a,f);
b=fmin(b,a);
f=(pow(e,e))*(pow(d,d));
}
b=(atan2(f,e))/(floor(g));
a=cos(a);
b=fmax(d,c);
a=asin(h);
a=fdim(b,g);
}