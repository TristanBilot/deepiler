#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(exp(d))+(fmin(g,f));
h=fmin(h,h);
d=(sin(f))-(atan2(c,a));
a=tan(c);
f=(fmax(h,d))+(floor(d));
if(islessequal(c,h)){
c=(atan2(e,d))/(sin(b));
e=(fmax(g,g))/(log10(d));
g=atan2(b,f);
b=fmin(c,g);
}
else{
f=(pow(h,e))/(exp(h));
b=fmax(d,g);
f=atan(a);
e=(fmax(c,e))+(fdim(e,c));
}
b=sin(d);
e=cos(g);
b=fdim(a,e);
f=log(a);
c=floor(h);
}