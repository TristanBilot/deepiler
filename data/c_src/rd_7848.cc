#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(a))+(atan(a));
a=(cos(d))-(atan2(d,c));
while(islessequal(d,c)){
e=(fmin(b,e))/(asin(e));
c=(exp(b))/(asin(a));
e=(exp(c))-(acos(b));
a=fmax(e,a);
}
if(isgreaterequal(b,e)){
d=sqrt(c);
e=(floor(a))-(fdim(e,b));
}
}