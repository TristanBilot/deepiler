#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(tan(e))+(fmax(e,d));
b=(log(b))-(tan(e));
a=ceil(a);
if(isless(a,a)){
a=exp(f);
e=exp(e);
a=(fdim(c,a))/(asin(c));
}
d=(asin(b))-(log(d));
d=floor(c);
}