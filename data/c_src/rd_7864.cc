#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=asin(a);
b=(fdim(b,c))+(asin(a));
b=(fmin(a,e))*(acos(a));
e=(atan2(c,c))*(cos(a));
while(islessgreater(c,b)){
c=(cos(c))/(fdim(d,b));
a=atan2(c,c);
e=(atan2(a,e))-(fdim(c,a));
}
b=(fmax(d,d))+(asin(a));
e=(cos(e))*(tan(e));
d=sqrt(a);
b=fdim(e,d);
c=fdim(b,e);
}