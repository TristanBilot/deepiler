#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=floor(a);
b=pow(b,g);
c=(floor(b))*(log10(g));
h=ceil(f);
g=sqrt(f);
b=fmin(b,e);
a=(log(f))+(pow(c,d));
g=(fmin(h,h))/(asin(g));
f=(pow(d,e))+(floor(c));
h=pow(b,f);
a=(log(c))+(fdim(f,g));
}