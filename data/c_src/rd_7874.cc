#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(a,c))+(fmin(d,b));
a=atan(d);
d=asin(c);
d=pow(c,b);
e=(cos(b))*(asin(b));
if(islessequal(e,c)){
e=(asin(e))+(floor(b));
e=(atan(d))*(exp(b));
a=(fdim(c,c))*(asin(e));
e=fdim(c,b);
b=(floor(c))/(pow(e,e));
}
else{
b=(fmin(a,b))-(fmax(d,c));
e=pow(b,e);
e=(fdim(a,b))-(log10(d));
b=asin(e);
c=(pow(b,a))-(pow(c,c));
}
d=log(d);
a=(cos(e))+(fdim(a,b));
c=(acos(c))/(atan2(a,b));
e=acos(c);
}