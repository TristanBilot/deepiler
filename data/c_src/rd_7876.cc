#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=exp(a);
e=tan(e);
d=(fmin(c,b))+(atan2(a,b));
a=(fmax(b,e))/(ceil(d));
e=(acos(c))*(fmin(b,d));
b=(fmax(a,f))-(ceil(a));
d=asin(d);
a=fmin(a,e);
if(islessgreater(f,b)){
f=fmax(a,d);
f=(asin(e))*(atan2(d,b));
}
else{
f=sin(d);
b=(acos(f))-(tan(c));
c=asin(d);
c=ceil(b);
c=(sqrt(c))-(atan2(f,b));
}
}