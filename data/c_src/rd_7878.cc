#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(b,e))-(fdim(e,c));
f=(fdim(e,d))/(cos(c));
e=fmax(d,c);
a=log10(d);
d=pow(d,a);
while(islessgreater(f,b)){
e=(log10(e))-(asin(d));
a=fmax(c,a);
d=(fdim(f,d))+(atan2(e,c));
}
while(islessequal(f,c)){
a=pow(d,b);
f=atan2(b,d);
d=fmax(f,f);
c=(acos(b))/(fdim(d,d));
}
}