#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=asin(a);
g=acos(a);
a=(sin(f))-(atan2(g,a));
b=fmax(a,h);
g=(fmax(a,g))+(fmax(d,a));
while(isless(d,a)){
b=(floor(a))/(fmax(b,c));
c=tan(g);
f=fmax(a,e);
}
if(islessgreater(e,d)){
e=(atan2(b,h))*(atan2(b,e));
d=(tan(b))/(acos(e));
h=(atan(e))/(atan2(g,d));
c=fdim(h,d);
g=log10(g);
}
else{
b=fdim(b,e);
g=sqrt(h);
h=sqrt(c);
e=(fmax(h,f))*(fmin(b,h));
}
}