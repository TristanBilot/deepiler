#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan(c))/(pow(f,e));
d=pow(c,e);
e=fmin(d,c);
b=fmin(c,f);
e=cos(a);
if(isless(b,f)){
b=(atan2(d,a))*(fmin(c,a));
e=(tan(f))/(fmin(d,f));
}
else{
a=(exp(e))*(sin(c));
d=fmin(c,e);
b=tan(e);
}
}