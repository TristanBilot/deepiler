#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(b,c);
b=fmax(a,g);
b=(log10(d))-(exp(a));
g=fmax(f,e);
e=fmax(e,c);
if(isgreaterequal(d,b)){
e=(fmax(c,b))/(sin(a));
c=floor(g);
e=atan2(a,e);
a=sin(e);
a=fmin(a,g);
}
b=(fmin(d,g))-(ceil(b));
g=(fdim(d,g))-(floor(e));
b=(fmax(g,f))+(fdim(e,e));
}