#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(atan2(a,d))*(asin(c));
e=acos(a);
b=asin(b);
c=(log(e))*(log(c));
d=(pow(e,c))-(pow(e,a));
while(islessequal(f,b)){
f=fdim(c,a);
e=(fdim(c,f))-(acos(b));
e=fmax(c,a);
}
a=(pow(e,e))*(ceil(a));
c=sin(e);
}