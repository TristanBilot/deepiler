#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,a);
c=(fmax(b,d))/(atan2(e,d));
while(isless(a,e)){
e=(fdim(c,a))-(atan2(d,b));
d=floor(e);
a=(cos(c))-(pow(d,d));
b=(sin(c))+(sqrt(d));
e=log(a);
}
if(islessgreater(d,b)){
d=(log(b))+(fdim(c,c));
b=(pow(a,d))+(fmax(e,a));
e=log(c);
b=ceil(e);
a=atan2(c,c);
}
else{
b=floor(c);
c=fmin(e,e);
a=exp(a);
}
}