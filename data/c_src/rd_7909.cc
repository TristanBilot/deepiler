#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(d,b);
e=fdim(d,d);
c=floor(b);
while(isless(c,e)){
c=pow(c,e);
b=fdim(a,a);
}
a=atan2(c,c);
b=asin(d);
a=exp(c);
c=log(e);
}