#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(pow(b,b))-(sqrt(a));
e=(pow(d,a))+(fmax(e,c));
a=asin(e);
a=log10(a);
g=(fdim(g,f))/(sqrt(a));
c=(atan2(e,b))*(sin(c));
g=(atan2(c,a))/(fmin(a,g));
f=(fdim(d,d))-(pow(g,a));
e=tan(e);
c=(atan(a))+(tan(f));
b=exp(e);
g=(atan(e))*(exp(f));
}