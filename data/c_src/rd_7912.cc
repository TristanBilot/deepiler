#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmax(b,g))+(atan(f));
f=ceil(d);
f=tan(f);
a=asin(c);
d=sqrt(e);
while(islessequal(a,g)){
g=(pow(f,d))+(asin(e));
c=floor(c);
c=sin(b);
g=log(d);
}
g=(atan(f))*(acos(b));
e=fmax(e,c);
}