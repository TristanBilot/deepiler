#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(g,f))*(exp(f));
b=floor(d);
h=(ceil(a))+(sin(g));
if(islessequal(h,d)){
d=atan2(h,b);
e=log(b);
}
g=(sin(a))+(floor(e));
e=(fmax(g,h))+(atan2(d,f));
}