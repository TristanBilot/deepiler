#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(b,c))/(pow(f,b));
g=(atan(a))+(sqrt(g));
b=(fmin(a,c))/(log(c));
f=atan2(h,d);
a=(fmax(a,e))-(atan2(d,b));
c=(sqrt(d))-(fdim(f,a));
g=asin(c);
h=(fmax(d,d))-(fmax(c,c));
g=fmax(e,h);
h=(tan(d))-(fdim(e,g));
a=asin(e);
a=fmax(h,e);
}