#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(e,d);
e=fdim(b,d);
if(isless(b,b)){
c=(fdim(d,b))*(tan(b));
c=cos(e);
c=fmax(a,e);
c=fmin(b,a);
}
else{
b=(fdim(b,b))-(fmin(b,a));
c=(sin(e))/(fdim(c,d));
c=sqrt(e);
c=sqrt(d);
}
while(islessequal(d,d)){
b=(pow(a,c))-(asin(d));
d=(tan(b))*(atan(a));
}
}