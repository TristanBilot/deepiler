#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(d))-(fmin(b,b));
e=(atan(e))*(fmax(e,d));
a=(tan(e))-(log10(b));
b=(fmin(e,d))/(pow(e,a));
e=fmax(c,b);
e=atan(e);
while(isless(a,d)){
e=(pow(a,e))-(tan(c));
e=(atan2(e,d))*(floor(d));
c=fmax(c,d);
}
}