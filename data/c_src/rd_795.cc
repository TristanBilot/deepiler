#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(e,e))/(fdim(d,d));
c=(asin(c))/(log(c));
d=fmin(b,c);
c=fdim(c,b);
while(islessgreater(d,a)){
b=fmin(c,a);
c=ceil(d);
a=pow(c,e);
e=(fmax(b,d))*(pow(d,e));
}
}