#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(cos(b))*(exp(d));
d=fmin(b,b);
d=(cos(d))*(fmin(d,c));
a=fmin(b,c);
a=atan(d);
a=cos(a);
a=(log(d))+(fmin(e,b));
d=pow(a,c);
a=(pow(e,c))+(cos(a));
}