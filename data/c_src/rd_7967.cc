#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=asin(e);
g=atan2(g,e);
f=(sqrt(f))/(acos(f));
g=acos(f);
while(islessequal(f,a)){
f=(floor(b))/(fdim(g,d));
c=log10(b);
a=fmin(c,d);
b=(log(e))-(floor(a));
}
while(isless(a,c)){
d=acos(a);
g=(acos(a))-(fmax(e,g));
}
}