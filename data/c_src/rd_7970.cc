#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(exp(b))*(fmin(b,a));
b=atan2(c,a);
c=fdim(g,c);
if(isgreaterequal(g,d)){
e=(log(c))+(log10(e));
f=(cos(f))+(atan(e));
}
else{
g=(fmax(a,f))+(exp(e));
e=fmin(d,a);
e=(atan2(c,f))*(log(e));
d=(sin(d))+(fdim(a,e));
}
if(islessgreater(d,f)){
a=pow(g,a);
b=(atan2(d,e))-(log(b));
f=exp(a);
f=fmax(f,d);
}
}