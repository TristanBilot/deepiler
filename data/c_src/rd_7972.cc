#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(e,a))+(acos(c));
a=(pow(a,a))*(exp(a));
a=(atan2(c,a))*(fdim(c,b));
while(islessequal(a,a)){
b=fmax(c,c);
b=fmax(b,e);
e=(fmin(a,c))+(atan2(d,c));
e=(atan(e))*(asin(b));
}
d=(sin(d))-(log(c));
d=fdim(e,e);
}