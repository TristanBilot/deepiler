#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(f,a);
f=(pow(f,b))+(floor(c));
if(islessequal(c,a)){
d=(atan2(c,f))+(tan(c));
a=tan(d);
}
while(isgreaterequal(f,d)){
c=(pow(b,c))-(exp(c));
e=floor(f);
d=(acos(e))*(fdim(d,d));
c=(pow(f,b))*(atan(c));
}
}