#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,a);
b=(fmax(b,c))+(sin(d));
b=(pow(b,b))*(asin(b));
c=(fmax(d,c))+(asin(e));
c=log(d);
d=(log(e))/(log(c));
e=(fdim(c,a))*(sin(c));
c=(fmin(b,a))+(exp(b));
d=(fdim(b,b))-(fmax(c,b));
e=(ceil(c))*(atan(b));
d=fdim(c,e);
e=(fmax(d,d))+(sin(a));
e=(sqrt(c))+(fmax(c,a));
}