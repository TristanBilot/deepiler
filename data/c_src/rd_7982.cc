#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(f,c))-(fmax(b,g));
c=(fmax(a,f))+(atan2(h,d));
h=(log(a))/(acos(h));
g=fmin(h,a);
while(isgreaterequal(b,g)){
g=(atan(e))+(cos(h));
g=log10(h);
f=(ceil(a))+(log10(g));
g=fdim(c,b);
d=(log(f))/(sin(e));
}
if(islessgreater(d,g)){
d=(exp(d))/(pow(a,h));
e=atan2(c,g);
}
}