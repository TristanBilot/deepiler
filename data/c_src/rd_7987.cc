#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan2(e,e))+(atan2(c,c));
b=cos(c);
f=(floor(f))*(sin(b));
e=(atan2(e,c))/(log(a));
e=atan2(c,f);
f=(cos(e))+(cos(f));
while(islessgreater(a,d)){
e=atan(b);
b=(pow(a,c))/(log10(e));
}
}