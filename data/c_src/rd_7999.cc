#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(d,c);
f=fmin(f,f);
b=exp(f);
d=log10(f);
f=fmin(c,a);
while(isgreaterequal(f,g)){
c=(exp(g))/(atan2(e,e));
d=fdim(b,d);
}
}