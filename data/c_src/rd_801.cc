#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(asin(c))-(pow(f,f));
d=fmin(a,e);
f=fdim(a,d);
b=fmin(c,b);
if(isless(f,d)){
d=fmax(e,e);
b=(pow(c,b))-(fmax(e,e));
a=tan(b);
f=fdim(f,e);
b=(fdim(d,e))*(pow(a,e));
}
if(islessequal(f,f)){
b=(fdim(a,c))-(fmax(c,d));
c=(acos(a))-(atan2(e,d));
d=fmax(d,a);
e=fmax(f,b);
}
else{
a=(ceil(c))*(acos(b));
c=(pow(f,b))*(sin(e));
d=atan2(e,e);
}
}