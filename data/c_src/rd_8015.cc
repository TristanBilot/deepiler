#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(d,c);
b=(sqrt(b))+(exp(b));
if(isgreaterequal(b,a)){
c=(fdim(d,c))/(fdim(e,e));
c=(pow(c,c))*(atan(a));
c=(tan(d))*(floor(b));
d=(atan2(e,d))-(log10(e));
b=atan2(a,c);
}
else{
e=sqrt(a);
b=pow(d,d);
a=pow(a,a);
}
c=tan(d);
b=fmax(e,b);
}