#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(a);
f=(exp(c))/(fdim(b,f));
while(isgreaterequal(h,h)){
c=(tan(c))/(fdim(b,b));
a=(fdim(g,e))*(asin(b));
h=(tan(c))+(exp(h));
e=(atan2(c,h))+(acos(d));
c=(fmax(e,a))*(cos(h));
}
while(islessgreater(e,a)){
d=tan(a);
a=(atan(e))+(log(b));
g=fmax(e,c);
h=pow(e,h);
}
}