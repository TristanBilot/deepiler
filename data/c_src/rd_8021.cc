#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(a,f))*(sqrt(a));
d=(pow(a,d))-(pow(c,h));
g=(floor(e))/(exp(d));
h=(log10(f))+(fmin(c,c));
a=(fmin(e,e))-(pow(b,e));
e=cos(a);
b=fmax(a,h);
g=(log(e))*(exp(g));
}