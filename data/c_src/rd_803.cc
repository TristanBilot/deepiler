#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=ceil(c);
e=(tan(d))/(atan2(b,d));
d=sqrt(a);
a=(ceil(c))-(atan(b));
c=(fmax(e,a))*(fdim(c,a));
while(islessequal(f,a)){
e=tan(f);
a=fdim(b,f);
c=fmax(a,d);
f=(fmax(b,a))/(fmax(f,f));
}
if(islessequal(c,e)){
a=(tan(b))*(fdim(d,a));
c=(pow(d,b))*(sin(a));
e=fmax(e,d);
c=atan2(b,e);
e=(pow(f,d))+(pow(f,a));
}
else{
a=exp(f);
b=(floor(f))*(ceil(d));
c=atan(e);
a=log(d);
}
}