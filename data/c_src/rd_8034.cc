#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log(e))*(log(d));
c=ceil(a);
if(islessequal(d,d)){
b=log10(b);
c=(pow(c,a))+(atan2(a,d));
e=(tan(a))*(fmax(d,e));
}
else{
b=fmax(c,c);
a=(acos(c))-(sin(d));
}
if(isless(a,a)){
b=fmin(d,d);
c=fmax(a,e);
c=(exp(c))/(fdim(a,b));
c=fmax(e,d);
e=(exp(a))-(cos(e));
}
else{
c=atan2(c,d);
d=(pow(a,a))+(atan2(a,a));
a=(fmin(d,e))+(acos(b));
}
}