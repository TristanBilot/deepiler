#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(c,b);
b=(tan(a))+(atan(b));
a=(pow(a,d))/(exp(b));
c=acos(d);
a=fmax(b,d);
e=(floor(b))-(fmin(c,d));
e=(atan2(a,e))+(fdim(b,b));
e=(atan(c))*(floor(e));
a=(asin(a))*(asin(c));
}