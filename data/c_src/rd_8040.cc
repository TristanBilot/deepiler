#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(a);
h=(exp(f))/(sin(g));
g=(sin(e))*(fmin(b,h));
if(isgreaterequal(h,b)){
e=(atan2(h,g))-(atan2(e,f));
h=(fdim(d,b))*(fmax(d,e));
e=(fdim(e,d))/(cos(b));
}
else{
e=log(c);
a=(fmax(f,c))+(atan2(b,b));
c=(log10(b))/(fmax(a,d));
c=(fmin(d,d))*(exp(d));
b=fmin(a,d);
}
if(isless(a,d)){
f=(atan(a))-(fmin(d,e));
c=pow(c,a);
g=(log(a))-(fdim(a,d));
a=fmax(g,b);
c=fmin(c,g);
}
}