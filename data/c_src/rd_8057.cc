#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(a))/(floor(f));
e=(fmin(e,f))/(sin(d));
d=(atan2(e,c))/(fmin(a,c));
d=pow(c,a);
c=log(d);
if(islessequal(c,d)){
c=(acos(c))*(exp(d));
f=fmin(b,c);
b=(fmax(a,f))*(exp(c));
b=fdim(d,f);
}
}