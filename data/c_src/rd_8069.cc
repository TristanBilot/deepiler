#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(a,d);
b=pow(a,d);
d=atan2(a,e);
b=tan(d);
a=(atan2(c,d))*(fmax(b,e));
while(isless(d,b)){
c=(atan(e))-(pow(c,e));
c=atan2(a,c);
d=(fmin(b,e))*(log(d));
}
if(islessgreater(a,b)){
d=(atan2(e,a))-(ceil(b));
a=(cos(e))*(fmax(e,d));
}
else{
b=fmin(d,b);
d=(atan2(b,b))*(log10(c));
a=fdim(b,a);
}
}