#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=floor(b);
c=(atan2(a,c))/(asin(f));
if(islessequal(d,b)){
g=(tan(c))/(sqrt(c));
f=exp(c);
e=atan2(c,c);
g=(asin(b))-(fmax(e,b));
}
else{
b=pow(b,d);
g=(fmax(e,b))*(asin(e));
b=fmin(c,c);
b=fmax(f,e);
}
e=pow(b,e);
f=(cos(e))+(sqrt(d));
}