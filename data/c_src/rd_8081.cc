#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(exp(c))/(ceil(a));
e=(fdim(c,d))/(acos(b));
c=log(f);
while(isgreaterequal(a,d)){
e=(atan(b))-(cos(e));
e=(sin(f))*(fmax(e,d));
}
if(isgreaterequal(d,d)){
d=exp(f);
b=fmin(c,c);
}
}