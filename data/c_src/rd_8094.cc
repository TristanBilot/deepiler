#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(d))*(exp(b));
d=fdim(d,c);
b=atan2(c,d);
e=(fdim(c,b))+(exp(e));
if(islessequal(a,a)){
a=(cos(d))+(floor(e));
c=fmin(d,e);
b=pow(a,c);
}
while(isless(e,b)){
d=(pow(a,b))+(floor(d));
d=(fmin(c,d))+(fdim(c,e));
e=(fdim(b,d))+(fmax(b,e));
}
}