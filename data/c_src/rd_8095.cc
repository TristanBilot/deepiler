#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(floor(b))-(pow(g,c));
g=floor(a);
f=asin(a);
h=fmax(e,b);
d=(atan2(g,f))-(exp(c));
f=pow(h,c);
d=ceil(c);
f=log(f);
f=cos(d);
}