#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(tan(a))*(fdim(e,c));
b=log(e);
c=(atan2(d,e))/(fdim(d,b));
d=sqrt(a);
a=(sqrt(e))+(fmax(e,c));
while(isgreaterequal(b,d)){
b=atan2(b,a);
c=cos(a);
d=(pow(b,c))+(fdim(b,d));
b=exp(b);
}
d=(atan2(c,d))+(fdim(b,b));
d=fmax(d,c);
}