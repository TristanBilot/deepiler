#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(c,b);
d=(fdim(b,f))*(atan2(c,a));
c=acos(f);
while(isless(f,f)){
c=fmax(a,f);
a=tan(d);
}
while(islessequal(b,a)){
c=fmin(c,c);
e=pow(f,d);
a=(fmin(f,e))*(exp(b));
b=pow(f,e);
e=atan2(e,b);
}
}