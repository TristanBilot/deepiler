#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(floor(b))+(exp(e));
d=(sqrt(b))+(fmin(a,c));
if(islessequal(e,a)){
b=(atan2(d,b))-(fmin(a,a));
b=(log10(a))*(fmax(b,b));
c=(floor(b))*(cos(d));
}
else{
e=(tan(b))+(ceil(d));
a=(sin(d))/(atan2(c,a));
c=(fmax(e,d))-(fmin(a,c));
a=atan(c);
c=asin(b);
}
while(islessequal(b,a)){
b=atan(e);
a=(cos(e))*(fmax(d,e));
b=(fdim(e,b))-(log(c));
}
}