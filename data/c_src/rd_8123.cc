#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(d);
d=fmax(a,d);
b=exp(e);
d=(pow(e,c))/(cos(c));
b=(acos(a))+(atan2(a,d));
c=log(b);
a=fmax(b,a);
a=(floor(a))/(fmax(e,b));
c=(atan(b))/(pow(e,e));
b=(sin(c))/(sin(b));
if(islessequal(a,a)){
d=(atan(b))/(pow(d,b));
a=ceil(b);
b=sin(b);
e=sqrt(c);
}
else{
a=(floor(e))/(fmax(c,a));
b=(sin(e))-(exp(b));
a=(atan2(c,e))+(cos(d));
}
}