#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(atan2(c,f))*(fdim(a,f));
b=cos(b);
while(isless(a,g)){
b=(fmin(b,f))+(exp(a));
b=(fmax(h,c))+(fmax(a,e));
}
d=(atan2(d,g))/(exp(b));
c=(fmax(h,c))*(fmax(c,f));
}