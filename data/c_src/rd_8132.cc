#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(acos(a))/(pow(f,h));
f=(atan2(h,a))-(asin(a));
g=(sqrt(d))+(exp(h));
b=(fdim(e,e))-(tan(f));
if(isless(g,c)){
c=fmin(a,h);
e=(fmin(a,g))/(fmax(e,f));
}
else{
h=(log10(e))/(atan2(f,e));
g=(floor(a))-(exp(b));
}
}