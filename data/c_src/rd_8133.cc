#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=tan(c);
b=fmax(c,a);
d=sqrt(e);
e=atan2(a,e);
c=(atan2(c,e))+(fmax(c,d));
d=tan(c);
b=tan(d);
c=fmax(c,b);
while(islessgreater(b,d)){
b=(atan2(c,c))-(tan(d));
d=log(e);
b=(fmin(b,a))/(fmin(d,d));
c=(fdim(e,d))/(floor(b));
a=fmax(b,c);
}
}