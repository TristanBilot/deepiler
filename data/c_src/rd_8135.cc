#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(d,h))/(fmax(b,g));
e=(atan(b))+(pow(h,e));
a=fmax(e,h);
c=fdim(g,f);
while(isless(a,a)){
b=(sin(e))*(fmax(c,b));
h=fmax(a,g);
}
g=fmin(b,d);
a=log(g);
e=(fmax(d,g))*(fmax(c,e));
e=(log(d))*(pow(b,a));
}