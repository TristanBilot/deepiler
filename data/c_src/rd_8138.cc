#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=tan(c);
d=fdim(d,b);
a=exp(c);
while(isgreaterequal(a,b)){
f=(fmin(a,c))/(fmin(f,e));
b=ceil(d);
}
if(islessequal(f,c)){
b=pow(f,e);
a=pow(e,e);
}
}