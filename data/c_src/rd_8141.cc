#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan(a))/(atan(e));
e=asin(b);
if(islessequal(e,a)){
f=tan(f);
e=(fdim(a,e))-(log(b));
}
else{
a=cos(a);
c=fmin(e,e);
}
while(islessgreater(c,b)){
b=fmin(a,e);
d=pow(d,a);
f=atan(b);
e=(tan(e))/(asin(d));
}
}