#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(c,d);
a=sqrt(d);
b=pow(c,c);
c=(pow(a,d))*(pow(c,c));
e=fmin(a,c);
a=(fmax(c,c))/(pow(c,d));
while(isgreaterequal(e,d)){
a=fmin(d,e);
b=acos(e);
}
}