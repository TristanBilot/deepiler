#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=ceil(b);
e=(fmax(b,d))+(atan2(b,a));
d=fmax(c,c);
d=pow(f,e);
if(isgreaterequal(f,e)){
c=cos(d);
a=(tan(c))/(fmin(b,b));
}
e=pow(e,b);
e=(acos(d))-(pow(e,a));
c=floor(f);
f=pow(d,b);
b=ceil(c);
}