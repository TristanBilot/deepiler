#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(b,c);
b=(pow(a,d))/(pow(a,e));
if(isgreaterequal(a,d)){
c=log10(d);
e=(atan2(b,c))/(atan(d));
c=(log(b))/(acos(a));
}
while(isgreaterequal(c,c)){
a=(tan(a))-(floor(a));
e=(tan(d))/(tan(c));
e=(fmax(b,d))-(fmax(b,e));
a=(cos(a))-(sqrt(b));
}
}