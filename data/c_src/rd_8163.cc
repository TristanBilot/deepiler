#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(d))*(fmax(a,c));
c=log(a);
d=(asin(a))/(log10(a));
e=fdim(b,c);
while(isless(b,c)){
e=pow(e,d);
e=(sin(a))+(fdim(d,d));
d=log10(a);
}
if(islessgreater(e,c)){
d=(fdim(e,a))+(acos(c));
d=(fmax(e,d))+(pow(a,c));
}
}