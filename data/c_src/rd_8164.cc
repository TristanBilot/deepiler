#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fmin(b,b))*(tan(d));
a=(exp(f))-(sin(a));
f=cos(c);
f=(log10(d))/(atan2(f,d));
e=atan(a);
c=pow(e,f);
b=asin(f);
c=pow(d,f);
a=atan2(a,d);
d=(fmin(b,b))+(sqrt(a));
while(isless(e,d)){
a=(ceil(a))-(atan2(d,a));
f=(atan2(f,a))-(sin(c));
c=(atan2(d,a))*(exp(a));
f=(pow(b,e))-(fmin(d,c));
}
}