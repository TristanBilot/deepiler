#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(c);
d=(floor(e))-(atan(f));
c=(fmax(a,h))/(fmin(d,b));
c=fmax(b,d);
h=atan(a);
if(isgreaterequal(f,f)){
h=cos(f);
g=ceil(c);
g=fmax(e,c);
d=(fmin(a,d))+(sqrt(h));
f=(atan2(f,f))/(ceil(f));
}
else{
h=(fdim(c,b))-(cos(g));
a=(fmin(h,a))*(log(d));
}
while(isless(e,b)){
f=(fmin(h,g))*(fmin(b,c));
c=(log(g))+(fdim(h,a));
g=(cos(d))*(sin(e));
g=log10(b);
h=(log(d))*(log10(c));
}
}