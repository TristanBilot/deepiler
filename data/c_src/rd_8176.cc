#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(c,d);
d=(pow(f,a))/(asin(e));
d=acos(b);
if(isless(g,c)){
f=exp(c);
a=sin(a);
}
if(isless(e,g)){
h=pow(a,e);
a=(fmax(e,b))+(fmax(h,h));
f=(exp(c))+(fdim(e,c));
h=(fmax(a,a))+(fdim(f,g));
d=(fmax(g,b))*(log(f));
}
else{
d=pow(d,f);
c=(sin(c))+(floor(f));
f=(acos(g))+(sin(b));
e=(cos(d))+(atan2(d,h));
f=(acos(b))/(atan2(d,f));
}
}