#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(f,h))/(atan2(c,a));
g=(log(e))+(fmin(a,a));
e=(fmin(e,d))/(atan(h));
while(isless(g,e)){
f=(pow(d,e))*(asin(h));
h=(fmax(e,d))+(fmin(a,g));
}
while(islessequal(a,f)){
f=(acos(h))*(pow(a,g));
h=(asin(e))+(exp(b));
e=(fmax(d,g))+(acos(f));
a=fmin(d,a);
d=fmin(f,g);
}
}