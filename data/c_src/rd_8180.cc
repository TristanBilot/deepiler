#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(d,e))*(atan2(b,d));
f=fmax(f,c);
c=tan(a);
d=sqrt(c);
b=fmax(e,a);
a=atan2(c,c);
e=fdim(e,b);
f=(tan(d))/(atan(e));
e=(atan(e))-(pow(b,c));
d=sqrt(b);
if(islessgreater(b,a)){
b=(ceil(f))+(pow(f,c));
e=fmin(b,b);
d=(fmax(e,f))-(acos(d));
}
}