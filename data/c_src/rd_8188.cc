#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(d,a))*(fmin(f,g));
c=(cos(g))*(cos(h));
while(isgreaterequal(h,f)){
h=atan2(c,g);
f=fmax(g,a);
e=(floor(e))-(fdim(a,f));
e=(pow(e,a))*(log10(c));
c=fdim(g,f);
}
c=pow(a,d);
e=asin(g);
f=(floor(b))-(pow(a,e));
e=tan(a);
a=(floor(g))+(pow(b,e));
}