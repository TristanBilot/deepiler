#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(b);
h=(sqrt(f))-(fdim(c,f));
e=(log(a))/(log10(a));
b=(atan2(g,a))*(fmax(b,h));
f=fmax(g,e);
c=(cos(h))*(acos(h));
a=(fdim(d,h))-(fdim(h,f));
g=(fdim(a,b))+(fmin(f,a));
g=(log10(e))-(atan2(b,e));
b=(acos(a))*(atan2(a,g));
e=fmax(g,h);
h=(pow(c,f))/(fdim(h,c));
}