#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(floor(d))*(asin(b));
a=pow(c,e);
d=(fmax(a,b))/(fmax(f,b));
e=fdim(b,e);
a=acos(d);
if(isless(a,c)){
e=fmax(d,c);
e=(floor(d))-(fmax(c,e));
e=(cos(d))*(tan(a));
c=(atan2(b,f))+(exp(e));
}
while(islessequal(d,a)){
a=(fmax(b,c))*(atan(c));
f=cos(f);
b=(log(c))*(floor(a));
}
}