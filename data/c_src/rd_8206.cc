#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(d,b))+(sin(d));
d=(pow(d,d))-(fmin(b,c));
e=(log10(c))+(ceil(a));
d=log10(c);
c=sin(b);
c=tan(d);
c=pow(d,b);
d=exp(c);
d=(acos(c))/(atan2(b,c));
a=asin(a);
e=log10(d);
}