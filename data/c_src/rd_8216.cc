#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(b))-(fmin(e,d));
d=log(b);
d=ceil(c);
c=floor(b);
c=fmax(a,e);
b=sin(d);
if(islessgreater(c,b)){
e=tan(a);
e=log10(b);
}
}