#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(b,b);
c=sin(c);
a=pow(b,c);
f=exp(e);
g=(log10(b))/(log(d));
if(isless(f,a)){
a=ceil(c);
g=(tan(c))+(fmax(a,f));
e=(atan2(g,e))+(pow(g,c));
e=(tan(c))-(fdim(a,b));
}
if(isless(f,a)){
a=atan2(c,f);
e=pow(b,d);
e=asin(a);
d=(sqrt(f))-(fmin(c,g));
}
else{
c=(atan(f))*(fdim(f,g));
e=(atan2(e,c))/(fmax(c,f));
f=pow(a,d);
f=fmax(g,e);
}
}