#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan(c);
d=(atan2(b,d))-(log10(d));
b=(pow(c,f))*(sin(f));
while(isgreaterequal(c,f)){
b=(pow(d,b))-(atan(c));
d=(log(c))/(fmax(a,f));
}
if(isgreaterequal(d,b)){
f=(pow(a,a))/(fmin(d,b));
e=(sqrt(f))/(floor(f));
c=(fmax(a,b))+(floor(b));
}
else{
d=sqrt(f);
e=(exp(f))-(asin(a));
d=(ceil(a))-(fmin(d,e));
}
}