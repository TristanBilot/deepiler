#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sin(a))*(fdim(b,d));
c=floor(d);
c=(floor(e))*(sin(d));
a=log(a);
if(isgreaterequal(d,c)){
c=(ceil(c))/(fmin(a,e));
a=asin(e);
b=(fmax(d,a))*(ceil(d));
a=(fdim(b,e))+(atan(a));
c=(fmin(b,c))*(log(b));
}
else{
e=fmax(b,a);
c=(fmax(e,b))+(log(a));
}
while(isless(e,a)){
b=(atan2(e,b))+(log(a));
e=(pow(d,e))+(fdim(e,c));
}
}