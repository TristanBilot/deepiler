#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=pow(e,a);
f=atan2(d,d);
b=(fdim(a,b))+(tan(b));
b=ceil(f);
a=(acos(e))/(atan2(f,d));
b=(atan(f))/(exp(c));
if(islessgreater(d,a)){
e=fmin(b,c);
a=(ceil(a))-(ceil(f));
f=(fmax(c,a))-(pow(b,c));
d=fdim(d,d);
}
else{
e=exp(d);
e=floor(c);
c=(sqrt(f))+(fmax(a,f));
}
}