#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(c,b);
a=(sqrt(d))/(asin(d));
e=fmax(f,f);
if(islessgreater(a,f)){
d=asin(a);
c=fmax(a,d);
f=floor(b);
}
else{
c=(pow(a,a))+(asin(c));
f=(fmin(d,f))+(fmin(a,d));
d=(log(e))*(atan(a));
c=(floor(c))*(pow(c,e));
d=exp(f);
}
if(islessgreater(c,e)){
d=sin(d);
c=(fmax(c,b))+(fdim(a,c));
e=sqrt(f);
d=(fmax(d,f))+(acos(d));
}
else{
c=fmax(c,b);
e=log10(d);
c=(ceil(f))/(atan(b));
}
}