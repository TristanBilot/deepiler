#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fdim(a,d);
d=log10(b);
b=pow(b,e);
d=(pow(e,a))/(atan2(d,d));
while(islessequal(a,b)){
e=floor(d);
f=(fdim(e,c))*(fmin(b,a));
b=(atan(c))+(atan2(f,a));
}
d=(atan2(a,e))*(atan2(e,d));
d=(exp(a))+(sqrt(d));
e=log(e);
e=(tan(b))+(atan2(c,e));
}