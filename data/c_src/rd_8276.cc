#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fdim(a,f))-(fmin(a,a));
e=(exp(a))/(pow(b,a));
d=pow(a,f);
g=atan2(e,e);
while(islessequal(a,d)){
f=(log10(a))*(cos(d));
e=(pow(f,a))-(fmax(a,c));
f=fmin(e,a);
c=(ceil(f))-(log(b));
}
a=(fdim(a,g))+(fmax(d,f));
a=(pow(c,c))-(pow(b,b));
b=(fdim(b,e))+(fdim(g,g));
b=(floor(a))-(fdim(e,e));
a=exp(e);
}