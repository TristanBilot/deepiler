#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan(f);
c=(fdim(e,g))-(ceil(a));
while(islessequal(f,c)){
f=cos(a);
c=(cos(a))/(atan2(d,e));
}
if(islessequal(c,b)){
g=(fdim(f,e))*(pow(e,e));
e=(pow(d,g))*(sqrt(e));
g=cos(b);
d=fmin(e,f);
}
}