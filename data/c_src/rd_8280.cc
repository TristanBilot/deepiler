#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=acos(e);
c=(log10(c))/(log(b));
c=(atan(e))+(tan(e));
if(islessgreater(d,c)){
d=pow(d,c);
c=fdim(b,b);
}
else{
c=atan(a);
c=(pow(b,b))*(sin(b));
b=(log10(a))+(sin(c));
c=(fdim(a,d))*(atan2(e,a));
a=(acos(e))+(tan(d));
}
c=cos(b);
d=pow(e,d);
b=(sqrt(c))+(fdim(c,d));
}