#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(exp(c))*(pow(d,b));
b=(sqrt(c))+(fmin(a,f));
b=tan(d);
b=(pow(e,c))-(fdim(f,b));
c=log10(b);
if(isgreaterequal(a,d)){
c=(fmax(d,a))-(fmin(g,c));
g=log10(g);
c=fdim(e,d);
a=(pow(g,a))-(sqrt(e));
}
if(isless(c,b)){
f=(fmin(g,g))+(tan(d));
f=(exp(a))-(fmax(e,b));
}
}