#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(b);
g=(fmax(f,d))/(pow(d,c));
g=fdim(e,d);
b=(asin(d))*(log10(b));
b=(cos(b))*(ceil(e));
g=pow(a,d);
h=fmax(d,b);
d=(fmax(a,h))*(fmin(e,g));
e=(fmin(f,g))/(floor(c));
c=fmax(d,f);
g=(ceil(c))/(fmax(f,d));
}