#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(cos(a))/(fdim(b,e));
d=fmax(a,a);
while(isgreaterequal(b,d)){
d=sqrt(e);
a=atan2(c,a);
a=(fdim(b,b))-(cos(c));
}
while(islessgreater(a,d)){
a=(atan(a))*(ceil(a));
e=(floor(d))-(fdim(e,b));
c=(atan(a))*(pow(e,b));
a=(atan2(e,e))-(floor(d));
}
}