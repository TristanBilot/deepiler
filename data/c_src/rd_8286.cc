#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(c,d))-(atan(d));
e=fmin(a,e);
b=(floor(b))+(log(b));
c=log10(b);
while(islessequal(b,e)){
e=exp(c);
b=pow(b,c);
d=(log(a))+(tan(d));
}
if(islessequal(d,b)){
d=(atan2(d,a))+(cos(e));
c=fmin(e,e);
}
else{
a=asin(a);
b=atan(d);
e=(log10(a))*(atan(a));
}
}