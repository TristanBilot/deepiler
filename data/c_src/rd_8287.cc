#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(a,d))/(pow(a,c));
c=(log10(d))-(fmin(e,e));
c=cos(b);
while(isgreaterequal(e,d)){
c=fmax(c,a);
b=fmin(e,e);
d=(atan2(d,e))*(exp(e));
}
while(isgreaterequal(b,a)){
a=(pow(d,e))/(asin(d));
c=(fdim(c,a))-(asin(e));
}
}