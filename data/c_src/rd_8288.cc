#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan2(c,b))+(acos(b));
d=exp(d);
f=(ceil(e))+(asin(b));
d=pow(f,d);
b=(log10(b))+(exp(d));
f=atan2(a,f);
f=(fdim(f,a))*(fmin(d,a));
b=log(e);
while(isless(e,d)){
f=asin(f);
f=(cos(d))-(log(d));
f=log10(b);
d=(log(e))/(pow(b,a));
}
}