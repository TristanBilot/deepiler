#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(exp(e))*(atan2(g,d));
a=fmax(a,c);
a=fmax(a,a);
e=fmin(e,a);
g=(cos(a))*(pow(f,c));
if(islessgreater(e,b)){
e=(floor(d))+(acos(a));
a=fmax(a,a);
c=fmin(a,f);
e=pow(b,a);
}
if(isless(c,a)){
b=sin(c);
d=(log(c))*(fmin(e,e));
g=pow(d,d);
}
}