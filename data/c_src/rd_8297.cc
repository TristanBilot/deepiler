#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(f);
b=(atan2(d,b))*(atan2(b,h));
if(islessequal(g,f)){
e=(sqrt(f))-(atan(e));
g=fmin(e,e);
h=sqrt(b);
e=fdim(e,b);
}
c=fmin(a,c);
g=(pow(a,d))-(fmax(d,h));
h=pow(h,d);
a=fmax(h,e);
}