#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(e);
h=(sin(e))+(atan2(a,c));
g=(fmax(g,g))+(ceil(c));
c=log10(h);
while(islessgreater(e,b)){
b=asin(b);
d=(sqrt(h))+(atan(e));
h=(pow(h,b))-(cos(h));
a=(asin(d))*(atan2(g,a));
g=(pow(a,b))+(exp(g));
}
while(isgreaterequal(f,h)){
h=(asin(d))/(sin(h));
f=cos(h);
h=ceil(c);
c=(fdim(d,g))/(tan(d));
c=fmax(g,c);
}
}