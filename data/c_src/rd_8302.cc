#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(d))-(exp(b));
b=(sqrt(e))+(fmax(e,d));
e=(fdim(c,e))/(atan2(c,b));
while(isless(e,b)){
c=(tan(b))*(asin(a));
c=(exp(c))+(tan(a));
e=(log10(b))+(fdim(a,e));
}
while(islessgreater(e,c)){
d=(exp(d))+(sin(b));
e=(exp(c))+(sqrt(c));
}
}