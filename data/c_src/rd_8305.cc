#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=cos(e);
e=floor(e);
f=(cos(e))/(fdim(c,g));
e=fmin(b,b);
g=(sin(a))-(log(e));
a=(fmin(a,a))/(fmax(c,b));
b=acos(d);
if(isless(c,f)){
e=fmax(e,a);
e=(log10(f))/(pow(c,a));
g=ceil(e);
g=(acos(d))/(tan(b));
}
else{
b=(fmin(g,e))*(sin(f));
g=(atan(d))+(fmax(f,g));
c=(pow(g,f))/(fmin(e,f));
g=(atan2(g,a))-(exp(g));
b=(atan2(b,d))+(pow(f,f));
}
}