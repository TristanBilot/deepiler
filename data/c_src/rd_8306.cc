#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(a,a);
c=sqrt(b);
b=(atan2(b,c))+(ceil(e));
e=(tan(a))*(cos(d));
e=fmin(c,d);
if(isgreaterequal(a,b)){
a=(fdim(e,c))/(floor(d));
d=(cos(b))/(fmax(c,b));
a=exp(c);
}
else{
a=fdim(d,b);
c=(fdim(b,b))/(sqrt(d));
e=floor(d);
d=(atan2(c,b))/(fmin(c,b));
}
}