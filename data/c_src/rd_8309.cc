#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(b,c);
h=(log(g))/(atan2(a,e));
e=(log10(b))/(sin(a));
if(islessequal(h,d)){
b=floor(g);
c=fmax(f,c);
f=(cos(f))/(exp(b));
b=(fmin(g,e))-(acos(b));
g=(sin(e))-(fmin(h,c));
}
if(isless(h,h)){
c=cos(g);
a=(sqrt(h))-(log10(a));
}
else{
g=sqrt(e);
e=(cos(b))/(atan(g));
g=exp(d);
}
}