#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=acos(e);
e=acos(c);
b=(log10(d))/(atan2(c,e));
c=(log10(a))*(fmax(a,e));
a=sqrt(b);
while(isless(b,e)){
d=asin(d);
b=(fmax(d,a))+(fmin(c,e));
}
}