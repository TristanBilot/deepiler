#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(a,e))*(log10(a));
e=asin(d);
if(isless(a,d)){
b=(acos(a))*(tan(e));
a=(atan(b))+(atan2(c,b));
e=(asin(b))/(fdim(a,e));
d=floor(c);
}
else{
a=log10(d);
d=log10(d);
b=(acos(a))+(pow(c,a));
c=tan(b);
}
if(isless(e,e)){
d=(acos(d))-(pow(d,b));
e=fdim(a,c);
c=(exp(b))*(fdim(c,b));
}
}