#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmin(c,b);
d=log(a);
c=(log(b))/(atan2(f,c));
d=pow(a,c);
while(isless(f,d)){
d=atan(f);
e=(atan2(a,c))-(fdim(a,a));
d=fmax(b,a);
e=fmin(f,e);
d=(pow(a,e))/(pow(c,a));
}
if(isgreaterequal(b,d)){
a=cos(c);
f=asin(e);
}
}