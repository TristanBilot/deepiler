#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(g);
c=pow(d,f);
while(isless(e,b)){
g=atan(h);
d=(fmax(e,h))/(exp(b));
d=cos(b);
}
if(islessequal(f,f)){
g=(exp(a))*(fdim(h,h));
e=(log(c))+(fmin(h,d));
f=fmin(c,g);
}
else{
f=(fmin(e,f))+(asin(e));
a=fmax(f,g);
c=(sqrt(c))/(fmin(c,g));
b=sqrt(h);
}
}