#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(sin(e))-(atan(c));
e=(log(d))*(fdim(d,a));
while(isless(d,b)){
d=(fmax(e,b))+(fmin(e,d));
e=(fmax(e,e))-(pow(a,c));
b=pow(d,c);
e=(sin(d))*(fdim(e,b));
e=fmin(a,d);
}
if(islessgreater(a,d)){
d=pow(b,d);
b=(floor(b))/(pow(b,e));
d=pow(c,a);
}
}