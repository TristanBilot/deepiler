#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log(e);
e=(sqrt(h))-(atan2(h,e));
f=tan(a);
h=(atan2(b,h))-(fmax(a,b));
h=fmax(g,c);
if(islessequal(a,a)){
h=exp(c);
e=log(e);
d=fmin(b,e);
d=(fmax(e,c))/(ceil(c));
g=(pow(f,d))+(log(c));
}
else{
h=(fdim(c,c))*(atan2(b,f));
h=ceil(d);
b=(log10(h))/(sqrt(b));
b=pow(b,b);
}
if(islessgreater(h,f)){
h=(fmax(e,c))+(atan2(f,d));
d=fmin(b,f);
}
else{
d=(sin(d))+(atan(g));
h=(acos(f))*(log(e));
}
}