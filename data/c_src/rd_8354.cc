#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmax(c,a);
c=(asin(f))/(asin(b));
g=atan2(a,a);
c=fmax(b,f);
if(islessequal(g,e)){
f=(acos(f))/(pow(d,e));
e=(fmax(b,c))/(acos(b));
}
if(isless(b,g)){
a=(atan2(e,g))/(asin(d));
d=(pow(a,c))*(fdim(b,a));
f=log(b);
a=(ceil(e))-(asin(a));
c=fmin(a,d);
}
else{
f=sin(c);
g=sqrt(f);
e=(log(f))/(atan2(f,g));
d=atan2(g,b);
e=(atan2(e,e))-(floor(e));
}
}