#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(atan2(a,f))*(fmax(g,e));
c=(atan2(f,h))+(atan2(b,b));
if(isgreaterequal(a,e)){
e=(log(h))/(fdim(h,f));
e=exp(a);
f=log(f);
}
else{
b=(fdim(a,b))+(atan2(e,e));
d=pow(g,b);
e=(pow(g,b))+(fdim(g,a));
d=tan(b);
f=(fmin(a,a))*(fmax(f,c));
}
while(islessgreater(b,g)){
g=(asin(e))-(fdim(a,c));
d=fmin(a,f);
f=fmax(d,c);
c=fdim(c,b);
h=atan2(f,h);
}
}