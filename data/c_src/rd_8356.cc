#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(b,e))/(sin(c));
d=ceil(c);
e=pow(a,b);
b=cos(g);
d=(log10(b))-(fdim(d,a));
f=fmax(e,d);
a=fmax(g,d);
c=(atan(a))+(fdim(b,a));
b=(atan2(e,d))/(atan(e));
}