#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan(b);
b=(asin(b))*(fdim(c,e));
c=(sqrt(d))*(fmin(c,d));
a=fmax(e,b);
if(islessgreater(d,a)){
e=floor(c);
e=fmin(b,b);
}
else{
d=(fmin(b,d))-(ceil(a));
c=fmin(c,d);
e=(pow(a,b))+(fmin(c,c));
a=ceil(e);
}
e=fdim(e,c);
a=fdim(c,b);
c=fmin(b,b);
a=fdim(c,a);
a=(sin(d))+(fdim(e,a));
}