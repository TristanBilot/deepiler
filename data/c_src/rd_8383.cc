#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(pow(c,e))+(pow(f,b));
g=exp(b);
e=(sin(f))/(exp(c));
e=(fmin(g,a))+(log10(c));
d=(ceil(c))+(pow(g,d));
while(islessgreater(e,b)){
b=fmax(e,g);
d=(sqrt(e))+(atan2(e,f));
g=pow(a,c);
e=(fmax(e,b))-(acos(a));
}
f=(asin(h))*(exp(d));
h=(fmax(a,h))*(fmax(a,h));
h=(floor(f))+(fdim(d,d));
}