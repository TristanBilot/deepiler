#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=asin(e);
a=(asin(b))/(cos(c));
f=(fmin(f,c))*(fdim(d,e));
f=(atan2(e,a))*(exp(c));
f=(pow(d,b))+(exp(a));
if(islessequal(f,f)){
b=log10(b);
e=tan(b);
a=cos(b);
}
if(isgreaterequal(d,a)){
c=(atan2(c,b))*(tan(a));
f=exp(e);
}
}