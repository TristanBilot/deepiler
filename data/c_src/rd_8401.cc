#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmax(e,f))+(atan(g));
f=(atan2(f,g))-(exp(c));
b=sqrt(e);
while(isgreaterequal(g,c)){
c=acos(c);
d=(cos(c))*(atan(d));
f=(pow(a,a))+(fmax(b,a));
}
e=floor(a);
c=(tan(b))/(atan2(c,a));
}