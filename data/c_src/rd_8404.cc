#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(e,b))/(fmin(b,c));
d=(cos(c))+(cos(b));
c=fmax(e,d);
while(islessgreater(a,e)){
c=(atan2(a,e))+(acos(d));
d=sin(d);
}
b=(log(e))*(fdim(d,c));
a=asin(c);
}