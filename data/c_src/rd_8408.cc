#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(d,d);
g=atan2(a,d);
g=(atan(e))+(log(a));
f=(atan2(g,f))+(log10(a));
d=sqrt(e);
while(islessgreater(g,e)){
c=(pow(g,c))+(pow(d,f));
e=asin(a);
d=cos(g);
}
if(islessgreater(d,d)){
f=(log10(b))/(ceil(a));
a=(fmin(f,g))-(fmin(a,a));
d=fmax(g,c);
}
else{
f=cos(g);
b=atan2(d,f);
c=(tan(e))/(fmax(b,f));
c=atan(f);
}
}