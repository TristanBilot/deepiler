#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(c,b))/(exp(e));
a=(atan(a))-(atan2(d,c));
d=(log10(d))*(asin(b));
e=(tan(d))/(sin(e));
while(islessequal(a,a)){
c=(sqrt(e))*(pow(c,e));
c=fdim(e,c);
a=tan(c);
e=fmin(d,b);
}
b=(asin(c))-(sin(e));
b=(pow(d,e))+(exp(e));
}