#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(b);
e=(fmin(f,b))+(atan(b));
c=(atan2(f,e))+(sqrt(c));
if(isless(h,c)){
b=fdim(a,a);
e=fmin(c,e);
h=(atan2(e,b))-(floor(b));
b=fmin(a,e);
e=fmax(b,h);
}
else{
b=(sin(d))+(sqrt(g));
h=sqrt(a);
e=(tan(b))-(ceil(a));
a=(fmin(a,a))+(exp(d));
b=(floor(c))/(asin(e));
}
if(islessequal(f,c)){
e=(pow(h,d))-(fmin(d,d));
d=(fmin(e,c))/(fmin(h,h));
c=(log(d))-(atan2(a,a));
a=(cos(h))-(log(g));
h=(tan(d))/(fmin(a,a));
}
}