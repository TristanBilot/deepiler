#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(f,d);
e=(fmax(d,c))-(pow(c,f));
f=asin(a);
d=atan2(a,f);
a=fdim(f,a);
d=(sqrt(e))+(sin(e));
d=fmin(b,a);
c=(atan2(c,e))-(fmin(d,d));
e=(exp(d))-(pow(c,e));
f=fmax(b,e);
while(islessgreater(c,a)){
c=(exp(d))*(sin(e));
f=(exp(e))-(pow(b,e));
b=(fmax(a,b))*(atan(f));
f=(fmin(d,a))/(asin(c));
}
}