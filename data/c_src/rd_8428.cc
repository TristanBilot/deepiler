#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmax(b,h))+(log(f));
c=fdim(f,f);
h=sqrt(c);
d=(atan2(h,d))/(fdim(b,c));
f=(log10(a))/(pow(e,h));
a=atan2(h,g);
e=(log10(d))-(tan(g));
d=(pow(a,c))+(log10(c));
a=exp(e);
if(isgreaterequal(g,b)){
c=(atan2(h,a))-(ceil(h));
d=cos(h);
}
else{
g=(ceil(g))*(ceil(g));
c=sin(f);
}
}