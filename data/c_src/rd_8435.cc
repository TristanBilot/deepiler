#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(b,d))/(floor(f));
f=acos(a);
e=(fmin(f,c))+(cos(a));
f=(pow(d,g))+(fmin(c,d));
e=(fmax(d,g))+(sqrt(e));
if(islessequal(f,g)){
b=(tan(b))-(fmin(d,e));
b=fmin(c,d);
}
while(islessequal(f,b)){
c=(cos(b))*(fmax(g,d));
a=floor(a);
a=(atan2(c,f))/(pow(b,d));
}
}