#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(c,a))-(tan(d));
c=(fmin(f,f))+(floor(b));
c=fdim(c,a);
if(islessequal(a,d)){
d=asin(c);
a=atan2(c,g);
g=(log10(d))*(asin(e));
}
else{
g=(sqrt(a))/(pow(b,d));
d=(fmax(a,a))*(fdim(c,a));
}
a=fmax(c,b);
c=(floor(f))+(tan(g));
d=(fdim(d,c))/(fmax(f,e));
a=(log(b))+(asin(b));
b=(pow(d,c))*(asin(g));
}