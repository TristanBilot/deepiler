#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(b,f);
g=(fdim(c,a))+(atan2(d,f));
f=floor(c);
g=(asin(c))+(atan2(h,b));
if(isgreaterequal(f,d)){
h=sqrt(d);
d=(atan(h))/(sin(f));
}
b=floor(b);
e=(log(d))/(fmax(d,a));
}