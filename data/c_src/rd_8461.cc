#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(d,f))+(cos(a));
a=(fmin(b,a))+(cos(b));
c=(acos(e))*(ceil(d));
a=(exp(f))/(sin(b));
b=pow(d,f);
c=(fmin(f,d))/(log10(h));
e=fmax(g,b);
g=(sin(d))+(sin(e));
e=(atan(g))*(cos(a));
if(isless(e,f)){
c=(sin(a))+(pow(g,a));
h=(fmax(b,e))/(exp(g));
}
else{
b=fdim(b,b);
g=fmin(c,h);
h=(atan2(d,a))*(atan2(f,c));
e=pow(c,f);
d=floor(c);
}
}