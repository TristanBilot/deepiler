#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fdim(h,h))/(atan2(d,b));
f=cos(c);
f=(pow(b,h))*(log10(h));
d=(fmin(f,f))*(fdim(f,f));
a=pow(f,f);
if(isless(f,h)){
c=log10(c);
f=atan2(e,d);
f=(fmax(f,a))*(log10(h));
g=atan2(b,g);
d=pow(f,h);
}
else{
e=(asin(e))+(atan(e));
b=(fmax(d,a))/(floor(e));
b=floor(c);
d=(asin(h))*(fmax(c,h));
e=atan(d);
}
e=(atan2(f,g))/(atan2(a,b));
d=fmax(c,h);
h=atan2(f,e);
}