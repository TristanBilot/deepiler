#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(floor(d))*(fmax(a,e));
d=(pow(b,a))/(tan(c));
b=floor(c);
if(isgreaterequal(a,e)){
a=log10(b);
d=atan2(a,d);
b=(atan2(a,e))+(log10(a));
a=(exp(e))*(atan(b));
c=fmin(b,d);
}
else{
a=(fmax(b,a))/(fmax(d,d));
c=(atan2(e,d))*(asin(e));
}
d=log10(d);
a=(fdim(b,a))-(pow(c,c));
b=fdim(d,c);
e=(atan(c))+(sqrt(c));
a=(fmin(e,d))/(pow(e,c));
}