#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(g,d);
e=(pow(c,d))/(asin(f));
f=(fmax(g,c))+(fmin(b,c));
f=fdim(g,b);
if(islessequal(f,b)){
f=(atan2(c,c))*(floor(f));
c=ceil(g);
c=(pow(d,f))-(exp(c));
c=(fdim(e,b))-(pow(f,e));
g=(fmax(b,e))-(fdim(b,e));
}
else{
a=(asin(g))+(log10(e));
c=(ceil(g))+(asin(c));
d=log10(e);
}
}