#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=floor(b);
e=(fmax(d,d))-(log(b));
f=(fmax(a,e))+(acos(b));
while(islessequal(b,e)){
c=fmax(d,f);
c=fmin(a,c);
}
while(islessgreater(c,e)){
d=fdim(a,f);
d=fmin(e,e);
d=atan2(e,f);
}
}