#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log10(a);
e=(log(c))/(pow(e,e));
f=floor(c);
a=(ceil(f))/(fmax(d,f));
while(isgreaterequal(f,g)){
a=(fdim(d,e))/(asin(f));
g=(fdim(g,g))-(fmin(d,b));
g=(exp(g))*(fmin(d,b));
}
if(isless(f,b)){
b=atan2(e,b);
f=(log10(d))/(floor(b));
e=(fdim(b,a))*(fmax(f,b));
}
}