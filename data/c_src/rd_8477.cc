#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sqrt(b))*(pow(c,d));
d=fdim(a,b);
d=fdim(a,e);
d=(tan(c))*(pow(d,e));
e=(fdim(d,c))*(fmin(a,d));
d=(fmax(a,d))+(fmax(d,a));
a=fmax(b,b);
e=fmin(a,b);
d=cos(c);
if(islessgreater(d,c)){
c=fmin(e,a);
c=(asin(e))+(fmin(a,a));
d=fmin(a,c);
}
}