#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(d,g))/(floor(h));
e=(cos(h))*(log(e));
h=(log10(h))*(fdim(b,d));
c=(log(b))+(pow(a,b));
a=fdim(h,c);
g=(fdim(h,f))+(acos(a));
d=(fmax(d,h))*(log(g));
if(islessgreater(b,b)){
d=atan2(d,c);
a=pow(d,g);
}
}