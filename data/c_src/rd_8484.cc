#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(exp(b))*(fdim(b,d));
b=atan(e);
f=(pow(e,c))-(fmin(g,b));
b=sin(a);
c=(fmax(c,f))/(atan2(a,e));
g=pow(b,c);
e=exp(b);
c=exp(c);
b=fmin(c,f);
}