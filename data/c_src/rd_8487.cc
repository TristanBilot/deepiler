#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(c);
c=fdim(a,e);
if(isless(c,d)){
b=fmax(e,a);
b=(ceil(a))/(sin(c));
e=(fmax(c,b))/(ceil(e));
c=(pow(a,e))*(log(e));
}
b=acos(c);
a=pow(a,c);
}