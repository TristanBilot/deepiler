#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(e);
e=cos(e);
a=fmin(d,e);
b=(atan2(b,b))+(asin(e));
while(isgreaterequal(d,b)){
b=pow(e,b);
a=log(e);
b=(fmin(a,e))*(log(d));
}
while(islessgreater(e,c)){
a=(exp(c))/(pow(a,c));
d=floor(e);
a=(fmin(d,a))-(atan2(b,a));
}
}