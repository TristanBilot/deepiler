#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(d);
e=(pow(b,e))*(fmin(a,b));
if(islessgreater(e,a)){
c=(log(e))+(fmax(c,d));
d=(fdim(b,b))+(log10(d));
}
else{
a=(fmin(b,a))-(log10(b));
e=(ceil(d))*(exp(b));
}
while(islessequal(d,c)){
c=(log(c))+(pow(c,b));
b=fdim(a,d);
}
}