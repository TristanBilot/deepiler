#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(atan2(c,f))-(cos(e));
c=(atan(b))/(atan(b));
while(isless(e,a)){
e=fdim(d,a);
g=acos(d);
a=fdim(d,d);
g=fmax(a,d);
f=atan(b);
}
f=(asin(c))/(log(e));
g=(fmin(b,f))*(pow(c,a));
e=pow(a,e);
d=pow(g,c);
}