#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(b,b))/(fmin(b,b));
c=(atan(c))-(fmin(b,c));
d=(fmax(e,d))*(fmax(d,b));
d=fmax(b,c);
b=pow(c,c);
e=(cos(a))-(log(d));
e=cos(a);
}