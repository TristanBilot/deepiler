#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(asin(a))-(fmax(c,d));
a=(fdim(e,g))/(sin(e));
e=(fmin(d,d))*(fmin(b,h));
b=(atan(e))*(log10(a));
b=sqrt(g);
if(islessgreater(h,c)){
e=(pow(b,f))*(asin(f));
c=(sqrt(h))/(ceil(b));
c=(pow(a,d))/(fdim(a,h));
}
else{
g=(sin(g))+(log10(g));
e=(exp(c))-(fdim(a,e));
c=atan2(a,h);
e=cos(e);
a=fmin(f,a);
}
while(islessequal(f,d)){
d=(sin(e))/(atan2(g,f));
a=floor(h);
f=fmax(d,g);
}
}