#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(d,e);
d=ceil(e);
b=fmin(c,c);
if(islessgreater(e,e)){
c=(sqrt(e))-(atan2(c,e));
a=(fdim(b,c))-(sqrt(d));
}
e=(log10(a))/(sin(e));
a=exp(d);
e=fmax(d,a);
}