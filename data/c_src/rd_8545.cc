#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(b,f))-(fmin(e,a));
a=(sin(d))*(asin(b));
while(isgreaterequal(f,b)){
a=ceil(a);
d=log10(c);
d=atan2(d,b);
}
if(isless(a,d)){
d=(pow(a,b))/(fdim(f,a));
f=(fmax(e,d))/(tan(c));
d=atan(e);
}
else{
c=atan2(e,a);
e=sqrt(d);
c=(sin(d))/(fmax(c,d));
f=fmin(b,e);
}
}