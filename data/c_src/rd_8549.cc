#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(cos(c))-(pow(d,e));
e=acos(e);
e=(fmin(a,e))-(floor(a));
e=(exp(b))*(log(e));
a=(sin(e))*(cos(b));
if(isless(c,a)){
b=(exp(b))-(cos(a));
e=asin(e);
}
}