#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(d,c);
c=cos(d);
c=(pow(f,c))-(pow(b,e));
a=sin(d);
e=(fdim(h,d))-(sqrt(a));
if(isless(g,c)){
g=pow(a,a);
c=exp(d);
e=(tan(a))/(fmax(f,c));
h=(cos(d))-(ceil(g));
f=(log10(h))/(tan(g));
}
else{
f=(atan2(a,c))*(pow(g,h));
g=atan2(g,b);
h=fdim(g,f);
d=(fdim(f,a))*(fdim(h,c));
}
}