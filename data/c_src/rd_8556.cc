#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=tan(e);
c=(acos(e))/(log10(b));
b=floor(e);
e=cos(b);
d=(fmax(e,b))*(atan2(a,a));
while(islessgreater(a,c)){
e=(log(b))*(fmin(a,b));
e=log10(d);
d=(fmax(b,a))*(fdim(c,e));
b=(sqrt(e))*(atan2(b,a));
c=(atan2(e,b))+(tan(b));
}
if(isless(a,b)){
a=sqrt(b);
a=(pow(a,d))+(fmin(a,b));
b=(exp(d))+(tan(b));
}
else{
d=(atan2(d,b))/(fdim(b,d));
d=exp(a);
}
}