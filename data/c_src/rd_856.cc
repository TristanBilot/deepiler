#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log(g);
b=fmin(e,a);
if(islessequal(c,d)){
h=(floor(c))/(fdim(h,b));
d=fmin(g,d);
g=sin(f);
e=(cos(c))+(atan(f));
}
else{
h=(fmax(f,e))/(sin(f));
d=(ceil(f))/(floor(h));
h=(exp(c))+(cos(g));
a=fmax(d,h);
}
if(islessequal(c,g)){
e=log(f);
f=(pow(c,h))*(exp(b));
h=ceil(a);
}
else{
e=(fdim(h,f))-(log10(a));
c=(sqrt(c))/(fmax(h,e));
}
}