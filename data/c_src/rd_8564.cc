#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(d);
d=(pow(e,a))*(fdim(e,e));
while(isless(d,c)){
e=(ceil(e))*(exp(c));
d=(asin(d))-(fmin(b,c));
}
if(islessgreater(d,e)){
c=(atan2(c,e))/(pow(d,d));
b=(fmax(a,c))*(atan2(e,d));
b=(fmax(b,e))*(exp(e));
a=(atan2(e,e))-(fmax(d,c));
c=(fmin(a,c))/(log10(b));
}
}