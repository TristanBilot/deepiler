#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(sin(c))+(fmax(d,a));
c=atan2(c,a);
a=sin(e);
g=(pow(c,f))+(acos(h));
g=acos(d);
a=(sin(b))+(atan2(c,f));
g=(atan(h))*(fmin(f,d));
d=(fmin(h,a))+(ceil(b));
g=(log(g))*(fmax(b,g));
if(isless(e,e)){
h=(floor(e))/(floor(h));
d=tan(a);
c=pow(e,e);
e=sin(c);
}
}