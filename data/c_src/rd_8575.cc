#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(d,c);
a=(fdim(d,e))/(atan2(e,b));
e=atan2(d,d);
b=pow(b,b);
e=(fdim(b,b))/(asin(c));
e=(atan2(a,e))+(acos(d));
if(islessequal(a,d)){
b=(fdim(c,b))+(fmin(c,d));
d=(atan2(c,c))/(fmin(d,b));
}
}