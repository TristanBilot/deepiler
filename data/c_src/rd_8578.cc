#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(b,g))+(fdim(c,f));
a=(log(g))-(log10(c));
e=(fmin(c,b))-(fdim(e,g));
e=tan(f);
g=fmin(b,h);
while(isless(e,f)){
e=log10(e);
e=(ceil(a))-(fmax(f,a));
g=(floor(b))+(log(b));
}
c=fmin(c,d);
c=floor(c);
a=sin(f);
}