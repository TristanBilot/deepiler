#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(e,d))/(fmax(b,a));
f=(sqrt(d))*(fmin(h,g));
b=fdim(c,e);
c=(fmin(f,b))*(log10(e));
d=(fmin(g,d))*(sin(e));
c=sin(e);
d=fmax(f,b);
f=fmin(c,f);
d=cos(d);
a=(sqrt(g))+(pow(g,d));
}