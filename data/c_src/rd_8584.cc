#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,d))/(exp(a));
d=(asin(b))/(cos(b));
b=(ceil(a))*(atan2(e,c));
d=(fmax(d,a))-(fdim(a,a));
d=ceil(b);
while(islessgreater(b,e)){
b=ceil(a);
a=floor(d);
c=(pow(d,e))/(fmin(e,e));
c=(acos(a))*(asin(a));
}
if(isgreaterequal(e,b)){
a=log(b);
b=fmin(e,e);
}
else{
a=atan(b);
d=sqrt(c);
b=fmax(e,c);
}
}