#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(b,b))-(fmax(e,d));
e=(log(b))+(fmax(a,f));
f=log10(b);
while(isgreaterequal(b,e)){
e=(fdim(e,e))+(fmin(f,a));
b=(cos(d))/(fmax(c,c));
a=(tan(d))*(asin(c));
e=fdim(e,f);
e=(atan2(c,e))*(sin(b));
}
while(isgreaterequal(d,c)){
a=ceil(d);
f=sin(d);
e=(cos(a))-(log(b));
}
}