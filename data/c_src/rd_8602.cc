#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(atan2(c,f))+(atan2(a,c));
e=pow(g,b);
f=cos(a);
c=fmax(b,d);
if(islessgreater(e,g)){
a=(log10(a))-(asin(f));
g=(atan(g))+(fmax(b,a));
c=(log10(a))+(atan2(e,b));
}
while(isgreaterequal(f,b)){
b=pow(b,a);
c=log10(a);
}
}