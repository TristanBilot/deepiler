#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fdim(b,f))-(atan(a));
e=(floor(h))*(fmax(g,c));
c=fmax(d,b);
g=(atan2(b,f))+(fmax(d,h));
d=fmax(d,f);
e=atan(g);
g=(fmin(g,h))*(sqrt(c));
while(islessequal(h,e)){
c=sin(d);
e=pow(d,g);
a=ceil(b);
g=(atan2(f,g))*(pow(e,h));
f=(asin(b))/(atan2(e,d));
}
}