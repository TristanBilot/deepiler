#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(e,f))-(exp(g));
b=(fmin(c,b))/(sqrt(g));
d=(cos(h))+(pow(h,g));
d=log(a);
b=(fmin(g,f))+(fmax(g,g));
b=fdim(e,g);
c=sqrt(d);
c=fdim(e,e);
a=(fmin(h,d))+(fmin(a,b));
a=(fmax(h,d))+(sin(d));
h=(log10(h))*(atan2(h,d));
a=fmax(g,h);
}