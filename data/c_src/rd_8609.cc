#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(g,f))/(pow(h,c));
h=(pow(c,b))/(fmin(c,h));
f=fmax(b,h);
b=fdim(h,e);
c=(fdim(g,b))*(fmax(e,c));
while(islessequal(d,a)){
c=(fmin(e,h))*(fdim(h,b));
a=(asin(a))-(atan2(b,h));
g=(log10(e))-(acos(e));
g=atan2(c,d);
}
d=fmax(e,a);
c=(fmax(a,d))*(acos(f));
h=(floor(h))*(tan(f));
h=pow(d,b);
a=ceil(b);
}