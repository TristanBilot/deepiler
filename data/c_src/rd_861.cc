#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=pow(c,d);
c=log10(e);
c=(pow(c,b))-(ceil(f));
d=fmin(a,f);
c=atan2(c,c);
c=(log10(a))*(fmin(b,e));
f=fdim(f,a);
while(islessequal(d,a)){
f=(atan(d))+(fmax(d,b));
c=pow(a,c);
}
}