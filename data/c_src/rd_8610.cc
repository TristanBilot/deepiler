#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(h);
b=(atan2(c,g))*(log(d));
c=(asin(f))-(cos(b));
f=atan2(b,a);
a=ceil(b);
while(islessequal(c,b)){
b=fmax(e,f);
e=pow(f,f);
f=(fmin(f,f))-(tan(e));
}
h=(floor(a))+(atan2(h,e));
g=atan(a);
}