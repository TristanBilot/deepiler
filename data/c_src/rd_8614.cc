#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fdim(b,d))/(asin(e));
a=pow(e,e);
c=(fdim(f,e))+(tan(b));
e=fmax(f,f);
b=(tan(b))+(floor(e));
d=pow(b,d);
a=(fmin(a,d))*(exp(a));
f=(fmax(d,c))/(sin(f));
}