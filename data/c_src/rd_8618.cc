#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=floor(e);
f=(fmax(g,b))+(sqrt(h));
c=(acos(c))+(atan2(f,b));
g=(atan2(e,d))*(pow(h,a));
f=(fdim(d,f))*(atan2(h,d));
while(islessequal(d,b)){
h=(cos(b))-(fmax(c,g));
d=fmax(h,g);
f=floor(g);
b=(pow(e,c))-(fdim(b,a));
}
g=(acos(f))*(atan2(b,c));
c=(acos(g))/(exp(b));
g=(pow(h,e))+(cos(d));
f=(tan(d))+(exp(h));
}