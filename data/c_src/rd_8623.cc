#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=exp(c);
e=(atan(e))+(log(d));
d=(sin(f))/(exp(a));
while(isless(c,a)){
f=sin(f);
c=sin(d);
}
while(isless(e,b)){
f=(fmax(e,b))+(pow(a,e));
a=acos(b);
e=acos(f);
}
}