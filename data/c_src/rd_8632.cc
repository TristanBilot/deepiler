#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(d,b);
c=(log10(b))-(atan2(a,b));
if(isgreaterequal(a,c)){
f=(fmin(a,d))+(exp(f));
b=fmax(c,g);
b=(ceil(f))-(fmin(b,d));
}
else{
d=(fdim(b,c))+(exp(b));
f=asin(f);
d=(fmin(b,a))/(atan(c));
d=fdim(d,a);
}
if(isless(g,b)){
a=floor(f);
d=log(e);
g=fmax(c,g);
c=floor(d);
b=(log10(a))*(log(f));
}
else{
c=(atan2(c,e))/(fmin(d,g));
b=(fmin(e,g))/(log10(b));
c=asin(e);
e=fdim(f,g);
}
}