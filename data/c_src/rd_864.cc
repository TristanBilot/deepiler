#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=exp(b);
a=(sin(f))*(sqrt(c));
while(isless(d,b)){
c=(pow(b,a))/(tan(c));
c=acos(d);
b=(atan2(e,d))/(log10(a));
e=sqrt(c);
}
if(isgreaterequal(a,e)){
f=log10(e);
e=(fmin(c,a))+(log(d));
}
}