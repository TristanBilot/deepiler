#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log10(c);
c=(pow(b,c))/(cos(c));
c=(acos(f))*(fmin(c,f));
b=(atan(a))*(exp(a));
e=(fdim(a,b))*(ceil(a));
e=sin(a);
a=(pow(b,b))+(fdim(f,b));
e=asin(a);
d=sqrt(f);
while(isless(d,c)){
e=(atan2(b,e))+(floor(e));
b=log10(f);
}
}