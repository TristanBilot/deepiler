#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(h,b);
b=(pow(c,b))*(fdim(e,f));
e=(fmax(d,d))+(ceil(g));
b=(fdim(h,h))/(fmin(c,c));
f=(sqrt(a))+(cos(h));
c=(pow(a,a))-(ceil(e));
if(isless(b,d)){
h=atan(e);
b=(pow(c,g))*(exp(e));
f=fmin(g,d);
h=(fmax(d,a))/(ceil(b));
}
}