#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(b,b))+(fmax(c,b));
b=(log10(d))*(tan(d));
e=pow(a,c);
while(islessgreater(b,b)){
a=(sqrt(b))*(fdim(e,d));
e=pow(d,c);
}
while(islessequal(a,b)){
c=sqrt(b);
d=atan2(c,a);
a=(fmin(e,d))/(acos(b));
c=log(c);
}
}