#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan(d))/(pow(b,c));
e=(acos(a))+(cos(c));
if(islessgreater(a,d)){
e=sqrt(c);
d=fmax(d,c);
c=pow(b,c);
b=(sin(d))-(cos(d));
e=tan(a);
}
b=floor(d);
b=pow(a,e);
}