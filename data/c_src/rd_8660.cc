#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(d,d))-(tan(b));
d=fmin(a,c);
while(isless(e,a)){
c=cos(b);
d=(floor(a))-(pow(d,a));
b=(sqrt(a))*(tan(a));
}
b=pow(c,e);
d=(pow(e,a))*(fdim(e,b));
d=(log10(b))+(atan2(e,a));
e=atan2(d,c);
}