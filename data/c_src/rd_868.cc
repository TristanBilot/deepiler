#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(b);
e=(sin(f))/(asin(c));
b=acos(g);
a=(sqrt(c))/(fdim(c,e));
e=(fmax(e,a))*(fmin(e,g));
a=(pow(a,a))+(log(f));
f=sqrt(e);
f=(pow(g,f))+(atan(b));
f=log10(d);
g=fmax(c,d);
e=(fmin(b,c))+(tan(c));
}