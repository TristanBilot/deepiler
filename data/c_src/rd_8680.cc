#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(d,b))-(floor(a));
e=(fmax(e,a))*(atan2(e,e));
c=log10(e);
d=fmin(a,b);
while(islessequal(d,b)){
e=cos(b);
d=(log10(c))+(sin(a));
}
d=(atan(a))-(ceil(e));
b=acos(d);
d=(fmax(a,d))/(tan(d));
}