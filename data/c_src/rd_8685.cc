#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(h);
h=(fdim(h,h))/(sin(e));
h=(fdim(a,e))*(pow(d,a));
if(isless(h,g)){
d=(fdim(a,b))/(fmax(b,d));
h=fmin(c,f);
b=fdim(e,b);
}
else{
h=(fmax(g,b))/(atan2(d,d));
f=(fdim(a,b))*(cos(f));
d=(pow(e,b))-(pow(g,g));
c=(exp(f))*(exp(e));
}
while(islessequal(a,g)){
d=(log10(e))*(fmin(b,c));
g=(log10(h))/(fdim(h,f));
d=exp(g);
h=(cos(h))+(atan2(d,c));
c=fdim(e,d);
}
}