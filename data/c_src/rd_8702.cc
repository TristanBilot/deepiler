#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(f,a))-(fmin(g,e));
d=log(f);
c=atan2(b,g);
d=fmin(d,a);
c=log(c);
b=(log(f))-(fdim(d,d));
d=fdim(b,b);
b=ceil(d);
f=atan(f);
a=cos(f);
d=(pow(f,d))/(pow(c,g));
g=fdim(e,a);
c=asin(c);
e=(log(f))*(fmax(e,a));
g=(pow(a,d))-(fmax(c,d));
}