#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmin(e,e);
c=(fmax(e,e))+(atan2(b,d));
if(isgreaterequal(a,e)){
e=(pow(b,f))*(fmin(e,a));
a=fmax(b,e);
}
else{
f=(log(c))-(acos(f));
c=(fmin(b,d))+(ceil(c));
d=(pow(f,e))/(fmin(c,a));
}
if(isless(a,d)){
b=exp(d);
f=(log10(d))-(sqrt(b));
a=fmax(e,d);
}
}