#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmax(f,e);
g=(fdim(e,d))/(fmin(b,c));
a=(log(b))+(atan(g));
c=ceil(d);
while(isgreaterequal(f,g)){
d=atan(d);
f=fdim(d,a);
g=fdim(e,d);
a=pow(a,f);
a=(fmax(f,e))+(pow(d,e));
}
if(isless(e,c)){
f=fmin(f,d);
d=acos(g);
}
}