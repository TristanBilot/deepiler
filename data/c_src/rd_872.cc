#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan2(e,c);
c=(asin(a))*(fdim(f,d));
b=sin(a);
e=(sin(f))-(atan2(b,g));
e=(fmax(a,d))/(exp(g));
a=(acos(a))*(fmax(a,c));
g=(atan(e))+(exp(a));
d=(pow(g,b))*(log(c));
e=(fmin(b,e))/(fmax(d,c));
c=fmax(b,c);
g=atan2(e,g);
c=exp(f);
}