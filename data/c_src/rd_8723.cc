#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
d=(acos(g))/(log(e));
f=fmax(b,a);
h=(tan(a))+(fdim(e,c));
c=(fmax(d,e))/(atan2(g,g));
if(isless(c,d)){
a=sqrt(e);
f=(exp(f))+(fmax(a,e));
h=(pow(c,b))/(fdim(e,f));
}
}