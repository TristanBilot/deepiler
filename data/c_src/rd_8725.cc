#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log(b))+(pow(c,b));
a=(atan2(a,e))*(pow(e,e));
e=fmin(e,a);
e=cos(b);
a=fmin(d,b);
a=atan2(c,e);
if(islessequal(a,e)){
e=ceil(d);
e=(fmin(b,e))-(pow(a,a));
c=sqrt(a);
}
else{
c=(atan2(e,b))*(acos(c));
a=fdim(d,e);
e=exp(b);
}
}