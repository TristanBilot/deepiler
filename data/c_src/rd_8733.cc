#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(tan(e))+(sin(b));
e=fmin(c,f);
c=(fdim(d,e))*(log(c));
e=fmin(e,d);
f=ceil(c);
d=log10(c);
e=(asin(d))-(sin(f));
c=fmax(e,d);
a=(floor(e))/(floor(b));
e=fmin(a,e);
}