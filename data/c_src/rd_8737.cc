#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(b);
d=(pow(c,g))/(sin(e));
g=(tan(c))/(fdim(b,a));
while(islessequal(h,d)){
c=(atan2(b,c))*(pow(a,b));
c=log10(c);
}
if(isless(g,h)){
d=(fmax(c,d))/(fmin(b,f));
g=(fdim(g,g))+(log10(c));
b=fmax(c,f);
b=(fdim(g,h))*(ceil(b));
f=cos(d);
}
}