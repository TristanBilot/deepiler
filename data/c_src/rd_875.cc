#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(atan(a))*(sqrt(b));
e=fmin(b,c);
while(isless(f,e)){
f=(cos(c))/(sqrt(a));
d=pow(a,c);
c=(cos(f))-(fdim(e,b));
b=fmax(e,e);
}
if(isless(c,e)){
c=acos(b);
e=(atan2(c,d))+(log10(c));
e=fdim(b,e);
e=(ceil(a))/(floor(a));
}
}