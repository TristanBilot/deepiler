#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(d,d))-(fmax(c,c));
g=(pow(e,d))-(atan2(c,a));
f=(asin(c))*(exp(c));
a=fmin(d,d);
b=cos(g);
f=(sin(a))-(log10(g));
a=sqrt(g);
}