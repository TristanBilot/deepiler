#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(b,d))/(sin(c));
f=fmin(b,f);
e=(log(a))*(fmax(b,b));
b=(exp(c))*(log10(d));
f=asin(f);
f=(pow(b,b))*(ceil(d));
b=pow(f,e);
if(islessequal(c,d)){
f=floor(e);
c=asin(e);
}
}