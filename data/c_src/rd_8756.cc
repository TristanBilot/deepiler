#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(f,a))-(log10(g));
e=exp(h);
d=sqrt(h);
g=pow(h,e);
f=(fmax(a,c))/(acos(a));
e=(ceil(e))/(acos(g));
g=fmin(h,e);
h=(fmin(b,e))/(log10(a));
f=(sin(b))*(floor(g));
c=(sin(g))+(fmax(h,f));
b=pow(b,b);
}