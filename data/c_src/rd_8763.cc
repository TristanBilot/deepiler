#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan2(f,d);
f=(sqrt(g))/(fmin(d,f));
f=log10(a);
f=(fmin(f,d))+(log10(e));
while(isgreaterequal(d,b)){
c=(cos(b))*(fmin(c,c));
b=(fmax(f,e))/(fmin(d,a));
b=fdim(b,c);
a=(asin(d))/(tan(a));
}
if(isgreaterequal(a,g)){
c=(fmax(d,f))/(fmax(c,a));
f=(fdim(a,b))/(fmin(f,e));
a=sqrt(c);
b=(fmax(c,c))/(fmin(b,a));
e=acos(b);
}
else{
f=(pow(c,c))*(acos(f));
a=fmin(d,b);
}
}