#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(tan(a))+(sin(d));
a=fmax(b,e);
d=(atan(a))*(log(b));
f=tan(f);
e=fmax(g,e);
f=sin(c);
f=(log10(e))*(pow(e,d));
d=fmax(b,c);
while(isless(c,a)){
b=(pow(h,e))*(acos(d));
c=(pow(d,b))+(asin(h));
}
}