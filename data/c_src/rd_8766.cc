#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(c,c);
d=(atan(c))-(sin(b));
while(islessequal(c,a)){
a=log10(a);
c=fdim(b,b);
d=fmin(c,c);
e=fmax(c,e);
c=cos(b);
}
while(isless(e,c)){
b=log10(c);
d=cos(a);
b=(log(c))+(exp(e));
}
}