#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan(d))/(asin(e));
f=(atan2(b,b))+(cos(d));
while(islessequal(f,c)){
e=(sqrt(f))-(log(c));
c=pow(g,e);
f=ceil(d);
d=fmax(f,f);
}
while(islessgreater(c,c)){
f=(pow(a,a))-(pow(e,d));
c=(fmax(b,d))-(atan2(e,a));
}
}