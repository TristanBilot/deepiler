#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(d,b))+(fmin(f,a));
e=(pow(c,c))+(cos(d));
a=fdim(b,e);
c=(atan2(c,c))-(sin(f));
e=atan2(a,e);
while(isless(e,e)){
e=(ceil(c))-(sqrt(b));
d=(atan2(f,a))+(cos(d));
}
if(islessequal(b,a)){
d=fmin(d,c);
d=(fmin(b,d))/(sin(d));
d=(fdim(f,f))+(pow(e,a));
}
else{
e=fmax(f,e);
c=(log(b))-(fdim(a,c));
a=floor(e);
e=exp(b);
}
}