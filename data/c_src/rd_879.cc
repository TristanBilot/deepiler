#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(b);
c=fmax(e,e);
if(isgreaterequal(c,a)){
d=pow(c,d);
e=(fdim(c,e))/(ceil(c));
c=fmin(e,c);
}
else{
b=(sin(b))/(acos(d));
d=asin(c);
a=fmax(a,e);
e=atan2(c,a);
a=(atan(e))-(sqrt(c));
}
while(islessequal(a,e)){
b=(fdim(d,e))*(fmax(a,d));
c=fmax(d,a);
e=(fdim(a,a))*(sqrt(e));
d=(fdim(b,a))+(pow(c,d));
e=log(b);
}
}