#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(c,b);
f=(floor(a))*(atan2(f,h));
f=(log10(a))+(acos(e));
g=(log10(b))*(asin(e));
if(isgreaterequal(f,h)){
d=atan2(a,g);
e=log(c);
a=exp(c);
}
else{
e=(pow(a,d))-(ceil(e));
a=(asin(a))/(fmin(g,e));
g=(acos(g))+(fmax(e,c));
b=(log(g))+(atan2(a,d));
a=ceil(e);
}
while(isless(e,d)){
e=(ceil(d))-(acos(g));
c=(exp(h))/(sqrt(c));
c=log10(a);
}
}