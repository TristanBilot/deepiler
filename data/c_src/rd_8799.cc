#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(a))/(exp(a));
e=(fdim(b,d))+(atan2(c,b));
if(islessequal(a,b)){
d=(log10(b))/(exp(b));
c=fdim(b,c);
e=fmin(e,b);
}
else{
d=fmin(a,b);
b=(sin(d))/(sin(c));
a=log(a);
c=asin(c);
b=(atan(d))-(cos(a));
}
while(isless(a,b)){
a=(fdim(d,c))/(fmax(e,a));
c=(pow(e,a))*(fmin(c,e));
c=cos(a);
b=fmin(d,b);
}
}