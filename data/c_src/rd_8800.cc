#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sqrt(b))-(acos(h));
e=(ceil(a))-(fdim(b,d));
if(islessequal(a,d)){
c=atan(f);
f=(log10(b))-(log(g));
e=pow(c,a);
}
while(islessequal(c,e)){
h=(pow(d,d))-(sqrt(c));
f=fmax(g,b);
e=fmax(h,e);
c=fdim(h,d);
}
}