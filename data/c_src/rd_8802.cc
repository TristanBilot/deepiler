#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=floor(b);
d=log(a);
c=tan(b);
c=(fmin(b,c))/(tan(e));
if(islessequal(c,f)){
a=(tan(c))/(fmin(c,e));
c=(acos(d))-(pow(d,d));
}
while(isless(e,e)){
a=atan2(e,e);
c=(atan2(a,a))+(atan2(e,b));
}
}