#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(b,a);
b=(atan2(d,a))+(fmin(b,e));
a=(exp(d))-(pow(c,a));
b=(log10(c))*(asin(e));
if(islessequal(a,d)){
c=log10(e);
e=(exp(b))+(pow(e,b));
a=(fdim(c,e))+(fmax(d,c));
}
else{
c=(fmin(b,d))-(floor(a));
a=(asin(b))*(asin(b));
e=acos(a);
}
b=fdim(c,a);
e=(fmin(b,b))/(floor(e));
a=(sin(d))*(tan(e));
e=(atan(c))/(tan(b));
a=sin(d);
}