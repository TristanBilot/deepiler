#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(c,c);
a=(floor(e))-(floor(d));
d=(ceil(d))/(pow(d,a));
while(isless(e,b)){
d=(pow(c,e))+(tan(a));
d=fdim(e,e);
}
while(isless(b,b)){
a=(ceil(c))/(pow(a,b));
a=fmin(e,a);
d=fdim(c,a);
a=(fmax(b,c))/(fmin(e,b));
b=pow(c,e);
}
}