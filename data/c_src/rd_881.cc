#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=ceil(d);
d=(log10(e))/(fmin(c,e));
c=(cos(a))/(fdim(a,d));
f=pow(a,a);
e=sin(e);
while(islessequal(f,b)){
d=fmax(c,f);
e=pow(f,e);
f=asin(b);
}
b=fmax(e,c);
b=pow(b,f);
}