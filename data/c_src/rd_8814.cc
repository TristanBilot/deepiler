#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan(e);
e=(log10(f))+(pow(b,a));
c=(pow(e,e))/(sqrt(e));
g=fmin(c,g);
if(islessequal(b,d)){
a=(fmax(d,f))/(atan(f));
c=atan(d);
b=cos(f);
f=cos(b);
a=pow(d,b);
}
if(isless(g,e)){
f=acos(e);
b=(fmax(b,a))+(pow(a,b));
}
}