#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(a,c);
e=fmax(e,f);
a=(acos(d))/(fmax(c,c));
f=fmin(c,e);
if(isless(b,c)){
b=(atan2(a,d))-(log10(e));
c=log(a);
a=(fdim(b,e))-(fmin(f,d));
f=(fdim(e,a))+(cos(c));
}
else{
a=(fmin(d,d))+(cos(c));
c=(atan2(c,b))*(fdim(c,b));
c=(ceil(a))+(atan2(a,c));
a=(atan(a))*(sin(f));
b=fmax(c,b);
}
e=(cos(b))*(atan2(d,c));
b=pow(e,e);
a=atan2(d,e);
c=fmax(c,d);
}