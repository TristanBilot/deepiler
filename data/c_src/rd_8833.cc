#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=exp(d);
a=atan(c);
b=atan(a);
a=(fmin(f,d))/(sin(e));
f=(cos(b))-(fmax(c,f));
g=(fdim(e,d))+(fdim(f,b));
c=(pow(c,c))/(fmin(d,e));
a=(pow(b,g))/(fmax(f,e));
f=(fmin(c,g))*(atan(c));
if(isgreaterequal(e,a)){
b=fmax(a,g);
a=asin(c);
d=asin(c);
b=atan2(g,f);
}
}