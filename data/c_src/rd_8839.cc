#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(b);
c=(atan2(d,e))-(log10(a));
b=(ceil(d))/(floor(b));
b=(fmin(a,b))-(pow(c,a));
e=floor(e);
if(isgreaterequal(c,a)){
d=(cos(d))+(fmin(e,a));
a=pow(c,e);
c=fmax(a,d);
}
else{
d=exp(b);
e=cos(c);
e=fdim(d,c);
}
while(islessgreater(d,b)){
a=log10(a);
d=pow(a,a);
e=(cos(c))+(log(b));
a=asin(b);
b=(cos(e))*(fmax(c,b));
}
}