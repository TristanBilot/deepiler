#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(e,b))/(ceil(d));
a=(fdim(d,a))/(ceil(a));
d=(sqrt(d))/(fmin(e,a));
b=(acos(c))/(fdim(b,d));
if(islessequal(a,c)){
a=(floor(a))/(fdim(a,b));
d=(pow(e,c))+(fdim(a,d));
}
if(isgreaterequal(e,d)){
c=pow(a,a);
d=exp(c);
}
}