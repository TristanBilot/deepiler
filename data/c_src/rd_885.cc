#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(pow(b,a))/(log(d));
a=(fdim(b,f))+(fmin(e,f));
if(isgreaterequal(d,b)){
c=fmin(f,e);
b=pow(b,f);
e=log10(e);
b=(fdim(e,e))+(fmax(c,e));
a=(acos(d))-(log(e));
}
else{
c=(atan(b))+(atan2(d,a));
f=ceil(c);
}
while(isgreaterequal(b,e)){
f=pow(c,e);
b=(fmax(a,f))/(fmin(a,a));
c=ceil(d);
}
}