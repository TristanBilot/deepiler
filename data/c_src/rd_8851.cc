#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(tan(d))-(ceil(e));
e=sqrt(d);
b=pow(g,h);
b=(fmax(c,b))+(pow(a,a));
c=sin(e);
d=(atan2(h,c))+(acos(g));
g=pow(f,h);
if(islessequal(f,d)){
a=(log10(c))/(atan(b));
h=fdim(b,b);
e=fmax(b,d);
a=(fmax(d,c))-(fmax(a,f));
d=(fmax(h,f))-(fdim(d,c));
}
}