#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(c,b))*(log10(f));
f=(sqrt(e))*(acos(f));
b=pow(f,a);
b=pow(c,e);
while(isgreaterequal(b,f)){
d=(log10(f))*(fdim(e,e));
f=(log10(b))*(fdim(f,e));
}
c=(tan(e))+(atan(b));
a=asin(c);
b=exp(e);
f=atan(e);
e=(fdim(f,d))-(exp(b));
}