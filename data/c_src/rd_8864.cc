#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(c,c))*(pow(b,c));
c=(fmax(a,b))*(asin(d));
f=log10(e);
d=atan2(f,d);
f=atan2(d,b);
b=sin(e);
c=(pow(d,b))-(exp(b));
e=(fdim(e,d))+(tan(c));
}