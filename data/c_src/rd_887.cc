#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(b);
b=(fmin(d,a))/(fmax(a,c));
c=fdim(b,b);
a=fdim(c,b);
b=(pow(d,b))/(pow(b,d));
c=(atan(e))-(fdim(a,d));
a=(fmax(d,a))-(acos(e));
b=pow(a,b);
d=(fmax(e,b))+(sqrt(b));
e=(ceil(c))+(pow(a,c));
if(isgreaterequal(c,c)){
d=(acos(e))/(ceil(c));
a=fdim(d,c);
d=(fdim(a,c))-(tan(e));
d=floor(d);
}
}