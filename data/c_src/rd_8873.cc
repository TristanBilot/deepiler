#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,c))-(asin(e));
e=(asin(a))*(fdim(c,d));
d=(fmin(d,b))*(fmin(c,c));
e=(pow(b,a))+(exp(a));
c=(acos(c))-(log(c));
b=(fmax(d,c))-(sqrt(d));
b=fmax(c,e);
c=(pow(a,d))-(pow(d,d));
d=pow(b,d);
b=(fdim(c,c))/(fdim(a,e));
d=atan2(c,b);
}