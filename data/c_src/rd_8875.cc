#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(exp(c))*(pow(d,e));
h=atan2(b,g);
f=(fmax(e,c))/(sin(h));
d=(fmax(d,f))-(atan2(h,a));
if(isless(b,e)){
d=(fmin(d,h))/(log10(c));
d=pow(b,d);
d=(acos(f))+(fdim(h,h));
e=fmax(c,h);
}
}