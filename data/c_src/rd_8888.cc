#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(log(c))/(atan2(g,c));
a=acos(h);
a=(fmin(e,d))*(atan(g));
if(islessgreater(d,c)){
b=(exp(a))*(pow(h,c));
h=(asin(d))/(atan2(b,a));
e=exp(h);
c=fmax(d,e);
}
if(islessequal(c,e)){
c=fmax(h,d);
g=(atan2(h,h))+(pow(c,f));
}
}