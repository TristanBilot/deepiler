#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(d,d);
f=(fmax(f,c))*(sin(d));
f=sqrt(g);
f=(atan2(h,e))*(fmin(c,b));
g=fmin(e,c);
g=(fmin(g,f))+(cos(f));
b=atan2(e,g);
h=(fdim(g,b))-(atan2(f,d));
h=(sin(g))*(acos(h));
h=(log10(b))+(exp(h));
h=(tan(d))+(tan(c));
g=pow(g,h);
g=fmin(a,e);
g=(asin(b))-(atan(c));
}