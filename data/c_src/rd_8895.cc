#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log(e);
d=(fmin(a,f))+(fdim(f,h));
g=(asin(e))*(pow(h,b));
f=(sqrt(h))/(atan2(h,f));
d=(fmin(h,g))+(fmax(h,a));
a=acos(e);
h=tan(b);
b=fmin(d,d);
if(islessequal(b,d)){
c=(atan2(f,c))+(sin(a));
e=(fmax(e,h))*(atan2(c,e));
e=(atan(e))+(fmax(d,e));
b=(fdim(g,h))/(log10(b));
c=fmax(f,b);
}
}