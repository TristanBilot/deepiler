#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(c,b);
a=(fmax(b,b))*(sin(e));
c=(log10(a))*(fmax(b,e));
if(islessequal(d,d)){
d=(pow(c,c))/(fmax(c,d));
b=fdim(a,a);
e=(fdim(b,b))-(cos(a));
}
else{
d=(asin(d))*(atan2(e,e));
d=(floor(d))+(cos(c));
c=fmax(a,d);
d=sqrt(c);
}
while(islessequal(c,a)){
e=log10(e);
e=ceil(d);
b=floor(d);
}
}