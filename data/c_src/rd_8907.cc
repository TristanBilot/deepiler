#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(c,a);
a=sqrt(d);
c=fmax(e,a);
c=exp(e);
c=(ceil(c))/(exp(b));
a=(fdim(a,c))*(atan2(b,b));
while(islessgreater(a,d)){
e=fdim(a,b);
a=(fdim(e,b))/(exp(b));
b=sqrt(b);
}
}