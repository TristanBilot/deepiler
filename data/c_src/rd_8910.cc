#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(d))-(ceil(e));
a=(exp(e))-(fdim(b,c));
d=(acos(d))*(log10(c));
while(isless(d,c)){
d=(fmin(d,e))+(fdim(e,e));
c=(fmin(d,b))/(ceil(a));
e=fmax(a,b);
}
d=atan2(e,a);
c=(fmax(e,b))-(pow(d,a));
b=sqrt(c);
a=tan(e);
c=fmin(d,e);
}