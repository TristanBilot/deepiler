#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(atan2(c,e))+(fmin(e,b));
b=(pow(f,a))*(floor(c));
c=fmax(e,b);
b=(atan2(b,b))*(tan(c));
while(isgreaterequal(f,a)){
e=floor(c);
d=(fmin(f,b))/(fmax(d,a));
a=(acos(c))*(cos(b));
d=(asin(d))/(floor(a));
}
a=(exp(f))*(exp(e));
a=(floor(e))-(log(a));
c=(ceil(d))*(ceil(a));
}