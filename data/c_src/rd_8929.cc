#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=ceil(a);
b=(fdim(c,c))/(atan2(d,c));
f=(log10(d))/(pow(f,c));
e=fmax(c,e);
d=(sin(e))*(fmax(d,c));
while(islessgreater(d,f)){
e=(fmax(a,e))/(pow(e,b));
b=(pow(a,b))+(pow(e,f));
}
while(islessequal(d,b)){
e=(tan(f))+(ceil(e));
b=fdim(e,f);
f=fmax(a,c);
a=exp(d);
c=asin(b);
}
}