#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(h,f))*(tan(a));
d=pow(a,d);
a=(fdim(d,h))+(log10(a));
b=log(g);
b=fmax(c,h);
if(islessgreater(e,a)){
a=(pow(d,g))/(floor(f));
c=log(d);
e=(sin(d))/(log10(e));
}
else{
d=pow(f,a);
h=(fmin(c,b))-(sin(h));
}
}