#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(f,e))/(log(g));
h=(pow(e,h))/(ceil(d));
g=(exp(a))-(acos(d));
while(islessgreater(f,g)){
h=(sin(e))+(exp(f));
f=fmax(f,e);
g=(atan2(a,h))/(pow(c,a));
}
e=(atan2(g,c))+(floor(a));
d=(acos(a))/(fmax(b,c));
c=sin(a);
}