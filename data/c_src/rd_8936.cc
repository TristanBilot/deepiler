#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fdim(a,a);
c=fmax(h,g);
f=(fdim(g,e))/(fmax(c,g));
d=(fdim(f,b))/(pow(h,e));
g=asin(a);
while(isless(h,a)){
e=(log10(f))-(log10(e));
b=(pow(h,a))+(pow(a,b));
d=fmin(d,c);
}
h=(fdim(d,g))-(sqrt(g));
h=tan(b);
d=(acos(d))+(sin(h));
h=(exp(d))-(log(g));
}