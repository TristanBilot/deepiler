#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(e,f))-(atan(f));
f=(exp(a))*(exp(f));
b=(fdim(f,a))-(fmax(f,f));
while(islessequal(b,c)){
f=(fmin(b,d))-(acos(e));
e=(pow(d,a))*(acos(e));
d=atan(d);
c=fmin(c,d);
}
d=(pow(f,f))+(log(c));
b=(fdim(f,d))+(asin(f));
e=(fdim(b,f))/(log10(a));
}