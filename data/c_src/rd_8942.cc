#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fdim(a,b))/(pow(c,c));
f=(asin(e))*(pow(a,a));
c=(asin(f))-(cos(g));
f=(fdim(a,d))*(cos(b));
while(islessequal(f,f)){
e=fmax(b,b);
g=(sqrt(g))/(log(b));
f=(cos(b))*(exp(a));
}
while(isless(e,d)){
h=(floor(c))/(cos(h));
g=(fdim(g,g))-(atan(g));
h=(fmin(d,f))*(fmax(g,d));
g=(floor(a))/(pow(d,h));
c=asin(g);
}
}