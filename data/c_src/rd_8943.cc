#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(c,e))/(exp(b));
e=atan2(d,e);
b=atan(b);
c=atan2(a,c);
a=sin(e);
while(isless(a,a)){
b=log(c);
e=cos(a);
a=(floor(e))*(atan2(c,e));
d=(log10(b))/(fmin(c,a));
d=(atan2(c,a))-(atan(c));
}
}