#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=sqrt(d);
e=fmax(e,a);
g=sqrt(a);
f=fmax(a,g);
a=cos(f);
if(isless(g,g)){
c=fdim(g,b);
f=ceil(g);
a=(ceil(d))/(floor(e));
}
e=(tan(f))-(log10(a));
c=(fmin(a,b))*(sqrt(b));
f=(fdim(e,b))/(fmax(c,d));
}