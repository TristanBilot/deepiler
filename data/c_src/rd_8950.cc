#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(tan(d))/(acos(e));
e=(asin(e))+(log10(d));
c=sin(f);
b=log(f);
d=fmax(c,b);
a=fmin(f,c);
a=atan2(d,b);
d=(asin(c))+(atan2(c,c));
if(isgreaterequal(a,d)){
e=fdim(a,f);
c=(cos(a))/(log10(d));
f=(sin(f))/(sqrt(b));
}
}