#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(b,b);
c=(atan2(h,f))*(fdim(g,c));
d=(cos(a))-(fmax(b,a));
a=(fmax(g,a))*(atan2(h,d));
h=(fmin(a,d))/(sqrt(d));
f=(fmax(g,g))+(fmax(d,h));
b=(acos(e))*(asin(a));
if(isgreaterequal(c,h)){
e=(sin(d))+(sqrt(b));
h=cos(d);
g=(log10(g))/(fdim(d,d));
}
}