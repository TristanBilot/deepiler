#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(a,d))-(atan(a));
d=(fmin(a,a))-(cos(e));
b=atan(d);
while(islessgreater(e,c)){
d=(log10(a))/(fmax(b,c));
b=fmax(c,d);
b=(fmin(e,c))-(atan2(e,b));
}
d=ceil(b);
d=fdim(a,d);
d=(fmin(b,b))-(pow(d,a));
}