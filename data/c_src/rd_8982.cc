#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(b,c))/(pow(c,a));
d=fdim(e,b);
d=fdim(e,d);
d=(floor(b))+(sin(b));
c=(tan(b))-(fdim(b,a));
b=sin(e);
c=(tan(b))-(atan(c));
e=(ceil(e))-(log(e));
b=(floor(b))/(pow(d,d));
d=fmax(a,d);
while(islessgreater(c,b)){
b=fdim(b,c);
b=log10(c);
a=(fdim(d,a))-(acos(c));
}
}