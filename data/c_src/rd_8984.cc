#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,a);
a=fdim(b,e);
c=atan2(d,a);
e=pow(c,c);
b=sqrt(a);
if(islessequal(b,a)){
c=fmax(d,e);
d=exp(e);
d=(pow(a,c))/(pow(d,b));
b=(tan(e))*(atan2(c,e));
}
if(isgreaterequal(e,d)){
e=(atan2(b,b))*(atan2(b,b));
c=(sqrt(e))/(atan(b));
e=acos(d);
d=cos(b);
b=(fmin(b,b))+(log(b));
}
else{
a=(sin(b))/(pow(e,c));
d=(atan2(e,a))/(atan(e));
}
}