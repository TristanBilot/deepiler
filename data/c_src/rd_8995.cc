#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,b);
d=(exp(h))-(ceil(d));
g=acos(h);
if(isless(g,f)){
e=(atan2(d,f))*(ceil(b));
a=(fmax(g,d))/(cos(a));
g=fmax(b,f);
f=fdim(h,c);
}
else{
g=atan(a);
e=(pow(h,c))+(atan2(c,a));
g=(pow(d,g))/(ceil(h));
h=fmin(c,a);
b=(sqrt(e))+(tan(d));
}
h=(cos(b))/(sin(e));
c=(acos(f))+(fdim(g,c));
e=(atan2(a,g))/(fmin(g,h));
}