#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=cos(a);
c=fmin(a,b);
a=(fmax(f,b))/(fdim(f,b));
e=(fmax(f,e))*(tan(d));
a=log(d);
if(isless(b,d)){
b=atan(e);
f=(fmin(f,e))-(asin(f));
d=(fmin(c,b))*(fmin(f,c));
f=fmax(d,a);
a=(fmin(e,e))*(exp(f));
}
else{
b=pow(f,d);
d=(atan2(d,b))*(fdim(c,a));
d=fmax(a,e);
a=sin(d);
}
while(isless(b,d)){
e=asin(e);
a=(atan2(e,e))+(sqrt(e));
d=acos(d);
f=pow(c,c);
}
}