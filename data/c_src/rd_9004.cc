#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(sin(f))/(fmin(c,h));
a=(log(b))+(fmin(d,d));
c=(atan(d))-(atan2(f,b));
d=fmin(c,d);
c=fmin(b,f);
e=atan(g);
h=(sin(h))+(log10(h));
g=log10(e);
c=atan(b);
e=(atan(d))+(atan2(c,f));
f=(exp(h))*(tan(c));
h=fdim(f,h);
e=(fmin(e,g))+(cos(e));
}