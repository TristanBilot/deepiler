#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(ceil(e))*(fmax(c,f));
f=log10(d);
c=(fdim(b,f))+(tan(d));
if(islessgreater(d,g)){
c=(log(h))*(atan2(g,d));
c=fdim(c,a);
g=(sin(g))/(floor(e));
e=log(h);
g=ceil(d);
}
else{
h=(pow(h,e))-(exp(e));
c=pow(h,a);
}
b=cos(e);
f=(atan2(b,c))/(pow(a,g));
h=fdim(f,d);
}