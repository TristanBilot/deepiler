#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(c,d);
a=atan2(c,c);
d=atan(c);
d=(log10(c))-(floor(b));
a=log10(b);
while(islessequal(e,e)){
a=(cos(e))/(fmax(d,b));
e=(atan2(c,d))+(exp(c));
d=pow(c,b);
}
}