#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(tan(b))/(pow(a,c));
e=fmax(c,c);
c=(floor(d))/(log10(c));
d=atan(d);
d=(fmin(e,d))/(fmin(e,a));
e=asin(e);
c=log10(a);
e=atan2(b,d);
a=floor(c);
d=(log(d))*(fmax(c,e));
}