#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(b,d);
a=fmax(a,c);
d=pow(b,a);
a=fdim(b,c);
a=(asin(b))+(pow(d,c));
d=fmax(e,a);
c=fmin(c,e);
if(islessequal(a,a)){
a=(fmin(b,d))-(exp(d));
b=fdim(c,c);
b=acos(b);
}
else{
e=fmin(b,a);
b=fmax(c,c);
a=asin(d);
}
}