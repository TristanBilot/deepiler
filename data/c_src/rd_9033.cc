#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(log10(a))*(pow(a,f));
f=fdim(c,a);
e=(pow(d,g))-(exp(b));
e=(fdim(g,a))/(fdim(c,c));
d=floor(f);
g=(fmax(c,a))-(cos(b));
c=tan(f);
a=floor(e);
g=fmin(e,g);
if(islessequal(c,b)){
d=(sin(c))+(atan2(a,g));
b=(pow(g,g))*(asin(b));
d=log(f);
a=(log(b))*(pow(a,a));
}
}