#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(a,e))-(exp(d));
c=(pow(a,d))+(pow(e,b));
a=(floor(d))+(atan(b));
b=(exp(e))*(fdim(c,a));
e=(pow(a,b))-(log(c));
c=(sqrt(b))/(fmin(c,e));
d=pow(b,d);
while(islessequal(c,a)){
c=(fmin(c,a))+(log(d));
d=fdim(d,b);
d=(atan(d))-(fdim(b,a));
}
}