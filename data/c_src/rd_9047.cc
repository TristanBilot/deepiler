#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(e);
d=(pow(d,b))/(tan(c));
c=(ceil(a))*(fmax(d,e));
if(islessgreater(a,e)){
e=fmax(e,a);
b=(exp(b))+(fmax(d,d));
c=(fmin(e,c))+(ceil(d));
}
if(isless(d,c)){
d=tan(a);
a=sin(d);
}
else{
a=(ceil(b))*(fmax(c,e));
c=tan(e);
b=(atan2(c,d))+(sin(d));
}
}