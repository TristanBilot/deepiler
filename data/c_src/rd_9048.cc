#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(c);
b=(acos(b))/(pow(a,a));
e=log(a);
c=fmin(a,a);
c=(fmax(e,a))/(fdim(d,a));
while(isless(e,e)){
d=(log10(d))/(fmin(e,e));
d=atan2(a,c);
b=fmin(b,b);
b=(atan2(b,e))-(exp(b));
}
if(islessgreater(e,b)){
d=(acos(d))+(atan2(a,b));
c=pow(e,a);
e=(fdim(d,a))+(pow(a,b));
e=(cos(e))/(cos(c));
}
else{
c=(asin(c))/(fmin(c,a));
d=(atan(a))*(sqrt(b));
d=(sin(a))/(atan(e));
c=fmax(d,c);
c=fmin(d,c);
}
}