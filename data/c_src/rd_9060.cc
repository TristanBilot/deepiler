#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(h);
b=exp(c);
a=acos(a);
f=(asin(g))-(exp(d));
while(isless(h,f)){
d=(tan(c))/(acos(h));
c=(asin(g))/(log(b));
a=(fmax(c,c))*(sin(a));
b=fmax(d,b);
}
if(isless(d,d)){
d=sin(a);
h=(atan(g))-(fmin(b,c));
}
else{
d=atan(d);
c=(fmax(c,b))/(pow(a,f));
e=log(a);
c=(ceil(f))+(atan2(h,e));
a=acos(f);
}
}