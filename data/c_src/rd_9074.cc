#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fdim(f,d))/(fmax(a,f));
c=fmin(g,a);
f=atan2(a,c);
c=(floor(e))*(atan2(f,b));
while(islessequal(d,e)){
c=atan2(c,g);
g=sin(c);
e=fmin(g,d);
e=(fdim(g,e))/(fmax(f,a));
a=(fdim(f,d))/(tan(b));
}
if(isless(f,b)){
d=atan2(c,b);
b=atan2(g,d);
f=(ceil(f))-(fmax(d,g));
e=cos(b);
}
else{
a=(atan2(g,e))*(fmax(g,f));
f=(floor(a))/(pow(g,b));
}
}