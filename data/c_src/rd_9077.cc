#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(a,c);
d=fmin(a,h);
f=atan(h);
h=floor(b);
g=(floor(b))+(fmin(g,a));
e=ceil(d);
d=fmax(d,e);
while(isless(b,g)){
c=(fmax(f,h))*(fmin(b,a));
e=(fmin(h,c))-(atan2(e,b));
}
}