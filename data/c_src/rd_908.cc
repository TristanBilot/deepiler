#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(e,e);
b=(exp(b))+(fmin(e,h));
h=fmin(e,e);
e=ceil(e);
g=log10(c);
h=asin(h);
d=(sqrt(b))*(pow(f,c));
if(isgreaterequal(e,f)){
e=atan2(g,e);
c=(pow(g,f))/(fdim(g,d));
g=(floor(d))-(fdim(h,g));
}
else{
d=log10(b);
d=(tan(g))-(fmax(e,e));
h=(fmax(d,h))+(log10(d));
}
}