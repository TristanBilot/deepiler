#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(tan(f))-(cos(e));
h=pow(f,c);
h=log(g);
d=(fmax(f,b))*(atan2(h,b));
c=(sqrt(d))-(acos(d));
a=fmax(f,b);
a=tan(b);
g=log(c);
f=(sqrt(d))-(fmax(g,d));
c=floor(d);
f=(cos(e))*(acos(c));
e=(log10(g))*(log10(g));
c=(fmax(b,d))*(atan2(h,a));
b=(floor(d))*(sin(h));
b=(floor(c))+(pow(e,a));
}