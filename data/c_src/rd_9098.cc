#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(exp(c))/(asin(h));
g=fmax(f,h);
c=floor(b);
b=sin(h);
if(isgreaterequal(e,b)){
c=(sin(g))/(asin(a));
f=atan(a);
}
else{
c=(exp(g))+(fmin(c,c));
g=(sqrt(c))/(fmax(f,b));
g=fdim(c,e);
}
if(islessequal(g,c)){
h=(ceil(c))-(atan(g));
b=(pow(h,e))-(fmin(a,c));
h=fdim(a,d);
g=fmax(g,h);
}
}