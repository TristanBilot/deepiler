#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(a,f))/(floor(g));
c=exp(h);
while(isless(c,b)){
f=atan2(e,f);
g=tan(g);
b=(asin(e))/(fmin(a,c));
e=pow(f,c);
}
while(isless(a,c)){
e=acos(e);
e=pow(e,c);
}
}