#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(e);
g=fmin(h,f);
h=(fdim(b,a))/(asin(h));
e=(acos(d))+(fmax(a,c));
d=(atan2(g,g))/(fmin(f,c));
while(islessequal(e,h)){
f=ceil(e);
a=(cos(b))*(tan(g));
e=fmax(g,h);
g=ceil(h);
f=(fmax(b,e))*(acos(f));
}
while(isless(c,h)){
e=(pow(b,c))+(fmin(g,e));
d=log10(d);
d=atan(d);
}
}