#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(d,c))+(fmax(d,e));
e=fmin(a,a);
c=atan2(b,d);
d=pow(c,c);
b=atan2(a,d);
while(isless(d,a)){
c=log10(d);
c=fmax(c,a);
}
while(islessequal(d,a)){
d=atan2(b,b);
c=(floor(e))/(atan(e));
d=atan2(d,a);
}
}