#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(exp(g))/(fmax(f,h));
e=(acos(a))-(atan2(d,g));
b=(atan2(d,d))-(fdim(h,h));
e=(fmax(h,g))+(fmax(a,b));
c=(pow(b,b))/(sin(g));
h=(ceil(g))+(exp(c));
g=(fmax(c,e))/(tan(a));
if(islessgreater(c,h)){
b=atan2(d,e);
e=acos(f);
d=floor(b);
}
else{
g=atan2(h,d);
a=(fmin(g,g))+(pow(f,e));
h=(tan(f))+(sin(g));
}
}