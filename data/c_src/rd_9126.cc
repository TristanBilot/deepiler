#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmin(c,d))/(fmin(e,h));
c=sqrt(c);
f=(fmin(c,d))*(sqrt(b));
d=(pow(d,f))/(fmin(h,a));
a=ceil(e);
f=tan(d);
d=fmax(a,h);
c=fdim(g,d);
c=(fmin(b,d))-(atan2(a,g));
e=(ceil(h))-(cos(f));
a=pow(a,a);
}