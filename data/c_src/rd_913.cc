#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan2(f,a))+(atan2(g,f));
d=(ceil(f))-(fmax(c,e));
f=(fmin(b,g))-(atan2(d,f));
e=(fmax(e,d))-(cos(b));
g=fdim(e,a);
while(islessgreater(c,e)){
f=fmin(b,c);
d=fdim(a,b);
a=tan(d);
e=(exp(c))+(fdim(g,f));
}
while(isgreaterequal(f,g)){
f=(ceil(d))-(floor(c));
f=exp(a);
c=(log10(g))/(sin(a));
}
}