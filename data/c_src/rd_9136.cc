#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(tan(e))/(atan2(a,d));
f=(cos(a))+(asin(b));
h=(log10(h))+(fmin(b,f));
e=sqrt(h);
f=(sin(c))*(fdim(d,c));
a=fdim(a,a);
d=log(e);
d=(asin(h))*(fmax(b,a));
d=fmin(e,g);
if(isless(d,h)){
a=(atan2(b,d))*(atan2(b,f));
b=sin(h);
h=(atan(f))/(atan(d));
}
}