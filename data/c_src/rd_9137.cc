#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(c))-(sin(a));
d=pow(d,g);
d=atan2(c,e);
while(islessgreater(e,e)){
g=exp(e);
d=cos(d);
}
if(isless(a,e)){
e=atan2(f,g);
a=(acos(e))*(fdim(d,a));
f=(atan(g))+(sqrt(g));
b=(log10(c))*(cos(b));
}
else{
c=cos(g);
b=(fdim(a,g))+(atan2(e,g));
}
}