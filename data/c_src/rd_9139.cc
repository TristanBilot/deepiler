#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(e,b);
e=(acos(e))/(log10(a));
b=pow(e,e);
d=(fdim(b,b))+(fdim(a,a));
d=floor(d);
b=(atan2(e,d))/(atan2(b,b));
c=(floor(a))-(fdim(d,b));
c=(fdim(e,e))*(fmin(a,e));
c=(fdim(a,d))+(fdim(c,c));
b=(fmin(c,a))-(atan2(e,b));
if(isgreaterequal(e,e)){
d=(ceil(c))/(fmax(e,b));
d=(atan(d))/(asin(a));
b=fdim(c,e);
e=atan(d);
}
else{
c=(fdim(b,a))/(fmin(a,e));
c=sin(c);
e=(cos(c))+(fmin(d,b));
e=pow(c,b);
}
}