#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(d))*(log(c));
e=(fmin(d,a))*(exp(d));
a=(pow(c,c))+(fdim(a,e));
d=(pow(e,d))*(fmax(c,c));
d=fmax(d,d);
if(isgreaterequal(d,b)){
a=(log(a))+(sin(a));
c=asin(e);
}
else{
a=cos(c);
e=log10(b);
e=fdim(b,e);
a=(fdim(e,b))/(atan2(b,a));
}
d=sin(c);
e=log(c);
b=(pow(c,d))+(pow(d,b));
}