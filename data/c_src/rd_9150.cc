#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(a,b))*(fdim(c,e));
a=(cos(c))*(atan2(c,d));
b=exp(a);
a=(floor(c))-(cos(a));
c=(pow(a,b))+(floor(a));
if(isless(d,a)){
a=floor(a);
b=(floor(d))+(ceil(e));
a=atan(a);
}
else{
c=ceil(b);
d=fdim(e,a);
a=cos(a);
c=(sqrt(d))-(fmax(a,c));
b=(fdim(c,b))-(sin(c));
}
while(islessequal(b,d)){
a=(exp(c))/(asin(c));
d=log10(a);
e=(atan2(a,a))*(sin(d));
a=asin(b);
}
}