#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(f,e);
f=fmax(e,b);
b=(exp(f))*(exp(c));
b=(ceil(c))-(log(b));
f=exp(a);
while(islessequal(e,e)){
f=pow(b,b);
c=sin(b);
}
if(islessgreater(b,f)){
d=fmin(d,b);
a=log10(e);
c=(fdim(c,a))*(fmin(e,d));
d=floor(f);
d=fmax(c,c);
}
else{
c=(floor(a))-(fmax(a,b));
d=acos(f);
}
}