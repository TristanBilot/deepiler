#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(e,g))*(fmax(d,a));
e=pow(b,c);
c=(asin(b))-(atan2(g,g));
d=fmin(a,f);
d=atan2(g,f);
e=log(a);
g=log(f);
h=(pow(c,e))-(atan2(a,c));
if(isgreaterequal(b,g)){
b=exp(g);
d=(floor(a))+(fdim(f,h));
g=fmin(a,g);
f=(sqrt(b))-(fmin(b,e));
}
else{
b=atan2(c,e);
a=(atan2(c,g))/(fmax(h,h));
g=atan(d);
h=(log(c))/(pow(d,a));
d=(fmin(b,c))+(fmin(d,b));
}
}