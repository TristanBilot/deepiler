#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(g);
d=(acos(b))-(atan2(g,c));
while(isless(a,f)){
d=sqrt(f);
f=(log10(a))+(fmax(a,a));
a=(pow(a,b))-(cos(f));
}
a=fmin(a,a);
d=(fdim(d,a))-(fmin(h,b));
}