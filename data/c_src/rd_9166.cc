#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(e,a);
f=(atan2(h,h))+(pow(g,h));
a=(pow(g,c))/(fmax(e,a));
g=fdim(f,d);
h=(pow(a,c))*(fmin(h,a));
f=fmin(c,g);
a=fmax(h,h);
f=floor(d);
while(isless(b,f)){
b=atan2(d,c);
g=atan2(g,b);
h=(log(h))+(fdim(c,h));
}
}