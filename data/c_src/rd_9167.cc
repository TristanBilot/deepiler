#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log(g);
c=(fdim(e,c))-(atan(g));
c=cos(f);
while(isless(b,d)){
d=log10(g);
g=log10(c);
d=cos(f);
e=(fdim(b,e))-(exp(b));
}
e=exp(g);
f=(pow(g,c))/(tan(b));
e=(pow(d,c))+(fdim(e,b));
c=(fmax(f,b))*(atan2(b,f));
}