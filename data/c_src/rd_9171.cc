#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=acos(b);
b=(exp(b))-(acos(b));
while(isgreaterequal(g,e)){
e=fdim(a,e);
e=(ceil(e))-(sin(b));
}
if(isless(e,e)){
e=atan2(c,g);
f=(log10(g))-(log(f));
f=fmin(e,g);
d=fdim(d,b);
}
else{
e=ceil(d);
e=(sqrt(a))-(atan2(f,g));
g=(log10(c))+(atan(c));
}
}