#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(a,a);
c=(exp(c))-(atan2(d,e));
f=fdim(a,b);
a=fmax(f,a);
d=(fmax(f,e))-(atan2(d,d));
if(isless(e,a)){
b=(atan2(d,d))*(fmax(a,c));
c=(atan2(b,d))+(exp(e));
a=log10(b);
}
if(isless(c,f)){
a=acos(f);
c=cos(c);
}
}