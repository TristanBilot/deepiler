#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(d,d))-(pow(b,g));
d=(exp(d))*(floor(b));
while(isless(f,c)){
f=(fmax(f,g))/(atan(d));
a=fmax(d,f);
g=pow(e,e);
c=(fmax(a,a))*(pow(e,g));
g=(fdim(g,d))*(pow(c,e));
}
while(islessgreater(f,c)){
f=cos(d);
a=(fmax(b,e))-(pow(a,b));
g=atan(d);
e=fmax(g,d);
}
}