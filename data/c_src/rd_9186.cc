#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log10(h))/(log(f));
f=fmin(e,d);
c=log(a);
h=log(g);
f=(atan(a))-(fmax(f,e));
g=(exp(f))+(fmax(c,d));
b=(fdim(g,d))/(tan(g));
if(islessequal(e,c)){
a=log10(h);
d=atan2(h,h);
e=(tan(b))*(fmin(b,b));
g=exp(f);
}
else{
c=(exp(g))*(log(b));
g=asin(d);
a=(floor(d))*(atan(b));
b=fdim(h,c);
f=fmax(b,e);
}
}