#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(a,c))*(fmax(g,g));
c=atan(d);
g=pow(a,a);
b=atan(f);
h=sin(f);
while(isgreaterequal(h,a)){
g=atan2(c,d);
f=log(g);
}
c=(sin(f))*(atan(c));
e=fmax(a,e);
d=exp(c);
}