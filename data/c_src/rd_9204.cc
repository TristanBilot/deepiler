#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(d,f);
e=(fdim(g,c))+(fmin(b,b));
d=fmax(g,c);
d=(fmax(b,g))/(pow(f,b));
c=(pow(a,a))*(pow(f,c));
a=(fdim(c,g))*(fmax(b,e));
e=cos(e);
while(isgreaterequal(f,g)){
c=tan(a);
g=(fmax(f,e))*(fmax(c,d));
d=cos(c);
}
}