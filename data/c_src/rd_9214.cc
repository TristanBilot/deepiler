#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fdim(b,b))+(atan2(c,a));
b=fmax(e,b);
while(islessequal(a,f)){
d=fdim(e,b);
e=pow(e,a);
e=sin(b);
d=fmax(c,d);
}
while(islessequal(e,b)){
d=ceil(a);
e=tan(e);
f=(exp(c))*(atan(c));
a=fmin(a,b);
}
}