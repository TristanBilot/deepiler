#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmin(c,a))*(atan2(g,a));
f=sin(b);
b=sqrt(d);
f=(exp(b))*(fdim(c,g));
f=(fdim(b,e))+(pow(f,c));
while(islessequal(d,f)){
f=(pow(c,e))/(atan2(d,e));
c=asin(a);
a=(fdim(e,c))/(fmax(b,e));
e=atan2(f,b);
d=(atan2(b,g))/(atan2(a,c));
}
b=floor(e);
g=sqrt(d);
d=(tan(d))-(pow(d,a));
b=(pow(b,b))/(atan2(a,a));
a=pow(d,g);
}