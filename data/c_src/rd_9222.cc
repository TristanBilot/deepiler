#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(g,c))/(fdim(b,h));
f=(asin(e))/(cos(e));
d=(atan2(a,b))-(ceil(h));
b=log10(d);
if(isgreaterequal(f,g)){
f=(pow(g,e))*(pow(h,d));
c=(asin(g))/(fdim(c,f));
a=tan(c);
}
else{
d=floor(e);
h=(log10(a))/(ceil(b));
}
b=tan(d);
e=pow(a,f);
f=(pow(a,e))/(sin(h));
}