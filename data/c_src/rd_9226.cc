#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(floor(f))/(cos(e));
f=(fdim(a,e))*(pow(e,b));
b=(log10(b))+(ceil(a));
while(islessgreater(f,e)){
e=sqrt(a);
d=fmin(e,c);
b=cos(e);
f=(sqrt(a))-(fmin(a,c));
}
if(isgreaterequal(f,c)){
f=(fdim(a,c))-(fmin(c,f));
a=fmax(f,e);
c=(log(f))/(log(e));
b=(fmax(e,b))+(fmax(e,d));
b=(exp(a))+(fmax(b,b));
}
else{
a=exp(c);
e=fdim(b,d);
a=fdim(a,d);
}
}