#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(g,h);
h=(log10(e))+(fmin(h,b));
e=log10(b);
a=(log(a))+(atan2(b,h));
while(isgreaterequal(g,d)){
f=ceil(h);
c=(fmin(g,e))-(fdim(b,g));
b=(exp(b))*(fmax(f,h));
f=acos(g);
}
if(isgreaterequal(g,d)){
c=fmin(a,b);
d=fmax(f,f);
h=(asin(a))-(log(c));
}
}