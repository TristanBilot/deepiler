#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=acos(a);
d=pow(c,d);
f=(atan2(a,g))+(exp(f));
d=fmin(b,b);
a=pow(c,a);
d=sqrt(d);
f=(atan2(d,g))/(asin(g));
b=(floor(f))/(fdim(f,b));
b=tan(f);
e=exp(d);
if(islessequal(c,a)){
g=(fmax(g,g))+(fmax(c,g));
a=(fmax(d,b))/(pow(a,e));
c=log10(b);
g=(atan2(d,a))/(tan(g));
e=asin(f);
}
else{
a=(fmax(g,d))*(cos(a));
b=atan2(d,g);
e=log(d);
}
}