#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmin(c,b);
c=fmin(b,b);
if(islessequal(d,c)){
c=(log10(d))/(pow(c,a));
e=acos(c);
}
b=fmin(d,d);
b=(tan(e))-(acos(d));
d=(exp(a))-(pow(d,b));
b=atan2(e,d);
}