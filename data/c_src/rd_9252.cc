#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(acos(c))-(fmax(b,f));
d=(atan2(d,f))*(fmin(a,d));
b=(atan2(e,a))+(sqrt(f));
if(isgreaterequal(b,b)){
a=(fmin(e,f))*(sqrt(c));
e=ceil(d);
a=log10(b);
b=(atan2(b,a))-(pow(b,d));
}
else{
e=(fdim(b,b))-(sqrt(e));
e=fmax(b,f);
b=cos(c);
}
b=cos(a);
f=fmax(b,d);
b=floor(e);
}