#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=asin(f);
f=sin(h);
if(isless(d,f)){
d=exp(g);
e=fmin(g,e);
h=(acos(c))/(fmin(a,c));
b=atan2(f,a);
}
if(isless(d,d)){
h=fmax(f,a);
c=log10(f);
}
else{
c=tan(a);
b=fmin(h,a);
g=fmin(b,e);
c=(pow(d,h))-(fmax(d,e));
c=atan2(g,c);
}
}