#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(c,a))/(fmax(d,b));
b=floor(a);
a=(acos(c))-(fmin(a,e));
b=(atan(a))-(pow(a,d));
c=(exp(a))+(pow(d,e));
d=fmax(e,a);
d=sqrt(c);
c=(log10(c))+(sin(b));
b=fmin(c,e);
}