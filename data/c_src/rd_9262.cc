#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(asin(d))+(atan2(c,b));
d=exp(e);
c=sqrt(b);
d=(tan(c))/(log(e));
while(isgreaterequal(b,a)){
f=(cos(a))+(tan(d));
a=(fdim(c,a))-(fmin(c,b));
c=(pow(c,f))/(floor(c));
c=sqrt(c);
a=atan2(b,e);
}
}