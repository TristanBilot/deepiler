#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(cos(f))/(ceil(b));
h=acos(c);
a=(fdim(g,g))*(fmax(c,e));
while(isless(b,h)){
e=(pow(d,f))/(fdim(f,c));
g=fmax(e,h);
h=fdim(a,b);
}
d=acos(f);
h=(log(d))-(fmax(f,d));
g=(atan2(e,c))+(fdim(c,a));
f=(fmax(c,h))/(fmin(a,d));
}