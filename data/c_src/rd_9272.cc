#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=ceil(b);
e=(floor(a))+(atan2(a,e));
while(islessgreater(d,e)){
d=(fmax(d,c))/(fmin(d,b));
a=atan(c);
d=(asin(c))+(fmax(e,d));
b=pow(c,a);
}
e=(ceil(e))-(fmax(c,b));
a=ceil(d);
}