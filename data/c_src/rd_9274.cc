#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=ceil(d);
c=(fmin(e,d))+(fmin(a,c));
c=ceil(a);
d=fmin(e,e);
while(islessequal(c,e)){
b=pow(b,d);
b=(pow(a,e))*(atan2(d,e));
d=tan(b);
}
if(isless(b,d)){
e=exp(c);
e=log(b);
a=atan2(b,b);
}
}