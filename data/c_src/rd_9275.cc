#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(a,e);
d=log(e);
d=(floor(a))+(fdim(g,h));
h=atan2(h,b);
if(islessequal(a,a)){
e=(log10(g))/(cos(b));
d=(atan(f))+(acos(a));
}
else{
c=fdim(b,h);
h=(fdim(h,h))*(fmin(h,e));
}
g=atan(f);
a=(floor(d))+(floor(e));
d=cos(d);
}