#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(a,c);
d=atan2(c,f);
c=(fmin(e,b))*(ceil(e));
b=(fdim(a,b))+(floor(g));
d=(fmax(a,c))/(fmax(e,e));
f=sin(f);
e=(fmax(d,a))*(pow(h,b));
e=(log(c))*(floor(b));
g=fdim(a,g);
a=(fmax(f,f))-(cos(b));
b=(sin(c))/(pow(g,c));
g=atan2(f,g);
c=(atan(h))*(atan(d));
c=fmin(e,e);
}