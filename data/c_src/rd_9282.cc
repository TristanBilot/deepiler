#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(sin(c))*(log10(c));
f=(log10(d))+(fmin(a,b));
g=fdim(f,f);
c=(fmax(f,g))+(pow(c,f));
while(isgreaterequal(c,g)){
e=sqrt(f);
b=fdim(f,g);
f=fmax(b,b);
b=(log10(f))+(atan2(d,a));
}
b=(atan(e))/(fmax(a,f));
a=(acos(d))*(sin(f));
a=fdim(e,c);
}