#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(log(a))+(log(e));
c=(tan(f))*(fmax(b,e));
d=pow(a,c);
f=pow(e,f);
if(isless(c,d)){
b=sin(d);
f=atan2(e,b);
}
if(islessgreater(b,f)){
f=cos(a);
f=(pow(f,e))/(sqrt(f));
b=fmin(a,c);
}
else{
f=(pow(e,e))/(tan(b));
b=fmax(f,b);
b=tan(b);
b=(ceil(c))-(sin(b));
}
}