#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(b);
c=(atan2(e,c))*(fmax(c,b));
while(islessequal(c,a)){
d=(exp(a))+(fmax(e,c));
a=(acos(a))*(atan2(e,e));
a=(cos(a))*(fmax(c,b));
d=atan2(b,a);
}
if(islessequal(e,c)){
a=(exp(b))*(asin(b));
c=pow(a,b);
a=acos(b);
c=(atan2(a,a))/(tan(a));
}
}