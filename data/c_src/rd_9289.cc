#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(asin(d))-(fdim(d,b));
c=(floor(a))-(fmin(e,a));
d=exp(c);
g=(floor(c))-(fdim(c,g));
b=(log10(g))/(cos(h));
c=fdim(c,b);
d=atan2(b,b);
h=asin(f);
d=fmax(e,g);
}