#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(sqrt(a))/(fmin(e,f));
h=(fdim(b,b))*(cos(g));
h=fmin(g,d);
if(islessgreater(f,d)){
f=log10(d);
c=(fdim(c,e))*(pow(d,g));
d=(fmin(f,c))+(fmin(a,f));
}
e=(asin(e))-(sin(c));
h=sin(f);
d=pow(a,h);
g=(pow(b,b))-(sqrt(g));
}