#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(a,e);
a=(ceil(e))-(fmin(d,d));
e=(fmax(c,a))-(log(a));
b=log10(a);
a=(fmax(d,e))*(asin(c));
e=(ceil(a))+(fmax(a,a));
a=(pow(a,a))/(atan2(d,d));
c=tan(a);
d=(acos(c))/(fdim(d,b));
d=atan2(d,d);
while(islessgreater(e,a)){
a=ceil(a);
d=atan2(a,d);
d=atan2(b,b);
}
}