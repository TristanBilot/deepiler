#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan(e);
a=(fmin(a,a))-(ceil(d));
a=fmax(g,f);
f=cos(g);
f=(tan(c))-(fmin(g,g));
while(isless(f,a)){
b=(cos(d))+(fdim(b,d));
f=(fdim(c,e))/(acos(e));
}
a=fmax(e,b);
f=fdim(a,b);
c=(fmin(c,d))/(atan2(g,d));
}