#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(tan(c))*(log10(b));
b=(atan2(e,c))+(atan2(e,a));
if(isless(a,e)){
e=fmax(d,a);
d=fmax(b,c);
a=cos(a);
}
while(islessequal(a,e)){
a=atan2(a,c);
d=(acos(d))-(fmax(a,d));
c=(exp(c))+(pow(b,d));
a=acos(d);
}
}