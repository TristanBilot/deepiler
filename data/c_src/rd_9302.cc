#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(a))+(pow(e,b));
c=(cos(a))+(pow(c,a));
if(islessgreater(a,b)){
c=atan2(e,d);
d=asin(d);
}
while(islessequal(d,e)){
e=(asin(e))-(fmin(c,a));
c=fmin(c,e);
c=log10(d);
e=fdim(a,d);
e=pow(d,b);
}
}