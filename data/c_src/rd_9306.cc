#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=sin(b);
a=(fdim(a,a))*(ceil(c));
d=(fmin(c,e))/(atan2(d,c));
if(isless(b,d)){
c=(pow(e,b))+(fmax(a,d));
c=asin(d);
a=fmin(a,b);
}
if(islessequal(e,d)){
d=fmax(a,e);
b=fdim(d,a);
a=(fdim(e,d))-(sqrt(c));
}
else{
c=sin(a);
c=(atan2(e,c))/(fmin(e,e));
a=(atan2(d,b))/(atan2(e,e));
}
}