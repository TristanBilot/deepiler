#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(e,a))*(log(c));
h=acos(f);
b=cos(h);
c=log(b);
d=(log10(a))-(asin(e));
while(islessequal(f,a)){
c=(floor(b))/(sqrt(a));
h=atan2(d,e);
e=(log(g))/(atan(e));
g=atan2(h,a);
}
while(islessequal(h,c)){
h=(log(a))/(sqrt(e));
a=fdim(h,f);
g=(log10(b))*(fdim(g,g));
}
}