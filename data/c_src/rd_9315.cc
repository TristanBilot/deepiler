#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(exp(b))-(asin(g));
f=floor(a);
a=atan2(a,g);
while(islessgreater(d,d)){
c=asin(e);
c=fmin(f,b);
}
if(isless(b,d)){
e=(fdim(f,e))-(log(b));
e=(floor(e))+(floor(f));
f=(atan2(f,f))/(sin(f));
d=pow(e,e);
b=fmin(g,c);
}
}