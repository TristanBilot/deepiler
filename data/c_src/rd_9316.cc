#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(d,a))-(exp(b));
e=fmax(a,g);
if(isgreaterequal(d,g)){
b=sin(d);
d=(atan2(e,e))/(exp(c));
g=acos(f);
e=fmax(d,c);
}
a=fmin(c,f);
e=(pow(g,c))/(fmin(a,a));
f=exp(f);
d=(pow(c,d))*(atan2(g,f));
c=(atan(a))/(fmin(b,e));
}