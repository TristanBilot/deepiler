#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(c,c))+(pow(b,c));
b=(atan2(d,c))*(pow(h,f));
d=sqrt(e);
a=fmax(e,a);
e=fmin(a,g);
e=fmax(c,b);
a=(exp(h))-(sqrt(d));
if(isgreaterequal(g,b)){
b=sin(b);
c=(fmax(f,f))+(atan2(a,e));
a=exp(g);
}
else{
a=fdim(b,b);
a=(atan(g))-(pow(a,d));
f=(pow(a,a))+(sin(b));
}
}