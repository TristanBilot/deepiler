#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(f,b);
d=(ceil(f))*(fmin(b,d));
f=(pow(c,b))+(fdim(f,f));
a=(fdim(a,a))+(fdim(e,e));
c=sqrt(c);
b=(pow(e,f))*(fmax(e,f));
f=pow(c,d);
f=acos(f);
e=(fmax(b,d))/(sin(e));
f=(fdim(b,e))-(fmax(a,c));
}