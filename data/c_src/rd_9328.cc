#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(e,c))/(fdim(c,a));
d=acos(d);
while(islessgreater(d,d)){
a=(sin(d))+(fmax(b,e));
d=cos(a);
}
while(islessgreater(c,e)){
a=log10(e);
d=cos(b);
c=(fmin(b,d))+(fmin(e,c));
b=tan(a);
e=(fmin(c,d))-(sin(e));
}
}