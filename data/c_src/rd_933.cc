#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(a);
d=exp(c);
f=floor(g);
g=(log10(g))+(log10(g));
g=(cos(d))+(fmin(a,b));
b=(fmin(d,e))/(atan(a));
e=(atan2(e,d))+(pow(c,e));
b=sin(c);
while(isgreaterequal(b,g)){
b=(log10(a))/(pow(f,e));
g=(sin(a))+(acos(a));
c=floor(b);
}
}