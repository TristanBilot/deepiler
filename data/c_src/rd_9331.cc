#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(c,a);
b=log10(a);
a=(fmin(e,a))+(fdim(b,c));
if(islessgreater(c,a)){
c=(log10(a))*(atan(e));
a=(atan2(d,d))+(atan2(e,c));
d=fdim(c,a);
b=(atan2(a,b))+(atan(a));
}
else{
d=tan(d);
a=tan(e);
}
c=(sqrt(a))-(pow(c,b));
a=(floor(e))-(tan(b));
d=fmax(c,b);
e=atan2(e,a);
}