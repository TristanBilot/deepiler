#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(d,e);
a=(log10(c))+(sin(e));
c=tan(d);
e=(log10(e))-(fmax(b,b));
g=(atan2(g,f))*(pow(f,c));
while(islessequal(d,f)){
e=fmin(f,d);
b=fmin(e,d);
f=fmax(c,a);
b=(exp(d))/(log10(a));
}
if(isless(b,f)){
g=(asin(d))/(atan2(f,d));
a=(floor(c))*(log(f));
}
}