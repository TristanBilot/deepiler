#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(c);
d=(asin(e))+(pow(a,c));
while(isless(f,a)){
d=atan2(a,f);
d=atan2(g,c);
g=(exp(h))+(sqrt(d));
g=(fmax(b,h))+(asin(g));
a=(asin(d))/(log(c));
}
if(islessequal(b,g)){
b=(log(h))-(exp(g));
g=cos(g);
}
else{
f=ceil(b);
e=fdim(e,a);
c=atan2(e,e);
c=fmin(g,b);
}
}