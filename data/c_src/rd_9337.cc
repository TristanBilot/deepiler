#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(exp(b))*(sin(c));
h=(atan2(e,h))+(fmax(h,b));
h=atan2(h,g);
while(isless(e,f)){
d=(log(h))*(floor(g));
a=(atan2(c,b))-(fmin(g,e));
}
while(isless(d,a)){
f=(fmin(g,d))/(log(a));
b=(fdim(e,e))-(log(e));
}
}