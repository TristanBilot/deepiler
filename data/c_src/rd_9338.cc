#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(e,g);
h=(asin(a))-(ceil(b));
f=cos(a);
e=(fmin(h,e))+(sqrt(f));
g=(pow(h,c))-(fmax(h,h));
if(islessgreater(c,a)){
e=asin(b);
b=(sin(c))+(cos(c));
}
}