#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(f,e);
c=fmax(a,e);
e=(atan2(a,a))*(fmax(c,d));
f=atan2(a,b);
b=(pow(d,f))+(fdim(c,a));
f=(fmax(c,c))+(atan2(d,f));
f=sin(a);
e=fmax(e,c);
e=(fdim(f,b))/(log10(c));
while(islessequal(c,a)){
c=(fmax(c,c))*(sqrt(f));
b=(atan2(b,b))-(fmax(b,d));
e=cos(b);
}
}