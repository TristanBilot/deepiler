#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(a,b))*(floor(e));
d=(tan(a))/(ceil(e));
b=(log(b))+(pow(a,d));
b=(cos(c))/(tan(f));
if(isless(d,c)){
f=fmax(b,a);
b=(fmin(a,d))/(atan2(c,f));
b=(pow(b,c))-(fmax(e,f));
f=floor(a);
d=asin(e);
}
if(islessgreater(f,e)){
e=(atan2(b,e))+(log10(c));
a=exp(c);
}
}