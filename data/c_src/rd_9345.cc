#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(e,c))/(fdim(c,e));
e=(fmax(a,b))*(ceil(d));
d=fmin(e,b);
c=(asin(a))+(fdim(b,b));
c=(atan(b))+(fmax(a,e));
c=(log(e))-(atan(a));
b=(cos(c))+(fmax(a,a));
a=fdim(e,c);
b=floor(a);
if(islessgreater(c,c)){
c=floor(e);
e=(fdim(b,c))+(log(c));
b=atan(d);
}
}