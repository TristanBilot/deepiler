#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(sin(d))*(atan(b));
g=floor(e);
c=(pow(c,d))*(fmin(e,c));
a=(tan(d))*(fdim(f,e));
e=(atan2(b,a))*(fmin(f,e));
d=exp(f);
f=pow(e,g);
if(islessgreater(g,a)){
b=(tan(b))*(pow(g,d));
d=(fdim(b,b))+(log10(a));
g=sin(f);
e=pow(b,c);
a=(sin(g))/(ceil(f));
}
else{
b=fdim(e,e);
f=pow(b,b);
e=(fmin(c,g))+(fmax(d,c));
a=(fdim(b,a))/(pow(a,b));
b=atan(d);
}
}