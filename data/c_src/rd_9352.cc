#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(c,b))+(cos(c));
b=pow(e,b);
a=exp(e);
a=exp(e);
while(isless(d,a)){
e=pow(b,c);
d=fmax(d,d);
}
if(islessgreater(d,e)){
a=fmin(c,d);
c=(floor(b))/(acos(e));
d=acos(a);
b=floor(b);
}
else{
c=fmin(e,e);
d=atan(d);
b=(fdim(b,e))-(atan2(c,a));
d=(ceil(b))/(tan(d));
c=(ceil(d))/(sqrt(b));
}
}