#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(c,c))/(fmin(b,e));
a=atan2(c,a);
d=log(b);
c=atan2(a,e);
b=pow(a,a);
a=asin(c);
d=(exp(a))/(pow(a,d));
d=pow(a,b);
c=fmin(c,d);
}