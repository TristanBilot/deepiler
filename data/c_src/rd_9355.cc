#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(b,d))-(fdim(c,d));
a=(acos(b))/(fmin(a,a));
if(isless(e,b)){
c=(atan(d))*(ceil(e));
d=(fdim(e,c))/(sqrt(d));
b=exp(c);
}
else{
e=(log10(e))/(asin(b));
e=pow(c,c);
c=log(b);
}
while(isgreaterequal(a,e)){
a=fmax(e,c);
a=pow(b,b);
}
}