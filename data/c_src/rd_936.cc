#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,c))+(fmin(e,e));
b=(atan2(c,a))*(pow(d,a));
d=fmax(d,c);
e=(pow(a,a))/(log10(d));
while(islessgreater(a,b)){
a=fdim(e,a);
e=(fmin(e,a))/(asin(c));
b=(fmax(b,c))*(pow(d,c));
b=sqrt(c);
e=pow(b,e);
}
if(isless(b,d)){
a=(atan2(a,d))/(pow(a,d));
c=fdim(b,b);
d=asin(a);
c=(pow(d,e))/(acos(d));
a=(ceil(b))+(cos(e));
}
else{
c=tan(c);
e=acos(c);
d=ceil(d);
c=fmin(b,e);
c=(sqrt(d))+(exp(b));
}
}