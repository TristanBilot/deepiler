#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fdim(d,e);
f=(fdim(e,f))-(atan(c));
e=acos(b);
c=fdim(g,b);
b=(fmax(c,d))/(sin(g));
if(islessequal(d,d)){
b=log(f);
g=(sqrt(d))-(cos(a));
}
else{
c=(cos(c))/(tan(b));
f=ceil(c);
a=fdim(f,g);
f=atan(b);
g=(pow(a,c))*(fdim(b,e));
}
c=(fmax(c,d))-(fmax(e,g));
f=acos(c);
}