#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(a))-(log(b));
c=(pow(e,d))+(fmin(d,b));
c=(fdim(b,c))*(acos(c));
a=(fmax(d,b))*(atan(d));
a=(fdim(b,c))*(fmin(d,b));
a=fmin(e,e);
d=(acos(b))/(acos(e));
if(islessgreater(c,c)){
a=(fmin(d,a))-(pow(d,a));
b=(fmin(c,b))-(fmax(c,c));
}
}