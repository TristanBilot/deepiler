#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(e,b))-(log(e));
d=sqrt(d);
d=fdim(a,e);
d=fmax(e,e);
while(islessgreater(e,a)){
a=atan2(d,d);
b=fmax(b,e);
}
if(islessequal(a,d)){
d=(sqrt(e))/(fmax(e,c));
a=fmax(c,a);
}
else{
e=acos(d);
b=atan2(b,e);
a=fmin(a,d);
e=fdim(e,b);
e=(pow(a,b))+(fmax(a,e));
}
}