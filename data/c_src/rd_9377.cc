#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=exp(e);
e=(fmax(e,a))+(pow(d,b));
f=(asin(e))*(sin(c));
f=sqrt(a);
d=sqrt(b);
d=fmin(a,d);
e=(fmax(b,e))*(atan(g));
b=(acos(b))+(log(b));
c=ceil(f);
g=asin(b);
b=atan2(e,g);
}