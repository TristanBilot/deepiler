#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=pow(f,c);
e=acos(d);
f=(fdim(c,c))+(cos(f));
d=(ceil(c))*(sqrt(d));
a=(log(f))-(fmin(b,b));
b=acos(d);
while(isless(c,e)){
e=(fmax(e,f))+(fdim(d,b));
d=pow(d,a);
}
}