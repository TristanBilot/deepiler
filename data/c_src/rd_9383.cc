#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(ceil(b))*(sin(c));
e=atan2(a,d);
b=pow(b,b);
b=fmax(c,b);
c=cos(a);
b=fmax(b,e);
e=ceil(b);
b=log10(b);
}