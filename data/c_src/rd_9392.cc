#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fdim(f,h);
g=(pow(g,f))*(atan2(e,b));
d=(atan2(h,c))+(tan(e));
g=(sqrt(h))+(atan(h));
if(isless(g,c)){
a=(pow(e,h))*(pow(d,c));
g=(atan2(h,c))-(atan2(d,a));
}
else{
f=(fdim(c,e))+(atan2(e,f));
c=fdim(g,g);
f=fmin(e,c);
}
h=fmax(g,c);
d=(fdim(a,c))*(cos(e));
b=(log10(g))/(fmax(c,a));
g=(atan2(d,h))-(pow(e,a));
}