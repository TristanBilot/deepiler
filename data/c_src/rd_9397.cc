#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(log(b))*(pow(a,c));
e=tan(b);
b=sqrt(c);
if(isless(d,b)){
c=fdim(c,b);
d=sqrt(c);
a=(atan(a))/(asin(e));
d=(asin(c))/(sin(d));
d=(cos(e))/(fmax(a,a));
}
while(isless(d,e)){
d=(fdim(c,b))-(fmax(d,b));
e=(tan(c))+(pow(e,b));
b=(exp(b))+(log10(c));
}
}