#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(b);
b=log10(d);
b=(floor(b))+(atan(d));
e=(atan(f))+(atan2(c,d));
while(isless(a,b)){
f=fmax(b,d);
h=fmax(f,e);
d=(tan(c))+(atan2(c,f));
}
while(islessequal(f,f)){
f=pow(b,h);
c=(log(f))/(log(d));
h=atan(g);
g=ceil(g);
}
}