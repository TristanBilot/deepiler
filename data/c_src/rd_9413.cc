#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(a,f);
c=log(c);
e=atan2(f,g);
a=(exp(a))*(fdim(f,h));
d=atan(f);
g=(log10(d))+(fmax(b,f));
f=(fdim(c,d))*(tan(h));
while(isgreaterequal(d,a)){
b=floor(f);
a=log(e);
g=(fdim(e,f))/(fmax(b,b));
a=(log(c))/(fmin(h,e));
c=atan2(d,f);
}
}