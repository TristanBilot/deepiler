#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log10(c))*(fmax(c,c));
d=sqrt(b);
d=(log10(b))*(cos(b));
e=(asin(b))*(fdim(c,b));
c=(cos(a))-(sin(a));
c=fmin(a,a);
d=fmax(e,b);
e=fdim(a,a);
d=(tan(b))+(atan2(a,e));
b=(ceil(d))*(exp(d));
b=log(d);
e=(fmin(d,d))-(atan(c));
c=exp(b);
c=(floor(c))-(pow(c,e));
c=(fmin(e,a))+(atan2(b,d));
}