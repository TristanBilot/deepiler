#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(b,c))-(atan2(d,a));
c=(fmin(b,b))+(atan2(c,d));
if(isless(a,a)){
a=(cos(d))*(log10(c));
a=sin(a);
}
c=(cos(d))*(log10(d));
a=(asin(a))+(atan(d));
}