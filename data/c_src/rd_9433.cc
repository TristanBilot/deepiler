#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(d,b))-(cos(c));
e=(atan2(a,d))*(fmax(d,c));
if(isless(e,f)){
f=(asin(b))+(log10(f));
a=acos(e);
f=fmax(f,b);
e=fmax(e,d);
f=(acos(g))-(asin(f));
}
else{
f=fmax(f,b);
e=(fdim(f,g))-(log(g));
c=(atan2(g,f))+(log(a));
c=atan2(g,f);
}
if(islessgreater(g,g)){
d=(floor(g))/(fmin(f,c));
e=(acos(g))-(atan2(a,g));
b=pow(a,c);
e=log(d);
}
else{
e=(fdim(c,b))/(log10(d));
d=fmin(c,e);
c=(cos(c))+(atan(e));
}
}