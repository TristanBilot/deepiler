#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan(c);
e=(fmax(a,b))/(fmax(c,c));
d=pow(e,a);
d=exp(e);
c=(fdim(c,e))/(asin(b));
c=log(a);
b=asin(e);
e=(fmax(c,d))/(cos(c));
if(islessgreater(d,b)){
c=asin(e);
c=(sin(c))*(sqrt(c));
b=(log10(a))*(exp(c));
}
else{
b=fmax(d,b);
a=(pow(d,b))-(exp(a));
c=(atan(a))*(pow(a,e));
d=(atan2(a,b))/(fmin(b,b));
d=(sin(d))/(exp(b));
}
}