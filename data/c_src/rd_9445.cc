#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(a);
b=(fdim(b,b))/(cos(a));
while(isgreaterequal(d,d)){
b=ceil(d);
c=log(e);
a=fmin(a,b);
e=(tan(a))-(atan2(e,a));
c=(atan2(b,a))-(pow(d,e));
}
while(islessequal(a,d)){
c=fmax(d,b);
d=pow(e,e);
b=fmin(c,d);
}
}