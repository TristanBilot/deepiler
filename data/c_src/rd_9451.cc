#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(e,a);
b=atan(d);
a=fmax(a,a);
e=(floor(a))-(acos(c));
b=(fmax(b,d))+(cos(c));
d=tan(a);
b=(fdim(c,d))+(fdim(c,f));
d=(fmax(b,b))-(floor(a));
d=log(a);
f=exp(f);
}