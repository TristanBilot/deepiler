#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(a))*(log(f));
c=pow(f,c);
e=(floor(e))*(log(f));
f=(floor(e))/(sqrt(a));
if(isless(c,c)){
b=fdim(a,c);
a=fdim(a,b);
d=(tan(c))-(fmin(b,a));
f=(log10(e))-(atan2(e,a));
f=sin(a);
}
else{
e=(fmin(c,c))*(fmax(d,b));
e=floor(e);
}
b=pow(d,c);
c=fmin(c,a);
}