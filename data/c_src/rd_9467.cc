#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,c);
h=fmax(e,c);
if(islessequal(h,h)){
e=asin(h);
e=(fmax(e,e))*(exp(d));
}
else{
c=fmax(g,f);
b=(asin(b))/(fmax(a,b));
h=(fdim(d,g))-(pow(a,g));
c=(sin(a))*(atan2(f,f));
g=(atan2(h,e))+(fmin(e,e));
}
if(isless(b,e)){
h=(ceil(c))/(exp(d));
c=fmax(g,c);
g=fmin(a,h);
}
}