#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(b,a))+(floor(d));
b=(exp(b))+(floor(c));
b=log(g);
d=fmin(d,g);
f=(pow(e,b))+(fdim(c,e));
e=(atan2(g,c))*(atan2(e,a));
f=(sin(e))/(exp(c));
c=(sin(d))*(asin(c));
c=atan2(f,d);
e=(floor(c))+(fmin(g,g));
}