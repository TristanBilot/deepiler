#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(f,c);
d=(tan(d))+(pow(d,f));
b=(ceil(e))/(sin(b));
d=fmax(g,e);
b=fmin(f,c);
b=atan2(f,g);
g=atan(f);
f=acos(g);
e=(floor(b))*(atan2(e,c));
f=(tan(g))/(exp(e));
b=fdim(a,g);
c=log(d);
}