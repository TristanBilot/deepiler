#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(a,c))+(atan2(b,b));
e=(atan(d))+(sqrt(d));
e=tan(e);
a=(fmax(e,a))-(fmax(c,d));
if(isgreaterequal(e,d)){
e=log(d);
e=asin(d);
}
while(islessequal(a,d)){
e=sin(d);
a=pow(e,d);
}
}