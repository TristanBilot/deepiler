#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(b,b))/(fmin(a,d));
a=fmax(d,b);
e=(fmax(d,d))+(exp(e));
b=(sin(a))+(fdim(e,e));
d=fmin(d,b);
while(isgreaterequal(e,e)){
b=fmax(a,d);
e=(acos(a))+(log(b));
e=log10(c);
}
c=(fdim(e,d))-(log(c));
e=exp(a);
}