#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(d,d))+(fdim(c,c));
e=(acos(b))-(atan2(d,e));
if(isgreaterequal(b,c)){
a=fmax(a,b);
a=(asin(a))+(exp(d));
a=(log10(c))/(fmin(e,e));
b=(fdim(d,e))*(fmax(a,e));
}
else{
e=(atan2(b,b))/(fmax(e,d));
b=log(e);
e=cos(e);
b=(fdim(b,d))*(cos(b));
}
d=fmax(e,b);
c=(asin(b))/(floor(a));
d=(floor(c))+(atan(b));
}