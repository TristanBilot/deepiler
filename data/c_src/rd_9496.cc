#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(exp(e))+(sin(d));
e=(floor(d))+(floor(b));
if(isless(c,d)){
g=cos(f);
b=fmin(b,d);
}
if(isless(f,b)){
h=(sin(g))-(fmin(g,g));
f=acos(g);
e=asin(d);
e=(exp(h))-(sqrt(h));
}
else{
g=(cos(g))/(fdim(f,a));
h=fdim(a,g);
e=(pow(b,g))+(fdim(g,a));
}
}