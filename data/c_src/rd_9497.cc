#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log(b))+(acos(a));
c=(log(e))-(fmax(f,d));
f=pow(f,a);
while(isless(e,a)){
b=atan2(d,f);
d=(sqrt(f))-(fmax(b,b));
e=(pow(d,a))/(cos(e));
f=atan2(e,f);
}
while(islessequal(b,d)){
e=acos(b);
c=(tan(a))-(fmax(f,e));
c=(pow(f,b))/(fmin(f,a));
}
}