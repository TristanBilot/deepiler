#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fdim(a,e);
e=(fdim(f,d))-(atan2(d,e));
f=(atan(a))/(sin(f));
e=fmin(b,d);
d=fmax(a,d);
d=acos(a);
d=(fmax(f,a))-(fdim(c,b));
d=floor(b);
while(islessgreater(c,f)){
c=(sqrt(c))-(asin(b));
e=(acos(c))-(atan(b));
b=(sin(d))-(ceil(b));
}
}