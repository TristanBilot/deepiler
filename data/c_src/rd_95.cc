#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmin(e,c))*(atan2(e,a));
e=sin(e);
b=pow(c,f);
d=fmin(a,c);
b=(atan2(d,e))/(fmin(a,e));
while(islessequal(b,d)){
d=(fmin(d,a))*(fmin(c,a));
d=(fmin(b,d))+(floor(d));
}
while(islessequal(c,c)){
f=(cos(a))*(asin(b));
f=(log10(a))*(fmin(a,b));
c=pow(f,b);
c=(pow(e,c))+(floor(e));
b=fdim(c,b);
}
}