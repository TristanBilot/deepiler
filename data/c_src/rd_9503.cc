#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=floor(b);
a=fdim(a,b);
while(islessgreater(e,d)){
e=fdim(d,a);
a=(sin(a))-(atan2(a,c));
c=sqrt(b);
}
a=fmin(c,b);
b=(floor(c))/(pow(a,c));
b=(sqrt(c))-(pow(e,b));
e=fmin(b,e);
e=sqrt(c);
}