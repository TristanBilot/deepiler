#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan(c))-(atan2(f,g));
a=pow(d,h);
h=fmax(d,g);
e=fmax(g,e);
b=(sqrt(c))*(sin(f));
h=(cos(f))*(log10(g));
a=fdim(e,c);
if(islessequal(c,f)){
c=(fdim(f,g))*(fdim(h,c));
f=(ceil(g))-(tan(c));
h=(fdim(d,f))-(exp(a));
c=(sin(d))-(acos(a));
h=atan(d);
}
}