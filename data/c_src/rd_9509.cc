#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=floor(a);
e=(cos(c))/(atan(a));
f=(pow(c,a))*(fmin(d,f));
e=(tan(c))+(fmax(c,d));
if(islessgreater(f,e)){
e=log10(f);
f=(fmin(c,c))-(atan2(d,a));
c=fmax(c,d);
}
else{
e=fmax(b,f);
a=cos(d);
d=fmax(d,e);
e=(tan(b))+(sqrt(b));
}
if(islessequal(c,e)){
a=fmin(c,a);
e=(log10(e))*(atan2(f,e));
c=sqrt(f);
}
else{
d=sqrt(f);
c=(floor(d))-(fmin(b,f));
}
}