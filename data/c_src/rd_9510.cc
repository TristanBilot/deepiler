#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(g);
d=fmax(h,c);
a=fdim(d,f);
e=(acos(a))*(fdim(g,d));
if(isless(a,a)){
f=(asin(c))-(tan(h));
e=(fdim(h,c))*(sqrt(b));
g=log10(d);
}
else{
g=sqrt(h);
e=sin(b);
c=(fdim(h,a))+(fmax(h,a));
c=(exp(c))/(acos(d));
}
}