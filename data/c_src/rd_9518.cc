#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(a,b);
c=(ceil(d))+(fmax(e,a));
d=atan2(a,c);
c=(floor(b))-(tan(a));
a=atan2(e,d);
if(islessgreater(e,e)){
a=floor(a);
e=asin(e);
d=(fdim(d,c))+(fdim(a,b));
e=(acos(a))+(atan(e));
c=log(a);
}
if(islessgreater(a,c)){
c=ceil(c);
b=(exp(e))-(fdim(a,a));
a=(log(a))-(log(d));
c=(acos(a))-(fmax(e,e));
}
else{
a=(atan2(e,d))/(fmin(c,a));
b=(fmin(c,b))*(log(a));
e=(log10(a))/(fmax(a,a));
a=pow(a,b);
a=atan2(e,c);
}
}