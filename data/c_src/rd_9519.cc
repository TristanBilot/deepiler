#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(d,a))*(fdim(a,d));
g=(tan(g))+(asin(c));
g=(pow(e,h))-(pow(e,b));
if(islessequal(e,c)){
c=exp(b);
g=(pow(c,f))*(fmax(f,f));
f=atan2(c,a);
f=(acos(a))-(fmin(g,d));
c=fmin(d,a);
}
c=fmax(g,h);
b=(sqrt(a))+(log(e));
d=sqrt(f);
}