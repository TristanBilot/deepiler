#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(a,b);
e=(sin(a))*(fmax(g,b));
f=exp(a);
d=(fdim(f,d))/(asin(a));
c=atan(f);
c=fmax(g,g);
f=(pow(g,c))/(atan2(f,e));
g=(log10(b))-(atan(e));
if(islessequal(e,a)){
f=(log10(a))*(tan(b));
c=asin(g);
c=log(f);
f=fmax(c,g);
f=fmax(g,b);
}
else{
a=(fmin(a,a))/(acos(d));
c=fdim(e,g);
b=(fmin(e,e))/(exp(f));
}
}