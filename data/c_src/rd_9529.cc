#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fdim(c,d))*(tan(g));
d=(fdim(e,a))/(sqrt(b));
a=fmax(a,e);
e=(pow(a,f))+(pow(b,c));
while(isless(f,b)){
f=log(c);
e=sin(e);
c=atan(g);
f=floor(a);
b=(fmin(g,g))/(tan(c));
}
b=(ceil(f))+(pow(c,e));
c=fmax(e,e);
}