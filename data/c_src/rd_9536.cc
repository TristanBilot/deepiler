#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan(a);
b=(log(b))*(cos(a));
e=(pow(d,c))-(atan2(c,d));
e=fmin(a,e);
d=(asin(c))*(pow(b,b));
e=fmin(e,e);
b=tan(a);
c=(atan2(d,e))-(atan(b));
e=(exp(c))+(fmax(e,a));
while(islessequal(a,a)){
a=log(e);
b=(sqrt(c))*(sqrt(a));
e=(fmax(d,d))/(atan2(d,c));
}
}