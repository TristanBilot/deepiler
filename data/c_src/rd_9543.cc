#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=log10(e);
e=atan(a);
while(isless(d,a)){
d=log10(b);
a=atan(a);
e=pow(e,c);
}
while(isgreaterequal(f,e)){
e=(fmax(d,e))/(asin(c));
f=(atan2(a,f))-(atan2(c,d));
e=cos(b);
}
}