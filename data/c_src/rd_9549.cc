#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(floor(c))*(log(a));
a=fdim(f,d);
a=pow(d,c);
g=(sqrt(a))+(log10(e));
if(isless(f,b)){
b=asin(g);
a=atan2(e,e);
e=(log10(d))/(fdim(a,d));
a=pow(e,e);
}
while(islessgreater(a,b)){
g=fdim(c,c);
c=fmax(d,c);
b=fmax(e,d);
d=(acos(c))-(asin(a));
g=(ceil(a))-(log(a));
}
}