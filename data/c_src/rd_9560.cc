#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(d,c);
d=fdim(d,f);
e=(fmin(a,e))+(fmin(g,g));
c=(atan(g))*(cos(c));
if(islessequal(d,a)){
e=(tan(d))/(acos(g));
f=fmin(e,g);
b=(log(g))+(fmax(a,c));
b=exp(e);
c=atan2(c,a);
}
else{
a=(fdim(b,c))*(fdim(d,f));
g=(exp(b))-(fmax(d,a));
b=pow(c,a);
d=(fmax(c,a))/(pow(d,c));
}
while(isgreaterequal(a,d)){
f=(pow(f,g))-(log10(e));
c=(sin(f))+(ceil(e));
a=log(e);
c=(pow(d,c))-(fmax(c,b));
}
}