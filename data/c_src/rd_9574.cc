#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(e,b))-(fmax(f,e));
a=fmax(g,f);
e=fmin(b,d);
e=atan(b);
f=fdim(b,f);
while(islessgreater(e,c)){
e=log10(c);
g=atan2(a,c);
a=exp(f);
}
while(islessequal(b,b)){
c=(floor(g))+(fmax(a,g));
f=log(f);
e=(atan2(d,c))-(atan2(g,c));
g=asin(b);
e=fmax(d,e);
}
}