#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(sin(a))-(pow(a,e));
b=(cos(e))+(fmin(f,f));
f=(exp(d))*(acos(c));
a=(asin(a))+(fmin(e,e));
g=(atan(e))*(ceil(f));
if(isgreaterequal(f,g)){
g=ceil(c);
c=atan2(b,e);
b=fdim(g,a);
}
else{
g=exp(c);
e=sin(c);
a=asin(b);
a=atan2(f,g);
b=(atan(b))-(acos(e));
}
if(isless(d,b)){
a=(atan2(b,e))-(atan2(b,a));
e=(sqrt(d))/(atan2(a,d));
g=fmin(d,e);
}
else{
a=cos(f);
d=(fmin(f,a))/(ceil(a));
}
}