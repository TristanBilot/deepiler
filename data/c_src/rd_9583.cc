#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(d);
d=fmax(e,b);
c=(pow(b,c))/(log10(a));
b=floor(a);
if(isless(b,b)){
c=fmin(c,b);
a=(exp(b))+(cos(c));
d=fmax(b,c);
a=fmax(a,b);
}
while(isgreaterequal(c,e)){
c=floor(e);
a=(sin(d))/(fmin(d,d));
c=asin(c);
b=pow(d,e);
c=atan(e);
}
}