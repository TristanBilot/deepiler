#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(e,h);
c=(sin(a))/(asin(f));
f=cos(h);
d=acos(f);
if(islessequal(b,h)){
c=fdim(c,c);
f=log10(a);
d=ceil(e);
d=sqrt(g);
b=fmax(g,c);
}
while(islessgreater(b,a)){
g=(pow(h,f))-(pow(d,g));
h=pow(g,h);
e=(fdim(g,d))/(asin(f));
}
}