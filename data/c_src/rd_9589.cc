#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(exp(b))+(sqrt(c));
c=(fmax(c,a))+(tan(a));
c=(cos(d))/(log10(c));
c=(atan2(c,d))+(tan(e));
if(islessgreater(e,a)){
a=fmin(c,e);
c=(acos(e))*(ceil(e));
e=fmax(f,d);
f=fmax(d,d);
}
}