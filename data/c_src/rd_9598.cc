#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan(g);
g=(fdim(d,g))*(acos(e));
c=(log10(f))/(fdim(e,e));
e=(atan2(a,e))+(atan2(c,f));
e=fmin(a,f);
b=fdim(a,e);
if(isless(e,f)){
b=pow(e,a);
c=pow(a,d);
f=asin(a);
e=(fdim(b,f))+(exp(e));
}
else{
g=(log10(a))*(atan(d));
e=fmax(c,d);
c=atan2(d,a);
c=(atan(e))/(ceil(c));
}
}