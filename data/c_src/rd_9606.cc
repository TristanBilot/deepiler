#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(atan2(g,c))*(atan(a));
f=(atan2(a,d))-(tan(g));
while(isgreaterequal(a,e)){
e=fdim(b,g);
c=(pow(b,f))-(asin(b));
f=sqrt(f);
}
if(isgreaterequal(c,c)){
a=(atan(g))-(atan2(g,d));
b=fmax(d,a);
}
else{
c=log10(a);
g=sqrt(f);
e=(atan(a))/(fdim(e,g));
}
}