#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=sqrt(e);
e=(floor(b))*(fdim(e,e));
f=(exp(a))-(fmin(g,e));
a=sin(d);
c=asin(c);
g=cos(f);
c=(fmin(b,g))*(asin(d));
d=(pow(e,e))-(pow(e,c));
f=fdim(f,b);
}