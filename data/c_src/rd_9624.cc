#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(c,a);
a=(fmax(c,f))+(fdim(d,e));
f=fmax(f,c);
if(islessequal(d,d)){
c=pow(c,d);
b=(log10(a))*(pow(b,c));
c=asin(a);
a=(fmax(d,e))-(tan(g));
}
else{
f=floor(a);
a=(fdim(b,f))*(asin(e));
c=cos(e);
}
if(isgreaterequal(e,c)){
e=(acos(e))*(floor(f));
f=pow(a,e);
a=log10(c);
}
else{
f=(exp(f))/(log10(e));
g=(pow(f,a))-(ceil(g));
g=ceil(b);
g=(fmax(g,g))*(tan(a));
f=pow(d,a);
}
}