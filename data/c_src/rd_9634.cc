#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(c,d))*(fmin(d,a));
c=atan(e);
a=fmax(c,d);
e=(cos(d))+(atan(d));
d=(fmin(c,d))*(atan2(a,b));
if(isless(c,a)){
d=(pow(b,e))+(acos(a));
e=sin(a);
d=asin(d);
}
else{
a=log10(e);
d=(pow(e,c))-(fmin(c,a));
a=(fmax(c,d))*(sin(c));
}
if(isless(a,b)){
c=(log10(c))-(fdim(c,c));
d=(floor(d))-(asin(a));
b=(floor(a))*(atan2(d,c));
c=(fmax(a,e))/(pow(c,d));
d=(fmax(c,d))*(fdim(d,e));
}
}