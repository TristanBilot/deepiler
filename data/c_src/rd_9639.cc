#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan2(a,e))-(fmax(c,e));
a=(fmin(b,b))*(fmin(e,f));
a=atan2(g,f);
while(isless(f,f)){
a=sqrt(f);
c=(tan(f))+(tan(e));
e=(tan(a))+(atan(b));
}
while(isgreaterequal(c,d)){
a=(atan2(d,a))-(cos(c));
a=atan2(f,e);
d=atan2(c,g);
}
}