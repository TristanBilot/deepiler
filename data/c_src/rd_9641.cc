#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(a,c);
d=exp(f);
a=(atan2(a,e))-(atan2(d,b));
d=(fmax(f,b))-(sin(d));
f=atan(c);
d=fmax(f,b);
b=(log(b))-(cos(b));
b=exp(a);
while(isgreaterequal(d,b)){
f=fmin(d,c);
d=sin(d);
c=(floor(f))/(exp(d));
}
}