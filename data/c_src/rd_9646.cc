#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(d,b))-(fmax(f,e));
a=atan2(d,a);
f=(cos(e))-(fmax(e,a));
c=(fdim(a,e))/(log10(c));
b=log10(d);
while(islessequal(b,b)){
e=(fmax(c,f))*(sqrt(f));
c=atan2(f,c);
f=(fdim(e,c))*(log(a));
f=(pow(f,f))-(floor(f));
}
while(islessequal(f,b)){
a=fmax(d,a);
c=atan(b);
}
}