#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(a,a);
a=fdim(d,d);
b=tan(c);
a=log10(b);
while(islessequal(a,e)){
e=(pow(b,a))*(tan(c));
d=(cos(e))/(atan2(a,c));
a=fmin(b,d);
c=(pow(e,e))/(log(e));
}
if(islessgreater(d,a)){
e=pow(b,a);
c=atan2(b,a);
c=(ceil(c))-(atan2(b,c));
d=(ceil(d))-(tan(e));
}
else{
c=pow(b,b);
b=(pow(e,c))-(fmax(c,c));
d=(fmin(b,d))*(atan2(b,a));
c=(atan2(b,e))/(fmax(c,b));
e=fmax(d,c);
}
}