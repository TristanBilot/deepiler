#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(b,c))+(fmax(a,d));
e=(atan(e))/(fmin(e,b));
c=floor(b);
while(isless(c,e)){
c=(log(b))/(pow(c,d));
a=(fmin(a,b))-(atan2(d,e));
c=fmin(b,c);
c=exp(a);
}
e=(ceil(d))/(sin(d));
d=(fmax(a,b))*(exp(a));
b=atan2(d,a);
b=fdim(a,b);
}