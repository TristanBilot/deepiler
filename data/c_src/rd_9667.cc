#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fdim(f,b))/(sqrt(c));
f=(fmax(e,e))-(asin(a));
g=atan2(e,a);
a=fmin(b,e);
f=(exp(f))/(sqrt(c));
b=atan2(d,a);
if(isless(f,b)){
c=fdim(a,a);
d=fmax(b,d);
b=atan2(d,b);
e=(fmax(d,d))*(asin(c));
b=(pow(f,c))/(acos(a));
}
}