#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan(h);
f=(log10(b))+(fmax(h,f));
while(islessgreater(f,b)){
g=(fdim(b,f))*(pow(f,b));
d=pow(c,a);
a=(fmax(h,h))/(fmax(c,a));
h=floor(a);
}
if(isless(d,f)){
h=fmin(f,a);
g=fmin(g,h);
g=fdim(b,g);
}
}