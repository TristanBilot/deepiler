#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=floor(f);
g=exp(e);
d=fmin(a,c);
f=(atan2(b,h))/(sin(f));
e=fmin(e,d);
d=(sin(h))*(cos(e));
b=exp(c);
b=fdim(c,h);
e=(fmax(e,b))+(floor(c));
e=(asin(g))-(pow(b,a));
b=(sqrt(f))/(atan2(d,f));
a=(fmax(g,c))+(log(g));
c=(fdim(g,c))/(fmax(h,a));
}