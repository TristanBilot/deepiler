#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=sin(a);
d=(log(e))+(fmax(e,b));
g=(pow(c,f))-(fmax(e,a));
g=(tan(d))/(ceil(e));
while(islessgreater(e,c)){
g=(pow(g,c))+(atan2(b,g));
c=(fmax(a,g))+(exp(b));
d=(cos(d))*(fmin(g,e));
g=(sqrt(e))*(acos(b));
}
e=pow(g,d);
g=(fdim(e,c))*(cos(g));
g=fdim(c,d);
f=atan(e);
a=pow(a,e);
}