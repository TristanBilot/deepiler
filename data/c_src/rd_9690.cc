#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=sin(d);
f=(fdim(a,b))*(fdim(b,f));
b=exp(c);
e=(log10(a))+(fmin(d,e));
a=acos(a);
d=fmin(f,b);
while(isgreaterequal(a,a)){
e=(fdim(b,c))-(pow(f,d));
f=(fdim(d,c))-(fmin(a,c));
b=(fmax(b,c))*(atan2(c,c));
}
}