#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(d,d))+(exp(b));
a=tan(e);
if(islessgreater(b,b)){
c=(fdim(b,e))/(exp(e));
e=(fdim(d,b))*(floor(a));
b=(log10(e))*(pow(e,a));
}
while(isgreaterequal(c,e)){
c=(atan2(b,e))*(atan2(b,d));
e=(pow(e,e))-(floor(a));
d=(fdim(d,a))/(asin(c));
}
}