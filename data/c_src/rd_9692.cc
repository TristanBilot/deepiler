#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan2(c,a);
e=fmin(g,a);
g=(fmin(b,f))+(sqrt(b));
d=(fdim(c,a))-(acos(g));
f=(atan2(d,f))+(pow(d,b));
while(islessgreater(d,e)){
c=(sqrt(a))*(sqrt(e));
f=pow(f,g);
g=tan(b);
}
while(islessgreater(a,a)){
e=fmax(a,c);
a=pow(a,c);
d=(cos(b))*(pow(b,d));
c=pow(g,g);
a=log(e);
}
}