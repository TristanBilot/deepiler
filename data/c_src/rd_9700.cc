#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(asin(a))/(fmax(a,d));
a=(fdim(e,c))/(floor(a));
while(isless(e,e)){
d=cos(c);
b=(pow(c,d))/(fdim(b,c));
a=(fmin(a,d))+(ceil(c));
d=exp(a);
}
a=fmin(b,a);
e=acos(e);
a=(log10(e))/(atan(a));
a=exp(c);
c=(tan(c))+(cos(a));
}