#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(b,a);
f=fmax(g,d);
g=pow(b,h);
while(isless(c,g)){
g=(floor(a))+(cos(a));
e=(fdim(g,d))/(fdim(a,b));
d=tan(b);
g=fdim(e,a);
}
if(isgreaterequal(a,a)){
a=fdim(a,c);
h=sin(h);
f=(sqrt(f))*(sqrt(g));
}
}