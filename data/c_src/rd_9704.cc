#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(e,e))+(atan2(d,g));
b=pow(d,f);
g=(ceil(f))-(pow(f,h));
f=(fmax(b,b))*(exp(d));
if(isless(h,f)){
h=(sin(g))*(pow(e,d));
c=(fdim(h,g))/(ceil(e));
c=fmin(c,h);
g=(atan2(g,d))+(cos(a));
c=(fmin(e,e))/(ceil(e));
}
else{
h=fmin(e,a);
g=tan(a);
}
if(islessgreater(h,a)){
f=fdim(e,d);
g=atan(a);
c=ceil(h);
h=(pow(b,g))-(pow(f,h));
}
else{
b=(acos(c))*(fmax(c,e));
g=atan(e);
c=fdim(f,h);
}
}