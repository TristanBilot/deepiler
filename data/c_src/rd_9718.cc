#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(cos(a))-(exp(b));
d=(sin(d))*(fmax(e,e));
e=fmax(d,c);
if(isgreaterequal(c,e)){
a=(floor(b))/(pow(a,a));
d=(fdim(e,d))*(pow(c,c));
}
else{
b=ceil(c);
b=(pow(c,a))/(exp(a));
d=fdim(d,b);
}
c=ceil(b);
a=cos(c);
c=fdim(c,a);
}