#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(tan(d))-(fmax(b,a));
c=fmin(e,b);
e=(log(e))*(sqrt(c));
d=(pow(c,a))/(tan(b));
a=fmax(c,c);
c=(cos(d))-(fdim(e,c));
e=sin(c);
b=(sin(e))+(floor(b));
a=(asin(d))/(fmax(c,b));
d=fmax(e,c);
a=tan(a);
d=fmin(d,c);
d=ceil(c);
}