#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(c,b);
a=fmax(c,a);
while(islessequal(b,a)){
d=log10(c);
c=pow(e,c);
d=(atan2(e,d))*(sin(e));
e=(fmin(d,c))-(fdim(b,b));
}
while(islessgreater(c,e)){
c=(atan2(a,c))-(fmin(c,d));
c=(fmax(c,c))-(fdim(d,a));
}
}