#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=exp(b);
g=(log(c))-(fdim(c,b));
b=fmax(a,c);
c=fmax(g,b);
a=fdim(g,b);
while(islessequal(g,f)){
d=(log10(d))*(fdim(f,a));
a=(fmax(f,b))/(sin(b));
c=(log10(f))-(cos(g));
a=(fmax(a,d))/(floor(c));
}
}