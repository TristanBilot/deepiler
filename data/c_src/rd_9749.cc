#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(sqrt(b))/(fmax(d,c));
c=(atan2(h,d))/(cos(b));
c=(floor(b))-(cos(a));
h=fdim(a,a);
d=(fdim(h,b))-(asin(f));
if(islessequal(c,h)){
d=fmax(g,d);
e=fmax(d,d);
}
else{
c=tan(c);
b=(floor(d))/(sqrt(b));
c=tan(b);
f=(sqrt(b))*(cos(f));
e=fmax(a,c);
}
b=fdim(a,b);
d=log(h);
b=(atan2(c,c))*(atan2(c,g));
}