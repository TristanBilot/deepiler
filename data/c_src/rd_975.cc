#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=acos(a);
d=(fdim(c,d))*(fmax(d,b));
d=fdim(a,c);
e=fdim(b,c);
c=(fmin(e,a))*(acos(a));
if(isless(e,e)){
b=(exp(a))/(exp(a));
a=sqrt(d);
}
else{
d=(fmin(c,c))*(log10(c));
e=(floor(c))+(ceil(b));
}
if(isgreaterequal(a,d)){
e=fmax(b,d);
a=log10(a);
c=(cos(d))-(ceil(d));
e=pow(c,b);
}
}