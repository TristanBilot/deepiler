#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(floor(d))-(atan2(b,a));
b=(asin(d))/(fmin(a,b));
b=(pow(d,c))/(tan(e));
a=(fdim(f,b))-(pow(d,b));
f=pow(b,d);
if(islessequal(b,c)){
e=atan2(e,f);
a=(asin(b))*(pow(c,f));
a=(atan2(b,d))/(pow(f,b));
}
else{
e=(ceil(a))+(log(c));
b=ceil(f);
}
c=(log(b))/(fmin(e,c));
d=(atan2(f,b))*(fmin(b,a));
a=pow(e,b);
a=(log10(b))/(acos(e));
a=(log10(a))+(log10(a));
}