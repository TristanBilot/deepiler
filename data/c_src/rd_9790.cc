#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(e,d);
d=tan(e);
a=(log(a))*(fmax(a,e));
c=pow(d,d);
c=(log10(d))+(floor(d));
if(isgreaterequal(e,a)){
c=(atan2(a,e))+(sqrt(e));
b=asin(b);
c=(fmin(d,b))-(fdim(b,e));
a=(pow(a,a))+(fmin(c,e));
}
if(islessequal(d,a)){
b=(fmin(d,a))*(pow(c,b));
a=fdim(c,b);
b=log(b);
}
else{
a=asin(c);
c=fmin(e,d);
b=fmax(d,c);
a=fmin(d,d);
}
}