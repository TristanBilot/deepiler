#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmax(b,b);
g=pow(d,g);
if(islessequal(e,b)){
a=fmax(f,g);
g=(pow(a,b))+(pow(c,f));
c=(fdim(e,e))*(cos(c));
}
g=(atan2(e,c))*(log10(e));
g=(fdim(b,b))-(sin(b));
}