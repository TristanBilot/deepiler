#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(e,d);
a=(fmax(d,c))/(fmax(a,e));
e=exp(a);
b=(atan2(b,b))*(fdim(a,d));
if(isless(e,c)){
a=log(e);
c=(pow(c,d))/(acos(f));
a=(exp(d))-(sin(d));
a=atan2(f,b);
}
else{
e=tan(e);
b=(asin(f))*(fmin(a,c));
c=acos(a);
f=cos(a);
}
if(islessgreater(b,b)){
b=fmin(d,a);
f=atan2(d,f);
c=log(e);
c=(fmin(b,f))-(fmin(b,e));
}
else{
d=(sin(d))/(fmax(b,b));
a=(atan2(c,c))*(atan(a));
c=sqrt(e);
}
}