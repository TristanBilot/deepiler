#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=sin(c);
e=(log(b))*(pow(a,g));
f=(sqrt(d))/(atan2(b,f));
g=fmin(c,e);
c=pow(f,g);
d=cos(d);
while(isless(a,d)){
f=(fmin(a,g))/(log(b));
a=(pow(f,c))+(pow(a,a));
e=fdim(d,d);
}
}