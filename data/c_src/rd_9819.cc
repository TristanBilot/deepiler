#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(d,e);
d=fmin(c,f);
a=fdim(b,e);
d=(atan(b))/(atan2(c,d));
while(islessequal(b,c)){
a=floor(f);
d=fdim(b,a);
}
e=(atan(c))/(fmin(a,d));
b=fmax(a,c);
a=fmin(b,e);
b=fmax(e,a);
}