#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(tan(a))/(atan(b));
a=atan(e);
f=(fmax(e,g))-(fmax(g,e));
f=fdim(b,g);
if(islessgreater(a,f)){
c=(atan2(e,f))*(fdim(b,e));
e=asin(e);
}
else{
c=sqrt(g);
g=sqrt(c);
d=fdim(c,e);
a=fmin(g,c);
}
if(islessgreater(g,d)){
g=fmin(c,b);
b=log(c);
f=floor(a);
g=log(f);
}
}