#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(b);
c=asin(a);
d=fdim(e,c);
b=(fmin(c,b))+(log(b));
e=floor(c);
e=(floor(e))+(fmax(a,b));
d=log10(a);
c=log10(e);
d=(acos(e))*(fmax(e,b));
b=atan2(d,b);
}