#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan2(a,e);
a=(sin(d))*(fdim(d,a));
e=fmin(f,g);
f=(cos(d))+(exp(e));
d=pow(c,c);
while(islessequal(f,g)){
c=(sin(e))+(atan2(b,b));
c=asin(c);
d=fdim(c,b);
}
c=pow(c,c);
c=(log10(c))/(cos(c));
d=atan(d);
d=(pow(c,a))+(ceil(g));
b=fmin(a,c);
}