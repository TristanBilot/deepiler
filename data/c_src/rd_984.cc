#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmin(f,c))-(asin(b));
h=fmin(g,b);
a=tan(a);
g=(log(b))*(atan(d));
if(islessequal(b,c)){
b=(fmin(e,b))-(sin(h));
g=fmin(a,e);
g=(pow(e,e))+(fmax(h,g));
h=exp(g);
h=asin(a);
}
h=(exp(a))+(acos(h));
g=atan2(b,b);
a=(fmax(f,c))-(acos(b));
c=atan2(g,f);
d=(fdim(d,h))*(pow(a,f));
}