#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(log10(c))/(atan2(f,b));
c=(atan2(e,e))-(log(c));
if(isless(b,e)){
c=atan(f);
b=(fmax(c,a))/(log10(b));
a=(fdim(a,b))*(log10(c));
d=sin(a);
e=(fmin(c,c))/(log10(b));
}
if(isgreaterequal(c,c)){
a=(fdim(c,d))/(exp(e));
f=(atan(b))*(asin(b));
d=(asin(e))+(sqrt(b));
e=tan(f);
}
else{
f=pow(d,b);
c=tan(a);
}
}