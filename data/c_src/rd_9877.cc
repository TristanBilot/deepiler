#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(b);
d=atan2(c,d);
while(islessequal(c,d)){
a=(log(b))*(fmax(a,c));
d=atan(a);
c=(sqrt(e))/(acos(d));
a=asin(a);
}
while(isless(b,d)){
b=fdim(c,d);
e=(floor(c))+(ceil(b));
d=(pow(d,d))+(fmax(b,d));
a=fdim(b,d);
}
}