#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=cos(e);
e=(tan(e))/(atan2(e,d));
c=fmin(b,a);
while(islessequal(a,d)){
c=tan(f);
f=(atan2(c,c))*(asin(a));
b=atan2(d,a);
f=(fdim(f,b))/(ceil(b));
b=(tan(e))/(fmin(a,f));
}
if(islessequal(a,b)){
a=(log(d))-(atan2(c,f));
e=(cos(a))+(fmax(d,e));
f=sqrt(b);
d=fmax(a,d);
b=(atan(b))-(floor(b));
}
else{
a=exp(e);
d=fmax(e,c);
f=(log10(e))+(sin(f));
f=(pow(d,a))/(sin(e));
e=(fdim(d,b))/(pow(a,a));
}
}