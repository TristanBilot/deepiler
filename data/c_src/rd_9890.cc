#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=ceil(a);
a=atan2(a,a);
d=pow(b,c);
e=(floor(d))-(fmin(c,d));
d=(tan(b))+(acos(e));
while(isgreaterequal(d,a)){
f=(fmax(f,d))+(log(e));
f=pow(a,a);
b=sqrt(a);
c=atan2(f,b);
}
f=cos(b);
a=fmax(a,a);
f=(fmax(b,e))/(floor(c));
a=(fdim(a,a))+(exp(f));
d=floor(d);
}