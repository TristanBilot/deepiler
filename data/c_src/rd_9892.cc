#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(b,b);
d=(fdim(c,c))-(log(c));
f=floor(b);
b=sin(d);
if(islessgreater(c,c)){
d=cos(f);
a=sin(f);
c=log(e);
b=fdim(b,e);
}
if(islessgreater(a,d)){
f=(fmin(a,f))/(fmax(c,d));
e=(log(e))+(pow(a,c));
b=log10(a);
}
}