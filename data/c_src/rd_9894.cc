#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log10(e);
c=atan2(d,e);
b=(atan2(e,c))-(atan(c));
d=fmin(b,d);
d=(atan(b))-(fdim(a,e));
if(isless(e,a)){
b=(sin(a))+(log(c));
c=atan2(a,d);
}
c=ceil(b);
b=(fdim(c,b))/(fmin(e,b));
}