#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(a,b))+(fdim(c,e));
d=acos(d);
a=pow(c,a);
a=(tan(a))*(fmin(a,d));
while(islessgreater(a,c)){
c=(pow(e,a))-(pow(a,e));
d=fdim(d,a);
c=asin(d);
e=sqrt(e);
}
while(isless(d,e)){
d=ceil(b);
c=(cos(a))*(pow(e,d));
d=fmin(a,d);
e=(asin(c))*(cos(a));
b=atan2(e,a);
}
}