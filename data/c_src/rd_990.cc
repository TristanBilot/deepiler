#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(c,d))+(fmax(a,d));
a=(atan(a))+(pow(g,d));
f=acos(g);
f=sqrt(e);
e=fmin(c,f);
if(isgreaterequal(c,b)){
g=atan2(g,g);
e=fmax(c,a);
g=pow(d,a);
}
else{
e=(tan(g))*(fmax(d,f));
g=fmin(d,f);
c=(atan2(f,d))-(fdim(b,g));
}
a=floor(g);
g=fmax(f,g);
}