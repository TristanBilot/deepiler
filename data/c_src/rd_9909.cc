#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(sin(e))-(tan(b));
a=floor(h);
h=atan2(f,b);
if(islessgreater(g,e)){
h=(atan2(a,e))*(fmin(f,b));
g=(atan2(h,d))*(fmax(c,a));
e=tan(f);
c=(pow(a,b))+(acos(e));
}
else{
b=(fdim(h,c))+(fmin(h,h));
d=(cos(a))*(log(e));
f=floor(b);
h=pow(e,b);
}
e=(asin(b))/(pow(e,d));
h=(fdim(h,g))+(exp(c));
h=tan(b);
h=(fdim(a,h))*(acos(f));
g=(floor(g))/(tan(h));
}