#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(c);
d=pow(b,d);
d=pow(a,e);
c=acos(d);
b=(fmin(a,d))/(acos(b));
d=(log(a))*(sin(d));
d=sin(a);
if(isless(a,a)){
a=(fdim(b,a))-(atan2(b,a));
c=(fdim(b,e))/(pow(c,e));
b=(exp(a))+(floor(b));
a=pow(a,a);
}
}