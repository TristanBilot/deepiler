#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=log10(e);
e=(log(f))+(pow(f,b));
e=atan2(d,d);
c=(floor(c))+(log10(a));
d=fmin(e,a);
if(isgreaterequal(b,d)){
f=(fdim(f,e))/(pow(c,f));
e=(log10(b))*(fmax(f,b));
e=atan2(b,c);
e=atan(e);
}
else{
e=(pow(d,f))*(fmax(b,f));
b=atan2(f,b);
d=sqrt(e);
}
while(islessgreater(a,d)){
c=exp(d);
d=(floor(e))+(atan2(d,d));
f=(exp(e))/(pow(c,c));
a=fdim(f,d);
}
}