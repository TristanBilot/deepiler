#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(b))/(atan2(a,c));
d=atan2(a,a);
d=pow(b,c);
b=pow(e,a);
d=sqrt(d);
d=(fmin(b,e))*(atan2(a,e));
d=tan(b);
d=(pow(c,d))*(fdim(a,e));
b=fmin(b,e);
b=(exp(c))/(atan(d));
b=atan(b);
}