#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(h);
a=ceil(h);
c=(pow(d,b))*(atan(b));
e=(atan2(c,a))-(sin(b));
g=(pow(f,e))-(fdim(h,g));
while(isgreaterequal(g,d)){
a=(fmin(b,g))/(fmax(d,a));
g=(floor(b))/(fmin(c,e));
a=(fmax(h,g))*(floor(e));
d=(pow(h,b))*(log10(e));
b=log(a);
}
if(isless(d,e)){
f=(fdim(g,a))-(fmin(c,h));
f=cos(b);
e=fmax(h,a);
}
else{
b=fdim(h,e);
d=(tan(e))-(cos(e));
}
}