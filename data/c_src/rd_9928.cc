#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(e,d);
d=pow(e,f);
d=(fmin(g,b))*(pow(e,d));
e=fdim(d,f);
c=(cos(b))*(fmin(h,d));
while(islessequal(b,g)){
a=(floor(g))/(fdim(f,a));
a=(tan(c))/(pow(c,f));
f=asin(e);
d=floor(b);
}
g=(sin(d))*(exp(a));
b=floor(g);
}