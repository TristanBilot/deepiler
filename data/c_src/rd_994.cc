#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sin(c);
g=(pow(e,e))-(pow(a,e));
c=(atan2(c,c))+(fmin(b,b));
f=(floor(b))+(exp(e));
if(islessequal(c,d)){
c=(fmin(f,d))*(fmax(b,g));
d=(acos(b))*(floor(e));
e=sqrt(e);
b=fdim(d,g);
f=(fmin(b,e))-(fmax(e,b));
}
a=fmax(c,a);
e=asin(g);
e=log(g);
f=(ceil(f))/(ceil(g));
f=fmin(a,f);
}