#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(f))+(exp(b));
a=(pow(c,c))-(fmin(a,d));
f=fmin(f,f);
c=fmax(c,e);
e=(exp(b))*(fmin(c,a));
if(islessgreater(f,e)){
e=(log10(a))*(fmin(e,d));
e=cos(b);
a=fmin(e,e);
d=sin(b);
}
else{
e=fdim(e,e);
a=(fdim(d,f))-(fmax(a,c));
e=(log(b))*(atan2(f,d));
}
if(islessgreater(c,b)){
e=atan2(f,e);
e=tan(d);
b=(sqrt(d))/(fmax(b,e));
}
else{
f=tan(a);
b=(atan2(f,f))+(floor(f));
d=(ceil(a))/(sqrt(b));
}
}