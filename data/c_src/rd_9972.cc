#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(b);
c=fdim(d,b);
e=ceil(c);
c=(fdim(a,d))*(pow(a,b));
while(islessequal(b,a)){
a=(fmax(e,c))/(atan(c));
a=(tan(e))+(asin(a));
b=(pow(e,a))+(sin(b));
a=(fmin(d,b))+(fdim(e,b));
}
e=pow(e,a);
c=(fmin(d,a))+(fmin(b,d));
a=(log10(a))*(pow(d,a));
b=(floor(e))+(fmax(b,e));
}