#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=floor(f);
a=fmax(e,a);
while(isless(a,g)){
e=atan2(b,a);
f=(pow(a,e))-(fmax(d,e));
g=fmin(e,d);
}
if(isgreaterequal(g,c)){
g=(fmin(g,d))/(ceil(f));
a=atan2(e,d);
d=log(e);
f=log(a);
c=atan2(d,e);
}
else{
b=(log10(d))+(atan2(c,e));
e=floor(a);
}
}