#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(floor(c))-(exp(b));
a=log10(g);
while(islessequal(g,d)){
f=(asin(e))*(ceil(a));
b=(ceil(f))+(fmax(d,b));
d=floor(d);
b=(acos(d))+(fmin(f,a));
}
while(isless(a,b)){
g=exp(f);
e=pow(d,e);
e=(sqrt(b))*(asin(a));
b=(fmin(d,d))*(log10(a));
}
}