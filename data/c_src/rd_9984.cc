#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(g,b);
e=asin(c);
a=(floor(h))*(pow(e,e));
e=(fmax(d,a))*(ceil(g));
h=fmin(e,a);
e=(exp(b))+(cos(b));
while(islessequal(a,f)){
g=(fmax(e,a))+(fdim(h,c));
d=(floor(a))*(ceil(d));
e=(pow(g,a))/(fdim(d,c));
h=cos(b);
c=(fmax(b,a))*(floor(a));
}
}