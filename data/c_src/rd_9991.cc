#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(c);
c=(sqrt(e))/(sin(c));
b=(cos(e))*(asin(a));
d=fmin(d,c);
a=fmax(e,c);
b=fmax(e,e);
e=cos(a);
c=fmax(e,c);
a=pow(c,c);
}