#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(h,b);
g=(pow(a,d))*(fmax(g,h));
f=atan2(d,e);
d=fmax(h,f);
while(isless(g,f)){
a=(ceil(c))/(fmax(g,d));
f=(pow(f,e))-(atan2(e,f));
f=(sqrt(a))-(tan(a));
}
a=atan2(f,f);
c=tan(a);
}