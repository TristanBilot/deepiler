#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmax(d,d);
f=acos(e);
c=(log10(c))/(floor(c));
f=(fmin(d,b))-(sqrt(a));
b=fmax(b,b);
c=floor(c);
if(isless(b,c)){
a=atan2(c,f);
b=fmin(b,b);
}
}