#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(d,b))+(log10(d));
b=log(a);
while(islessgreater(c,f)){
a=(fmin(g,g))*(acos(f));
c=fmax(a,g);
}
if(isless(d,a)){
b=atan2(b,f);
f=(pow(d,f))+(sin(e));
e=(asin(g))+(pow(e,f));
f=(fdim(d,a))+(fdim(g,e));
}
else{
c=(fmax(a,g))/(sin(f));
g=pow(e,f);
}
}