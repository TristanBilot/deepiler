#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(a,b);
b=atan2(d,d);
e=asin(b);
c=(pow(f,d))/(fmin(a,a));
c=(asin(g))*(fmax(g,g));
b=(pow(b,g))/(pow(d,f));
b=fmax(e,g);
g=pow(a,a);
if(isgreaterequal(f,b)){
g=atan2(f,b);
e=fdim(d,d);
g=exp(b);
b=(atan2(f,f))/(fmax(d,d));
a=fmax(f,d);
}
else{
b=cos(a);
b=(fmax(d,b))+(cos(g));
f=(atan(e))/(atan2(a,a));
c=atan(c);
}
}