#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(cos(c))-(acos(a));
c=(log(b))/(acos(e));
f=fdim(d,a);
if(islessgreater(e,f)){
c=fdim(c,b);
a=sin(d);
f=(log(b))*(cos(c));
f=acos(e);
f=asin(c);
}
else{
e=atan2(b,a);
b=(atan2(a,d))+(fdim(d,b));
b=(atan2(e,f))-(sqrt(f));
}
if(islessequal(d,e)){
d=(pow(a,d))/(tan(a));
a=pow(b,e);
f=(log10(a))/(exp(a));
}
else{
a=(fmax(a,f))+(acos(c));
f=(asin(c))/(tan(d));
}
}