#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(d);
a=(sqrt(d))*(fdim(b,b));
e=(asin(e))/(atan(c));
g=(fdim(e,h))+(floor(a));
c=(fdim(c,e))/(pow(h,g));
while(islessequal(h,f)){
f=sin(f);
h=atan2(d,c);
}
if(isless(f,e)){
h=(log10(g))/(fmin(g,b));
h=(fmin(a,e))+(fmin(h,g));
c=(asin(g))*(fmin(h,h));
d=(log10(b))-(asin(g));
g=atan2(h,f);
}
else{
d=(atan2(a,c))-(atan(f));
b=(pow(a,a))+(tan(c));
}
}