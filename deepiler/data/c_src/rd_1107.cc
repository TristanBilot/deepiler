#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(exp(a))-(sin(e));
e=atan(f);
g=cos(b);
d=(acos(b))*(acos(d));
e=atan2(f,e);
c=(floor(b))*(log10(a));
c=(atan(f))-(fmin(f,d));
b=pow(c,c);
while(islessequal(e,c)){
g=(atan(a))+(fmax(c,e));
c=(log10(f))*(fmax(d,f));
g=acos(g);
a=acos(a);
}
}