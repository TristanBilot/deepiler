#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan(d);
a=(cos(b))+(atan2(e,d));
f=(fmax(c,a))-(floor(a));
if(islessequal(a,a)){
b=(exp(a))/(sin(d));
d=(pow(c,c))-(atan2(b,c));
}
else{
d=fmax(f,f);
d=(fmax(f,d))+(fmax(a,b));
}
while(isless(d,a)){
d=log10(b);
e=pow(c,a);
e=cos(d);
}
}