#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log(b);
b=(atan2(d,d))/(cos(c));
f=(acos(g))/(exp(g));
c=(asin(g))*(fmax(d,d));
g=floor(f);
b=tan(g);
if(isless(c,g)){
f=(cos(g))+(pow(g,g));
d=(log(e))/(log10(b));
d=(exp(g))+(atan2(d,f));
a=(atan(a))/(floor(d));
f=sin(g);
}
}