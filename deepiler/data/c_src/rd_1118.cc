#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fdim(f,b))/(log10(d));
e=(sin(a))*(log(d));
d=sin(g);
g=(ceil(e))+(fdim(e,g));
e=(log(b))/(acos(a));
while(isless(b,e)){
g=atan2(c,a);
c=(cos(d))-(fdim(c,e));
c=(pow(a,a))*(fmax(c,d));
}
e=pow(g,a);
f=(sin(a))-(fmax(g,g));
e=(pow(e,g))+(floor(e));
}