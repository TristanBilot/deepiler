#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(f,d);
e=(sin(c))-(pow(h,b));
f=(exp(c))+(atan2(b,g));
h=atan(h);
d=fmin(d,c);
d=pow(f,c);
e=(fdim(h,e))*(pow(f,d));
f=floor(f);
h=atan2(d,d);
a=(floor(d))-(sqrt(b));
g=pow(h,h);
d=(atan2(a,h))+(cos(h));
}