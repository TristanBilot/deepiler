#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=sqrt(b);
f=fmax(f,b);
a=acos(e);
c=(asin(g))*(fmax(f,d));
d=exp(d);
a=atan2(e,a);
while(islessgreater(d,a)){
e=ceil(f);
a=log(e);
a=(sqrt(f))*(fmin(e,b));
f=fmax(f,c);
}
}