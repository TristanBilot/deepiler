#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(asin(c))*(pow(b,c));
e=fdim(f,f);
g=acos(b);
e=pow(a,c);
c=log(a);
while(islessgreater(d,g)){
b=(fmin(c,b))-(log(e));
b=(fmin(f,e))*(fdim(a,b));
b=fmin(g,a);
a=(ceil(c))+(fdim(g,a));
c=fmax(d,g);
}
}