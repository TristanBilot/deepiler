#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=floor(a);
a=atan2(e,a);
b=fmax(d,a);
e=(fdim(b,a))-(sin(e));
c=(fdim(b,b))-(fdim(a,c));
while(isless(e,d)){
c=asin(d);
e=(exp(a))/(cos(a));
a=(tan(b))/(atan2(b,b));
a=fdim(e,c);
c=fmax(c,b);
}
while(isless(c,d)){
d=(log10(b))+(atan2(c,d));
d=fmin(e,a);
}
}