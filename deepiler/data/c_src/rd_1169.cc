#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,c))*(sqrt(b));
a=sqrt(c);
while(islessequal(c,b)){
e=(ceil(c))/(log(b));
d=(fdim(b,b))/(fdim(e,d));
c=(acos(a))-(ceil(d));
e=atan(a);
}
a=floor(a);
b=pow(a,d);
b=(pow(c,b))*(atan(a));
c=(atan2(d,b))+(atan2(d,b));
}