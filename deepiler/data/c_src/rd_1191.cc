#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,b))-(atan(b));
b=(pow(d,c))+(acos(a));
b=pow(c,e);
b=(atan2(d,d))+(atan2(b,a));
b=(atan2(d,b))+(fmin(c,b));
if(isless(d,b)){
c=sin(b);
d=(log(e))*(fmax(e,a));
b=(fdim(c,c))-(log(c));
e=pow(b,e);
}
while(isgreaterequal(c,d)){
c=tan(d);
a=(asin(e))*(exp(a));
d=(asin(c))*(fdim(d,d));
a=asin(e);
}
}