#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(atan2(d,g))-(fmin(c,e));
a=pow(d,g);
while(isless(d,e)){
c=fmax(c,b);
d=(fmin(e,d))-(fdim(a,g));
}
e=(pow(c,a))*(atan2(d,g));
d=(pow(c,g))+(pow(b,e));
}