#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(a,d))/(atan2(a,b));
a=(tan(a))+(sqrt(a));
e=cos(e);
c=ceil(c);
c=pow(e,b);
while(isless(e,a)){
b=(pow(e,e))*(floor(d));
d=pow(b,c);
}
if(islessequal(e,d)){
d=(atan(e))*(floor(a));
a=fdim(d,d);
}
else{
d=(acos(e))/(atan2(b,d));
d=fdim(a,b);
e=(fdim(b,b))*(asin(b));
}
}