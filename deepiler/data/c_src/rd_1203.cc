#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fdim(b,g);
c=(pow(b,d))*(pow(a,f));
if(isgreaterequal(b,g)){
g=fmax(b,b);
e=(sqrt(f))+(tan(b));
}
else{
c=floor(d);
g=(atan2(d,g))+(exp(b));
}
if(isless(a,d)){
c=floor(d);
f=fdim(g,e);
}
}