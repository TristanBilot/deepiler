#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(b);
d=atan(a);
g=(fdim(c,e))-(pow(c,g));
c=atan2(e,h);
h=asin(f);
while(isgreaterequal(f,c)){
f=fmin(b,f);
h=asin(e);
f=(fdim(h,d))/(fmax(h,g));
}
f=(atan2(a,d))-(asin(e));
g=log10(c);
e=fmin(h,c);
}