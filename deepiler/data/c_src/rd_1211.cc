#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=acos(d);
e=atan2(d,d);
d=fdim(b,b);
e=(fdim(c,b))+(pow(d,d));
d=(cos(b))+(atan(e));
d=pow(e,e);
a=sqrt(c);
d=atan2(c,e);
while(islessgreater(b,a)){
c=(atan(d))+(fmax(a,b));
a=(pow(b,e))+(log10(c));
e=atan2(d,b);
c=(pow(b,d))/(fmax(a,d));
}
}