#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(g,a);
f=exp(h);
a=fmax(c,b);
f=(fdim(c,f))*(sin(a));
g=(pow(e,g))-(atan2(f,h));
if(islessequal(g,b)){
f=(floor(c))+(floor(c));
g=(exp(d))*(fmax(e,d));
}
c=(fmin(a,f))-(fmax(e,b));
e=(fmax(c,e))+(sqrt(e));
f=(fmin(g,g))-(exp(b));
d=atan2(d,e);
}