#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=log10(c);
c=(pow(e,a))-(fmin(g,c));
d=(pow(e,b))-(tan(g));
b=(fdim(f,b))+(fmax(d,b));
if(islessequal(e,a)){
e=(floor(b))/(atan2(e,c));
d=(pow(c,b))+(fdim(e,d));
c=(fmax(b,d))+(atan2(e,e));
b=(atan(e))/(fmax(e,d));
e=(atan2(d,a))/(sin(b));
}
while(isless(c,b)){
f=(sin(b))-(pow(d,c));
d=(log10(c))-(asin(g));
f=exp(d);
g=atan(a);
e=(cos(f))-(sqrt(d));
}
}