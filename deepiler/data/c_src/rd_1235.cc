#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(b,c);
d=(pow(d,a))/(tan(e));
a=cos(e);
d=atan2(c,e);
while(islessgreater(b,b)){
e=(ceil(b))/(atan(a));
c=fmin(a,c);
}
if(isless(b,e)){
e=(fmax(b,a))*(acos(a));
b=fmin(b,b);
c=(acos(d))-(asin(b));
e=ceil(e);
}
}