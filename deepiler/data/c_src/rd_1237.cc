#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fdim(g,b))-(fmin(g,f));
g=(sin(e))-(acos(g));
g=(acos(f))/(pow(a,b));
f=(cos(b))-(pow(a,a));
a=(acos(b))-(acos(b));
if(isgreaterequal(f,e)){
f=(fmax(a,a))*(pow(d,c));
a=(pow(a,f))*(exp(g));
}
else{
b=floor(d);
a=pow(d,f);
d=sin(g);
f=pow(a,c);
}
if(isless(a,f)){
g=(cos(d))/(cos(f));
e=fmax(g,g);
}
}