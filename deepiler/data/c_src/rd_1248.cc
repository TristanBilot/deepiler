#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(pow(g,b))+(floor(f));
a=(fmax(c,g))*(fmin(e,f));
f=(exp(f))/(pow(g,c));
d=(atan2(f,d))-(exp(f));
c=pow(a,c);
if(isgreaterequal(e,b)){
f=(log10(f))/(fdim(c,c));
e=(fmax(e,e))+(acos(c));
}
else{
f=(fmin(g,e))*(fdim(d,e));
c=exp(a);
g=fmax(b,f);
d=(tan(e))+(atan2(a,b));
}
a=fdim(b,a);
c=(fmax(c,e))/(atan(g));
d=(fdim(f,a))*(fmin(e,e));
c=log(g);
f=sqrt(d);
}