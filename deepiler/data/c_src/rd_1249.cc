#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(h);
f=(fdim(g,f))*(pow(h,c));
h=(pow(g,b))+(fmax(c,h));
g=(fmax(a,a))/(fdim(g,h));
g=(fmin(f,a))-(fdim(d,h));
h=fdim(e,b);
a=tan(c);
c=fmax(d,g);
e=(acos(e))*(sqrt(a));
c=ceil(b);
c=(ceil(d))/(log(c));
}