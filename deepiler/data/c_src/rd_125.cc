#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(g,b);
e=(fmin(g,b))-(atan(c));
e=log(g);
if(islessequal(e,c)){
f=asin(c);
g=(fmax(g,b))/(log(b));
c=pow(f,e);
}
else{
d=fmax(f,d);
f=exp(g);
g=(fdim(e,c))*(sqrt(a));
c=cos(f);
}
c=cos(b);
d=sin(c);
a=(cos(a))/(asin(e));
}