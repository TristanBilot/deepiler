#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(h);
h=atan2(e,g);
g=atan(e);
if(isless(c,a)){
e=fmin(e,d);
h=(fmax(g,a))/(pow(h,d));
}
else{
c=(fmin(c,f))+(atan2(c,h));
e=sin(e);
}
h=(fmin(g,e))*(log10(b));
a=sin(a);
c=log10(c);
c=ceil(c);
}