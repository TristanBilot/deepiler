#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(pow(a,c))*(atan(c));
d=(tan(a))*(atan2(b,c));
e=log(a);
b=atan2(d,a);
if(islessequal(c,f)){
c=fdim(c,c);
f=(asin(d))/(acos(f));
}
else{
c=(atan2(f,b))+(atan2(d,a));
f=(fmax(d,f))/(fdim(c,c));
e=(fmax(a,b))/(fmin(d,a));
c=pow(f,f);
}
if(islessequal(d,c)){
b=(exp(e))/(floor(c));
c=(atan2(e,c))*(exp(a));
a=exp(f);
}
else{
e=pow(f,d);
b=(pow(a,e))/(fmax(b,a));
}
}