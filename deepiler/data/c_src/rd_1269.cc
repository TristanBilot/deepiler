#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(b,b))-(fmin(b,e));
e=(exp(a))/(fmin(b,f));
c=fmin(d,f);
c=fdim(f,d);
b=(fmin(d,b))/(atan(d));
e=(log10(c))/(log(b));
f=(fdim(f,f))+(ceil(a));
if(isless(e,f)){
f=fmax(c,e);
c=log(e);
}
else{
d=(atan2(a,f))/(fmin(d,e));
b=(atan2(a,a))+(pow(a,f));
}
}