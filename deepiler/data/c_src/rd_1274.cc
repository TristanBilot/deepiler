#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(e,c);
d=(fmin(a,d))-(ceil(b));
while(isless(a,b)){
e=(pow(f,e))*(ceil(c));
c=(floor(e))/(fmin(b,b));
}
if(isless(e,c)){
d=log(e);
f=floor(f);
}
}