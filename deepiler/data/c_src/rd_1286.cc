#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=acos(d);
f=(asin(d))*(atan2(g,e));
b=fmin(c,d);
g=(sin(d))-(atan2(a,c));
f=(fmin(g,d))/(exp(d));
if(islessgreater(g,f)){
f=(fmax(g,g))/(fmax(g,f));
e=(acos(f))-(ceil(c));
d=(acos(b))/(pow(f,a));
b=atan2(b,a);
}
if(islessgreater(a,d)){
a=(asin(e))*(floor(g));
g=acos(b);
b=(sqrt(g))/(fdim(g,g));
d=sin(d);
}
else{
b=sqrt(c);
c=fmax(e,b);
}
}