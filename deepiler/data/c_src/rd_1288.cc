#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(e,d);
e=(atan2(f,c))+(fdim(c,d));
f=(exp(a))-(pow(f,e));
d=(pow(e,f))+(fmax(f,c));
f=pow(b,d);
if(isless(d,e)){
e=cos(a);
c=(floor(f))-(fmin(d,c));
}
}