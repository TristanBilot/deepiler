#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(b);
e=ceil(d);
if(isless(d,c)){
a=pow(a,a);
a=atan(c);
b=(pow(c,d))-(fmin(e,a));
c=(sin(c))*(log(b));
e=(pow(e,b))+(sqrt(b));
}
if(isgreaterequal(a,e)){
b=(exp(d))-(exp(c));
b=(exp(d))+(ceil(d));
c=fmax(c,a);
}
}