#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=cos(c);
c=(acos(d))/(exp(c));
if(isgreaterequal(a,b)){
b=fdim(e,a);
b=fdim(c,e);
d=fdim(a,e);
a=(sqrt(d))*(fmax(e,b));
a=(pow(d,b))+(fmin(a,d));
}
else{
c=log(d);
a=(sqrt(d))/(atan(c));
}
if(islessequal(a,d)){
a=(tan(b))-(fdim(b,c));
b=fdim(a,e);
d=floor(e);
e=(exp(b))*(fdim(e,c));
a=(floor(c))-(asin(e));
}
}