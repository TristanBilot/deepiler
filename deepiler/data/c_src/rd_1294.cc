#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log10(c))-(exp(b));
e=(cos(a))+(fmin(e,c));
if(islessgreater(g,b)){
c=exp(f);
e=exp(e);
g=fmin(d,c);
}
else{
g=(log10(d))*(pow(b,e));
g=pow(a,a);
f=(fdim(a,c))*(tan(f));
}
f=(log(a))-(atan2(d,b));
c=fmin(e,b);
}