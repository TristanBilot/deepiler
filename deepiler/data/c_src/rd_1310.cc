#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fdim(f,b))*(log(a));
c=pow(g,c);
d=atan2(f,b);
f=asin(d);
f=(asin(g))+(pow(e,b));
if(isgreaterequal(c,c)){
b=(log(d))-(fdim(a,a));
g=fmax(g,a);
b=exp(d);
}
else{
e=(fdim(c,d))-(sqrt(b));
a=(log10(a))+(exp(c));
c=pow(e,b);
}
}