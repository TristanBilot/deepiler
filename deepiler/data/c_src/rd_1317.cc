#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(exp(d))/(floor(g));
d=exp(f);
e=fmax(e,g);
b=floor(f);
a=(sin(g))/(fmin(g,d));
while(islessequal(f,d)){
f=(pow(f,e))+(fmax(a,d));
f=(tan(e))*(tan(g));
f=(exp(f))*(fmax(g,g));
c=pow(d,f);
b=(fmax(d,a))/(pow(b,e));
}
}