#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan(e))/(tan(e));
b=pow(d,b);
d=fdim(c,c);
b=(ceil(d))-(fdim(d,c));
a=fdim(b,e);
if(islessequal(d,b)){
d=sin(d);
d=(exp(a))+(atan(b));
}
else{
d=(tan(c))*(log(b));
c=fmin(c,d);
d=(fmin(a,b))+(atan2(a,d));
e=floor(b);
}
if(islessgreater(c,c)){
c=exp(e);
a=tan(b);
a=(fmax(c,e))-(atan(c));
}
}