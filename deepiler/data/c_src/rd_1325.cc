#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log10(a))*(log(d));
e=(cos(a))*(pow(e,e));
while(islessequal(d,e)){
d=tan(a);
e=(log(d))/(sqrt(b));
c=(fdim(b,b))-(fmin(a,e));
b=fmax(c,c);
c=pow(e,d);
}
if(isless(c,e)){
b=(atan2(d,d))+(fmax(c,c));
b=(log(e))+(fmax(d,a));
c=(fmax(c,d))/(atan2(d,c));
b=(pow(d,e))-(sin(c));
d=(acos(b))+(fmax(a,b));
}
else{
a=(log(d))*(tan(e));
a=(fmax(a,b))-(log10(c));
}
}