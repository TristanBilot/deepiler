#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=tan(e);
a=log10(e);
e=exp(b);
if(islessequal(f,c)){
c=fmax(b,a);
d=(floor(f))-(acos(f));
f=(acos(d))/(fdim(a,b));
e=(fdim(a,b))*(acos(b));
e=(atan2(d,c))/(atan(b));
}
if(islessgreater(e,f)){
f=ceil(a);
f=tan(b);
d=(exp(e))-(atan2(e,c));
c=atan2(c,c);
}
else{
f=atan(b);
e=atan2(f,e);
a=(tan(c))*(log(c));
}
}