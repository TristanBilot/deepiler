#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(a,c))/(atan(a));
e=asin(b);
f=(fdim(d,c))/(atan2(f,a));
f=(cos(a))/(log(d));
d=(fmax(f,d))/(fmax(f,d));
b=fdim(c,d);
a=(acos(a))+(fdim(b,b));
e=sqrt(c);
d=sin(c);
e=(atan2(e,b))+(asin(b));
c=floor(c);
}