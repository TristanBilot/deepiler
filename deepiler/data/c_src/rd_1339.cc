#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=cos(f);
g=(atan2(a,d))+(log10(f));
a=pow(e,d);
while(islessequal(f,e)){
f=(log(c))*(atan2(e,e));
g=(sin(g))/(fdim(b,b));
d=tan(d);
a=(pow(e,c))/(fmax(f,a));
e=(pow(f,e))/(fmin(d,g));
}
if(islessgreater(f,e)){
c=pow(d,e);
b=exp(a);
}
else{
f=(cos(d))/(fmin(b,e));
e=fdim(e,g);
d=fmax(g,e);
b=cos(f);
c=(fmin(e,e))+(pow(e,g));
}
}