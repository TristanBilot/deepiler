#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(b,a);
b=(ceil(e))*(fmin(a,e));
while(isless(f,b)){
e=log(g);
d=(pow(d,h))/(fmin(f,g));
}
b=(fmin(b,b))*(fmin(b,a));
b=(sqrt(h))*(fmax(d,f));
d=fmax(d,c);
b=(log(g))+(fmax(h,c));
}