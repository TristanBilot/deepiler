#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(c,c);
b=fmin(d,d);
c=(atan(b))*(fmin(e,d));
b=(exp(c))*(fmax(c,a));
a=(exp(d))*(fmax(e,c));
e=(acos(c))/(asin(c));
b=(fdim(b,c))/(sin(d));
b=(tan(e))+(fmin(b,e));
b=pow(a,b);
a=(sqrt(c))/(pow(c,b));
b=(fmin(c,a))*(atan2(e,e));
}