#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan(d))+(floor(c));
e=(cos(b))+(fmin(c,d));
c=(fdim(e,d))-(fmax(b,b));
while(islessequal(e,b)){
a=(fmin(d,b))-(atan(c));
c=(floor(d))-(fdim(c,e));
}
b=(exp(b))/(fdim(b,c));
b=pow(a,a);
}