#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(d,b))/(atan2(c,b));
e=pow(c,d);
c=(atan2(d,b))/(asin(d));
c=(fmax(b,b))*(fdim(a,d));
a=(exp(e))*(fmax(a,b));
e=pow(c,d);
e=(asin(c))-(tan(a));
a=fmax(b,d);
e=(pow(c,a))-(fmax(d,d));
a=(atan2(c,b))+(fmax(e,a));
}