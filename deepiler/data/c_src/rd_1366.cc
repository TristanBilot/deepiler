#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=exp(b);
d=exp(b);
f=floor(e);
c=floor(e);
g=(log(e))*(sin(g));
while(islessequal(b,g)){
e=(fdim(a,b))*(pow(b,e));
e=exp(f);
d=(sin(a))*(fmin(c,d));
b=atan2(e,a);
d=fmax(a,a);
}
while(islessgreater(b,d)){
e=(fdim(g,g))*(fmin(c,a));
c=fmax(f,e);
d=tan(a);
c=(acos(g))/(ceil(a));
f=fmax(a,d);
}
}