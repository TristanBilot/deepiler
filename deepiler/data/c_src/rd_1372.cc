#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(log10(a))*(tan(f));
a=fmax(e,d);
f=(atan2(f,c))+(fmin(a,e));
e=ceil(c);
c=asin(e);
if(islessequal(b,c)){
b=pow(c,f);
e=exp(a);
}
else{
f=(fmin(f,a))-(pow(b,f));
f=ceil(b);
f=fmin(d,c);
f=(atan(f))*(tan(f));
}
}