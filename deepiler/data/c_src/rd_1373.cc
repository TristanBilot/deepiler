#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(b,c))/(tan(a));
a=exp(b);
a=(log(c))-(asin(b));
while(islessequal(b,g)){
b=floor(b);
f=atan2(e,f);
e=fdim(g,a);
}
a=(pow(d,d))-(log(b));
a=atan2(a,g);
g=(fmax(a,g))*(log10(e));
}