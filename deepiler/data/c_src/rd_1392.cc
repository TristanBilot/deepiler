#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sin(b))*(log10(c));
e=pow(a,b);
b=fdim(f,a);
d=(pow(d,c))-(asin(d));
d=atan2(c,b);
if(isless(c,f)){
b=(ceil(e))/(atan2(f,d));
e=(sqrt(a))-(pow(b,b));
a=(cos(d))/(ceil(a));
}
else{
e=fmax(f,e);
b=exp(b);
e=(fmin(c,e))*(log(e));
a=log(c);
}
b=(atan2(e,f))*(sin(d));
e=sin(b);
}