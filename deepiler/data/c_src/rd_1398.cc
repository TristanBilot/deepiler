#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=asin(d);
f=(exp(d))-(fmax(f,b));
d=cos(a);
f=(floor(g))+(fdim(b,g));
g=(fmax(g,a))*(fdim(e,g));
c=pow(d,d);
f=(floor(b))/(sqrt(g));
c=log(e);
b=(atan2(b,g))+(log(e));
d=cos(e);
g=(acos(c))/(fdim(b,e));
f=ceil(d);
}