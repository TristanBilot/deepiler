#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(e);
c=pow(b,b);
d=(fmax(c,b))/(fmin(e,d));
c=sqrt(d);
if(islessequal(e,e)){
d=(fmin(d,d))*(sin(b));
a=atan(c);
b=(sqrt(e))/(floor(d));
c=ceil(c);
c=pow(b,c);
}
else{
e=log(b);
d=(cos(c))-(ceil(e));
b=atan2(d,d);
b=fmin(c,a);
}
if(islessequal(c,c)){
a=fmax(a,d);
c=tan(d);
}
}