#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan2(c,d))-(fmax(a,g));
e=(atan(f))*(fmax(e,f));
d=sin(g);
f=(atan2(g,f))-(atan2(f,c));
f=atan(g);
if(isgreaterequal(d,b)){
d=(sin(h))-(atan2(f,b));
g=log10(a);
a=floor(g);
}
f=atan2(g,c);
h=fmin(e,b);
b=(sin(g))*(pow(g,c));
}