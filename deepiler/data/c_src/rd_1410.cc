#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(e,c);
a=asin(c);
b=(fdim(a,c))-(asin(f));
c=(atan(c))*(fdim(c,f));
if(isless(a,e)){
b=(exp(a))+(fdim(c,c));
d=fmax(f,d);
c=(atan2(d,d))/(acos(e));
}
else{
f=atan(f);
f=(floor(a))-(atan2(b,c));
c=log(a);
}
if(islessequal(d,d)){
d=(acos(c))/(exp(e));
a=atan(a);
c=(log10(a))+(atan(b));
e=pow(c,f);
}
else{
e=fmax(f,f);
c=(sin(e))-(tan(c));
a=(fmax(f,c))-(sin(a));
d=pow(a,d);
}
}