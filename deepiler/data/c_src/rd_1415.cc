#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=exp(d);
d=(fmax(c,a))+(fdim(a,a));
a=(fdim(c,b))-(fdim(c,a));
d=fdim(c,c);
a=(atan(c))/(exp(d));
c=pow(b,c);
a=fmax(b,d);
e=(pow(c,a))/(fdim(d,d));
d=sin(a);
b=fmax(d,c);
}