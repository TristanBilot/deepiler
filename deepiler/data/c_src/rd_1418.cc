#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=pow(b,e);
a=(atan2(d,d))-(log10(a));
g=(fmax(b,b))/(cos(e));
b=(log10(f))/(sin(g));
if(isless(a,c)){
c=exp(a);
c=(pow(g,f))+(fdim(a,d));
b=atan2(c,g);
c=log(a);
g=fmax(f,g);
}
else{
f=atan2(d,c);
c=(atan(e))*(pow(b,c));
f=cos(c);
b=(fmin(b,d))*(exp(b));
d=tan(d);
}
if(isgreaterequal(a,b)){
e=sin(b);
f=ceil(f);
a=atan2(e,d);
g=(fdim(g,b))*(sin(g));
}
else{
c=(floor(a))*(atan2(b,a));
f=fmax(e,b);
a=sqrt(a);
}
}