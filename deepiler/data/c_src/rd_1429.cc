#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=floor(f);
a=fdim(h,g);
a=(fmax(b,f))+(cos(g));
a=(log10(b))*(fmin(c,c));
g=fdim(h,h);
c=atan(d);
c=exp(c);
e=fmax(b,c);
e=(asin(e))+(fmax(g,f));
e=(fmin(c,g))*(fmin(g,c));
c=sin(a);
a=(acos(e))*(atan2(d,f));
b=log10(a);
}