#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(c);
c=atan2(d,c);
if(isless(c,c)){
d=(fmax(b,e))+(cos(b));
b=(log10(c))*(fmin(a,c));
a=acos(a);
}
d=fmin(d,d);
b=(asin(a))/(atan(e));
}