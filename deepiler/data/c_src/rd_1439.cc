#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmax(e,e);
d=(fmin(d,d))-(exp(a));
b=(asin(f))*(exp(d));
if(islessequal(b,a)){
f=(fmin(f,b))*(pow(d,d));
b=log10(e);
f=(cos(d))*(fmax(b,e));
f=fmin(e,f);
}
f=pow(a,a);
f=fmin(a,c);
}