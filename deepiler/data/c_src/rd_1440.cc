#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=asin(c);
c=atan2(b,e);
d=exp(d);
b=fmax(e,b);
c=atan2(b,f);
while(isless(b,f)){
e=tan(b);
f=(fdim(a,f))+(atan2(d,d));
f=fdim(a,e);
d=(pow(e,b))/(atan(a));
b=(ceil(b))-(log10(e));
}
c=(fdim(a,f))+(pow(d,f));
a=pow(e,a);
e=(fmin(c,a))-(fdim(a,e));
}