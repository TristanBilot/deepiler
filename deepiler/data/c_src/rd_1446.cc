#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(e,e))/(fmin(c,d));
c=(fdim(d,f))/(atan(a));
d=(fdim(h,f))+(acos(e));
d=fmin(b,e);
while(isless(d,g)){
d=fmin(f,e);
h=atan2(d,g);
e=(sqrt(e))+(atan2(d,d));
g=(pow(c,g))*(fmax(c,a));
g=(atan2(f,e))*(fdim(h,h));
}
d=(atan(c))*(atan2(e,h));
g=(fmax(e,d))/(fmax(f,a));
c=(pow(h,g))+(exp(c));
c=(asin(a))*(log10(g));
}