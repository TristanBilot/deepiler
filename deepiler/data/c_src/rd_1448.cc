#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(cos(c))*(pow(b,a));
f=floor(b);
f=(ceil(d))/(fmax(b,g));
f=log10(a);
b=ceil(f);
while(isless(c,c)){
e=(log10(f))*(fdim(b,e));
f=atan(a);
}
while(islessgreater(b,f)){
f=tan(a);
g=(fdim(g,g))/(atan2(g,e));
c=cos(e);
}
}