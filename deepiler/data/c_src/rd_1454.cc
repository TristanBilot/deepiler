#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(c,b))+(fmin(a,a));
e=fmax(c,b);
e=atan2(f,b);
e=fmin(f,d);
f=pow(f,c);
if(isless(d,e)){
c=(sin(e))+(fdim(f,e));
d=atan2(c,c);
c=(exp(e))-(log10(d));
}
else{
d=ceil(b);
b=(atan2(b,b))-(ceil(f));
b=log10(c);
}
a=(fdim(a,a))*(floor(e));
d=fmax(e,e);
}