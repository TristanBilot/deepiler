#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmin(c,a))/(fmax(f,a));
b=exp(e);
d=log10(d);
if(islessequal(f,a)){
a=asin(c);
d=fmin(b,e);
f=(pow(d,b))/(pow(d,a));
d=fmin(e,a);
}
else{
f=(floor(c))-(fdim(b,f));
c=(fdim(b,f))*(floor(c));
f=fdim(f,f);
}
if(islessgreater(d,b)){
a=(fdim(f,b))-(fmax(a,f));
a=pow(d,d);
}
else{
f=sin(c);
d=(exp(c))/(ceil(c));
a=pow(e,a);
}
}