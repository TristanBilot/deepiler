#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(f,f);
f=(fmin(f,b))-(tan(e));
g=(fmax(b,c))+(exp(d));
e=(fmin(f,e))+(atan(a));
d=(fdim(f,a))-(fmax(g,d));
b=acos(c);
a=(fmin(b,b))/(log10(e));
b=(sqrt(b))*(fmin(e,c));
while(islessgreater(e,g)){
c=(log10(g))/(fmin(f,e));
b=acos(f);
}
}