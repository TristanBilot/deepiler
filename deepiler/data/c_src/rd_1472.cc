#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(pow(f,c))*(fmax(e,b));
b=floor(b);
a=fdim(b,f);
b=(sin(a))*(exp(a));
e=(pow(c,a))-(sqrt(f));
a=fdim(d,a);
a=pow(f,e);
d=(tan(d))-(pow(e,c));
b=fmax(f,a);
b=pow(a,c);
e=pow(a,f);
}