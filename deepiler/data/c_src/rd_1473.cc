#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(b))+(tan(c));
c=tan(e);
a=fmax(d,c);
c=(fdim(e,a))/(atan(b));
e=exp(a);
c=pow(d,a);
while(isless(c,d)){
c=(fdim(e,d))+(fmin(d,e));
d=fmax(a,d);
c=fmin(b,c);
c=(cos(a))+(atan(b));
c=tan(e);
}
}