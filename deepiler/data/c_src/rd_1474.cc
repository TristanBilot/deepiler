#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(a,b))-(atan2(c,e));
a=fmax(b,b);
c=pow(a,d);
if(isless(b,c)){
c=pow(a,d);
a=(log10(e))*(pow(c,d));
a=(fdim(d,b))+(fmin(d,e));
e=cos(b);
}
else{
c=(asin(a))*(cos(c));
b=tan(e);
b=(pow(e,e))*(pow(c,d));
a=(ceil(b))-(exp(c));
}
if(isgreaterequal(e,b)){
e=(log10(e))*(ceil(e));
b=(fmax(c,d))/(floor(d));
d=(fmax(d,d))*(cos(c));
}
else{
a=(ceil(d))*(sqrt(d));
d=log(b);
a=pow(b,c);
a=log10(c);
}
}