#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=ceil(c);
f=cos(a);
d=ceil(c);
b=(acos(d))/(tan(a));
f=(fdim(e,e))-(pow(c,a));
f=atan2(e,b);
while(isless(c,g)){
d=pow(f,f);
a=(fmin(d,e))*(fmin(b,e));
a=fdim(e,f);
a=fmax(a,d);
}
}