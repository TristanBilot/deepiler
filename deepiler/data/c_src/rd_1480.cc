#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(acos(g))+(pow(f,b));
c=fmin(h,c);
h=(fmin(e,d))/(atan2(b,d));
while(islessequal(g,b)){
f=asin(a);
c=(sin(a))+(fmax(a,e));
f=pow(f,h);
}
if(islessequal(c,c)){
c=(fmax(d,b))+(fmax(e,h));
h=cos(d);
b=(fmax(f,f))*(exp(a));
h=exp(h);
c=sqrt(b);
}
}