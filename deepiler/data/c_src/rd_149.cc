#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=pow(b,g);
f=(fmax(e,d))+(asin(d));
f=(sqrt(e))-(tan(e));
a=(fmax(d,a))/(pow(c,f));
g=(pow(e,f))*(fdim(c,d));
b=log10(e);
f=(tan(d))*(fmax(e,c));
d=atan(e);
g=exp(d);
b=(asin(d))*(ceil(c));
}