#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(log(g))*(log(g));
f=atan2(b,g);
f=atan(b);
a=log(f);
a=fmin(f,d);
g=asin(d);
g=(log10(c))*(atan2(g,g));
g=atan2(a,e);
b=ceil(c);
h=(ceil(b))-(atan2(f,a));
if(islessgreater(d,f)){
f=(fmax(f,c))+(ceil(d));
c=(asin(f))-(atan2(e,b));
g=sin(a);
}
else{
c=fmin(e,f);
b=(atan2(h,d))*(cos(e));
e=fmin(a,f);
}
}