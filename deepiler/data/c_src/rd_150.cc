#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmax(a,a);
f=(atan2(d,f))-(log10(a));
e=(atan2(c,d))-(atan2(e,d));
b=atan2(f,c);
d=pow(c,e);
if(islessequal(c,b)){
c=(asin(f))+(log(c));
a=(fmin(f,d))*(sin(e));
f=fmax(f,b);
}
}