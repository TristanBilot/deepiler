#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(d,e);
d=fmax(d,e);
e=(fdim(c,c))*(fmin(b,c));
e=floor(a);
b=atan(e);
while(isless(e,b)){
b=(fmax(c,e))*(atan(e));
c=(pow(b,c))*(floor(a));
}
e=fmin(e,b);
c=(fmax(e,d))+(fmin(b,a));
}