#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(d,c))+(fmax(a,f));
g=sqrt(e);
e=(fmax(c,e))-(tan(d));
d=fmax(d,c);
if(isless(b,g)){
b=atan2(d,f);
g=(fdim(d,a))-(atan2(g,c));
b=fmin(f,b);
e=(fdim(e,a))*(fdim(a,g));
}
else{
e=(pow(b,c))*(atan2(b,a));
c=tan(g);
d=(floor(f))*(fdim(g,g));
f=(log10(e))-(floor(e));
}
if(isless(b,b)){
c=fmin(c,b);
f=(log(e))-(cos(f));
c=atan(c);
}
else{
d=sin(g);
d=atan2(f,b);
b=fmax(b,c);
}
}