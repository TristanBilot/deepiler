#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(e,g))/(cos(a));
c=ceil(a);
f=(fmax(g,a))*(atan2(b,g));
b=(fdim(f,d))*(sqrt(g));
b=(acos(a))/(pow(d,b));
a=fmax(c,a);
b=pow(c,c);
f=asin(a);
e=(atan2(a,a))+(log10(c));
c=(log(a))+(ceil(e));
}