#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(a))-(sin(d));
c=sqrt(e);
e=log10(c);
b=(fdim(d,e))*(cos(c));
while(islessequal(a,b)){
a=(atan(a))/(acos(e));
b=(pow(e,b))+(pow(e,d));
a=(fmin(e,a))*(log(c));
}
while(islessgreater(a,b)){
c=(pow(c,d))/(fmax(c,b));
d=fmin(b,e);
}
}