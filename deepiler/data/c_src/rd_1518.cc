#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(c))*(pow(e,a));
d=(exp(d))-(pow(d,d));
while(islessgreater(a,b)){
e=pow(a,a);
c=(pow(c,a))*(asin(c));
b=fdim(b,e);
}
while(isgreaterequal(e,c)){
b=(atan2(b,e))-(log10(c));
d=atan2(a,c);
}
}