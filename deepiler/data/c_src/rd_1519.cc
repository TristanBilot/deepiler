#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(e,a);
e=asin(a);
b=(cos(d))+(asin(b));
if(isgreaterequal(a,b)){
e=(log10(a))*(fdim(c,d));
b=(atan2(d,e))/(fmin(d,c));
}
else{
d=tan(c);
b=floor(a);
c=asin(c);
}
d=log(e);
d=floor(b);
b=atan2(c,e);
a=(fmax(a,a))+(log(a));
}