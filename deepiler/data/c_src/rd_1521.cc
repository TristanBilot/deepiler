#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(d,a);
e=(fmin(a,d))*(atan2(d,c));
while(isless(b,a)){
e=atan2(e,e);
b=fmax(e,b);
e=atan(b);
c=atan2(e,c);
}
if(isgreaterequal(c,b)){
d=sin(e);
c=(cos(e))-(pow(d,c));
e=(sin(c))+(tan(a));
e=fdim(e,e);
a=fmax(a,a);
}
}