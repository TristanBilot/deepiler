#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(f,a);
d=fdim(c,c);
a=fmin(g,d);
c=(fdim(b,b))*(log(d));
while(islessequal(g,h)){
a=(log(a))/(fmin(d,b));
c=(fdim(e,b))-(pow(h,d));
g=asin(c);
f=exp(c);
h=(sqrt(f))/(floor(f));
}
while(islessequal(a,c)){
b=(sin(h))/(atan(a));
h=fdim(a,a);
}
}