#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(d))/(fmax(e,a));
a=(fmin(f,c))-(fmax(d,f));
d=fmax(b,f);
a=(fmax(e,f))/(fdim(c,a));
c=(floor(a))-(fmin(a,b));
f=(ceil(a))/(log10(a));
c=fmin(c,f);
c=(ceil(b))/(pow(c,d));
a=(exp(a))-(acos(e));
c=log10(b);
}