#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=ceil(e);
f=fmax(d,b);
e=(fdim(f,c))/(atan2(c,f));
a=(fmax(f,e))-(log10(b));
while(islessgreater(a,a)){
d=log(c);
e=(sqrt(a))*(fmax(e,e));
b=(fdim(c,e))/(acos(a));
}
if(islessgreater(d,f)){
a=(atan(c))/(asin(c));
a=atan2(e,a);
e=(exp(a))/(asin(a));
}
else{
e=pow(f,d);
e=log(b);
c=(fmax(d,e))/(fdim(e,a));
a=exp(e);
b=atan2(e,f);
}
}