#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(e,c);
d=(sqrt(g))*(asin(c));
b=tan(c);
e=fmax(d,e);
while(isless(b,e)){
b=sqrt(b);
d=(fmin(h,g))*(log10(b));
}
h=pow(c,h);
f=fmax(f,c);
a=floor(g);
d=floor(a);
h=(fdim(g,b))*(pow(b,f));
}