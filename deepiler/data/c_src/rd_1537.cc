#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(c);
b=(fmax(a,d))-(log(d));
a=fdim(e,d);
e=(fdim(c,d))/(sin(a));
e=cos(a);
e=(pow(a,e))/(atan2(c,e));
c=(atan2(e,c))/(exp(e));
while(isgreaterequal(d,b)){
a=fmin(c,d);
c=atan(b);
}
}