#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(exp(b))-(acos(d));
d=(pow(d,a))/(fmax(c,a));
while(islessgreater(c,f)){
f=(fmax(a,b))-(asin(d));
c=fmax(c,e);
}
if(islessequal(e,e)){
d=tan(b);
d=(exp(a))-(ceil(b));
}
}