#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(g,b);
d=sqrt(g);
g=(log(a))-(log(e));
h=fdim(g,f);
c=sin(h);
while(isless(h,c)){
d=(atan(f))-(exp(a));
c=(fmax(e,c))-(fmin(b,b));
b=acos(g);
b=fmin(d,a);
g=fdim(d,g);
}
h=ceil(c);
b=(sqrt(g))+(fmin(d,h));
b=pow(g,b);
g=(pow(c,d))+(atan2(b,h));
d=fmin(a,e);
}