#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(e,b);
f=(fdim(d,g))*(pow(d,a));
e=(ceil(c))-(pow(a,e));
c=(fmin(f,a))/(sin(c));
f=asin(c);
if(islessequal(a,e)){
f=fdim(b,e);
c=tan(b);
f=(asin(f))/(atan2(a,g));
g=(atan2(e,c))+(atan(e));
g=(tan(f))/(atan2(a,a));
}
if(isgreaterequal(g,d)){
g=fmin(b,c);
c=(fdim(d,f))-(sin(f));
g=(fdim(d,a))*(atan2(g,g));
}
}