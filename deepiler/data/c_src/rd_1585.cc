#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=log(c);
a=fdim(e,d);
c=atan(d);
e=(log(f))*(log(a));
a=(fdim(c,d))-(fmax(b,f));
if(islessgreater(b,a)){
a=fmin(f,c);
e=tan(f);
c=(sin(e))-(sqrt(c));
c=(atan2(a,c))-(atan(f));
}
else{
f=sqrt(b);
b=pow(a,b);
}
if(isless(b,a)){
e=sin(a);
a=(fmin(f,b))/(fmax(f,b));
d=(log10(c))-(pow(b,a));
e=sin(b);
}
else{
a=(fdim(a,e))/(cos(f));
e=(fmin(d,e))*(atan2(f,a));
b=(fmax(d,a))*(acos(f));
f=(fmax(e,f))/(fdim(d,d));
}
}