#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(ceil(b))-(floor(f));
c=fmin(g,e);
a=(fmin(f,e))*(sin(b));
a=log(e);
f=fmax(c,g);
while(islessequal(e,c)){
b=(exp(f))+(asin(g));
e=(log(d))*(ceil(c));
f=(sqrt(h))-(cos(c));
e=(atan(c))-(fmin(f,c));
d=sqrt(h);
}
if(islessequal(d,h)){
c=(pow(a,f))*(atan2(g,f));
h=(atan(c))+(fmin(g,c));
a=log(c);
b=atan2(f,e);
d=atan2(c,a);
}
}