#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(c,f))*(log10(e));
b=(acos(g))/(fmin(b,e));
g=(log10(c))-(fmax(f,g));
d=fmin(g,d);
g=floor(d);
a=atan2(d,b);
g=fdim(f,a);
e=fdim(g,b);
if(islessgreater(e,e)){
c=(ceil(e))-(fmax(g,b));
f=atan2(a,c);
}
else{
g=(sqrt(g))*(log10(d));
b=fmin(f,e);
e=(pow(b,e))/(atan2(g,f));
}
}