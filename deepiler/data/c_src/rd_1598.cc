#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(b,a))+(atan(a));
b=fmin(d,c);
e=fmin(c,d);
d=floor(e);
c=fdim(c,b);
d=pow(a,a);
a=atan2(b,e);
d=(acos(b))/(atan2(d,d));
c=(fmax(a,c))*(acos(c));
d=log(d);
c=(atan2(b,b))/(fdim(a,c));
c=atan(d);
}