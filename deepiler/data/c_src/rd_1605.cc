#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=asin(e);
e=(asin(d))*(cos(d));
d=(fmin(b,b))+(sin(e));
if(islessequal(a,a)){
a=(atan2(c,b))/(fmax(b,e));
c=fmax(d,d);
b=(atan2(c,b))-(sqrt(e));
}
else{
c=(atan(a))-(tan(c));
d=(fmin(b,e))-(fdim(b,a));
}
a=exp(c);
d=(floor(b))/(asin(e));
d=(atan2(a,b))+(cos(c));
a=floor(c);
d=asin(a);
}