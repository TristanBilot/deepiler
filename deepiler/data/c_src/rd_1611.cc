#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=exp(c);
d=sqrt(d);
g=(sin(d))-(tan(b));
d=log(g);
e=(pow(d,c))-(pow(f,d));
d=sin(a);
f=atan2(e,f);
while(islessequal(c,b)){
b=(sqrt(a))-(fmax(e,g));
d=fmin(g,g);
b=(asin(a))+(fmax(f,e));
}
}