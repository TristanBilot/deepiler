#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(e,b))*(fmax(c,f));
b=floor(a);
d=atan(b);
b=(atan2(c,c))*(fmin(f,e));
if(isless(e,f)){
a=(atan(d))/(fmin(c,a));
b=(atan2(b,e))/(ceil(e));
f=pow(c,d);
}
f=fmax(f,a);
e=(floor(c))/(acos(c));
c=pow(d,a);
e=asin(a);
}