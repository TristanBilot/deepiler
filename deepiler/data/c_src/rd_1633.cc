#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log10(d);
a=(pow(e,e))/(fmax(a,a));
f=(cos(c))-(cos(f));
a=floor(e);
f=fdim(f,f);
b=(fdim(f,b))/(atan2(b,d));
b=ceil(c);
if(islessgreater(b,c)){
f=fdim(e,a);
e=ceil(f);
}
else{
f=fmax(a,a);
d=(log(e))*(sqrt(b));
c=fmax(b,a);
}
}