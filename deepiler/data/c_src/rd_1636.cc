#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(e,c))*(ceil(e));
e=(pow(b,c))*(fmin(b,a));
e=(log(e))+(floor(c));
if(isless(d,a)){
e=floor(c);
d=pow(c,a);
d=floor(e);
e=(fmax(a,b))-(fdim(e,b));
}
else{
d=log10(c);
e=sqrt(d);
a=pow(c,d);
}
if(islessequal(b,d)){
d=(pow(d,b))+(fdim(b,c));
d=acos(a);
a=fdim(b,e);
d=(atan2(a,e))+(asin(a));
b=fmax(b,b);
}
}