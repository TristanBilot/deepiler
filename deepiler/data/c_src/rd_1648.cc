#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(c,d))*(exp(b));
f=fmin(a,d);
b=(fmax(d,d))*(atan(c));
f=(pow(e,d))*(atan(e));
c=(exp(f))+(pow(e,f));
if(islessequal(d,b)){
d=pow(f,f);
f=fmax(a,e);
}
while(isless(c,b)){
c=pow(e,a);
a=(acos(f))+(fmin(d,f));
}
}