#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=exp(d);
d=pow(a,d);
b=fmin(b,f);
a=fdim(c,a);
a=(log10(e))+(fmin(d,f));
if(isgreaterequal(b,a)){
a=(atan2(a,f))*(exp(c));
b=(atan(a))*(pow(f,e));
f=fmin(f,f);
}
else{
f=(atan2(a,f))+(cos(e));
a=exp(e);
}
while(islessequal(b,b)){
e=(cos(d))/(asin(a));
f=ceil(f);
f=(fdim(e,a))-(pow(c,b));
}
}