#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sqrt(d);
d=(log(d))+(floor(g));
h=fmin(d,b);
g=tan(h);
if(isless(a,c)){
c=log10(b);
d=cos(g);
}
else{
d=(fmin(h,h))*(fmax(d,d));
b=fmin(a,d);
b=(pow(c,c))*(log10(b));
h=tan(h);
c=(floor(e))+(pow(d,e));
}
if(isgreaterequal(g,d)){
c=fmin(d,g);
g=fmax(e,e);
}
else{
h=(pow(c,e))-(cos(b));
h=tan(b);
e=atan(d);
d=(tan(c))+(log(c));
a=fmin(d,c);
}
}