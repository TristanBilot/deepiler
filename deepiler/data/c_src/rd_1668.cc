#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=log10(a);
a=fmax(c,f);
b=exp(d);
c=(pow(d,e))*(fmax(f,c));
d=fmax(f,f);
if(islessgreater(c,e)){
e=log(d);
b=cos(c);
d=(tan(e))+(exp(g));
}
else{
a=(fmax(e,e))+(fdim(g,a));
b=(log10(c))+(fdim(a,e));
e=(sqrt(b))/(pow(f,g));
}
if(isless(e,d)){
d=(fdim(a,b))/(pow(d,b));
a=(fmin(g,b))/(fmax(c,f));
a=(fmax(d,b))-(fdim(c,e));
e=fmin(g,c);
}
}