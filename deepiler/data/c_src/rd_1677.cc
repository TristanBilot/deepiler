#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(ceil(e))*(pow(d,g));
g=fmax(g,e);
h=tan(g);
d=(log(f))/(asin(a));
a=atan2(g,c);
g=atan2(b,g);
h=atan(h);
g=fmax(d,h);
g=(fdim(h,f))-(cos(h));
b=(fmin(b,h))+(atan2(e,b));
}