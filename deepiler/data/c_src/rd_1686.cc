#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fdim(d,e))-(exp(d));
f=fmin(d,e);
f=(fmin(f,d))*(log(b));
while(isless(b,d)){
f=fmin(b,e);
b=(log10(f))+(atan2(d,a));
b=fmax(f,e);
a=(fmax(f,a))-(fmin(e,e));
c=(ceil(a))+(floor(a));
}
if(islessgreater(c,b)){
f=(pow(f,a))*(fdim(d,d));
c=fmax(c,b);
}
}