#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(b))-(pow(f,b));
c=asin(a);
d=tan(d);
b=asin(b);
e=pow(b,e);
if(isgreaterequal(f,c)){
e=fmax(f,b);
f=(floor(d))-(sqrt(f));
c=fmin(b,f);
f=(atan(b))/(fmin(a,d));
a=(atan2(a,b))-(fdim(e,b));
}
else{
e=floor(b);
c=(log10(c))/(atan(f));
d=atan2(b,e);
}
if(isless(a,a)){
e=log(c);
c=fmax(d,e);
}
else{
c=(pow(c,c))+(sin(d));
d=(fdim(c,f))-(acos(a));
d=(atan2(b,b))+(log10(e));
}
}