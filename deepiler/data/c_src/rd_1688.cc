#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(g,e);
g=(log10(g))*(exp(e));
g=(tan(e))+(ceil(b));
g=(pow(g,b))/(fmin(g,a));
g=(sin(h))-(floor(b));
b=(sqrt(c))+(cos(d));
e=(fdim(h,b))+(ceil(g));
h=(atan2(e,d))*(log10(e));
g=fmax(a,d);
if(isgreaterequal(g,h)){
d=fdim(a,e);
e=acos(h);
g=pow(c,g);
f=(floor(a))/(fmax(b,h));
}
}