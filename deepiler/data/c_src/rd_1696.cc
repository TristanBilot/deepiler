#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(c,a);
c=(floor(a))-(atan(d));
d=floor(b);
d=(tan(d))/(fmax(b,d));
a=fmax(d,e);
while(isgreaterequal(e,b)){
e=pow(e,b);
b=(floor(e))-(atan2(e,d));
a=pow(a,e);
}
while(isgreaterequal(d,b)){
b=fdim(e,b);
b=(log(d))+(atan(c));
b=(fmax(b,b))*(pow(e,e));
}
}