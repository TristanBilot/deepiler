#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(e);
e=(cos(b))/(cos(d));
d=asin(d);
b=cos(a);
if(islessgreater(e,d)){
a=exp(d);
c=(fmax(e,e))-(fdim(d,c));
c=cos(e);
a=pow(c,c);
}
if(islessequal(c,c)){
a=(fmax(c,e))/(pow(e,a));
c=pow(b,a);
}
else{
e=(sqrt(a))+(pow(b,c));
c=exp(e);
d=(fmin(c,e))*(atan2(a,e));
}
}