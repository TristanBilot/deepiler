#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmax(e,e);
a=(asin(d))+(fmax(b,a));
while(isless(e,e)){
g=(fmax(g,c))+(asin(a));
a=(pow(g,b))-(pow(a,b));
}
while(isgreaterequal(c,g)){
b=(asin(b))*(cos(b));
d=(sqrt(e))*(sin(a));
d=log(e);
a=floor(e);
}
}