#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(asin(a))-(asin(c));
f=(atan2(e,f))+(atan2(d,c));
b=(fmax(a,c))+(asin(a));
d=fdim(h,c);
d=(log10(a))/(fdim(d,e));
a=tan(a);
d=fdim(d,b);
h=(cos(e))*(exp(h));
f=fmax(h,d);
g=(pow(c,c))*(fmin(c,g));
b=atan2(e,f);
a=fmax(h,d);
}