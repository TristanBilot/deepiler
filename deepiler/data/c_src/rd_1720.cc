#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(e,g))-(exp(d));
e=(log(g))-(exp(e));
b=(acos(a))*(atan2(d,c));
b=cos(b);
h=atan2(f,c);
while(isless(b,f)){
h=(tan(d))+(atan2(d,h));
f=(pow(e,g))-(atan2(e,c));
}
g=(fmax(d,d))/(atan2(b,d));
e=fmax(f,g);
}