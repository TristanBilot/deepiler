#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(e,b))/(log10(e));
e=fmax(c,c);
c=(asin(a))-(pow(c,d));
b=(sin(a))/(log(c));
if(islessequal(b,a)){
a=ceil(e);
b=asin(c);
a=cos(b);
b=cos(d);
c=atan2(e,c);
}
while(isless(a,e)){
b=sin(b);
d=fmin(c,c);
d=(fmax(c,c))/(pow(e,a));
e=atan(a);
e=(atan2(e,e))+(pow(d,d));
}
}