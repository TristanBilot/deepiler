#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(a);
b=(pow(a,h))-(asin(h));
e=(pow(e,g))+(cos(g));
while(isgreaterequal(f,f)){
a=atan2(e,b);
a=(sin(d))*(atan2(g,c));
f=(acos(d))*(sin(g));
g=atan2(e,b);
}
if(isgreaterequal(d,f)){
d=sqrt(g);
b=(fmin(h,h))/(fmin(c,h));
b=fdim(e,d);
f=(asin(h))-(atan(e));
}
else{
b=fdim(e,e);
e=(ceil(g))*(cos(e));
}
}