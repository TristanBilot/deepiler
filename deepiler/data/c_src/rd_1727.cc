#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(b,f);
f=(atan2(b,e))*(atan(b));
b=acos(f);
if(islessequal(b,d)){
e=(pow(g,a))/(atan2(f,f));
a=exp(a);
b=cos(f);
e=asin(b);
}
while(isless(c,f)){
b=(ceil(e))+(log10(e));
f=(floor(e))-(asin(d));
c=(cos(f))*(cos(a));
}
}