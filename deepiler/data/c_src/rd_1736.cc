#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(a,d);
a=asin(d);
while(islessgreater(b,b)){
b=pow(a,e);
f=(pow(e,f))-(floor(b));
}
if(isgreaterequal(e,e)){
b=tan(d);
f=fmax(e,a);
a=(ceil(a))-(pow(e,c));
c=log(a);
a=(atan2(b,f))-(fmax(c,f));
}
else{
e=fmin(b,e);
a=atan(c);
a=fmin(c,c);
}
}