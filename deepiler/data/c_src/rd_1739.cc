#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(e,g))*(pow(h,e));
g=sqrt(f);
a=(ceil(b))/(fmax(e,g));
g=(fmax(g,f))-(log10(b));
while(isgreaterequal(e,a)){
g=(pow(g,e))-(log10(f));
d=(sqrt(c))-(ceil(b));
h=cos(a);
f=(fmax(d,a))+(exp(a));
}
while(isgreaterequal(e,e)){
e=(floor(b))/(fdim(g,h));
d=(fmin(c,a))+(cos(h));
e=(ceil(f))+(asin(h));
}
}