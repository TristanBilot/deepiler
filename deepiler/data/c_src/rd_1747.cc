#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sqrt(b);
c=sqrt(d);
a=pow(d,a);
e=pow(b,b);
if(isgreaterequal(a,c)){
a=(fdim(b,a))*(fmin(a,a));
e=fmin(d,e);
d=(log(e))-(fmax(c,b));
a=fdim(d,b);
}
else{
a=(atan2(d,b))+(sin(a));
e=fmax(e,d);
e=(exp(b))*(atan2(c,d));
d=pow(d,e);
e=(fdim(a,d))+(fmax(d,c));
}
b=(fmax(e,e))+(tan(c));
b=(fdim(b,c))+(log10(b));
}