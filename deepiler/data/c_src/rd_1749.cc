#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=pow(b,d);
f=fmax(e,e);
e=asin(g);
b=sqrt(f);
g=ceil(g);
c=(fmin(e,a))*(ceil(e));
if(isless(b,a)){
c=(log10(c))-(tan(d));
a=(fmax(b,g))-(exp(a));
f=(acos(c))*(atan(a));
c=atan2(b,e);
}
else{
b=atan2(b,f);
c=ceil(f);
e=(log(d))*(floor(f));
c=(fmax(b,f))-(cos(e));
b=(fmin(g,c))*(fmin(d,c));
}
}