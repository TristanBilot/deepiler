#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(e,d))/(fmin(a,a));
d=pow(d,e);
e=tan(d);
b=(pow(d,d))/(log10(e));
c=tan(a);
while(islessequal(a,d)){
d=cos(b);
c=pow(e,e);
d=(atan2(a,c))-(cos(e));
b=(pow(a,a))*(log(e));
}
while(islessequal(c,e)){
c=asin(a);
a=(tan(e))*(asin(c));
}
}