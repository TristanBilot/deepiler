#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(g,f))/(fdim(b,a));
b=(fdim(c,g))/(cos(c));
d=(sin(e))/(fmax(a,b));
while(isless(e,b)){
d=fdim(g,b);
d=sin(d);
}
d=cos(a);
a=(atan(e))/(log(e));
}