#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(d,b);
e=(sin(a))/(fmax(b,a));
e=acos(b);
d=fmax(e,c);
while(isless(a,c)){
e=log10(b);
a=(asin(b))*(fmax(d,d));
}
if(isgreaterequal(b,c)){
b=atan2(a,b);
d=(floor(d))-(asin(a));
}
else{
a=(asin(b))+(fdim(d,d));
b=acos(e);
c=fmin(e,e);
e=(atan2(b,e))*(tan(b));
a=(atan2(b,b))+(floor(d));
}
}