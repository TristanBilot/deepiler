#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=sqrt(d);
d=(ceil(a))+(fmax(c,a));
if(islessequal(h,b)){
c=(pow(b,e))+(fmin(b,g));
c=(fmax(e,b))+(fmax(e,h));
a=exp(g);
}
e=tan(c);
b=(fmax(g,a))+(asin(h));
d=(sqrt(g))/(fdim(e,a));
}