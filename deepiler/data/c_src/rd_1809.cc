#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(b,b))*(acos(a));
c=(log10(e))+(sin(b));
c=(fdim(e,d))-(floor(e));
e=pow(d,b);
while(isless(d,e)){
b=(fdim(a,b))*(tan(c));
d=fmin(a,d);
}
while(isgreaterequal(d,e)){
a=sqrt(d);
d=atan2(d,c);
c=log10(e);
d=floor(d);
c=(fmax(d,b))+(pow(c,b));
}
}