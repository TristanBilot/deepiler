#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(exp(f))*(cos(d));
e=(atan2(d,b))-(fmax(d,g));
a=(pow(e,g))-(fdim(d,a));
if(islessgreater(g,g)){
f=(ceil(b))/(atan2(d,c));
e=(sin(a))-(atan(a));
d=(atan2(f,d))-(tan(e));
e=(atan(b))*(fdim(e,a));
}
else{
e=(atan(g))+(atan2(g,e));
b=fmin(b,b);
e=log(a);
}
if(isgreaterequal(f,b)){
e=tan(f);
b=(floor(c))-(ceil(b));
c=fmax(a,c);
g=log(b);
}
else{
b=(ceil(c))/(cos(a));
d=(fdim(c,b))+(fmax(a,e));
}
}