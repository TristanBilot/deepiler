#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan(a))/(floor(c));
a=log(d);
a=(pow(d,d))/(atan(b));
a=(fmax(b,e))+(atan2(c,b));
c=(tan(e))/(ceil(e));
e=floor(e);
d=(pow(c,b))+(sqrt(e));
if(isgreaterequal(e,c)){
a=(sin(d))-(log10(a));
e=atan(d);
}
}