#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(a,a))/(atan2(e,b));
c=acos(b);
d=fmax(b,b);
while(isless(d,c)){
d=fdim(d,d);
b=atan(a);
}
if(islessgreater(e,b)){
c=(sqrt(a))*(fdim(e,d));
c=(fmax(a,a))/(tan(c));
d=(fmax(d,e))-(fdim(c,b));
}
}