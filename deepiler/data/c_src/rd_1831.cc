#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(b,c))-(floor(e));
a=(asin(c))+(sin(e));
d=fmax(b,a);
while(isgreaterequal(a,f)){
f=acos(a);
c=(atan(b))*(fdim(f,d));
c=(ceil(b))+(fdim(a,b));
f=fmax(c,c);
}
d=(fmin(e,d))/(atan2(f,d));
a=acos(b);
f=pow(a,a);
}