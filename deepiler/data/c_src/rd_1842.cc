#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(tan(g))/(ceil(d));
g=(sin(d))-(fmax(g,a));
h=(cos(c))-(floor(e));
f=fmax(b,c);
if(isgreaterequal(d,e)){
d=(log10(a))/(cos(c));
e=(atan2(e,f))*(floor(a));
c=(fmax(c,b))*(sin(b));
}
else{
g=(fmax(a,h))*(pow(h,d));
f=(sin(e))-(sqrt(f));
c=(atan2(e,d))/(ceil(h));
h=(atan2(e,b))/(log10(d));
e=fmin(d,c);
}
e=sin(f);
e=(fdim(h,h))*(tan(d));
h=floor(f);
h=(fdim(c,h))+(asin(b));
}