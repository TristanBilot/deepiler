#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=pow(c,c);
b=(atan2(b,b))-(floor(d));
f=fmin(b,c);
d=log(b);
f=(asin(a))*(asin(a));
c=(floor(c))/(atan2(a,e));
d=(fmin(f,b))/(acos(d));
e=(asin(f))*(atan2(e,b));
if(islessequal(d,d)){
f=fmax(b,b);
c=fmax(c,e);
d=sqrt(e);
e=(log10(c))*(pow(f,c));
}
}