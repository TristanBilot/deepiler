#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(c);
d=(log10(c))+(fmax(a,b));
e=pow(b,b);
b=(log10(e))+(atan2(d,a));
c=pow(e,a);
while(isless(d,e)){
a=log10(e);
e=(tan(a))+(asin(e));
a=(atan(d))*(fmax(c,e));
e=(exp(e))-(ceil(a));
}
}