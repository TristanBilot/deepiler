#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(e,b))/(sin(g));
d=tan(f);
g=(fmax(c,c))/(cos(f));
while(islessgreater(g,d)){
h=(atan2(e,e))*(atan2(c,a));
h=pow(c,h);
}
f=atan2(g,d);
a=(fdim(e,b))+(sin(g));
d=(fdim(h,g))+(ceil(c));
}