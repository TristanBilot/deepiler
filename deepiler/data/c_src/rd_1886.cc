#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(a);
g=(cos(g))-(cos(b));
c=ceil(c);
while(isgreaterequal(b,b)){
f=fmax(b,e);
e=sin(g);
c=(atan2(b,b))/(fmin(h,b));
f=pow(h,d);
}
while(isless(e,g)){
f=(cos(b))*(sin(a));
h=(acos(f))-(atan(d));
}
}