#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(b,a))+(log10(c));
b=exp(a);
e=sqrt(e);
a=fdim(a,a);
d=fmax(a,b);
while(isless(d,a)){
c=sin(b);
c=atan2(e,b);
c=(pow(b,a))/(log10(c));
a=(fmax(c,c))/(fmin(a,d));
e=fdim(e,e);
}
while(isgreaterequal(e,e)){
e=(sin(b))*(atan2(b,d));
b=log10(c);
d=(floor(a))/(fdim(b,b));
}
}