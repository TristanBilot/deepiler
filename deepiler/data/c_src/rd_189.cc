#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(a,b);
f=fdim(c,b);
d=(asin(c))-(tan(d));
a=(fdim(c,b))*(pow(b,f));
d=fmax(b,b);
while(islessgreater(e,b)){
f=(sqrt(e))+(acos(f));
d=(log(d))-(sin(c));
c=sin(c);
c=(pow(e,f))*(fdim(f,c));
a=fdim(a,e);
}
while(islessgreater(e,f)){
b=asin(c);
c=(atan2(f,e))/(exp(b));
b=(cos(d))*(atan2(b,a));
}
}