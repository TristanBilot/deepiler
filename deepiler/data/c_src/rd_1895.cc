#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(cos(b))-(pow(a,d));
g=acos(c);
e=(fmax(h,f))+(pow(d,a));
while(isgreaterequal(h,f)){
f=acos(b);
f=asin(a);
c=(acos(f))/(log10(a));
g=sin(f);
}
b=(sin(d))/(pow(h,b));
e=(fmax(h,g))+(exp(d));
d=(atan2(g,g))+(log(c));
}