#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmax(d,c);
b=(log(b))*(pow(a,d));
c=(log(d))+(pow(c,c));
a=(sin(f))+(pow(c,c));
c=(atan2(d,e))*(fmax(c,a));
if(isless(b,d)){
d=fmax(b,c);
d=asin(f);
e=cos(a);
f=atan2(b,e);
}
}