#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(exp(c))*(fdim(f,c));
e=(log10(c))*(floor(c));
a=fmin(f,d);
while(islessequal(c,a)){
b=(fdim(f,e))-(fmin(c,c));
f=(cos(b))*(fmin(c,b));
f=fmin(d,a);
}
b=fmax(e,e);
a=log(b);
}