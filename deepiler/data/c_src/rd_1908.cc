#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(a,b))/(fmax(a,b));
c=(floor(e))/(sqrt(a));
while(isless(d,b)){
d=(fdim(d,b))-(fmin(e,d));
a=(fmin(a,b))*(fdim(e,a));
}
while(isgreaterequal(c,d)){
c=(exp(b))+(log10(b));
c=fmin(a,c);
c=(log(d))*(fmin(a,b));
e=(asin(c))+(exp(e));
}
}