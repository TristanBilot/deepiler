#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(b);
d=(sqrt(b))-(pow(a,a));
e=atan2(c,a);
e=fdim(b,e);
a=fdim(b,e);
while(isless(d,c)){
e=(fmin(c,a))*(ceil(a));
d=fmax(a,b);
e=(log10(d))*(fmin(e,a));
a=(fmax(b,b))/(floor(c));
a=(floor(e))*(tan(c));
}
a=(log(b))+(sqrt(e));
e=(sqrt(b))/(atan2(a,b));
}