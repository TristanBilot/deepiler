#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(b,c);
b=sin(d);
c=fmin(d,a);
d=log10(e);
c=sin(b);
d=fmax(e,d);
if(islessgreater(d,a)){
e=atan(c);
d=(acos(d))+(atan2(c,b));
e=atan(f);
}
else{
b=cos(d);
f=log(c);
b=(fmax(e,e))/(floor(b));
b=(fmin(f,d))+(ceil(e));
d=fmax(b,b);
}
}