#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(acos(b))-(floor(c));
e=(sqrt(b))/(fdim(b,c));
while(isgreaterequal(c,a)){
c=fmin(d,b);
d=pow(a,d);
e=pow(a,b);
}
c=cos(b);
d=(fmax(e,a))*(fdim(e,c));
b=fmax(b,d);
}