#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmin(f,e))-(log(c));
a=atan2(d,c);
e=log(g);
c=fmax(c,b);
while(isless(g,e)){
g=fmax(d,a);
a=(fmin(a,f))/(fmax(b,g));
}
while(isgreaterequal(c,f)){
c=(floor(f))+(fmin(a,c));
f=(atan(c))+(pow(a,f));
b=tan(f);
d=pow(a,f);
c=(acos(b))/(fmin(a,e));
}
}