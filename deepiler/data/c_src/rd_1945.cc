#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=acos(b);
b=sin(f);
a=log(a);
a=(sin(f))-(fmin(c,a));
while(isless(e,b)){
d=cos(c);
c=fmax(d,f);
f=(atan2(e,c))*(pow(a,a));
e=fdim(d,e);
}
while(isgreaterequal(d,c)){
e=(tan(d))-(pow(c,c));
f=atan2(f,f);
f=(fdim(c,d))*(fdim(c,f));
c=(sin(a))*(sin(a));
}
}