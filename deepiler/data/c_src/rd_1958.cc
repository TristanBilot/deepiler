#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(f,b);
h=(pow(c,g))-(atan2(e,b));
d=(fmax(h,d))+(fmax(a,g));
d=(fmax(e,f))+(asin(g));
while(islessequal(g,b)){
h=(pow(e,h))+(pow(h,a));
a=(pow(f,f))*(fmax(d,b));
c=acos(g);
e=exp(f);
}
while(isgreaterequal(c,d)){
g=fmin(e,f);
b=atan2(e,h);
e=fmax(h,c);
a=tan(e);
}
}