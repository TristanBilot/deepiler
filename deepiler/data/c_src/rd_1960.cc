#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan2(h,h))*(log10(a));
d=sqrt(e);
b=asin(d);
a=(sin(a))-(fmax(a,g));
g=atan2(a,c);
a=(fdim(g,h))*(fmax(a,f));
if(islessequal(f,h)){
c=fmax(f,d);
d=fmax(g,d);
d=fdim(e,a);
h=atan(d);
}
}