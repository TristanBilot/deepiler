#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=pow(d,f);
c=(fmax(a,d))/(pow(g,a));
b=(cos(b))-(fmax(f,d));
d=fdim(f,a);
b=cos(c);
while(isless(b,d)){
e=fmin(a,f);
f=fmax(a,b);
e=(fmax(e,b))/(pow(b,f));
g=pow(d,g);
f=(fmax(d,e))+(cos(e));
}
g=(fmax(b,b))-(tan(c));
f=exp(a);
b=atan2(b,g);
}