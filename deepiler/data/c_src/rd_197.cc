#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(f,e);
e=ceil(e);
f=fmax(g,c);
while(isless(a,g)){
f=(log(a))*(tan(a));
e=sqrt(b);
g=fmax(c,f);
c=fdim(c,d);
f=(fdim(g,c))+(exp(g));
}
while(isgreaterequal(a,a)){
g=(fmin(a,c))-(fmin(f,f));
a=atan2(a,g);
b=(tan(d))*(floor(d));
d=exp(e);
f=(fmax(a,d))*(atan2(a,a));
}
}