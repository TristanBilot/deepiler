#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(a,e);
f=pow(f,e);
e=floor(f);
c=(fdim(f,b))+(exp(b));
f=(floor(e))-(log10(d));
b=(fdim(c,e))+(floor(d));
b=(pow(c,f))/(ceil(f));
while(islessequal(b,d)){
b=atan2(c,e);
d=floor(a);
f=fmin(c,f);
d=log(f);
a=fdim(e,e);
}
}