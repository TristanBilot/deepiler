#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(g,f))/(sqrt(b));
g=(fmax(b,c))/(fmin(e,c));
c=pow(a,e);
g=fmin(e,e);
e=fmax(f,h);
h=fmax(h,g);
c=acos(c);
b=(fdim(d,f))/(fmin(g,f));
d=(sqrt(a))+(fmin(d,a));
c=floor(b);
g=(floor(a))-(fmin(h,a));
}