#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(log10(a))+(ceil(d));
d=fdim(d,b);
while(isgreaterequal(d,a)){
f=pow(b,c);
d=atan(f);
e=(atan2(d,c))-(fdim(b,a));
c=(cos(d))/(pow(a,e));
a=pow(e,e);
}
while(isgreaterequal(e,a)){
f=(asin(d))*(fmin(a,b));
a=(atan(d))+(sqrt(f));
e=pow(a,e);
}
}