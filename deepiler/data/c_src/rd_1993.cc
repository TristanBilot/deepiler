#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(c,h))-(cos(a));
g=(fmax(h,f))-(fmax(a,b));
h=atan(g);
a=(atan2(a,b))-(atan(f));
e=(ceil(h))*(fmin(d,g));
c=(pow(d,g))/(fmin(g,c));
a=exp(b);
d=(exp(b))/(ceil(e));
while(islessequal(h,c)){
d=(acos(d))+(exp(e));
f=(tan(d))*(sqrt(b));
}
}