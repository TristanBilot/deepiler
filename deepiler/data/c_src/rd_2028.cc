#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(h,b);
d=(fmax(d,b))+(acos(a));
if(isless(b,b)){
h=(exp(a))/(fdim(c,f));
f=fmin(g,h);
d=fmin(b,e);
c=asin(e);
f=log10(f);
}
else{
h=(sin(g))-(fmin(a,f));
g=(log(a))-(tan(c));
g=atan2(d,e);
h=exp(b);
}
f=cos(d);
g=(fdim(a,d))-(tan(d));
a=ceil(f);
b=(log(f))+(sqrt(h));
}