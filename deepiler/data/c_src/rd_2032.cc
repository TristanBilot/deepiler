#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(f,c))-(atan(c));
e=atan2(b,a);
a=ceil(g);
e=sin(b);
while(isgreaterequal(d,f)){
f=log(d);
d=log10(b);
g=(fmin(g,e))-(pow(g,d));
}
if(isless(c,d)){
c=(log10(c))-(fmin(g,e));
b=fmin(d,a);
d=cos(g);
a=asin(c);
}
}