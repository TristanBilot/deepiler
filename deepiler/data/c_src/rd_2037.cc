#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=sin(a);
d=atan2(a,e);
while(islessequal(d,c)){
c=fmin(e,e);
d=(fmin(e,d))-(fmax(d,b));
a=(pow(e,b))*(fmin(b,b));
a=pow(e,a);
b=ceil(a);
}
d=log10(b);
a=(log(b))*(atan2(d,e));
d=(atan(a))/(asin(d));
}