#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(e,d);
f=fmax(a,e);
g=atan2(f,d);
c=asin(g);
f=fmax(d,a);
while(islessgreater(f,a)){
f=fmin(f,d);
b=(exp(e))+(floor(e));
f=ceil(e);
d=pow(g,f);
d=ceil(d);
}
c=pow(b,c);
d=fmin(f,c);
e=(ceil(b))/(pow(b,g));
b=(fdim(e,g))*(sqrt(e));
a=tan(g);
}