#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(tan(c))/(pow(d,a));
a=(atan(f))*(log(d));
a=asin(f);
while(isgreaterequal(a,b)){
b=fmin(b,a);
c=fdim(f,f);
f=(log10(f))+(fdim(b,c));
}
while(isless(d,e)){
c=(atan(b))+(atan2(c,e));
a=atan(c);
}
}