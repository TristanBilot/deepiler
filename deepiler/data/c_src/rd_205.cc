#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(c))*(fmax(e,a));
g=pow(b,a);
c=exp(c);
if(islessgreater(d,d)){
b=fdim(g,b);
d=fdim(d,f);
b=(sin(a))*(pow(e,g));
g=pow(e,d);
}
while(islessgreater(d,a)){
c=(atan2(f,f))/(sqrt(e));
d=atan2(d,b);
f=(log(b))/(tan(f));
b=fdim(f,g);
e=atan2(f,f);
}
}