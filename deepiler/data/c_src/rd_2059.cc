#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(b,a))/(pow(b,a));
b=(fmin(e,d))/(atan(e));
b=(fdim(b,d))-(floor(d));
d=pow(b,c);
c=atan(b);
d=fdim(a,e);
b=(ceil(d))/(cos(a));
c=(exp(b))-(fmin(c,d));
a=log(e);
c=(fmax(c,b))/(ceil(a));
if(islessgreater(b,a)){
c=(acos(d))+(cos(b));
a=atan2(e,a);
}
else{
a=fmax(c,c);
c=ceil(c);
b=cos(c);
a=fmax(e,b);
a=atan2(e,e);
}
}