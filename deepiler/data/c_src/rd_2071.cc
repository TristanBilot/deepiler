#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(b);
a=atan2(a,c);
e=fmin(a,d);
while(isless(e,b)){
c=(acos(e))/(log(a));
e=acos(b);
e=(fmax(d,a))*(atan(b));
c=(sin(e))*(atan2(c,b));
}
if(islessequal(c,c)){
e=(fmin(b,e))/(pow(c,e));
d=fdim(b,d);
}
else{
c=(pow(a,a))-(fmin(a,c));
b=exp(b);
}
}