#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmax(b,a))/(log10(a));
e=atan(b);
while(islessgreater(f,g)){
g=(log(a))+(pow(f,c));
g=(exp(g))+(fmin(b,c));
b=floor(g);
g=(fmin(f,c))-(pow(g,e));
}
e=(pow(a,a))*(fmin(f,g));
e=(exp(c))+(fmax(b,g));
e=acos(f);
e=(pow(a,e))*(fmax(d,b));
g=(pow(g,g))*(acos(b));
}