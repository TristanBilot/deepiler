#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log10(d))/(pow(d,a));
e=fdim(c,a);
b=log(c);
b=(asin(b))/(log10(c));
while(islessequal(a,a)){
d=atan(d);
b=floor(e);
}
if(isgreaterequal(a,d)){
e=(fmin(b,b))/(fdim(b,d));
e=asin(d);
}
else{
e=(exp(c))*(log10(a));
b=(fmin(e,e))/(atan2(d,e));
d=asin(d);
c=floor(a);
}
}