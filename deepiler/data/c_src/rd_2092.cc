#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=pow(c,c);
e=exp(f);
e=pow(d,e);
g=(cos(f))-(pow(e,e));
d=fmax(f,c);
c=sqrt(f);
e=atan2(c,c);
while(islessgreater(b,e)){
a=acos(g);
f=exp(f);
}
}