#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(f,b))-(fmax(d,a));
f=ceil(c);
b=fmin(d,d);
f=(fdim(b,c))/(cos(b));
b=(floor(d))-(exp(c));
while(isless(a,f)){
a=fmax(c,d);
d=acos(f);
e=pow(e,d);
a=(log(f))+(log(d));
b=pow(b,a);
}
while(isgreaterequal(c,b)){
c=(atan2(d,d))/(fdim(d,e));
e=(atan2(e,e))/(floor(d));
b=(acos(f))*(exp(b));
d=(sin(b))+(atan2(e,f));
d=floor(d);
}
}