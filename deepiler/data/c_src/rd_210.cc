#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(c,b);
c=fmin(a,f);
f=(acos(a))+(log(a));
f=(fmax(a,d))-(tan(d));
if(isgreaterequal(e,c)){
b=(asin(a))/(fdim(a,b));
f=ceil(f);
e=(log10(e))-(pow(a,b));
c=fdim(b,b);
}
while(islessgreater(e,a)){
b=pow(b,a);
c=fmin(f,c);
f=floor(b);
c=atan(b);
}
}