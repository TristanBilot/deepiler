#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(b);
e=fdim(c,d);
a=log10(a);
b=(fmax(c,b))*(pow(e,b));
if(islessequal(b,e)){
a=fmin(b,b);
c=(fmin(b,e))/(atan2(a,a));
c=log(b);
}
else{
b=fmax(c,a);
c=atan2(e,d);
d=(fdim(b,c))+(atan2(c,c));
a=fmin(a,d);
b=(log10(e))-(sin(b));
}
while(isgreaterequal(d,e)){
a=(fmin(e,a))*(log10(b));
c=(log10(b))-(asin(a));
a=(ceil(e))-(fmin(b,e));
a=(atan2(b,d))/(fmax(e,d));
b=fmin(d,a);
}
}