#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(e,b))*(log(e));
c=(ceil(a))*(fdim(a,e));
c=(sin(b))*(asin(c));
if(isgreaterequal(g,e)){
d=floor(c);
f=pow(c,d);
e=floor(f);
}
else{
d=(fmax(f,e))-(fdim(d,g));
e=(fmax(e,c))/(fmin(c,a));
}
if(islessgreater(f,a)){
c=floor(g);
a=(acos(b))+(log(b));
a=(cos(d))*(acos(b));
d=fmin(g,c);
a=atan2(d,g);
}
else{
e=fmax(g,e);
c=(atan2(b,f))+(sin(d));
d=asin(d);
}
}