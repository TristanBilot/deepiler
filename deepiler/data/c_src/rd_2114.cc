#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(b);
a=log10(g);
d=fdim(h,b);
b=(fmin(a,g))+(ceil(c));
while(isgreaterequal(h,b)){
b=log(e);
h=(acos(c))-(pow(a,f));
f=fmax(b,e);
a=fmax(f,e);
b=atan2(b,e);
}
if(isless(h,d)){
c=floor(e);
g=fmax(h,h);
e=(fmin(f,c))*(fmax(a,b));
}
else{
c=atan2(b,f);
f=log10(c);
}
}