#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmax(e,h))/(fdim(c,c));
d=fmax(h,f);
g=atan(a);
a=(log10(a))*(fmax(e,b));
g=fmin(c,e);
while(isgreaterequal(g,b)){
c=(sin(b))+(fdim(a,e));
f=sin(e);
h=(fdim(d,g))+(floor(f));
f=(cos(b))-(exp(b));
e=(asin(d))/(tan(h));
}
c=fdim(g,d);
b=(floor(h))+(log10(a));
a=(log(h))-(atan2(b,h));
}