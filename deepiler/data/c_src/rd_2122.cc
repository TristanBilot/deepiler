#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=sin(f);
f=(log(b))*(fdim(b,c));
f=(atan2(c,a))/(pow(a,g));
e=fmax(c,e);
while(isless(a,d)){
b=(cos(a))*(pow(f,b));
d=(pow(g,e))*(atan2(a,g));
g=(fdim(a,a))/(log(d));
c=(sqrt(a))+(exp(g));
}
f=sin(d);
b=(pow(c,a))+(acos(d));
e=pow(c,b);
d=(asin(e))*(pow(d,e));
}