#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan2(b,g);
g=(fdim(f,a))*(exp(e));
c=atan2(b,f);
if(isless(c,c)){
b=(tan(a))-(atan(a));
g=(tan(e))/(sin(g));
}
else{
f=(pow(d,c))-(pow(c,a));
f=exp(d);
b=(pow(f,a))/(sin(e));
g=atan2(b,a);
a=(log10(e))/(atan2(g,g));
}
while(isgreaterequal(b,g)){
e=(fmin(d,f))*(floor(d));
a=(cos(a))-(exp(g));
c=(acos(g))/(sin(c));
e=(asin(c))-(log(f));
g=fmax(c,a);
}
}