#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmax(g,f);
b=fmax(b,a);
a=pow(e,g);
a=(atan2(e,b))*(exp(e));
d=ceil(c);
if(isgreaterequal(f,b)){
e=(atan2(c,c))-(tan(d));
g=(floor(f))*(log(a));
d=fmin(a,d);
d=atan(b);
d=(floor(g))+(fmax(d,b));
}
else{
c=fmin(a,f);
f=(pow(g,g))+(log(f));
f=(cos(g))-(fmin(e,g));
d=(atan(a))+(log10(g));
f=(pow(g,b))-(atan2(e,b));
}
if(isless(f,e)){
f=fmin(b,b);
c=(pow(e,a))-(atan2(e,a));
e=log(d);
}
}