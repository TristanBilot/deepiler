#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(c,b);
h=pow(g,d);
g=fmin(d,d);
b=pow(e,c);
e=(atan(d))+(acos(a));
a=(pow(d,e))/(sin(c));
f=log10(b);
b=(fmax(c,e))/(acos(h));
if(islessgreater(a,g)){
e=(fdim(e,e))/(log(e));
b=floor(g);
f=floor(c);
g=(pow(e,f))/(pow(c,f));
b=sin(h);
}
else{
e=atan(c);
g=(asin(a))-(atan2(c,g));
b=(log(f))*(atan(b));
}
}