#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fmax(d,c);
d=log10(c);
d=(acos(f))/(fmin(e,b));
g=(floor(a))/(sin(d));
g=pow(a,f);
f=(fdim(f,a))*(sqrt(a));
e=pow(c,a);
a=(fdim(f,f))-(log10(d));
d=acos(f);
f=acos(g);
f=(log(e))/(fmin(g,d));
b=sqrt(g);
d=(atan2(g,d))*(fdim(c,e));
}