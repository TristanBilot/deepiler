#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(b);
d=pow(a,b);
while(islessgreater(a,a)){
b=sqrt(d);
d=log10(a);
}
a=pow(c,a);
d=fmax(d,a);
c=(atan2(e,d))*(pow(a,b));
b=(fmin(d,a))+(atan(b));
e=ceil(e);
}