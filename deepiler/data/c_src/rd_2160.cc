#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(floor(b))*(acos(e));
c=sin(c);
f=atan2(g,c);
a=(pow(g,d))+(atan(d));
f=(fdim(d,g))-(sin(a));
b=(exp(c))-(atan2(f,e));
e=(pow(g,b))-(ceil(g));
c=(atan2(f,f))/(fdim(g,b));
if(islessequal(c,a)){
f=pow(g,a);
f=(log10(e))*(tan(d));
}
else{
a=(fmax(c,b))/(fdim(b,d));
f=(fmax(g,b))/(pow(f,e));
}
}