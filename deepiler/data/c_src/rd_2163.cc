#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmax(f,d))*(ceil(b));
b=fdim(g,c);
a=log10(g);
c=(pow(a,f))/(pow(c,d));
while(isless(b,f)){
b=(sin(c))+(fmin(e,g));
e=(fmin(c,c))-(acos(g));
a=(floor(g))*(pow(g,c));
f=fmin(c,a);
}
g=floor(a);
f=atan2(g,b);
c=acos(c);
e=fmax(d,g);
}