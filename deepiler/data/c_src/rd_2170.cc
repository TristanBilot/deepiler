#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(pow(d,g))/(fdim(b,a));
b=atan2(g,f);
f=fmin(g,a);
b=(fdim(e,f))/(fmax(c,b));
e=(atan2(b,b))+(floor(f));
d=(fmax(d,b))+(asin(d));
g=(atan(b))*(fmax(a,f));
f=fmax(e,e);
if(islessequal(d,c)){
c=(fmax(b,c))/(atan2(f,g));
e=fmax(b,g);
b=(fdim(g,b))*(atan(c));
}
}