#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(f,c))+(fmax(b,c));
d=(fdim(c,c))+(fdim(b,f));
b=(fmax(b,a))+(atan2(a,d));
f=(sqrt(c))+(acos(a));
c=fdim(d,f);
if(islessgreater(c,a)){
a=ceil(a);
f=(atan2(b,c))+(ceil(d));
}
}