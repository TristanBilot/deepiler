#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(e,d);
d=(sin(e))+(exp(c));
e=fmin(d,c);
if(isless(a,d)){
c=(ceil(b))+(asin(d));
c=(sqrt(b))-(fmax(d,e));
c=fmax(d,e);
d=(fmin(a,d))*(log(a));
d=fmin(a,c);
}
else{
b=(fmin(e,e))*(atan2(b,c));
e=pow(b,c);
c=(fmax(d,c))-(pow(c,a));
e=(cos(b))-(cos(a));
c=atan2(a,c);
}
a=cos(e);
a=(asin(e))*(fmin(d,d));
b=(fdim(d,c))*(fmax(c,e));
}