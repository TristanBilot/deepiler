#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(a,c))/(cos(d));
b=fmin(a,c);
a=(fmax(d,a))+(pow(c,c));
a=(pow(e,b))-(ceil(c));
b=(atan2(d,b))+(atan2(a,d));
a=fmax(e,c);
c=fmin(e,b);
a=exp(e);
a=(pow(e,a))*(fmin(e,d));
}