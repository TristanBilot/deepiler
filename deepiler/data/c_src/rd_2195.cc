#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(c,h))/(fmin(f,c));
a=cos(c);
d=floor(b);
a=fdim(a,h);
g=(floor(e))/(ceil(b));
g=(log10(a))/(fdim(d,c));
g=(fmin(f,h))+(ceil(a));
b=(sin(g))/(log10(g));
f=(ceil(e))-(fmax(f,g));
f=atan(h);
if(isgreaterequal(b,d)){
h=(sqrt(f))/(fmax(d,c));
c=(atan2(b,b))+(fdim(a,e));
}
}