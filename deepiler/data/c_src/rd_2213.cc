#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(a,a);
b=(pow(c,c))*(fmax(d,a));
b=(acos(d))/(fdim(e,d));
a=(atan2(e,c))+(fdim(c,e));
c=asin(a);
d=(pow(c,e))/(fmax(a,c));
d=fmax(b,c);
if(islessequal(c,a)){
b=tan(d);
e=(atan2(d,d))*(atan(d));
a=atan(a);
c=tan(c);
}
}