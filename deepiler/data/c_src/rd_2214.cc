#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan(a);
f=(fmax(a,b))-(asin(a));
b=atan2(b,b);
c=log10(f);
d=(fmin(f,b))*(log10(c));
f=fmin(a,b);
d=(exp(e))+(fmax(a,d));
if(isgreaterequal(d,c)){
c=ceil(b);
e=acos(c);
}
else{
f=atan(c);
c=(sin(b))/(pow(a,b));
}
}