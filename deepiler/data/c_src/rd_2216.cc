#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(h,b))*(log10(e));
e=pow(f,d);
c=(fmax(c,c))+(exp(d));
b=log10(f);
e=pow(d,e);
f=cos(d);
while(islessgreater(h,c)){
a=(pow(h,e))-(sin(b));
h=(fdim(b,h))-(atan2(e,f));
}
}