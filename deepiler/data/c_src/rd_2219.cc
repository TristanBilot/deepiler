#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmin(c,d);
b=log10(a);
e=(fdim(b,e))+(fmin(e,c));
d=(fmax(c,c))/(atan2(c,a));
e=(pow(d,b))+(fmax(c,d));
d=ceil(d);
b=(tan(a))+(tan(e));
b=atan2(e,a);
while(isless(b,c)){
d=exp(e);
c=(fmax(b,a))-(fmin(c,e));
}
}