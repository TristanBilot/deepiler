#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmax(a,b);
b=(pow(c,d))/(acos(e));
if(islessequal(f,c)){
c=fmax(d,e);
e=tan(b);
}
else{
e=(floor(a))/(floor(e));
b=(fmax(e,b))/(asin(d));
a=fmin(a,b);
d=(atan(e))+(fmax(f,d));
}
f=exp(c);
c=(fdim(e,b))/(acos(f));
d=fmax(c,a);
}