#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(b,c);
c=(fmin(d,b))+(sqrt(d));
c=(cos(e))+(cos(a));
e=(ceil(b))-(floor(c));
while(isgreaterequal(d,c)){
a=(fdim(e,a))*(log10(c));
d=(pow(b,a))/(asin(d));
e=(exp(e))/(atan2(e,e));
b=(sqrt(e))*(fdim(c,c));
c=(fmin(b,c))/(sqrt(c));
}
if(isless(b,c)){
c=fmin(b,a);
a=(fmax(a,b))*(atan2(e,d));
}
else{
c=pow(b,a);
e=tan(d);
e=atan2(a,a);
b=(fmax(b,a))+(floor(b));
}
}