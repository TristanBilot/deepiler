#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(f,c);
g=fmax(f,b);
g=(sqrt(g))/(fmax(c,a));
g=(atan2(a,a))-(fdim(b,f));
h=fdim(c,h);
a=sin(g);
h=(fdim(d,f))*(asin(c));
b=fmax(b,d);
c=fdim(e,c);
a=(fdim(a,a))/(ceil(f));
while(islessgreater(e,g)){
g=(cos(f))*(pow(c,d));
b=(pow(c,a))/(fmin(h,e));
}
}