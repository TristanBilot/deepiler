#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(g,b))*(atan(d));
a=(log10(a))/(atan2(d,f));
e=(pow(c,a))+(ceil(a));
if(islessgreater(e,c)){
c=atan2(b,e);
a=atan2(g,g);
d=(cos(f))/(cos(a));
}
if(islessequal(g,e)){
e=atan2(f,d);
f=(fdim(g,b))+(log10(c));
f=(atan2(e,b))+(atan2(g,c));
f=fmin(e,f);
}
else{
g=fmax(f,c);
d=fmax(e,e);
f=(sqrt(g))*(tan(c));
b=(asin(b))+(floor(b));
f=(log(d))-(fmin(d,e));
}
}