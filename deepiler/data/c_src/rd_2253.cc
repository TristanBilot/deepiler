#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=asin(h);
g=(fmax(h,b))/(exp(e));
f=pow(a,f);
b=log(h);
while(isgreaterequal(e,f)){
c=asin(g);
f=(fdim(e,c))/(fmax(h,g));
h=(exp(h))-(fmin(f,h));
e=(exp(c))*(acos(h));
a=pow(c,a);
}
if(isgreaterequal(c,g)){
f=(cos(c))/(fmin(a,d));
d=(atan2(h,d))-(fdim(f,h));
g=(asin(c))-(atan(d));
}
else{
c=ceil(d);
f=(atan2(e,d))-(ceil(e));
b=sin(c);
b=(fmin(d,c))/(cos(c));
}
}