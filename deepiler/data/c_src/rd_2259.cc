#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=log10(a);
c=pow(b,f);
f=(asin(e))/(fdim(e,b));
b=(pow(c,e))*(acos(d));
c=log(b);
while(islessequal(a,b)){
b=floor(a);
d=(atan(d))*(pow(b,f));
f=(tan(f))+(fmin(a,f));
f=atan2(d,e);
e=atan(b);
}
if(islessequal(d,e)){
e=pow(b,f);
a=fmin(a,b);
a=log(f);
e=sqrt(a);
c=fmax(c,d);
}
}