#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan2(f,f);
d=atan2(a,a);
d=sqrt(g);
b=pow(d,c);
e=atan2(g,g);
while(isless(b,d)){
d=(log(c))*(fmin(a,g));
e=sqrt(g);
c=log10(c);
a=(pow(g,g))*(acos(g));
}
while(islessequal(a,b)){
e=(cos(b))+(tan(b));
e=fmax(g,e);
}
}