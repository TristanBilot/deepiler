#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(b,b);
d=(fmax(b,a))*(acos(f));
f=log10(f);
if(isgreaterequal(a,f)){
d=fmax(c,b);
f=(fmin(c,a))/(fmin(d,a));
a=sqrt(d);
b=(pow(b,a))*(fmax(b,e));
e=(fmax(e,c))/(fmax(b,b));
}
else{
d=(pow(f,e))+(pow(d,c));
d=(tan(c))/(atan2(e,a));
f=(pow(c,b))/(tan(a));
d=(pow(a,f))/(fdim(e,e));
a=sqrt(e);
}
a=(ceil(a))+(pow(d,e));
d=(pow(b,f))+(tan(c));
e=(fmax(b,b))*(fmin(e,d));
c=(fdim(c,f))/(pow(c,e));
e=sin(f);
}