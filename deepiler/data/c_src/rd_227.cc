#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fmax(a,c);
d=(atan2(g,a))/(log(c));
d=sin(a);
while(islessgreater(a,c)){
a=floor(c);
c=(fdim(g,c))*(atan2(a,f));
e=(floor(b))/(fmin(d,d));
}
if(islessequal(f,d)){
c=(fmax(f,c))-(log10(c));
a=(atan2(e,c))+(cos(f));
c=ceil(c);
d=floor(a);
}
else{
a=(fmin(g,g))*(log10(c));
e=(asin(e))+(asin(g));
}
}