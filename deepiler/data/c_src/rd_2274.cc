#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(pow(f,g))/(cos(e));
e=(asin(f))+(cos(f));
d=atan2(b,b);
d=(tan(d))*(fmax(d,g));
d=log(h);
d=fmin(a,h);
a=(pow(a,a))+(fmax(c,f));
if(islessequal(g,f)){
g=(log(c))*(floor(a));
d=(pow(f,c))/(sqrt(a));
a=fmin(d,g);
g=(pow(d,d))*(pow(g,e));
c=fmin(e,g);
}
else{
a=tan(c);
c=log(g);
a=(pow(b,b))*(log10(d));
}
}