#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(c,d))+(log10(a));
a=fdim(e,a);
a=(sqrt(a))+(log(a));
c=fdim(d,d);
b=asin(b);
while(islessequal(d,a)){
c=atan2(c,b);
d=fdim(b,b);
}
if(isgreaterequal(a,b)){
d=(cos(a))/(fmax(c,d));
b=(acos(a))-(atan(b));
b=(pow(d,a))+(fmax(a,e));
b=pow(a,d);
}
}