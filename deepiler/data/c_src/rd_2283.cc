#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(e);
c=(pow(d,b))/(pow(b,a));
while(isless(a,a)){
e=log(c);
c=(tan(a))+(fmin(d,c));
e=(acos(c))-(pow(b,a));
d=(log10(a))+(sin(b));
}
a=pow(a,b);
a=(fdim(c,b))+(ceil(c));
a=acos(e);
}