#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(e,g))-(fdim(f,d));
f=(fdim(e,b))*(fmin(d,a));
b=(fdim(g,g))+(cos(c));
e=sin(d);
if(isgreaterequal(c,f)){
c=(fmin(a,d))*(fmin(g,g));
g=(acos(g))-(fdim(d,d));
c=(log10(g))/(pow(c,c));
}
else{
d=asin(g);
f=asin(e);
a=(tan(b))/(atan2(a,d));
c=(ceil(f))-(fmax(a,a));
}
while(islessequal(g,g)){
f=(exp(d))-(pow(c,d));
b=(ceil(d))+(fmax(a,c));
g=(acos(f))-(cos(a));
b=(log10(c))+(atan(a));
}
}