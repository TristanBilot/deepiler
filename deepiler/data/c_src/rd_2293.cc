#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmin(e,b);
a=asin(c);
c=atan2(a,e);
if(isgreaterequal(b,c)){
a=fmin(d,a);
a=(fmax(a,e))*(atan(a));
f=(ceil(b))*(pow(f,d));
}
f=(fmax(d,f))*(exp(d));
d=floor(c);
a=pow(e,f);
a=(asin(c))/(fmin(d,e));
d=(asin(d))+(cos(c));
}