#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fdim(f,e))/(asin(a));
c=fmax(a,a);
while(islessgreater(g,e)){
b=(fmax(d,e))+(atan2(a,d));
c=fdim(g,a);
e=atan2(c,c);
g=sin(c);
}
if(islessequal(a,g)){
d=(log10(d))*(fmin(c,f));
f=(asin(a))+(fmax(d,a));
g=(fdim(b,c))*(exp(b));
}
else{
g=(fmin(e,e))*(fmax(c,e));
b=(atan2(a,a))/(acos(c));
}
}