#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(c,b);
a=(fmax(c,c))*(sin(c));
while(islessgreater(d,a)){
d=(cos(d))/(atan2(e,a));
c=cos(a);
}
if(isless(c,b)){
d=atan2(b,a);
d=(tan(c))+(pow(b,b));
e=(fmax(b,a))-(fmax(b,d));
c=exp(b);
}
}