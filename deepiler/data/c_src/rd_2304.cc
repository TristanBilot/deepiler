#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(c,b))*(sqrt(h));
g=cos(d);
d=exp(g);
e=exp(a);
while(isless(e,d)){
a=cos(f);
c=(atan(h))-(fmin(h,d));
f=asin(d);
f=log(b);
}
if(isless(e,e)){
g=log10(a);
h=fmax(a,h);
g=acos(b);
c=pow(h,h);
h=(fmax(e,e))-(atan2(e,b));
}
}