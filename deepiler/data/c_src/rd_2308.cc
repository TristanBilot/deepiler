#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(tan(e))+(asin(b));
h=exp(c);
e=(ceil(h))/(sqrt(g));
f=(atan(b))-(sqrt(c));
while(isgreaterequal(b,g)){
g=(log10(a))/(atan2(a,a));
f=(cos(f))/(sin(f));
}
c=(atan2(f,f))/(sqrt(d));
g=atan2(h,c);
a=(fdim(e,e))+(asin(d));
d=(ceil(h))+(fmax(c,d));
}