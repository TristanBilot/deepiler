#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(c,b);
c=cos(b);
c=(acos(d))-(fmin(a,c));
d=asin(b);
if(islessequal(a,e)){
e=fdim(d,c);
d=(fmax(a,c))*(fmax(d,b));
d=fmax(b,b);
}
}