#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(g);
e=(fmax(f,g))-(floor(f));
c=fmax(h,e);
f=(floor(a))/(log10(d));
b=(sqrt(a))*(ceil(c));
while(islessequal(a,e)){
d=atan2(f,h);
a=(exp(b))/(cos(h));
g=(fmin(a,a))/(fdim(h,h));
b=(fmin(h,b))+(tan(d));
}
if(islessgreater(d,b)){
e=(pow(b,c))/(pow(b,d));
d=fmax(g,a);
e=pow(a,d);
a=(log(c))+(pow(d,b));
}
else{
f=exp(g);
e=pow(d,f);
}
}