#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=sqrt(b);
f=(fdim(e,c))-(ceil(e));
d=(log10(d))*(sin(c));
if(islessequal(d,e)){
b=pow(f,e);
f=pow(a,f);
d=(asin(c))+(fmax(a,c));
a=fmin(e,a);
}
if(islessgreater(b,e)){
f=(cos(a))-(atan2(a,f));
b=(fmax(c,c))-(cos(c));
d=atan(a);
}
else{
d=(sqrt(b))-(fmax(b,f));
a=log10(d);
}
}