#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(tan(b))*(fmax(e,c));
c=pow(a,b);
b=(pow(f,e))-(pow(c,d));
b=fmax(e,f);
h=pow(a,c);
if(islessequal(c,c)){
b=pow(b,c);
c=(fmax(a,f))*(acos(c));
}
else{
a=atan2(e,e);
a=(sin(e))+(fdim(g,c));
g=log10(d);
}
while(islessgreater(d,g)){
b=(fmax(d,d))*(atan2(b,c));
c=(log(c))/(fmin(b,a));
e=(fmax(g,h))/(tan(b));
g=(ceil(d))-(pow(c,f));
}
}