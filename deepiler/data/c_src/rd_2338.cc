#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(e,g))/(sin(d));
e=(atan(a))+(fdim(a,b));
if(isless(a,e)){
a=asin(a);
d=fmax(e,g);
}
d=atan(c);
g=(fmin(f,e))*(asin(d));
d=(fdim(d,a))/(fdim(d,d));
}