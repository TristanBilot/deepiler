#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(e,c))*(fmin(c,e));
d=(log(g))/(fdim(b,b));
c=(pow(a,e))*(fmin(c,d));
c=floor(a);
e=pow(d,b);
a=pow(d,c);
a=atan(a);
e=(tan(d))*(fmin(f,g));
c=exp(f);
}