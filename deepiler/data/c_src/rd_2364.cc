#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(c,f);
f=sin(g);
b=asin(c);
e=(sin(a))*(pow(f,d));
d=(fmax(e,f))/(fmin(h,e));
while(isless(b,g)){
h=fmin(a,g);
d=(log10(b))+(log(d));
}
if(islessgreater(a,c)){
c=(ceil(a))*(pow(f,a));
f=log10(g);
g=sin(b);
}
}