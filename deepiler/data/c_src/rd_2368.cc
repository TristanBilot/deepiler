#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(acos(a))+(fdim(a,a));
d=pow(g,b);
e=(fdim(c,d))*(atan2(d,f));
if(isless(g,c)){
a=(exp(g))/(tan(d));
e=floor(f);
}
else{
c=fdim(g,d);
g=atan2(f,b);
c=(fmin(e,b))-(pow(a,a));
g=(tan(f))*(fdim(g,e));
}
a=(ceil(g))/(asin(d));
f=(fmax(f,b))*(acos(b));
}