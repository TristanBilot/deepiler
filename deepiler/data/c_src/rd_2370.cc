#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(b,d);
e=(log10(b))/(fmax(e,b));
b=(pow(e,a))/(sqrt(c));
c=floor(b);
c=(log(b))+(atan2(b,e));
d=(exp(b))*(atan2(d,c));
e=fdim(a,b);
if(isless(e,d)){
a=(floor(c))+(fmax(c,c));
a=exp(a);
e=(pow(b,a))*(acos(b));
}
}