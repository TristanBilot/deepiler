#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(a,e);
d=atan2(b,c);
b=floor(b);
a=fdim(d,d);
b=(sin(e))/(ceil(e));
while(islessequal(e,c)){
c=(atan2(d,c))/(fmax(e,b));
c=fmax(e,a);
d=pow(d,d);
c=(atan2(c,b))/(log10(b));
}
if(islessgreater(c,b)){
b=(sqrt(e))-(asin(c));
a=tan(d);
}
}