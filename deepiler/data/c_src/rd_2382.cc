#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(e))-(fdim(a,e));
c=atan2(d,a);
e=(atan(d))*(pow(c,a));
e=(fmin(e,b))/(fmax(b,e));
a=(atan2(d,b))+(tan(b));
while(isgreaterequal(e,a)){
e=ceil(d);
b=(fmin(b,b))+(fmin(c,c));
d=(fdim(d,a))+(fdim(d,d));
}
if(islessgreater(e,e)){
a=pow(d,d);
b=(sqrt(d))+(acos(c));
d=fmin(c,c);
}
}