#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(c,a))+(floor(e));
g=(fmin(f,c))-(atan(a));
b=fmin(g,g);
b=(pow(c,c))+(fmax(e,c));
e=(sqrt(b))/(fmax(c,b));
b=(log(c))*(sin(c));
while(isgreaterequal(g,d)){
g=atan2(g,d);
f=(fmin(c,a))+(atan(d));
e=(log10(e))*(fmin(c,b));
d=sqrt(f);
g=atan2(e,d);
}
}