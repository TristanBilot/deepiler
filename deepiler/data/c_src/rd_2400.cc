#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(d);
f=fmin(f,d);
if(isless(e,f)){
f=(tan(b))+(atan(g));
b=(pow(e,a))+(fmax(h,a));
c=log(c);
g=fmax(c,c);
d=fmin(g,g);
}
else{
b=log10(b);
c=(fmin(h,d))-(sqrt(e));
d=(fdim(d,h))-(fdim(d,h));
g=fmax(e,c);
f=(atan2(a,d))-(asin(d));
}
if(islessequal(h,a)){
b=acos(f);
a=cos(b);
a=fmax(b,b);
a=(pow(b,d))-(tan(g));
h=(fmax(b,a))*(fmax(d,g));
}
else{
g=(atan(e))*(exp(c));
h=exp(e);
a=atan2(h,g);
b=(ceil(a))/(floor(b));
c=(pow(h,b))/(fmin(a,b));
}
}