#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(b,b);
g=atan2(d,e);
g=(atan2(a,b))*(acos(e));
d=(fmax(b,h))*(tan(f));
a=tan(g);
c=exp(h);
h=sqrt(c);
a=pow(g,c);
a=(atan2(a,e))-(fmin(f,g));
if(isless(c,a)){
g=(floor(e))*(atan2(c,f));
h=(fmin(a,b))+(exp(g));
}
}