#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(b,a);
a=(tan(d))+(fmin(c,c));
c=(floor(d))-(log(a));
while(isless(d,b)){
c=(sin(e))*(sin(b));
c=log10(e);
}
a=(sqrt(b))*(fmax(e,d));
d=fmin(d,e);
e=(fmin(e,a))*(tan(a));
d=log10(d);
}