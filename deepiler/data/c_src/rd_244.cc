#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(c))-(fmax(a,f));
a=(atan2(d,d))-(sqrt(d));
d=(pow(c,c))*(cos(e));
if(isgreaterequal(a,e)){
f=(asin(e))*(fmax(b,d));
e=(cos(d))+(sqrt(a));
d=sin(b);
a=(sin(d))/(tan(e));
}
else{
c=(asin(f))*(sin(c));
b=(atan2(f,b))-(pow(b,a));
b=fmin(f,a);
c=tan(f);
d=pow(c,e);
}
f=(pow(b,e))/(fmax(c,c));
a=(acos(b))*(pow(a,f));
}