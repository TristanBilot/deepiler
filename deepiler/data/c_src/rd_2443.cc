#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(a,c))*(atan2(a,e));
d=(atan(h))-(fdim(e,f));
c=(sqrt(a))*(log10(g));
d=(acos(g))+(fmin(f,a));
e=(sqrt(g))+(fdim(b,g));
c=(acos(g))+(cos(h));
g=asin(f);
h=fdim(e,e);
if(isgreaterequal(e,e)){
a=atan2(c,a);
c=atan2(h,h);
c=sqrt(h);
f=fmax(e,b);
}
else{
c=exp(h);
d=acos(c);
b=(ceil(a))+(fdim(e,h));
h=(acos(e))/(log10(d));
}
}