#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmin(e,g))*(floor(e));
h=sqrt(b);
a=(atan2(d,c))+(pow(a,a));
a=(sin(c))*(fmax(c,g));
if(islessgreater(h,e)){
f=(acos(f))+(log(h));
c=(atan(f))*(log(d));
}
c=acos(h);
a=(sin(a))*(fmin(d,d));
g=(log(g))+(pow(h,c));
}