#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(d,c))-(acos(e));
b=exp(b);
if(isless(e,b)){
d=(fmin(a,d))-(fdim(e,c));
d=tan(d);
}
while(islessgreater(c,d)){
b=(fmax(e,c))/(log10(d));
c=(floor(a))*(fdim(b,e));
b=(pow(c,e))-(log(b));
c=(acos(e))/(pow(d,b));
a=(pow(c,d))/(exp(b));
}
}