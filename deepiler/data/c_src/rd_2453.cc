#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sqrt(a))/(exp(a));
f=atan(a);
e=(tan(e))-(fmin(c,g));
while(isgreaterequal(f,g)){
h=(fmin(a,f))*(atan(f));
h=fdim(b,h);
f=(fmin(a,b))-(asin(c));
}
g=fmax(e,c);
c=(log10(g))-(sin(d));
a=(pow(e,d))+(log10(b));
g=(asin(d))*(fmax(h,c));
g=(fmax(c,c))*(fmin(f,g));
}