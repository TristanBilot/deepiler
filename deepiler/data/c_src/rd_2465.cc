#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(cos(a))-(floor(b));
d=(asin(b))+(fmax(c,e));
while(isless(b,f)){
e=(asin(a))+(fmax(a,c));
b=(floor(d))*(fmax(e,f));
c=sqrt(b);
d=(exp(c))+(pow(g,b));
}
c=(pow(e,f))+(acos(f));
b=(fdim(f,f))-(acos(a));
c=(atan2(b,b))+(atan2(c,c));
d=(fmin(d,e))-(exp(b));
a=(atan(d))/(sin(f));
}