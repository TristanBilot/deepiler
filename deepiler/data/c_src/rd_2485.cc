#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(c,c))+(log10(c));
c=exp(a);
b=(atan2(c,a))+(log(d));
c=atan2(a,e);
while(islessgreater(a,d)){
a=(floor(d))-(pow(b,d));
b=(fdim(d,c))/(atan2(e,b));
e=(fdim(a,c))-(fdim(b,c));
a=(cos(d))+(fmin(c,b));
}
if(islessequal(b,d)){
a=(fmax(b,d))+(atan2(a,e));
e=(log(d))-(log(c));
d=fmin(c,c);
e=(log10(b))+(pow(d,d));
}
else{
a=pow(a,d);
a=fdim(b,a);
}
}