#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(b);
a=asin(d);
a=(sqrt(a))*(pow(c,b));
d=(pow(b,d))*(exp(e));
c=atan(c);
a=(atan2(e,a))/(pow(a,a));
d=(cos(b))/(sqrt(d));
a=sqrt(e);
b=(cos(e))*(acos(d));
d=(atan2(e,a))*(exp(b));
a=fdim(c,e);
e=(fdim(c,b))+(fmax(b,a));
c=(pow(a,b))-(fmax(a,a));
}