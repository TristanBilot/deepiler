#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(b,a))*(pow(g,c));
f=(fmin(c,a))-(atan2(d,c));
d=fdim(f,g);
a=cos(e);
if(islessgreater(c,b)){
h=fdim(f,g);
f=log(f);
d=(cos(f))+(atan2(b,c));
a=log(c);
}
else{
d=log(f);
f=(log10(c))+(sin(a));
}
h=(fmin(f,f))-(fdim(d,f));
d=(sqrt(c))*(pow(d,c));
e=fmin(f,e);
f=sin(g);
}