#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=asin(d);
d=asin(c);
e=(fdim(b,a))/(fdim(a,e));
a=asin(b);
a=ceil(c);
a=(exp(c))+(log10(e));
c=fmax(b,b);
c=(sin(e))/(pow(b,e));
while(islessequal(a,e)){
d=(fmax(e,a))/(acos(b));
a=(sin(b))/(fdim(b,c));
e=atan2(e,d);
a=(fmax(e,e))/(pow(d,c));
a=acos(c);
}
}