#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=tan(c);
e=(pow(c,e))*(sqrt(f));
a=(fmax(f,g))+(fmax(g,a));
while(islessequal(b,f)){
f=fdim(d,d);
b=fdim(d,c);
d=(fmin(d,f))-(sqrt(g));
d=sqrt(e);
e=sin(c);
}
f=(exp(f))+(pow(f,f));
c=(fmax(c,a))/(sin(b));
d=(log10(b))+(atan2(b,d));
}