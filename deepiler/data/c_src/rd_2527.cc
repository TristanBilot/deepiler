#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=fdim(c,f);
e=(acos(d))+(pow(c,g));
f=fmin(a,d);
e=exp(b);
d=pow(d,b);
g=(pow(b,c))-(atan2(c,g));
g=(sqrt(b))*(fdim(g,c));
g=(fmax(d,g))*(fmin(g,e));
c=(tan(f))*(fmax(d,e));
d=atan2(g,f);
g=(sin(f))*(fmin(e,a));
e=(atan2(b,g))/(log10(b));
e=(log10(e))-(atan(d));
}