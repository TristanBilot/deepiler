#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(f,h);
h=fmax(g,b);
while(isless(f,c)){
g=log(a);
e=fdim(c,b);
f=pow(h,e);
e=(fmin(f,d))/(sin(f));
a=(fmax(b,b))+(fdim(a,f));
}
if(islessequal(e,a)){
a=(sqrt(b))/(cos(f));
e=fmax(b,d);
e=(log10(h))/(asin(g));
b=sin(g);
f=(asin(h))/(log10(a));
}
}