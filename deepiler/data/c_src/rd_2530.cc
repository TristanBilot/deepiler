#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(c);
c=cos(b);
while(isless(e,h)){
h=cos(g);
b=exp(a);
c=fmin(c,c);
d=(atan(f))+(cos(f));
}
while(islessequal(c,d)){
d=(asin(h))/(tan(e));
f=pow(d,b);
c=sin(a);
e=(fmax(e,b))*(fdim(c,d));
f=exp(g);
}
}