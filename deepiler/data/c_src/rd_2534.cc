#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(e,d);
e=fmax(e,b);
c=(floor(a))/(cos(e));
if(isless(a,e)){
e=(fmin(e,a))-(log(e));
c=(fmax(c,a))-(fmin(b,b));
}
while(islessgreater(d,d)){
e=(log10(e))/(sqrt(b));
e=pow(e,f);
f=fmin(b,e);
f=cos(d);
}
}