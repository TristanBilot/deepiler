#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(c,e))/(sin(e));
e=(ceil(d))-(fmin(c,e));
b=(fmin(b,a))*(acos(e));
while(islessgreater(b,a)){
d=(fdim(a,b))-(fmax(e,a));
e=fdim(c,c);
e=fmin(c,e);
a=fdim(c,a);
}
if(isless(e,a)){
c=(exp(c))/(atan2(c,a));
c=fmin(e,d);
d=fmin(d,e);
d=fmin(a,a);
d=(sin(d))+(cos(a));
}
else{
a=sqrt(a);
e=fdim(d,a);
a=(fmax(d,d))+(sin(b));
a=(atan2(e,e))/(ceil(e));
b=fmax(b,c);
}
}