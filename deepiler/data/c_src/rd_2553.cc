#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(e);
c=fmax(f,g);
a=(floor(h))-(acos(f));
b=(exp(a))-(cos(c));
g=(floor(d))/(atan2(b,b));
f=(fdim(h,f))+(floor(a));
while(isless(e,b)){
c=fdim(a,d);
f=(asin(e))/(log10(b));
a=(fdim(e,b))-(atan2(h,h));
f=floor(b);
d=fmin(f,f);
}
}