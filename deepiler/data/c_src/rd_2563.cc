#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(sin(d))+(sqrt(a));
a=(floor(c))/(pow(d,b));
d=fmin(d,a);
d=(log(a))+(fmax(b,a));
e=fdim(a,a);
while(islessgreater(a,b)){
e=log10(c);
a=acos(c);
d=pow(a,a);
}
while(islessgreater(d,a)){
a=log(e);
d=(fdim(b,a))/(atan2(e,d));
c=(ceil(e))+(pow(a,a));
}
}