#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=ceil(d);
e=(pow(a,d))+(fmax(b,c));
if(islessequal(b,d)){
d=(sqrt(d))+(acos(d));
e=(acos(c))*(atan2(d,c));
b=fmax(e,e);
}
else{
d=fmax(b,c);
a=acos(d);
a=fmax(d,a);
e=exp(a);
e=fmax(b,e);
}
if(isless(b,d)){
a=(pow(e,d))-(pow(b,e));
d=(tan(a))/(atan2(c,d));
b=sin(b);
d=fmin(c,e);
}
}