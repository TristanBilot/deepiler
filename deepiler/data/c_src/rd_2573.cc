#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(b,b);
c=(atan2(f,f))/(fmin(c,d));
if(islessequal(d,b)){
e=fdim(f,c);
e=(atan2(a,b))-(fdim(c,c));
c=(fmax(f,a))*(fmax(f,b));
c=sqrt(a);
}
while(isgreaterequal(e,f)){
e=(fdim(f,e))/(sqrt(f));
f=(fmin(f,a))+(atan2(e,a));
}
}