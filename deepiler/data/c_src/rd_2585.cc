#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=ceil(e);
b=cos(b);
b=(fmax(c,e))+(floor(b));
e=(pow(a,a))-(fmax(c,b));
e=(fmax(d,c))*(fdim(a,b));
e=(sin(c))/(pow(d,b));
b=pow(e,b);
d=(pow(c,d))+(acos(c));
c=fmin(a,a);
}