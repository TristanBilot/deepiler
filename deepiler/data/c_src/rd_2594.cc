#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(d,a);
c=fdim(e,e);
d=exp(a);
c=exp(a);
a=fmin(d,d);
a=(fdim(a,e))/(ceil(c));
b=fmax(d,c);
a=(tan(c))-(pow(b,e));
if(isgreaterequal(d,a)){
a=fmax(b,c);
c=(log(d))+(cos(a));
c=fmax(d,a);
d=(fmax(e,c))*(atan(b));
a=ceil(c);
}
}