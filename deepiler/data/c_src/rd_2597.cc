#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(b,d))*(pow(d,a));
d=(pow(c,b))-(acos(c));
c=(pow(e,a))/(acos(b));
d=(pow(d,a))/(sin(a));
if(islessgreater(c,e)){
d=sin(d);
e=cos(e);
d=(atan2(c,a))+(atan(a));
}
while(islessgreater(a,e)){
e=log10(e);
e=(asin(b))+(fmin(b,b));
}
}