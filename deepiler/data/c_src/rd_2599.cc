#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan2(a,d))+(log10(d));
h=atan2(c,d);
e=(sin(c))/(exp(d));
if(islessequal(d,f)){
f=(fmax(c,c))-(atan2(f,b));
b=asin(d);
}
else{
h=pow(e,g);
f=(floor(d))-(fmin(a,e));
b=(fdim(f,e))+(pow(a,g));
g=atan2(a,g);
}
e=(fmin(h,b))-(acos(d));
c=log(e);
h=atan2(a,h);
g=log10(d);
b=sqrt(f);
}