#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(f);
g=log10(h);
while(islessgreater(b,h)){
h=(fdim(b,g))*(atan2(b,c));
a=(asin(d))*(pow(a,h));
}
if(islessequal(h,e)){
h=(pow(d,a))+(exp(f));
d=log10(a);
a=(pow(e,a))+(log10(e));
}
}