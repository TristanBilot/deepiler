#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(asin(a))-(fdim(e,e));
d=(fmin(b,b))+(exp(a));
if(isless(f,d)){
f=exp(d);
a=(asin(d))+(sin(c));
d=floor(b);
e=atan(a);
}
while(islessequal(b,a)){
f=atan(b);
a=(fmax(c,a))/(fdim(e,e));
}
}