#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(sqrt(a))/(log(b));
g=(fmax(e,g))/(cos(g));
g=fmin(b,a);
g=atan2(f,c);
g=(floor(f))/(atan2(g,e));
while(isless(e,c)){
c=(fmax(c,b))/(fmin(g,b));
c=floor(d);
e=fmin(a,f);
g=(exp(a))-(exp(b));
b=atan2(g,b);
}
while(isgreaterequal(g,a)){
c=fmax(f,c);
f=(fmax(c,c))/(fmin(e,d));
}
}