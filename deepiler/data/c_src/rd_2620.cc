#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(b,d);
f=pow(e,d);
f=(sin(a))/(atan2(e,e));
d=pow(b,b);
if(isgreaterequal(b,e)){
a=(log(e))-(asin(f));
b=(pow(d,f))*(sin(d));
b=fmax(f,a);
}
else{
e=(cos(b))*(atan2(a,b));
d=fmax(a,c);
b=cos(f);
}
while(isgreaterequal(d,e)){
e=fdim(b,e);
c=atan(a);
}
}