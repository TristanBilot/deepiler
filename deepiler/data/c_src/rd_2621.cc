#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(d,b))+(floor(a));
a=(tan(d))+(pow(a,b));
d=pow(c,e);
d=ceil(c);
c=fmin(b,c);
while(islessgreater(a,b)){
a=(pow(b,a))/(fmin(c,a));
e=(asin(b))-(atan2(d,e));
e=(asin(c))-(atan2(c,d));
e=(fmin(b,c))+(fdim(e,a));
b=(asin(e))+(atan2(e,c));
}
if(isgreaterequal(a,c)){
c=atan2(c,d);
d=log10(b);
b=fdim(b,c);
b=fmin(d,d);
b=(fmin(c,a))*(sqrt(c));
}
else{
c=asin(c);
b=(log(b))-(fdim(e,d));
c=(fmin(b,e))/(fmax(d,c));
}
}