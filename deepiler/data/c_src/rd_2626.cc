#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(sqrt(f))-(atan(g));
f=sqrt(e);
g=(atan(c))*(fmax(b,f));
if(isless(d,d)){
g=(fdim(a,e))*(log10(e));
b=(fmax(f,b))/(cos(d));
e=(sqrt(g))/(log(b));
d=atan(a);
e=fdim(f,e);
}
if(isgreaterequal(f,a)){
b=pow(c,g);
e=(atan2(d,b))/(log(a));
}
}