#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(a,f);
g=fmin(h,a);
b=fdim(e,d);
a=(pow(f,a))-(floor(d));
g=atan2(h,d);
if(isless(d,c)){
c=floor(d);
a=pow(h,g);
e=exp(d);
}
if(islessgreater(h,g)){
a=(atan2(h,f))-(fmax(f,a));
h=(pow(f,e))*(fmax(b,g));
a=floor(g);
e=(cos(g))*(fmin(a,c));
}
else{
c=(atan2(b,f))-(atan(a));
h=tan(g);
d=log10(c);
d=(fmin(a,b))*(asin(e));
}
}