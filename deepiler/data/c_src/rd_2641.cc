#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=acos(d);
f=(fmax(f,b))/(fdim(a,b));
b=(fmax(f,f))*(log(f));
e=log10(f);
d=fmax(b,e);
if(islessequal(b,c)){
a=pow(e,f);
c=fdim(a,d);
}
if(isgreaterequal(a,d)){
a=exp(d);
f=(sqrt(d))-(fmax(d,a));
}
else{
c=(asin(c))/(exp(c));
c=(exp(e))*(sin(e));
f=(sin(a))/(sin(b));
d=fmax(b,f);
}
}