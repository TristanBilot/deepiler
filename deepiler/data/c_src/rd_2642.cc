#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log(e);
b=(fmax(f,c))-(fmax(c,e));
b=(ceil(d))-(fdim(f,b));
f=fdim(d,g);
a=fmax(a,d);
while(isless(g,g)){
b=atan2(g,g);
a=fdim(b,a);
f=(floor(c))*(fmax(g,g));
}
e=(fmax(a,c))*(pow(c,b));
e=(ceil(f))-(ceil(g));
g=atan(b);
c=fdim(f,c);
a=sin(c);
}