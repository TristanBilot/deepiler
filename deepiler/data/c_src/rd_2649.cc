#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=floor(f);
a=tan(d);
if(isless(d,f)){
g=floor(g);
e=asin(c);
c=(atan(c))+(fdim(d,b));
e=(cos(e))-(acos(a));
}
a=(sqrt(c))+(log10(e));
g=(fmax(g,a))+(fdim(e,e));
}