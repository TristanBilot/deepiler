#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,c))/(floor(a));
d=(ceil(a))*(exp(a));
a=(pow(c,c))-(fdim(d,e));
e=(cos(b))/(sin(a));
d=(log(a))+(fmin(a,e));
while(isless(b,c)){
b=(tan(b))/(atan2(b,b));
a=(fmax(e,c))+(atan2(c,e));
b=pow(a,d);
d=fdim(b,b);
}
while(isgreaterequal(d,d)){
d=cos(a);
c=atan2(b,c);
b=(sin(c))*(log10(a));
b=(sqrt(a))+(tan(e));
}
}