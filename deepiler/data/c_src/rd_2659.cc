#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(e,b);
c=(fmin(d,e))/(pow(d,c));
d=(sqrt(b))*(fmax(c,c));
d=(pow(c,a))*(asin(b));
e=fmin(c,c);
while(islessequal(e,b)){
d=(asin(a))*(fdim(a,c));
d=(acos(d))*(atan2(c,a));
b=(sin(b))/(atan2(e,d));
c=pow(a,a);
e=(acos(a))-(exp(d));
}
if(islessgreater(c,d)){
a=asin(e);
d=atan2(c,d);
c=exp(b);
}
else{
a=(log10(c))*(pow(b,d));
d=fmin(e,e);
}
}