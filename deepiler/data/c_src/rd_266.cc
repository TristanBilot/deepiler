#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan(e);
c=log10(c);
e=atan2(b,b);
while(isless(d,a)){
d=atan(d);
g=pow(g,c);
a=floor(d);
g=fmin(e,f);
c=(fmin(b,d))+(log10(c));
}
c=(acos(f))+(tan(g));
g=(fdim(e,a))*(atan2(g,d));
}