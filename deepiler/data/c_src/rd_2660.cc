#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(a))+(pow(c,d));
e=(atan(b))*(exp(b));
while(isgreaterequal(c,b)){
e=log(a);
a=atan2(e,d);
c=(atan2(a,e))*(log10(b));
d=(asin(a))-(atan(a));
b=(fmin(e,c))+(fmax(a,b));
}
if(islessequal(e,e)){
d=(cos(c))/(asin(b));
b=tan(c);
e=fdim(d,d);
c=sqrt(a);
}
else{
c=(asin(c))-(exp(b));
d=(log(b))/(floor(a));
c=fdim(d,d);
b=(sqrt(e))-(atan(a));
c=(atan2(c,c))/(ceil(c));
}
}