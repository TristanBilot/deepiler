#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(c,b))-(asin(c));
e=(exp(d))-(floor(b));
e=(fmax(a,c))-(fdim(c,b));
d=fmax(e,d);
while(islessequal(b,c)){
e=(tan(b))+(acos(c));
c=atan2(d,e);
}
a=(fdim(a,c))-(fdim(a,e));
c=floor(e);
b=exp(b);
}