#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(c,b);
c=(tan(e))+(pow(b,c));
b=(fmax(c,d))+(atan2(c,c));
while(isless(d,c)){
d=acos(b);
c=(sqrt(b))/(log(b));
c=(cos(e))/(ceil(a));
}
if(islessequal(e,e)){
b=fmin(a,e);
e=(log(d))-(fdim(c,e));
}
else{
e=(cos(a))-(atan2(c,a));
e=(acos(b))-(log10(c));
a=atan2(a,d);
e=asin(d);
}
}