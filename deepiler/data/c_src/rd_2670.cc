#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(cos(c))+(atan2(c,a));
c=(floor(b))*(log(f));
e=fdim(c,d);
while(isless(d,c)){
c=floor(f);
d=(atan2(f,d))-(tan(a));
c=(atan2(a,e))/(log10(a));
}
while(isless(b,c)){
a=(atan2(d,b))-(log(e));
b=tan(c);
f=(atan(a))/(pow(a,a));
a=(atan(b))+(ceil(a));
d=floor(f);
}
}