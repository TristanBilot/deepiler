#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=tan(c);
a=(fmax(f,e))/(cos(d));
c=tan(e);
f=fmax(f,e);
a=fmin(b,f);
if(islessgreater(c,d)){
a=(floor(c))-(sqrt(d));
d=atan2(f,f);
}
else{
d=sqrt(c);
e=atan2(a,f);
b=(log(c))*(tan(c));
e=(atan2(a,c))*(log10(a));
}
d=log(e);
a=cos(f);
}