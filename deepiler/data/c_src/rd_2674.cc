#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(f,h);
f=asin(f);
h=(fmin(f,a))/(log(a));
if(islessgreater(b,f)){
c=log10(a);
a=ceil(b);
g=(floor(c))*(atan2(a,e));
a=(cos(c))*(fmin(e,h));
}
else{
f=atan(h);
e=(atan(e))+(log10(e));
e=(exp(d))*(ceil(c));
g=(atan2(e,c))/(tan(a));
c=(log10(h))-(sin(g));
}
c=(exp(e))+(exp(e));
g=fmax(g,b);
}