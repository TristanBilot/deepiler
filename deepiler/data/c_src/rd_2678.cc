#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(e,c);
e=(fdim(g,c))+(sin(e));
b=(atan2(e,a))/(fmax(b,g));
c=fmax(g,b);
b=sqrt(e);
d=(pow(b,g))+(fmax(c,c));
c=pow(f,g);
f=(log(c))-(fmin(e,a));
g=ceil(b);
if(isgreaterequal(a,g)){
b=(sin(f))/(atan2(g,g));
d=(fmax(g,c))*(cos(d));
f=(atan(c))+(fdim(a,a));
}
else{
g=(ceil(e))/(fmin(d,a));
c=(fmin(g,g))*(fmax(f,f));
b=(ceil(d))/(ceil(b));
e=(floor(e))/(fdim(c,e));
f=atan2(e,b);
}
}