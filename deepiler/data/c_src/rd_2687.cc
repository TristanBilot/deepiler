#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=exp(f);
d=fmax(c,c);
while(isless(b,a)){
g=(pow(b,a))/(asin(d));
d=sqrt(a);
c=sqrt(b);
}
if(islessequal(c,f)){
g=sin(e);
f=(pow(b,c))+(atan2(a,f));
d=sqrt(g);
a=(log10(a))/(acos(g));
}
}