#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=exp(f);
c=cos(c);
d=fmax(a,a);
f=log(e);
e=fmin(b,a);
c=atan2(d,b);
a=fdim(a,f);
e=(fmax(d,a))*(pow(b,c));
while(islessgreater(d,b)){
a=fmax(d,b);
a=(pow(a,c))+(fdim(a,f));
e=tan(f);
}
}