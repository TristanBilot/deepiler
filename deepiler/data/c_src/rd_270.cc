#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=floor(d);
a=pow(d,b);
while(isless(c,c)){
b=(fmax(e,b))+(fmin(e,b));
e=ceil(d);
b=(pow(a,a))*(sin(a));
a=atan2(e,c);
}
if(isgreaterequal(d,c)){
a=(sqrt(e))*(atan2(e,c));
d=pow(e,b);
d=(atan(d))+(exp(c));
}
}