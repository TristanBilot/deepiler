#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=exp(b);
f=atan(f);
f=(log(a))+(atan2(f,d));
while(islessequal(c,f)){
a=(tan(a))/(fmax(b,c));
d=fmin(c,b);
a=(ceil(a))-(asin(b));
f=fdim(c,a);
}
e=sqrt(e);
b=ceil(c);
}