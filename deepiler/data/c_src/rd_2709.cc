#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(g,c);
d=cos(g);
g=(floor(a))/(sin(a));
e=fmax(g,e);
f=(exp(d))+(atan2(b,b));
a=ceil(g);
d=(pow(b,g))*(atan(e));
g=tan(b);
c=(fdim(h,a))*(log(e));
}