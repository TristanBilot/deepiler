#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log10(b);
d=(sqrt(g))-(pow(e,e));
a=pow(c,g);
a=fmin(c,d);
f=(atan2(c,c))*(fmin(e,g));
d=asin(e);
f=(log(c))-(log10(g));
d=atan2(d,d);
f=sin(g);
b=(fmax(c,f))+(log(a));
}