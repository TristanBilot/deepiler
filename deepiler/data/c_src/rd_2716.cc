#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(exp(a))/(atan2(b,c));
c=ceil(b);
while(isgreaterequal(b,a)){
a=sqrt(d);
e=(fdim(e,e))*(fmax(c,c));
d=cos(e);
d=(ceil(e))*(pow(a,c));
c=floor(c);
}
if(isless(e,b)){
c=(fmin(c,b))+(fmin(a,b));
d=pow(c,c);
b=tan(e);
a=(atan2(e,d))+(atan2(e,a));
d=(sin(d))/(atan(c));
}
else{
c=cos(b);
c=fmin(d,d);
b=(atan(d))-(pow(a,a));
d=(sqrt(c))*(acos(a));
a=(log(c))/(fmax(c,c));
}
}