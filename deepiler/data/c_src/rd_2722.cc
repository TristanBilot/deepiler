#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(e,d))*(tan(b));
b=(fdim(e,b))/(fmin(a,c));
a=(pow(d,b))+(fdim(a,e));
c=sqrt(a);
while(islessgreater(a,d)){
b=log(a);
a=atan2(d,d);
b=(fmax(d,a))*(atan(e));
c=pow(c,c);
}
if(islessgreater(d,d)){
b=log(b);
d=(atan(c))*(floor(c));
d=fmax(c,d);
}
}