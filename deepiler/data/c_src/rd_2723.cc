#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(e,c);
b=atan(b);
c=log(c);
e=asin(b);
while(isless(d,d)){
d=(fdim(e,b))/(fdim(f,f));
a=atan2(b,d);
}
if(islessequal(e,d)){
b=(floor(b))-(asin(a));
c=(fmin(f,b))*(cos(f));
}
}