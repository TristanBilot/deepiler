#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(e,d);
c=floor(a);
c=(floor(f))/(atan2(d,a));
h=cos(c);
g=(fmin(g,g))+(ceil(c));
f=fmax(e,f);
a=fmin(d,h);
while(isless(g,a)){
b=(pow(a,d))-(log10(c));
f=pow(f,d);
b=fmax(e,d);
}
}