#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=floor(b);
c=(atan2(b,f))/(log10(c));
e=(atan2(d,e))/(tan(d));
e=(fmin(a,c))/(pow(c,f));
if(islessequal(f,e)){
b=floor(b);
d=tan(c);
b=(atan2(c,a))/(pow(b,a));
d=exp(a);
d=exp(c);
}
else{
a=fmax(a,b);
c=acos(d);
}
if(isgreaterequal(f,d)){
a=(sqrt(d))+(cos(c));
c=fmin(a,d);
b=(atan2(f,b))+(log10(e));
f=(atan(f))-(pow(b,c));
a=(sin(e))/(atan2(e,d));
}
}