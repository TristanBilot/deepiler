#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(e,e);
c=log10(b);
d=atan2(a,c);
e=fmin(a,c);
e=fmax(c,d);
c=(fmin(e,d))+(tan(a));
c=exp(b);
if(isless(b,a)){
b=(atan2(b,b))/(sqrt(b));
a=(atan2(e,b))*(fdim(e,e));
e=(ceil(d))+(fdim(b,d));
c=cos(a);
b=(fmin(c,c))+(atan2(a,d));
}
else{
c=tan(e);
d=fdim(e,a);
c=(pow(b,a))+(fdim(e,d));
e=fmax(a,c);
}
}