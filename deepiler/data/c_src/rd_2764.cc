#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(sqrt(a))-(ceil(e));
a=fdim(b,f);
f=atan(f);
f=(log10(a))+(fmax(e,b));
c=(fmax(g,b))-(exp(g));
b=log10(d);
while(isgreaterequal(d,b)){
e=(sqrt(b))+(log(a));
b=(fmin(a,f))+(fmin(d,c));
g=pow(a,d);
a=log(c);
}
}