#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(b,d);
b=(sin(c))-(fmax(d,c));
c=pow(c,e);
c=(sin(c))/(asin(b));
b=(ceil(c))*(atan(b));
a=(floor(d))/(fmin(c,d));
c=(fdim(c,d))/(atan2(c,a));
d=log(b);
a=pow(b,b);
b=(pow(a,b))-(fdim(b,d));
d=(log(b))+(asin(a));
a=(cos(d))/(log(d));
e=(sin(e))/(fdim(c,a));
d=(atan(a))+(atan2(c,a));
e=fmax(d,a);
}