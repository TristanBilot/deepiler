#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(asin(e))*(log(d));
c=(fmin(d,f))+(acos(f));
if(islessgreater(e,b)){
e=(fmax(b,a))-(pow(b,c));
b=pow(d,e);
c=fmax(f,c);
c=fmin(a,e);
}
else{
a=(sqrt(a))*(cos(c));
a=(fdim(c,e))+(exp(c));
f=fmin(a,f);
}
if(islessgreater(b,e)){
b=(cos(f))+(pow(e,e));
c=(sin(a))+(fmax(e,a));
}
}