#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,b))/(log(e));
c=sqrt(d);
c=fmax(a,a);
c=atan2(e,a);
if(islessgreater(c,d)){
c=(atan2(a,e))/(cos(e));
d=(fdim(a,a))-(fmax(e,c));
e=atan2(c,b);
}
if(islessgreater(c,e)){
a=(atan(a))*(log10(e));
e=(sqrt(e))+(floor(d));
c=(fdim(a,e))-(log(b));
a=(floor(b))+(sqrt(d));
a=sqrt(e);
}
}