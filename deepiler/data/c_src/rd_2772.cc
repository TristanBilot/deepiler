#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan(c))+(fmax(d,e));
f=sqrt(f);
d=(pow(e,a))+(fmin(a,f));
c=(cos(a))-(fmax(d,c));
h=ceil(f);
b=ceil(f);
b=exp(b);
b=fmin(f,c);
if(islessequal(g,h)){
b=fdim(f,b);
a=atan2(b,g);
}
}