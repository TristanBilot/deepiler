#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(d,d))*(log10(d));
c=(log10(a))+(floor(b));
c=(pow(c,e))+(fmax(d,e));
e=atan2(e,d);
while(isgreaterequal(d,b)){
a=(sin(c))-(pow(c,d));
e=(pow(d,c))-(acos(d));
}
if(isless(d,c)){
d=(sin(d))*(exp(c));
c=cos(d);
e=(atan(b))*(pow(e,a));
}
else{
e=(fmax(d,c))*(cos(b));
e=fdim(b,b);
c=sin(d);
}
}