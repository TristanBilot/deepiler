#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(c,c);
d=fmax(e,d);
c=(fmin(d,a))*(fmax(e,d));
while(islessgreater(e,b)){
e=atan2(b,d);
e=fmax(e,c);
a=atan2(a,d);
}
d=pow(d,d);
d=(sin(b))-(fmax(b,c));
}