#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(g);
e=fmax(d,h);
f=fmax(a,d);
h=atan2(g,a);
g=(pow(f,f))+(atan(d));
e=fdim(b,d);
h=tan(c);
while(isgreaterequal(d,a)){
e=(acos(g))*(fmax(e,d));
a=(log(b))/(fmax(h,b));
}
}