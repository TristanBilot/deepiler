#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(pow(c,a))/(fdim(g,d));
d=ceil(d);
c=(log10(d))/(fmax(e,f));
a=fdim(b,c);
g=atan2(f,a);
f=cos(a);
e=(log(c))*(fmax(f,g));
if(islessequal(f,e)){
b=exp(d);
f=(fmax(c,a))/(tan(f));
a=sqrt(f);
d=fmax(a,b);
}
}