#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(floor(c))*(fmax(c,b));
e=fdim(f,a);
c=asin(d);
f=log(d);
d=(ceil(c))/(floor(c));
b=(fmax(c,g))*(sin(c));
a=fmax(e,e);
f=(sin(f))-(sin(f));
d=(atan(c))-(atan(c));
b=(asin(e))-(atan2(e,c));
c=(fdim(d,f))/(log10(d));
c=(asin(c))/(sin(b));
g=(floor(a))*(fmax(f,b));
}