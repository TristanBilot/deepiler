#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=tan(c);
f=(asin(c))/(atan2(c,h));
h=(cos(h))-(atan2(h,f));
d=fdim(b,c);
if(islessgreater(a,a)){
e=(pow(c,b))+(fmin(f,a));
d=(fmax(b,h))/(asin(c));
c=(fmax(e,d))*(sqrt(e));
}
}