#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(b,f))+(pow(g,c));
a=pow(b,f);
a=sin(d);
a=log(g);
f=pow(c,f);
d=fmin(c,b);
a=tan(c);
b=(fdim(d,f))*(cos(b));
c=(pow(a,c))/(log(e));
g=(pow(b,g))+(acos(e));
g=fdim(g,b);
e=(atan2(c,g))-(log(a));
}