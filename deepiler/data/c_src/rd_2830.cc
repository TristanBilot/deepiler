#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=asin(g);
f=(pow(d,g))+(log(d));
c=cos(f);
b=cos(a);
g=pow(f,d);
g=ceil(g);
b=pow(a,c);
c=sqrt(e);
}