#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(tan(a))/(tan(e));
c=(log10(a))*(atan(e));
d=fmin(c,e);
e=(log10(c))*(log10(c));
c=(fdim(c,e))-(fmax(f,e));
b=exp(a);
c=(pow(b,f))+(sin(b));
while(isless(b,c)){
d=fmax(c,d);
a=fmax(b,c);
d=fdim(e,e);
c=atan2(a,a);
a=sqrt(d);
}
}