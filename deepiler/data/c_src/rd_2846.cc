#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(d,b);
b=fdim(d,e);
e=ceil(b);
if(islessequal(b,e)){
c=sqrt(d);
d=(fmax(d,a))/(atan2(c,d));
a=(fmin(b,b))*(pow(e,a));
a=atan2(e,a);
}
else{
d=log10(b);
a=pow(e,e);
e=atan2(c,a);
}
if(isless(c,d)){
c=(fmin(b,d))*(tan(c));
a=fmax(a,e);
a=(fdim(c,a))-(fmax(b,a));
}
}