#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(c);
e=(floor(a))+(pow(b,b));
a=fmax(a,e);
a=(asin(a))/(atan2(a,a));
e=(pow(a,c))-(sqrt(e));
while(islessgreater(e,d)){
e=sqrt(c);
a=sin(a);
b=(atan2(c,b))/(fmin(a,a));
d=(fdim(a,e))/(acos(c));
b=pow(b,e);
}
if(isgreaterequal(c,a)){
e=(fdim(a,b))-(floor(a));
b=(acos(d))/(floor(e));
}
else{
c=cos(e);
c=log10(e);
}
}