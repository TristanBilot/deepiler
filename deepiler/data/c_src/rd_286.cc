#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(d,g))/(pow(g,g));
b=(sin(a))-(pow(e,a));
c=(atan2(a,h))+(asin(a));
h=(pow(b,g))-(atan2(g,d));
b=(fmax(h,h))-(floor(f));
if(islessequal(b,c)){
d=(fmax(b,h))/(atan2(a,g));
b=(fmin(f,d))+(sqrt(f));
c=(ceil(f))+(fdim(f,f));
}
while(islessequal(g,c)){
a=fmax(d,e);
e=fdim(f,g);
}
}