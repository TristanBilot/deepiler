#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan2(c,c);
b=(fmax(c,b))/(pow(e,f));
c=fmax(e,e);
if(isgreaterequal(d,b)){
f=(fmax(f,f))/(log(d));
d=cos(a);
e=atan2(f,e);
b=(fmin(c,f))*(acos(d));
c=acos(e);
}
if(islessequal(e,f)){
e=(fmax(e,b))/(atan2(b,c));
b=atan2(e,d);
}
else{
b=(atan(f))*(log(b));
b=floor(e);
}
}