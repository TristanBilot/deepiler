#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(a);
d=pow(e,b);
c=(ceil(d))*(sqrt(c));
e=(atan2(c,b))-(fdim(d,a));
e=exp(c);
e=fmin(d,c);
d=fmax(d,b);
while(isless(a,c)){
d=(atan2(d,e))*(log(e));
c=fmax(d,b);
c=atan2(a,b);
b=(log10(b))+(tan(c));
}
}