#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(acos(c))*(log(a));
c=log10(a);
d=(fmax(e,a))*(fdim(a,e));
while(isless(d,b)){
d=(fmax(e,c))-(cos(b));
b=(fmax(c,a))*(cos(b));
a=pow(b,e);
}
if(isgreaterequal(b,d)){
b=(floor(b))*(fmin(a,b));
d=(fmax(e,e))/(fdim(c,a));
}
else{
c=sin(d);
a=fmax(d,e);
}
}