#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(pow(c,a))*(fdim(b,f));
d=fmax(b,e);
b=fdim(f,f);
b=(sqrt(e))*(fmin(f,e));
e=(pow(c,e))+(floor(d));
d=pow(b,a);
f=(floor(e))*(fmin(a,f));
a=(fmin(c,e))-(fmax(a,e));
c=(tan(b))/(fmax(d,a));
d=atan(d);
}