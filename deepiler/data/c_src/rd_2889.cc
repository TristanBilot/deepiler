#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(log(b))+(log(c));
e=pow(f,c);
e=(fmax(a,e))+(asin(c));
while(isless(a,c)){
f=(fmax(b,a))-(log10(e));
a=(ceil(a))-(fmax(e,e));
d=fdim(f,e);
d=pow(c,f);
}
if(islessgreater(f,e)){
f=fdim(c,b);
f=(pow(b,c))+(pow(c,e));
d=(tan(c))+(tan(d));
}
}