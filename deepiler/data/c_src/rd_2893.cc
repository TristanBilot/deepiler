#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(d,d))-(floor(e));
b=(fmax(c,d))*(fdim(b,a));
c=fmin(e,b);
e=(log10(e))+(fmin(d,e));
d=(fmax(a,d))/(atan2(b,a));
if(isgreaterequal(c,b)){
b=(fmax(c,a))/(fmin(a,d));
c=cos(c);
a=(fdim(c,c))+(floor(e));
d=log10(b);
d=(fmax(d,e))-(fmax(b,e));
}
d=(fmax(c,e))*(fdim(e,b));
d=fdim(e,b);
}