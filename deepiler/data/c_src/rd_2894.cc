#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(f,e))+(fdim(c,c));
a=asin(f);
e=(cos(e))/(atan2(b,b));
if(isgreaterequal(d,e)){
a=fmin(e,e);
e=fdim(a,d);
a=(log10(e))+(fmax(c,d));
f=fmin(a,a);
d=(atan2(e,c))*(atan2(b,e));
}
if(islessgreater(f,c)){
f=(exp(a))-(asin(e));
d=atan(b);
}
else{
a=(fmin(d,a))+(cos(d));
c=pow(b,c);
}
}