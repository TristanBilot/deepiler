#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(floor(b))*(fmin(c,a));
g=floor(c);
c=(sqrt(g))*(fdim(a,c));
if(islessequal(b,f)){
g=(sqrt(b))/(fmax(b,f));
e=sin(d);
c=atan2(f,e);
g=pow(e,f);
c=(fmax(g,d))+(pow(c,f));
}
while(isless(e,g)){
e=(fmax(g,e))*(sin(a));
e=(asin(f))/(pow(f,f));
g=atan2(c,e);
a=(fmax(g,g))*(tan(d));
}
}