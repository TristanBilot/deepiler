#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=pow(a,e);
e=atan2(e,a);
d=(atan(a))*(asin(e));
f=ceil(b);
a=(log(f))/(exp(f));
f=(cos(c))+(fmax(e,b));
f=pow(d,d);
b=(exp(f))-(atan2(e,a));
e=atan(c);
}