#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(e,b);
b=fdim(a,c);
g=(log(b))+(ceil(f));
a=(fmax(c,f))+(atan2(b,a));
if(islessgreater(d,e)){
b=(pow(g,g))+(log10(g));
b=(ceil(b))/(acos(d));
d=(pow(g,e))/(pow(c,a));
f=(sin(f))+(pow(a,g));
f=(fmin(e,b))/(pow(a,f));
}
else{
e=(fdim(g,f))/(fdim(c,b));
f=(log10(b))+(fmin(a,a));
}
while(isgreaterequal(c,f)){
a=fmax(d,d);
a=(acos(a))*(asin(f));
g=fdim(e,d);
}
}