#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(atan2(e,d))-(tan(f));
d=fmin(e,g);
f=(fmax(a,b))-(fdim(a,f));
g=floor(d);
b=(log(f))/(fdim(e,d));
g=(fmin(d,d))*(pow(d,d));
f=fmin(c,g);
a=log10(a);
if(isless(f,f)){
e=(fmax(g,d))+(atan2(d,d));
c=atan2(f,d);
}
else{
f=(fdim(f,e))*(acos(d));
a=(atan2(f,a))/(atan(d));
e=(fmin(g,d))*(asin(e));
}
}