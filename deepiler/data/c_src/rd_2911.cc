#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(pow(e,b))/(acos(c));
e=cos(d);
e=(fdim(e,e))+(fdim(d,b));
while(islessequal(f,g)){
f=fmax(b,a);
b=(log(f))/(pow(g,b));
b=cos(g);
f=sqrt(e);
}
if(islessgreater(c,g)){
c=(tan(b))/(atan2(g,c));
e=(fmin(g,g))+(atan2(b,f));
}
else{
d=atan2(b,e);
f=(log10(b))/(atan2(b,b));
c=(fmax(d,a))/(fmax(b,e));
}
}