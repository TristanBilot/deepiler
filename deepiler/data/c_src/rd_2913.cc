#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(a);
e=fmax(e,c);
e=(floor(d))-(atan2(a,c));
c=(floor(c))*(pow(d,e));
e=cos(c);
a=(acos(b))-(atan2(d,a));
a=pow(e,d);
c=(exp(a))*(atan2(d,d));
e=exp(e);
if(isless(b,e)){
e=(fdim(e,b))/(fdim(e,c));
d=(fdim(b,d))-(asin(e));
}
}