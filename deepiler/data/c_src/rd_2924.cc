#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(a))-(pow(d,c));
b=fmin(e,d);
b=cos(c);
c=fmax(b,b);
if(islessequal(d,e)){
d=(fmax(c,b))-(pow(d,b));
d=(atan2(a,c))*(fmax(b,e));
c=(atan(d))-(exp(e));
}
else{
e=ceil(e);
e=sin(a);
}
if(islessequal(d,e)){
c=asin(c);
a=(atan(a))*(ceil(c));
a=fdim(a,a);
b=(fmax(a,e))-(atan(c));
}
}