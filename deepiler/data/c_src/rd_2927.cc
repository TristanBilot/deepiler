#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=exp(e);
c=cos(d);
c=tan(a);
while(isless(c,d)){
b=(atan2(a,b))*(log(d));
a=pow(e,e);
}
d=sqrt(e);
d=pow(b,a);
d=(ceil(d))/(fmin(e,e));
a=exp(a);
c=(atan2(c,b))-(log10(e));
}