#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(c,e);
c=fmin(c,d);
c=(ceil(b))+(fmax(b,c));
c=(asin(c))+(sqrt(a));
while(isgreaterequal(d,c)){
c=(fmin(a,d))/(sin(d));
d=(log10(b))/(fmin(a,c));
a=(exp(b))*(ceil(c));
b=(fmax(e,b))*(ceil(a));
c=(fmax(d,d))-(fdim(b,b));
}
if(isgreaterequal(e,e)){
b=sin(d);
a=(atan2(d,d))*(fmin(b,d));
}
}