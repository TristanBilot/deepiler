#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(d,f);
b=cos(a);
a=(exp(c))*(fmin(f,e));
f=(fdim(e,b))/(ceil(a));
d=(pow(g,h))-(cos(g));
f=tan(g);
b=ceil(g);
g=(pow(d,a))*(sqrt(e));
g=atan(f);
while(isless(c,a)){
g=floor(g);
d=(fmax(g,h))+(fmax(f,f));
}
}