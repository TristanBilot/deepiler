#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(b,a);
c=(floor(a))-(fmax(b,e));
b=(log(b))/(atan2(b,c));
d=(pow(a,e))-(fmax(c,a));
c=(fmin(a,d))*(fmax(a,e));
a=(fmax(c,c))*(fmax(b,e));
while(isgreaterequal(e,b)){
b=(ceil(d))*(sqrt(a));
d=(sin(d))+(fmax(e,e));
}
}