#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(g,d);
f=pow(b,e);
e=(acos(a))-(log(b));
a=(fdim(a,c))/(fmax(d,e));
if(islessgreater(a,b)){
b=(log10(f))/(atan(f));
a=(pow(f,g))*(fmin(d,d));
c=(pow(c,c))/(cos(b));
a=(log(c))*(ceil(b));
b=pow(d,b);
}
if(isless(g,d)){
c=tan(a);
g=(asin(c))*(asin(b));
f=(log(c))/(sqrt(b));
d=fdim(f,b);
}
else{
d=(ceil(d))/(fdim(e,f));
b=(log(d))/(pow(e,c));
g=fmin(g,f);
a=pow(b,d);
}
}