#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=floor(b);
g=fmin(c,f);
c=(pow(e,f))/(asin(c));
d=(pow(e,c))+(atan2(e,c));
c=fmin(f,e);
f=(atan2(d,b))-(atan(d));
e=(fmin(d,f))*(log(a));
d=fmin(c,d);
a=log10(a);
}