#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(d,c))/(atan2(f,c));
c=(cos(g))*(asin(b));
c=cos(e);
c=atan2(a,d);
b=log10(f);
if(islessequal(h,e)){
e=(log10(d))*(exp(f));
a=(fmin(g,f))+(fmin(d,f));
e=(log10(c))/(log(f));
h=(atan2(h,b))+(fdim(e,b));
g=(fmax(a,g))*(fmin(a,d));
}
else{
e=(atan(d))/(fmin(e,d));
e=asin(a);
b=(sin(e))+(atan2(b,b));
b=(atan2(g,e))/(ceil(e));
}
if(isless(g,f)){
d=fmax(c,h);
f=fmin(c,h);
a=(exp(h))*(fmax(c,g));
e=(fdim(a,h))/(tan(d));
d=(sin(d))/(fdim(g,g));
}
}