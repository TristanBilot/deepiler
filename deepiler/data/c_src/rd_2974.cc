#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,a))/(floor(d));
d=log(d);
c=(pow(b,a))-(atan2(b,b));
d=(pow(a,b))*(fmin(d,a));
a=fmin(c,c);
if(isless(c,d)){
e=atan(b);
c=log10(e);
}
if(isless(e,d)){
c=(asin(d))/(fmax(e,a));
b=atan2(d,d);
}
}