#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(b,a))/(log10(a));
e=(pow(g,a))+(pow(d,f));
g=(log(d))-(pow(f,c));
c=asin(g);
g=atan2(b,a);
f=pow(f,d);
b=(floor(f))/(floor(a));
if(isgreaterequal(f,a)){
a=log10(c);
g=(atan2(g,g))*(pow(e,g));
e=sqrt(f);
f=cos(e);
}
}