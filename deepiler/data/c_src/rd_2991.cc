#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(c))-(pow(b,e));
b=(sqrt(a))*(log(b));
e=ceil(b);
b=(fmin(b,b))/(sin(e));
if(islessequal(d,d)){
e=(exp(b))*(log(d));
c=(atan2(a,e))-(pow(b,d));
b=(tan(b))+(pow(e,d));
d=log(b);
d=(fdim(d,b))/(fmax(e,b));
}
}