#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(ceil(e))+(ceil(a));
d=(tan(b))+(fmax(d,a));
b=floor(d);
while(islessequal(e,d)){
b=(atan(e))-(fmin(b,d));
e=ceil(c);
a=atan2(a,c);
}
e=sqrt(d);
b=(fmax(e,b))-(atan2(c,c));
d=fmax(a,b);
c=(pow(d,d))-(floor(b));
}