#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,b))-(pow(e,a));
c=atan(c);
c=(sqrt(a))-(fmax(d,a));
c=floor(e);
d=sqrt(d);
a=cos(c);
c=(fdim(a,c))/(pow(a,c));
while(isgreaterequal(a,a)){
c=cos(a);
b=(log(c))/(fmax(c,a));
}
}