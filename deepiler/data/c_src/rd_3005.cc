#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(a,c);
e=fmax(c,a);
c=atan2(a,c);
a=pow(c,b);
d=(asin(c))*(atan(b));
while(isless(c,b)){
e=fdim(c,a);
a=exp(a);
}
e=(atan2(e,b))*(acos(e));
e=log(d);
c=log10(a);
a=exp(b);
c=(atan2(a,c))*(fdim(c,b));
}