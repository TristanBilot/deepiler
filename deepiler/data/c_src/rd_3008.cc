#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,d);
d=ceil(b);
while(islessgreater(c,e)){
d=sin(c);
a=(cos(d))*(fmin(e,e));
c=fmin(a,e);
d=(fdim(e,c))/(fmin(a,a));
e=(pow(b,d))/(sin(a));
}
d=exp(e);
e=(sqrt(b))+(pow(e,a));
a=(pow(c,c))-(cos(e));
}