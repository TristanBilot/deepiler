#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=acos(a);
f=(log(e))*(pow(g,f));
c=(exp(a))-(pow(b,b));
f=(atan(a))/(log10(a));
g=(atan2(f,b))-(log(c));
f=floor(g);
b=(atan2(c,g))+(fdim(c,h));
}