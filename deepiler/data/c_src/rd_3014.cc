#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(a,f))-(fmin(e,d));
e=atan2(b,c);
c=atan2(f,b);
while(islessequal(a,d)){
c=tan(b);
f=(fmax(f,e))+(fmax(e,a));
e=(pow(a,f))*(asin(d));
}
if(islessequal(f,f)){
c=(fmax(f,e))/(cos(f));
e=(fmin(f,c))/(log10(c));
a=cos(a);
}
else{
f=log10(c);
d=(exp(c))+(fmin(e,b));
d=pow(c,f);
}
}