#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(e,c);
a=fmin(d,c);
a=pow(b,c);
b=fmin(a,b);
a=pow(c,a);
while(islessequal(e,e)){
c=(fmin(a,a))+(log10(d));
b=fdim(a,d);
c=cos(c);
b=(fmax(e,d))+(pow(e,b));
c=log10(b);
}
}