#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan(e))+(fdim(c,a));
b=fmin(b,c);
d=tan(b);
b=(tan(c))*(fmin(b,b));
while(islessgreater(e,e)){
d=(floor(d))+(fdim(e,d));
d=fmax(d,c);
c=(fmin(e,a))+(log(a));
a=(fdim(d,b))*(fmin(a,b));
c=pow(e,e);
}
e=sqrt(d);
e=(fdim(e,b))/(fmax(d,e));
e=asin(e);
}