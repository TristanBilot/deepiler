#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(g,f))/(fdim(c,d));
f=(sin(f))/(pow(c,c));
e=exp(d);
b=pow(d,d);
c=(fdim(a,d))+(sin(a));
d=tan(f);
f=tan(f);
g=(log10(g))*(floor(d));
c=pow(b,a);
c=exp(d);
f=(fmax(c,f))*(fmax(a,c));
c=fmin(b,d);
}