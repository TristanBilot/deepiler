#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(e,b);
d=(tan(b))*(log(a));
c=(asin(b))+(pow(d,b));
while(isless(c,a)){
e=log10(d);
b=atan2(a,c);
}
e=atan2(b,d);
a=tan(c);
c=fdim(c,b);
e=log10(d);
e=(fmax(b,a))+(cos(d));
}