#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=asin(d);
b=floor(a);
e=fdim(e,f);
f=fmax(a,e);
d=(fmax(e,b))/(ceil(e));
while(islessequal(e,d)){
c=fdim(a,b);
d=(fdim(f,g))/(fmin(c,e));
c=(exp(f))-(atan2(b,e));
}
if(islessequal(c,a)){
b=(sin(a))-(log(f));
e=pow(f,f);
f=(acos(g))*(fmax(e,a));
}
}