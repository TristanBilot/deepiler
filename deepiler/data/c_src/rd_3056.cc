#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(g,g))-(atan2(c,g));
b=(fdim(c,b))-(sin(h));
b=(fmax(g,d))-(fdim(e,c));
h=atan2(b,c);
c=(pow(f,h))*(fdim(d,a));
c=(acos(d))+(atan2(d,e));
while(islessequal(c,h)){
h=floor(b);
c=cos(b);
}
}