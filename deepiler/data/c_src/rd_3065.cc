#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(b,b))-(fmax(c,c));
e=(pow(b,f))/(pow(a,e));
if(islessgreater(e,a)){
e=(atan(f))*(fmin(b,b));
f=(floor(e))*(asin(a));
d=fmin(a,c);
d=(sqrt(b))-(sin(f));
e=sqrt(f);
}
else{
c=fmin(e,c);
e=(fdim(d,f))*(fmin(e,e));
}
f=tan(c);
b=(pow(b,c))+(fmin(f,a));
f=log10(f);
c=pow(b,b);
}