#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=tan(c);
f=(tan(g))+(pow(d,d));
d=(fmax(e,e))+(fmax(e,f));
g=(atan(e))*(asin(a));
while(isless(b,d)){
b=ceil(e);
b=(fmax(d,f))*(atan(b));
f=(fmax(a,a))/(pow(b,a));
}
while(islessequal(a,a)){
e=(cos(g))+(pow(b,a));
a=floor(g);
f=atan2(f,g);
c=fmin(e,c);
}
}