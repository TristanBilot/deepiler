#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=ceil(a);
a=(floor(e))/(log(a));
a=(log10(b))/(cos(d));
a=log(c);
b=(atan2(e,b))+(sin(b));
e=fdim(e,c);
e=fmax(a,b);
if(isless(d,c)){
b=pow(b,e);
b=sin(b);
d=pow(a,d);
a=fmin(e,e);
}
}