#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(exp(g))-(exp(e));
d=fdim(e,a);
while(islessequal(h,a)){
f=fmin(f,e);
e=cos(c);
h=fmax(b,e);
}
d=(tan(a))/(fdim(c,d));
f=fmax(g,d);
e=atan(f);
}