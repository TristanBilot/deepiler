#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(b);
a=(fmax(g,d))/(fmin(d,f));
d=pow(f,e);
b=log10(h);
e=(asin(d))/(atan2(a,a));
while(islessequal(b,g)){
b=asin(a);
f=pow(h,g);
d=cos(e);
}
while(isgreaterequal(a,b)){
a=pow(a,b);
c=atan(h);
g=(atan2(c,g))+(sqrt(b));
}
}