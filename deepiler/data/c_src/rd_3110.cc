#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(c,c);
e=fmin(b,b);
d=fmax(e,a);
e=(fdim(a,d))/(atan(d));
b=fmax(d,a);
c=(cos(a))-(fmax(c,e));
b=(pow(d,b))*(tan(a));
c=(atan2(d,c))+(asin(c));
d=(acos(e))+(fdim(b,d));
b=(fdim(d,d))-(log10(b));
}