#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(asin(e))*(atan2(f,g));
f=(sin(a))-(pow(g,b));
b=floor(g);
d=(acos(g))+(cos(c));
d=(pow(f,e))*(fmax(c,a));
while(isless(d,f)){
c=atan2(d,a);
a=fmin(f,g);
g=(exp(b))/(cos(b));
e=sqrt(e);
}
if(isless(d,a)){
b=(acos(d))*(atan2(e,e));
g=pow(g,g);
}
}