#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fdim(d,b);
f=ceil(a);
e=fmax(a,b);
if(islessgreater(h,a)){
f=pow(a,e);
b=fdim(e,h);
d=asin(e);
c=log10(a);
c=(cos(d))-(fdim(b,c));
}
h=(sqrt(d))*(sin(a));
b=(pow(b,h))/(fdim(f,g));
d=cos(a);
h=log10(e);
}