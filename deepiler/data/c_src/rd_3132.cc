#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(a,c))-(atan2(b,e));
a=(atan2(e,a))/(atan(e));
e=asin(d);
while(isgreaterequal(d,e)){
c=(acos(e))*(pow(b,e));
b=(asin(d))-(cos(e));
}
while(islessgreater(e,a)){
e=(pow(e,e))-(asin(b));
e=(fdim(c,a))*(acos(e));
e=pow(c,c);
}
}