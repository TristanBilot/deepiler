#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(a,h);
f=atan2(f,g);
b=(sin(c))+(fmax(e,d));
c=(exp(f))/(log10(d));
d=acos(f);
g=sqrt(c);
if(isless(a,g)){
e=fmin(h,d);
a=floor(a);
c=fmax(f,e);
f=(asin(g))/(pow(a,h));
}
else{
d=atan(a);
e=acos(a);
h=(atan2(f,g))+(floor(f));
}
}