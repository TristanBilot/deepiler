#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(c,a))-(pow(c,a));
a=fmin(c,b);
d=fmin(e,d);
e=(ceil(e))+(fmin(e,d));
e=cos(a);
d=(atan2(a,e))*(log(e));
c=atan(e);
d=(floor(e))-(ceil(b));
c=floor(a);
d=(log10(c))*(fmax(e,b));
c=(log10(a))-(pow(d,d));
b=log10(b);
}