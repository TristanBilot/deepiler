#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(a,c);
c=fdim(d,c);
e=fdim(b,a);
if(isless(f,a)){
e=(asin(d))/(ceil(g));
b=(sin(g))/(atan(f));
d=(fmax(a,e))-(pow(g,d));
f=(tan(a))*(asin(f));
}
a=fdim(f,f);
g=(acos(c))-(atan2(b,a));
b=(pow(c,d))/(atan2(d,g));
}