#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(ceil(f))-(fmin(b,e));
e=(fdim(f,d))-(tan(b));
e=(fmax(g,b))*(cos(d));
b=(fmin(e,d))+(fmax(c,f));
g=(fmin(b,g))*(fdim(h,c));
if(islessequal(c,h)){
h=(pow(g,a))/(atan(b));
f=fmax(f,a);
c=asin(h);
f=fmax(a,b);
f=(ceil(g))+(fmin(d,g));
}
else{
a=atan2(g,g);
g=(pow(d,h))-(log(e));
h=tan(g);
d=(acos(h))*(pow(b,h));
b=(exp(e))/(exp(c));
}
d=fmax(c,d);
f=(floor(b))/(acos(h));
g=(fmax(c,g))-(fdim(b,f));
}