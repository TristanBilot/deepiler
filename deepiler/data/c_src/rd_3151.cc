#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(a))/(fmax(b,c));
e=sin(c);
if(isless(b,d)){
d=(fmax(e,c))/(fdim(c,d));
a=tan(b);
c=log(b);
b=(cos(d))*(atan(c));
}
else{
e=ceil(c);
b=(fmin(c,c))/(floor(c));
a=(pow(b,d))+(fmin(e,e));
a=cos(b);
}
b=(pow(d,b))-(asin(a));
b=exp(d);
d=(fdim(b,b))*(atan2(e,d));
}