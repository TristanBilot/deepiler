#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(d,d))*(floor(e));
d=fmin(b,b);
c=log(c);
c=(log10(c))-(fmax(c,c));
while(isless(c,e)){
c=floor(e);
e=fmax(c,e);
d=sin(b);
}
a=(fmin(b,d))-(ceil(e));
d=pow(e,b);
}