#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log10(b))/(exp(c));
a=(acos(e))/(tan(c));
b=exp(d);
d=(fmax(c,d))/(pow(e,a));
while(isless(b,a)){
b=fdim(b,c);
a=fmin(a,b);
b=(pow(e,c))+(acos(d));
c=(atan(c))-(fdim(c,a));
d=log(d);
}
e=atan(a);
b=exp(c);
}