#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fdim(g,b);
g=atan2(f,f);
if(isgreaterequal(c,b)){
d=pow(e,g);
g=(fmax(a,a))/(fmax(d,a));
d=sin(f);
d=(log10(c))+(pow(a,g));
}
if(islessequal(c,d)){
c=(acos(c))+(sqrt(e));
a=fdim(d,f);
a=(atan2(b,e))*(sqrt(e));
a=cos(g);
}
else{
b=sqrt(d);
a=(atan2(c,f))+(acos(g));
}
}