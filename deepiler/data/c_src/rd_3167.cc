#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(f,c))/(log10(f));
a=(floor(e))/(sin(g));
while(islessgreater(f,f)){
g=(asin(f))/(asin(h));
h=tan(b);
f=(fmin(h,g))*(acos(h));
f=(ceil(d))-(fmin(d,h));
}
while(islessequal(d,a)){
a=(pow(h,d))*(acos(h));
c=(pow(b,e))/(atan2(g,f));
}
}