#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(e,e);
e=fmax(d,f);
a=(pow(f,c))-(exp(c));
h=floor(d);
f=(floor(c))/(atan2(a,c));
b=(sin(d))+(pow(a,d));
c=exp(d);
a=fdim(d,b);
e=asin(a);
a=asin(h);
if(isless(h,b)){
c=(atan(e))*(atan(d));
d=fmin(e,h);
g=log(e);
c=floor(d);
b=(sqrt(a))-(tan(h));
}
}