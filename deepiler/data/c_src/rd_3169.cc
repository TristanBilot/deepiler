#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(b,f))-(sin(e));
f=acos(a);
while(islessgreater(b,a)){
c=asin(h);
b=fmin(a,b);
e=tan(c);
}
while(isless(g,b)){
a=fmax(h,h);
h=(atan2(a,g))/(fmax(f,e));
b=pow(b,h);
a=pow(a,h);
f=(fdim(d,d))*(fmin(h,b));
}
}