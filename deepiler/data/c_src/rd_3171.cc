#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(d,d);
e=(tan(e))-(exp(e));
while(isless(e,a)){
e=sqrt(c);
c=fmax(b,a);
c=(atan(c))*(atan2(a,b));
b=atan(d);
c=(atan2(a,d))/(fmax(b,a));
}
if(isless(b,d)){
c=(asin(e))-(tan(e));
b=(floor(d))*(pow(c,e));
}
}