#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(b,a))+(fdim(a,d));
c=floor(d);
a=(floor(e))/(fmin(b,e));
d=pow(b,d);
if(islessgreater(d,b)){
b=fmax(c,b);
a=cos(b);
d=(pow(c,a))+(fdim(e,a));
b=fmax(a,b);
}
else{
e=sqrt(a);
a=(tan(b))+(cos(e));
b=(log10(d))/(atan2(b,d));
b=fmin(e,d);
e=(pow(b,c))*(pow(a,e));
}
d=exp(b);
a=atan2(a,b);
c=sin(c);
a=(floor(c))*(ceil(e));
}