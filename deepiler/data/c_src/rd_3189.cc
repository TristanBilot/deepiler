#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=sin(b);
e=ceil(b);
c=(fdim(d,c))-(sqrt(b));
e=(ceil(b))+(pow(c,c));
if(isgreaterequal(c,f)){
c=tan(b);
d=fdim(b,a);
f=(pow(e,b))*(fdim(a,f));
d=(sqrt(b))+(floor(d));
f=(ceil(a))/(log10(a));
}
else{
e=(atan(d))+(tan(d));
f=(pow(d,b))/(pow(d,d));
c=cos(e);
}
f=(log10(b))/(atan2(c,d));
b=sqrt(c);
a=(sin(a))*(cos(c));
f=fmax(a,f);
}