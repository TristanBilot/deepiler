#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan2(g,f);
e=ceil(a);
while(isless(d,c)){
d=(fmax(e,f))/(fmax(b,b));
d=(log10(e))/(exp(g));
e=(pow(d,g))*(fmin(d,b));
a=(log(a))/(exp(a));
}
d=(atan2(b,f))-(fdim(d,a));
c=sin(g);
}