#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(a,c);
e=log10(f);
e=(fmin(a,c))*(fmin(b,h));
c=(asin(a))+(atan(h));
a=fmin(d,g);
if(isgreaterequal(a,g)){
g=(log10(g))/(fmin(a,g));
c=(log10(b))-(pow(c,e));
g=pow(f,f);
}
}