#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(acos(b))*(log10(b));
c=(log10(a))*(fmax(a,d));
c=(atan2(a,b))/(fmax(a,e));
c=(ceil(a))/(asin(b));
b=(fmax(e,e))/(acos(c));
while(isless(a,a)){
a=fmin(a,c);
c=(cos(e))+(floor(a));
e=atan2(a,d);
c=atan(b);
a=(pow(d,a))+(sqrt(e));
}
while(isless(a,a)){
d=cos(b);
e=(atan2(b,b))-(log(e));
e=fdim(b,c);
a=atan2(c,c);
}
}