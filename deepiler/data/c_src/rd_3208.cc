#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(b))/(fmin(a,a));
c=(fdim(e,c))-(log(c));
b=log10(e);
b=asin(e);
e=(pow(d,c))/(fdim(b,a));
while(islessequal(c,b)){
a=(acos(c))/(atan(b));
a=sqrt(b);
b=(fdim(b,c))+(sin(d));
e=(acos(d))-(sin(d));
a=atan(d);
}
if(islessequal(c,b)){
e=fmax(e,a);
d=exp(b);
a=acos(e);
c=sqrt(e);
b=(fdim(e,b))*(log10(a));
}
else{
d=sqrt(c);
e=(fdim(d,d))+(log(c));
}
}