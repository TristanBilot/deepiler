#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(a,e))+(atan(f));
f=log10(c);
g=(tan(b))-(pow(e,e));
while(isgreaterequal(g,f)){
f=(log(g))-(cos(d));
f=atan2(d,e);
}
if(islessequal(e,g)){
c=(atan(g))*(exp(g));
e=(pow(d,d))*(fmax(a,e));
}
else{
b=(fmin(a,d))*(tan(a));
c=(tan(a))/(atan2(c,e));
}
}