#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(g,a))*(cos(h));
f=log(a);
e=(fmax(f,b))*(acos(h));
while(isless(g,h)){
c=tan(c);
g=(atan2(a,b))/(fmin(c,h));
h=(pow(b,h))*(log10(g));
c=(fmax(f,d))*(acos(e));
}
b=(fmin(c,a))-(log10(e));
e=pow(e,d);
}