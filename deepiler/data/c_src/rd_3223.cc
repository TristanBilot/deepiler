#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,e);
e=fmax(a,e);
while(islessequal(d,e)){
a=(fmax(b,c))*(exp(a));
b=acos(c);
a=fdim(d,c);
a=(log10(c))*(asin(d));
}
while(islessequal(b,c)){
b=tan(d);
b=(tan(c))+(sqrt(b));
a=(fmax(a,a))*(fdim(c,a));
}
}