#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=tan(d);
d=(exp(b))*(floor(a));
a=fmax(b,c);
d=(atan2(e,e))+(fmax(c,c));
d=sqrt(d);
if(isgreaterequal(d,a)){
a=(sqrt(d))+(tan(b));
c=pow(a,b);
b=(log10(e))/(exp(e));
}
b=(ceil(a))-(pow(b,e));
e=fmin(c,e);
a=fmax(c,c);
}