#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=exp(b);
a=(atan2(c,b))*(atan2(e,c));
b=atan(d);
c=(atan2(f,a))*(atan2(e,a));
f=(pow(e,e))/(atan(a));
while(isless(a,b)){
d=exp(e);
f=fmax(f,c);
}
while(isless(f,e)){
d=(fdim(a,c))-(atan2(d,a));
e=(log(d))+(pow(a,f));
a=fmin(c,b);
e=(cos(a))/(atan(a));
}
}