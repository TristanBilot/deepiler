#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=asin(a);
g=(pow(f,b))/(fmin(a,g));
if(isless(d,g)){
b=(fmin(a,d))/(asin(a));
d=atan2(b,d);
b=(atan2(c,a))/(atan2(d,c));
}
else{
f=fmin(e,b);
a=sin(b);
c=tan(e);
d=(fmax(a,c))/(sqrt(a));
}
if(islessequal(c,c)){
c=(floor(f))*(fmin(b,a));
a=(sin(f))+(sqrt(d));
f=pow(g,f);
d=(atan(g))/(pow(d,d));
c=floor(d);
}
}