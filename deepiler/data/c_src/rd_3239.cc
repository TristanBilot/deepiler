#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=floor(c);
e=(pow(b,g))*(cos(d));
d=(fmin(f,d))-(fdim(g,b));
if(isless(c,c)){
d=tan(d);
g=pow(d,c);
e=(atan2(g,e))/(log10(a));
}
while(isgreaterequal(d,e)){
g=(log10(d))-(cos(b));
a=(fmax(e,b))/(fmin(g,b));
g=asin(c);
c=atan2(f,g);
}
}