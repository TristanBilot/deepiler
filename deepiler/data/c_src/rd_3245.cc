#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(atan2(a,f))/(atan2(d,d));
d=(log10(c))+(floor(d));
e=(atan2(f,a))/(fmin(f,d));
g=(pow(b,e))-(floor(e));
e=(sqrt(f))/(atan2(c,g));
a=exp(f);
e=(atan2(c,f))+(floor(b));
d=(ceil(g))*(fmax(e,e));
if(isless(d,a)){
b=(fmin(c,a))*(sin(a));
a=(sin(b))-(fdim(g,a));
e=(cos(g))*(pow(d,g));
a=(fmax(g,g))-(pow(a,e));
c=fmax(d,d);
}
else{
g=atan(b);
d=(fdim(f,a))*(sqrt(f));
g=pow(b,c);
}
}