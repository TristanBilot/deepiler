#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(d,f))*(fmax(d,e));
e=fmax(a,b);
d=(fdim(d,c))-(atan2(c,e));
e=(pow(f,f))+(exp(f));
f=atan2(d,a);
e=(sin(d))+(fdim(b,c));
if(islessequal(f,c)){
b=(fdim(c,d))+(fmin(d,a));
e=fmax(d,e);
}
}