#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(ceil(e))-(exp(e));
c=atan2(h,a);
if(isless(b,a)){
d=exp(a);
h=atan2(c,g);
}
a=(sqrt(f))*(sqrt(h));
g=atan2(e,c);
f=log10(g);
g=atan2(d,f);
e=fmax(d,h);
}