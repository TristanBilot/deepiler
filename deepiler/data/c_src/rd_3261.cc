#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=tan(b);
c=(pow(c,c))+(pow(d,e));
d=(atan2(e,d))/(sqrt(b));
a=pow(b,d);
c=fdim(a,b);
a=log10(a);
c=atan2(e,e);
c=log(c);
d=(fmax(b,a))-(fmax(e,c));
d=floor(c);
c=(fmax(b,c))/(ceil(b));
}