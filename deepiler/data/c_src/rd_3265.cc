#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(c,a);
c=(fmax(d,c))/(asin(d));
while(islessequal(d,b)){
c=(pow(c,a))/(pow(c,a));
c=log(c);
b=(fdim(d,d))/(asin(d));
b=fmin(b,b);
c=(sqrt(e))-(pow(c,d));
}
if(islessgreater(a,b)){
a=(tan(e))*(atan(e));
b=atan2(c,b);
d=(fmax(e,b))-(atan(e));
d=(pow(e,c))-(fmin(a,b));
d=(log10(b))*(pow(e,e));
}
else{
c=(cos(d))+(fmax(c,d));
d=exp(a);
e=(exp(b))+(ceil(d));
}
}