#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(c,e))+(pow(a,b));
e=(acos(d))-(atan2(b,e));
if(islessgreater(e,d)){
e=fdim(b,e);
e=acos(e);
a=fmax(c,d);
}
else{
e=tan(c);
b=atan(d);
c=fmin(b,a);
}
if(islessequal(c,b)){
a=(fmin(c,c))*(ceil(a));
a=(fdim(a,d))*(log10(e));
b=(fdim(e,b))/(fdim(a,c));
}
}