#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fdim(g,e))/(fmin(b,d));
f=atan2(e,e);
c=tan(f);
c=pow(h,b);
g=ceil(g);
if(islessequal(f,c)){
h=(log10(d))+(fmin(c,c));
c=asin(g);
g=(pow(c,g))*(exp(h));
h=(tan(e))/(fmin(c,g));
}
else{
g=(cos(e))-(acos(c));
a=tan(d);
d=(pow(b,b))*(fmin(e,a));
g=fmax(e,f);
g=(fmin(e,e))*(fdim(d,a));
}
while(islessequal(c,d)){
e=exp(h);
f=acos(b);
c=asin(e);
}
}