#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=log10(c);
d=asin(d);
d=pow(b,c);
c=asin(a);
f=(atan2(b,d))-(atan2(d,c));
a=fmin(c,e);
while(isgreaterequal(a,a)){
e=fdim(c,f);
d=sqrt(a);
b=(sqrt(f))-(pow(c,d));
}
}