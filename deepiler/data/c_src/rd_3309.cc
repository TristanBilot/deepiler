#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(c,a))*(cos(e));
c=log10(a);
b=fmin(d,e);
d=fmin(b,d);
b=fmin(e,b);
b=(fdim(d,c))+(cos(a));
b=(atan2(e,c))+(fdim(d,b));
a=log(b);
}