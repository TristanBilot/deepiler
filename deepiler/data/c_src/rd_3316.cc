#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan(a);
d=(tan(a))*(log(b));
if(islessequal(a,a)){
b=(sqrt(c))*(sin(d));
b=(pow(d,a))/(atan(e));
e=(fmax(a,b))-(fmin(a,c));
b=(fdim(e,d))*(log10(b));
}
e=fdim(a,d);
c=ceil(d);
e=(ceil(e))*(log10(a));
}