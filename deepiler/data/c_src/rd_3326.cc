#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(d);
d=ceil(d);
e=floor(d);
d=(atan2(c,b))*(pow(b,b));
d=pow(c,c);
d=(pow(d,d))*(tan(d));
a=pow(c,a);
if(isless(d,d)){
d=log(a);
e=(fmax(e,c))-(pow(c,d));
}
else{
a=(fmin(c,a))-(fdim(b,b));
c=(atan2(a,b))+(fmax(d,e));
a=(asin(b))-(cos(c));
}
}