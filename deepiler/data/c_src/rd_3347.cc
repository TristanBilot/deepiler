#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(ceil(a))+(pow(d,a));
g=sqrt(f);
b=log(f);
e=ceil(g);
a=(exp(c))*(fdim(e,c));
if(isless(b,b)){
c=(log10(b))+(log10(f));
a=fmin(f,c);
c=ceil(a);
a=(atan(g))-(fdim(b,c));
g=(fmin(f,c))-(sin(a));
}
else{
b=asin(f);
f=sin(c);
b=(atan2(c,a))+(atan(g));
}
if(islessequal(b,g)){
c=fmax(e,g);
f=tan(b);
f=(ceil(c))+(log(f));
g=floor(d);
d=(ceil(c))-(exp(e));
}
else{
d=(fmax(c,g))*(log(c));
f=(ceil(f))*(atan2(d,a));
b=atan2(e,g);
f=fdim(g,c);
}
}