#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(c,a))-(pow(a,b));
b=(ceil(b))*(tan(b));
c=asin(e);
e=fmax(b,a);
c=(pow(d,a))-(atan2(d,d));
f=(log(c))/(ceil(b));
d=ceil(a);
e=atan2(c,b);
a=(pow(e,c))-(atan2(e,f));
d=ceil(b);
d=pow(c,b);
c=(fmax(f,e))*(fmax(f,b));
b=(fmax(f,c))-(asin(c));
}