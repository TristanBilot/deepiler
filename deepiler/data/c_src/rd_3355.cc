#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(floor(d))+(cos(e));
f=atan2(a,a);
b=atan2(a,f);
while(isless(d,f)){
d=(fmax(b,d))-(log10(g));
e=cos(f);
}
if(islessgreater(e,c)){
g=fmax(d,b);
a=fmax(b,b);
d=(atan(e))-(fmax(f,b));
}
}