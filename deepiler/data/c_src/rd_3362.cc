#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(b,b);
a=log10(b);
e=(atan2(c,d))*(pow(a,d));
while(isgreaterequal(b,e)){
d=log10(b);
e=log10(c);
}
while(isless(c,a)){
b=sqrt(d);
e=log(c);
b=log10(b);
}
}