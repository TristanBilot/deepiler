#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(cos(a))-(fdim(e,f));
c=acos(b);
d=log10(f);
b=(floor(g))-(exp(e));
f=fdim(e,b);
e=(fmin(g,b))*(acos(b));
h=(fmin(b,h))/(fdim(c,a));
a=fmax(a,a);
g=(acos(h))/(sin(f));
h=log10(f);
}