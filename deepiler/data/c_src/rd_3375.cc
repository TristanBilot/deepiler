#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(c);
h=(fmin(b,f))*(floor(e));
b=exp(b);
b=(fmin(f,h))/(fdim(e,d));
if(islessequal(h,h)){
e=(log(b))*(tan(d));
h=(atan2(h,h))*(fdim(a,b));
}
else{
c=ceil(a);
d=atan2(a,b);
f=pow(c,c);
b=(pow(d,g))-(pow(f,f));
c=(fdim(b,f))+(fmin(e,f));
}
if(isless(e,d)){
e=fmax(b,a);
g=ceil(b);
h=(sin(a))+(fdim(f,f));
e=(exp(d))*(fmin(e,b));
g=log10(a);
}
else{
h=fmax(c,e);
e=asin(h);
f=(fdim(g,h))-(fmax(f,d));
}
}