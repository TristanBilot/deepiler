#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fdim(e,e);
f=(asin(b))+(fmax(a,g));
while(islessequal(g,a)){
f=(fmin(f,a))*(sin(e));
b=(sin(g))/(log(g));
}
while(islessequal(d,c)){
d=log(a);
c=(floor(d))*(fmax(f,d));
c=(ceil(e))/(fmax(e,e));
d=asin(e);
}
}