#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=ceil(f);
e=(fdim(e,f))/(atan2(g,e));
a=(cos(g))*(fdim(a,f));
f=fmax(b,e);
while(islessequal(a,c)){
d=(fmax(f,a))+(fmax(d,a));
e=fdim(f,d);
b=(fdim(c,f))*(log10(e));
}
while(islessequal(a,c)){
c=pow(b,f);
e=fdim(c,a);
f=log10(b);
d=fmax(f,f);
}
}