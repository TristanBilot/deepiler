#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log10(b);
c=atan2(e,e);
a=ceil(b);
if(islessequal(d,a)){
e=fdim(c,c);
e=(pow(a,c))*(sin(d));
}
b=sin(a);
e=fmin(e,b);
a=(fdim(e,c))+(fmax(b,a));
e=(pow(b,c))+(pow(a,c));
}