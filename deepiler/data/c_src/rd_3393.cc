#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(c,g))+(pow(e,d));
g=pow(d,h);
while(islessgreater(f,a)){
g=acos(h);
f=fmin(e,f);
}
if(islessequal(c,d)){
c=tan(h);
h=(fdim(f,f))/(pow(g,b));
c=pow(d,a);
}
}