#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(sin(h))*(fmin(d,e));
a=fmin(e,d);
e=(atan2(d,b))*(cos(b));
b=fdim(c,g);
f=(exp(d))+(atan2(h,b));
f=(acos(h))/(fmin(c,d));
f=(pow(e,c))/(pow(d,d));
b=(fmin(c,f))*(acos(f));
h=(fdim(b,c))+(pow(b,h));
c=asin(g);
}