#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(asin(c))+(sin(c));
f=acos(a);
d=fdim(g,d);
a=(fdim(e,f))-(pow(g,e));
e=(log10(g))*(tan(a));
if(islessgreater(e,b)){
f=log10(a);
c=(fmax(a,b))+(cos(d));
}
else{
g=fmax(g,c);
b=(acos(b))/(pow(d,b));
}
if(islessgreater(f,f)){
c=fmin(d,g);
c=(cos(c))+(cos(c));
e=acos(c);
g=sin(g);
}
}