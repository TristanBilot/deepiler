#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(f,f))/(fmin(d,b));
h=(log10(f))*(pow(f,d));
f=cos(c);
if(isless(b,h)){
g=(acos(a))-(atan2(b,e));
h=log(h);
a=pow(g,d);
h=acos(d);
a=asin(f);
}
else{
a=fmin(b,c);
h=(fmin(h,f))+(sqrt(h));
f=log(e);
g=fdim(h,a);
e=atan(d);
}
while(isgreaterequal(c,c)){
e=pow(d,a);
b=(fmin(g,g))*(fmax(a,b));
g=(fmax(h,h))-(log10(a));
a=fmax(e,f);
}
}