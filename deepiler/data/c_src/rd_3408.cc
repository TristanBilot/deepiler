#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(b,e);
b=(pow(a,c))*(fmax(d,c));
c=sqrt(c);
if(islessequal(e,c)){
e=log10(d);
c=(atan2(c,d))-(acos(d));
d=(acos(a))*(pow(c,a));
}
else{
c=fmin(a,e);
a=exp(c);
e=(log10(d))*(fmin(a,b));
}
while(islessequal(e,a)){
e=exp(c);
e=tan(d);
d=fdim(b,a);
d=sin(e);
}
}