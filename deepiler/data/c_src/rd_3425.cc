#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(f);
d=atan2(e,f);
e=(fmax(b,b))-(fmax(f,a));
d=(atan2(h,f))+(floor(b));
f=pow(b,e);
while(isless(c,f)){
d=(fmin(f,g))/(log(a));
d=atan(d);
a=exp(d);
}
while(isgreaterequal(f,c)){
b=pow(e,f);
a=atan2(b,c);
}
}