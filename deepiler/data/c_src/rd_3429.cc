#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(c,c))+(fmin(a,c));
a=atan2(d,e);
e=(asin(h))/(fdim(g,b));
h=fdim(h,d);
f=cos(b);
d=ceil(e);
a=sqrt(h);
c=(fdim(e,b))*(sin(d));
d=(atan(b))-(fdim(g,b));
while(isless(f,b)){
h=(ceil(c))*(atan2(e,c));
c=(sin(g))/(atan2(h,d));
}
}