#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(e,a);
e=fdim(a,d);
d=(log(d))+(pow(e,a));
a=(sqrt(d))+(atan2(d,d));
if(isless(b,c)){
c=atan(b);
d=(fmax(b,b))-(fdim(b,b));
d=sqrt(d);
}
c=(atan2(b,e))*(cos(d));
a=fdim(c,e);
e=(acos(c))-(floor(b));
}