#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(a,d);
e=sin(e);
if(islessequal(d,a)){
c=atan2(b,e);
c=(fdim(e,a))/(ceil(e));
c=(log10(c))-(acos(d));
}
if(islessequal(d,d)){
d=floor(c);
e=(fdim(c,c))-(sin(e));
c=(fmin(b,d))-(fmax(c,d));
a=fmax(e,e);
}
else{
e=(asin(b))+(cos(d));
d=fmax(c,a);
b=(fmin(e,c))-(atan2(e,d));
}
}