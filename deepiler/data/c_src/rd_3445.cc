#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=cos(e);
e=pow(d,d);
g=atan2(a,g);
while(isless(f,g)){
g=acos(c);
b=(atan2(e,e))*(fdim(b,f));
d=asin(a);
}
while(isless(a,b)){
c=(asin(f))-(log10(a));
d=(sqrt(f))*(floor(d));
g=(fdim(e,e))*(cos(b));
}
}