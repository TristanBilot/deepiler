#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(b,c);
d=acos(a);
e=floor(a);
d=fmin(d,b);
b=(sqrt(b))*(fdim(c,a));
c=(acos(c))-(log(d));
a=(sqrt(a))-(log(c));
c=(pow(a,d))+(sin(a));
c=(fmin(d,b))*(cos(e));
a=(pow(a,b))*(fmin(a,a));
b=(atan(b))-(acos(b));
}