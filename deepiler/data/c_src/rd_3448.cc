#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(floor(a))-(atan2(e,e));
f=exp(d);
while(isless(g,e)){
c=fdim(d,c);
a=sin(a);
}
d=tan(d);
e=(floor(c))+(atan2(a,f));
}