#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(f,b))/(fmax(a,d));
f=(pow(f,e))-(pow(c,b));
while(isgreaterequal(f,b)){
c=asin(d);
d=(fmax(d,e))/(ceil(b));
f=(asin(e))/(fmax(a,f));
}
if(islessequal(e,d)){
e=cos(d);
e=fmin(b,d);
d=(fmin(a,a))*(ceil(d));
}
else{
d=fdim(c,a);
e=pow(c,c);
b=(fmax(d,d))*(asin(b));
}
}