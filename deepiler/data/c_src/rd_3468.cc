#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(c,e))/(asin(g));
e=(fdim(f,d))*(atan2(c,b));
e=exp(c);
while(isgreaterequal(e,e)){
b=(atan2(a,f))/(log(d));
g=sqrt(a);
g=fmax(d,d);
a=fmax(b,g);
}
b=asin(c);
c=(asin(f))/(sqrt(e));
a=(acos(f))*(fmax(f,g));
g=(atan2(d,e))/(acos(c));
g=log10(g);
}