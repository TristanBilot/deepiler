#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(pow(e,f))-(atan(a));
a=asin(d);
d=fmin(a,f);
g=(pow(e,b))-(atan(a));
d=(fmax(a,d))-(atan2(f,g));
b=(log(g))+(cos(c));
b=pow(a,b);
while(isless(b,g)){
g=exp(g);
g=fmin(a,e);
}
}