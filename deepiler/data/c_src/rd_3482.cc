#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan2(e,f);
e=pow(d,g);
g=pow(c,f);
c=log10(e);
g=(fmin(e,d))-(floor(g));
c=pow(e,d);
f=(floor(g))*(fmin(f,g));
a=sin(e);
while(isless(a,a)){
b=(atan2(f,g))/(log(g));
e=(sqrt(f))-(exp(e));
d=asin(b);
}
}