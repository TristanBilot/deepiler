#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=floor(c);
a=fdim(c,c);
e=(tan(g))*(fmin(a,d));
d=(log(g))+(sin(c));
e=atan(e);
while(isgreaterequal(b,c)){
a=atan2(b,d);
b=fmax(e,f);
}
if(isgreaterequal(g,a)){
a=(pow(e,b))*(fdim(b,e));
d=fdim(c,b);
b=(cos(g))+(sqrt(g));
e=(cos(e))/(sin(g));
g=fmax(e,b);
}
}