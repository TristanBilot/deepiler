#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(h,c))/(pow(c,b));
b=(fmax(d,b))*(exp(h));
d=floor(f);
h=log(b);
e=(tan(g))/(exp(g));
e=(fmin(b,e))-(sqrt(c));
f=(fmin(f,h))-(pow(h,f));
c=(atan(a))*(fmax(d,f));
if(isless(a,h)){
c=asin(d);
b=(floor(h))/(fmin(c,c));
a=(fdim(f,b))-(fdim(g,a));
e=tan(h);
f=(fmin(g,d))*(sqrt(c));
}
else{
f=fmin(a,b);
b=(fmax(e,d))*(fmax(d,b));
f=log10(g);
}
}