#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(g,c))*(atan(b));
h=(exp(d))-(log(g));
e=(fmin(f,g))*(fmax(b,a));
g=(fmin(a,g))+(atan2(b,d));
if(isgreaterequal(h,a)){
h=log(d);
b=(log10(f))*(cos(g));
c=fdim(g,g);
}
else{
f=(log10(c))+(fmin(h,c));
g=(atan(f))*(log(f));
h=(tan(g))/(exp(h));
f=(floor(d))*(fdim(b,f));
h=(log10(d))*(fmin(e,f));
}
a=pow(b,d);
g=(fmax(g,g))+(sqrt(h));
f=(fmin(g,g))/(sqrt(a));
a=fdim(h,f);
}