#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan2(h,f))/(atan2(d,h));
g=exp(a);
c=acos(g);
b=ceil(c);
c=floor(a);
while(isgreaterequal(a,g)){
f=asin(d);
e=floor(b);
b=(fdim(d,a))+(cos(d));
h=tan(c);
g=pow(a,d);
}
d=(fdim(g,b))*(acos(a));
a=(fdim(g,b))*(atan2(g,f));
c=fdim(g,b);
d=fmax(f,g);
}