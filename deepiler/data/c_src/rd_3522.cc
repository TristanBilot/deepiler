#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmin(d,e);
f=ceil(b);
g=(fdim(f,b))*(pow(b,b));
g=pow(c,d);
while(isless(g,c)){
e=(fmin(e,d))/(cos(g));
a=(pow(b,g))/(atan2(d,f));
}
while(islessequal(d,d)){
f=sin(c);
d=(fmin(a,f))/(pow(d,a));
d=(atan(c))*(atan(d));
b=(sqrt(b))/(fmin(g,a));
}
}