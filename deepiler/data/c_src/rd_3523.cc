#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(d,g))*(atan2(c,c));
b=log10(c);
c=cos(b);
while(isless(a,g)){
d=tan(c);
f=tan(g);
a=(fdim(g,g))*(asin(e));
}
if(isless(a,a)){
c=(asin(b))+(atan2(d,g));
f=atan2(c,a);
}
else{
d=exp(f);
c=pow(b,c);
d=atan2(f,f);
g=(fmax(a,d))+(log10(b));
}
}