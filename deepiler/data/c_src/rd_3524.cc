#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmin(g,c);
f=pow(a,b);
d=(fmin(a,c))*(log(b));
g=sin(d);
b=fdim(a,g);
g=fdim(g,f);
f=log(a);
f=log(f);
if(isless(c,g)){
a=pow(e,a);
a=(fdim(b,c))/(ceil(g));
a=sqrt(a);
f=log10(c);
}
else{
e=(acos(f))-(sqrt(f));
b=pow(d,a);
d=(asin(g))*(cos(b));
}
}