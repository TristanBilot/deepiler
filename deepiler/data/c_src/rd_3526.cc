#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(asin(a))*(tan(c));
c=(atan(b))/(atan2(f,b));
b=fmin(c,b);
if(isless(c,a)){
c=fmin(f,d);
g=(fmax(c,d))+(sqrt(f));
}
f=fmax(a,g);
g=(tan(a))/(exp(d));
b=(tan(e))/(atan(f));
}