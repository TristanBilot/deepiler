#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(e,d))-(tan(c));
c=fmax(b,a);
b=(sqrt(e))/(log(c));
c=(fdim(e,a))/(sqrt(e));
if(isless(d,a)){
c=(fdim(b,a))*(tan(a));
c=fdim(d,b);
b=(cos(d))+(fmax(c,b));
}
b=(fmin(d,c))/(atan2(c,a));
e=(sqrt(c))*(sin(a));
}