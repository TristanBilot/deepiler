#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=sin(e);
c=(tan(a))/(fmin(e,a));
d=(atan2(c,b))-(tan(e));
e=(fdim(b,a))/(floor(d));
if(isless(b,e)){
b=(atan(d))-(exp(a));
c=exp(e);
}
a=(fmax(d,c))+(tan(c));
b=exp(e);
d=floor(a);
b=fdim(a,e);
}