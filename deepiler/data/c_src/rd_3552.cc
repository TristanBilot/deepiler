#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(atan2(a,c))-(acos(e));
f=cos(c);
while(isless(b,b)){
e=pow(g,f);
g=(floor(d))/(atan2(d,d));
d=fmin(c,d);
f=(fmax(g,b))+(acos(a));
}
c=fmin(e,g);
c=fdim(e,a);
a=pow(e,g);
a=exp(c);
}