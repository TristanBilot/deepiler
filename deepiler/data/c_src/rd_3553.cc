#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(e,e);
b=floor(d);
b=(floor(a))*(pow(d,b));
b=pow(a,a);
b=floor(c);
while(isgreaterequal(b,a)){
d=atan2(a,b);
a=fmax(a,c);
a=(pow(b,e))+(fmax(d,b));
}
if(islessequal(e,a)){
d=(ceil(d))-(asin(e));
b=ceil(d);
e=pow(a,d);
}
}