#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(a,e);
h=atan2(g,d);
if(islessgreater(d,c)){
a=ceil(c);
g=log(a);
a=(atan2(g,f))+(floor(g));
f=tan(f);
}
else{
g=(pow(h,f))-(fmax(c,a));
e=cos(d);
c=acos(e);
f=pow(a,d);
g=(fmax(g,a))*(asin(c));
}
if(islessequal(d,b)){
e=fmin(g,a);
e=fmax(a,c);
b=floor(f);
}
}