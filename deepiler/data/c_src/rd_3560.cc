#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(b);
g=(fmax(h,c))+(sqrt(h));
while(isless(d,b)){
h=(fmax(a,a))/(sin(d));
c=ceil(h);
b=(pow(b,c))*(fmin(d,d));
g=(atan2(c,b))+(fmin(d,f));
}
if(islessequal(f,b)){
b=(cos(a))/(atan(f));
e=(fmax(b,f))*(atan2(c,b));
f=(asin(b))/(pow(f,d));
c=(sqrt(f))+(pow(d,h));
}
}