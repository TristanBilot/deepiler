#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(b,b))+(fdim(a,b));
a=(asin(d))+(atan2(a,c));
d=(fdim(b,d))/(acos(c));
b=(ceil(c))*(ceil(c));
d=(log(b))+(atan2(d,c));
c=tan(e);
c=pow(c,a);
if(islessgreater(b,b)){
c=(fdim(a,b))*(acos(b));
b=(fmax(e,a))/(sin(e));
}
}