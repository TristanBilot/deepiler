#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=exp(b);
d=exp(d);
d=asin(b);
d=sqrt(d);
a=(fdim(e,c))-(pow(b,d));
e=pow(c,f);
f=(acos(c))-(asin(a));
c=(fmax(d,c))-(ceil(c));
f=atan2(d,b);
}