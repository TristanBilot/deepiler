#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=asin(e);
f=sqrt(f);
b=log(c);
d=(fdim(c,b))/(acos(a));
while(isless(e,b)){
b=fmin(a,a);
e=(pow(d,f))/(cos(b));
d=sqrt(f);
}
while(isless(a,e)){
a=asin(f);
d=(exp(f))-(cos(b));
d=ceil(e);
}
}