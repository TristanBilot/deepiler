#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(b,d))/(exp(f));
b=(floor(a))-(sqrt(d));
d=tan(f);
if(islessequal(e,d)){
f=(fmax(b,f))+(fmin(d,a));
b=sqrt(c);
e=fdim(c,e);
}
else{
d=(ceil(b))/(fmax(c,f));
b=(sin(f))-(log10(a));
f=(floor(a))/(pow(c,c));
}
if(islessgreater(a,c)){
d=(pow(a,e))-(pow(a,c));
c=pow(e,f);
b=(fdim(d,b))/(cos(e));
}
else{
b=floor(a);
b=fdim(b,d);
}
}