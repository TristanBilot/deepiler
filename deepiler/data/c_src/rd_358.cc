#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(a,e))*(atan2(b,b));
c=(atan2(b,d))-(sqrt(b));
if(islessequal(d,c)){
d=(fmax(a,c))/(log(a));
d=pow(e,a);
}
while(islessgreater(b,d)){
c=ceil(b);
c=(exp(b))/(fmax(c,e));
d=atan2(c,a);
}
}