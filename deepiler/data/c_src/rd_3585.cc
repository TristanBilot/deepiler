#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(f,h);
d=(pow(e,c))/(ceil(b));
d=acos(e);
b=(ceil(c))*(sin(f));
h=(sin(c))-(fmax(a,a));
g=tan(g);
e=(fmin(e,h))-(pow(g,f));
h=(sin(b))-(fdim(c,e));
c=(fdim(f,h))*(floor(b));
c=fdim(f,d);
g=(pow(a,d))*(log(g));
}