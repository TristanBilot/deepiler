#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(c))-(fmin(d,b));
d=tan(e);
c=fmin(b,b);
d=(log(e))/(fmin(c,a));
c=(atan2(d,c))*(fmax(b,a));
a=fmax(c,a);
e=atan2(d,a);
b=fmax(b,a);
while(islessgreater(c,a)){
c=fmin(d,a);
a=pow(a,d);
b=(atan2(e,d))+(sin(c));
}
}