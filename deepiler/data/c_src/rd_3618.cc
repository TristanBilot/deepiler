#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=exp(a);
g=(sqrt(a))*(log(c));
d=sin(g);
d=pow(d,d);
if(isless(d,f)){
a=(pow(b,a))-(log(c));
c=log10(a);
a=(fdim(f,g))/(fmin(e,b));
c=(pow(f,c))*(atan2(b,a));
c=(sin(g))-(exp(e));
}
else{
b=(fmax(e,c))-(ceil(e));
a=fmin(a,a);
e=(cos(f))/(atan(f));
b=exp(d);
}
f=(tan(b))*(exp(c));
e=(fmin(f,g))*(fmax(c,a));
a=cos(d);
d=fdim(a,b);
e=atan2(e,g);
}