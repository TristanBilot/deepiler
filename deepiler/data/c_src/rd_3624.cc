#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(e,c);
d=sqrt(e);
while(islessgreater(e,c)){
e=acos(a);
c=fmin(b,d);
e=exp(c);
}
while(islessgreater(d,c)){
a=(atan2(d,a))-(atan2(a,d));
e=ceil(e);
c=log10(b);
}
}