#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(asin(b))*(atan2(b,a));
c=fdim(d,a);
d=(fdim(b,b))+(fmin(a,e));
while(islessgreater(d,c)){
e=(log10(c))+(pow(e,c));
a=fdim(c,d);
d=(atan2(b,a))*(fmax(b,e));
}
while(isgreaterequal(a,a)){
b=(atan(b))-(tan(b));
b=fmax(b,b);
}
}