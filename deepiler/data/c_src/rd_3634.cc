#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(b);
a=sqrt(c);
e=(atan(b))+(fmax(d,a));
b=log(b);
b=pow(a,d);
while(isgreaterequal(e,c)){
a=(floor(a))/(pow(b,e));
a=atan(a);
a=fmin(a,a);
a=(fdim(d,c))+(sqrt(c));
}
if(islessgreater(b,b)){
a=fdim(c,a);
d=sqrt(c);
}
else{
a=atan2(d,a);
d=(log10(c))/(fmax(a,d));
c=fmax(d,a);
}
}