#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(b,d))/(fdim(g,f));
a=(pow(d,b))-(atan2(g,c));
d=(fmax(e,h))/(pow(a,d));
c=(pow(b,a))/(pow(b,h));
if(isless(a,c)){
h=(fmax(a,c))-(pow(a,f));
c=atan2(f,f);
e=(pow(g,f))+(fdim(c,e));
e=sqrt(a);
c=pow(c,h);
}
while(isless(a,e)){
d=(fdim(b,f))*(atan(c));
e=(fdim(e,d))-(floor(b));
b=pow(g,b);
c=(fmin(h,h))*(exp(a));
a=fmax(h,e);
}
}