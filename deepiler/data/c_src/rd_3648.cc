#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(exp(d))-(fdim(f,e));
a=(cos(c))+(pow(d,f));
while(isless(d,e)){
c=atan2(a,d);
e=atan2(b,e);
e=(atan(d))/(log10(e));
b=sqrt(d);
d=cos(e);
}
while(islessequal(d,e)){
c=fdim(f,e);
d=fmax(a,a);
e=atan2(d,c);
f=floor(d);
a=(fdim(e,b))*(fmax(d,c));
}
}