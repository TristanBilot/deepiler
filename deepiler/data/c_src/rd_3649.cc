#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(b,b);
g=(cos(c))+(fmin(g,f));
f=cos(b);
h=fmax(a,f);
e=floor(d);
a=fdim(d,e);
g=exp(b);
e=(sin(e))/(pow(c,f));
e=(log10(a))*(atan2(c,d));
c=(asin(h))*(fdim(b,a));
while(islessequal(a,e)){
c=(atan(h))-(exp(g));
e=(cos(f))*(atan2(b,c));
f=(log(b))*(cos(a));
a=(sqrt(g))+(atan2(g,b));
b=(fmax(b,g))+(fmin(h,f));
}
}