#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=floor(b);
a=fdim(b,e);
g=acos(c);
g=(atan(f))*(fmin(g,a));
e=fmax(g,a);
while(islessgreater(a,b)){
g=fmin(e,e);
e=(fdim(e,c))*(asin(b));
d=pow(f,g);
}
if(islessequal(d,f)){
g=(acos(d))-(fmax(f,a));
a=(fdim(a,f))*(log10(a));
e=pow(g,c);
g=(acos(e))/(cos(e));
}
else{
a=(fmin(f,e))/(ceil(e));
e=acos(g);
c=(atan(e))+(atan2(d,d));
f=tan(g);
}
}