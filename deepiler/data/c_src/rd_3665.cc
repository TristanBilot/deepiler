#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(b,a))/(fmin(d,d));
e=(log(a))+(fmin(c,b));
b=fmin(b,d);
a=(floor(a))*(fdim(d,b));
while(islessgreater(c,a)){
e=fmin(e,b);
a=floor(e);
a=(ceil(a))-(fdim(b,b));
a=(fmax(e,c))+(log(c));
}
while(islessequal(b,b)){
d=atan(a);
b=sin(d);
c=pow(b,b);
e=(fmax(e,a))*(fmin(e,c));
}
}