#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(b,d);
d=atan(d);
a=(fmin(b,c))/(atan(e));
b=sin(b);
if(isgreaterequal(c,d)){
d=(sqrt(a))/(ceil(c));
d=(fmin(c,e))-(atan2(a,e));
e=(sqrt(e))/(pow(c,e));
c=pow(e,a);
}
if(islessequal(c,b)){
a=(atan2(e,d))-(fdim(e,a));
a=floor(c);
c=fdim(b,c);
e=fmin(b,a);
}
}