#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(c,a))*(asin(a));
b=(cos(e))+(atan2(c,b));
c=acos(c);
b=fdim(d,c);
a=(acos(c))+(atan2(d,d));
if(islessgreater(c,b)){
d=(fmax(d,c))*(fmax(d,d));
a=atan2(e,a);
c=atan2(a,a);
d=fmin(c,e);
}
if(isless(c,c)){
b=(pow(e,d))/(atan(c));
e=fmax(b,a);
c=(log(d))+(fmin(d,a));
a=(fdim(c,b))+(pow(d,c));
c=(atan2(a,e))*(atan2(e,d));
}
}