#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=log(b);
a=fmin(d,c);
c=fmax(b,e);
b=atan2(c,e);
b=(fdim(d,c))/(cos(e));
d=(tan(c))+(asin(a));
c=(acos(d))-(fmax(d,c));
if(isless(a,d)){
d=sin(a);
a=pow(a,b);
a=(acos(c))*(fdim(c,b));
b=(fdim(a,e))/(pow(e,e));
}
}