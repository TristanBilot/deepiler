#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log10(e);
f=(log10(a))/(exp(c));
while(islessequal(b,e)){
c=fdim(f,b);
a=(pow(a,d))/(exp(a));
c=(acos(b))-(atan(d));
d=pow(c,c);
}
c=pow(a,c);
d=cos(f);
e=exp(e);
}