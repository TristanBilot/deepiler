#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(b);
e=(pow(d,b))/(log(f));
b=pow(d,c);
f=(asin(e))+(pow(g,d));
if(islessequal(e,c)){
a=log(d);
f=acos(b);
b=(fmax(a,e))*(fdim(g,g));
}
if(isgreaterequal(b,g)){
f=(asin(f))/(fdim(c,a));
b=(log10(a))-(atan(e));
}
else{
c=tan(c);
b=sqrt(a);
c=(fmax(d,b))+(pow(b,b));
g=(fmax(f,d))-(asin(f));
}
}