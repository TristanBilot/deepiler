#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(log10(e))/(tan(f));
f=log10(b);
d=fdim(c,e);
f=(fmax(e,b))*(fmin(a,d));
while(isless(a,d)){
e=pow(a,a);
d=acos(b);
c=pow(f,e);
}
if(islessgreater(a,c)){
c=(tan(a))/(asin(a));
b=(fmax(c,a))-(pow(f,c));
c=(cos(b))+(pow(a,f));
b=asin(e);
}
}