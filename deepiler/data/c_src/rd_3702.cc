#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(b))+(tan(d));
c=fmin(a,d);
e=pow(c,d);
b=sqrt(d);
if(islessequal(d,a)){
d=fdim(c,c);
b=fdim(c,a);
}
if(islessgreater(e,c)){
c=log10(c);
e=fmax(e,e);
d=exp(e);
b=(fmin(e,e))+(exp(c));
}
}