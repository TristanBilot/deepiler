#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log(b))+(fmax(d,e));
g=atan(b);
while(islessgreater(a,f)){
e=log(c);
g=(tan(a))/(atan2(d,g));
b=atan2(b,c);
g=log(a);
d=(atan2(c,b))+(ceil(a));
}
while(isgreaterequal(c,a)){
b=fmax(g,c);
g=sqrt(d);
d=pow(g,b);
}
}