#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(g);
f=(ceil(e))*(pow(b,e));
e=(pow(e,a))+(floor(d));
g=sin(g);
if(isgreaterequal(e,c)){
a=fmin(e,b);
b=fmin(a,g);
e=fmin(c,d);
f=(sin(h))+(sin(d));
}
f=floor(h);
c=atan2(a,c);
g=(fmax(g,d))/(pow(c,a));
}