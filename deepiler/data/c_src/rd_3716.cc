#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=pow(f,e);
d=cos(f);
f=fdim(a,c);
while(islessgreater(f,f)){
d=fmax(e,a);
c=(fmin(c,c))/(log10(e));
f=log10(c);
b=fdim(f,d);
}
a=ceil(b);
f=(atan2(b,d))-(pow(d,f));
e=(exp(e))+(asin(e));
c=cos(a);
}