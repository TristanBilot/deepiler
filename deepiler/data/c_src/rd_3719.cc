#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(a,a);
b=(atan2(a,a))*(pow(c,f));
d=(ceil(c))-(tan(c));
d=(pow(e,a))+(fdim(a,c));
while(islessequal(f,e)){
b=sqrt(a);
e=(cos(c))*(sqrt(f));
}
a=(pow(f,d))-(exp(b));
c=(asin(c))-(fdim(d,b));
e=(pow(c,f))+(fmin(f,b));
e=atan(b);
d=(atan2(a,a))-(atan2(f,f));
}