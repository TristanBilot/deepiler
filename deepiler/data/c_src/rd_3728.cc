#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(b,a))/(log10(c));
h=(acos(c))+(fdim(e,c));
d=(ceil(h))/(atan2(h,d));
a=(fdim(e,d))/(tan(c));
e=fmin(c,d);
if(islessgreater(h,d)){
e=(fmax(h,f))*(fmax(b,d));
g=(log10(g))/(atan(h));
g=pow(d,f);
f=(fmin(c,a))/(fmax(g,e));
}
d=(fdim(f,b))/(sin(c));
e=(log(f))/(exp(e));
h=tan(h);
b=(fmin(f,b))*(fmin(d,f));
}