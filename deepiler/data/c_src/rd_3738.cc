#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(e))+(asin(d));
e=fdim(f,c);
f=(exp(f))-(cos(c));
c=log10(f);
while(islessgreater(d,a)){
a=(fmax(d,c))/(cos(a));
d=sqrt(a);
c=(asin(a))-(fmin(c,b));
e=fmax(e,c);
d=atan2(e,e);
}
if(isgreaterequal(b,f)){
e=fdim(a,b);
d=exp(a);
}
}