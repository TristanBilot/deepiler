#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(floor(d))+(atan2(d,d));
d=(fmin(c,a))-(sin(e));
e=atan2(d,b);
while(isgreaterequal(a,c)){
e=atan2(e,c);
d=fmin(c,c);
b=fmax(d,d);
}
if(isless(c,a)){
a=floor(e);
d=atan(a);
b=(floor(e))*(atan(b));
}
else{
b=(fmax(a,b))+(fmin(d,b));
a=atan(d);
d=fmin(d,d);
a=(fmin(c,e))+(fmax(a,b));
}
}