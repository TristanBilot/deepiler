#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(a,d);
c=(fmax(a,e))*(atan(b));
c=exp(a);
b=(fmin(c,c))/(cos(a));
while(isgreaterequal(d,b)){
d=acos(b);
e=acos(a);
b=atan2(e,a);
b=(tan(c))*(fmin(d,a));
a=fmax(a,a);
}
a=atan2(b,a);
d=log10(d);
d=(atan2(a,e))*(fmax(e,a));
a=(atan2(b,c))-(fmax(d,a));
}