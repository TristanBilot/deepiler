#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=log10(b);
d=fmin(c,e);
c=fmin(a,e);
f=floor(a);
e=(floor(c))/(log(g));
if(isgreaterequal(e,g)){
b=atan2(f,g);
c=exp(b);
a=(fdim(e,g))+(fmin(g,a));
d=floor(d);
}
else{
b=log10(e);
d=asin(f);
}
g=(fmin(c,e))+(fdim(b,d));
d=floor(a);
g=(fmax(g,d))+(sqrt(g));
f=(fdim(c,e))-(atan2(f,e));
}