#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(c,g))-(atan2(a,a));
f=(asin(f))-(asin(e));
a=fmax(f,a);
if(islessequal(g,c)){
d=acos(h);
d=log10(a);
}
else{
h=pow(b,b);
a=sqrt(c);
b=asin(g);
h=(pow(d,h))*(fdim(b,g));
}
h=(fdim(g,g))+(fmax(h,a));
e=(fmin(b,d))*(atan(f));
c=pow(a,b);
e=pow(e,a);
d=(asin(f))*(pow(g,h));
}