#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan2(d,b);
d=(fmin(c,e))*(asin(g));
d=atan2(d,b);
d=(acos(f))+(fmax(g,b));
e=floor(f);
c=log(e);
a=fmax(a,f);
e=(exp(a))/(ceil(f));
c=(floor(b))+(pow(a,f));
if(islessequal(b,b)){
e=(fmin(c,d))/(sqrt(c));
f=pow(c,g);
}
}