#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(a,d))/(fdim(a,d));
e=(fmax(g,b))-(atan2(g,c));
g=(fdim(a,d))/(fmin(a,e));
b=(pow(b,f))+(fmax(f,d));
d=(fdim(e,d))+(fdim(a,d));
if(isless(c,g)){
d=(tan(a))-(fmin(d,a));
a=fmax(d,f);
a=fmax(g,d);
e=pow(f,g);
}
else{
e=(sqrt(a))+(atan(d));
a=atan(a);
a=sqrt(b);
g=(fmax(e,a))+(floor(d));
}
c=atan2(e,g);
c=(atan(d))/(cos(c));
e=(pow(e,b))+(log10(a));
}