#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(e,d))/(fdim(a,d));
a=atan2(e,a);
e=fmin(a,c);
while(isless(c,e)){
a=fmax(b,b);
e=(fmax(a,c))/(exp(d));
}
b=(pow(e,c))/(fmax(c,a));
b=acos(d);
b=(atan(c))+(log10(b));
a=(cos(b))/(fdim(d,e));
e=fmin(c,b);
}