#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log(e))*(atan2(e,a));
c=(floor(b))+(atan2(c,d));
e=fdim(c,d);
b=(fmax(c,b))+(fmax(c,b));
a=floor(b);
if(islessgreater(c,a)){
c=(fdim(d,a))-(fdim(c,c));
d=atan2(b,a);
a=(tan(b))+(tan(c));
e=(fmin(d,b))-(fdim(c,e));
a=fmax(d,b);
}
else{
a=(asin(a))*(fmin(c,d));
d=fmax(a,e);
c=(sin(c))-(log10(e));
}
while(islessequal(c,c)){
b=tan(d);
a=fmax(e,a);
c=(fdim(b,a))*(log10(a));
c=(fmin(e,b))*(atan(a));
b=(ceil(b))*(fmax(e,a));
}
}