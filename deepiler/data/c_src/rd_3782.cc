#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(tan(f))+(floor(d));
d=(pow(c,a))+(atan(c));
b=(fmax(c,g))+(atan(g));
d=log10(g);
e=ceil(a);
e=(fmin(a,f))/(atan2(f,d));
a=(sin(b))/(floor(f));
if(islessequal(a,a)){
c=fmin(e,e);
d=fmax(d,b);
}
}