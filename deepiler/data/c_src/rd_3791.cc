#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(d,g))+(fdim(g,g));
h=(fdim(g,h))/(ceil(f));
d=ceil(d);
g=(tan(b))/(fmin(e,b));
b=ceil(h);
if(isgreaterequal(f,h)){
h=(atan2(e,e))*(fmin(e,c));
b=(pow(f,f))+(fdim(a,d));
}
else{
f=(atan(a))-(log10(b));
g=(atan(d))*(fmin(h,h));
b=ceil(c);
}
h=atan(e);
g=atan2(a,b);
c=atan2(h,c);
d=(asin(a))/(fmin(a,a));
d=(pow(h,c))+(cos(h));
}