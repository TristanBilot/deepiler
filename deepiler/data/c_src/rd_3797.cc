#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(floor(g))+(atan2(g,h));
b=(sqrt(e))*(floor(g));
e=(atan2(d,a))-(floor(b));
h=fmax(a,d);
g=(exp(c))-(tan(a));
while(isgreaterequal(h,e)){
h=fmax(h,f);
g=atan(f);
}
e=fmin(c,f);
b=atan2(e,g);
}