#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(b,b);
g=pow(c,b);
a=(fdim(g,h))+(pow(f,g));
e=sqrt(e);
f=(fmin(c,b))+(pow(h,a));
if(isless(c,c)){
d=sqrt(g);
f=(pow(e,e))-(atan(d));
b=fmin(b,c);
d=(fdim(d,c))*(exp(c));
a=ceil(a);
}
else{
f=pow(f,b);
d=atan(a);
}
while(isgreaterequal(c,d)){
g=(acos(g))/(atan2(c,b));
g=(fmax(h,c))/(fmax(g,g));
f=cos(b);
h=atan(a);
e=(atan(a))+(fmax(f,c));
}
}