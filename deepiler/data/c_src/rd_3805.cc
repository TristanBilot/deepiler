#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(asin(a))/(fmax(e,b));
e=asin(a);
while(isless(b,d)){
b=asin(d);
e=floor(d);
b=(tan(a))*(pow(f,c));
}
if(isless(a,b)){
d=pow(d,e);
f=(sin(e))/(fmin(f,e));
f=(sin(a))-(log(c));
d=(sqrt(c))*(atan(d));
b=fmax(a,c);
}
}