#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(d,e);
f=cos(a);
while(islessequal(c,g)){
a=floor(b);
f=(fmax(e,f))-(floor(f));
}
d=(asin(e))-(sqrt(e));
f=exp(d);
}