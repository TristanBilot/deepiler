#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(b);
b=(log10(a))*(fmax(e,e));
if(islessgreater(e,e)){
a=acos(b);
a=(atan2(a,a))*(cos(b));
b=atan2(e,e);
}
else{
e=sin(d);
c=pow(a,c);
e=ceil(a);
c=(fmax(d,e))/(cos(c));
c=(log(e))/(pow(b,c));
}
c=(log(c))/(pow(b,d));
b=fmax(e,a);
e=log(c);
}