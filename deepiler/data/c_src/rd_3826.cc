#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(b);
a=log(f);
g=acos(d);
while(isless(d,d)){
a=(fmax(b,f))-(log10(a));
a=floor(g);
d=(sqrt(a))-(asin(c));
}
if(isless(h,d)){
g=ceil(e);
c=(log(b))+(log(a));
d=fdim(h,f);
}
}