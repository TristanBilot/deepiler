#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(c,e))-(sin(a));
c=(fdim(a,a))-(atan2(b,e));
d=pow(c,e);
c=pow(c,c);
b=(ceil(c))-(fmin(d,c));
while(islessequal(b,d)){
b=(log10(a))/(acos(b));
a=(sqrt(e))-(sin(e));
c=(fmin(c,e))+(pow(e,b));
}
if(isgreaterequal(e,d)){
e=atan2(e,e);
c=(fmin(e,c))-(asin(c));
e=(cos(c))+(log(b));
d=pow(c,c);
d=(fmax(a,c))/(log(d));
}
}