#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(c,f))*(log10(b));
e=log10(b);
a=(atan2(a,a))+(cos(e));
f=(ceil(f))*(asin(e));
f=(floor(b))/(floor(a));
if(islessequal(c,e)){
c=acos(d);
e=(fdim(a,b))/(exp(d));
b=(fmin(d,c))+(fmax(e,d));
c=(exp(a))+(asin(f));
}
while(islessequal(d,e)){
b=(fdim(c,d))-(cos(b));
e=(sqrt(a))-(tan(c));
f=fdim(c,c);
}
}