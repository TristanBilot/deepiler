#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan(c);
e=fmin(b,b);
while(isless(d,b)){
b=(ceil(f))*(fmax(d,e));
a=fmin(f,b);
e=(asin(c))+(fdim(c,e));
d=ceil(d);
b=log(f);
}
while(islessequal(a,d)){
f=(log10(c))/(fdim(c,e));
b=sin(c);
}
}