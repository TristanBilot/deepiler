#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(a,e))-(atan2(f,b));
e=(atan2(a,e))+(sin(b));
g=(fmin(c,d))*(floor(c));
f=(fmax(h,b))+(fmin(f,g));
c=(log(a))+(fmax(d,f));
if(isless(f,a)){
d=asin(a);
a=(acos(c))-(sqrt(d));
c=(fdim(c,e))/(sin(b));
e=fmin(f,b);
g=fmin(d,e);
}
else{
g=(sqrt(e))-(fdim(e,h));
h=(fmin(h,d))+(cos(h));
f=pow(h,e);
e=log10(f);
e=(asin(b))*(fmax(a,b));
}
if(isgreaterequal(d,b)){
a=fdim(h,g);
d=fmin(a,b);
}
}