#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=ceil(c);
d=cos(c);
a=fmin(e,a);
if(isless(a,e)){
a=(sqrt(d))/(floor(e));
a=pow(a,c);
b=(cos(e))+(atan2(a,c));
e=(cos(b))/(fmin(b,e));
}
else{
a=(log10(d))*(log(c));
c=fmax(e,d);
b=asin(c);
}
while(isgreaterequal(c,e)){
c=(pow(d,e))*(fmax(b,d));
a=pow(b,b);
a=(sqrt(d))/(pow(e,a));
a=(log10(d))*(pow(d,b));
d=(atan2(b,b))*(fmin(b,e));
}
}