#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(d,g))/(pow(a,e));
g=exp(f);
f=fmin(a,c);
while(isless(g,f)){
h=(pow(c,h))*(fmin(h,c));
e=(floor(b))+(tan(d));
f=log(f);
}
if(isgreaterequal(a,d)){
b=(log(e))/(pow(d,f));
g=pow(c,f);
h=log10(f);
}
else{
b=(fmax(e,a))/(sin(e));
h=(ceil(e))*(atan(a));
h=fmin(a,a);
f=(cos(f))/(fdim(a,d));
}
}