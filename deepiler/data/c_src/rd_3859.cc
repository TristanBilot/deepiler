#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(b);
c=pow(e,g);
a=(pow(f,g))*(fmax(d,a));
if(islessequal(h,a)){
h=(fmax(h,a))+(fmax(g,b));
e=(tan(b))+(pow(b,e));
}
a=asin(b);
g=(fmax(h,f))-(acos(e));
}