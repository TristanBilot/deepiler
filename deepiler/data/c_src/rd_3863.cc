#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmin(c,b);
c=(pow(f,a))+(pow(e,e));
f=atan(e);
e=atan2(f,c);
while(isgreaterequal(c,b)){
b=(sqrt(f))/(log10(a));
b=(sin(e))*(asin(c));
f=acos(e);
}
if(islessequal(c,a)){
c=(pow(c,b))-(sqrt(b));
b=fdim(f,c);
}
else{
b=sin(f);
e=(atan2(d,f))*(atan2(f,b));
}
}