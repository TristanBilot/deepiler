#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(b,h);
h=floor(a);
b=fdim(h,c);
while(isgreaterequal(e,b)){
g=(fmin(e,b))+(fdim(e,f));
a=(atan2(g,f))*(floor(d));
e=fmin(g,g);
a=pow(e,g);
h=fdim(f,d);
}
c=sin(h);
a=fmax(f,d);
a=fmax(a,b);
d=(cos(g))*(atan2(g,e));
e=(ceil(f))+(pow(f,e));
}