#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(b);
c=(fmin(f,d))-(pow(g,g));
b=(tan(a))*(fdim(a,f));
while(islessgreater(f,e)){
f=fmax(e,a);
d=ceil(f);
d=exp(e);
}
if(isgreaterequal(g,b)){
g=(exp(a))/(log(c));
a=fdim(a,g);
b=fmax(d,a);
}
}