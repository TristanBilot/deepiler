#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(atan2(f,h))-(pow(e,a));
h=asin(d);
d=fmin(d,g);
e=(exp(d))+(pow(e,f));
a=atan2(g,a);
if(islessgreater(h,c)){
d=(fmax(b,f))-(fdim(f,d));
h=(tan(b))/(fdim(b,h));
a=log(d);
}
else{
f=(pow(g,c))+(fmax(a,h));
c=(log(c))*(log(h));
e=(log(c))*(log(a));
b=fmax(a,d);
}
while(isless(a,f)){
h=(sin(d))*(log10(f));
g=atan2(a,a);
}
}