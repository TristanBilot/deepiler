#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan(e);
f=cos(c);
d=asin(f);
d=(fmin(f,b))*(fdim(f,d));
b=(fmin(e,b))-(fdim(c,d));
b=(fdim(d,e))+(asin(c));
f=fmin(e,f);
c=fdim(a,f);
while(islessequal(f,d)){
d=(fmin(c,a))/(pow(b,e));
c=(atan2(e,b))+(fmax(a,e));
}
}