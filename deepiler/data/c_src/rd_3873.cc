#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(d,c);
g=(ceil(d))-(fmin(h,g));
if(isgreaterequal(b,b)){
h=cos(c);
c=sqrt(b);
a=(fmin(b,g))+(asin(g));
c=(atan(e))*(asin(d));
}
else{
a=(fdim(b,b))*(log(a));
b=fdim(e,h);
c=(tan(h))/(asin(e));
a=(fdim(e,d))*(cos(b));
e=tan(e);
}
if(islessgreater(e,d)){
c=(acos(g))*(asin(f));
f=(fmin(e,f))*(pow(h,e));
}
}