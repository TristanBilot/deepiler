#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(b,d))-(atan2(d,b));
b=(fmax(b,c))+(fmin(c,e));
e=(atan2(c,b))+(fmax(c,e));
d=ceil(b);
d=fmin(a,d);
e=(fmin(e,d))*(log10(d));
e=(cos(a))*(log10(b));
b=sin(a);
e=cos(c);
if(isless(b,c)){
e=atan(c);
e=fmax(c,e);
c=atan2(e,c);
a=(fdim(e,e))*(asin(a));
}
else{
a=(asin(e))-(fmin(e,b));
e=acos(b);
d=atan2(c,a);
d=(atan2(c,a))/(ceil(d));
}
}