#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(a,c);
e=(atan2(c,a))*(fdim(b,d));
if(islessgreater(b,e)){
b=(atan2(d,d))+(fmax(b,e));
b=fmin(a,d);
a=(fmax(b,a))-(fmax(e,a));
}
else{
a=ceil(a);
e=(asin(e))/(atan2(c,a));
c=(tan(b))*(cos(c));
d=(acos(d))/(fdim(e,d));
}
d=(log10(a))-(sqrt(a));
d=(tan(e))+(sin(b));
e=(pow(d,e))/(pow(c,a));
}