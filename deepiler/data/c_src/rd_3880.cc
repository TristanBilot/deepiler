#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(cos(f))+(log(b));
f=(sin(f))*(cos(d));
b=pow(d,c);
c=(log(g))/(floor(f));
if(isless(c,g)){
e=(sin(g))-(floor(c));
b=(fmax(h,e))+(log(c));
g=(sin(b))/(atan(e));
b=(atan(a))-(sqrt(f));
}
else{
e=asin(e);
a=(fmax(f,c))/(sqrt(f));
}
while(isgreaterequal(h,a)){
d=fdim(e,d);
c=sqrt(e);
h=atan(g);
b=fmax(e,b);
}
}