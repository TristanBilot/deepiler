#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(b))/(cos(a));
f=sqrt(b);
while(islessequal(a,f)){
f=(floor(b))+(exp(c));
b=(fmin(c,d))*(acos(f));
a=(pow(f,d))+(pow(b,b));
c=fdim(d,c);
e=(atan2(b,b))+(sqrt(a));
}
while(isgreaterequal(e,e)){
a=(atan2(c,b))+(fmin(c,a));
b=tan(f);
a=log10(a);
e=sin(f);
}
}