#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(e,c);
f=cos(g);
f=(fmin(e,g))*(cos(a));
g=(log10(a))+(cos(b));
if(islessgreater(b,g)){
d=pow(g,f);
b=atan2(c,f);
g=log(a);
b=pow(b,e);
}
if(islessequal(d,g)){
b=(sqrt(g))*(fmin(d,e));
a=log10(f);
}
else{
b=(cos(b))*(acos(f));
g=(sin(g))/(acos(g));
c=exp(a);
c=acos(e);
}
}