#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(e,c);
a=(tan(b))/(fdim(c,d));
c=exp(b);
f=log10(d);
d=(atan2(b,e))+(fmax(a,g));
a=(fmax(e,a))*(acos(e));
g=(pow(f,c))*(fmin(b,b));
if(islessequal(d,e)){
g=fmax(b,e);
d=(ceil(a))+(fmin(e,e));
}
}