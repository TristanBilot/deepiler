#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(e,c))*(tan(c));
d=(atan2(e,b))+(log10(b));
b=(log10(d))-(atan2(c,d));
e=(exp(b))/(floor(e));
d=fdim(e,e);
while(isless(d,a)){
e=cos(a);
c=(sin(d))-(log(c));
c=(fdim(a,e))*(cos(b));
c=pow(e,c);
}
while(islessgreater(b,b)){
d=(fmin(a,e))*(asin(a));
e=sin(e);
d=(fmin(d,b))/(sqrt(a));
e=sqrt(a);
c=fmin(d,c);
}
}