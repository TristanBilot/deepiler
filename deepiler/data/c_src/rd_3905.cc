#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(h,a))*(fdim(f,a));
h=sqrt(c);
c=fmax(h,a);
e=fmax(h,d);
while(isless(e,d)){
a=sqrt(d);
c=fmax(h,e);
b=log(b);
f=(log10(a))/(fmin(f,h));
h=(fmax(c,e))-(fdim(c,e));
}
while(islessequal(h,h)){
c=fdim(a,d);
a=floor(g);
h=(tan(g))/(atan2(b,a));
f=tan(b);
h=fmin(a,e);
}
}