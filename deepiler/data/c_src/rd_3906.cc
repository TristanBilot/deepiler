#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(a);
c=(fdim(f,d))-(cos(c));
f=(fdim(c,a))*(log10(b));
g=pow(b,c);
g=pow(d,g);
if(islessgreater(g,a)){
e=(fdim(c,d))/(exp(e));
a=(pow(h,b))+(acos(h));
d=(fmin(f,e))-(exp(f));
}
else{
c=(log(g))-(sqrt(a));
h=atan2(f,h);
c=(sqrt(f))+(exp(h));
g=exp(d);
}
if(islessequal(d,g)){
g=atan(b);
d=exp(d);
e=fdim(b,d);
}
else{
c=acos(b);
a=(pow(h,g))/(floor(b));
}
}