#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(a,e))+(floor(b));
b=ceil(d);
if(isless(g,g)){
e=asin(g);
c=(pow(b,e))-(pow(g,a));
e=ceil(e);
g=pow(a,g);
}
if(isless(b,f)){
d=(fmin(g,d))/(fmin(g,g));
e=fmax(a,e);
d=fmax(b,f);
}
}