#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(a,e))+(asin(e));
e=floor(c);
b=exp(b);
d=(atan2(d,b))*(tan(e));
e=(fmin(e,c))-(sqrt(b));
d=(atan2(b,a))-(pow(c,e));
d=(sin(d))-(sqrt(e));
while(isless(e,e)){
e=(fmin(d,b))+(sqrt(b));
d=floor(b);
a=(ceil(b))+(log10(c));
a=log(b);
b=fmin(c,a);
}
}