#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=asin(a);
d=(atan2(b,f))*(fdim(c,f));
e=(acos(e))+(pow(c,e));
f=(fmin(f,b))/(fmax(f,e));
if(isless(b,b)){
c=(atan2(c,f))*(fdim(a,f));
b=(atan2(a,c))/(log(c));
f=(asin(f))-(fmax(d,c));
d=(acos(a))-(log(d));
b=(pow(d,a))+(sqrt(f));
}
c=sqrt(f);
f=(cos(f))+(fmax(c,c));
a=tan(b);
}