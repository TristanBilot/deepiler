#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(d,e))/(log(d));
d=(ceil(e))+(sin(e));
while(isgreaterequal(e,e)){
e=atan2(e,e);
b=(fdim(b,d))+(fmin(a,b));
}
if(islessequal(e,a)){
b=(pow(b,e))*(exp(e));
e=(pow(c,b))-(pow(e,e));
}
else{
a=pow(c,d);
c=(fmax(b,a))/(fdim(d,c));
b=log10(e);
b=(acos(b))-(ceil(c));
c=(pow(a,a))/(fmax(b,c));
}
}