#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=ceil(c);
g=(pow(d,d))*(atan2(g,c));
f=atan2(b,b);
b=(pow(e,d))+(cos(b));
e=(acos(b))/(cos(d));
g=asin(e);
f=ceil(c);
b=(sqrt(a))-(log10(b));
if(islessequal(e,a)){
a=(floor(c))*(cos(a));
e=(fdim(d,g))*(pow(b,e));
f=fdim(g,e);
c=pow(c,g);
}
else{
d=fdim(f,g);
c=(cos(a))*(atan2(a,d));
f=fmax(e,e);
b=pow(d,f);
f=atan(a);
}
}