#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=acos(b);
c=(atan2(c,h))+(tan(e));
f=(cos(d))+(atan(h));
d=fmax(a,b);
g=(pow(d,b))+(fmax(c,h));
g=ceil(e);
e=fmin(e,e);
h=floor(d);
d=log10(a);
while(isgreaterequal(d,a)){
d=acos(h);
h=sqrt(a);
g=fdim(g,a);
h=log(g);
}
}