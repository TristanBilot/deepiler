#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fdim(f,c);
g=(fmax(c,b))-(ceil(g));
g=log(a);
f=fmax(f,f);
g=log(g);
b=(log10(e))/(cos(a));
b=pow(d,d);
d=(fmin(e,a))/(sqrt(c));
a=(floor(d))*(exp(f));
b=fmax(d,f);
b=fmin(c,g);
}