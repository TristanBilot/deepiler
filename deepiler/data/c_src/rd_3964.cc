#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(pow(e,g))/(sqrt(c));
f=ceil(a);
e=ceil(a);
g=(fmin(a,g))*(fmax(b,c));
c=(exp(e))-(sqrt(f));
d=fdim(c,a);
g=fmin(c,b);
e=(fdim(b,b))*(fmax(b,f));
e=tan(f);
b=floor(e);
}