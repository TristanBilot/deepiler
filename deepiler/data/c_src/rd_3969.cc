#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=pow(c,a);
c=(asin(d))*(cos(c));
c=fdim(a,b);
d=(fmax(e,d))/(fmin(d,b));
a=floor(b);
d=fdim(a,e);
b=(floor(d))*(acos(e));
e=(fdim(c,a))+(exp(d));
if(islessgreater(a,e)){
e=pow(e,b);
d=(pow(d,e))/(tan(b));
}
}