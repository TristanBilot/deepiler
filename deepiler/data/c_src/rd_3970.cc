#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log10(b);
e=fmax(b,a);
c=(atan2(c,b))/(fmax(a,d));
while(islessequal(a,a)){
g=pow(f,a);
a=(fmax(e,g))-(pow(g,d));
}
g=(fdim(e,a))*(fmax(b,f));
d=(sin(c))+(atan2(a,c));
}