#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(c);
a=sqrt(e);
b=atan2(d,a);
a=ceil(d);
if(islessgreater(c,e)){
a=(pow(c,b))/(fmin(a,b));
e=(fdim(b,b))*(fdim(a,c));
c=tan(e);
b=(floor(b))*(fmax(b,b));
e=(floor(b))+(asin(d));
}
else{
c=(atan(b))+(exp(b));
e=(pow(d,c))-(sqrt(b));
e=(acos(c))-(ceil(c));
b=(cos(c))+(atan2(b,d));
a=sqrt(d);
}
if(isgreaterequal(d,a)){
c=(floor(b))-(floor(d));
a=(sqrt(e))-(fmax(c,b));
}
}