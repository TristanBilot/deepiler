#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(d,e))*(exp(c));
d=(fdim(e,c))*(log10(h));
e=fmax(a,c);
while(islessequal(c,c)){
h=atan(c);
h=(pow(d,g))/(pow(e,d));
e=(fmax(h,d))*(tan(c));
d=(atan2(e,d))-(ceil(d));
}
while(isless(f,a)){
e=(pow(h,g))-(log(e));
c=(floor(c))*(pow(c,d));
f=pow(a,a);
}
}