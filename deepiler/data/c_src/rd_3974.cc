#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(c,e);
e=(acos(b))*(floor(c));
b=(atan(c))*(log(a));
e=(sqrt(e))+(fmin(c,d));
a=(atan2(d,d))-(pow(c,b));
if(islessequal(d,e)){
d=(ceil(a))/(fmax(c,c));
b=sqrt(a);
c=pow(b,d);
}
else{
c=fdim(e,e);
b=(floor(e))*(sqrt(a));
c=(ceil(e))/(sqrt(d));
e=floor(e);
}
while(islessequal(b,c)){
e=atan2(c,b);
b=log(b);
}
}