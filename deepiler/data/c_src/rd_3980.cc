#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(b);
b=floor(g);
g=floor(f);
h=(tan(f))*(fdim(a,d));
if(islessequal(a,d)){
c=(sqrt(a))/(acos(c));
f=(fdim(e,d))+(fmax(c,f));
f=fmax(d,b);
a=(sqrt(h))/(atan(g));
g=fmax(f,f);
}
else{
d=acos(c);
b=(ceil(g))/(fmax(a,f));
h=fmax(d,d);
e=(sin(e))-(asin(a));
}
while(isless(g,g)){
d=(sqrt(e))+(floor(d));
a=(atan2(g,h))-(atan2(g,c));
h=(sqrt(b))/(fmin(e,g));
}
}