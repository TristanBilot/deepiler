#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(d,a);
d=atan(g);
h=cos(c);
b=log(c);
g=(log(d))/(ceil(c));
d=(fmin(g,g))*(log(h));
e=(asin(e))/(sin(d));
h=sin(d);
e=(asin(e))+(atan2(c,d));
e=(asin(f))/(cos(e));
a=fmax(h,b);
b=(cos(b))/(log(b));
}