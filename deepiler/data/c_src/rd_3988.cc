#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(pow(f,f))-(atan2(e,e));
h=(pow(c,a))-(fmax(h,h));
if(isless(e,c)){
e=fmax(h,a);
d=atan2(h,c);
}
while(isless(b,e)){
f=fdim(a,a);
e=atan2(e,f);
a=tan(d);
f=(tan(b))+(acos(a));
h=asin(e);
}
}