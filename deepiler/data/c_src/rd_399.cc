#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(e);
a=(cos(d))-(pow(b,c));
c=cos(a);
e=(fmin(b,b))-(atan2(c,d));
while(isgreaterequal(d,a)){
e=(pow(a,a))-(acos(e));
b=ceil(e);
}
if(islessgreater(b,b)){
d=(fmax(d,b))*(fmin(c,e));
c=(pow(e,e))+(pow(d,d));
b=fmin(a,a);
}
}