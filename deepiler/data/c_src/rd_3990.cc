#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log10(b))-(atan2(d,b));
b=ceil(b);
a=cos(d);
a=floor(d);
d=(sin(c))/(pow(e,c));
d=(cos(a))/(atan2(d,e));
if(isless(d,e)){
e=sqrt(a);
a=fdim(b,a);
e=tan(a);
b=atan2(b,e);
d=(floor(d))/(fdim(c,d));
}
else{
c=asin(a);
a=(ceil(e))/(sin(c));
b=(atan2(d,b))-(sqrt(c));
}
}