#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(exp(g))+(fmax(b,g));
b=(atan2(h,e))+(fmin(g,g));
h=(log(a))*(atan2(g,g));
d=asin(b);
d=tan(g);
h=atan2(f,c);
d=ceil(h);
e=(tan(b))*(fmin(g,h));
e=sqrt(c);
g=(fdim(a,f))-(atan2(c,e));
a=fmax(d,a);
g=(floor(g))-(exp(b));
h=(fdim(e,b))+(cos(d));
h=(acos(b))-(sin(d));
b=pow(g,a);
}