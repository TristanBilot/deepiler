#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(g,d);
g=fmin(g,d);
d=atan(a);
while(islessequal(e,c)){
a=(pow(e,e))/(fmin(b,d));
g=(atan2(f,h))-(log(c));
c=fdim(b,d);
e=(exp(b))+(log(d));
g=(fmin(f,b))-(acos(e));
}
f=(exp(c))-(atan2(b,f));
a=(sqrt(c))-(fmax(h,f));
d=(sin(f))+(fmin(g,b));
h=atan(b);
a=fmax(f,h);
}