#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(b,a);
b=fmin(c,g);
e=fdim(c,e);
e=(atan2(b,b))-(acos(a));
f=acos(e);
f=(fmax(e,f))/(sqrt(e));
c=fmin(e,d);
g=(fmin(g,e))/(atan2(a,a));
g=atan2(f,d);
d=(fmax(a,b))/(fmin(g,e));
if(isgreaterequal(d,d)){
b=(fmin(g,d))-(floor(a));
f=(pow(d,e))-(fmax(b,d));
a=fmax(d,a);
}
else{
c=(fdim(a,b))*(atan(b));
b=(pow(d,f))+(fmin(c,d));
g=(log(a))+(fmin(e,g));
}
}