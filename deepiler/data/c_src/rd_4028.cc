#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan(a))+(fmin(d,e));
e=fmin(a,e);
c=pow(d,a);
d=(fmax(b,a))+(tan(d));
if(isgreaterequal(c,c)){
e=(sqrt(e))/(tan(c));
a=(fdim(c,d))/(floor(b));
b=(floor(e))/(tan(a));
}
else{
c=tan(b);
e=fdim(c,e);
d=pow(d,b);
e=(fmin(e,b))/(floor(e));
}
while(islessgreater(a,b)){
b=(exp(d))/(cos(b));
c=(exp(a))*(fmax(c,b));
e=pow(d,e);
}
}