#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(f))/(pow(e,f));
c=(atan2(d,d))/(fmax(e,b));
e=(log10(f))*(floor(c));
d=fmin(e,b);
d=atan(b);
while(isless(e,d)){
a=atan2(d,d);
f=(pow(b,a))*(atan(e));
a=asin(d);
d=(fmax(d,c))/(log10(a));
b=(cos(b))-(ceil(f));
}
b=fdim(d,a);
a=floor(e);
}