#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sqrt(e))/(pow(d,a));
e=tan(a);
if(islessequal(c,b)){
b=fmin(e,b);
e=(ceil(b))*(pow(e,e));
c=(acos(b))-(sqrt(e));
}
while(isless(a,a)){
b=atan(b);
a=sin(b);
c=acos(d);
c=(atan(b))+(pow(d,e));
c=fmax(c,d);
}
}