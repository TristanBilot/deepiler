#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan2(f,d);
d=atan2(f,b);
while(isless(b,d)){
d=fdim(e,a);
d=(cos(d))+(atan2(c,b));
d=ceil(d);
d=(atan(e))/(fmax(f,f));
e=ceil(c);
}
if(isless(d,a)){
f=(log(f))/(log10(b));
a=fmin(e,e);
}
}