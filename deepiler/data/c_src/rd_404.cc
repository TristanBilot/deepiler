#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fdim(a,c);
f=tan(c);
e=pow(c,a);
c=(atan2(b,c))-(exp(f));
while(isless(b,f)){
c=(ceil(f))+(log(b));
d=pow(b,c);
}
}