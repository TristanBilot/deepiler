#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=ceil(e);
f=cos(f);
f=(pow(c,e))*(log10(e));
c=sqrt(b);
while(islessequal(b,f)){
c=(fmax(d,e))+(atan2(c,e));
d=(fmax(c,f))*(atan(e));
f=(sqrt(d))*(pow(c,d));
}
if(islessequal(c,e)){
d=(sqrt(e))+(fmax(f,b));
e=(atan2(a,f))-(atan(e));
e=(asin(f))+(fmin(e,f));
b=(fdim(c,a))/(atan2(e,d));
e=(fmax(d,b))+(fmax(d,e));
}
else{
f=(fmax(a,e))+(atan2(e,c));
d=(pow(d,b))*(fdim(a,b));
f=(fdim(c,a))*(fmin(a,e));
}
}