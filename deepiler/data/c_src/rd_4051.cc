#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(b,g))/(fmin(d,b));
d=(asin(d))/(cos(e));
e=cos(a);
f=(fmin(h,h))-(exp(c));
g=sin(e);
while(isless(d,g)){
h=fdim(c,b);
b=fmax(f,g);
c=sqrt(e);
a=(asin(f))*(sqrt(h));
}
e=(tan(f))/(tan(e));
c=(pow(b,d))*(fmax(a,e));
f=(fmax(e,e))/(exp(a));
}