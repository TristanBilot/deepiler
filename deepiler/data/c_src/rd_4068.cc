#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(exp(a))-(pow(c,a));
a=acos(e);
c=(fmin(d,f))+(atan(g));
c=fmin(c,a);
g=fmax(d,g);
d=floor(e);
b=pow(c,f);
c=sqrt(a);
d=floor(g);
e=(atan2(a,b))/(atan2(f,b));
while(isgreaterequal(f,c)){
c=asin(a);
d=exp(d);
g=(cos(c))*(atan2(f,e));
b=fdim(g,d);
e=atan(g);
}
}