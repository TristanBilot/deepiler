#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(h,a);
b=(floor(e))+(log10(g));
f=atan2(c,a);
if(isgreaterequal(b,g)){
e=(floor(d))/(log10(f));
d=(fmax(d,f))/(fdim(c,d));
}
else{
g=fdim(a,b);
e=(sqrt(d))*(fmax(e,a));
}
h=fdim(h,g);
d=pow(c,c);
a=(cos(e))+(exp(f));
}