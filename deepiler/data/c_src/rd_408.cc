#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(h,e))-(floor(e));
h=(fmax(g,c))*(asin(a));
f=exp(c);
h=(fdim(h,d))/(fmin(b,h));
a=(sqrt(g))*(fmax(f,d));
while(islessequal(f,f)){
b=(pow(c,g))-(fmin(e,b));
a=(pow(e,g))*(pow(a,g));
}
if(islessequal(h,f)){
c=(fdim(g,h))-(fdim(e,f));
e=log10(f);
}
else{
f=(fmin(h,b))+(atan2(e,c));
f=fmax(h,f);
d=sin(a);
d=fdim(f,f);
d=(sin(a))-(log10(c));
}
}