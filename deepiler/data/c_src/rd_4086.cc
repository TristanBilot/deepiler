#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(atan2(a,b))+(exp(a));
c=pow(e,d);
c=atan(b);
e=tan(e);
c=(ceil(c))/(exp(a));
if(isless(c,d)){
b=(fmax(a,a))*(tan(e));
c=sqrt(d);
}
while(islessgreater(c,b)){
b=(cos(e))+(atan(c));
e=(fdim(c,e))+(cos(a));
a=(acos(c))+(log10(b));
c=tan(e);
c=(tan(d))-(sqrt(d));
}
}