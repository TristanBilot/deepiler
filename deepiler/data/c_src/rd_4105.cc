#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(sqrt(f))-(log(a));
f=(fmin(a,b))+(pow(c,b));
a=(fdim(g,b))+(tan(d));
c=(log10(b))*(log(f));
f=(fmax(e,f))+(fdim(f,d));
f=(fmax(c,e))*(fdim(b,g));
f=fdim(b,f);
g=tan(d);
c=pow(c,e);
a=log(e);
if(isless(e,d)){
d=atan(b);
e=(acos(d))+(fmin(e,f));
c=(pow(d,c))/(fmax(a,a));
g=sin(a);
}
}