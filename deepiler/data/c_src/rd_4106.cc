#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmin(d,e);
c=(ceil(c))*(fmin(e,c));
b=(atan(c))*(fmin(a,e));
d=(exp(f))-(atan2(f,b));
d=(fdim(e,b))/(floor(b));
f=(atan2(d,c))*(fmax(d,d));
b=(log(e))/(cos(f));
f=(fmin(b,f))+(log(c));
d=(acos(f))*(floor(a));
a=ceil(f);
d=floor(b);
}