#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(c,b))-(fdim(b,a));
d=(log10(b))+(fmax(c,e));
a=(ceil(e))-(fmin(b,a));
a=atan2(a,d);
c=cos(d);
b=fmax(b,a);
c=(log10(b))/(acos(c));
e=(log(b))/(tan(e));
}