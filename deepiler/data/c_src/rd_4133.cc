#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(a,b))*(atan2(e,a));
e=fmax(d,e);
a=(acos(a))-(fdim(d,c));
a=(log(e))-(fmax(d,a));
c=pow(a,e);
b=(floor(b))-(tan(d));
d=(ceil(c))-(atan2(b,c));
b=(tan(d))/(atan2(c,e));
d=(fmax(c,e))/(atan2(c,b));
d=acos(b);
}