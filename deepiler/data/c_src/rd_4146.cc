#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=cos(b);
b=(log10(c))-(tan(b));
e=(fmin(b,c))*(cos(a));
while(isgreaterequal(a,d)){
e=fmax(e,a);
e=(ceil(c))*(sin(a));
c=exp(a);
a=(log10(e))-(fmin(c,a));
d=(pow(e,c))*(fmin(a,d));
}
if(islessgreater(e,e)){
d=(pow(a,a))-(fmax(d,e));
e=sqrt(c);
}
else{
e=(asin(a))+(cos(c));
c=fmin(b,b);
e=(fmin(d,a))-(acos(e));
}
}