#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan(b);
d=fmax(a,e);
e=atan2(c,e);
c=(sin(c))/(atan(e));
b=fdim(c,c);
while(isless(b,b)){
e=(pow(b,e))+(atan(c));
e=(fmin(a,b))/(exp(d));
a=(pow(b,b))+(fmin(c,d));
c=log10(e);
d=fmin(d,b);
}
while(islessgreater(c,d)){
d=log10(a);
c=(log(c))-(cos(c));
}
}