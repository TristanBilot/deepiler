#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log(c))-(pow(b,c));
e=fmax(b,b);
e=fmax(a,a);
d=floor(b);
if(islessequal(e,c)){
a=floor(b);
c=(fmin(c,a))/(sqrt(a));
}
d=(atan(b))*(fmin(e,e));
a=cos(b);
b=(cos(a))-(pow(e,c));
e=fmin(c,a);
b=fmin(b,e);
}