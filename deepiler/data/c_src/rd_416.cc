#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(ceil(d))*(fmax(f,a));
a=atan(e);
f=pow(a,b);
e=fmax(a,b);
d=(sin(b))+(fmax(e,b));
f=fdim(e,c);
b=pow(f,e);
if(islessgreater(c,b)){
f=atan2(e,b);
f=tan(a);
a=(atan2(e,d))-(fmax(d,b));
e=(floor(e))+(pow(e,c));
a=(log10(b))+(sin(e));
}
}