#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(ceil(b))*(asin(g));
a=exp(g);
e=fmin(f,d);
f=fmin(f,c);
h=(log(e))/(fmin(g,h));
c=fmax(g,h);
b=(pow(f,d))/(log10(b));
d=(atan2(g,f))*(fmin(h,b));
}