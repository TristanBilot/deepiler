#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(c);
d=(cos(d))*(fmax(e,b));
g=(floor(f))*(exp(f));
f=(exp(c))*(sqrt(b));
b=sqrt(a);
d=ceil(f);
e=fmax(b,h);
d=fmax(f,c);
e=(fmax(d,h))/(fmax(f,g));
f=(atan2(h,d))*(fmax(e,b));
a=fmin(d,g);
f=(log(c))*(tan(b));
h=(asin(e))+(atan2(e,h));
d=(pow(b,f))*(atan2(e,c));
}