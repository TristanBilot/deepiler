#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(a,g))+(asin(b));
g=(tan(f))/(fmin(g,b));
d=fmin(e,g);
c=(atan(f))/(pow(b,a));
b=sqrt(c);
b=(fdim(c,e))/(fmax(d,e));
while(isless(a,c)){
b=fdim(f,f);
g=fdim(d,f);
g=fmin(e,b);
a=pow(b,e);
}
}