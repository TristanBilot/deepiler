#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=pow(d,a);
d=(atan2(g,d))+(pow(c,g));
b=(fmin(g,c))/(log(d));
b=(ceil(c))-(pow(c,a));
if(isgreaterequal(a,e)){
b=atan2(e,a);
d=(fmax(d,e))+(fdim(e,f));
}
else{
g=fmax(c,g);
e=(atan(a))-(fmax(e,b));
}
if(isless(e,b)){
b=(ceil(d))-(fdim(a,c));
c=(sin(e))/(fmax(d,f));
c=(asin(d))+(fmax(a,g));
b=pow(c,b);
b=sqrt(g);
}
}