#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(asin(a))*(atan2(g,e));
b=(fmin(a,f))-(atan2(c,a));
c=sqrt(e);
if(islessequal(b,d)){
a=atan2(c,c);
c=(fmin(f,d))/(pow(b,g));
g=asin(f);
}
while(islessgreater(d,a)){
d=fmax(b,e);
a=(fmax(f,e))-(atan2(e,e));
b=(fmin(f,d))/(fmin(d,c));
f=(exp(e))-(pow(d,f));
}
}