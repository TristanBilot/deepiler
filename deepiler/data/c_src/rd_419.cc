#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(tan(c))*(atan(f));
d=(asin(d))/(sin(e));
c=fmax(a,d);
while(islessequal(a,b)){
a=tan(f);
h=(acos(f))*(log10(h));
b=pow(a,f);
b=(atan2(a,b))-(log(a));
c=(pow(f,f))/(cos(e));
}
while(islessequal(d,g)){
b=fmax(h,h);
e=(fmax(h,c))/(cos(h));
}
}