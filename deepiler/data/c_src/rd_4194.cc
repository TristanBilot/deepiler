#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(a,e))-(log10(b));
e=(pow(b,e))/(ceil(f));
e=fmax(g,d);
f=(atan2(e,d))+(fmax(e,f));
e=(floor(e))+(fdim(d,e));
if(islessgreater(a,g)){
f=sqrt(d);
a=cos(b);
e=acos(e);
g=fmin(d,g);
d=sin(c);
}
else{
b=(pow(e,b))/(pow(f,d));
e=fmax(c,b);
d=(pow(d,b))*(fmax(d,d));
b=log(a);
c=atan(c);
}
if(isgreaterequal(c,b)){
b=fmin(g,g);
a=pow(f,a);
f=floor(c);
}
}