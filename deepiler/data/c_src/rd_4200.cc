#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(ceil(a))-(fmax(e,a));
g=(pow(g,h))-(fmin(c,a));
a=(pow(b,g))*(floor(d));
if(islessequal(d,f)){
a=(fdim(c,c))-(acos(h));
g=atan2(e,d);
}
else{
c=cos(h);
f=pow(c,g);
g=fdim(a,b);
d=(fdim(e,a))+(tan(g));
b=exp(f);
}
a=(fdim(g,e))-(atan2(a,h));
g=log(g);
}