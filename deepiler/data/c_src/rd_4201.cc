#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(c,d);
a=(fmin(b,g))-(pow(a,g));
f=(asin(e))-(fmin(a,g));
a=(fmax(d,e))+(log10(a));
b=(acos(d))-(fmax(d,g));
e=(fdim(c,e))-(fmax(c,c));
c=atan(e);
g=(sin(b))/(fdim(b,f));
if(islessequal(g,f)){
e=fmin(e,f);
e=(log(g))/(log(d));
}
else{
d=(fmin(a,b))*(asin(f));
c=atan2(b,d);
d=(asin(a))*(atan2(f,e));
b=pow(b,f);
}
}