#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan(b);
a=fdim(d,c);
e=log10(a);
while(isgreaterequal(a,b)){
e=atan(d);
d=(exp(a))+(pow(c,b));
d=(sqrt(b))-(asin(e));
d=fmax(c,c);
}
while(isless(e,c)){
b=(acos(c))+(fdim(c,d));
e=atan2(c,e);
a=(exp(b))+(ceil(e));
b=atan2(a,c);
a=fmax(c,e);
}
}