#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log10(b);
a=(sqrt(a))-(fmax(b,a));
while(islessequal(c,a)){
b=(sqrt(d))/(fdim(b,b));
c=(acos(c))-(pow(a,a));
a=(sin(e))-(fmin(b,e));
d=acos(e);
}
a=(sin(a))/(log(d));
b=cos(a);
d=sqrt(c);
c=floor(e);
}