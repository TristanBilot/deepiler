#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmax(c,e);
d=sqrt(e);
g=atan2(g,c);
while(isless(e,e)){
e=log10(d);
e=atan2(g,d);
g=sqrt(b);
}
e=pow(f,e);
e=fmax(e,c);
}