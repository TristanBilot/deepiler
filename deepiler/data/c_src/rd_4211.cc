#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=sin(a);
b=(fmin(d,c))/(tan(b));
while(islessequal(b,e)){
b=fdim(c,e);
e=pow(e,a);
}
while(isless(a,c)){
e=pow(d,d);
c=sqrt(e);
e=fmax(a,e);
}
}