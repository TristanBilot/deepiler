#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sin(b);
a=(pow(f,d))/(fmax(d,e));
f=pow(e,b);
b=(fmin(g,b))*(log(g));
a=(sqrt(d))+(pow(d,e));
e=(exp(b))-(log(g));
a=(sin(b))/(log(b));
if(isless(f,a)){
g=(cos(c))+(fmax(f,c));
a=cos(d);
d=(asin(b))*(fmax(g,d));
b=cos(e);
}
}