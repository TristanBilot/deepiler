#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(a);
h=fdim(f,e);
f=(pow(h,e))/(log10(d));
d=atan2(b,h);
if(islessequal(b,c)){
a=(sqrt(h))/(fmax(g,e));
b=sqrt(g);
e=ceil(b);
}
else{
h=atan(d);
c=acos(c);
h=(ceil(b))-(fmax(f,d));
h=ceil(h);
}
h=(fmax(e,d))+(exp(a));
d=pow(a,h);
h=fmax(e,b);
b=(pow(e,d))-(log(e));
}