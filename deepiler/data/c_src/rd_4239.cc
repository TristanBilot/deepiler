#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(e,g);
a=fmin(f,c);
c=(pow(h,b))*(fdim(d,b));
f=tan(c);
h=(floor(h))+(exp(d));
h=ceil(d);
if(islessgreater(b,g)){
h=(fdim(g,a))*(atan(c));
g=(sin(a))-(log10(f));
}
else{
b=pow(h,b);
f=ceil(a);
c=(fdim(c,c))-(log10(f));
g=(fmin(f,a))/(atan(b));
c=fmax(c,h);
}
}