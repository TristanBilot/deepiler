#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(a,h))*(fdim(c,b));
d=(fmax(c,b))*(fdim(a,h));
d=fdim(b,e);
d=(acos(g))*(exp(b));
g=fdim(g,e);
f=log(b);
a=(fmax(e,a))+(atan2(d,f));
if(islessequal(h,a)){
g=atan2(g,f);
c=atan2(g,b);
a=(fmax(b,a))-(exp(c));
}
}