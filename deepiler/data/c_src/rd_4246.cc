#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log10(e))+(atan2(d,b));
c=fmax(a,e);
while(isless(c,d)){
d=exp(a);
c=(fdim(b,b))+(fmax(a,d));
a=exp(d);
c=atan2(b,e);
c=log(e);
}
if(isgreaterequal(e,d)){
c=(fmin(d,d))-(cos(d));
e=(atan2(e,c))+(fdim(c,d));
}
else{
d=tan(b);
e=(fdim(b,d))/(fmax(b,b));
c=pow(b,e);
e=(cos(c))+(log10(c));
}
}