#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(tan(b))*(tan(h));
b=atan(h);
d=(tan(g))+(atan(b));
a=fmin(c,h);
while(isless(g,h)){
f=sin(d);
c=(fmax(a,h))-(fmin(b,c));
d=atan(f);
h=(atan(d))-(exp(g));
}
a=fmin(b,g);
c=sin(c);
c=(fmax(a,a))/(atan2(f,e));
a=tan(g);
}