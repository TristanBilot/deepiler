#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(exp(e))*(sin(g));
h=(fdim(a,g))+(exp(e));
if(islessequal(b,e)){
g=(fmax(f,h))/(fdim(c,d));
g=(pow(b,b))*(fdim(g,b));
e=(floor(b))*(cos(h));
f=(ceil(d))*(tan(e));
}
g=floor(b);
b=(acos(b))*(pow(c,f));
g=(pow(f,c))-(atan2(a,e));
c=(log(c))*(sin(b));
a=fmax(h,b);
}