#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(d,e))/(fmin(c,b));
c=log10(a);
if(islessequal(c,c)){
e=atan(a);
e=(fmin(b,e))/(fmin(e,b));
d=cos(d);
b=pow(c,c);
}
a=(fmax(a,a))/(fdim(e,b));
a=pow(e,b);
}