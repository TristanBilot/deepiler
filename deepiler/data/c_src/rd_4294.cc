#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmax(g,f))-(cos(h));
b=(exp(h))/(asin(d));
c=(sin(c))-(fdim(d,f));
g=pow(e,g);
d=(atan2(a,d))*(fdim(h,b));
a=(fdim(h,h))*(atan2(e,c));
b=(log(g))*(log10(g));
g=(fmax(c,c))*(fmax(e,g));
}