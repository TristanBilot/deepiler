#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(sin(c))*(exp(a));
d=fdim(b,a);
a=fdim(e,b);
b=(asin(d))/(log10(a));
e=(fmin(a,d))-(exp(d));
c=atan(b);
a=atan(b);
b=(cos(b))*(acos(a));
b=(cos(b))/(log10(c));
c=fmin(c,e);
}