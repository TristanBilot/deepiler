#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(floor(a))-(exp(g));
h=(atan(g))/(log10(c));
h=(asin(f))+(fdim(f,c));
f=(fmax(c,e))-(pow(g,a));
b=(fmax(b,g))+(fmin(d,f));
if(isgreaterequal(e,g)){
c=fmax(f,f);
d=(atan2(b,f))-(fdim(b,h));
}
else{
h=(sin(g))/(fdim(h,f));
f=(exp(b))*(log(c));
}
while(isless(a,g)){
e=(pow(a,d))+(fdim(a,g));
b=(cos(g))*(log10(f));
}
}