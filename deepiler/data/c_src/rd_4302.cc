#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan2(c,e))-(log10(a));
e=pow(a,c);
d=acos(a);
e=(pow(d,b))/(tan(d));
a=pow(e,f);
c=atan(f);
c=atan(e);
b=atan2(a,a);
c=fmax(f,b);
while(islessgreater(b,a)){
f=(cos(d))*(atan2(d,d));
e=(log(a))+(fdim(e,f));
}
}