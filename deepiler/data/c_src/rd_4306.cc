#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(d);
b=(exp(c))/(fmax(a,a));
e=log10(e);
c=(tan(e))+(sqrt(d));
a=(log10(a))/(fmax(e,b));
if(isgreaterequal(b,d)){
c=log(a);
d=fmax(a,c);
}
else{
d=floor(c);
b=sqrt(c);
b=(fmax(e,c))*(tan(e));
}
if(islessgreater(b,c)){
a=pow(d,e);
b=ceil(d);
}
}