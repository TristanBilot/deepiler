#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=asin(b);
d=(sqrt(d))*(log10(c));
if(isgreaterequal(d,d)){
d=tan(a);
e=exp(c);
b=sqrt(b);
}
e=(sqrt(c))*(asin(c));
e=sin(d);
e=(exp(c))*(cos(c));
}