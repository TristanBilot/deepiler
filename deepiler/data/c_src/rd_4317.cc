#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(a,b))-(exp(d));
e=tan(b);
f=ceil(b);
a=cos(a);
a=(fdim(c,b))-(tan(d));
b=fmax(f,e);
while(isless(c,c)){
f=ceil(f);
f=(fmax(b,f))-(exp(b));
c=pow(b,e);
d=(fmin(f,a))-(fdim(d,c));
}
}