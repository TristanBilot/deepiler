#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(log10(b))-(atan(e));
b=exp(a);
e=log10(g);
e=fdim(e,b);
d=pow(h,b);
while(islessequal(a,g)){
g=(tan(e))*(ceil(d));
h=(sin(d))*(asin(a));
}
}