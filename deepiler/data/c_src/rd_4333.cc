#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(a))-(atan2(b,d));
c=tan(d);
b=(floor(e))+(fmax(a,c));
if(islessequal(c,d)){
e=(sin(b))+(fdim(a,e));
d=floor(b);
c=tan(e);
}
d=(fmax(e,b))/(cos(d));
c=acos(b);
a=sqrt(c);
}