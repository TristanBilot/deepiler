#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(e,b))+(pow(f,d));
c=exp(c);
d=fmax(a,d);
if(islessgreater(e,f)){
g=pow(b,a);
a=(fmin(d,f))*(atan2(c,c));
f=(log10(b))+(pow(f,a));
g=atan2(c,f);
a=fmin(c,e);
}
else{
a=log10(g);
b=(pow(e,a))/(atan(a));
f=(sqrt(g))-(pow(c,c));
}
if(isgreaterequal(b,b)){
f=fdim(d,f);
f=(log(c))*(sqrt(b));
e=log10(e);
c=(atan(b))-(tan(g));
e=log(e);
}
else{
b=(cos(b))+(tan(e));
a=atan2(f,f);
d=(ceil(b))*(atan2(d,c));
e=(asin(d))*(pow(b,f));
d=(cos(f))*(tan(d));
}
}