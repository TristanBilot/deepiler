#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,c);
e=asin(g);
d=(sin(f))*(log10(d));
h=(ceil(f))/(pow(a,b));
h=atan2(d,a);
f=(exp(f))/(fmax(c,e));
d=ceil(h);
b=(fmax(a,b))*(atan(f));
c=(fmax(c,b))-(exp(a));
while(isless(f,d)){
g=(pow(c,f))/(fmin(b,e));
h=(cos(h))/(atan2(a,c));
}
}