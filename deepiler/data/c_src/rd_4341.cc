#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan(e);
a=sin(c);
b=exp(a);
b=ceil(d);
d=fmin(c,e);
if(isless(e,b)){
b=asin(c);
b=(atan2(b,a))*(pow(a,b));
e=(sin(e))+(atan2(e,b));
}
else{
b=atan2(c,c);
b=sqrt(b);
e=fmax(b,c);
e=(sin(c))/(fmax(d,a));
}
while(isgreaterequal(d,b)){
e=fmax(e,c);
b=(sin(d))*(tan(e));
e=(atan(c))+(fdim(e,b));
}
}