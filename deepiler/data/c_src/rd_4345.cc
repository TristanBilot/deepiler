#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmax(d,c);
a=(pow(a,b))/(fdim(f,c));
a=sqrt(f);
a=fmin(e,d);
f=(log10(c))/(fdim(e,f));
while(islessequal(b,c)){
b=floor(e);
a=(ceil(f))/(fmax(b,a));
}
}