#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=tan(a);
d=atan(c);
d=fdim(b,f);
b=(log(f))/(fmax(e,g));
a=fmax(d,f);
g=(ceil(f))/(fmax(c,e));
f=(atan(g))+(ceil(b));
f=fdim(b,d);
e=(floor(c))-(fmax(a,c));
}