#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmin(e,b);
d=exp(d);
while(islessequal(a,d)){
e=(fdim(e,e))+(fmin(b,b));
c=sqrt(c);
a=atan2(d,e);
e=fmin(a,c);
}
while(isgreaterequal(a,d)){
a=(log(e))+(atan2(e,b));
d=pow(c,c);
c=(fdim(d,e))-(fmin(b,e));
}
}