#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(b,a);
e=fmin(c,c);
a=floor(a);
e=(cos(e))*(exp(c));
c=(atan(b))*(atan2(d,b));
a=exp(b);
a=(fdim(a,c))+(atan2(e,b));
b=(fmin(b,c))-(sqrt(b));
e=pow(c,c);
b=exp(c);
b=(atan2(a,a))-(pow(a,e));
}