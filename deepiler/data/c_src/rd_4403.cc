#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmin(e,d);
f=fmin(c,c);
f=(pow(e,a))+(atan(b));
e=log10(c);
a=(floor(b))*(fmax(a,d));
while(islessequal(c,e)){
a=atan(f);
c=(atan2(f,f))-(acos(a));
c=fmax(d,a);
a=cos(f);
}
if(islessequal(f,a)){
a=floor(a);
d=fmin(d,d);
c=fmax(c,a);
}
}