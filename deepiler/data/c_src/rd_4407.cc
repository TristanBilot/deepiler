#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(g,a);
g=asin(c);
g=(exp(e))-(pow(a,c));
d=(pow(e,a))+(tan(c));
b=(fdim(c,d))/(atan(f));
if(islessequal(d,e)){
d=(pow(d,c))-(fdim(d,g));
b=sin(a);
}
else{
d=atan2(b,c);
f=(log(a))*(asin(g));
g=(fmax(f,d))+(atan2(c,f));
}
}