#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(e,h);
c=(fmin(c,e))*(asin(e));
g=(fdim(h,c))-(exp(g));
if(isless(c,b)){
d=log10(h);
f=(sqrt(h))-(pow(h,h));
}
else{
a=(fdim(e,f))-(fmax(b,b));
h=acos(e);
}
if(isgreaterequal(h,c)){
b=tan(b);
c=cos(d);
h=sin(f);
}
else{
b=asin(e);
b=fmin(d,h);
}
}