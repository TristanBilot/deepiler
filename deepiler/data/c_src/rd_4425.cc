#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(d,b))/(asin(f));
b=(sqrt(c))/(ceil(e));
a=fmax(b,e);
if(isless(f,a)){
c=acos(c);
b=(atan2(f,a))+(fdim(f,c));
}
else{
c=(acos(f))+(fdim(e,a));
f=pow(f,e);
e=log(a);
e=atan2(e,c);
e=fmin(d,e);
}
if(islessequal(a,f)){
e=(tan(a))+(fmax(b,a));
e=fmax(b,c);
}
else{
a=sin(d);
f=(fmax(a,b))-(asin(b));
d=(asin(e))-(ceil(c));
}
}