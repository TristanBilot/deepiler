#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fdim(d,b))/(fmax(e,d));
c=(log10(d))+(atan(b));
b=(pow(e,f))-(fmin(f,d));
d=sqrt(d);
d=(fdim(c,g))*(tan(d));
b=fmax(a,e);
d=(pow(e,f))+(fdim(b,c));
f=fmin(c,b);
if(islessgreater(c,g)){
c=pow(b,a);
d=acos(g);
}
else{
c=(cos(b))-(fmax(d,b));
b=(cos(e))/(sin(c));
g=(exp(b))-(ceil(g));
f=(sqrt(b))*(fmax(g,b));
d=fmin(f,b);
}
}