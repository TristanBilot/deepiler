#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(cos(b))+(fmin(c,b));
e=(pow(c,a))*(tan(e));
if(islessequal(a,f)){
b=(fdim(f,d))/(fmin(c,e));
d=acos(e);
b=(fdim(a,e))+(floor(b));
a=(fmin(a,b))+(log(f));
d=(atan2(d,a))*(atan(f));
}
else{
f=(atan2(d,b))+(fmax(e,f));
a=(pow(d,d))/(fdim(f,c));
c=(fdim(f,a))*(exp(b));
}
if(islessequal(e,e)){
a=(acos(a))-(sin(c));
f=(log(c))+(fmin(f,c));
}
}