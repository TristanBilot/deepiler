#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(d);
b=acos(b);
if(islessequal(a,a)){
a=ceil(e);
d=(exp(c))*(exp(e));
}
while(islessequal(a,d)){
b=(atan2(c,b))/(ceil(e));
e=fmax(a,d);
d=(pow(c,b))/(exp(d));
e=pow(b,e);
}
}