#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(sin(h))/(fmax(c,c));
e=fmax(b,a);
c=(pow(e,c))+(atan2(c,d));
g=(fmax(f,c))/(floor(h));
a=fdim(d,a);
e=atan2(a,h);
e=(pow(g,c))-(fmax(f,h));
f=(floor(a))/(tan(d));
d=atan2(e,g);
h=(sin(c))-(acos(c));
a=asin(f);
b=fmax(e,d);
c=fmax(f,a);
}