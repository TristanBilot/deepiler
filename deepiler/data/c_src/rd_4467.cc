#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(c,b))-(acos(e));
c=tan(b);
c=(fdim(d,c))*(fmin(c,d));
if(islessequal(e,c)){
d=floor(d);
c=(floor(e))/(atan(c));
d=fdim(c,d);
}
else{
d=(fmin(a,a))/(floor(b));
d=(fmax(c,d))*(log10(c));
b=atan(b);
}
if(isgreaterequal(d,a)){
e=(fmin(a,d))*(sin(d));
c=fmax(a,e);
e=(pow(c,b))+(atan(e));
d=(asin(a))*(sqrt(e));
}
}