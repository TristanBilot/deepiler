#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmax(b,c))+(fmin(g,a));
f=(log10(e))*(fdim(c,a));
f=atan(f);
if(isgreaterequal(f,d)){
f=log10(b);
e=(pow(d,b))+(fdim(f,f));
}
f=log(d);
d=fdim(a,b);
}