#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=cos(d);
e=atan(d);
b=(pow(e,e))*(floor(a));
d=floor(b);
while(isgreaterequal(c,c)){
a=(fdim(a,c))-(fmin(d,a));
c=fmax(c,b);
a=fdim(d,c);
}
if(islessequal(b,d)){
c=log(c);
e=fmax(c,e);
e=floor(b);
}
else{
c=pow(e,b);
a=(fdim(a,a))-(acos(b));
b=(sin(e))*(fdim(c,b));
}
}