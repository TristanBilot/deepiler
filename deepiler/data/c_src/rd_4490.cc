#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=acos(e);
c=(log(d))*(fmax(e,a));
a=fmin(a,b);
if(isgreaterequal(e,b)){
a=pow(b,e);
b=fmax(a,d);
d=(exp(c))*(atan2(e,c));
d=(fmax(c,e))+(atan2(c,e));
}
if(isless(d,b)){
b=pow(b,d);
c=pow(b,b);
}
}