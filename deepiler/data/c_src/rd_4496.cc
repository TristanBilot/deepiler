#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(acos(b))/(tan(b));
c=(fmax(b,a))*(pow(f,f));
d=(ceil(a))*(atan2(a,c));
if(isgreaterequal(a,b)){
a=(sqrt(d))/(cos(c));
a=pow(f,e);
b=(atan(f))/(fmax(f,d));
f=(pow(e,e))/(tan(c));
e=(log(a))/(fdim(c,c));
}
else{
d=pow(e,d);
e=(asin(b))/(fdim(e,d));
a=cos(b);
c=(fmin(a,e))+(floor(b));
}
d=(fdim(d,f))+(pow(c,f));
e=tan(b);
}