#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(c,d);
e=atan2(a,e);
c=fmin(b,a);
a=(fdim(a,c))/(pow(b,e));
b=(atan2(c,b))/(fmin(c,a));
while(islessequal(b,a)){
d=fdim(b,c);
a=(log10(d))*(sqrt(a));
c=(acos(d))*(exp(c));
b=(cos(a))-(pow(e,b));
b=fdim(a,a);
}
d=(ceil(c))+(fdim(c,e));
b=(ceil(d))*(fmin(a,d));
b=floor(d);
a=fmax(e,a);
}