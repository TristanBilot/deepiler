#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(pow(d,b))*(atan(d));
b=(fmin(e,c))-(pow(e,h));
h=fmax(f,a);
if(islessequal(a,h)){
f=atan(b);
c=(atan2(f,a))/(fdim(e,d));
g=(pow(h,b))-(tan(e));
}
else{
g=log10(f);
c=(ceil(b))*(log(e));
e=(pow(c,f))+(fmin(b,f));
d=fdim(b,h);
e=(asin(h))-(fmax(b,d));
}
if(isgreaterequal(b,g)){
h=fmin(f,a);
f=fdim(d,b);
b=fmin(f,d);
a=(log10(c))+(atan2(g,f));
e=(atan2(c,e))/(floor(h));
}
}