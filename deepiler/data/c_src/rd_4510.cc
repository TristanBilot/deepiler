#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(e,b))/(atan2(a,c));
b=(atan(e))/(ceil(a));
a=fmin(d,d);
c=ceil(b);
a=(fdim(c,b))*(fmin(a,c));
b=atan2(b,b);
e=sqrt(e);
a=(atan(b))+(fdim(d,d));
d=fmax(b,d);
}