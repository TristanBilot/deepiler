#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(c);
e=(log(a))/(atan2(d,e));
a=(pow(a,e))*(fmin(a,a));
while(islessgreater(d,d)){
a=(exp(c))*(log10(d));
b=(fdim(e,d))+(fdim(b,b));
b=tan(c);
c=floor(c);
}
while(islessgreater(b,a)){
d=(asin(e))*(exp(d));
b=acos(c);
e=atan2(c,d);
e=(pow(c,a))+(atan(e));
}
}