#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=floor(f);
b=(log10(d))+(cos(c));
while(isgreaterequal(c,b)){
a=floor(c);
a=(atan(b))-(fmin(c,e));
b=(acos(a))-(cos(b));
c=cos(e);
e=(atan2(e,a))-(fmin(f,d));
}
while(islessgreater(e,b)){
a=(pow(b,b))-(cos(c));
f=atan(b);
d=(sin(c))*(fmax(d,f));
}
}