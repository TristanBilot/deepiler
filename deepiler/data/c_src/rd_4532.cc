#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log10(d);
f=atan2(c,b);
d=pow(b,f);
d=fdim(e,d);
a=atan2(c,b);
if(isless(f,a)){
b=(atan(b))-(exp(d));
d=(exp(e))*(sin(c));
}
if(isless(f,a)){
c=atan(d);
d=(pow(a,c))-(fmax(e,f));
}
else{
e=(atan2(d,c))*(atan2(e,e));
a=(sqrt(b))*(fmax(a,c));
d=atan2(f,c);
b=floor(c);
}
}