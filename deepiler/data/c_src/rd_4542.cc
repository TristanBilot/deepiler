#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(f,c);
c=log10(b);
a=atan(f);
g=fmin(f,b);
g=(sin(c))-(atan2(d,b));
while(isgreaterequal(e,d)){
d=acos(e);
b=atan2(d,e);
}
a=(pow(b,e))/(fmax(b,g));
g=(fmax(a,g))+(acos(f));
}