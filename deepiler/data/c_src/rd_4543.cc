#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log(f))+(pow(a,a));
e=(tan(f))-(cos(c));
e=atan2(d,e);
c=(atan2(d,f))*(pow(c,c));
f=(fmax(f,a))-(exp(e));
if(isless(e,b)){
e=(log(b))*(log10(b));
e=(atan(a))/(atan(b));
}
else{
a=pow(b,b);
d=acos(a);
a=fmax(e,c);
a=fmax(d,c);
b=(sin(a))+(sin(c));
}
if(islessequal(c,b)){
f=log(a);
f=pow(d,a);
}
else{
b=tan(f);
d=fdim(f,c);
c=(floor(c))*(ceil(f));
b=cos(e);
d=(atan(a))/(pow(c,b));
}
}