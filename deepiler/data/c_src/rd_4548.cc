#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=sin(d);
c=fdim(d,c);
c=log10(b);
a=(atan(d))+(tan(c));
a=pow(e,a);
e=(atan2(b,d))+(tan(a));
d=(acos(a))*(pow(d,c));
c=fmin(c,b);
c=atan2(e,e);
if(islessequal(d,e)){
d=pow(e,d);
c=(exp(a))/(fmin(b,a));
c=log10(b);
}
else{
e=fmax(c,c);
e=floor(c);
d=(log(a))/(log10(c));
d=sqrt(d);
d=cos(e);
}
}