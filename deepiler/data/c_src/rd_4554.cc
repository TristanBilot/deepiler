#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(log(g))/(pow(c,d));
g=(pow(d,b))+(fmax(f,b));
h=exp(g);
c=(fmax(g,h))-(sin(h));
d=acos(c);
c=fmin(d,a);
g=pow(d,h);
}