#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=sqrt(b);
c=fdim(a,a);
c=(fmin(c,e))/(fmin(d,c));
a=exp(b);
c=fmin(e,d);
e=(fmax(d,a))+(log(e));
while(isgreaterequal(b,b)){
c=sin(e);
a=(atan2(c,d))+(pow(a,e));
}
}