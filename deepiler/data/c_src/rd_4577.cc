#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(a,d);
g=atan2(a,b);
f=fmin(f,g);
if(islessgreater(d,c)){
d=(fmin(d,b))+(floor(d));
a=(exp(c))-(atan2(e,c));
g=sqrt(b);
}
else{
f=acos(e);
e=(tan(g))*(fmax(d,a));
}
g=(tan(e))-(fmin(f,d));
f=exp(e);
d=atan2(c,a);
b=log10(f);
}