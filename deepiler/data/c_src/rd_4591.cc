#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(sqrt(d))-(tan(c));
b=exp(c);
a=fdim(e,e);
a=(fmax(a,c))*(pow(e,c));
while(isgreaterequal(a,e)){
b=(fmax(c,a))*(pow(c,a));
d=(fdim(d,d))*(fdim(c,c));
e=fdim(e,e);
e=atan(d);
d=(cos(b))+(log10(c));
}
a=fmax(a,b);
a=(fdim(b,a))+(floor(d));
}