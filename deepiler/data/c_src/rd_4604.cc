#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
f=(sqrt(g))-(fdim(b,h));
while(isless(g,c)){
f=(fmax(h,a))/(fdim(g,e));
g=(atan2(c,c))/(cos(d));
e=(fmax(d,g))-(cos(g));
h=fmin(c,g);
d=sqrt(f);
}
if(islessequal(a,c)){
c=atan2(h,e);
a=log10(b);
}
}