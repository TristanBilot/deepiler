#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(d,b);
e=ceil(e);
e=log10(a);
a=cos(c);
while(islessequal(a,e)){
d=floor(b);
e=(fdim(e,e))+(asin(b));
c=(acos(d))-(fmin(e,b));
e=(sqrt(b))/(fmax(a,e));
b=sqrt(a);
}
if(islessequal(d,d)){
c=sin(e);
b=tan(d);
a=(acos(a))/(atan(a));
d=(sin(b))-(fmax(c,a));
}
else{
b=(fmin(d,b))-(sqrt(b));
d=sqrt(b);
d=fmax(d,a);
}
}