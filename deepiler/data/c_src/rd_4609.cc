#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(tan(a))-(acos(d));
a=log(c);
f=sin(b);
b=fmax(e,b);
d=log(g);
while(isless(d,d)){
c=(fmin(b,g))*(fmin(f,f));
g=(fmax(e,b))-(sqrt(c));
b=fmin(b,f);
}
}