#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(ceil(a))*(log10(d));
d=pow(d,b);
while(islessgreater(b,c)){
c=(fdim(f,f))*(pow(f,c));
a=(fmin(e,f))+(fmax(f,c));
f=(fmin(f,a))/(exp(a));
b=(pow(b,b))/(log(a));
e=exp(e);
}
while(islessgreater(b,c)){
c=fmax(e,f);
e=pow(f,b);
}
}