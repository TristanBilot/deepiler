#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(exp(e))/(atan(c));
c=(log(c))/(fmax(b,a));
d=(fmax(b,b))/(fmax(a,b));
b=(atan2(a,b))/(log(c));
if(isgreaterequal(a,b)){
e=exp(e);
a=(cos(d))/(cos(e));
e=sin(a);
}
if(isless(d,c)){
e=fmax(b,c);
a=pow(b,c);
c=(fdim(c,c))/(pow(c,c));
a=(fdim(b,e))*(atan2(d,b));
}
else{
c=(asin(a))*(pow(e,a));
e=pow(b,d);
}
}