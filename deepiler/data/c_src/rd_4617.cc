#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(e,e);
b=(sin(d))*(fdim(b,d));
a=(log10(a))+(fdim(a,d));
while(isless(b,e)){
c=(atan(b))/(cos(a));
a=acos(d);
a=sin(d);
}
if(isless(b,a)){
b=fdim(e,e);
e=log(c);
d=fdim(c,e);
a=(ceil(e))+(fmax(e,e));
}
else{
a=(log(b))-(fmax(e,c));
e=(floor(a))-(atan(b));
a=asin(d);
c=pow(a,b);
}
}