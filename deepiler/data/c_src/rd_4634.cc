#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan(h))*(fdim(f,b));
e=(atan2(h,d))+(log10(d));
e=(fmax(c,f))-(fmax(g,d));
while(isgreaterequal(a,d)){
f=(atan(g))+(fmax(g,e));
a=pow(h,f);
d=atan2(a,b);
}
e=tan(d);
g=fmin(f,e);
f=fdim(h,a);
b=fmax(d,b);
a=pow(f,c);
}