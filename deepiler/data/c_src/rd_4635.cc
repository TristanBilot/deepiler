#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(cos(g))+(ceil(b));
f=(log10(g))/(atan2(c,b));
c=(fdim(d,c))/(cos(a));
g=asin(e);
while(isgreaterequal(b,d)){
g=(fdim(e,f))/(sqrt(a));
b=fmin(f,a);
}
f=asin(f);
b=ceil(b);
}