#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=acos(a);
a=floor(c);
f=(exp(d))-(cos(e));
if(islessequal(f,f)){
d=(sqrt(g))+(pow(g,b));
f=log10(c);
e=log(b);
e=(log(a))+(atan(d));
g=ceil(d);
}
d=floor(c);
d=(pow(a,g))/(atan(f));
}