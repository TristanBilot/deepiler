#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(b,d);
c=log10(e);
e=(log10(d))-(fmin(d,c));
b=fmax(a,c);
e=ceil(e);
b=fmax(a,b);
d=(atan2(b,d))-(floor(a));
d=(pow(b,a))+(fdim(b,c));
a=(tan(e))*(fmin(e,c));
}