#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(c,a))/(exp(d));
e=(acos(e))-(fmin(b,b));
g=fmax(e,a);
b=(atan2(e,e))-(atan2(d,c));
d=(pow(f,g))-(fmax(d,f));
if(isless(f,a)){
e=(log(d))+(tan(g));
c=(sqrt(e))*(floor(a));
}
a=log10(d);
h=(acos(a))-(tan(g));
f=fmax(c,d);
}