#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(e,e);
e=asin(d);
b=log10(d);
b=(log10(c))/(fdim(a,a));
b=(exp(e))+(fmin(e,b));
e=(fmin(c,c))/(sin(d));
if(isless(a,b)){
d=(log(c))+(atan2(a,a));
a=log10(c);
}
else{
d=pow(e,b);
c=fdim(c,c);
a=fmax(e,a);
e=(atan(e))-(atan2(b,d));
e=atan2(e,b);
}
}