#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(b,e);
c=(asin(c))+(pow(a,a));
f=(fmin(f,f))/(fdim(c,c));
d=pow(c,d);
b=fmin(d,b);
if(islessgreater(b,f)){
f=(log10(e))*(atan2(a,b));
f=(fdim(f,a))-(fmin(f,b));
}
else{
d=atan2(b,d);
b=sin(f);
}
e=pow(b,e);
e=atan2(f,f);
b=(fmax(a,b))*(cos(f));
}