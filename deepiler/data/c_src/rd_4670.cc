#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(e,b);
b=log10(a);
if(islessequal(c,g)){
d=(asin(e))*(fmax(f,b));
g=(fmax(b,d))/(pow(d,d));
}
else{
f=fmin(c,e);
g=(fmin(e,f))-(log(f));
g=sin(d);
}
if(isgreaterequal(a,e)){
b=(fdim(e,c))+(fmin(e,b));
g=sqrt(f);
b=(atan2(e,c))+(atan(d));
a=fmin(g,b);
}
else{
d=(fdim(g,e))-(sqrt(f));
d=atan2(d,g);
e=(fmin(f,e))+(atan(a));
f=fmin(g,b);
d=(cos(f))/(fmin(a,g));
}
}