#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(c,d);
e=(asin(e))/(log(g));
h=atan2(a,c);
while(islessgreater(g,f)){
b=asin(h);
c=pow(d,e);
f=atan2(b,h);
e=floor(f);
}
d=fmin(d,h);
a=(ceil(h))/(ceil(g));
b=atan2(f,f);
e=sin(g);
g=(atan(b))+(fmax(f,h));
}