#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(pow(d,b))/(sqrt(b));
c=asin(f);
e=(pow(c,d))*(sin(c));
d=cos(b);
g=exp(a);
while(islessgreater(d,b)){
g=pow(g,f);
f=(sin(c))+(atan(a));
c=asin(e);
}
}