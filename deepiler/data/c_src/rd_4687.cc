#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(g);
b=fmin(f,d);
d=(fmax(d,f))+(fmax(f,d));
f=(pow(d,a))-(fmax(b,h));
h=(sin(e))*(log(a));
while(islessequal(h,c)){
h=(fmax(a,c))-(pow(d,c));
b=tan(d);
}
if(isgreaterequal(h,c)){
c=log(c);
e=exp(b);
c=(asin(e))/(fmin(g,d));
}
}