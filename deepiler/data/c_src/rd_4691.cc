#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(g,a);
f=(asin(c))*(fdim(e,c));
a=(pow(f,c))*(sin(e));
while(isless(e,d)){
d=(exp(c))/(fmax(f,g));
b=atan(e);
h=log10(a);
d=atan2(b,c);
f=floor(d);
}
h=(acos(h))/(sqrt(e));
a=log(b);
g=exp(b);
e=(fmax(b,g))-(atan2(e,b));
a=fmin(a,g);
}