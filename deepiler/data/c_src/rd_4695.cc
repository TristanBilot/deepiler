#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(cos(a))-(atan2(c,c));
b=fdim(e,c);
c=(pow(e,b))*(fdim(b,b));
a=fmin(c,a);
d=cos(b);
e=(cos(a))-(fmin(c,c));
a=log10(e);
}