#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(tan(b))*(sqrt(e));
b=(acos(d))-(asin(c));
while(isgreaterequal(a,d)){
a=sin(d);
e=acos(a);
}
if(isgreaterequal(b,a)){
e=fmax(e,e);
d=fmin(c,e);
a=exp(d);
}
}