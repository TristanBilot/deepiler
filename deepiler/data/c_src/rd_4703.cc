#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(e);
c=(ceil(e))/(ceil(e));
e=(atan(e))+(fmax(a,c));
c=(atan2(b,c))+(atan2(e,b));
a=fdim(c,b);
e=(fmin(c,e))-(sin(d));
b=(cos(b))/(fdim(e,c));
e=fdim(d,e);
a=fmax(b,b);
}