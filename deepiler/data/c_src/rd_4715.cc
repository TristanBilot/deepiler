#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(b,a);
e=(sin(a))*(fdim(c,e));
e=(ceil(a))+(fmax(c,c));
a=atan2(d,d);
d=(acos(c))+(pow(a,d));
while(islessgreater(c,d)){
d=ceil(b);
c=(fmax(e,c))-(fmin(e,b));
}
}