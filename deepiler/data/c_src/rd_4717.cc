#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(e,b))+(acos(c));
f=fmin(b,d);
if(islessequal(e,d)){
a=(sqrt(f))+(pow(b,f));
b=(fmin(f,d))*(fdim(d,b));
c=fmax(d,f);
c=log10(f);
f=atan2(f,f);
}
else{
c=acos(f);
f=(asin(b))+(sin(f));
e=pow(e,c);
f=(fdim(f,e))*(fmin(e,f));
}
d=(fmax(a,f))*(sin(b));
f=(sqrt(a))*(tan(a));
b=fmax(e,c);
}