#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(c,b))/(fmax(a,d));
c=(fdim(d,e))*(exp(e));
c=fmin(a,a);
b=(pow(c,d))*(fdim(e,a));
a=pow(a,e);
while(islessgreater(a,a)){
e=(tan(b))/(log10(a));
d=(atan2(b,c))+(fmax(c,b));
}
while(isless(b,c)){
e=pow(a,e);
a=log10(a);
d=(fmax(b,d))+(cos(b));
a=sqrt(c);
c=fmax(b,d);
}
}