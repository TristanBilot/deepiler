#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(c))-(pow(b,c));
e=asin(c);
c=(fmax(e,a))+(asin(c));
a=pow(e,e);
d=atan2(b,a);
if(islessgreater(c,b)){
d=fmax(c,b);
c=fmax(e,a);
}
else{
d=pow(e,a);
d=(fdim(e,a))/(pow(b,b));
e=atan(a);
d=(atan2(c,a))/(log10(a));
b=fmax(e,a);
}
a=(pow(c,b))+(fdim(d,a));
d=(fmax(c,b))-(atan2(b,a));
}