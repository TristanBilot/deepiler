#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(a,d))*(fmax(b,d));
a=(fmin(c,c))*(fdim(a,g));
c=(pow(c,b))+(log10(g));
c=fmax(a,g);
b=(atan(a))-(atan2(c,e));
b=(tan(b))-(fmax(e,c));
a=atan2(b,b);
g=(fmax(g,h))+(ceil(b));
e=(ceil(a))+(atan2(b,e));
e=cos(g);
b=(fdim(b,c))/(ceil(h));
g=fmax(d,e);
}