#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=cos(e);
c=(pow(b,c))/(fdim(b,a));
while(isgreaterequal(a,d)){
d=(fdim(d,d))-(asin(e));
a=fmin(e,c);
e=pow(e,e);
}
if(isless(c,b)){
a=(atan2(d,b))-(exp(e));
d=(fmax(c,d))-(fdim(a,a));
d=fmax(c,d);
}
}