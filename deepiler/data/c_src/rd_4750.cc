#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(exp(d))+(exp(e));
b=pow(a,c);
if(isless(e,e)){
a=(pow(b,c))/(fmin(b,a));
c=pow(c,b);
a=(fmin(b,f))+(pow(f,d));
f=pow(f,f);
}
else{
f=acos(a);
c=(acos(b))/(atan2(e,f));
a=(asin(d))-(log10(c));
}
if(isless(a,f)){
b=(fdim(f,a))*(tan(c));
a=(floor(e))+(cos(e));
e=atan2(c,c);
f=(sqrt(d))/(atan(b));
b=(floor(a))*(log(a));
}
else{
e=pow(c,c);
a=exp(b);
a=(pow(e,e))/(atan2(e,d));
}
}