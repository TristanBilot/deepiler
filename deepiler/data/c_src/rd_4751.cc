#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(e,f);
e=(ceil(d))+(fmax(g,h));
h=(fmin(g,e))/(fdim(h,c));
g=(sin(g))+(fmax(f,g));
h=(tan(d))-(log(d));
e=ceil(f);
b=log10(b);
b=(tan(b))-(log(g));
g=(cos(c))-(exp(g));
e=fmax(g,f);
while(islessgreater(a,f)){
f=fmax(c,f);
h=acos(a);
g=log10(b);
c=(ceil(d))/(pow(c,e));
g=asin(a);
}
}