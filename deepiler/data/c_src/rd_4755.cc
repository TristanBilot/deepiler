#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(b,d);
b=fdim(a,e);
if(isgreaterequal(d,b)){
e=(sin(a))-(asin(c));
c=(atan2(a,d))*(sin(b));
b=log(d);
b=fmax(c,e);
c=fmin(c,e);
}
while(islessgreater(d,b)){
e=acos(c);
b=(pow(c,c))-(fmax(e,b));
}
}