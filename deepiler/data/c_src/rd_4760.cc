#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log10(b))*(sin(b));
d=(acos(f))/(ceil(a));
f=(atan(c))/(log(d));
a=(fmax(e,e))-(fmax(g,c));
if(isgreaterequal(f,d)){
g=tan(g);
e=pow(e,b);
}
else{
g=sin(e);
d=(log(e))-(atan(b));
f=atan2(f,f);
b=(cos(c))+(exp(c));
a=tan(g);
}
if(isless(e,a)){
e=floor(f);
e=atan2(c,d);
a=(atan2(c,c))-(acos(f));
}
}