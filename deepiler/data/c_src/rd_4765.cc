#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(d,e))*(atan2(g,h));
e=(cos(g))+(pow(b,a));
while(islessequal(g,f)){
h=(fdim(h,g))*(ceil(h));
h=fdim(b,d);
}
a=fmax(e,d);
b=(exp(d))-(atan2(c,e));
d=pow(c,c);
e=(pow(h,a))-(acos(e));
}