#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(d,a);
d=ceil(c);
e=(log10(c))*(atan2(a,b));
c=(fmin(e,c))*(cos(f));
b=pow(f,e);
d=fmax(b,e);
while(isgreaterequal(b,a)){
d=(fmin(f,a))/(atan(c));
f=(asin(f))*(tan(b));
e=(sin(f))/(fmax(f,a));
}
}