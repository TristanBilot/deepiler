#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(exp(a))/(pow(e,b));
e=fmax(f,c);
e=(sqrt(a))-(ceil(c));
while(islessequal(b,c)){
c=pow(e,e);
c=(pow(e,c))*(fmin(d,f));
f=(fmin(f,d))*(pow(b,b));
f=exp(a);
}
while(islessgreater(f,e)){
d=floor(b);
f=(sin(c))+(atan2(c,a));
b=fmin(b,e);
f=(log10(d))*(pow(c,d));
c=fmax(b,c);
}
}