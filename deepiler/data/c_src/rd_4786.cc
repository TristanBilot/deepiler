#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmax(e,e);
c=pow(c,a);
f=(pow(b,d))-(sqrt(d));
b=(cos(d))-(tan(c));
if(islessequal(a,f)){
e=fmax(b,e);
c=(fdim(b,e))*(pow(c,e));
c=atan2(f,a);
}
}