#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(b,f);
a=fmin(b,e);
g=log10(h);
c=(cos(e))*(sin(e));
g=fmin(b,c);
if(isgreaterequal(d,e)){
f=atan(f);
f=(fdim(e,c))-(log10(f));
}
else{
d=(cos(g))+(fdim(f,c));
c=(sqrt(d))+(asin(b));
b=(fdim(c,a))/(log(f));
f=fdim(d,h);
}
if(islessequal(b,b)){
g=(fmax(d,e))+(log(g));
f=fmin(b,d);
f=exp(e);
h=(fdim(g,a))*(tan(h));
d=pow(c,e);
}
}