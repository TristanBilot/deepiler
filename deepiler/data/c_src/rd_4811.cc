#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,c))+(pow(e,c));
a=(sqrt(c))*(exp(a));
a=pow(d,d);
a=fmax(b,a);
a=(sqrt(a))*(pow(e,e));
if(islessequal(b,e)){
a=exp(d);
a=sin(e);
}
else{
e=(tan(a))*(pow(c,a));
c=tan(a);
d=log10(d);
c=fdim(b,a);
}
while(islessequal(e,e)){
a=(tan(d))/(fmax(a,e));
a=(fmax(b,a))+(log(b));
}
}