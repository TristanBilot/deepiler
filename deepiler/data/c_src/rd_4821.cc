#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(b);
b=(fmax(d,c))*(sqrt(h));
f=(log(d))+(pow(g,h));
d=pow(b,g);
if(isless(g,c)){
c=(floor(a))/(exp(e));
h=(fdim(f,a))/(asin(g));
b=fmin(a,b);
f=(pow(g,a))-(pow(d,b));
g=(exp(e))+(atan2(a,g));
}
else{
h=fmin(a,e);
a=(log10(f))/(fmax(d,c));
}
while(isless(b,b)){
c=(log10(f))-(fdim(b,b));
a=(fmin(g,e))-(cos(f));
g=(sin(h))/(pow(f,h));
b=log10(g);
}
}