#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(c,e))-(exp(d));
a=atan(b);
e=(fmax(b,c))+(sin(a));
c=(atan(a))+(cos(a));
while(islessequal(a,a)){
e=exp(c);
d=(floor(a))/(log10(b));
d=exp(e);
}
while(isless(c,d)){
b=acos(c);
d=floor(c);
}
}