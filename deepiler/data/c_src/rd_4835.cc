#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log10(a))+(log10(b));
a=pow(b,e);
b=(fmax(a,b))+(pow(d,c));
a=(fdim(d,a))/(sin(e));
d=log(a);
b=asin(b);
a=fmax(d,c);
d=(acos(d))/(atan(d));
c=fmax(d,d);
a=(pow(c,e))*(fmin(d,a));
}