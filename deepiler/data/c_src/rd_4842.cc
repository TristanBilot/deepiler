#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(c,a))/(acos(a));
c=asin(d);
b=sqrt(a);
c=log10(e);
a=(fmax(b,d))+(asin(b));
while(islessequal(a,e)){
b=(tan(b))*(atan2(b,e));
b=(pow(c,d))/(exp(d));
a=(fdim(c,a))/(atan(b));
d=ceil(a);
}
while(isless(e,b)){
d=fdim(c,a);
d=fdim(a,a);
b=(tan(d))/(atan2(b,d));
c=fdim(d,c);
}
}