#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=acos(c);
d=floor(e);
while(islessgreater(c,c)){
a=log10(b);
a=(fdim(d,c))*(pow(e,a));
d=(pow(e,e))*(exp(c));
d=log(a);
c=fmax(c,a);
}
e=(floor(d))+(sqrt(c));
e=(fdim(a,d))+(fdim(c,a));
e=(pow(a,e))*(atan2(b,a));
a=(sin(a))+(tan(c));
e=fmax(e,e);
}