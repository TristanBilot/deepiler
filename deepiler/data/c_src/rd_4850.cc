#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan(d))/(exp(d));
b=(sqrt(a))/(sqrt(a));
a=fmax(d,d);
c=fdim(b,b);
d=fmin(e,b);
while(islessequal(c,e)){
e=cos(c);
b=(fdim(a,b))*(fmin(b,c));
b=sin(e);
}
if(isgreaterequal(e,c)){
a=(fmax(a,b))+(pow(e,e));
e=fdim(a,d);
}
else{
a=(tan(a))-(pow(e,e));
c=tan(b);
e=(exp(d))*(atan2(d,e));
e=tan(e);
}
}