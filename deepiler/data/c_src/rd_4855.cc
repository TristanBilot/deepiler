#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(f);
c=(acos(h))+(atan(d));
f=(log(c))+(fdim(c,c));
b=(cos(e))/(atan2(a,b));
if(islessequal(c,b)){
g=exp(c);
d=pow(a,h);
}
else{
b=(fdim(g,e))+(fmin(f,f));
d=acos(b);
c=(atan2(a,c))-(pow(d,d));
d=(fmin(g,a))+(pow(b,a));
f=atan2(c,d);
}
g=(sqrt(f))-(atan(c));
a=(fmax(g,h))-(asin(b));
e=tan(f);
a=sqrt(d);
h=log10(h);
}