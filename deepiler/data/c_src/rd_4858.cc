#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(pow(a,c))+(pow(d,g));
e=exp(d);
c=atan(b);
a=log10(a);
while(islessgreater(d,a)){
e=(asin(g))/(atan2(b,g));
d=atan2(d,g);
f=floor(f);
}
if(islessequal(f,a)){
c=(pow(b,c))/(fmax(g,e));
c=atan(d);
f=atan2(g,g);
e=(log10(f))*(fdim(c,a));
d=pow(a,e);
}
}