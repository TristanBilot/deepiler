#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(e,d))-(pow(d,a));
d=cos(g);
b=(fmin(a,b))+(log10(a));
f=cos(d);
d=cos(c);
while(isgreaterequal(c,b)){
a=exp(f);
b=(fdim(b,c))/(fmin(c,f));
a=(cos(e))/(fmin(b,g));
b=asin(b);
g=exp(g);
}
a=tan(c);
c=asin(g);
}