#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(b);
a=(pow(a,d))/(log(c));
d=log(b);
c=(cos(d))+(pow(c,e));
b=atan2(b,e);
while(isless(d,e)){
a=(exp(c))-(atan(d));
b=(sqrt(d))+(pow(a,e));
b=(log(b))/(fmax(e,d));
}
}