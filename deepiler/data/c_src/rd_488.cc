#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(ceil(e))-(fmax(e,a));
c=(log(b))-(fdim(b,d));
d=cos(a);
d=ceil(c);
if(islessequal(d,e)){
c=(fdim(c,a))*(log10(c));
a=(fmin(c,b))/(ceil(a));
a=asin(a);
c=fmin(d,b);
b=(sqrt(d))-(asin(c));
}
if(islessgreater(a,e)){
c=(exp(a))*(fdim(c,c));
b=(floor(d))/(atan2(d,b));
d=ceil(a);
a=pow(a,e);
b=fmin(d,b);
}
}