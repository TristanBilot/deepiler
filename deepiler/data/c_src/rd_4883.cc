#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log(a);
f=fmax(b,c);
a=cos(g);
if(isgreaterequal(h,d)){
e=sqrt(a);
h=acos(f);
e=(fmin(e,d))+(sin(f));
b=(pow(c,d))/(fmin(d,f));
}
else{
c=(fmin(f,h))+(sqrt(g));
f=(fmax(c,b))*(atan2(e,c));
}
while(islessequal(d,b)){
e=(pow(g,d))-(cos(c));
e=(sin(e))+(log10(h));
f=fdim(f,c);
b=fmax(d,e);
}
}