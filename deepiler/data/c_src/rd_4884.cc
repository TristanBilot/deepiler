#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(f,d))*(fmin(d,h));
g=(acos(a))-(tan(a));
while(isgreaterequal(b,d)){
e=(acos(b))+(log10(g));
b=(tan(b))*(log10(g));
h=(tan(f))-(exp(e));
f=(atan2(c,a))/(fdim(g,b));
e=(fmin(c,b))/(pow(c,a));
}
if(islessequal(h,b)){
f=pow(f,a);
b=exp(h);
c=ceil(f);
b=(exp(a))-(fmax(g,h));
b=(log(h))-(fmax(b,b));
}
else{
a=acos(g);
e=fdim(c,b);
a=(asin(f))+(fdim(g,c));
c=(fmax(d,f))+(fdim(d,e));
d=floor(f);
}
}