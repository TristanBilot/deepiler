#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(b,g))/(fdim(e,h));
g=(fmax(f,b))*(pow(h,f));
b=fmax(f,b);
d=tan(a);
f=log10(d);
while(isgreaterequal(h,c)){
h=(pow(h,g))-(pow(e,f));
d=log(f);
b=fmax(b,e);
}
while(isgreaterequal(b,h)){
f=(atan2(g,g))+(tan(d));
d=asin(h);
}
}