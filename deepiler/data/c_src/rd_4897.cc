#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(d);
c=log10(d);
b=(log(b))/(fdim(e,a));
c=pow(b,d);
if(islessequal(b,a)){
a=fmin(a,c);
c=(fmin(c,b))/(fmin(a,b));
}
a=(pow(b,d))/(atan2(e,a));
a=fmax(a,b);
a=(acos(a))/(cos(b));
d=(ceil(d))*(fmax(a,d));
b=(floor(b))*(cos(e));
}