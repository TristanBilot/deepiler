#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(ceil(b))/(fmin(b,a));
d=pow(a,b);
a=(fmin(b,b))/(exp(e));
while(islessgreater(c,c)){
c=(fmin(d,d))/(pow(a,c));
d=(pow(d,d))-(pow(d,c));
c=cos(b);
e=atan2(c,a);
a=(log(d))-(atan2(c,a));
}
if(isless(e,a)){
d=(sin(c))*(log(d));
e=(acos(d))/(log10(d));
c=(fdim(c,d))+(fmin(c,d));
b=fdim(e,e);
d=(tan(c))/(fmin(a,e));
}
}