#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=tan(c);
f=(tan(c))-(floor(f));
f=fdim(b,g);
g=(log10(e))-(atan2(e,e));
d=floor(a);
a=(pow(d,f))*(fmax(e,a));
b=log(g);
b=fmin(c,c);
f=(tan(g))-(tan(d));
d=(pow(d,b))-(exp(b));
d=fdim(b,e);
a=(asin(f))*(floor(g));
}