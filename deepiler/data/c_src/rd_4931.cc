#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=acos(c);
c=(acos(e))+(atan(e));
if(islessgreater(d,a)){
d=fdim(c,c);
d=exp(e);
b=(log(d))+(log10(d));
}
b=fmax(d,a);
b=(fmax(b,a))-(fmin(d,c));
b=(asin(d))/(fdim(c,a));
}