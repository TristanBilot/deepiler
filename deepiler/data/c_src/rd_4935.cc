#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmax(c,d))/(fdim(a,a));
c=fdim(e,h);
e=exp(a);
e=(pow(b,d))-(acos(b));
b=(atan2(d,a))*(cos(f));
while(isgreaterequal(g,e)){
a=(fdim(e,a))/(fdim(f,d));
f=floor(h);
d=(fmax(e,c))/(atan(a));
b=fmax(b,b);
e=(fmax(c,d))/(sqrt(b));
}
e=log10(f);
h=fmin(b,d);
c=(ceil(e))+(pow(c,a));
b=fdim(a,g);
g=atan(c);
}