#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(acos(b))*(pow(a,b));
c=floor(e);
e=fmin(a,b);
f=cos(d);
c=(pow(c,c))+(log(a));
if(islessequal(f,d)){
f=tan(e);
b=asin(c);
}
d=(floor(e))/(atan(f));
d=pow(a,d);
e=fmax(c,f);
}