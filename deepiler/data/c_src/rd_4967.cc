#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(sqrt(f))+(atan2(c,f));
g=pow(g,b);
f=(fmin(c,c))-(atan2(g,d));
f=(fmin(e,a))+(atan2(e,c));
g=floor(f);
while(islessequal(f,e)){
a=sqrt(d);
c=(ceil(e))+(asin(d));
g=fmax(c,g);
}
e=acos(a);
e=atan2(g,b);
a=(fdim(b,c))*(atan(f));
e=cos(a);
}