#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(e);
d=pow(c,c);
c=asin(a);
b=pow(b,d);
b=(log10(c))*(fmin(a,e));
e=(log(a))*(fmax(a,e));
c=atan2(d,e);
e=atan(a);
while(islessequal(d,e)){
c=pow(e,c);
e=log10(d);
c=(atan2(a,a))+(sin(b));
}
}