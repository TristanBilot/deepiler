#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(d,d))/(log10(g));
f=fmax(b,c);
e=atan2(h,c);
while(isless(f,a)){
e=sin(b);
f=(tan(g))+(tan(b));
c=(fmax(e,c))-(sqrt(d));
c=(fmin(c,h))-(tan(a));
c=(fmax(g,f))/(sqrt(b));
}
c=atan2(g,c);
h=cos(f);
}