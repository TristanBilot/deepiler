#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(d,b))+(sqrt(a));
e=atan(b);
d=(exp(c))*(atan2(a,e));
c=atan2(d,d);
while(isgreaterequal(c,c)){
d=(atan2(d,b))+(log(b));
c=fmax(b,e);
b=atan2(d,c);
c=(acos(c))+(atan2(a,d));
}
if(islessgreater(a,a)){
d=(asin(b))+(atan2(b,d));
b=(fmin(b,c))*(atan(a));
}
else{
a=(log10(b))-(fmax(c,a));
e=(ceil(a))+(exp(d));
}
}