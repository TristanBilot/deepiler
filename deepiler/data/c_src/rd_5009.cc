#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fdim(c,d))*(ceil(b));
a=ceil(d);
a=(pow(a,e))+(floor(f));
if(isgreaterequal(f,f)){
b=(fmax(c,b))/(fmax(c,a));
f=(sin(a))/(sin(e));
}
if(isgreaterequal(f,a)){
c=tan(f);
a=(atan(c))*(log10(e));
b=(acos(c))-(log(f));
a=(fmax(b,a))+(acos(c));
f=atan(c);
}
}