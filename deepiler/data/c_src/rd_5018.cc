#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,a))*(sin(e));
c=fmax(a,a);
e=ceil(d);
d=(pow(b,b))-(fdim(e,c));
d=atan(d);
b=(cos(c))+(log(c));
d=(log10(b))/(fmin(e,e));
while(isless(d,d)){
d=cos(b);
d=(pow(b,d))/(fmax(b,e));
b=(atan2(d,b))+(fmax(d,e));
b=fmin(a,a);
d=(asin(e))/(cos(e));
}
}