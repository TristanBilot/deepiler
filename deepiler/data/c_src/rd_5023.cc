#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log10(g))*(fdim(b,a));
g=(floor(b))*(atan2(c,f));
b=fdim(f,c);
g=pow(b,c);
a=(asin(b))*(sin(a));
c=log10(d);
e=fmax(g,e);
while(isless(a,b)){
f=(log10(c))-(floor(e));
e=(fmin(d,f))/(atan2(d,a));
f=(pow(d,f))+(acos(d));
}
}