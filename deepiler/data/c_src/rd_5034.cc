#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(pow(c,d))-(fdim(a,e));
e=fdim(a,a);
c=fdim(b,c);
b=sin(c);
e=log(d);
d=(atan2(c,b))+(atan2(e,a));
c=pow(a,d);
c=(fmin(c,c))-(atan2(e,b));
a=(sqrt(d))+(cos(c));
c=fmax(c,a);
b=sin(d);
d=(fdim(b,a))-(fmax(c,b));
}