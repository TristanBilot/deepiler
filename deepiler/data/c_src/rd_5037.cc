#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=exp(f);
c=(fdim(b,d))/(cos(b));
while(islessgreater(f,b)){
b=(fdim(d,e))/(pow(a,c));
a=sqrt(c);
e=(cos(a))-(atan2(b,b));
d=fmin(d,e);
}
while(islessgreater(b,f)){
a=pow(f,e);
c=(atan(a))-(tan(e));
b=exp(e);
c=fdim(f,e);
f=pow(d,e);
}
}