#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(a);
a=fdim(d,d);
d=fdim(a,e);
if(islessgreater(b,b)){
a=log(e);
d=fdim(b,a);
a=pow(d,d);
}
if(isgreaterequal(b,d)){
a=(pow(a,e))*(fdim(c,c));
a=(ceil(d))*(fmax(d,c));
}
else{
c=atan2(c,b);
a=tan(a);
e=(log10(d))+(sqrt(d));
e=fdim(c,e);
b=pow(e,e);
}
}