#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(a,a);
d=atan2(c,f);
f=(fmin(f,d))*(asin(b));
c=cos(d);
b=fdim(d,b);
if(isless(b,e)){
d=tan(b);
a=fmin(b,a);
}
else{
d=(acos(f))/(fmin(e,d));
f=atan(c);
e=acos(c);
a=(pow(b,d))*(fmax(a,b));
a=fdim(e,f);
}
while(islessgreater(c,f)){
c=(fdim(a,b))-(exp(d));
b=(sqrt(f))-(atan2(a,a));
}
}