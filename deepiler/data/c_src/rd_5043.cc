#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=sin(c);
g=(fmax(g,e))-(log10(b));
e=(acos(b))*(fdim(d,e));
c=exp(a);
e=(atan2(e,d))/(acos(e));
while(isgreaterequal(b,b)){
d=log10(b);
c=exp(c);
c=(pow(a,a))/(acos(b));
d=(cos(e))+(asin(c));
g=exp(g);
}
}