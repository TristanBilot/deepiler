#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(d,c))*(log10(b));
d=fdim(e,b);
b=cos(a);
while(islessgreater(c,c)){
c=fmax(b,c);
e=fdim(a,a);
e=fmax(e,b);
e=atan(e);
d=(fdim(c,d))-(fmax(e,b));
}
while(isgreaterequal(c,b)){
d=(pow(d,c))-(sqrt(e));
e=asin(c);
e=asin(b);
c=(fmin(b,d))*(fmin(c,c));
e=(cos(a))+(fmax(c,c));
}
}