#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(tan(d))-(fmin(a,c));
c=(tan(d))-(pow(a,c));
while(isgreaterequal(e,a)){
e=(acos(c))/(asin(c));
b=(asin(e))-(cos(d));
}
if(islessequal(c,a)){
c=(atan2(e,c))-(fmax(d,c));
b=asin(b);
d=log10(d);
}
else{
a=fmin(e,d);
c=(fmax(b,d))*(log(e));
d=log(c);
}
}