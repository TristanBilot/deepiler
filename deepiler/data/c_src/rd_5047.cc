#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(b,d);
e=(exp(d))+(log(b));
c=(floor(e))/(fmin(d,a));
f=floor(a);
e=(cos(a))-(atan2(f,a));
while(islessequal(f,b)){
b=fmax(f,d);
b=(exp(d))*(tan(e));
e=asin(b);
f=(fmax(c,a))*(fmin(a,f));
d=(atan2(d,c))/(tan(c));
}
b=(acos(c))/(fmin(c,b));
e=sqrt(b);
d=pow(c,b);
e=(fmax(c,d))+(atan2(a,c));
f=(pow(f,b))-(fdim(a,e));
}