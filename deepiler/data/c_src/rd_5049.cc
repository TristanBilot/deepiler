#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=acos(e);
b=fmax(c,e);
f=(tan(d))-(asin(a));
if(islessequal(h,d)){
e=sin(c);
g=(fdim(f,b))*(asin(g));
b=fdim(d,a);
c=ceil(d);
}
else{
a=log(e);
g=fmax(f,c);
f=(fdim(g,b))+(fmin(b,e));
}
if(islessgreater(c,b)){
d=(asin(c))+(pow(f,g));
g=sqrt(e);
}
}