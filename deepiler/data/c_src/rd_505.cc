#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(log(e))-(pow(a,c));
g=sqrt(g);
a=pow(b,h);
f=pow(g,b);
while(islessgreater(c,d)){
h=(exp(g))-(fmin(d,f));
h=atan2(a,h);
}
if(isgreaterequal(e,a)){
a=fdim(c,d);
c=(log(d))+(sin(b));
a=(log(f))+(atan2(c,c));
a=tan(e);
f=(pow(e,e))-(fmin(a,a));
}
else{
h=atan2(c,h);
a=tan(h);
h=pow(c,b);
c=(fmax(e,b))*(exp(g));
}
}