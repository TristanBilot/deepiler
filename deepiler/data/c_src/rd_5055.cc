#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(log10(a))+(fdim(e,d));
e=(cos(e))-(fmax(d,c));
d=fmin(e,a);
b=(pow(d,a))-(log10(e));
while(isless(d,e)){
a=(tan(d))/(fmax(b,c));
e=fmax(d,d);
e=(sin(c))-(asin(a));
a=(ceil(a))+(ceil(b));
}
a=sqrt(b);
d=ceil(d);
}