#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(e);
f=sqrt(e);
b=(fdim(h,f))+(fdim(d,b));
a=(fdim(f,d))*(asin(b));
f=(fmin(a,b))+(atan2(d,e));
h=(fmin(c,g))-(fmin(e,d));
g=log10(b);
c=(pow(f,f))*(asin(h));
}