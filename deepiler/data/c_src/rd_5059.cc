#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(b,a))+(fmin(e,d));
e=(ceil(a))+(sin(d));
c=fmin(e,d);
if(isless(a,c)){
b=(atan2(d,b))/(log10(c));
e=(atan2(a,e))-(fmin(e,e));
d=(log10(b))*(sin(a));
c=floor(d);
}
else{
d=(fmin(c,a))/(atan2(b,a));
d=(fmin(a,d))/(fmin(d,e));
}
c=(pow(b,c))*(sqrt(a));
e=pow(c,b);
}