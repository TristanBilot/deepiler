#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fdim(a,d);
c=asin(d);
if(islessequal(c,e)){
e=sin(e);
b=atan2(a,d);
}
f=(asin(c))*(ceil(e));
d=exp(f);
}