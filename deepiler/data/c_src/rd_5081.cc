#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(d,a);
b=(exp(d))-(atan(e));
f=(tan(b))-(exp(d));
e=log10(f);
c=(pow(d,e))*(fmax(f,c));
a=ceil(f);
if(islessequal(a,b)){
e=fmin(f,c);
e=fmin(d,b);
f=(exp(a))+(log10(a));
d=(atan(e))+(fmin(b,d));
}
else{
a=(fmin(c,a))*(atan2(c,a));
d=fmax(e,e);
c=atan2(f,b);
d=fmin(b,a);
}
}