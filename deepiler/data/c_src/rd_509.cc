#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(log(c))*(tan(b));
c=fmin(d,c);
if(isgreaterequal(b,b)){
a=log10(b);
c=tan(e);
e=(asin(c))*(atan2(a,d));
}
while(isless(e,e)){
c=fmax(e,a);
e=(sin(d))/(sin(c));
a=exp(e);
}
}