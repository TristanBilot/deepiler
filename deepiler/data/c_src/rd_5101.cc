#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(c,a);
d=atan2(f,e);
a=(fmax(b,b))/(pow(d,a));
if(islessequal(c,f)){
f=(atan(e))/(cos(b));
d=fdim(d,f);
e=fmax(a,b);
a=(cos(d))/(asin(c));
}
if(islessequal(c,b)){
b=(log10(e))-(fdim(c,d));
d=(floor(e))*(fmax(d,d));
}
}