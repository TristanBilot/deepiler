#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(g);
b=fmin(c,b);
b=(fmax(a,f))-(fdim(c,e));
a=log10(a);
while(islessequal(f,b)){
e=acos(g);
g=fdim(c,d);
}
if(isless(d,h)){
c=(pow(g,e))/(pow(b,a));
b=fmax(e,h);
e=atan2(e,c);
}
else{
e=(fmax(a,d))*(fdim(c,d));
h=exp(c);
h=(pow(g,f))*(log(b));
g=atan(g);
}
}