#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan2(a,c))+(fmin(a,f));
d=exp(a);
h=(ceil(f))*(fdim(g,h));
b=atan2(e,b);
if(isless(d,a)){
f=(fmax(f,d))*(tan(g));
b=(floor(g))-(fmin(h,g));
h=fmax(b,c);
}
else{
a=pow(b,f);
c=atan(e);
g=log(e);
f=(sqrt(b))*(log10(d));
}
a=(log10(e))*(fmin(h,h));
g=sqrt(a);
e=(exp(c))-(fdim(e,h));
c=fmax(d,d);
f=fmin(b,c);
}