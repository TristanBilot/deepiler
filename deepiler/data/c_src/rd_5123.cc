#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(a,b))-(pow(h,g));
c=floor(h);
c=(asin(a))-(log(h));
e=(atan2(c,b))*(acos(h));
g=(fmax(d,d))+(sin(c));
f=fmin(f,b);
g=fdim(d,a);
g=(floor(f))*(sqrt(c));
e=(log10(a))/(asin(c));
h=(atan2(e,b))*(atan2(f,f));
}