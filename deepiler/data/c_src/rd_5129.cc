#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(d,d);
e=(fdim(b,a))*(exp(c));
c=(atan2(a,b))+(pow(b,b));
d=(fmax(b,c))-(log10(a));
d=(fmax(d,b))/(fdim(b,e));
while(isgreaterequal(a,c)){
e=(fmax(b,e))+(pow(e,a));
c=acos(e);
b=log10(c);
c=sin(a);
}
while(islessgreater(d,e)){
b=fmin(d,a);
b=(cos(a))-(pow(c,c));
b=(cos(a))*(pow(e,d));
c=(exp(a))/(atan2(c,b));
}
}