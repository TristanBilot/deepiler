#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(e,e))-(atan2(a,b));
f=(pow(e,f))*(fmax(f,b));
d=sqrt(c);
f=(floor(e))-(acos(c));
f=(atan(e))-(fmin(c,c));
c=(exp(b))-(fmin(a,b));
a=fmax(c,e);
e=(pow(e,f))+(log10(f));
while(isgreaterequal(d,f)){
d=(tan(a))-(atan2(e,e));
f=(log10(a))+(fmax(e,d));
a=acos(a);
f=(pow(b,b))-(tan(a));
}
}