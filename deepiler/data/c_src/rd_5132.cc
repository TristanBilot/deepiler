#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(c);
b=sqrt(d);
if(islessequal(d,d)){
d=asin(a);
e=log(a);
d=fmin(c,e);
d=log10(b);
e=(cos(c))-(fmin(e,b));
}
if(isless(b,c)){
c=pow(c,d);
d=(atan2(d,b))*(atan2(e,e));
a=(atan2(e,e))*(fmax(e,a));
b=(sin(a))+(fmax(b,c));
d=(pow(d,d))/(fdim(e,e));
}
}