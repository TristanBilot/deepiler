#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log10(a);
b=sqrt(e);
e=(sin(c))/(fmax(a,e));
if(islessequal(a,e)){
a=pow(e,a);
c=atan2(a,c);
d=(pow(a,b))*(sin(c));
e=(atan2(c,e))-(acos(d));
e=(asin(d))/(fmin(e,b));
}
else{
b=pow(e,d);
c=atan2(e,b);
a=(floor(e))-(tan(d));
d=(floor(c))+(acos(d));
d=atan2(d,b);
}
while(isgreaterequal(c,b)){
a=(fmin(c,d))/(ceil(c));
c=(acos(b))*(fmax(d,b));
b=pow(b,d);
b=log(d);
c=(atan2(d,d))+(floor(e));
}
}