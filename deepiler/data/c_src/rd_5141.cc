#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(a,d))+(pow(a,e));
c=(asin(b))-(log10(d));
b=sin(e);
while(isgreaterequal(d,d)){
e=(pow(d,e))/(fdim(a,a));
a=pow(d,e);
}
while(islessequal(d,c)){
b=ceil(b);
d=fmax(c,b);
e=(cos(b))+(exp(d));
c=(tan(a))+(fdim(a,d));
}
}