#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(tan(e))*(cos(d));
c=(fdim(f,f))+(ceil(c));
b=(fmin(e,e))+(atan2(c,c));
f=log10(c);
if(isgreaterequal(e,c)){
d=(ceil(f))*(atan(b));
c=(log(c))/(cos(b));
a=(fmax(a,e))*(sqrt(c));
}
else{
a=cos(f);
b=log(a);
}
f=(floor(d))+(atan2(d,f));
b=(cos(a))*(atan2(d,d));
c=(atan2(d,d))-(atan(c));
}