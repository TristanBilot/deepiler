#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(c);
e=floor(e);
d=(pow(b,d))*(log(e));
if(islessequal(a,c)){
a=(fmax(c,e))/(tan(d));
a=sqrt(e);
c=fdim(b,e);
d=(log(c))+(fmin(d,e));
a=fmax(b,b);
}
c=log10(b);
a=fmin(a,b);
d=atan2(a,c);
d=fmin(c,d);
a=fmax(e,a);
}