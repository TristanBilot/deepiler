#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(atan2(c,b))*(asin(e));
h=tan(c);
b=(exp(e))*(cos(a));
e=sin(h);
while(isless(g,c)){
e=log10(g);
a=acos(a);
}
}