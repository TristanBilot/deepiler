#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(log10(c))+(log(e));
e=(cos(b))/(fmin(c,e));
a=(exp(c))-(sin(e));
a=tan(c);
e=(fdim(e,e))+(pow(e,a));
e=atan2(b,a);
e=pow(d,c);
d=atan(f);
a=atan2(f,b);
f=(log10(b))-(sin(f));
e=pow(d,c);
}