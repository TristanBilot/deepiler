#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(d,d);
a=floor(d);
c=fdim(d,d);
a=(log(c))+(fmin(c,d));
a=(tan(e))-(exp(a));
while(islessgreater(d,b)){
c=fdim(a,a);
a=(log10(d))-(sin(a));
}
if(islessequal(e,c)){
e=floor(a);
e=(atan2(d,b))-(fmax(c,c));
e=(ceil(e))*(fmin(c,d));
d=(floor(e))-(cos(c));
}
else{
d=(exp(a))-(sqrt(b));
c=(fdim(b,c))-(fmax(a,a));
d=cos(d);
}
}