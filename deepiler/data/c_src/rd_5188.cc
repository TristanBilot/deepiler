#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(tan(b))+(asin(a));
c=(log(b))+(fmin(c,d));
while(isgreaterequal(a,e)){
a=(ceil(b))+(fmax(d,d));
a=(acos(b))*(fmax(d,b));
}
e=pow(e,a);
c=(pow(d,c))+(floor(b));
}