#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(e);
b=fdim(c,a);
b=(asin(e))/(asin(a));
if(isless(e,b)){
c=(sqrt(e))/(fmin(c,e));
a=log10(d);
}
else{
b=sin(c);
e=(fmax(b,e))*(fmin(c,c));
c=sqrt(c);
a=(fmin(e,d))/(tan(d));
}
a=(pow(c,b))*(fdim(d,d));
e=(fdim(d,e))*(fdim(b,a));
c=pow(b,b);
c=log10(b);
}