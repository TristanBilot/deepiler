#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(b,d))+(exp(h));
a=(pow(f,f))/(floor(g));
f=(exp(a))-(fdim(g,a));
while(islessgreater(a,h)){
b=floor(g);
c=log10(h);
a=(fdim(c,c))-(fdim(e,c));
e=fmax(f,d);
}
while(isless(a,e)){
c=ceil(d);
f=(sqrt(f))+(floor(h));
f=(fmin(f,c))/(sin(c));
b=pow(f,a);
e=log(c);
}
}