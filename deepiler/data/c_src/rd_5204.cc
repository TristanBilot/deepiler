#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan(c);
d=fmin(g,f);
d=(exp(b))+(log(d));
while(isgreaterequal(f,d)){
a=pow(e,f);
e=pow(d,f);
g=acos(d);
}
if(isless(d,g)){
a=floor(c);
g=(floor(b))/(fmax(e,d));
e=atan(b);
g=sqrt(e);
e=(log10(b))+(log10(d));
}
}