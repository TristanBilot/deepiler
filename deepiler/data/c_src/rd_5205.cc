#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=acos(d);
e=(exp(b))*(sin(a));
c=(exp(e))-(exp(f));
e=(ceil(c))*(log(e));
g=tan(c);
if(isless(b,e)){
e=fmax(f,b);
c=atan2(a,d);
e=atan(e);
d=fdim(a,c);
e=fdim(b,b);
}
if(isless(e,b)){
g=(acos(d))*(log(e));
e=(ceil(e))/(fdim(f,c));
e=fmin(b,g);
f=(acos(e))-(atan2(e,a));
}
else{
c=(fmin(b,a))*(pow(b,d));
f=acos(b);
}
}