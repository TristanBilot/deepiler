#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(acos(g))-(fdim(d,d));
f=(fmax(e,c))/(fdim(f,d));
g=(fmax(d,c))/(fdim(f,b));
f=(ceil(c))/(pow(f,e));
b=atan2(a,g);
if(isless(e,g)){
g=sin(b);
d=(fmax(e,g))+(fdim(c,a));
e=(log10(b))+(tan(a));
}
else{
f=cos(e);
b=(asin(b))*(fdim(g,d));
}
}