#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(e,a))*(fdim(e,b));
a=fmax(c,d);
if(islessequal(a,a)){
b=floor(b);
d=tan(e);
d=(tan(d))*(sin(e));
}
else{
a=(floor(d))-(pow(a,d));
b=(fmax(a,e))*(atan2(b,b));
d=atan(e);
a=(exp(d))*(pow(d,d));
}
if(islessgreater(c,c)){
c=(floor(c))-(ceil(b));
c=floor(e);
b=(exp(a))*(fmin(c,d));
e=(fmax(b,a))+(ceil(c));
d=(fmax(d,d))/(fmin(c,c));
}
else{
b=pow(c,d);
e=fmax(d,d);
d=(fmin(b,b))/(log(a));
}
}