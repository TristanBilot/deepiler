#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(cos(b))/(acos(e));
a=fmax(d,c);
f=sqrt(a);
f=(log10(a))-(acos(d));
if(isgreaterequal(f,b)){
d=(cos(a))+(tan(c));
b=(log(f))*(sin(a));
c=atan2(a,e);
}
if(isless(a,f)){
e=(fdim(f,b))-(exp(a));
c=log(a);
}
else{
d=pow(b,a);
d=(atan2(d,e))*(log10(b));
a=(fdim(a,c))-(asin(c));
d=(exp(b))*(acos(f));
b=tan(f);
}
}