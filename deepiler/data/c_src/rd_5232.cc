#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(a,b))+(asin(d));
b=log10(b);
d=(fmax(d,b))*(ceil(d));
if(isless(e,b)){
d=(fmin(c,c))-(fdim(d,c));
a=atan(d);
e=(fdim(d,d))+(pow(c,b));
}
if(isgreaterequal(c,b)){
e=cos(c);
d=tan(e);
}
else{
d=(atan(a))*(fmax(a,c));
b=(fdim(d,c))-(fdim(b,d));
d=fmax(b,d);
a=floor(d);
c=(cos(a))-(ceil(b));
}
}