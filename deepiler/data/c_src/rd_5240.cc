#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(acos(d))/(pow(b,d));
b=(atan2(d,c))*(acos(d));
b=(exp(a))/(atan(d));
a=(fmin(b,c))+(cos(a));
if(islessequal(a,e)){
e=(fdim(c,e))*(fmin(e,c));
b=asin(a);
}
e=(atan2(d,d))-(acos(a));
e=atan2(c,a);
d=log10(a);
}