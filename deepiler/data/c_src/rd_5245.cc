#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(b);
f=(fmin(b,b))+(fmax(a,b));
h=(exp(g))*(exp(h));
while(isless(e,a)){
a=(acos(f))*(asin(b));
c=(fmin(c,e))/(pow(f,e));
}
if(isgreaterequal(a,a)){
f=(fmax(h,c))-(atan2(a,h));
a=(cos(g))*(log10(g));
g=fdim(b,g);
d=fdim(f,c);
e=fdim(d,b);
}
else{
d=atan2(f,g);
h=sin(d);
f=(pow(b,b))-(fdim(c,e));
}
}