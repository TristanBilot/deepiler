#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(f,g))*(ceil(d));
b=(fmin(c,e))+(pow(a,f));
if(islessequal(b,c)){
d=(floor(f))+(acos(g));
d=(fmin(e,a))*(log(c));
d=(pow(e,c))/(floor(e));
}
else{
e=sin(g);
b=cos(e);
a=(fmax(a,d))+(fmin(a,h));
}
if(islessequal(b,f)){
f=(fmax(b,b))+(fmax(e,g));
f=(exp(e))+(fmin(g,h));
h=(sin(b))-(tan(g));
}
}