#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=asin(c);
d=(atan2(c,d))*(acos(b));
e=acos(e);
e=(atan2(b,b))/(sin(a));
if(isless(c,c)){
e=log(e);
b=cos(b);
a=floor(e);
d=exp(c);
d=log10(c);
}
if(isless(e,b)){
e=(pow(e,e))+(fmin(a,d));
c=log(c);
a=fmin(d,c);
}
else{
d=fmin(a,c);
c=fmin(e,d);
}
}