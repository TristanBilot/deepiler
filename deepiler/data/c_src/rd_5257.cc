#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(b);
d=(pow(f,h))*(fmin(c,c));
f=(sin(h))-(atan(e));
h=(asin(b))+(ceil(g));
if(isless(g,a)){
e=sqrt(h);
b=fmax(b,a);
b=pow(c,e);
}
else{
a=log10(d);
e=fmax(c,g);
b=atan(d);
}
while(islessequal(b,e)){
b=(ceil(e))/(sin(d));
h=(sin(f))/(sqrt(h));
e=pow(a,h);
d=floor(a);
}
}