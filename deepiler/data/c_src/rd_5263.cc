#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmin(d,b))+(fmin(b,d));
e=sin(a);
e=pow(f,a);
a=(atan2(c,b))*(fdim(c,a));
e=floor(a);
while(isless(a,f)){
f=(exp(a))/(cos(e));
b=(fmin(e,c))-(fmax(c,e));
f=exp(a);
b=(fmax(f,d))-(atan(a));
c=sqrt(e);
}
c=(fmax(f,a))/(atan2(b,c));
a=(pow(f,a))-(fdim(e,c));
c=atan2(a,a);
a=(sqrt(d))-(fmin(e,b));
e=pow(e,c);
}