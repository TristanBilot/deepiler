#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmax(b,e))+(acos(c));
b=(pow(g,a))/(cos(b));
d=(cos(f))*(fdim(e,c));
g=(log10(f))*(asin(e));
f=exp(e);
if(islessequal(c,b)){
e=(fmax(g,d))-(sin(d));
a=fdim(c,c);
}
else{
g=(pow(c,f))+(pow(e,f));
f=(fmax(c,c))*(ceil(d));
f=fmin(c,c);
b=(sin(f))+(fmin(a,e));
c=(tan(b))/(fmax(d,f));
}
while(isgreaterequal(b,b)){
d=log(d);
g=tan(c);
d=cos(e);
b=sqrt(c);
a=tan(f);
}
}