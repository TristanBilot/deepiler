#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(b,b);
b=(pow(d,c))-(atan(d));
a=(log10(c))/(fmax(d,a));
a=pow(e,c);
e=(log10(c))-(atan2(d,d));
while(isless(d,b)){
d=pow(d,d);
a=(acos(d))-(fmin(b,b));
}
while(islessequal(a,d)){
d=cos(e);
a=(fmin(b,b))+(pow(b,d));
e=pow(c,a);
a=(floor(a))+(atan(c));
a=fmin(d,e);
}
}