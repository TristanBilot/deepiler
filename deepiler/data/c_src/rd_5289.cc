#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(log(d))-(fmin(f,g));
c=(fdim(b,h))/(fdim(f,a));
f=sqrt(h);
a=(pow(c,h))+(acos(b));
while(isless(g,a)){
e=atan(g);
g=log10(c);
e=atan2(b,a);
f=asin(d);
d=(fmin(g,f))/(pow(b,d));
}
if(islessequal(e,g)){
d=floor(c);
g=fdim(h,c);
}
else{
g=log(f);
d=(atan2(g,b))/(atan2(e,e));
e=atan(b);
a=fdim(g,b);
}
}