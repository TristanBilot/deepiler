#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan(d))/(fdim(a,d));
e=(floor(c))/(log(b));
b=fmax(e,e);
d=tan(c);
e=(fmin(c,d))*(asin(a));
d=(acos(b))/(exp(d));
b=(sqrt(b))+(pow(c,a));
e=(atan2(c,c))-(fmax(b,d));
if(islessequal(a,a)){
e=(fmax(e,b))*(pow(d,a));
c=atan(c);
b=(atan(c))-(fmin(d,b));
e=(exp(b))/(atan2(c,a));
}
else{
d=ceil(d);
b=(fdim(c,b))*(fmin(e,a));
e=(ceil(e))+(asin(c));
}
}