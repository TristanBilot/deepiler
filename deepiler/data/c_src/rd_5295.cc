#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(a);
e=(fmax(b,e))-(fmax(e,c));
b=pow(e,e);
d=fmin(c,c);
while(islessgreater(a,a)){
e=fmin(e,d);
e=pow(b,b);
}
while(islessgreater(e,a)){
c=exp(b);
b=log(c);
d=fmin(c,b);
}
}