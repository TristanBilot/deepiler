#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=exp(c);
g=log10(f);
d=(pow(c,d))*(log(a));
c=(floor(a))-(sqrt(e));
while(islessequal(a,b)){
f=(fmax(d,f))+(sin(b));
c=fmin(g,c);
}
e=(fmin(g,b))/(fdim(e,f));
d=tan(d);
}