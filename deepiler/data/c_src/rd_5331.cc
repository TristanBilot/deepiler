#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(c,d))/(fmax(c,d));
g=fmin(f,d);
a=acos(a);
e=(fmin(g,b))/(pow(f,b));
c=sin(d);
b=log(f);
f=log(a);
b=fdim(c,g);
d=fmax(d,f);
if(isless(b,a)){
e=ceil(f);
f=(fdim(c,a))+(cos(a));
d=(cos(d))-(fmax(d,g));
b=ceil(a);
a=exp(a);
}
else{
f=atan(c);
f=(fmin(a,g))/(atan(f));
c=(tan(b))*(cos(c));
}
}