#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(log(g))-(pow(b,h));
c=log(g);
e=(pow(g,f))*(fmax(f,d));
c=(sqrt(g))+(asin(a));
b=(fdim(f,e))+(sqrt(a));
a=fmin(b,e);
a=(fmax(h,c))/(atan2(c,f));
d=(fmin(h,a))/(ceil(b));
g=fmax(a,h);
f=(atan2(c,d))/(fmax(f,c));
c=sin(d);
b=(pow(d,a))-(fdim(f,h));
b=fdim(a,g);
}