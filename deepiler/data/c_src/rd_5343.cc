#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(b,c);
b=acos(a);
d=(atan2(e,a))+(fmax(c,e));
while(isless(d,e)){
d=(pow(a,b))-(ceil(d));
b=(tan(c))/(acos(a));
c=(atan2(a,d))+(fdim(d,c));
a=(exp(c))+(fmin(d,a));
}
if(islessgreater(c,a)){
d=atan2(d,d);
b=atan(c);
a=pow(a,b);
e=(fdim(e,a))+(ceil(a));
}
}