#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(b);
a=(tan(c))-(log(c));
d=fdim(d,d);
d=asin(e);
d=fmax(a,b);
c=fdim(e,d);
e=(sqrt(a))*(fmax(e,d));
a=(exp(a))*(sqrt(d));
a=acos(c);
while(islessequal(d,b)){
d=(pow(e,b))-(sin(b));
c=(pow(a,d))*(tan(a));
}
}