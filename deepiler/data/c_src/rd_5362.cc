#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(b);
c=log10(a);
d=tan(a);
while(isless(c,e)){
c=atan2(e,d);
e=(pow(e,c))*(fmin(b,e));
d=tan(e);
}
if(islessequal(d,b)){
d=(pow(e,b))-(atan(e));
b=(fmin(b,d))+(fdim(d,d));
e=(sin(b))/(ceil(b));
b=asin(d);
}
}