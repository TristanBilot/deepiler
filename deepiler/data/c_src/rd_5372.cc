#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(acos(c))+(ceil(c));
e=fmin(e,b);
b=ceil(a);
a=(pow(a,e))+(tan(c));
c=(fmin(d,c))/(sin(a));
e=fmax(a,c);
while(islessequal(a,b)){
d=(tan(b))-(acos(e));
e=(fmax(c,b))/(cos(d));
}
}