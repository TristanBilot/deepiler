#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=acos(c);
c=log(b);
f=(fmin(b,h))+(floor(a));
b=(fmax(f,g))+(asin(d));
e=fdim(a,e);
while(islessgreater(b,f)){
e=sin(g);
f=(fdim(b,g))/(exp(b));
a=(fmin(b,h))*(atan2(f,h));
f=log(e);
d=(cos(d))+(fmin(e,c));
}
if(islessequal(e,e)){
f=exp(d);
c=log10(f);
}
}