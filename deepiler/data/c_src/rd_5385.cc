#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=sin(c);
b=fdim(b,a);
d=cos(e);
while(isless(b,b)){
d=(log10(a))+(floor(e));
b=fmax(c,c);
}
while(islessgreater(e,b)){
c=pow(c,d);
d=fdim(b,a);
d=(atan2(b,d))+(fmax(e,a));
a=(log(c))+(asin(d));
}
}