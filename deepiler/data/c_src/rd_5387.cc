#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=tan(f);
d=(fmin(f,b))-(fmin(c,a));
a=(asin(a))/(cos(e));
d=(asin(a))-(fmax(c,e));
while(islessgreater(d,b)){
a=(tan(b))*(atan2(e,c));
e=(ceil(b))+(cos(f));
e=fmax(f,d);
f=pow(e,e);
}
while(islessequal(c,e)){
a=floor(b);
e=pow(c,e);
}
}