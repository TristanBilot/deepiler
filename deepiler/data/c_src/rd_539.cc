#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(acos(e))-(fmax(f,h));
c=fdim(f,c);
g=(pow(d,d))-(pow(h,g));
f=(atan(e))-(fmin(h,f));
d=(ceil(f))+(cos(b));
f=(sin(a))-(fmax(b,e));
c=(tan(d))/(atan(b));
c=(fdim(g,b))+(atan2(g,e));
e=fdim(b,c);
while(isgreaterequal(h,e)){
h=fmin(h,f);
e=(atan2(c,f))+(atan2(f,e));
f=fmax(d,d);
}
}