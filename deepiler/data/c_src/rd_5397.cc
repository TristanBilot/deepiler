#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(e,g);
f=(fmax(c,d))/(ceil(a));
a=fmin(f,c);
d=exp(c);
b=tan(c);
while(isgreaterequal(d,a)){
b=(exp(b))/(sqrt(a));
c=log(b);
h=atan2(b,e);
g=(floor(d))*(sin(d));
}
while(isgreaterequal(f,b)){
e=asin(e);
e=(fdim(h,e))/(atan(b));
g=(cos(c))*(atan(c));
d=(pow(d,c))*(atan(c));
}
}