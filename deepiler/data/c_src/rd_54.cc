#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log10(a);
b=(pow(e,c))/(sqrt(e));
a=(pow(b,e))+(fdim(d,b));
e=(fmax(c,d))+(sqrt(d));
d=cos(b);
if(isless(d,e)){
c=ceil(b);
e=pow(c,c);
}
c=(sin(e))*(pow(b,b));
c=fmin(a,a);
c=pow(b,c);
d=(atan2(c,d))+(fdim(c,b));
}