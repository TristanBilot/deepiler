#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(b))-(atan2(e,b));
a=(atan2(d,d))*(asin(c));
b=(asin(a))*(fmin(e,d));
b=(fmax(a,a))/(exp(d));
d=fmax(d,e);
e=fdim(c,c);
b=(acos(a))/(acos(b));
c=exp(c);
a=(fmax(e,d))+(log(b));
}