#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log(d))*(acos(c));
c=(sin(a))-(fmin(c,c));
c=fdim(e,a);
d=(fmax(e,e))*(fdim(e,a));
e=fdim(e,e);
e=(ceil(d))+(pow(e,a));
c=(atan2(d,a))-(sqrt(d));
a=(fmax(c,c))*(fmin(a,c));
a=atan2(c,e);
a=atan(a);
}