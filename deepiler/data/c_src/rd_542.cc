#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=pow(a,c);
b=(exp(f))+(sqrt(b));
if(islessequal(b,f)){
b=tan(f);
g=(fmax(f,f))-(pow(b,c));
}
else{
b=(atan2(e,e))-(ceil(c));
b=fmin(a,e);
f=(fmin(e,g))+(fmin(a,b));
f=(pow(e,d))*(floor(f));
b=(atan2(b,b))+(tan(e));
}
while(islessequal(e,g)){
e=(tan(e))/(fmax(e,g));
g=(atan2(f,c))-(floor(g));
b=log10(c);
b=(exp(e))+(log(b));
e=log(a);
}
}