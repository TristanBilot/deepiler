#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(h);
a=fmax(f,f);
g=ceil(e);
if(islessequal(c,f)){
d=(fmax(e,g))-(fdim(d,g));
b=(atan(a))-(ceil(c));
d=(log(d))/(atan2(c,a));
c=tan(e);
b=fmax(a,h);
}
else{
h=fmax(g,h);
h=fdim(b,h);
e=pow(f,c);
e=fmin(c,b);
}
if(isgreaterequal(b,f)){
a=(exp(d))-(floor(c));
g=fmin(f,a);
h=atan2(h,c);
}
}