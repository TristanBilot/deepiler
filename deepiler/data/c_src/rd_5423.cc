#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(c,e);
c=tan(d);
d=fmin(b,b);
d=(sin(b))+(fmax(a,b));
b=(fmax(c,a))-(exp(d));
if(isless(c,d)){
d=(acos(a))/(acos(e));
a=sqrt(e);
e=asin(d);
}
else{
d=fmax(a,e);
c=fmin(d,b);
d=fmax(a,a);
}
c=(sqrt(d))+(acos(e));
b=(fmin(e,d))*(fmin(d,e));
c=tan(a);
c=(fmin(a,b))*(sqrt(c));
}