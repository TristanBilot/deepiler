#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=tan(b);
d=(ceil(a))-(fdim(a,b));
while(islessgreater(b,d)){
b=(ceil(f))*(fmin(b,c));
a=(fdim(e,f))*(fmin(d,f));
a=(exp(d))+(exp(e));
b=fmin(d,d);
f=log(f);
}
if(islessgreater(b,a)){
f=log(b);
e=exp(d);
c=cos(c);
}
else{
a=(atan2(a,f))/(sin(a));
c=atan2(f,e);
e=(floor(a))-(acos(c));
}
}