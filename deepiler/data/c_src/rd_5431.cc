#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sqrt(e))*(fmax(f,e));
b=ceil(a);
h=(fdim(d,h))*(floor(g));
h=(fdim(d,e))+(log10(g));
c=floor(e);
if(isless(h,c)){
b=(log(h))-(fmax(d,f));
b=fmin(d,f);
f=fmax(g,d);
a=log10(c);
f=fmin(a,g);
}
c=fmin(g,g);
g=(fmax(b,f))*(cos(g));
d=fmax(f,g);
}