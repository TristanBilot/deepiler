#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=tan(d);
e=(atan2(f,e))*(fdim(f,d));
f=(fdim(d,a))/(cos(d));
d=atan(d);
f=(sqrt(c))/(log(f));
c=(fmin(f,b))*(atan2(d,c));
d=fmax(a,c);
a=(atan2(a,e))*(cos(d));
c=(atan(e))+(atan2(b,e));
while(islessgreater(a,c)){
e=(cos(f))/(sin(c));
b=atan2(b,e);
a=acos(d);
f=sqrt(b);
}
}