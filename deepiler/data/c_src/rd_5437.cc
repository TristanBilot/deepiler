#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(b,g);
e=(pow(d,e))*(asin(a));
e=fdim(e,f);
if(isless(c,f)){
f=(tan(g))/(fmin(e,b));
h=fdim(g,h);
c=fmax(b,a);
}
if(islessequal(g,c)){
h=pow(c,a);
h=ceil(g);
e=fmin(h,g);
h=(sin(a))/(atan2(d,d));
}
else{
b=(atan2(b,d))-(fdim(h,a));
b=atan(a);
d=fdim(b,c);
}
}