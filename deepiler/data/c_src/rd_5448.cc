#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=floor(f);
e=(floor(g))-(atan(b));
g=log10(f);
f=cos(c);
d=(fmin(e,f))/(fmax(d,g));
while(isless(d,c)){
a=fmin(a,d);
g=fmax(d,b);
d=log(b);
a=(ceil(c))-(atan2(d,d));
}
g=(pow(e,b))/(floor(g));
e=(pow(g,c))-(fmin(e,c));
c=(fmax(e,b))+(floor(d));
e=(atan2(e,b))*(fmin(b,f));
f=pow(f,e);
}