#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(c,d))/(cos(e));
b=(atan2(d,c))-(tan(d));
a=fmin(f,d);
c=cos(e);
e=(log10(d))+(sin(d));
while(islessgreater(e,b)){
c=(pow(c,e))+(atan2(e,c));
c=(fdim(a,d))*(atan2(a,a));
e=(asin(c))-(log(e));
e=atan2(d,c);
f=(atan2(c,b))*(pow(b,c));
}
f=(fmin(b,f))/(floor(b));
c=(fmax(d,c))/(ceil(a));
a=(fdim(c,d))-(cos(f));
d=(acos(a))/(floor(b));
}