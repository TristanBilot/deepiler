#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=tan(e);
d=(fdim(c,a))-(pow(f,f));
while(islessequal(b,f)){
b=(sin(f))+(atan(e));
a=(fdim(d,c))*(tan(d));
}
if(islessgreater(d,c)){
a=fdim(d,a);
a=(pow(d,e))*(fmax(b,b));
b=fmax(c,d);
}
else{
e=(fmin(b,f))+(sqrt(b));
e=(tan(b))-(log(e));
}
}