#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan(d))+(tan(b));
b=(exp(d))/(fmin(b,f));
c=(atan2(d,f))+(tan(e));
f=floor(c);
d=(acos(d))*(atan2(d,e));
a=(acos(c))-(sqrt(f));
while(islessgreater(f,c)){
c=atan(a);
d=atan2(b,c);
f=pow(b,d);
d=sqrt(a);
e=(pow(a,e))-(fmax(b,c));
}
}