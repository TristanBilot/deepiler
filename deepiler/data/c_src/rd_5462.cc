#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(d,a))+(pow(f,d));
a=atan2(d,e);
while(isless(f,g)){
a=(pow(e,h))+(fmax(e,c));
g=floor(g);
}
while(isless(g,d)){
f=log(c);
h=exp(b);
a=(fmax(e,a))*(sin(a));
c=(pow(d,c))-(atan(c));
g=(atan(f))/(exp(c));
}
}