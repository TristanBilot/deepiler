#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(d);
f=(fmax(e,d))+(log(c));
while(islessequal(a,f)){
f=(atan2(d,a))*(atan2(b,c));
c=atan2(d,e);
c=(fmin(a,d))/(exp(c));
b=(log(c))*(pow(b,e));
d=(fmin(d,d))*(tan(c));
}
if(isgreaterequal(f,e)){
f=(floor(c))+(pow(a,a));
c=(exp(b))/(atan(c));
}
else{
a=(fmin(c,f))*(sqrt(c));
a=(tan(a))-(fmax(a,a));
}
}