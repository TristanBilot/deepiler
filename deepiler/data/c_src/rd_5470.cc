#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(cos(d))/(sin(a));
e=(fmin(a,e))-(floor(e));
f=(fdim(e,d))*(fdim(d,e));
d=(pow(c,a))-(sin(e));
c=fmax(b,d);
while(islessgreater(c,c)){
e=(atan2(d,f))-(fmin(a,f));
b=(exp(a))+(pow(f,e));
a=fmax(e,e);
}
if(islessequal(a,f)){
e=(atan(c))/(exp(a));
d=atan(c);
}
else{
a=(atan(d))+(atan(f));
f=(acos(d))/(sin(c));
e=(pow(e,c))*(sqrt(b));
}
}