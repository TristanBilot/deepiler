#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sqrt(c))/(pow(c,d));
d=(pow(c,b))+(atan2(c,b));
c=atan2(d,b);
e=fmax(e,d);
a=(fmax(e,c))/(fmin(c,b));
b=(floor(e))-(log(e));
e=(fmin(b,a))*(log(e));
d=(cos(a))/(atan(d));
b=(atan2(c,b))*(sqrt(a));
b=(fdim(b,d))/(atan2(d,b));
d=fdim(b,e);
c=(floor(a))/(fdim(a,c));
c=sqrt(d);
}