#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(log(e))*(fmin(g,d));
g=floor(f);
g=tan(c);
g=(fmax(a,c))*(atan2(c,g));
c=(atan2(d,a))+(pow(c,g));
if(isless(e,e)){
g=atan(g);
e=fdim(a,c);
d=fdim(c,a);
a=(asin(a))/(fdim(c,f));
}
else{
e=fmax(c,e);
b=(fmin(c,a))+(sqrt(b));
}
if(isless(e,b)){
f=(fmax(d,e))*(fmax(d,c));
f=fmin(d,d);
b=(sin(c))-(atan(g));
}
else{
d=fmin(d,f);
a=atan(d);
e=log10(a);
f=fdim(b,d);
}
}