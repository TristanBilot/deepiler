#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(a,c);
d=fmax(c,b);
a=(fmax(e,e))*(sin(d));
d=(fdim(b,b))+(acos(a));
while(islessequal(a,d)){
b=pow(a,b);
a=acos(d);
a=sin(e);
e=fdim(d,d);
}
c=sin(e);
b=(cos(c))+(atan2(d,c));
b=(log10(e))*(atan2(a,a));
c=log10(b);
e=(fmin(b,b))+(log(b));
}