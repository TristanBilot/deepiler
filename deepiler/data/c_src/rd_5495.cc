#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(c,c))/(asin(a));
e=fmin(e,a);
b=(fmax(a,e))+(fmin(e,d));
e=fmin(d,d);
if(islessequal(d,c)){
b=(fdim(c,e))*(log10(c));
e=fdim(c,c);
b=fmin(e,a);
a=(fmin(a,e))/(ceil(d));
d=(atan2(a,c))*(log(b));
}
if(isgreaterequal(e,a)){
a=(atan(c))+(atan(d));
a=sin(a);
b=(tan(b))*(pow(d,d));
b=exp(a);
}
else{
a=(ceil(c))-(floor(e));
c=(ceil(d))*(atan(c));
c=sqrt(c);
c=(floor(b))/(fmin(b,c));
}
}