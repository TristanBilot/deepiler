#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(tan(c))-(fmax(b,a));
b=sqrt(d);
a=fdim(d,f);
d=(pow(d,a))-(pow(a,b));
if(islessgreater(d,b)){
a=(atan2(e,f))/(atan(b));
f=pow(d,e);
}
else{
e=(fmax(f,f))-(floor(f));
c=floor(a);
b=(fdim(d,a))*(pow(b,a));
}
if(isgreaterequal(b,e)){
f=sin(d);
d=atan2(d,b);
e=(fdim(b,e))/(atan2(e,d));
c=ceil(b);
a=(sqrt(e))/(fmin(f,f));
}
else{
f=atan2(c,d);
c=exp(a);
d=atan2(a,a);
e=(fmax(f,b))-(sqrt(c));
a=fmin(b,c);
}
}