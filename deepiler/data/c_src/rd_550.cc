#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=asin(a);
d=atan(e);
c=(fmin(f,e))/(sqrt(d));
c=log(e);
if(isless(c,c)){
a=(fmax(c,b))-(sqrt(a));
a=exp(d);
d=atan(f);
}
while(isgreaterequal(b,a)){
e=fmax(f,f);
d=(fmax(b,b))*(acos(b));
c=fdim(d,e);
a=fdim(f,d);
}
}