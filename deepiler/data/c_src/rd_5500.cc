#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(f,b);
f=asin(e);
if(islessequal(f,b)){
d=pow(e,b);
b=(ceil(e))/(exp(b));
e=fmax(b,e);
a=fmin(d,e);
e=fmax(a,d);
}
b=fmax(a,b);
d=floor(b);
b=(fmax(d,d))-(log(c));
b=sin(b);
a=(tan(b))*(pow(a,f));
}