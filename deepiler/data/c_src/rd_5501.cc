#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(d,c);
c=atan(b);
e=fdim(c,c);
b=fdim(c,b);
while(isless(f,e)){
b=(atan2(b,d))-(fdim(d,b));
e=fdim(c,a);
}
if(islessgreater(d,a)){
a=(log10(e))-(log10(f));
d=exp(c);
c=fdim(d,c);
}
else{
b=(fdim(a,f))/(fmin(e,f));
f=(sin(e))-(sqrt(c));
f=exp(a);
}
}