#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(h,h);
d=fdim(h,b);
d=(sqrt(a))*(sqrt(f));
c=(exp(g))-(fmin(a,e));
c=(fmin(e,h))-(asin(h));
f=(fmax(b,f))*(pow(h,c));
g=pow(e,d);
h=(atan2(d,c))+(log10(a));
c=tan(e);
b=(pow(e,b))-(ceil(a));
e=sin(a);
}