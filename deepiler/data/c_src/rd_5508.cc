#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan(f);
f=fmin(a,d);
e=sin(g);
f=asin(d);
c=cos(c);
c=cos(f);
e=(atan2(b,e))/(fmax(g,f));
while(isless(d,b)){
e=acos(d);
g=(fdim(a,f))-(fdim(e,a));
}
}