#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(a,g);
c=sqrt(c);
e=pow(b,d);
d=exp(g);
e=atan2(f,d);
if(islessgreater(b,c)){
f=acos(g);
a=sqrt(d);
e=(atan(c))/(pow(b,a));
f=(ceil(c))*(cos(a));
d=atan(d);
}
else{
e=cos(d);
c=sin(d);
c=(log(a))*(atan(c));
b=tan(a);
}
e=(atan2(g,g))+(cos(e));
g=(log10(b))*(asin(d));
g=fmax(e,a);
}