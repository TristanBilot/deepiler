#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(e,b);
c=sin(b);
a=pow(e,b);
d=(acos(d))*(fmax(c,e));
b=(fdim(e,e))/(fdim(c,c));
c=(tan(b))/(fmin(e,b));
a=(pow(c,a))-(fmax(c,b));
if(isgreaterequal(b,c)){
e=sin(e);
d=(tan(e))-(sin(a));
d=fdim(b,b);
c=(log10(a))+(fmax(b,a));
}
}