#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(pow(b,b))*(fmax(e,a));
g=(fmax(e,c))+(fmax(f,a));
while(islessequal(g,d)){
e=(atan(d))*(cos(b));
b=pow(g,b);
f=atan(a);
e=cos(f);
}
c=ceil(b);
b=(fmax(g,d))+(cos(b));
}