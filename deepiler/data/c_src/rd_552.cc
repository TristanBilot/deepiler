#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(a,b))+(atan2(d,h));
e=(sqrt(b))+(floor(b));
d=pow(g,b);
g=fmin(d,e);
d=(fdim(h,e))-(log(a));
a=fdim(a,g);
b=(fmin(c,c))*(exp(b));
g=(atan2(h,f))/(fdim(f,c));
a=atan2(f,b);
b=(fmax(h,b))-(log10(d));
}