#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fdim(c,b);
f=(asin(e))/(log(g));
a=(fmin(e,c))-(pow(b,a));
e=fdim(f,d);
c=log10(a);
a=(atan2(c,g))-(log10(e));
while(isgreaterequal(b,e)){
c=(fmin(a,f))+(atan2(g,a));
c=atan(g);
d=(fmin(a,d))/(floor(b));
f=(log10(b))*(fdim(c,d));
}
}