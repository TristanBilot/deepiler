#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(d,f);
a=(fdim(e,f))*(fmax(f,g));
while(isgreaterequal(e,a)){
g=(sin(e))-(floor(c));
a=pow(g,a);
b=(fmin(f,d))*(fmax(g,c));
}
if(isgreaterequal(a,f)){
d=pow(e,e);
e=sqrt(b);
c=log10(d);
a=acos(f);
a=(fdim(d,b))-(fmax(b,f));
}
}