#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(a,c))/(atan(c));
d=tan(e);
c=(sin(d))+(atan2(a,d));
b=(sin(e))/(fmax(a,c));
while(islessequal(c,d)){
c=atan(d);
a=(fdim(e,e))*(asin(b));
e=fmin(c,e);
b=(atan(e))*(atan2(a,c));
a=atan(d);
}
if(isgreaterequal(a,a)){
c=(fmin(e,b))/(fmax(d,d));
b=ceil(d);
b=(sqrt(c))+(ceil(a));
}
}