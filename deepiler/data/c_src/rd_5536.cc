#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(c,d);
c=ceil(b);
b=fdim(d,c);
while(islessequal(e,d)){
a=(floor(d))/(tan(b));
e=(ceil(d))*(fdim(e,a));
}
c=(fdim(e,c))*(floor(c));
d=(fmax(c,c))-(atan2(e,d));
}