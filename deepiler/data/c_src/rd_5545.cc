#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fmax(a,c))+(cos(c));
d=(atan(b))-(tan(e));
f=sin(e);
c=pow(c,d);
b=fdim(f,h);
e=atan2(e,c);
h=(exp(h))/(fdim(e,c));
e=atan2(d,f);
c=acos(c);
c=acos(b);
d=(exp(a))*(fmax(a,g));
h=atan2(f,h);
g=(asin(c))/(fmax(g,d));
}