#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmin(f,c);
e=(fdim(c,b))-(fmax(b,c));
f=pow(c,e);
while(islessequal(a,b)){
a=(fdim(d,e))/(fdim(d,e));
a=(pow(c,f))*(sin(d));
b=(floor(a))*(fdim(d,b));
}
f=(acos(f))-(tan(f));
d=(pow(d,b))*(fdim(e,a));
c=log(a);
c=sqrt(f);
e=(pow(a,d))-(exp(a));
}