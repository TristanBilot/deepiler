#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(f);
g=fdim(h,f);
b=(sin(a))*(ceil(e));
e=(exp(c))-(pow(d,f));
g=(exp(e))-(cos(f));
a=fmin(b,c);
h=asin(c);
d=(pow(b,e))+(fmin(f,a));
if(islessgreater(a,c)){
f=(atan2(d,e))-(sin(h));
c=fmax(d,a);
e=fmax(a,c);
e=(fdim(e,f))*(ceil(g));
}
}