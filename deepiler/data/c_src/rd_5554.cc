#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=cos(a);
e=(fdim(g,b))/(atan2(c,f));
b=ceil(d);
while(isgreaterequal(e,e)){
d=(fmin(g,e))-(atan(b));
e=acos(e);
}
if(islessequal(d,a)){
c=fmin(a,g);
d=fdim(c,c);
}
else{
d=pow(e,g);
f=asin(f);
b=(sqrt(e))/(fmax(g,f));
c=pow(e,e);
}
}