#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(e,b);
a=atan2(b,e);
while(islessgreater(c,d)){
a=fmax(d,e);
c=tan(a);
}
if(isless(e,a)){
c=fmax(e,d);
d=(pow(e,b))/(sin(c));
}
}