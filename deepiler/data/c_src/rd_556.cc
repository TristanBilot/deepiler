#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(b);
c=ceil(a);
if(isgreaterequal(c,b)){
e=(exp(b))+(fmin(e,d));
e=fdim(e,e);
c=atan2(d,c);
b=fdim(b,b);
a=atan(e);
}
else{
c=(pow(c,a))-(fdim(e,b));
d=(floor(c))+(fdim(c,a));
c=(sqrt(c))-(pow(d,d));
}
while(isgreaterequal(b,c)){
b=(fdim(e,b))*(floor(a));
c=(acos(b))-(fmax(c,e));
d=(floor(e))-(atan(b));
a=sin(b);
}
}