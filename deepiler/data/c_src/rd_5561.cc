#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(h,a);
g=(sin(c))-(sqrt(e));
f=(pow(d,c))*(atan2(b,d));
f=(fmax(e,b))*(tan(e));
while(isgreaterequal(b,e)){
a=fdim(f,c);
h=fmax(h,g);
b=(ceil(b))-(fdim(a,g));
}
if(islessequal(e,d)){
b=fdim(f,f);
h=(asin(f))-(fmax(g,d));
}
else{
d=log(d);
f=(atan2(f,a))/(fmax(g,d));
e=atan2(h,g);
}
}