#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=sqrt(c);
a=log(g);
a=(log(d))/(log(c));
e=fmax(a,b);
e=(fmax(g,f))/(log10(c));
d=(fmin(a,a))/(atan2(d,g));
if(isgreaterequal(g,e)){
b=sqrt(d);
e=(floor(d))-(fmax(c,a));
f=sqrt(c);
d=floor(b);
}
}