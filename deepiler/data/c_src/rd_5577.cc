#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(h);
d=fmin(f,d);
while(isgreaterequal(g,g)){
e=(fmax(a,b))+(pow(a,c));
f=atan2(c,g);
g=fmax(c,a);
}
while(islessequal(b,c)){
a=atan(a);
d=(log10(c))-(fmin(a,h));
c=fmax(e,c);
c=tan(c);
}
}