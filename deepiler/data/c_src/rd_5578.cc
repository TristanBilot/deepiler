#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=acos(c);
e=(floor(c))+(fmax(d,e));
b=(fdim(a,d))+(log(a));
c=(tan(e))-(sqrt(d));
if(islessgreater(a,b)){
b=ceil(a);
a=(fdim(c,c))+(fmin(d,e));
}
while(isless(e,a)){
b=fmax(e,d);
a=(atan2(a,c))-(sin(a));
c=(fmax(b,b))/(fmin(a,c));
a=(acos(e))+(acos(c));
}
}