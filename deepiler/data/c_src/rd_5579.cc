#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(e,b);
f=fdim(a,e);
if(isgreaterequal(f,a)){
d=(asin(a))+(fdim(f,e));
d=(fmax(e,a))-(fdim(c,e));
e=sqrt(c);
}
else{
c=cos(d);
d=tan(c);
c=fdim(b,d);
c=(log(d))+(fdim(e,a));
f=ceil(b);
}
if(islessgreater(f,b)){
e=atan(e);
d=(fdim(f,d))-(pow(c,d));
b=log10(c);
e=(pow(f,f))*(log(e));
b=(sin(d))*(fmax(d,f));
}
}