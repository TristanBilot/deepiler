#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=pow(e,g);
d=(cos(b))*(acos(b));
e=pow(e,f);
a=(atan(g))/(ceil(b));
if(isgreaterequal(g,g)){
b=(fmax(g,f))+(fdim(e,f));
f=(fdim(f,g))-(atan2(a,d));
d=atan2(b,c);
c=acos(f);
e=acos(a);
}
else{
d=exp(a);
g=tan(c);
f=(fdim(f,b))*(atan2(g,f));
e=atan2(c,g);
}
e=fmax(f,a);
b=floor(e);
g=(pow(g,c))-(floor(a));
d=(sin(b))+(floor(f));
g=fmax(c,g);
}