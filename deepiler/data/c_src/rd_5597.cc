#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(c,a))+(floor(b));
a=(log(c))+(fdim(a,a));
e=(atan2(d,b))/(exp(b));
b=(fmax(e,b))+(pow(d,e));
while(islessgreater(c,d)){
b=atan2(d,d);
e=(fmin(b,c))*(fdim(c,b));
d=(asin(a))-(fmax(b,e));
e=(sqrt(e))+(fdim(b,b));
b=atan2(c,b);
}
}