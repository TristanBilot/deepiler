#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(d,c);
c=fmax(d,e);
d=log(a);
d=ceil(e);
a=(pow(b,b))*(log(b));
while(isless(b,a)){
c=pow(b,b);
d=fdim(d,a);
e=(cos(e))*(exp(e));
b=(fmin(e,a))/(sin(e));
a=(exp(e))+(ceil(a));
}
while(isgreaterequal(d,a)){
b=(atan2(e,c))+(fmin(b,e));
c=log10(c);
}
}