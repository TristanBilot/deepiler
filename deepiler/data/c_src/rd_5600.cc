#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(atan2(a,e))+(log(f));
e=log10(b);
d=(cos(b))*(fdim(c,d));
b=(fdim(d,a))+(acos(f));
f=fdim(d,a);
if(isless(e,a)){
b=(sin(b))-(log(e));
c=atan2(d,f);
e=(fmax(d,a))/(log(c));
}
if(isless(f,c)){
f=(fmax(f,f))+(cos(b));
b=log10(e);
}
else{
e=acos(d);
e=(fmin(b,e))-(fmax(d,a));
}
}