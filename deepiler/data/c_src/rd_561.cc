#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=sqrt(f);
d=pow(d,e);
a=(fdim(a,c))-(tan(c));
f=(ceil(c))*(tan(e));
a=(fdim(e,b))/(fmax(e,f));
c=(cos(a))*(sin(c));
if(islessgreater(c,f)){
d=fdim(d,d);
d=fmin(d,a);
}
else{
a=sqrt(f);
e=(sin(f))*(atan(a));
b=fmax(d,d);
b=(fdim(a,b))+(pow(a,e));
b=pow(f,b);
}
}