#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(tan(e))-(cos(e));
b=(log10(h))+(cos(f));
a=(pow(g,c))-(fmax(b,d));
d=fdim(h,c);
e=sin(d);
f=(sin(f))/(atan2(h,e));
while(islessgreater(b,g)){
h=(log(f))-(fmax(a,b));
c=(pow(a,h))+(exp(c));
f=(log10(h))-(cos(g));
}
}