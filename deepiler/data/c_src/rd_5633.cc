#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(d);
e=(exp(c))*(fmax(a,e));
c=(fmax(b,c))*(pow(a,b));
c=(log(c))*(fmin(e,e));
c=(fmax(b,a))+(asin(d));
d=(atan2(b,b))/(log10(c));
c=(fdim(b,c))-(log(b));
e=fdim(b,d);
c=(fmax(c,c))/(atan(e));
c=(ceil(b))*(fdim(c,d));
d=atan2(a,a);
}