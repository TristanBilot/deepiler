#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(f);
a=tan(b);
d=fmax(e,a);
a=log10(a);
g=(atan2(a,d))-(floor(g));
f=(log10(e))+(fmin(f,e));
e=fmax(a,h);
g=fdim(e,b);
h=fmax(f,d);
}