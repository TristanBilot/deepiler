#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(pow(c,b))/(acos(d));
e=(log10(b))/(fdim(a,a));
while(isgreaterequal(b,c)){
d=(tan(b))/(tan(d));
f=(fmax(d,e))/(asin(c));
}
if(isgreaterequal(e,d)){
e=pow(a,c);
b=(atan2(c,c))-(fmax(d,c));
b=log10(d);
a=fmax(c,a);
}
else{
c=fdim(d,b);
b=(acos(d))+(sqrt(f));
d=atan2(b,b);
e=fmin(d,f);
}
}