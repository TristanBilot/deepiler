#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(c);
c=fmax(b,f);
f=(fmin(c,d))/(pow(e,b));
e=(fmin(f,f))/(atan2(c,e));
c=acos(c);
if(isless(h,h)){
c=(fmin(c,c))-(fmin(c,a));
b=(log10(g))*(atan2(d,a));
e=fmin(c,c);
}
else{
g=fmin(b,b);
c=(pow(d,f))-(pow(e,a));
}
if(isgreaterequal(e,d)){
a=(cos(h))*(fmax(d,f));
h=fmax(h,a);
}
}