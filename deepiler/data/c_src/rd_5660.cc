#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(d,d);
f=(fdim(b,b))+(fdim(h,c));
b=fmin(b,c);
c=(log10(f))*(fdim(d,e));
g=atan2(b,g);
d=(log(a))+(fmin(c,h));
g=fdim(e,c);
g=(fmin(g,f))*(floor(b));
a=(fdim(g,c))+(sin(h));
g=pow(a,h);
a=fmin(g,g);
}