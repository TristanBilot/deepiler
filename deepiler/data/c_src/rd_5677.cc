#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(c,c);
c=(fmin(e,b))*(acos(a));
d=atan(d);
d=exp(a);
c=fmin(b,e);
c=pow(e,d);
if(islessequal(b,c)){
c=(asin(a))/(fmax(c,d));
a=(cos(c))/(fmin(a,c));
}
else{
d=fmax(b,b);
a=(fmax(b,e))/(log(c));
}
}