#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(b,b);
a=(fmax(e,b))/(sqrt(c));
a=atan2(b,a);
a=(pow(d,e))+(atan2(b,e));
e=(atan2(d,a))*(sin(a));
a=(fmax(e,b))+(fdim(a,d));
a=atan(b);
e=log(c);
if(islessequal(d,b)){
b=(atan2(d,c))/(acos(d));
e=atan2(a,d);
}
else{
e=(sqrt(e))*(fmin(c,b));
d=fdim(b,c);
}
}