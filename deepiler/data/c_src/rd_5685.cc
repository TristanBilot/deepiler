#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(a,a);
e=fmin(a,e);
d=floor(d);
c=(cos(a))/(log(d));
c=(fmax(a,c))-(log10(a));
if(islessgreater(b,a)){
e=(cos(c))/(asin(b));
c=(acos(b))*(fmin(e,a));
}
c=exp(a);
c=fmin(e,e);
c=atan(e);
d=(acos(a))+(exp(c));
}