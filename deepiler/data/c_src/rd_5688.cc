#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(g,b))-(log10(g));
c=floor(b);
e=(floor(e))*(atan2(d,e));
a=(tan(e))/(pow(b,a));
while(islessequal(a,e)){
b=pow(g,a);
g=(sin(d))/(cos(c));
h=(pow(f,b))/(log(d));
e=cos(d);
}
if(islessequal(h,g)){
f=fdim(d,g);
a=(ceil(d))*(ceil(c));
d=(acos(h))/(sin(h));
a=asin(g);
}
else{
b=floor(c);
f=(asin(c))/(log(e));
b=(fmax(a,a))-(atan2(g,c));
g=(fmax(d,e))/(exp(a));
}
}