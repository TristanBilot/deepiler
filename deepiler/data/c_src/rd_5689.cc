#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(pow(e,d))+(fdim(f,e));
f=pow(b,c);
a=(fmax(c,b))*(sin(e));
e=(fmax(g,a))/(fmax(a,f));
c=(fmin(a,g))/(fmax(g,c));
c=(cos(f))-(log(d));
g=(atan2(e,h))*(fmax(h,h));
f=(pow(d,g))/(fmax(a,e));
b=(fdim(f,a))+(asin(b));
a=(acos(g))-(atan2(e,h));
a=(floor(h))-(ceil(f));
c=(log10(e))/(sin(a));
g=ceil(g);
e=atan(b);
}