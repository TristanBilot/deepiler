#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(b,c);
e=(acos(d))/(fmax(a,e));
b=(tan(e))/(pow(e,b));
c=fmax(a,c);
c=sqrt(d);
a=fdim(d,a);
d=(fmax(c,c))/(pow(c,a));
c=sqrt(d);
d=(sqrt(b))+(atan2(e,a));
a=(atan2(d,e))+(pow(a,a));
d=(cos(b))-(fmin(e,e));
}