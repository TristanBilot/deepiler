#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(b);
e=(pow(e,b))-(fmin(a,e));
b=(exp(d))+(pow(a,c));
d=log(b);
c=(atan2(a,a))*(fmin(d,b));
if(islessgreater(b,c)){
d=atan(a);
d=(fdim(a,e))*(exp(e));
}
while(isless(b,e)){
e=atan2(c,a);
c=cos(a);
b=(fmax(d,a))+(pow(e,e));
}
}