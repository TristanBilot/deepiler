#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(a,b))/(log10(d));
d=fmin(d,e);
d=(fmax(d,d))/(atan2(a,a));
c=(asin(c))-(fmax(c,e));
b=(fmax(b,d))*(atan2(c,c));
if(isgreaterequal(c,d)){
d=asin(c);
c=(tan(d))-(fmax(e,b));
e=exp(c);
c=sin(a);
a=(asin(d))+(exp(a));
}
d=(fmin(e,c))-(fmax(c,e));
c=(atan(a))/(log10(e));
}