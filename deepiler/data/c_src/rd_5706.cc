#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(b,e);
c=ceil(f);
f=tan(d);
c=(fdim(c,a))-(atan(b));
b=pow(f,c);
c=fmax(b,f);
f=fmax(e,a);
f=pow(f,d);
d=(fmax(f,b))+(pow(b,e));
if(islessequal(f,c)){
a=fmin(b,e);
d=atan2(a,a);
a=fmin(b,e);
f=pow(d,f);
}
else{
b=(sqrt(e))+(fmax(e,f));
c=pow(f,f);
b=tan(b);
}
}