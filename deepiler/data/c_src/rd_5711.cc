#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(c);
g=fdim(g,a);
a=fdim(h,b);
g=(sqrt(b))-(acos(d));
f=(fmin(b,b))-(floor(a));
f=(pow(b,g))+(pow(f,f));
e=(exp(c))*(fdim(d,e));
a=(fmin(g,h))/(atan2(a,a));
f=pow(a,h);
f=(acos(h))/(fmin(a,c));
f=fmax(h,a);
h=(fmax(d,g))+(log10(e));
c=(fdim(a,e))-(fdim(d,d));
}