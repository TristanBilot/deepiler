#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(c,b))/(atan2(a,h));
b=fmin(c,f);
f=(sin(a))+(exp(e));
while(isgreaterequal(f,f)){
a=(floor(a))+(pow(f,d));
f=floor(b);
}
g=atan2(e,c);
c=(fmax(b,c))/(sin(h));
f=(floor(d))*(ceil(c));
b=(pow(d,f))*(ceil(d));
}