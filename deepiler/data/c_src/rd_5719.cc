#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(e,d))/(fdim(a,b));
a=(tan(c))+(fmin(b,e));
c=ceil(b);
while(isgreaterequal(b,a)){
a=pow(d,b);
b=(fdim(c,b))/(fmin(a,a));
e=(fmax(e,c))+(fdim(b,a));
}
b=(log(d))+(fmax(c,c));
c=tan(b);
c=(atan(d))+(sin(c));
b=fdim(a,e);
}