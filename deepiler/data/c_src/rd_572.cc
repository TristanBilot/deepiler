#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=sin(a);
d=(sqrt(c))-(fmax(f,b));
c=(fdim(d,e))+(atan2(a,g));
a=fmax(c,g);
d=(fdim(c,g))+(atan(g));
while(islessgreater(b,g)){
f=(cos(g))*(log(b));
b=fdim(d,a);
}
f=(fmax(d,b))+(sqrt(g));
a=(pow(c,a))-(atan2(e,f));
a=exp(b);
}