#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=asin(g);
a=log10(c);
while(islessequal(g,g)){
a=fmax(f,e);
f=pow(e,f);
d=pow(f,d);
}
if(isless(f,c)){
g=(fmin(b,e))/(fmin(f,e));
g=exp(b);
d=floor(f);
f=pow(e,f);
a=log(f);
}
}