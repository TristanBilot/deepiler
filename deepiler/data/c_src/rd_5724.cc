#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(f,f);
c=floor(a);
c=(atan(f))+(fmax(b,d));
e=(cos(f))*(pow(a,b));
b=(exp(f))-(floor(e));
d=(fdim(d,a))+(sqrt(b));
e=log10(b);
d=(pow(c,c))-(asin(b));
e=(fmax(d,d))/(atan2(a,a));
f=pow(e,a);
if(isless(c,b)){
d=log10(c);
c=(sqrt(b))-(atan2(b,a));
a=(atan2(f,c))+(fdim(b,b));
}
else{
b=atan2(c,e);
f=fmin(e,f);
a=ceil(b);
f=floor(d);
f=fmax(f,e);
}
}