#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(b,g))+(fmax(a,d));
b=(floor(c))/(exp(d));
g=(sin(e))/(cos(h));
h=sin(h);
c=(fmin(d,b))/(fmax(e,e));
while(isless(d,h)){
g=(fmax(d,f))/(log(g));
c=fmin(d,e);
c=ceil(b);
a=fmin(g,g);
e=fmax(e,c);
}
if(islessgreater(e,h)){
g=tan(c);
g=cos(e);
g=(atan2(d,f))+(fmin(f,e));
d=(fdim(c,c))/(tan(h));
a=(pow(h,f))-(ceil(b));
}
else{
f=(sin(e))-(cos(a));
h=(sin(f))*(log10(d));
h=fmin(c,e);
}
}