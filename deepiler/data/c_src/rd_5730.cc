#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(d,a);
a=pow(e,b);
g=exp(h);
b=(atan2(c,b))*(log10(a));
c=fmin(h,h);
g=asin(e);
a=(fdim(b,f))*(cos(c));
h=(fmin(b,b))/(pow(f,e));
while(islessequal(f,h)){
e=(fmax(g,d))+(pow(f,c));
f=ceil(b);
c=floor(a);
g=(log10(g))/(sin(b));
a=acos(e);
}
}