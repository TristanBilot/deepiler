#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan2(d,b);
b=atan2(d,e);
f=(cos(e))*(log(a));
if(islessgreater(e,c)){
b=fmax(a,f);
d=(cos(c))/(acos(b));
e=fmax(d,e);
}
else{
a=fdim(c,b);
c=log(a);
f=(fmax(d,c))+(cos(f));
e=(atan2(b,e))-(ceil(f));
}
while(isless(a,e)){
c=(acos(c))-(log10(f));
c=(fdim(d,b))/(atan2(b,b));
}
}