#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(c);
e=(fmax(d,c))/(fmax(b,c));
while(islessequal(a,e)){
d=(cos(d))*(fmax(d,d));
a=(atan2(c,e))/(cos(d));
d=(pow(c,d))/(fmax(b,e));
e=(fdim(b,d))-(log10(c));
}
d=fmax(b,b);
a=fmin(c,b);
}