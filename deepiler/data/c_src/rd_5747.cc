#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(e,b))+(atan2(c,a));
d=log(a);
g=atan2(g,f);
f=(log10(e))-(log(g));
f=(atan2(c,g))/(cos(a));
if(isless(f,f)){
g=fmin(f,b);
e=fmax(f,b);
d=(log10(a))-(cos(e));
}
while(isgreaterequal(b,g)){
d=fdim(a,d);
b=(fmax(b,f))-(asin(f));
e=sin(e);
}
}