#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(b,b))-(fmin(d,a));
a=(atan2(e,b))+(sqrt(b));
e=atan(e);
e=(atan(d))/(asin(d));
a=(fmin(e,b))/(sin(d));
c=atan2(e,a);
while(isless(a,d)){
a=(sin(a))/(tan(c));
a=floor(e);
a=sin(d);
e=(pow(d,a))-(sqrt(e));
b=(tan(a))-(sqrt(e));
}
}