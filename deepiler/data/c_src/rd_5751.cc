#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log(a);
d=fmin(e,c);
d=sin(c);
if(islessgreater(a,e)){
d=(fdim(d,d))-(ceil(e));
e=(pow(d,b))-(atan(d));
}
while(isgreaterequal(d,e)){
e=(sin(e))*(fmax(e,e));
d=atan(d);
a=(fmin(d,c))+(ceil(a));
}
}