#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(e);
a=fdim(c,b);
f=tan(d);
e=asin(f);
a=(sqrt(d))+(atan2(e,b));
f=log10(d);
c=(fmax(d,g))*(fmax(e,h));
d=(tan(e))/(cos(d));
h=(acos(c))-(fmin(f,c));
g=(pow(a,a))/(sqrt(b));
f=(atan2(f,e))*(log(e));
}