#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(a,d))-(pow(h,c));
b=exp(d);
b=pow(g,d);
e=floor(f);
while(isless(g,h)){
e=fdim(e,b);
b=asin(f);
b=(asin(e))-(fmin(f,a));
}
g=log(g);
d=fdim(d,b);
d=fmax(e,c);
e=log(d);
a=(sqrt(e))+(sin(a));
}