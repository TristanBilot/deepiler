#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(cos(e))+(fdim(a,h));
a=(atan2(c,d))+(fmax(c,g));
d=floor(a);
if(isgreaterequal(c,d)){
b=pow(h,h);
b=atan2(g,h);
}
f=pow(c,h);
e=log(g);
c=asin(g);
h=(pow(a,d))-(exp(e));
b=fmax(e,b);
}