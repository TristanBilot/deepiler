#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(f,g))*(atan2(c,f));
b=(floor(c))-(exp(b));
if(islessequal(f,d)){
e=log10(c);
c=atan2(b,g);
}
else{
g=pow(b,g);
a=fmax(d,g);
g=fdim(d,c);
}
g=(fmax(e,c))+(tan(c));
b=(acos(d))*(fmax(b,c));
}