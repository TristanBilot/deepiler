#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=asin(a);
c=(log(b))*(atan2(c,a));
b=cos(e);
e=pow(b,d);
while(isless(d,e)){
c=(fmax(c,d))*(asin(d));
d=pow(a,a);
}
while(isless(e,a)){
b=sqrt(e);
a=atan2(e,a);
b=(pow(d,a))-(tan(c));
}
}