#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=ceil(d);
c=pow(d,d);
c=(atan(a))+(fmax(d,d));
b=(pow(a,e))/(log(c));
f=(fmax(f,f))+(fmax(a,d));
f=asin(e);
a=exp(b);
c=floor(b);
d=(exp(c))+(fmin(a,b));
c=floor(b);
}