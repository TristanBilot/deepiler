#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(h);
e=pow(f,f);
b=(pow(d,h))*(floor(d));
g=exp(h);
f=(fdim(g,e))+(tan(g));
if(islessequal(e,b)){
f=(fmin(g,e))*(log(g));
b=(sqrt(c))+(atan2(g,d));
c=fmin(e,a);
}
if(islessgreater(h,b)){
a=(log(a))-(fmax(f,c));
e=exp(g);
c=fmin(d,g);
f=atan2(a,a);
e=(atan2(e,d))/(log10(a));
}
}