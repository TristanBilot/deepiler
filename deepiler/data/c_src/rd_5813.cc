#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(log10(a))/(exp(d));
a=(fmax(b,g))-(sin(c));
c=(atan(c))*(log10(d));
f=pow(c,a);
b=asin(b);
f=(cos(f))/(pow(f,c));
e=(pow(a,e))/(fmin(b,f));
while(islessgreater(b,f)){
a=(fdim(a,d))-(fdim(g,b));
c=exp(d);
}
}