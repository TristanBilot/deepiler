#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmax(b,g);
a=sqrt(g);
c=fmax(g,a);
e=cos(f);
if(islessequal(a,f)){
f=acos(g);
d=atan2(b,d);
e=fmin(b,g);
c=fdim(b,b);
}
else{
e=(fmax(c,f))+(pow(g,b));
f=(exp(g))+(fmin(b,a));
g=(cos(b))-(atan(d));
g=log10(f);
}
}