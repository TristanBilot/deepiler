#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(floor(b))*(sqrt(d));
b=floor(e);
b=cos(c);
b=fmin(c,e);
b=(sin(a))*(log10(a));
while(isless(a,e)){
b=fmax(a,c);
a=(atan(e))-(fdim(d,a));
b=(atan2(a,e))-(fdim(e,b));
d=floor(a);
d=log(e);
}
if(islessgreater(d,d)){
d=(atan2(a,a))-(pow(a,c));
e=sqrt(d);
c=(fdim(d,d))*(pow(b,d));
}
else{
a=(fdim(e,e))/(fmin(e,a));
d=(fdim(b,d))-(sqrt(e));
b=cos(c);
c=(fdim(e,e))*(acos(b));
}
}