#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(pow(a,g))*(tan(b));
e=fdim(a,d);
e=(ceil(g))*(pow(g,a));
a=atan(d);
f=(pow(e,b))+(pow(b,f));
if(islessgreater(e,a)){
g=(ceil(c))-(tan(a));
b=log(f);
b=pow(a,a);
}
else{
d=floor(e);
b=(ceil(b))-(sqrt(f));
c=(atan2(b,f))*(cos(d));
a=log(d);
}
if(islessequal(c,g)){
d=(sqrt(e))/(fmin(c,f));
e=floor(a);
b=(fdim(c,d))/(cos(b));
d=(cos(a))*(acos(a));
}
else{
e=exp(a);
c=(sin(b))/(atan(b));
b=tan(b);
d=sqrt(e);
f=(pow(c,c))/(acos(b));
}
}