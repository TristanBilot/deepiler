#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(g);
c=fdim(c,f);
b=(fdim(b,h))+(pow(h,e));
while(isless(a,g)){
g=(fdim(e,c))+(sin(h));
h=acos(g);
b=(floor(h))*(exp(f));
h=(sqrt(b))/(cos(g));
}
f=(pow(a,a))-(sqrt(h));
a=fdim(d,f);
d=(pow(e,a))/(cos(e));
}