#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(g);
b=pow(g,f);
b=log10(g);
h=(tan(d))-(atan(e));
b=(log10(e))-(fmax(h,e));
c=exp(h);
a=(atan2(e,e))*(fdim(h,g));
g=pow(b,d);
h=tan(f);
if(isless(b,b)){
c=log(a);
b=(fmax(b,f))+(atan2(c,a));
b=(fdim(f,g))*(atan2(h,a));
e=(fmin(b,g))/(atan2(c,d));
a=(pow(b,h))/(pow(d,f));
}
}