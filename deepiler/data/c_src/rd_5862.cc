#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(log(a))/(ceil(a));
c=log10(f);
d=(ceil(d))*(atan(a));
e=atan2(a,d);
while(isless(f,d)){
a=fmax(d,a);
g=log(d);
f=atan2(e,g);
f=(ceil(c))-(fdim(c,f));
b=ceil(a);
}
}