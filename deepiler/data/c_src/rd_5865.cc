#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fmax(b,a);
d=(pow(c,a))+(acos(e));
if(isgreaterequal(a,e)){
a=fmax(c,e);
c=(pow(c,e))+(tan(d));
e=(atan(a))/(sin(a));
}
d=fdim(a,b);
c=tan(a);
d=sqrt(e);
e=(sin(a))*(sin(d));
}