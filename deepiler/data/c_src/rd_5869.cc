#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=exp(f);
f=(fmax(c,g))/(floor(a));
if(islessequal(c,f)){
c=fdim(a,c);
g=(tan(f))-(atan(g));
}
else{
e=(asin(c))-(sin(d));
c=asin(b);
}
f=(fmax(g,e))+(tan(b));
a=(atan2(b,f))+(cos(f));
}