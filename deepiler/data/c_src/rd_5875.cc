#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(e,g);
c=cos(g);
f=(fmax(h,a))+(fmax(a,g));
if(isgreaterequal(c,d)){
e=fdim(g,f);
f=(exp(d))*(fdim(g,f));
h=fdim(h,f);
g=fmax(g,b);
}
if(isgreaterequal(e,c)){
b=pow(b,b);
a=(fdim(h,g))*(fdim(d,a));
}
}