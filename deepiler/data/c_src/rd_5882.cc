#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(pow(e,a))+(sin(e));
e=atan(c);
e=(atan2(g,b))-(sin(e));
d=atan(c);
d=atan2(g,d);
b=fmax(a,g);
c=fmax(c,f);
d=(pow(d,a))-(exp(b));
while(islessgreater(b,c)){
c=asin(a);
f=fdim(c,g);
}
}