#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(c,e))/(atan(d));
f=atan(b);
e=fmax(b,f);
if(isless(b,f)){
a=(cos(b))*(log10(b));
d=fmax(f,f);
d=(cos(b))-(tan(b));
f=(fmin(a,d))/(log10(d));
e=(tan(b))+(fmin(f,d));
}
if(islessgreater(d,b)){
e=fmax(f,f);
f=(fmax(c,b))+(log10(d));
a=(atan2(e,c))/(fmin(c,f));
d=(fmax(d,a))*(tan(f));
d=floor(a);
}
}