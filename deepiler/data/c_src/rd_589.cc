#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=asin(a);
c=(sqrt(d))+(pow(d,a));
b=(log(a))+(ceil(d));
e=sqrt(c);
c=(pow(e,b))*(fmax(b,e));
d=(atan(b))-(asin(b));
a=pow(e,b);
while(isless(c,b)){
b=(pow(c,d))+(ceil(c));
c=acos(c);
e=(atan2(a,c))-(atan2(b,d));
b=(atan2(c,b))+(fmin(b,a));
}
}