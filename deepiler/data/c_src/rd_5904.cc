#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmax(d,e);
e=log10(f);
e=(floor(g))*(asin(f));
f=(acos(c))-(fdim(e,c));
d=acos(d);
d=fmax(d,b);
a=sin(d);
}