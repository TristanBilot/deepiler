#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(e,f))+(pow(f,c));
f=exp(c);
e=ceil(b);
d=(atan(b))/(asin(e));
e=(fmax(e,f))/(fmin(c,e));
b=pow(a,a);
if(islessgreater(b,d)){
a=(exp(e))+(atan(f));
a=(fmin(a,a))/(log10(b));
c=fdim(c,c);
}
}