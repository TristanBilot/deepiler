#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=asin(d);
c=tan(a);
d=log(d);
e=ceil(e);
b=floor(a);
while(islessequal(d,b)){
e=(ceil(b))-(atan(b));
b=atan(d);
b=(tan(b))+(cos(e));
b=(cos(d))/(fdim(a,b));
a=(atan(e))/(sin(d));
}
while(isless(a,c)){
c=(fdim(b,e))/(log10(c));
d=floor(d);
c=(pow(d,c))-(atan(d));
}
}