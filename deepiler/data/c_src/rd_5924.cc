#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(pow(h,e))/(pow(g,b));
a=(log10(a))/(sin(b));
b=log10(g);
b=(fmax(e,h))*(asin(b));
h=tan(c);
e=(sin(f))-(tan(c));
b=fmin(h,g);
if(isless(e,d)){
f=log(g);
h=pow(d,h);
b=(fmin(g,f))/(asin(c));
g=(floor(f))*(fmin(d,a));
}
else{
h=(cos(g))*(pow(h,h));
c=(pow(g,h))+(pow(d,d));
d=tan(h);
f=fmin(f,d);
}
}