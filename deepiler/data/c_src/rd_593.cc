#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(f))-(atan2(a,b));
e=fmax(b,f);
while(islessgreater(a,d)){
c=(atan(f))+(log(e));
e=asin(f);
d=exp(b);
c=(atan2(e,b))+(log(a));
}
while(isless(a,e)){
d=(cos(e))+(atan(b));
d=(atan2(c,b))/(pow(c,b));
e=log(a);
d=ceil(f);
c=fmin(f,f);
}
}