#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sin(c);
c=log(g);
a=(fdim(e,b))-(ceil(d));
while(isless(c,b)){
e=(pow(a,b))*(fmin(e,a));
h=(cos(g))/(fdim(a,d));
e=(sqrt(a))-(exp(d));
}
a=(fmax(a,e))-(pow(f,e));
g=(asin(b))*(floor(d));
d=(log10(e))/(fdim(e,g));
g=fmin(h,d);
}