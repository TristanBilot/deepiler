#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=pow(e,b);
b=(sin(b))*(atan2(e,b));
b=(sin(d))*(pow(b,d));
d=(atan2(d,e))-(fdim(b,c));
b=(log(d))+(atan2(d,e));
a=log10(a);
d=(fdim(b,a))*(fdim(e,a));
e=pow(a,d);
while(islessgreater(c,a)){
b=fdim(e,c);
c=fdim(e,d);
d=atan(a);
d=(pow(d,c))+(atan2(e,d));
}
}