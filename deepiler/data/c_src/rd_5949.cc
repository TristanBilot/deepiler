#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=sin(f);
f=(asin(g))/(atan2(a,f));
a=(pow(f,a))-(log10(b));
f=sqrt(g);
g=sin(g);
e=sqrt(d);
b=(log10(g))*(atan2(f,b));
}