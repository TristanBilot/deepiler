#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(b,b);
c=cos(e);
b=(log(a))*(fmax(a,b));
while(islessequal(c,d)){
c=atan2(c,a);
e=atan2(c,b);
}
if(isless(c,a)){
b=(atan2(d,c))*(pow(b,d));
b=pow(e,b);
}
}