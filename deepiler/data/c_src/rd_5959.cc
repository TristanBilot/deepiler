#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(tan(a))-(pow(b,b));
e=(ceil(e))/(asin(a));
d=sin(e);
c=ceil(c);
c=(fdim(a,e))-(fmax(e,d));
d=(fmax(d,e))+(cos(e));
if(islessequal(a,b)){
b=sin(c);
d=(fmin(a,e))*(exp(e));
}
}