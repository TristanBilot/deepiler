#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=pow(e,d);
f=(tan(f))-(fdim(a,f));
c=(cos(b))*(sqrt(f));
if(isgreaterequal(a,a)){
c=(fmax(c,c))/(atan2(d,a));
b=(log(f))+(sqrt(e));
b=pow(e,b);
f=sqrt(c);
}
else{
a=log10(d);
f=(pow(c,b))/(floor(a));
d=(fdim(c,e))*(fdim(a,e));
}
b=(pow(d,e))*(fdim(d,c));
f=(acos(a))-(fmax(a,f));
e=log(c);
f=cos(b);
e=(fmax(a,b))*(fdim(c,a));
}