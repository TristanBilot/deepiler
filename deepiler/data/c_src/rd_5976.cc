#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(d,c))-(floor(a));
e=fdim(b,b);
e=(tan(e))-(pow(d,c));
c=(pow(b,c))/(atan2(d,c));
e=atan2(d,e);
b=(ceil(c))+(exp(c));
b=log(b);
d=fmax(d,d);
a=(log10(e))-(cos(c));
b=floor(b);
if(islessequal(d,b)){
b=(pow(a,e))/(sin(b));
b=(sqrt(b))+(atan2(a,c));
a=floor(c);
a=(floor(d))/(fdim(c,b));
d=atan2(d,b);
}
}