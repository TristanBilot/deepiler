#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(exp(c))/(pow(a,c));
e=(acos(b))+(log10(e));
d=(fmax(a,c))*(fmin(d,a));
d=exp(e);
b=(fmax(c,c))/(log10(b));
while(islessgreater(e,a)){
c=(atan2(b,e))-(fmax(a,a));
e=fmax(d,d);
}
c=log10(c);
a=fdim(e,b);
d=(acos(a))+(log(e));
}