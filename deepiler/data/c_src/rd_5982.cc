#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(d);
a=(ceil(d))*(fdim(a,c));
while(islessequal(c,d)){
a=pow(a,c);
e=pow(a,c);
}
if(isless(a,b)){
a=sqrt(b);
b=(sqrt(a))-(pow(d,a));
}
else{
c=asin(d);
b=(fmin(e,e))+(sin(a));
}
}