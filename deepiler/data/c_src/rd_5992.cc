#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(e,c))-(atan(g));
d=exp(f);
g=(atan(a))/(fmax(g,e));
b=(fmax(e,d))+(ceil(a));
d=(pow(a,f))/(asin(a));
while(isless(d,e)){
e=fdim(b,d);
c=fmax(a,e);
d=(fmin(f,g))-(cos(f));
}
}