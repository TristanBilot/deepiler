#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log10(a))+(pow(b,a));
d=(log(e))-(pow(a,f));
while(islessequal(f,c)){
a=ceil(c);
f=(ceil(d))-(sqrt(g));
c=fdim(c,d);
}
e=(atan2(a,g))+(pow(c,a));
e=(pow(g,e))-(fdim(d,b));
b=log10(g);
}