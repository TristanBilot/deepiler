#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=pow(c,c);
f=cos(c);
e=(fmin(b,a))-(log10(f));
while(isgreaterequal(c,d)){
a=atan2(d,f);
d=(sqrt(b))-(fmin(a,c));
}
while(islessgreater(d,c)){
f=(fdim(b,c))*(fdim(c,f));
c=(asin(d))+(fmin(d,a));
b=(sin(d))/(atan2(d,f));
f=(fdim(f,a))*(atan2(c,d));
d=pow(f,e);
}
}