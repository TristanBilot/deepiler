#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmin(d,a))/(fmin(g,g));
f=log10(d);
e=(sin(e))*(atan(g));
if(isgreaterequal(b,d)){
g=(ceil(e))+(fmax(c,g));
e=fdim(a,c);
h=(log(e))/(log10(e));
}
else{
e=fmax(e,b);
h=(fmin(d,h))/(pow(a,c));
d=(pow(f,a))/(asin(f));
e=(pow(e,g))/(pow(b,g));
e=(fmax(h,e))*(log10(e));
}
while(islessequal(g,h)){
d=pow(a,g);
h=atan2(h,f);
}
}