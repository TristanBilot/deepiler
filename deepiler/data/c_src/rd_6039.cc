#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(f,g);
h=fmin(h,e);
c=(pow(c,e))/(fdim(a,c));
f=(exp(g))+(fdim(e,b));
b=(floor(a))*(sqrt(c));
d=(sin(h))/(fmax(c,e));
a=atan(f);
a=(fmax(d,f))-(floor(f));
g=exp(c);
h=fmin(d,f);
f=(floor(d))-(fmin(c,f));
}