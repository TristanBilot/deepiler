#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(f,b);
f=(fdim(b,d))-(fdim(c,d));
d=(acos(f))/(fmax(c,d));
c=exp(d);
b=(floor(c))/(tan(d));
c=fmax(d,f);
while(isgreaterequal(f,c)){
b=(exp(c))+(atan2(d,d));
b=fmin(c,c);
b=ceil(f);
d=asin(f);
f=(fmax(f,d))-(fmax(d,f));
}
}