#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(d);
b=(fmax(d,a))/(atan2(b,b));
d=(sin(c))*(log10(b));
if(islessequal(c,e)){
c=fmax(e,c);
e=floor(c);
c=acos(b);
d=tan(d);
}
else{
a=sqrt(e);
d=(fmax(e,c))/(fmax(e,d));
a=(fmax(c,c))+(fmin(b,e));
d=(acos(d))/(atan2(b,e));
a=fmax(d,e);
}
if(isgreaterequal(e,a)){
d=(sin(e))-(ceil(e));
a=(log10(e))/(fdim(d,c));
c=log(c);
d=atan(a);
}
else{
a=(floor(e))/(sin(b));
c=(fmax(a,e))*(fmax(a,b));
}
}