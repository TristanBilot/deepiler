#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmax(a,e))/(fmin(e,f));
b=(floor(e))-(cos(c));
b=(pow(c,b))-(fmin(e,d));
while(islessequal(d,e)){
e=(cos(b))-(fdim(c,b));
c=atan2(b,f);
d=(log10(d))+(acos(b));
d=(sin(a))/(fdim(e,a));
a=cos(c);
}
d=fmax(f,e);
e=(exp(b))/(log(e));
d=(pow(f,d))+(pow(c,d));
}