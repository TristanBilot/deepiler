#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(d,f))-(asin(g));
e=(atan(e))/(sin(d));
a=cos(h);
d=fmin(e,g);
c=(acos(a))+(fmin(g,d));
a=(cos(a))+(log(b));
h=fmin(c,f);
while(isgreaterequal(g,h)){
d=(tan(b))+(atan2(h,g));
g=pow(b,b);
a=atan2(h,f);
b=atan(f);
h=(fdim(c,c))-(pow(h,e));
}
}