#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fmax(e,e))*(fmin(b,d));
f=(sqrt(d))-(fdim(d,a));
c=(log(c))/(exp(f));
while(islessgreater(a,d)){
a=tan(b);
c=(atan(f))/(atan2(f,e));
}
d=(atan(d))+(pow(a,d));
e=fmax(b,c);
}