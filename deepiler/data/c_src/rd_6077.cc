#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(d,c))+(atan2(c,a));
e=fmax(d,a);
c=(fmax(b,d))-(atan(a));
c=fmax(c,a);
c=(floor(c))+(fmin(e,e));
e=(acos(d))/(log10(e));
d=(pow(a,e))+(atan(e));
a=(atan2(c,b))/(atan2(d,d));
d=(sqrt(b))+(sin(d));
e=(fmin(a,b))/(fmin(a,a));
}