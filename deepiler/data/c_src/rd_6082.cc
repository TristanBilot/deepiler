#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=tan(b);
a=(pow(e,c))+(fmin(f,e));
c=(pow(d,f))/(floor(e));
while(islessgreater(f,c)){
c=sin(e);
e=atan2(f,e);
}
if(isless(d,b)){
a=(atan(d))+(fmin(b,a));
c=sin(e);
}
else{
a=atan2(c,f);
d=fmin(f,d);
d=(floor(a))*(asin(f));
d=(atan2(f,a))/(ceil(c));
}
}