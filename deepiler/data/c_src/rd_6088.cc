#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(g,c))*(sqrt(c));
h=(fmax(f,f))*(log(c));
c=pow(a,g);
if(isgreaterequal(g,a)){
f=(pow(c,g))/(fmax(d,g));
g=(fmax(g,f))+(asin(a));
e=(fmin(c,a))*(asin(c));
f=(log10(b))-(pow(b,a));
b=(fmax(b,a))*(fmax(c,h));
}
else{
a=(cos(d))/(floor(h));
c=pow(h,e);
g=(log(f))-(floor(c));
}
b=pow(f,b);
d=(sin(d))-(log10(e));
}