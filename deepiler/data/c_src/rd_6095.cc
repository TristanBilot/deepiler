#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(b);
f=(log(a))*(fmax(e,b));
h=(fdim(e,e))-(fmax(g,a));
c=acos(e);
h=fmax(h,a);
if(islessequal(c,e)){
c=log(g);
h=log(c);
}
f=fmin(d,e);
b=ceil(c);
g=(sin(b))*(pow(c,h));
e=fmax(b,e);
}