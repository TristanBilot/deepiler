#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(e,d);
b=fmax(c,a);
if(islessequal(e,a)){
a=pow(a,e);
c=(sqrt(b))*(sin(b));
e=(acos(c))-(ceil(d));
}
else{
c=(atan2(c,e))+(atan(d));
d=(fmin(e,e))-(pow(e,b));
}
while(islessgreater(d,d)){
d=fdim(c,c);
b=fmax(c,e);
a=(atan2(a,b))-(fdim(a,e));
}
}