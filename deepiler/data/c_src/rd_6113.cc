#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(f);
e=(asin(d))+(cos(h));
a=atan(b);
d=(fdim(d,a))+(exp(a));
f=asin(e);
c=(atan(d))*(fdim(b,c));
h=asin(e);
d=(fdim(c,b))/(floor(c));
e=(atan(c))-(pow(h,b));
e=pow(b,g);
while(isless(a,a)){
h=(tan(a))*(fdim(d,f));
c=(sqrt(d))+(fmax(f,b));
b=exp(f);
a=fmax(e,b);
}
}