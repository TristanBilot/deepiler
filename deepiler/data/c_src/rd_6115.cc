#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log10(a))+(log(d));
b=pow(a,e);
b=(floor(c))*(floor(d));
b=sqrt(b);
a=fmax(b,c);
while(islessequal(d,b)){
d=ceil(d);
b=fmin(c,d);
e=(floor(e))-(fmax(b,a));
}
if(islessequal(c,e)){
a=sin(b);
d=fmax(d,b);
}
}