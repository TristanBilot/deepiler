#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(h,a))-(cos(d));
f=log(c);
if(isless(h,b)){
f=(atan2(c,g))+(cos(c));
a=(fmin(b,b))-(ceil(f));
g=(atan2(b,f))/(fmax(h,g));
f=sqrt(f);
f=(fdim(e,h))-(atan2(e,c));
}
else{
a=pow(b,a);
a=(fdim(d,f))/(floor(e));
}
c=(asin(f))-(cos(c));
b=(sin(b))+(atan2(f,b));
h=(fmin(b,h))/(atan2(a,b));
d=(sqrt(f))-(acos(d));
g=(log10(g))*(fmax(f,a));
}