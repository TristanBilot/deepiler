#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(d);
g=log(d);
b=sin(e);
while(isless(f,a)){
b=pow(f,e);
d=sqrt(a);
}
if(islessequal(c,f)){
b=(sqrt(g))/(floor(e));
d=acos(b);
h=atan2(d,g);
g=fdim(e,c);
a=log10(d);
}
else{
c=log(g);
a=fmax(e,h);
b=exp(h);
c=(log(b))+(atan2(d,a));
}
}