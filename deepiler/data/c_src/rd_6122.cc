#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan(a);
e=fmin(f,a);
d=fmax(a,b);
c=(acos(e))/(fdim(b,e));
f=fmax(e,b);
while(isless(a,d)){
d=(acos(a))*(ceil(b));
b=log10(a);
}
if(islessgreater(c,c)){
c=fmax(d,f);
a=fdim(d,f);
d=acos(a);
d=(pow(a,b))-(fdim(b,e));
d=atan2(c,b);
}
else{
b=(fmax(e,b))+(exp(c));
c=(fdim(b,b))-(fmax(a,c));
}
}