#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(a,d))/(ceil(g));
a=(fmin(e,e))/(fmax(c,a));
g=ceil(h);
d=fdim(b,c);
if(isless(b,a)){
h=tan(e);
b=(tan(f))-(pow(g,b));
}
else{
a=floor(d);
f=(fmin(b,f))-(fdim(a,d));
d=(fmin(f,b))/(tan(h));
a=ceil(b);
}
if(isless(d,a)){
a=log10(b);
f=(floor(b))+(floor(f));
f=(pow(h,e))/(fdim(e,f));
b=pow(c,d);
g=pow(g,c);
}
}