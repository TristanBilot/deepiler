#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log(e))/(pow(e,b));
a=fmax(d,d);
c=log(d);
a=pow(e,e);
if(islessequal(a,b)){
c=(fmax(a,b))-(sqrt(a));
d=log(d);
b=(pow(c,c))*(fmin(c,b));
}
else{
d=(log(c))-(sqrt(d));
c=(acos(a))+(fmin(b,e));
c=(asin(d))-(ceil(a));
}
while(islessgreater(c,c)){
a=fmin(e,b);
e=(exp(d))*(acos(b));
c=(fdim(c,d))*(acos(c));
c=(fmin(b,a))*(fmax(c,d));
}
}