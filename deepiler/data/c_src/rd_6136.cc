#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(e,a);
f=(atan2(b,f))+(atan(a));
e=(ceil(a))-(floor(g));
while(islessgreater(h,a)){
f=(fmin(d,b))*(pow(e,h));
b=fmax(c,h);
g=(pow(b,e))*(atan2(h,f));
f=fdim(b,f);
}
while(isless(d,a)){
b=fdim(b,d);
a=(acos(f))/(fmin(c,c));
a=(floor(b))-(exp(e));
e=asin(a);
e=fmax(d,a);
}
}