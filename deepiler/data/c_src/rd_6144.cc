#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=acos(e);
a=(fmin(a,c))*(atan(e));
d=(ceil(b))*(log10(c));
b=exp(b);
c=fmin(c,d);
e=(fmin(a,a))-(fmin(d,c));
e=(atan2(a,b))*(log(e));
if(isless(e,c)){
d=(sin(a))+(tan(b));
d=log(c);
d=pow(b,e);
}
else{
c=atan2(d,c);
a=(ceil(a))*(fmin(d,a));
}
}