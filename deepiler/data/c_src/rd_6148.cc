#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(e,a))*(atan2(b,c));
a=(acos(d))+(acos(f));
d=(fmin(b,e))*(fmin(b,f));
e=pow(a,e);
if(islessequal(a,e)){
f=(pow(d,e))/(atan2(b,d));
c=pow(a,b);
a=cos(e);
b=pow(e,a);
}
else{
f=(fmin(c,c))*(fdim(f,c));
c=log10(f);
}
while(islessgreater(e,f)){
c=tan(f);
d=(acos(f))-(acos(b));
d=fmax(f,c);
}
}