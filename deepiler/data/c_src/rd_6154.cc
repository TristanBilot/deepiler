#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(h,e);
d=(log10(e))*(fdim(a,d));
if(islessequal(a,f)){
c=(exp(e))-(asin(b));
b=pow(e,a);
d=cos(f);
}
else{
a=(ceil(f))-(fdim(h,c));
e=(exp(a))*(asin(d));
}
if(islessgreater(h,d)){
d=(asin(e))/(sin(c));
d=cos(e);
a=(fdim(a,b))-(atan2(c,g));
g=floor(g);
h=log10(d);
}
else{
f=pow(e,e);
b=atan2(c,c);
h=(atan2(a,g))*(exp(c));
}
}