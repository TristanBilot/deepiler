#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(d,a))*(fmax(b,e));
f=pow(g,f);
b=(fmin(e,c))-(atan(b));
d=atan2(c,a);
while(isless(f,g)){
b=log10(f);
f=cos(b);
f=(fmin(e,c))+(fmin(f,c));
}
g=atan(e);
e=(pow(e,c))-(log(b));
c=(pow(e,c))/(exp(f));
g=fmax(f,f);
a=fdim(a,g);
}