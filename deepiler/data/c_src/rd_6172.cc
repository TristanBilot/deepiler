#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fdim(c,a))-(asin(c));
b=cos(b);
b=(sin(d))+(fdim(a,e));
b=fmax(a,c);
c=(sin(a))+(sin(c));
c=pow(d,c);
a=acos(d);
if(islessgreater(d,a)){
b=pow(d,b);
c=(fmax(e,a))+(acos(b));
c=pow(d,b);
}
}