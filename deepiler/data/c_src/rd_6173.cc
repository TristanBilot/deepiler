#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fdim(d,f))*(log10(f));
f=(fdim(a,f))+(log10(b));
d=fmax(e,a);
g=pow(f,g);
while(islessequal(d,f)){
g=(fmax(d,a))/(sqrt(e));
e=(atan2(g,a))*(tan(g));
b=log(g);
g=(exp(g))/(pow(d,b));
d=sin(f);
}
if(islessequal(e,d)){
e=(fmin(d,b))-(atan2(f,e));
f=sin(a);
b=fmin(e,b);
g=(atan2(f,f))*(atan2(c,d));
}
}