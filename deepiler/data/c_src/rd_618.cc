#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(d,b);
d=atan2(a,c);
b=ceil(e);
while(islessequal(c,b)){
b=fdim(c,c);
c=(atan2(a,e))*(atan2(b,e));
}
while(islessgreater(c,e)){
e=(pow(d,e))+(exp(c));
c=fmax(a,d);
e=sin(a);
e=(fmax(b,e))/(cos(a));
e=(fdim(e,b))/(fmax(d,b));
}
}