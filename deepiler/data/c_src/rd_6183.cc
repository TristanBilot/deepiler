#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmax(c,b))+(log(f));
g=(fmin(e,g))*(atan(a));
c=(fdim(b,c))+(atan2(d,f));
a=fmax(f,g);
b=(log(b))/(sqrt(b));
g=(sin(f))-(fmax(c,b));
f=(fmax(e,a))*(cos(a));
c=(ceil(f))*(log10(f));
e=asin(c);
a=(cos(b))/(atan2(e,a));
f=(log(a))+(cos(a));
d=(acos(c))*(sin(c));
}