#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(floor(c))-(fmax(c,b));
e=(fmax(a,d))*(ceil(b));
d=atan(b);
c=(fdim(a,c))+(log10(a));
while(isgreaterequal(a,d)){
d=exp(c);
a=log(d);
d=(cos(c))/(atan2(e,e));
d=acos(b);
}
if(islessgreater(e,c)){
b=(pow(d,a))*(pow(b,d));
a=(pow(b,b))+(log(e));
c=log10(b);
d=atan2(d,b);
}
}