#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(log(a))-(sqrt(d));
d=fdim(a,d);
c=(fmin(d,b))+(floor(c));
a=fmax(a,c);
b=(asin(b))/(pow(b,c));
e=(tan(a))/(fmin(b,c));
a=(log10(a))/(cos(e));
while(isless(e,c)){
d=(fmax(b,a))*(asin(b));
d=atan2(c,b);
}
}