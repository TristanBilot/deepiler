#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(e,d);
d=(log(b))-(ceil(e));
b=exp(b);
d=(log(d))-(pow(a,a));
c=cos(a);
d=atan2(d,b);
d=atan2(e,b);
while(islessequal(c,b)){
c=atan(e);
e=cos(d);
}
}