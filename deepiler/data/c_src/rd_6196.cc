#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=asin(b);
b=(atan2(d,f))-(fmax(a,a));
e=(log(e))+(cos(d));
e=(fmax(c,a))+(floor(f));
a=(fmin(c,e))-(fmin(e,e));
d=pow(d,e);
if(isless(c,a)){
f=(floor(d))-(sqrt(c));
b=(ceil(b))+(fmin(d,b));
}
else{
f=(asin(a))/(fmax(c,f));
f=log10(a);
a=(fmax(d,f))+(log(c));
}
}