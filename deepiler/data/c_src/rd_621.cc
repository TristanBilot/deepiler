#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan(e))+(pow(b,b));
e=(exp(d))*(ceil(c));
f=(fmin(f,b))+(fmax(b,b));
if(isgreaterequal(f,d)){
d=fmin(a,e);
c=asin(b);
}
else{
c=fdim(d,f);
d=tan(f);
}
if(isgreaterequal(b,c)){
c=ceil(a);
f=fmax(g,d);
e=floor(d);
c=fmin(b,c);
g=pow(c,c);
}
}