#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(ceil(c))*(exp(a));
a=(pow(e,c))-(log10(a));
e=fdim(e,d);
e=tan(e);
while(islessequal(a,c)){
a=asin(a);
a=(pow(e,c))+(pow(b,c));
a=(pow(a,a))/(fmin(c,a));
b=pow(d,a);
}
if(isgreaterequal(a,b)){
b=(pow(c,c))/(log10(a));
e=(fmax(d,e))+(fmax(c,a));
}
else{
b=cos(d);
b=ceil(c);
a=atan(e);
d=tan(b);
}
}