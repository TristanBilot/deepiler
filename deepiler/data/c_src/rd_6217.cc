#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sin(d))/(fmax(a,c));
c=(atan(e))*(sin(a));
a=(sin(d))-(sin(b));
b=fmin(b,c);
e=(exp(d))/(fmax(d,c));
while(islessequal(b,a)){
c=atan(c);
e=(pow(a,c))/(ceil(c));
b=(pow(c,a))-(acos(d));
}
}