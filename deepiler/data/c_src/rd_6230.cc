#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log(a);
d=fdim(c,f);
e=atan2(a,f);
if(isless(f,f)){
d=(tan(b))-(fdim(b,f));
d=(fmax(f,f))+(fdim(d,b));
e=(sqrt(c))-(tan(d));
}
else{
c=(atan2(c,f))+(pow(b,a));
e=(fmin(a,e))*(atan2(a,c));
c=(sqrt(d))*(fmin(a,d));
a=(fmax(d,b))+(fmin(e,e));
}
if(isless(c,c)){
c=fmin(f,b);
a=(ceil(b))-(fdim(a,b));
}
}