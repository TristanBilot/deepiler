#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(b,e))*(floor(a));
a=(fdim(e,a))+(sqrt(b));
d=ceil(d);
a=(fmax(e,a))+(tan(b));
e=(log(e))-(fmax(d,d));
while(isgreaterequal(a,e)){
c=fmax(c,c);
b=cos(a);
}
e=(fmin(b,b))-(sin(b));
c=atan(a);
b=fmax(d,e);
}