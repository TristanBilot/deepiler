#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(exp(a))/(sin(h));
f=fmin(b,e);
h=(sin(h))-(sin(c));
while(isless(b,h)){
b=fmax(b,h);
e=(pow(c,c))/(log(a));
c=atan(a);
}
h=sqrt(f);
g=atan2(a,h);
}