#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(log(b))-(fmax(g,e));
f=log10(a);
a=log(e);
while(islessequal(b,g)){
f=(tan(b))+(log10(a));
a=(fmax(d,c))*(atan2(b,d));
b=fmin(a,a);
c=acos(a);
b=(atan2(f,g))*(fmin(a,b));
}
if(islessequal(f,d)){
b=pow(b,a);
g=log(a);
}
else{
f=(asin(g))+(pow(g,d));
b=exp(c);
a=atan2(b,a);
b=atan(d);
c=asin(d);
}
}