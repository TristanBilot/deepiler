#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(a,a);
b=(exp(a))+(fdim(a,d));
if(isless(d,e)){
e=pow(a,d);
c=pow(d,e);
e=atan2(e,d);
c=(fmax(e,d))/(fmax(e,e));
}
while(isgreaterequal(d,d)){
d=floor(c);
b=fmin(e,e);
b=(atan2(c,b))*(pow(b,d));
b=(fmin(d,b))*(fdim(e,c));
}
}