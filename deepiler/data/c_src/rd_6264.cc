#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(sin(b))/(acos(g));
g=(acos(c))-(atan2(f,a));
a=(floor(c))+(pow(d,f));
g=(ceil(b))/(exp(g));
if(islessequal(g,b)){
g=(fmax(a,f))+(cos(g));
f=atan2(d,b);
}
else{
d=(fmax(g,b))/(log(b));
c=log(a);
d=asin(c);
}
if(islessgreater(f,b)){
f=(atan2(d,g))-(fmin(c,f));
b=asin(d);
a=(atan2(b,c))+(ceil(d));
}
else{
c=fdim(a,c);
b=(pow(e,a))+(floor(c));
b=fdim(d,a);
}
}