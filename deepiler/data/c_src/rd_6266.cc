#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan2(e,e);
e=fdim(b,a);
while(islessequal(a,a)){
c=tan(f);
e=ceil(c);
a=atan(a);
a=atan2(e,b);
}
while(islessgreater(c,c)){
b=(fmax(b,c))-(atan2(e,f));
b=(ceil(d))/(fdim(e,f));
d=(cos(e))/(fdim(b,f));
}
}