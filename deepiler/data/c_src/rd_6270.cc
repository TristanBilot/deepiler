#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(floor(b))*(atan(c));
d=(pow(d,g))-(pow(g,b));
e=(sin(c))*(fdim(d,a));
f=asin(f);
a=exp(a);
a=atan(a);
g=sin(c);
b=(log10(d))/(atan2(b,b));
if(islessgreater(a,a)){
a=fmax(g,d);
g=(atan2(d,d))/(pow(g,d));
e=(fmin(e,b))*(acos(g));
}
}