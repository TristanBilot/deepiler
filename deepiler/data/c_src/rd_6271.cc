#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(d,c);
a=(atan2(f,d))/(atan2(d,b));
d=sin(h);
g=(pow(f,g))+(fdim(h,a));
if(islessgreater(a,h)){
a=fdim(c,c);
d=(sin(b))+(atan2(e,b));
f=atan2(e,e);
h=(fmax(g,a))-(exp(h));
}
if(islessgreater(a,d)){
a=log(c);
a=fdim(h,e);
}
else{
b=(pow(c,d))/(asin(d));
a=sqrt(a);
c=fmin(f,c);
f=ceil(a);
h=pow(a,g);
}
}