#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(a,d))*(cos(b));
f=(floor(e))/(fmin(b,a));
c=(log10(a))*(sin(f));
f=fmax(e,f);
b=atan(c);
b=acos(f);
f=(fmin(d,e))/(atan2(e,d));
d=(exp(b))/(floor(b));
a=(fdim(c,c))/(exp(a));
b=fdim(e,e);
}