#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=sin(a);
f=sqrt(g);
e=acos(c);
e=fmin(d,g);
a=pow(c,d);
while(isgreaterequal(f,g)){
b=sin(f);
e=(atan2(c,b))+(sqrt(f));
c=exp(d);
e=(atan2(a,d))*(sqrt(f));
d=fmin(f,b);
}
while(islessgreater(b,d)){
g=(floor(c))*(atan(d));
f=acos(b);
d=atan2(b,d);
e=fmax(e,c);
}
}