#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log10(e);
c=fmax(d,b);
e=(acos(d))+(cos(b));
d=(atan(c))*(atan(c));
e=(fmax(b,b))/(atan2(d,d));
b=(ceil(c))-(acos(e));
while(isless(c,b)){
e=(fmax(e,a))*(pow(e,d));
e=atan2(c,a);
e=(atan2(a,b))-(atan(c));
d=(fmax(a,d))-(log10(d));
}
}