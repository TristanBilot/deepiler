#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fmin(f,d))+(fdim(d,b));
f=(acos(c))*(fmax(a,b));
a=(fmax(f,e))/(fmax(c,f));
a=(sqrt(b))/(fmax(d,d));
while(isgreaterequal(b,c)){
d=atan(f);
e=(sqrt(f))*(pow(d,b));
f=fdim(f,b);
d=(log(e))-(log10(a));
}
if(islessequal(a,a)){
e=sqrt(e);
d=(sin(b))*(exp(d));
}
else{
a=(fmax(e,e))/(ceil(b));
e=fdim(c,f);
f=pow(a,b);
b=fmax(a,c);
c=fmin(a,b);
}
}