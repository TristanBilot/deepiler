#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(d,a);
a=fmin(c,e);
while(islessequal(d,a)){
b=(sqrt(b))*(fmin(d,c));
d=(sqrt(a))/(cos(a));
d=floor(c);
}
c=fdim(d,c);
c=(fmax(e,a))/(asin(b));
d=(pow(c,c))*(atan2(d,c));
a=(fmax(d,a))*(ceil(e));
}