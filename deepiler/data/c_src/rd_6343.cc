#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(log(d))/(asin(a));
g=log(c);
if(islessgreater(g,c)){
h=(asin(c))-(acos(d));
d=atan2(f,g);
h=log(b);
c=fdim(g,d);
}
else{
b=cos(c);
f=fdim(a,e);
f=(sin(e))*(tan(f));
}
e=(floor(h))/(fmin(b,f));
g=(floor(d))*(cos(f));
}