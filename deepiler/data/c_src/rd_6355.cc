#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(c,g))+(pow(e,e));
e=atan(e);
e=fdim(g,b);
e=sqrt(f);
while(islessequal(e,c)){
c=(cos(a))/(fdim(e,g));
f=(fmax(d,e))*(exp(c));
a=atan(f);
}
while(isless(d,e)){
b=fmax(e,c);
e=(atan2(b,f))-(sqrt(f));
a=atan2(f,g);
}
}