#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(atan2(d,c))*(acos(c));
e=(log10(b))-(log(g));
f=(fdim(h,e))+(pow(f,h));
d=cos(b);
g=(fmax(g,f))-(atan2(f,b));
if(isgreaterequal(b,h)){
a=fmin(e,a);
a=atan2(f,f);
f=(ceil(d))*(fmin(a,a));
b=fdim(c,c);
f=(fdim(e,e))*(pow(f,h));
}
while(isgreaterequal(f,g)){
c=(fmax(g,g))+(ceil(d));
d=asin(a);
e=(log10(c))/(sqrt(h));
d=(pow(f,f))*(fmax(a,d));
}
}