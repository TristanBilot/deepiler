#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=exp(d);
d=(pow(e,a))+(fmax(f,f));
c=(tan(f))+(asin(c));
if(isgreaterequal(f,d)){
a=(acos(b))*(exp(b));
f=fmax(c,f);
}
while(isgreaterequal(f,e)){
c=(exp(c))-(asin(f));
f=(log10(d))*(sin(c));
d=(log(c))+(fmax(b,c));
}
}