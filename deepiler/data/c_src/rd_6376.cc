#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(pow(b,f))-(asin(d));
c=(pow(a,f))-(acos(e));
a=(log(e))/(fdim(e,b));
c=(tan(b))*(log(c));
if(isless(f,a)){
f=atan2(e,b);
b=fmax(c,a);
}
a=ceil(b);
a=(sin(b))-(sin(f));
d=asin(e);
c=log(d);
a=(fdim(c,a))+(fmax(a,f));
}