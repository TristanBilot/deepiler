#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(g,d))*(log10(c));
f=(atan2(f,h))+(tan(h));
d=(fdim(g,e))/(fmin(g,h));
if(isless(d,e)){
g=cos(e);
d=floor(h);
d=floor(g);
}
while(isless(f,g)){
a=(fmin(d,h))/(exp(h));
e=pow(h,h);
b=(fmax(f,c))+(pow(d,f));
e=cos(a);
h=(fmax(h,b))*(pow(f,d));
}
}