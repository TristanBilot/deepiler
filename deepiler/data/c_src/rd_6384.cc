#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(f,d))/(pow(e,c));
e=(fdim(c,b))+(exp(a));
a=cos(f);
while(isless(d,e)){
f=(tan(e))-(exp(c));
c=fmin(d,c);
f=pow(f,b);
e=(fmin(b,e))/(atan2(c,d));
}
f=(floor(e))+(log10(b));
c=(fmin(f,a))+(fmin(a,a));
}