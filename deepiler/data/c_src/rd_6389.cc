#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=sqrt(d);
a=sqrt(c);
d=fmin(b,a);
d=(exp(f))/(fmin(b,c));
if(isless(b,c)){
a=fdim(a,e);
d=pow(a,f);
f=(log(c))/(pow(c,c));
}
else{
c=(log10(e))*(pow(b,e));
e=(pow(e,e))-(fdim(f,a));
e=(fdim(e,b))-(ceil(a));
a=sqrt(a);
}
if(islessgreater(b,d)){
a=(exp(d))/(fdim(e,c));
f=fmax(c,f);
d=sin(a);
e=fmin(c,c);
d=pow(c,e);
}
}