#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=log(c);
e=atan2(c,b);
d=(fmin(b,b))+(pow(c,c));
if(isless(b,b)){
e=(log(d))+(asin(c));
a=(atan2(c,e))-(sqrt(a));
}
else{
a=fmin(b,a);
a=acos(a);
a=(pow(b,e))-(atan2(e,c));
a=log10(c);
d=cos(d);
}
c=atan(d);
c=(atan(a))+(pow(c,b));
d=pow(e,d);
d=fdim(c,d);
}