#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(a);
f=fmax(d,d);
c=fdim(d,f);
a=cos(d);
while(isless(c,e)){
g=atan2(d,h);
h=fmax(c,b);
a=fmax(a,g);
}
a=(sin(f))-(fmax(h,h));
h=(sin(d))+(fdim(d,d));
a=exp(c);
e=(acos(b))/(log10(g));
h=fmax(h,a);
}