#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan(a);
c=(exp(c))/(acos(b));
e=floor(e);
while(isless(b,a)){
b=(asin(b))/(tan(a));
e=(fmax(d,d))+(tan(d));
a=floor(c);
a=(atan2(e,c))+(atan2(a,b));
a=(fmax(c,a))-(fmax(a,a));
}
while(isless(d,d)){
d=atan2(a,b);
e=fmax(e,e);
}
}