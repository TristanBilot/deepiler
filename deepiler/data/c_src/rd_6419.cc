#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(b,c);
f=atan(e);
c=(fmax(g,e))+(atan(b));
if(islessequal(f,f)){
a=cos(f);
a=(ceil(a))+(exp(d));
h=ceil(c);
}
else{
e=fdim(b,b);
c=(ceil(b))-(atan2(h,c));
}
if(islessequal(a,a)){
g=fmax(a,g);
a=(atan2(e,b))*(fdim(b,b));
}
else{
g=(atan2(c,f))/(acos(g));
e=log(f);
}
}