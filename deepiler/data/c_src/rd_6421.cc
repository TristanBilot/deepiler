#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(d);
c=(fdim(b,c))+(floor(g));
g=(fmax(f,g))+(pow(h,h));
d=exp(b);
if(isless(a,c)){
g=(atan2(g,d))-(cos(b));
b=(atan2(g,e))+(asin(e));
}
else{
g=floor(b);
a=fmin(a,c);
d=asin(e);
}
while(islessequal(e,b)){
d=cos(c);
a=floor(a);
e=(fdim(b,f))-(log10(c));
h=fmin(e,a);
d=fmax(f,f);
}
}