#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fdim(d,b))*(fdim(e,c));
e=tan(b);
f=fmin(f,b);
f=(atan(f))*(fmax(a,f));
c=(sqrt(e))/(atan2(b,d));
d=floor(c);
e=(fdim(a,a))*(atan(b));
a=asin(e);
d=(fmin(d,b))-(ceil(d));
f=(fdim(e,b))*(atan2(e,b));
e=atan2(f,f);
a=(atan(a))*(atan2(b,f));
}