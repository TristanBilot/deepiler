#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(a);
c=(fmax(d,b))*(floor(d));
b=(fmax(a,e))+(fmin(d,h));
c=(pow(g,f))*(log10(g));
d=sin(f);
g=log(a);
h=(ceil(h))/(ceil(c));
if(islessequal(a,b)){
b=atan(d);
g=(fdim(e,f))*(fmax(g,c));
}
else{
h=(fmin(f,a))*(fmin(f,e));
g=tan(c);
a=pow(a,b);
}
}