#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=cos(c);
e=asin(a);
c=ceil(a);
b=(tan(b))-(pow(e,b));
d=(fdim(e,b))/(fmin(b,d));
if(islessequal(e,c)){
b=(log10(a))*(floor(a));
e=(fmax(b,b))-(tan(b));
a=(pow(b,b))*(fmax(d,e));
a=(fmax(b,b))/(fdim(a,e));
a=fmax(d,c);
}
else{
c=atan2(a,c);
d=acos(a);
}
}