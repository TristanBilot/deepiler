#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmin(b,a))/(sin(d));
f=(cos(c))+(fdim(b,e));
h=(fdim(h,d))/(atan(g));
d=(atan(c))/(atan2(d,d));
d=floor(f);
while(isless(c,a)){
f=(sqrt(g))-(log10(h));
h=(exp(d))/(exp(a));
d=sin(f);
c=pow(c,g);
b=(exp(e))*(tan(d));
}
while(islessequal(a,f)){
g=ceil(d);
h=(atan(b))+(fmax(f,g));
d=log(c);
}
}