#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(pow(d,b))+(ceil(f));
e=atan2(f,g);
g=fmax(e,d);
d=tan(b);
e=(atan2(b,e))/(acos(g));
a=ceil(e);
g=(tan(c))/(pow(b,e));
d=atan2(d,b);
d=(fdim(a,d))*(fmax(c,g));
while(isless(f,a)){
g=(tan(b))-(fmin(c,d));
e=pow(b,c);
}
}