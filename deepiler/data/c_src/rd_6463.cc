#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(ceil(a))*(sqrt(a));
f=atan2(a,g);
e=log(c);
b=cos(f);
a=(log(f))*(fdim(e,a));
while(islessgreater(e,c)){
c=pow(f,b);
e=atan2(f,e);
f=(fmax(f,c))+(fmax(f,e));
e=(cos(f))-(pow(d,a));
}
while(isgreaterequal(f,d)){
e=fdim(d,b);
a=fmin(f,d);
g=fmax(e,d);
d=atan2(c,d);
}
}