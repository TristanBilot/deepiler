#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(c,h))*(fmax(c,c));
c=(fmin(f,h))+(floor(c));
c=(atan2(d,d))+(fdim(g,e));
e=fmax(h,a);
b=acos(h);
if(islessgreater(e,b)){
a=(pow(b,c))*(asin(e));
d=(floor(b))-(atan2(g,d));
d=(log10(h))*(fdim(a,f));
f=(fmin(b,d))/(log10(d));
g=(cos(d))+(log(c));
}
else{
f=(log(a))*(atan2(f,h));
a=cos(h);
g=(fmin(e,e))+(fmax(g,b));
}
b=(pow(e,f))*(exp(c));
a=sin(c);
a=cos(b);
}