#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=sin(f);
b=(sin(d))-(tan(c));
while(islessequal(b,e)){
c=(cos(a))/(fdim(h,d));
c=pow(b,a);
d=(acos(e))+(pow(c,d));
}
if(islessequal(f,b)){
a=acos(e);
e=(exp(a))+(log10(a));
f=tan(h);
b=(pow(b,a))+(acos(d));
}
else{
h=atan(b);
c=cos(d);
e=(sqrt(c))/(atan2(b,h));
h=(atan(d))+(pow(e,a));
}
}