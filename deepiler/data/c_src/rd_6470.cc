#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=exp(e);
c=fmax(f,b);
e=(pow(b,d))-(acos(b));
e=pow(d,d);
while(islessequal(c,e)){
e=(log10(f))-(log10(g));
b=pow(g,f);
}
if(islessequal(g,e)){
g=(sin(f))/(fdim(c,d));
g=fmax(f,b);
a=(exp(c))+(asin(g));
d=pow(d,c);
d=(sin(f))+(tan(a));
}
}