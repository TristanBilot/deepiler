#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmin(e,b);
f=(exp(c))/(exp(e));
e=pow(d,c);
if(isless(a,e)){
e=floor(f);
b=fdim(f,e);
b=fdim(f,a);
f=(fdim(c,d))-(acos(e));
}
f=(asin(d))/(fdim(d,d));
c=(floor(f))+(cos(c));
e=floor(b);
}