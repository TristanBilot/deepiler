#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(floor(f))*(cos(e));
b=(sin(e))*(fmax(d,a));
d=sin(g);
if(isless(b,a)){
c=fmin(e,e);
e=(atan(d))*(atan2(g,e));
f=(sin(a))-(fmin(d,c));
}
if(islessequal(e,h)){
e=sqrt(g);
f=atan2(g,g);
}
else{
h=atan2(d,g);
f=atan2(h,d);
d=(atan2(h,b))/(tan(b));
}
}