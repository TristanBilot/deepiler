#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan2(d,d);
a=(fmax(b,c))+(log(d));
if(isless(a,b)){
a=ceil(d);
d=cos(a);
a=acos(d);
a=asin(f);
}
while(islessequal(a,a)){
c=(tan(e))+(pow(d,e));
f=(acos(c))+(atan(b));
b=(sin(e))-(fmax(c,e));
}
}