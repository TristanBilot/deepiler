#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(g,a))*(ceil(a));
b=(tan(b))*(sqrt(g));
c=(atan2(c,a))-(fmin(c,a));
b=(fmax(e,a))-(atan2(b,a));
g=(sin(d))/(fdim(b,c));
f=(fmin(c,e))*(pow(c,c));
e=fdim(e,g);
d=(fmax(g,c))*(sqrt(g));
e=atan(b);
if(islessequal(g,c)){
c=sin(e);
e=fdim(g,c);
b=(log10(f))+(pow(e,g));
g=(acos(e))*(atan(b));
}
else{
f=(cos(e))-(fmax(c,b));
b=cos(c);
d=asin(a);
}
}