#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=atan(d);
d=acos(d);
f=(sin(a))*(cos(e));
if(isless(f,e)){
f=pow(f,d);
b=atan2(a,b);
e=log10(a);
}
else{
f=sqrt(b);
d=pow(e,f);
}
f=(asin(d))*(fmin(c,a));
d=(atan2(e,f))+(log(a));
e=(fmin(c,f))/(atan2(d,b));
}