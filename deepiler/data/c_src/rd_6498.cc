#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(b,b))*(tan(b));
c=(atan2(c,a))*(fmin(c,g));
while(isgreaterequal(h,h)){
g=(fmax(f,a))*(atan2(e,e));
d=exp(g);
f=(atan2(b,e))*(pow(e,e));
c=fmax(f,a);
b=(pow(e,h))-(sqrt(f));
}
if(isless(d,c)){
d=sin(g);
d=(pow(a,h))*(tan(e));
c=(atan2(f,d))+(fmax(a,c));
}
else{
c=exp(h);
f=ceil(e);
c=asin(g);
a=(atan2(a,g))*(pow(a,e));
}
}