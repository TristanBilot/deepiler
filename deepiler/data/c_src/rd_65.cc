#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(c,e);
c=fdim(c,b);
b=(tan(c))-(acos(e));
a=ceil(c);
while(islessequal(a,d)){
c=(fdim(c,a))-(sqrt(d));
e=atan2(a,d);
}
if(islessequal(e,a)){
b=(atan2(e,b))/(atan2(a,d));
c=fdim(a,c);
e=fdim(c,e);
c=fdim(c,a);
d=atan(b);
}
}