#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=floor(d);
a=(exp(c))/(atan(d));
b=sin(d);
if(isless(d,e)){
d=(cos(a))*(cos(a));
d=cos(a);
a=(tan(c))+(atan(d));
a=(fmax(d,d))/(fmin(b,a));
}
while(isless(b,b)){
e=(pow(b,c))*(log(e));
b=(log(d))*(log10(d));
}
}