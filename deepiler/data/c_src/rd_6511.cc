#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(a,e);
c=atan2(g,d);
f=(atan2(h,h))/(atan(c));
b=(log(a))*(fmax(e,a));
e=(exp(g))*(acos(f));
while(islessequal(b,h)){
h=atan(h);
b=(fdim(g,g))/(sqrt(h));
a=fmax(f,g);
e=cos(d);
b=(acos(f))-(atan2(a,d));
}
while(islessequal(f,f)){
b=atan2(f,g);
a=(sqrt(d))-(floor(e));
f=(pow(h,c))*(tan(g));
}
}