#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(b,b))*(fmax(c,e));
e=sqrt(f);
c=pow(b,c);
c=(fmax(c,b))+(log10(a));
if(islessgreater(a,b)){
f=fmin(e,a);
f=fmax(b,b);
d=fdim(a,c);
}
while(isless(b,e)){
d=(floor(f))+(acos(c));
b=(log10(f))-(exp(a));
d=(exp(c))/(fmax(e,d));
d=(floor(c))+(acos(c));
a=(fmin(d,a))-(ceil(f));
}
}