#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fmax(c,e))/(atan(b));
e=(sqrt(d))/(log10(e));
g=(log(d))/(log10(f));
g=(fmin(d,g))-(atan2(f,e));
b=(fmax(f,c))-(atan2(a,d));
f=(fmax(a,e))-(floor(d));
a=fdim(b,g);
g=(asin(d))+(log(f));
while(isless(a,d)){
a=(fmax(d,a))+(asin(b));
d=floor(e);
}
}