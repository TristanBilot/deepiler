#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(a,d))/(pow(b,e));
a=(sqrt(d))*(log10(f));
a=fdim(b,d);
d=(atan(f))/(fmax(a,e));
b=fmin(b,c);
a=asin(f);
e=ceil(d);
while(isgreaterequal(b,f)){
d=(fdim(b,b))/(atan(c));
f=(pow(a,a))+(fmax(f,a));
}
}