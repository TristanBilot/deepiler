#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(a,c);
e=(sin(b))*(log(d));
d=fmax(d,c);
e=fmax(e,d);
while(islessgreater(c,b)){
c=atan2(g,d);
g=(fmin(d,g))-(acos(b));
}
if(isless(d,a)){
g=fmax(g,d);
e=(log(e))/(ceil(d));
e=fdim(e,g);
a=(log(e))/(fmin(f,e));
}
}