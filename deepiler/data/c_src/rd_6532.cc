#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(e,b))/(pow(c,c));
h=sin(d);
a=(sqrt(c))-(sqrt(a));
e=fdim(g,e);
while(isless(c,h)){
a=log(d);
b=(fmax(f,f))/(log10(c));
h=exp(e);
}
if(isless(f,a)){
f=fmin(g,b);
f=(fdim(d,b))*(log10(a));
d=pow(c,a);
g=(fdim(b,f))-(pow(g,e));
b=(fmin(c,a))+(fmax(e,a));
}
else{
c=fdim(h,g);
h=ceil(e);
g=fdim(d,a);
d=(sqrt(b))+(fdim(b,b));
d=log(e);
}
}