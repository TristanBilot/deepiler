#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(f,d))+(atan(a));
g=pow(f,g);
while(islessgreater(c,b)){
c=atan2(f,h);
g=(fmax(g,a))+(pow(e,f));
d=atan2(a,f);
b=(log10(e))/(pow(b,a));
}
e=(asin(d))-(ceil(c));
c=(atan2(h,f))-(asin(e));
e=log10(b);
e=cos(d);
}