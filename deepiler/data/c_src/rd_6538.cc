#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan(a))+(fmax(g,g));
d=fmax(d,c);
e=atan2(c,e);
g=(fmin(e,f))*(tan(b));
c=(pow(a,b))/(sqrt(d));
while(islessgreater(e,g)){
d=(asin(a))-(exp(b));
g=(tan(d))-(asin(c));
c=(atan2(d,e))*(log10(g));
d=tan(g);
}
b=(fmax(a,c))*(atan2(c,f));
e=(fdim(g,d))+(pow(e,b));
c=(fmax(f,a))-(atan2(f,f));
e=(atan2(g,g))+(fmax(a,a));
}