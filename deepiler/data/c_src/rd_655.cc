#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(g,b);
d=(fdim(d,f))/(atan2(b,a));
f=(log10(f))-(fdim(h,e));
c=(sin(f))/(pow(e,c));
f=(log(h))*(fdim(b,e));
if(islessequal(c,e)){
e=acos(h);
a=(fmax(g,b))/(fdim(d,a));
d=fmax(c,a);
}
else{
e=fdim(f,f);
g=(fmin(f,b))*(acos(g));
a=fmin(d,b);
}
}