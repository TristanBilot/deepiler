#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(e);
b=fdim(e,b);
c=(fdim(d,f))-(fdim(b,e));
while(isless(e,f)){
a=tan(c);
b=fdim(b,b);
e=sin(e);
}
while(isless(a,f)){
c=(atan2(f,b))*(atan2(b,b));
a=fmin(c,f);
d=(pow(b,b))+(fmin(c,c));
e=(atan2(e,c))*(pow(d,b));
b=log10(a);
}
}