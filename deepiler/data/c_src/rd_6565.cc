#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan(e))*(pow(d,e));
e=(atan(a))*(fdim(a,c));
b=(tan(c))-(log10(e));
g=ceil(b);
f=(atan2(e,b))+(fdim(c,g));
b=(tan(g))/(fdim(f,f));
c=(cos(b))-(atan2(a,d));
a=atan2(f,g);
f=(fmax(f,g))*(pow(d,b));
}