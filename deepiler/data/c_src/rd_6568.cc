#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(b))/(exp(d));
d=(atan2(c,d))-(fmax(c,c));
if(isless(c,d)){
c=acos(d);
e=(fmax(c,e))+(acos(a));
e=(exp(c))*(log(c));
b=fdim(b,e);
}
while(islessgreater(b,d)){
c=cos(a);
a=(atan2(e,d))/(atan2(d,b));
d=(atan2(c,b))*(fmax(d,c));
}
}