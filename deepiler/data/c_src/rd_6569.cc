#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(d,c);
a=(fmax(b,e))*(atan(e));
while(isless(a,e)){
b=(pow(c,c))+(fdim(b,a));
c=atan2(c,a);
b=(pow(e,c))/(exp(c));
e=(fmax(e,c))*(fmin(b,b));
e=(cos(e))-(log10(e));
}
a=(fdim(d,d))-(fdim(a,e));
c=sqrt(b);
a=(fdim(b,e))+(pow(c,c));
a=(atan2(c,c))*(log10(d));
c=(pow(c,e))-(asin(d));
}