#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(floor(a))+(log10(b));
g=(exp(e))/(pow(c,e));
f=atan2(d,e);
e=(fmin(e,g))*(exp(c));
b=(floor(g))*(atan(d));
while(islessequal(b,f)){
g=atan2(b,b);
d=(acos(c))*(fmax(b,g));
g=(fdim(e,e))/(log(f));
}
while(isless(e,b)){
a=fmin(c,f);
a=fdim(d,a);
b=fmax(b,g);
}
}