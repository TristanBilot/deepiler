#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(exp(d))*(cos(d));
b=fmin(h,c);
if(islessgreater(e,f)){
h=ceil(a);
e=acos(b);
f=cos(f);
}
else{
c=fdim(b,e);
h=(acos(e))*(sqrt(b));
e=(fmin(a,g))*(log10(e));
b=sqrt(e);
}
b=(fmin(h,f))+(atan(b));
g=cos(d);
c=pow(a,a);
}