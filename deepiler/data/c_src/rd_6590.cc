#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(asin(b))/(atan2(c,b));
e=fmax(b,b);
e=(pow(a,d))*(pow(c,e));
c=(fdim(a,a))+(log(b));
d=fmin(b,f);
d=(atan2(b,b))*(atan2(b,a));
f=(sqrt(c))/(fmax(a,a));
c=(asin(d))-(acos(b));
d=asin(e);
}