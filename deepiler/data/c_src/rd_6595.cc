#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(g,d);
d=(asin(b))-(pow(b,f));
e=fmin(d,g);
b=fdim(b,b);
a=log10(d);
g=(pow(c,c))*(log10(b));
e=fdim(a,f);
a=(tan(d))/(fdim(e,c));
}