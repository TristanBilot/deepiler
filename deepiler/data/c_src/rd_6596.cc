#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(log10(a))+(log10(a));
d=(asin(b))/(fdim(f,a));
f=fdim(d,d);
c=fdim(d,d);
if(isless(c,f)){
f=fmin(d,b);
d=(floor(b))-(fmax(c,d));
c=atan2(c,c);
f=fmax(f,f);
}
while(islessgreater(f,f)){
c=sqrt(d);
e=(fmax(b,e))/(exp(b));
d=(fmin(f,f))-(tan(a));
b=(pow(a,d))-(fmin(d,a));
}
}