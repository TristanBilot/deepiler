#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log10(b);
e=atan(c);
if(islessequal(d,e)){
b=fmax(e,c);
a=atan2(d,a);
a=(atan(e))-(cos(c));
a=fmin(a,c);
c=sin(b);
}
while(isless(e,c)){
c=(atan(e))*(tan(c));
e=log(c);
b=asin(d);
d=acos(b);
}
}