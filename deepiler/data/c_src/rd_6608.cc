#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(a,f))/(pow(a,g));
b=(tan(c))-(fdim(d,d));
g=(fmax(d,a))-(cos(b));
while(islessequal(g,f)){
g=fmax(e,g);
c=(floor(d))-(atan2(g,e));
b=(fmax(b,e))+(fmin(g,f));
g=fmax(d,f);
e=(atan2(d,c))+(atan2(f,b));
}
while(islessequal(c,g)){
e=acos(e);
e=atan2(f,e);
d=fmax(g,f);
g=(pow(a,a))*(exp(g));
b=floor(c);
}
}