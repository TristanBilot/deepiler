#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(e,a);
b=exp(a);
d=(log10(a))/(fdim(d,c));
c=(atan2(e,b))+(fdim(e,c));
b=(atan2(c,b))+(atan2(d,c));
d=cos(c);
a=fdim(b,b);
d=(log(b))/(pow(d,a));
d=fmin(d,a);
if(islessequal(e,e)){
b=fmax(a,d);
a=(exp(a))*(asin(b));
e=fmin(b,c);
}
else{
c=(log(b))/(asin(d));
e=log10(a);
e=log(b);
}
}