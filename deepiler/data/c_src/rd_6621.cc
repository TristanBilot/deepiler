#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=log(a);
f=sin(g);
while(islessequal(b,g)){
b=fdim(c,e);
g=(fmin(e,c))-(pow(f,b));
a=fdim(c,g);
b=(atan2(d,c))*(fdim(a,g));
}
b=atan2(c,e);
a=(fmax(a,f))*(fmin(f,e));
}