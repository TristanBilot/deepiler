#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(e);
g=(pow(c,d))*(fdim(d,b));
if(isgreaterequal(g,h)){
d=asin(f);
b=(sin(a))*(tan(g));
}
else{
g=(fdim(b,c))/(fmin(a,d));
e=(pow(e,a))*(asin(d));
}
if(islessgreater(g,e)){
g=fdim(c,h);
b=asin(f);
d=fmin(f,b);
}
}