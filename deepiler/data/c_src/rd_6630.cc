#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(acos(b))*(atan2(c,e));
e=(atan2(a,a))-(log(e));
b=asin(c);
b=(fdim(c,b))/(atan(a));
while(isless(b,d)){
a=(atan2(e,e))+(fdim(d,a));
c=fmax(d,b);
e=pow(a,e);
}
while(isless(b,c)){
d=(tan(a))-(sqrt(b));
e=log(d);
e=fmax(c,c);
d=(floor(d))/(pow(e,e));
}
}