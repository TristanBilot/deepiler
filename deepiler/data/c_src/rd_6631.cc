#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(log(b))-(atan2(f,e));
e=(atan2(c,a))*(cos(d));
b=exp(d);
c=(fmax(c,a))-(asin(d));
while(isless(c,e)){
c=cos(e);
b=(atan2(f,f))+(fmax(c,d));
d=(pow(c,f))*(pow(b,b));
}
if(isless(a,f)){
f=fmax(e,c);
d=fmax(e,f);
f=(floor(a))+(atan2(e,b));
e=fmax(b,e);
d=asin(f);
}
else{
a=(atan(e))+(log10(f));
b=log(f);
}
}