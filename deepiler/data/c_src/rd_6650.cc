#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fdim(b,g);
f=(fmin(g,a))-(floor(c));
c=(sqrt(d))-(log(b));
c=sqrt(e);
a=(fmax(c,b))/(fdim(c,d));
while(isgreaterequal(g,g)){
b=(fmax(b,c))/(cos(c));
g=atan2(d,d);
e=(log(f))-(log10(c));
c=(log(d))*(fmin(b,d));
}
}