#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(b,a))-(pow(b,a));
d=(pow(a,b))/(atan(b));
d=log10(b);
if(islessequal(a,d)){
a=(floor(e))/(fmin(b,d));
b=ceil(c);
a=(atan2(e,a))+(pow(d,e));
}
else{
d=fmin(c,b);
d=(fdim(b,c))/(log10(b));
d=fmax(d,d);
c=fmin(b,d);
a=(pow(b,e))/(fmax(c,a));
}
while(isless(d,a)){
a=(ceil(b))-(acos(b));
e=atan(c);
}
}