#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(e);
d=atan(d);
e=acos(d);
if(isless(e,e)){
b=fmax(d,e);
c=exp(a);
c=fmax(b,d);
}
a=(pow(d,b))*(atan2(c,d));
a=(asin(a))*(fmin(e,b));
}