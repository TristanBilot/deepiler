#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(b,b))-(fmax(d,c));
c=floor(e);
while(isgreaterequal(d,b)){
b=log(d);
b=fmax(c,e);
d=log10(a);
a=fmin(a,b);
}
c=(atan(a))*(fmax(b,a));
a=atan2(e,c);
}