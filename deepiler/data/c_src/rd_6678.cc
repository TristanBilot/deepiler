#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(sin(e))/(asin(c));
g=fmin(c,f);
d=(fmin(b,b))/(fdim(f,f));
c=fmax(a,b);
c=cos(b);
e=(asin(b))-(acos(f));
a=pow(a,c);
g=(ceil(e))+(fmax(d,c));
if(isgreaterequal(g,c)){
b=(acos(c))+(fmax(g,e));
e=(pow(a,a))/(pow(b,a));
g=(tan(b))*(log(f));
c=pow(a,b);
}
}