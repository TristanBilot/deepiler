#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmin(a,e);
a=pow(d,d);
a=atan(e);
a=pow(e,d);
c=fdim(d,b);
if(islessgreater(c,d)){
d=(pow(d,a))*(asin(a));
f=floor(e);
f=(log10(d))*(acos(e));
b=log(e);
a=(exp(d))*(acos(d));
}
while(islessequal(a,b)){
c=exp(b);
a=fmax(e,d);
c=(atan2(e,f))/(tan(b));
c=sqrt(b);
}
}