#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(e,e))-(atan2(c,e));
d=(pow(c,g))/(fdim(b,a));
c=(pow(e,d))*(cos(d));
d=(atan2(e,f))+(log10(a));
while(islessequal(d,f)){
g=(atan2(f,c))*(fdim(f,b));
d=fmin(e,e);
d=ceil(g);
}
if(isgreaterequal(g,a)){
a=ceil(d);
a=fmax(a,d);
g=(sin(e))/(acos(e));
}
else{
f=exp(d);
b=pow(b,c);
b=exp(c);
d=(log10(d))-(sqrt(a));
e=sqrt(g);
}
}