#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(b,d);
c=(floor(c))-(fdim(e,e));
e=(pow(e,d))-(acos(c));
if(islessgreater(d,a)){
d=fmin(e,b);
a=(acos(a))/(fmax(b,c));
c=asin(c);
}
e=sqrt(a);
a=fmin(b,a);
}