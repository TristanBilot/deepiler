#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(a,a);
e=(log(a))-(fmin(b,d));
b=(fdim(a,b))+(fmin(d,a));
e=atan2(e,a);
a=(pow(c,a))/(fmin(a,e));
while(islessequal(b,f)){
c=fmax(b,d);
f=acos(d);
e=ceil(b);
d=log10(a);
}
while(isgreaterequal(d,b)){
a=cos(d);
a=(exp(d))/(atan(b));
c=(atan2(a,d))*(pow(e,e));
}
}