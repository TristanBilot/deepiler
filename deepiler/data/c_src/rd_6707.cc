#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(log(f))/(atan2(c,d));
a=fdim(d,f);
if(isgreaterequal(g,e)){
h=(log(a))*(atan2(g,g));
h=fmin(a,d);
c=fmax(h,h);
b=(fdim(e,d))+(ceil(f));
}
else{
e=atan2(g,f);
d=acos(g);
h=(fdim(b,h))/(atan2(a,f));
}
if(isless(e,b)){
f=(floor(c))/(log(b));
g=pow(e,h);
e=fdim(g,c);
c=fmax(h,c);
f=(atan2(a,c))*(pow(b,b));
}
else{
e=fdim(a,g);
f=(fmax(g,e))*(fdim(f,b));
f=(atan2(f,b))-(log(b));
}
}