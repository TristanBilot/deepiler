#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(b,e))*(fdim(e,b));
a=sqrt(b);
b=tan(c);
b=atan2(e,a);
if(islessgreater(d,d)){
b=exp(d);
e=fdim(e,c);
c=fmax(b,b);
d=sqrt(b);
}
else{
c=ceil(a);
a=atan(d);
b=(pow(b,c))-(pow(c,a));
e=ceil(b);
}
}