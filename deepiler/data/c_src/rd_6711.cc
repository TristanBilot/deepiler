#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(b);
b=(atan2(b,e))*(ceil(c));
e=(sin(d))-(sin(a));
b=(ceil(b))-(cos(c));
a=(tan(a))/(atan2(c,e));
a=cos(b);
while(isless(d,a)){
c=fmax(a,a);
a=(atan2(d,a))/(fmax(b,b));
b=(fmax(c,b))/(atan2(a,c));
a=(atan2(e,b))-(atan2(c,c));
a=fmax(b,a);
}
}