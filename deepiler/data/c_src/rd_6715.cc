#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(a);
d=(asin(b))/(pow(e,c));
c=fmin(b,b);
a=(exp(b))+(cos(e));
c=(sqrt(e))*(fdim(d,e));
c=atan2(e,d);
a=(exp(b))/(fmax(d,d));
if(islessgreater(a,e)){
e=fmax(b,b);
d=(fmin(d,a))-(atan2(c,e));
e=fdim(c,e);
e=(fmin(d,e))/(fmax(d,e));
}
else{
c=fmin(b,d);
d=(asin(e))*(sin(a));
b=log10(c);
c=(exp(b))/(cos(c));
a=(atan2(b,c))+(fmax(c,a));
}
}