#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(exp(b))-(atan2(d,b));
a=(fdim(a,a))-(pow(b,c));
f=fdim(e,c);
c=(fmin(c,b))-(fmin(b,a));
f=log(d);
while(islessgreater(f,e)){
a=pow(b,d);
c=fmin(f,b);
}
a=atan2(e,a);
c=(cos(d))-(tan(c));
d=cos(b);
a=(pow(e,b))-(fmax(d,d));
}