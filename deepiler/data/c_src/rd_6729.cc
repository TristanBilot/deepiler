#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=atan(c);
c=ceil(e);
f=cos(b);
c=tan(f);
if(isless(a,e)){
c=(atan2(e,f))/(exp(a));
e=floor(e);
a=(pow(b,c))*(fmin(c,c));
d=fmax(a,c);
}
else{
d=(fmax(f,d))+(log(e));
b=(cos(a))-(pow(d,b));
f=(atan2(b,e))/(pow(f,e));
e=(sin(f))-(cos(c));
f=log(d);
}
f=fmax(f,b);
d=log10(b);
}