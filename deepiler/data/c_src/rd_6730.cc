#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan2(e,d);
a=(fmin(f,a))-(log10(e));
while(isless(b,d)){
f=ceil(a);
e=cos(b);
e=sqrt(g);
a=(fmin(b,b))/(ceil(b));
e=sin(b);
}
if(isgreaterequal(f,d)){
b=exp(g);
a=fmax(g,f);
}
}