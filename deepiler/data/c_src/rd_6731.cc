#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(h,b))+(fmin(c,g));
a=(fmin(e,f))+(exp(h));
d=atan2(e,a);
e=log(d);
e=(fmax(c,b))*(pow(h,e));
while(isgreaterequal(g,c)){
c=pow(g,d);
b=fdim(a,b);
a=(fmax(f,b))+(atan(a));
e=(fdim(a,c))+(atan2(b,a));
}
}