#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(c,g))-(sqrt(a));
b=floor(d);
f=(fmax(g,a))+(floor(a));
d=(fmax(d,g))*(ceil(a));
d=fmin(g,a);
b=(floor(e))+(acos(c));
if(isgreaterequal(b,c)){
d=(log10(d))+(log(d));
c=asin(b);
e=(cos(e))-(floor(f));
f=(atan2(a,d))/(fmax(d,b));
e=log(d);
}
else{
b=pow(a,e);
e=(log10(g))*(fmax(d,b));
}
}