#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmax(b,e);
e=(atan2(d,f))-(fdim(d,b));
d=fdim(a,c);
d=ceil(g);
f=fdim(a,b);
g=(log10(e))-(pow(g,c));
c=asin(c);
if(isgreaterequal(g,a)){
d=(atan2(f,e))*(pow(e,a));
e=(fmax(b,d))/(floor(g));
}
}