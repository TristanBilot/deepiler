#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(c,e);
b=(atan2(g,h))/(fmax(d,e));
b=fdim(d,g);
a=fdim(e,d);
c=(fmax(e,g))+(acos(e));
h=(log10(b))*(fdim(c,b));
h=(fmin(f,b))-(exp(e));
if(islessgreater(c,f)){
d=(pow(g,c))-(tan(f));
g=(fmax(b,a))+(sqrt(f));
a=(fmax(b,h))+(pow(e,a));
}
else{
h=fdim(d,a);
g=(floor(b))/(atan(h));
a=(acos(b))+(exp(h));
d=exp(a);
}
}