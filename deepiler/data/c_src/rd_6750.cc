#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(e,a);
a=fmax(d,a);
e=(fmax(c,c))-(exp(c));
a=(log10(c))*(fmax(d,c));
b=(pow(a,a))/(ceil(d));
d=(atan2(b,e))/(fmax(e,b));
if(isgreaterequal(c,a)){
b=(atan2(d,a))/(exp(b));
d=pow(b,c);
e=atan(e);
e=fmin(b,d);
e=(sin(b))*(fdim(a,c));
}
else{
d=(floor(a))*(floor(e));
a=floor(d);
}
}