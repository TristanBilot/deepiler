#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sqrt(c))+(fmax(f,a));
f=(log(a))*(fmax(d,a));
if(islessequal(f,f)){
c=(pow(a,a))*(ceil(c));
g=(acos(g))+(fdim(f,f));
c=sin(c);
d=fmin(g,a);
c=(fmin(g,e))-(ceil(b));
}
d=(sin(a))*(asin(b));
d=atan2(d,g);
f=(pow(a,g))-(ceil(e));
b=(sin(d))*(cos(f));
f=(atan2(f,f))*(fmax(f,c));
}