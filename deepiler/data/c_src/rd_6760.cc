#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(c,a))/(exp(e));
e=(fmin(e,c))-(asin(f));
c=(fmax(e,f))*(sin(b));
f=(pow(b,a))+(pow(d,f));
if(isless(a,a)){
a=floor(e);
f=(pow(a,b))/(atan2(a,e));
e=cos(e);
}
}