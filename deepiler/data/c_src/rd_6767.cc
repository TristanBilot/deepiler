#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=tan(a);
d=(acos(e))/(ceil(b));
d=sqrt(a);
if(islessgreater(c,d)){
a=(fdim(a,d))*(fdim(d,a));
e=(cos(e))+(fmin(e,a));
a=(exp(c))/(fdim(a,b));
a=(fmax(d,e))-(atan(c));
}
if(isless(c,c)){
b=(log10(c))-(fmax(e,d));
a=(asin(e))+(fdim(b,a));
c=(log(a))*(pow(d,c));
}
else{
b=(fmax(e,d))*(sin(d));
b=fmax(d,e);
c=log10(a);
}
}