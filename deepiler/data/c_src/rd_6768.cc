#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(asin(b))/(fmin(b,a));
e=(atan2(e,e))-(cos(a));
b=log(d);
while(islessequal(a,d)){
d=asin(d);
e=atan(c);
}
d=(floor(b))+(acos(d));
a=(pow(d,d))*(sqrt(d));
a=pow(a,b);
c=fmin(e,b);
e=floor(e);
}