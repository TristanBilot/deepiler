#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan(d))-(atan2(b,d));
f=(atan2(f,c))-(atan2(d,b));
f=(fdim(a,f))+(atan2(f,g));
e=fmax(b,d);
if(isless(c,f)){
b=log(d);
d=exp(e);
b=(floor(d))/(log(a));
g=sqrt(g);
}
while(isgreaterequal(d,g)){
e=fdim(f,c);
d=pow(a,g);
}
}