#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=sin(f);
e=fdim(a,e);
if(islessequal(b,e)){
a=(fmin(e,a))/(fdim(d,b));
f=fmax(c,b);
g=fdim(d,b);
}
else{
d=(fmax(c,a))*(sin(d));
a=(fmin(g,f))/(fdim(c,f));
}
a=log10(g);
d=(sqrt(g))/(fmin(f,f));
d=(atan(b))*(cos(f));
}