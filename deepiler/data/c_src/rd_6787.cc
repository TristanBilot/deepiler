#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(log(d))-(fmax(e,d));
a=atan(d);
d=fmax(b,d);
e=fmin(f,g);
if(isgreaterequal(a,f)){
e=(acos(f))+(exp(d));
b=(fmin(c,g))+(ceil(c));
}
while(islessgreater(f,b)){
b=fmin(g,c);
g=(log10(d))-(sin(a));
f=cos(e);
c=atan(g);
}
}