#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(e,d);
g=(fmin(g,a))*(fdim(g,g));
g=log(c);
f=(pow(d,g))/(fmin(e,c));
while(islessequal(e,c)){
c=fmin(f,a);
d=(atan2(g,f))/(atan2(b,g));
d=fmin(b,c);
a=atan2(f,d);
}
g=(fdim(d,b))+(acos(c));
b=floor(d);
a=(pow(b,c))+(floor(e));
}