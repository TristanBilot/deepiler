#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=exp(c);
b=(sqrt(b))/(log(e));
e=fmin(e,f);
while(isless(e,e)){
c=(pow(a,e))-(atan2(a,d));
e=acos(c);
e=(atan(b))-(fdim(e,f));
f=(fmin(a,b))*(fdim(e,b));
c=(atan2(c,d))-(sin(b));
}
if(islessgreater(d,a)){
d=(acos(a))-(atan2(a,c));
a=sqrt(b);
}
}