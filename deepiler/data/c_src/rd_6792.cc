#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(floor(d))/(fmin(d,e));
d=(fmin(d,b))/(pow(d,c));
c=pow(c,b);
if(isgreaterequal(d,e)){
e=log10(e);
c=(fmax(e,c))*(ceil(b));
}
else{
c=fmin(b,b);
c=pow(d,a);
b=(sin(a))*(asin(c));
d=(atan(e))*(pow(a,c));
}
while(islessequal(e,b)){
b=cos(e);
b=sin(b);
c=pow(e,a);
c=(tan(b))*(ceil(e));
}
}