#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(c,f);
c=atan2(e,h);
c=(pow(d,a))+(asin(b));
c=log10(c);
d=(exp(h))+(log(h));
f=fdim(f,g);
if(isless(h,c)){
f=fdim(g,c);
b=(ceil(d))/(pow(h,c));
b=ceil(d);
f=(tan(f))*(tan(e));
d=atan2(c,f);
}
else{
b=log(g);
b=(floor(e))*(floor(e));
d=log10(b);
h=pow(b,f);
h=(atan(c))/(tan(g));
}
}