#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(asin(b))-(fdim(c,g));
e=(fmin(c,f))+(fmin(f,a));
d=asin(e);
e=pow(g,g);
g=atan(h);
if(islessequal(f,d)){
c=(fmax(f,a))-(fmin(d,a));
g=(atan2(h,e))*(log10(h));
}
else{
h=pow(a,b);
c=(atan2(d,b))/(pow(e,f));
a=acos(f);
d=(fmin(b,b))-(fdim(a,h));
a=atan2(d,c);
}
if(isgreaterequal(h,c)){
a=fmax(a,e);
e=(atan2(d,b))/(sqrt(c));
e=(cos(b))/(atan(h));
}
}