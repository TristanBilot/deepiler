#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=ceil(c);
d=fmax(c,e);
b=sin(d);
if(isgreaterequal(c,b)){
a=(sqrt(c))*(pow(c,d));
d=(pow(c,b))/(fmin(c,d));
a=floor(a);
c=(fdim(b,d))*(pow(a,c));
c=fdim(c,e);
}
else{
e=(cos(b))/(fmin(b,e));
a=(pow(d,b))/(pow(d,c));
}
e=(sin(d))*(fdim(b,e));
a=fmin(d,e);
b=asin(c);
}