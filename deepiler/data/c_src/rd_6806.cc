#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=asin(f);
f=(log(e))-(tan(c));
e=fdim(c,e);
c=(exp(e))/(atan(c));
b=(ceil(a))+(atan(a));
d=fmin(b,b);
a=(sin(c))+(atan2(f,d));
c=tan(d);
if(isgreaterequal(b,b)){
f=pow(e,c);
d=atan2(e,b);
d=floor(b);
f=(pow(c,e))+(ceil(b));
b=(sin(b))+(exp(d));
}
else{
e=ceil(e);
c=(pow(a,a))*(tan(b));
f=exp(f);
a=(fdim(c,f))*(tan(f));
a=atan(c);
}
}