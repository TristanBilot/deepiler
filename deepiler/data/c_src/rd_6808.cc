#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(c,a);
b=fmin(d,c);
if(isless(c,c)){
c=(pow(c,b))-(fdim(b,b));
b=exp(a);
d=(exp(b))/(floor(b));
}
else{
e=fmax(c,a);
b=pow(c,d);
}
while(isgreaterequal(d,c)){
b=fmin(a,b);
b=(exp(d))*(fdim(d,c));
a=fmin(d,d);
a=atan2(b,a);
}
}