#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=acos(c);
c=(pow(g,b))/(atan2(e,e));
h=(pow(c,e))+(exp(d));
e=(pow(d,a))/(log(e));
d=atan(a);
h=floor(f);
b=(ceil(f))*(log10(f));
c=(ceil(e))-(acos(h));
g=fmax(h,d);
if(islessgreater(g,c)){
f=fmin(f,e);
g=(fmax(e,h))+(exp(e));
}
else{
d=fmax(h,g);
h=atan2(a,c);
h=fdim(h,e);
c=floor(e);
}
}