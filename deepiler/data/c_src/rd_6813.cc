#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(g);
h=exp(f);
h=pow(g,d);
if(isless(b,c)){
e=fmin(c,g);
c=exp(e);
d=(acos(h))-(log10(b));
f=(log(a))*(fmax(b,d));
a=(pow(c,d))+(atan(g));
}
if(islessgreater(g,b)){
h=fmax(c,h);
a=fmin(d,c);
f=(pow(b,e))-(sin(d));
}
}