#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fdim(e,d))/(fdim(c,e));
c=(atan(b))/(tan(a));
e=(fmax(a,d))*(fmin(b,f));
c=fmin(d,b);
e=(fmin(a,e))*(fdim(b,f));
f=(fmax(b,f))-(tan(e));
f=fdim(f,a);
a=asin(f);
}