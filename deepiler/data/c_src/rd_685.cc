#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fdim(a,a))*(atan2(f,c));
c=(fdim(c,d))+(atan2(c,f));
while(islessequal(b,c)){
b=(asin(f))*(log(d));
c=asin(c);
e=(fmin(e,a))*(tan(c));
}
while(islessequal(d,g)){
a=(fdim(g,d))+(asin(c));
e=(fdim(e,e))/(fmax(c,e));
g=(atan2(d,g))+(log10(a));
}
}