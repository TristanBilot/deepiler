#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(b,a);
e=fdim(g,d);
g=acos(f);
b=fdim(b,e);
if(isless(g,c)){
f=(pow(a,g))/(atan2(b,c));
a=fdim(c,c);
g=(fdim(g,g))-(fmax(g,c));
f=(fdim(f,c))+(cos(b));
c=atan2(g,c);
}
else{
g=sqrt(a);
g=fmin(a,g);
a=asin(g);
}
f=atan(a);
f=(atan2(g,a))/(pow(f,g));
g=fmax(b,g);
b=(fmin(c,c))-(acos(e));
}