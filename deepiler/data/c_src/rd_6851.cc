#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(pow(g,g))+(tan(d));
d=atan(e);
b=(acos(b))/(sin(f));
b=(cos(e))*(log10(d));
b=pow(e,a);
a=(fdim(g,a))/(atan2(d,e));
if(isless(f,g)){
e=(fdim(f,c))/(pow(f,f));
e=(tan(g))/(fmin(b,b));
g=log(b);
g=(acos(d))-(atan(g));
c=(fdim(g,c))/(sqrt(g));
}
else{
g=(atan2(f,c))-(atan2(c,e));
b=(log(e))+(log10(g));
g=fdim(c,f);
a=fmax(a,b);
}
}