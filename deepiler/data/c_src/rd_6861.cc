#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(d,c))*(log(e));
c=(fmin(a,f))+(fdim(b,b));
c=(sin(f))+(atan2(b,f));
f=fdim(d,d);
if(islessequal(c,a)){
d=(exp(b))+(fmax(f,f));
c=fmax(f,d);
a=atan2(d,e);
e=(fmax(e,d))+(fmin(d,c));
b=log10(c);
}
else{
b=(fmax(e,b))+(fmax(e,a));
f=(log10(d))*(atan2(b,b));
}
b=(acos(d))*(pow(b,c));
f=(atan2(b,f))*(sqrt(a));
}