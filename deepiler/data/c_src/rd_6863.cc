#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=asin(b);
f=(fmin(a,d))-(log10(a));
e=fdim(c,a);
if(islessgreater(b,c)){
e=(tan(b))/(cos(f));
f=pow(a,f);
d=(fmax(a,c))/(fmax(b,d));
}
a=pow(c,a);
d=atan2(f,f);
}