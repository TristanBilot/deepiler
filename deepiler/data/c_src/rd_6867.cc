#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=log(a);
g=log10(e);
while(islessequal(d,d)){
f=sqrt(g);
f=(fdim(b,a))-(fmax(a,d));
a=fmax(e,b);
}
while(islessequal(d,b)){
f=ceil(d);
b=(log10(e))-(log10(b));
e=atan2(c,f);
a=(log10(c))+(tan(e));
d=(fmin(e,e))*(exp(g));
}
}