#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(b,g);
c=asin(d);
e=(acos(g))-(floor(g));
h=cos(b);
d=floor(b);
h=sin(g);
f=exp(h);
h=(atan2(h,b))+(atan2(a,h));
a=(atan(g))-(pow(a,b));
g=(fmin(h,e))+(tan(a));
}