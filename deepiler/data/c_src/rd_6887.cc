#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmax(f,c))*(floor(c));
g=(acos(g))-(atan(a));
b=exp(a);
a=(fmax(g,g))-(asin(d));
if(isgreaterequal(c,a)){
a=fmax(g,b);
c=atan(d);
a=floor(a);
e=(fdim(c,c))-(exp(a));
e=(pow(d,f))/(floor(f));
}
if(islessgreater(c,b)){
f=ceil(a);
c=(fmax(f,g))+(fmax(d,b));
f=(floor(a))*(log(a));
d=(exp(d))+(atan2(b,b));
b=fmax(c,b);
}
}