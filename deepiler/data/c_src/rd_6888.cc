#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(e,b))*(fdim(b,d));
c=(fmax(e,c))/(asin(e));
b=(pow(c,e))/(cos(a));
e=acos(d);
if(isless(c,e)){
d=log10(d);
b=(exp(b))-(floor(b));
d=fmax(e,e);
d=pow(c,a);
}
e=fdim(a,e);
a=(atan2(c,c))*(asin(a));
b=fmin(e,a);
e=fmin(e,d);
e=(atan(c))+(atan(c));
}