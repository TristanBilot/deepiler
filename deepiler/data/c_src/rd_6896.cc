#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmin(b,e))/(fmax(b,b));
d=(fdim(a,f))*(fdim(b,e));
a=ceil(b);
while(isless(d,f)){
f=ceil(b);
a=(exp(f))*(fmax(f,g));
c=(fmax(a,a))/(fmax(d,c));
}
c=(atan2(d,c))-(fmax(e,b));
f=log10(d);
c=atan2(d,g);
}