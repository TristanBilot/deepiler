#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log(f);
d=exp(a);
c=fmax(f,f);
f=(atan2(c,e))+(asin(b));
f=acos(a);
b=(fmax(a,c))*(floor(a));
e=log(d);
e=atan2(d,c);
f=(fmin(a,b))+(atan2(d,b));
d=pow(f,e);
c=fmin(c,e);
}