#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmin(b,d);
e=(fmax(d,c))*(tan(e));
b=asin(b);
b=(ceil(d))+(atan2(d,a));
c=tan(a);
while(isgreaterequal(a,c)){
b=fmax(e,e);
a=atan2(e,e);
e=(fdim(e,e))/(cos(a));
c=fmin(e,e);
d=pow(e,b);
}
while(islessequal(c,c)){
b=ceil(d);
e=(atan2(e,b))*(acos(a));
b=fdim(a,a);
c=pow(a,e);
}
}