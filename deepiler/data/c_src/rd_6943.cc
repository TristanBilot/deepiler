#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fmax(e,b);
b=(pow(a,e))-(pow(b,e));
b=atan(b);
f=(sqrt(b))*(acos(c));
f=sin(f);
f=(ceil(f))-(fmax(d,b));
f=exp(f);
d=(fmax(b,a))+(fmax(e,a));
b=(fmax(b,d))+(atan(e));
c=atan2(d,b);
d=fmin(e,d);
f=pow(d,e);
e=(floor(f))-(fmin(b,e));
}