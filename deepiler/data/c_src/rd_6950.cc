#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=atan2(b,d);
b=exp(e);
d=(asin(f))-(exp(d));
d=log(b);
while(isless(f,c)){
c=(atan(f))+(fdim(a,e));
b=(fdim(e,f))-(fdim(c,f));
}
if(isless(c,d)){
b=fmin(d,b);
d=(pow(f,b))-(fmax(f,d));
b=fmax(e,e);
e=log(d);
c=(log10(b))-(fmin(a,a));
}
else{
a=atan2(c,a);
f=log10(d);
a=cos(d);
}
}