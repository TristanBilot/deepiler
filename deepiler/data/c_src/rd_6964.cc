#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(b,d))*(sqrt(c));
e=acos(e);
d=ceil(c);
e=fmin(e,d);
c=(cos(b))*(atan(c));
while(isless(c,d)){
d=atan2(e,b);
e=fmin(d,d);
}
if(islessequal(a,a)){
c=(pow(e,b))*(atan(a));
e=(atan2(e,a))*(cos(a));
c=fmax(d,d);
a=(sqrt(e))/(exp(a));
d=(log10(b))*(fmin(b,b));
}
else{
e=(fmax(a,c))/(log10(e));
c=(log(b))+(atan2(a,b));
e=(fmax(a,d))-(atan(a));
e=(atan2(c,b))-(log(d));
}
}