#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmin(d,e);
a=tan(c);
a=(pow(c,c))/(atan2(b,e));
d=(atan2(e,d))/(pow(c,b));
b=fdim(b,a);
d=(ceil(d))+(atan2(a,d));
e=(fdim(c,c))*(ceil(a));
while(islessequal(c,e)){
b=ceil(d);
c=(acos(c))*(ceil(d));
e=(sqrt(b))/(fmin(e,c));
a=(fmax(a,e))+(pow(e,b));
e=tan(b);
}
}