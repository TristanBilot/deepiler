#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmin(a,b))-(ceil(e));
c=(asin(e))/(cos(e));
b=sqrt(c);
while(islessequal(d,a)){
b=(ceil(d))/(fmin(c,b));
e=(log10(b))-(atan(b));
d=ceil(b);
c=(fmin(c,b))+(log10(e));
}
while(isgreaterequal(c,c)){
e=(fmin(c,c))+(fmin(e,d));
a=(cos(a))+(log(e));
e=(atan2(d,d))-(fmin(e,c));
a=asin(e);
a=(acos(e))/(fmax(b,b));
}
}