#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(a,a);
e=(sqrt(e))*(fdim(e,b));
c=pow(e,b);
a=(atan(c))/(sqrt(c));
a=(fdim(d,d))/(fmin(b,e));
e=(acos(d))/(atan2(e,d));
c=sqrt(e);
e=sin(d);
}