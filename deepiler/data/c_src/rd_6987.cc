#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(g,e);
f=pow(e,a);
c=(fmax(d,b))-(sqrt(g));
h=log10(b);
while(isgreaterequal(g,f)){
c=fmax(d,c);
h=(pow(d,d))/(fdim(e,b));
}
}