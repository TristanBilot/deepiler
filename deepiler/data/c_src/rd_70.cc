#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(a,a))+(fmax(d,c));
b=log10(d);
if(isless(d,c)){
d=(fdim(b,a))*(cos(a));
b=exp(e);
e=(atan2(c,e))-(atan2(e,a));
}
else{
a=asin(a);
b=fdim(b,a);
c=fdim(e,c);
d=(ceil(c))/(asin(d));
d=atan(c);
}
if(isgreaterequal(a,c)){
c=sqrt(b);
e=log(a);
e=(ceil(e))-(pow(b,b));
d=(log10(d))+(fmax(c,d));
a=(floor(a))+(cos(b));
}
else{
b=atan2(d,e);
a=fmax(c,a);
c=log10(d);
}
}