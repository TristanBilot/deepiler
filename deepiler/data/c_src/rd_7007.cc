#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmin(a,h))-(fmin(h,h));
c=pow(f,g);
d=fdim(d,g);
g=(atan(e))-(fmax(e,b));
h=cos(e);
if(islessequal(d,e)){
f=atan(f);
f=(sin(b))/(exp(h));
}
f=fmax(e,a);
e=(pow(c,d))/(floor(f));
a=log10(d);
d=fmax(g,g);
e=fmax(e,c);
}