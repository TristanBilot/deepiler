#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(a);
h=atan2(h,f);
c=fdim(e,h);
b=(tan(g))/(cos(h));
h=fmax(d,c);
if(islessgreater(h,c)){
h=(pow(c,a))/(pow(g,d));
a=sqrt(d);
}
}