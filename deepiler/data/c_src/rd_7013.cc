#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(e,a))-(fmax(d,g));
g=(asin(h))-(fmax(e,e));
a=(fmax(h,a))*(tan(a));
while(isless(e,h)){
c=atan2(c,a);
a=cos(a);
a=log10(d);
d=sqrt(c);
e=ceil(f);
}
if(isless(g,b)){
a=(tan(e))+(fmax(h,e));
h=(sqrt(a))+(exp(c));
d=acos(g);
}
else{
a=atan(g);
f=(pow(d,f))+(floor(c));
h=(pow(d,c))+(sin(g));
}
}