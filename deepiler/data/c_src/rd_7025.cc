#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(asin(a))-(acos(e));
b=(fdim(c,b))/(tan(e));
if(islessequal(b,a)){
c=(cos(d))+(fmin(a,c));
d=fmax(c,c);
c=(fmin(b,e))/(asin(a));
d=fmin(e,a);
e=fmax(d,c);
}
else{
c=log(c);
a=(exp(d))-(atan(b));
}
while(islessgreater(b,a)){
b=pow(e,d);
e=fmin(a,c);
}
}