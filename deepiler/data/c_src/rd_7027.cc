#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(a,a))/(sin(c));
g=fmin(e,h);
b=(cos(d))+(pow(b,c));
h=exp(g);
g=(pow(d,b))+(fmax(b,d));
g=log(g);
c=(log10(a))*(log10(h));
e=(fdim(f,b))+(fdim(b,h));
f=(atan(g))-(pow(a,f));
g=fdim(b,a);
}