#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(asin(c))/(cos(b));
b=(atan2(b,e))-(atan2(h,f));
d=acos(d);
f=sqrt(a);
while(islessgreater(e,e)){
b=(floor(b))/(ceil(d));
d=fmin(b,d);
e=(floor(h))/(asin(c));
c=(sin(f))/(atan(a));
b=(sin(b))/(floor(a));
}
b=(exp(b))*(fmax(h,e));
h=atan(e);
c=(atan2(h,g))*(sqrt(a));
f=(log10(b))-(asin(e));
d=(atan(a))-(fmax(f,h));
}