#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sqrt(e);
f=exp(a);
b=(log10(a))+(fdim(b,a));
g=fdim(f,c);
if(islessequal(d,g)){
c=pow(c,e);
g=(atan(a))/(fdim(e,c));
f=cos(e);
e=cos(g);
c=(fmax(d,a))*(log10(f));
}
if(isless(f,e)){
d=sin(f);
b=(ceil(e))+(fmax(a,d));
e=(sin(d))/(ceil(d));
d=fmax(c,d);
e=(fdim(h,a))-(fmax(b,a));
}
}