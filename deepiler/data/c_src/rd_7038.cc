#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(tan(g))*(fdim(a,g));
d=(atan(e))+(log10(e));
e=(log10(b))+(fmin(a,c));
c=fmax(e,b);
e=fmin(f,d);
while(islessgreater(f,e)){
d=fmax(c,c);
g=(fdim(c,e))*(fmax(g,b));
e=tan(f);
a=sqrt(e);
a=fmax(b,b);
}
b=exp(c);
a=(fdim(b,c))/(fdim(a,d));
b=(fmax(d,b))/(atan2(e,g));
b=pow(a,c);
}