#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(a,h);
g=fmin(f,e);
d=acos(b);
d=(exp(b))-(fdim(g,e));
g=(fmax(a,d))+(atan2(d,f));
f=acos(e);
b=(fmax(c,h))*(acos(c));
d=floor(g);
while(isgreaterequal(c,b)){
d=log(b);
c=atan(d);
}
}