#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(a,e);
h=log10(d);
if(isgreaterequal(b,a)){
e=atan2(f,f);
a=(ceil(a))+(tan(b));
e=(atan2(b,f))+(fmax(c,b));
e=pow(c,c);
}
else{
f=atan2(a,c);
h=(pow(f,g))*(log(c));
c=(asin(b))/(tan(b));
f=ceil(h);
d=(acos(c))-(asin(a));
}
if(islessequal(h,d)){
e=(atan2(h,g))*(fmin(e,f));
a=(floor(f))-(atan(f));
h=fmax(h,c);
}
}