#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(b,d);
c=(cos(c))-(pow(f,f));
g=log10(e);
h=log(g);
c=fmin(g,b);
b=(fdim(d,f))+(log(h));
h=atan2(c,b);
g=(fmin(b,f))-(tan(f));
h=log(f);
d=log10(b);
f=(pow(h,c))-(asin(f));
g=(atan2(a,d))+(acos(a));
}