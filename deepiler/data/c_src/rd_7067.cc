#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(f,c);
a=exp(a);
c=tan(e);
c=pow(a,d);
while(islessequal(d,f)){
a=asin(a);
c=fdim(c,e);
e=(log10(d))+(asin(d));
d=(pow(d,d))/(pow(d,b));
}
d=(tan(e))*(cos(b));
f=(log10(f))-(fmin(c,a));
}