#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log10(c);
d=(fmax(a,e))/(acos(h));
b=sqrt(f);
if(isless(a,g)){
c=pow(b,e);
b=cos(c);
g=(exp(g))*(floor(a));
}
else{
b=(pow(h,b))-(pow(b,b));
f=(atan(a))-(sin(g));
f=(ceil(b))-(fdim(b,e));
e=(acos(g))+(floor(c));
}
a=(sqrt(c))*(asin(e));
d=(fdim(g,e))-(log(h));
d=log(c);
}