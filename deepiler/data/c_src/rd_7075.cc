#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=log(c);
d=(fmin(d,f))-(asin(f));
e=ceil(a);
c=(acos(b))/(fdim(b,e));
a=(fdim(f,f))/(fmax(f,e));
if(islessequal(c,f)){
b=tan(c);
a=pow(b,e);
b=(fmin(e,b))*(fdim(a,a));
}
else{
f=fmax(a,a);
a=(atan2(b,f))/(acos(a));
f=(acos(b))*(acos(f));
}
f=(atan(d))+(fmin(c,e));
c=asin(a);
}