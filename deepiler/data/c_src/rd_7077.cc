#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(acos(e))-(log(c));
e=(cos(a))*(fmax(f,f));
g=sqrt(e);
b=log10(g);
d=(ceil(e))*(log(c));
while(isless(d,d)){
g=(log(e))-(fdim(g,c));
b=(sqrt(b))*(asin(f));
g=exp(g);
b=ceil(b);
}
}