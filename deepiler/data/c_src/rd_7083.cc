#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log(b))*(floor(f));
d=log(c);
e=acos(c);
if(isgreaterequal(g,g)){
e=(floor(a))*(ceil(b));
c=sin(g);
}
if(isless(c,g)){
f=(atan2(b,a))*(ceil(f));
e=(floor(b))/(asin(d));
d=(pow(e,d))+(sqrt(a));
}
}