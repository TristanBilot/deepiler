#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmin(b,f))+(atan2(d,e));
f=cos(f);
b=(fmax(e,c))-(fmax(e,e));
b=log(a);
while(islessgreater(a,c)){
c=(fmin(c,a))*(atan2(d,e));
a=(fmin(a,c))-(pow(e,a));
b=log10(a);
}
f=(acos(d))*(log(d));
c=sin(c);
d=atan2(a,f);
e=asin(c);
f=(sin(b))-(log(c));
}