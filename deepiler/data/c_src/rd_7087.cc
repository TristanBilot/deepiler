#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan2(f,d);
f=atan2(d,b);
if(islessequal(g,f)){
c=fmax(a,b);
e=(fmax(f,c))*(log10(a));
}
while(isgreaterequal(a,f)){
b=(fdim(a,d))/(fdim(a,f));
a=pow(g,b);
}
}