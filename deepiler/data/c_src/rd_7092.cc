#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(atan2(b,c))/(cos(b));
a=(fmax(b,f))-(tan(c));
d=(log10(e))/(fmax(c,c));
if(islessequal(a,c)){
e=floor(a);
c=(fmax(a,f))+(pow(b,c));
}
if(islessequal(e,c)){
b=pow(a,d);
e=(floor(d))+(fmax(f,a));
}
else{
a=(fmax(c,b))+(pow(b,f));
b=fmax(a,b);
f=sin(f);
a=acos(d);
c=(atan2(e,a))-(sqrt(e));
}
}