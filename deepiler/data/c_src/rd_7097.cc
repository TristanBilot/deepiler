#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(e,d))+(fmin(c,b));
f=log10(b);
f=(fmin(f,b))+(sqrt(c));
e=acos(d);
a=pow(e,a);
while(isless(a,b)){
a=cos(f);
d=asin(a);
}
while(isgreaterequal(c,a)){
e=atan2(b,d);
a=ceil(e);
}
}