#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(b);
e=(asin(e))-(sqrt(e));
a=fdim(e,a);
d=floor(c);
b=(atan2(d,a))+(fmax(a,e));
if(islessequal(e,d)){
b=sin(c);
a=pow(c,e);
a=fmax(a,d);
}
if(islessequal(a,b)){
a=atan(c);
b=(acos(d))*(log10(b));
a=atan2(c,c);
b=(fdim(e,b))-(fdim(c,b));
}
}