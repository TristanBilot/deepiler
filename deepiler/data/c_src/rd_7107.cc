#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(tan(e))-(fmin(c,a));
e=(sqrt(d))*(ceil(b));
while(isless(d,c)){
b=(log10(c))-(fdim(b,d));
e=(exp(a))-(fdim(e,d));
}
while(islessgreater(b,e)){
a=(fmax(e,d))+(fmax(b,a));
c=fdim(e,c);
a=(pow(a,e))*(sqrt(c));
c=fmin(e,e);
}
}