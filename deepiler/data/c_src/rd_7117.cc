#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(b);
e=(fmin(b,a))-(fmax(d,d));
g=fmin(f,d);
while(islessgreater(a,c)){
c=fmin(g,b);
d=(fmin(e,a))*(fmin(d,h));
f=(atan(b))*(pow(f,e));
f=pow(a,g);
f=(asin(h))+(fmax(h,h));
}
if(islessequal(b,h)){
d=sin(d);
c=pow(g,f);
f=fdim(f,a);
b=(fmax(f,c))+(log10(b));
}
}