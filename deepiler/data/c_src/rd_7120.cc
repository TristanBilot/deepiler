#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(fdim(b,f))/(asin(d));
f=(ceil(c))/(tan(d));
c=fdim(e,a);
f=(pow(e,f))*(ceil(a));
if(islessequal(e,e)){
c=(fdim(c,f))-(log10(a));
b=(pow(e,c))*(exp(f));
e=atan2(a,a);
c=(ceil(a))/(fmin(b,b));
c=(log10(b))*(log10(c));
}
while(isless(d,a)){
e=pow(b,d);
f=exp(b);
}
}