#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(exp(a))+(atan(c));
c=tan(a);
a=(tan(d))-(fdim(a,b));
e=atan2(c,a);
while(islessgreater(a,c)){
e=(atan(a))*(pow(d,c));
a=ceil(e);
}
a=(fmin(e,c))-(fdim(a,d));
e=(fdim(e,a))*(asin(e));
d=fmin(c,d);
}