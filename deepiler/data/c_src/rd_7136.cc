#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(c,a);
d=acos(c);
b=(asin(c))-(fmax(c,d));
b=(fmax(c,c))*(fmin(c,d));
while(isless(e,a)){
d=(log10(c))-(pow(c,e));
d=(atan(c))+(pow(c,c));
e=(tan(a))+(log(c));
e=fmax(d,d);
e=fmin(b,d);
}
if(isgreaterequal(c,d)){
d=(pow(c,b))*(sqrt(c));
e=(pow(c,e))*(atan(c));
d=log10(a);
b=(atan2(e,e))/(cos(e));
}
else{
c=(ceil(e))*(atan2(c,d));
a=(atan2(c,e))*(fmax(a,d));
d=sqrt(b);
}
}