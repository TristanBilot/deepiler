#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(ceil(d))/(pow(c,d));
b=fmin(f,b);
d=(cos(e))+(fmax(d,e));
d=(fmax(b,d))-(pow(b,d));
while(islessequal(b,d)){
b=(fmin(a,d))-(sqrt(a));
f=(log10(d))-(tan(c));
c=(sin(d))-(exp(e));
}
if(islessgreater(c,e)){
a=(sin(b))-(atan2(e,a));
d=tan(e);
a=pow(b,c);
c=log10(c);
}
}