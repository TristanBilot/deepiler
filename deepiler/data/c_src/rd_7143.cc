#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fdim(b,a);
f=(pow(d,c))*(sin(g));
g=(fmax(e,b))/(sqrt(d));
if(islessgreater(c,c)){
d=(fmax(b,b))-(tan(c));
d=sqrt(a);
b=atan(a);
a=sqrt(e);
e=atan(b);
}
if(islessgreater(e,d)){
a=cos(g);
g=log10(g);
b=(cos(g))*(fmax(d,g));
}
else{
c=fmax(d,e);
c=acos(b);
}
}