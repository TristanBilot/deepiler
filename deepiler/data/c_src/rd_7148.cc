#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(a))/(fmax(b,c));
e=(atan2(a,a))*(fdim(d,d));
if(islessequal(c,d)){
b=ceil(e);
c=fdim(e,b);
}
if(isgreaterequal(e,a)){
a=(fmax(e,d))/(ceil(a));
d=pow(c,a);
d=pow(d,d);
d=(atan(c))/(fmax(c,c));
}
else{
c=(sqrt(d))+(exp(b));
b=(sqrt(d))+(log(e));
}
}