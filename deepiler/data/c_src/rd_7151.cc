#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=asin(b);
c=floor(e);
d=(fdim(e,c))+(floor(d));
c=ceil(d);
c=sin(c);
if(isless(e,e)){
d=tan(d);
d=(sin(c))*(asin(e));
d=(exp(d))/(atan2(d,a));
a=(fdim(d,b))/(fmax(d,b));
e=fdim(b,e);
}
b=fmax(d,d);
e=(asin(b))+(pow(e,a));
}