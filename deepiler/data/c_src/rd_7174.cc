#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(pow(a,e))+(floor(d));
e=(pow(a,a))*(fmax(a,b));
f=sin(a);
e=(acos(c))*(log10(a));
f=atan(e);
b=(fmin(d,d))+(fmin(d,c));
if(isgreaterequal(c,a)){
b=(fmax(d,d))+(log(f));
d=(pow(f,c))/(fmax(d,b));
d=atan(d);
a=atan2(b,f);
}
}