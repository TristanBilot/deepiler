#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(ceil(a))*(fdim(g,c));
c=(fmin(e,d))*(atan(g));
d=fmax(b,d);
while(isgreaterequal(a,d)){
e=fdim(d,d);
c=(asin(f))/(ceil(b));
g=(fdim(g,g))+(asin(b));
}
while(islessequal(e,b)){
f=(pow(g,f))/(sin(a));
g=tan(e);
d=log10(b);
}
}