#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(f,g))+(fdim(e,h));
h=(fmax(a,c))*(ceil(b));
f=cos(h);
f=acos(b);
b=cos(g);
b=(pow(d,c))*(fmin(a,g));
g=(fmin(e,h))+(sqrt(e));
g=fmax(g,g);
d=ceil(a);
c=tan(g);
}