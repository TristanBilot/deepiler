#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan2(h,h))*(asin(f));
d=(pow(c,a))*(atan2(a,d));
g=sqrt(e);
f=fmin(b,h);
g=fdim(e,e);
c=fmin(b,h);
e=(fdim(c,e))+(log(g));
while(islessequal(d,b)){
b=(log10(a))-(pow(h,d));
c=ceil(g);
a=sin(a);
h=(fdim(h,f))/(cos(g));
f=(fmax(d,d))*(pow(b,h));
}
}