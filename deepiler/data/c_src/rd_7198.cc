#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(f,f))+(fmax(a,b));
e=ceil(c);
f=(exp(b))+(pow(a,e));
if(islessgreater(b,a)){
c=(acos(a))+(tan(e));
e=(acos(b))+(pow(a,f));
a=(log(a))+(pow(c,b));
a=(log(b))-(atan2(f,c));
}
if(islessgreater(c,e)){
b=asin(e);
a=pow(e,a);
}
}