#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=atan2(f,d);
f=atan2(d,b);
f=atan2(e,a);
a=(fmax(d,d))-(ceil(d));
d=(pow(b,e))/(sin(e));
if(isgreaterequal(e,c)){
e=pow(b,c);
f=fmax(b,c);
}
f=(fmin(d,f))*(cos(a));
e=(fmax(d,d))+(fdim(e,d));
f=cos(c);
d=acos(c);
b=tan(e);
}