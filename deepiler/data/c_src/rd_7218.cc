#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmin(b,c))*(atan(d));
f=(fmin(d,e))*(exp(c));
d=(pow(b,g))/(sin(f));
while(isless(c,a)){
b=(fmin(h,g))*(fdim(c,d));
e=(log10(d))-(log(b));
c=fmax(c,h);
}
while(islessequal(c,e)){
h=fmax(d,g);
a=(log10(c))/(cos(d));
d=(fmax(d,d))*(acos(d));
a=asin(g);
h=(cos(a))/(fdim(a,c));
}
}