#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(g,b);
g=sin(e);
g=fmin(a,c);
d=log(d);
if(islessequal(g,g)){
b=log10(c);
f=(acos(e))*(pow(e,c));
e=(pow(c,a))-(atan2(e,a));
f=(cos(c))*(log10(f));
b=(ceil(g))/(pow(g,b));
}
else{
d=(log(a))-(log10(g));
c=(fmin(e,g))/(cos(c));
g=atan(b);
b=(sin(a))*(atan2(c,b));
}
}