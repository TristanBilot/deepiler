#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(h,b);
g=(acos(a))+(fmax(f,c));
e=tan(d);
c=(sin(h))*(ceil(b));
h=exp(d);
if(isless(a,f)){
g=(fdim(c,b))+(fmin(h,b));
f=fdim(h,e);
}
g=fmax(f,f);
d=acos(g);
a=(exp(g))+(exp(g));
g=pow(e,e);
e=(pow(c,d))-(floor(g));
}