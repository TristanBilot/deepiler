#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(d,c);
c=(sin(b))/(atan2(c,g));
b=pow(h,f);
f=(fmax(a,d))-(fmax(e,b));
while(isless(c,h)){
c=tan(c);
d=(atan2(f,e))/(log10(b));
h=floor(e);
d=(fmin(f,h))/(atan(d));
}
e=(atan2(f,e))*(atan(a));
a=(exp(g))+(sin(f));
d=pow(a,c);
e=(atan2(h,c))-(fmax(e,c));
h=(tan(d))/(fmin(c,c));
}