#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan2(d,b);
e=fdim(a,c);
while(islessgreater(g,b)){
c=floor(f);
d=(fmax(e,c))+(fmin(f,a));
d=(fmax(d,c))/(sqrt(d));
}
while(isgreaterequal(f,d)){
f=atan2(a,f);
b=(ceil(f))*(atan2(c,a));
a=(acos(a))+(atan(c));
f=(cos(c))+(pow(d,a));
g=fmin(e,g);
}
}