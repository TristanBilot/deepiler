#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(a,a);
c=atan(d);
h=exp(e);
d=atan2(c,c);
f=(tan(b))*(fmax(h,b));
if(isless(a,a)){
g=log10(d);
b=(asin(h))/(exp(a));
e=cos(d);
g=(pow(b,f))-(ceil(c));
}
else{
e=(atan2(h,f))*(atan2(c,a));
b=(fmax(b,d))/(tan(f));
e=floor(a);
f=(ceil(d))/(fmax(e,d));
h=pow(c,c);
}
a=fmin(h,d);
e=(pow(h,h))/(ceil(a));
f=log(c);
}