#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(d,a))*(pow(a,b));
a=cos(b);
e=(acos(c))*(floor(a));
e=fmin(c,e);
c=(exp(c))/(atan2(a,d));
while(isless(e,b)){
a=acos(d);
b=(fmax(d,d))*(log10(b));
d=exp(b);
e=(exp(e))-(atan2(b,a));
}
if(isgreaterequal(b,d)){
e=(pow(e,d))+(fmin(c,d));
a=(fmax(e,c))*(cos(a));
a=exp(c);
c=(fmax(e,d))/(atan2(a,b));
a=(log10(c))*(fmax(d,c));
}
}