#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmax(b,e);
a=log10(f);
while(islessequal(a,d)){
e=(fmax(d,c))-(atan2(e,d));
e=(fdim(f,e))+(fmin(e,a));
a=log10(f);
}
e=fmin(a,d);
c=exp(d);
a=atan2(c,b);
}