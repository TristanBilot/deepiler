#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmin(h,g))/(fmin(b,e));
c=atan2(e,e);
while(islessequal(e,f)){
e=fdim(b,d);
d=(acos(g))*(pow(a,d));
e=(sqrt(b))/(sin(e));
}
if(isless(c,e)){
d=(log10(c))+(cos(d));
a=pow(c,c);
}
else{
h=fmin(d,g);
a=log(h);
a=atan(d);
c=exp(c);
a=(fmax(a,d))/(sqrt(g));
}
}