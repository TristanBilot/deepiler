#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(b,d))+(fdim(c,c));
d=ceil(h);
h=floor(a);
g=atan2(h,h);
b=pow(a,c);
c=(fmax(g,e))/(pow(c,f));
if(islessequal(e,a)){
b=asin(h);
g=(acos(f))*(fmax(e,g));
g=atan2(g,d);
b=(sqrt(h))*(sin(h));
d=pow(d,d);
}
else{
a=(fmin(g,a))-(fmax(b,c));
g=fmax(h,b);
c=(pow(c,d))+(fdim(d,a));
a=pow(b,h);
}
}