#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(cos(a))/(fdim(d,a));
e=floor(b);
b=floor(a);
a=(fmax(e,d))*(ceil(c));
c=log10(d);
c=(cos(e))/(atan(e));
a=(log10(d))*(log10(d));
if(isless(b,c)){
e=cos(c);
b=acos(d);
}
else{
b=(atan2(d,e))-(ceil(a));
c=(atan2(a,b))-(fmax(c,e));
}
}