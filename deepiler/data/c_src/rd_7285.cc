#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=acos(c);
f=pow(c,a);
while(isgreaterequal(d,b)){
d=(cos(b))/(fmax(c,d));
g=fmin(b,f);
d=atan(b);
}
f=pow(f,c);
d=(cos(c))+(atan2(b,f));
e=fmax(g,d);
f=atan2(d,g);
f=atan2(g,b);
}