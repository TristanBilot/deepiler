#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(b,b))*(atan2(e,b));
d=(log10(b))-(exp(f));
a=(floor(b))-(atan(c));
f=(asin(d))*(exp(d));
while(isless(c,c)){
f=log10(f);
b=ceil(a);
a=fmin(c,a);
d=(atan(a))-(ceil(a));
}
while(isless(c,b)){
f=(log10(b))+(fmin(d,d));
c=fmax(e,e);
}
}