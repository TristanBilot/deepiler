#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(c,b);
a=fdim(f,a);
d=(tan(b))*(atan2(g,a));
c=(fmin(g,e))+(fdim(a,e));
b=acos(f);
if(isless(g,b)){
b=(ceil(g))-(fmax(b,f));
b=(fmin(d,b))*(pow(f,b));
c=sin(b);
g=fmin(b,a);
c=cos(f);
}
else{
f=(acos(f))*(log10(b));
d=atan2(a,d);
}
a=exp(d);
f=(log10(c))-(atan2(b,c));
e=(floor(g))*(fmax(b,a));
}