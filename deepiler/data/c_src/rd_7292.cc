#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(a,c))+(atan2(c,a));
d=floor(f);
if(isless(d,c)){
f=fmax(f,a);
b=pow(c,b);
d=(fmin(a,c))/(fmin(a,d));
}
else{
c=(exp(c))-(cos(a));
b=cos(f);
}
e=log10(a);
b=atan2(d,b);
d=(fmin(b,e))*(asin(b));
c=(acos(c))-(atan2(b,e));
}