#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmax(a,e);
c=(acos(f))*(fmin(a,e));
c=acos(c);
c=(atan2(f,e))+(cos(c));
while(islessequal(c,e)){
e=(acos(c))*(sin(c));
c=(fmax(e,d))-(exp(c));
a=asin(f);
a=fmin(e,b);
c=log(b);
}
if(isgreaterequal(a,b)){
e=fmin(c,e);
d=(exp(e))/(fmax(e,d));
d=pow(b,d);
a=log(c);
f=(sqrt(f))+(ceil(f));
}
else{
b=sqrt(f);
c=(fmax(c,b))/(atan2(f,c));
a=pow(b,f);
d=tan(f);
b=(fmin(f,b))/(exp(c));
}
}