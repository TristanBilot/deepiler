#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(sin(c))+(fmin(b,a));
a=log10(b);
d=(atan2(d,b))-(floor(c));
c=fmin(d,c);
d=(fmin(a,a))-(atan(d));
while(islessequal(c,d)){
c=(exp(a))*(fmin(e,c));
e=(log(b))/(fdim(d,b));
a=fmin(d,d);
}
c=(floor(b))/(atan2(b,e));
b=cos(e);
c=(ceil(a))/(sqrt(d));
}