#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(exp(c))+(exp(h));
d=(floor(d))/(log10(b));
while(isgreaterequal(b,d)){
d=tan(e);
g=(cos(d))+(fmax(g,e));
b=(atan2(g,c))*(atan2(g,a));
f=pow(d,g);
}
while(isgreaterequal(a,h)){
f=asin(d);
f=fdim(g,f);
h=(pow(h,b))/(cos(h));
}
}