#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(e,d);
a=tan(b);
a=(fmin(a,c))+(atan2(a,a));
d=(atan2(a,e))/(fmax(d,b));
a=pow(c,d);
e=fmax(c,a);
d=(atan2(c,b))*(pow(c,a));
while(isless(c,d)){
c=(fmin(b,c))-(atan2(c,c));
a=acos(b);
b=acos(a);
b=exp(a);
c=pow(d,e);
}
}