#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(e,c))-(pow(b,d));
d=fmax(e,e);
if(islessgreater(c,b)){
a=acos(e);
e=fmax(e,a);
}
else{
e=(fmin(c,e))/(log(b));
b=pow(d,c);
}
b=(fmin(b,a))/(fmin(a,e));
e=(atan(a))+(fmax(e,d));
a=(floor(e))-(fmin(c,e));
c=asin(d);
e=acos(d);
}