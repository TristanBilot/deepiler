#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(c,e))-(fmax(a,e));
a=fmin(c,e);
d=atan2(a,d);
c=(log(d))*(log(e));
d=(atan2(b,d))/(ceil(e));
e=(fdim(b,b))/(atan2(c,c));
d=log10(a);
if(isless(a,b)){
a=(floor(e))/(exp(c));
e=(floor(c))+(ceil(c));
d=atan(c);
}
else{
e=(pow(a,b))+(sin(a));
b=(pow(e,c))-(log10(b));
d=cos(d);
}
}