#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=tan(a);
d=pow(e,d);
while(islessequal(a,b)){
f=(fdim(b,a))+(exp(f));
d=(log(b))/(pow(a,c));
}
if(islessequal(b,d)){
f=cos(g);
d=(floor(e))/(fdim(g,d));
f=(asin(d))/(sqrt(a));
}
else{
a=(fmax(d,a))-(fmax(f,a));
a=fdim(a,d);
e=(fmin(c,a))+(atan(a));
c=(acos(f))*(atan2(e,c));
e=(log(f))*(atan2(c,e));
}
}