#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(d);
f=(fdim(c,b))-(log10(e));
d=pow(b,d);
h=(fmax(g,g))/(fmin(h,c));
h=(sin(b))/(fmin(d,g));
if(isgreaterequal(d,g)){
g=(atan(b))/(sin(b));
e=(fdim(a,b))*(floor(h));
}
if(isgreaterequal(b,c)){
h=(fdim(c,a))-(floor(f));
g=(fmax(d,h))+(cos(g));
f=fmin(g,a);
}
else{
e=floor(h);
b=fmin(d,b);
f=fmin(b,c);
h=(pow(f,e))+(pow(b,b));
}
}