#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(g,g))+(log(c));
a=acos(a);
while(islessequal(d,b)){
g=(pow(g,f))+(pow(c,g));
f=exp(d);
c=atan2(c,a);
e=fdim(d,b);
}
if(islessgreater(a,d)){
d=atan2(g,d);
a=sqrt(e);
}
else{
b=asin(e);
d=(floor(a))/(asin(b));
}
}