#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(c,c))/(acos(d));
a=(fmax(d,d))*(atan2(d,f));
f=(fmax(c,f))-(atan2(d,f));
while(islessgreater(e,c)){
a=(exp(f))-(cos(c));
e=(fdim(b,c))/(atan2(f,f));
c=(fmax(c,d))+(asin(e));
e=(atan2(e,d))/(ceil(a));
}
d=atan2(f,d);
b=(atan2(a,b))/(fdim(f,f));
b=(fdim(f,d))/(pow(e,b));
a=asin(a);
}