#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(a))*(fdim(b,c));
e=exp(c);
c=(atan2(e,c))-(fmin(e,d));
c=atan2(b,e);
a=(atan2(b,a))+(fmin(e,b));
if(islessgreater(e,e)){
b=fmax(e,b);
e=fmin(e,a);
a=(atan2(d,d))/(atan2(b,d));
}
if(isless(b,d)){
e=tan(e);
e=pow(b,b);
d=(fdim(e,b))+(sin(e));
a=sin(b);
}
}