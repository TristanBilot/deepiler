#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=asin(a);
c=fdim(e,c);
while(isless(g,b)){
a=log(f);
d=pow(d,c);
b=fdim(g,d);
}
c=(atan2(g,a))*(atan(f));
b=(acos(e))-(log10(a));
}