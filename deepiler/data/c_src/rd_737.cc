#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(c,e))+(fdim(c,d));
e=fmax(c,b);
if(isless(c,b)){
c=fmax(a,c);
a=(acos(a))-(exp(c));
b=(fmin(a,b))+(atan(c));
}
if(isgreaterequal(a,b)){
c=ceil(c);
d=(floor(e))+(atan2(e,c));
b=log(a);
d=pow(a,d);
}
else{
c=(cos(c))-(sin(d));
d=(acos(e))/(tan(a));
}
}