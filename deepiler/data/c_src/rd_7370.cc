#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(acos(b))*(log10(a));
c=fmax(e,b);
if(isless(b,a)){
c=fdim(e,d);
a=(fdim(a,b))+(floor(e));
d=(log(a))/(tan(d));
c=(cos(b))-(fdim(a,c));
c=(ceil(d))-(ceil(a));
}
else{
d=(atan2(a,e))-(fmax(b,b));
a=atan2(d,b);
}
c=(pow(b,c))/(fmax(b,d));
c=(atan2(c,c))/(tan(d));
b=(pow(a,a))/(atan2(a,b));
}