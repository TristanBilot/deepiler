#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log10(a))+(fdim(f,d));
e=(sin(a))/(fmax(h,d));
f=(tan(c))/(sqrt(b));
g=(atan2(b,e))+(ceil(g));
b=fdim(c,e);
g=(pow(b,g))-(acos(g));
c=fdim(b,e);
e=log(g);
c=(cos(c))/(fmax(d,c));
}