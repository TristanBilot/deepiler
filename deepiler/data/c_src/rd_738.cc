#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(b,b))-(fmax(c,a));
f=(ceil(e))-(log(e));
d=tan(a);
a=(pow(d,d))*(sin(f));
e=floor(e);
e=log(b);
f=fmin(e,c);
while(islessequal(f,e)){
d=(log(a))/(atan(b));
a=(atan2(a,e))*(fmax(d,f));
d=fdim(f,b);
}
}