#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(atan(c))*(fmin(c,c));
f=fmin(d,e);
b=sqrt(d);
f=(fmin(e,f))+(fmax(d,b));
g=floor(d);
while(islessgreater(e,e)){
d=sin(g);
f=sin(b);
g=exp(f);
}
f=pow(g,b);
b=tan(f);
}