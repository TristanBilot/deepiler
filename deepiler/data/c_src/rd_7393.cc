#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(b))*(asin(a));
e=(sin(b))+(atan(a));
d=fmax(e,c);
e=atan2(d,b);
a=asin(c);
while(isless(d,e)){
a=(fmax(a,c))-(fdim(d,b));
d=fdim(a,c);
a=sin(d);
}
if(isless(e,a)){
c=sqrt(c);
e=(log(a))-(fmax(c,c));
b=(fmin(b,d))+(asin(c));
}
else{
d=atan2(d,d);
c=(atan2(a,b))-(fmin(e,d));
b=fmax(a,d);
d=tan(c);
b=(pow(c,b))+(pow(d,c));
}
}