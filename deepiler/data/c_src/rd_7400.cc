#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(e,e))*(asin(d));
c=(sqrt(c))*(fmax(b,d));
a=floor(b);
if(islessequal(c,a)){
c=(fmin(e,a))/(log(a));
e=pow(e,e);
d=(atan2(e,a))/(pow(e,d));
b=(log(c))*(fmax(c,a));
}
b=floor(a);
d=fdim(d,e);
b=cos(c);
a=(floor(a))*(log10(a));
}