#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(asin(e))*(fmin(d,d));
h=atan2(g,g);
h=(fmin(g,e))*(fdim(f,d));
d=atan2(e,g);
g=(atan2(a,g))+(sqrt(h));
c=(fdim(d,c))*(log(b));
f=(fdim(a,h))+(fmin(f,h));
while(islessequal(f,b)){
b=(cos(f))+(atan2(e,f));
e=pow(h,c);
a=fmin(f,d);
}
}