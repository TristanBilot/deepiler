#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(log10(a))/(fmax(e,d));
f=(acos(b))*(tan(a));
if(islessgreater(a,b)){
d=pow(d,a);
d=fdim(b,f);
c=(log(e))*(tan(c));
d=fmin(e,f);
}
else{
b=(log(f))+(asin(b));
e=sqrt(f);
}
d=exp(b);
e=(log(e))*(floor(f));
d=(pow(a,c))-(log10(a));
}