#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(tan(a))*(fmax(b,e));
c=floor(f);
while(isless(d,b)){
b=(fdim(a,d))-(fdim(c,b));
e=acos(d);
b=(ceil(f))*(fmin(c,a));
}
f=atan2(f,e);
c=(exp(b))+(fdim(c,b));
}