#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(log10(c))-(cos(e));
c=sin(d);
while(isgreaterequal(d,c)){
c=(sqrt(a))*(atan2(d,d));
b=fmin(c,e);
e=atan(e);
d=(atan2(d,d))-(fmax(c,e));
d=tan(d);
}
c=(floor(b))*(atan2(d,e));
d=asin(e);
b=(sin(e))*(atan(a));
a=(asin(a))-(sqrt(a));
d=(ceil(a))+(pow(b,b));
}