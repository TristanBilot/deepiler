#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(b,a))+(atan2(b,e));
a=fdim(a,c);
a=sqrt(a);
a=cos(c);
d=(fmax(a,a))*(atan2(e,e));
a=(fmax(e,e))/(fmin(e,a));
e=fdim(d,d);
e=fmax(d,d);
while(islessgreater(e,b)){
d=log(d);
c=pow(a,e);
b=ceil(a);
b=(fmin(a,d))/(fmin(b,a));
d=(atan2(c,d))*(floor(b));
}
}