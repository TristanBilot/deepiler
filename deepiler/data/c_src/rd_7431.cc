#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(b);
b=(fmin(c,b))/(fmax(d,c));
if(islessequal(d,d)){
e=fmax(a,b);
b=tan(d);
c=fmin(e,c);
}
e=(atan2(b,b))+(fdim(e,e));
e=(tan(e))/(sqrt(e));
d=fdim(b,a);
}