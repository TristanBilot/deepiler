#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=pow(c,f);
d=(tan(c))-(sqrt(c));
if(isless(e,b)){
d=tan(e);
f=(fmin(a,b))/(ceil(e));
b=fmax(b,b);
}
else{
e=fmax(d,c);
b=(tan(a))*(pow(e,d));
e=(cos(b))-(exp(b));
b=fmin(e,f);
}
if(isless(f,b)){
e=log(e);
e=tan(b);
}
}