#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(atan2(f,d))+(exp(c));
f=(exp(c))*(fdim(b,d));
b=fmin(a,d);
while(isless(c,b)){
b=(asin(a))*(fdim(e,a));
c=fmin(a,e);
d=sin(c);
}
while(islessequal(e,c)){
f=tan(d);
e=atan2(b,f);
d=fmax(d,c);
a=(atan2(a,f))*(log(e));
b=atan(f);
}
}