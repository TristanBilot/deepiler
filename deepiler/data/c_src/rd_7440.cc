#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=asin(d);
f=fmin(b,c);
c=(ceil(c))-(fdim(a,c));
while(islessgreater(e,c)){
a=(asin(c))-(asin(d));
f=atan2(c,d);
f=exp(d);
}
if(islessequal(b,d)){
c=fmax(a,f);
f=fmin(f,a);
a=fmin(d,d);
b=(atan2(b,a))-(pow(c,c));
f=(exp(d))+(pow(a,f));
}
else{
f=(atan2(d,a))-(exp(a));
e=fdim(e,d);
b=(pow(b,b))*(exp(c));
b=(fmin(f,d))+(pow(c,d));
}
}