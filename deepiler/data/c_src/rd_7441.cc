#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(sin(a))/(cos(a));
h=fmax(c,f);
d=ceil(h);
c=(fdim(f,b))*(ceil(g));
g=pow(e,c);
b=fdim(b,g);
g=atan2(h,f);
if(islessgreater(c,g)){
e=(floor(c))-(fmin(g,a));
f=fmin(d,f);
f=pow(e,b);
f=(atan2(h,g))/(fdim(g,c));
}
else{
a=sqrt(g);
a=(pow(d,d))-(acos(g));
f=(asin(a))-(atan(a));
e=asin(f);
}
}