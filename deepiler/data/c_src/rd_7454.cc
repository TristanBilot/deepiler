#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(acos(e))-(pow(e,b));
f=(cos(a))/(tan(f));
c=(fmin(f,c))-(atan2(e,g));
a=(floor(g))*(atan2(a,e));
d=floor(a);
if(islessgreater(f,b)){
f=asin(b);
e=fmax(e,a);
f=(tan(d))-(log10(a));
g=floor(d);
}
}