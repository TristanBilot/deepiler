#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(e,e))*(fmax(a,c));
e=log10(a);
d=(asin(e))-(cos(c));
if(islessequal(d,c)){
c=pow(a,a);
e=(asin(e))*(log10(c));
c=(fmax(b,d))*(fdim(e,b));
c=(sqrt(c))*(tan(d));
}
e=fmax(d,a);
e=(asin(b))-(atan2(d,d));
a=(acos(a))/(atan2(c,a));
d=fmax(d,d);
}