#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(sqrt(f))/(cos(c));
f=pow(a,d);
g=(fdim(d,b))-(atan2(e,a));
c=(ceil(c))-(floor(b));
a=(fdim(e,e))*(log10(b));
while(islessgreater(g,e)){
e=(fdim(a,g))+(cos(e));
b=sin(c);
}
if(isless(f,g)){
d=sin(e);
a=atan2(f,g);
d=(tan(a))-(cos(g));
}
else{
f=(sqrt(b))/(log10(b));
a=(atan2(g,e))/(sin(e));
d=fmin(d,b);
b=tan(g);
}
}