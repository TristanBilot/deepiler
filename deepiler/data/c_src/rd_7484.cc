#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(atan2(h,d))*(fmax(d,d));
f=(fmax(h,h))-(fdim(g,c));
g=fmax(e,a);
if(isgreaterequal(a,f)){
d=fmax(a,b);
h=acos(e);
b=(fmax(c,g))-(acos(a));
g=(ceil(c))*(fmax(g,b));
}
c=fmin(b,e);
c=fmax(h,f);
f=log10(d);
c=asin(c);
}