#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(d,d);
c=(pow(a,d))-(fmax(e,a));
c=atan2(a,a);
if(islessgreater(a,b)){
a=(exp(b))/(asin(e));
c=(pow(e,d))+(fmin(c,a));
}
else{
e=(fmax(c,c))/(fmax(e,d));
e=pow(b,e);
e=(atan(a))/(log(d));
a=(fdim(d,c))/(pow(a,b));
}
if(islessgreater(a,b)){
e=(acos(d))*(fmax(b,e));
e=(sin(d))-(fmax(e,d));
e=pow(e,a);
d=(fmax(d,b))/(atan2(d,a));
}
else{
b=log(d);
a=log(a);
}
}