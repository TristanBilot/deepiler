#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=asin(e);
b=fmax(e,e);
f=asin(g);
c=atan2(d,b);
if(isless(b,a)){
f=fdim(b,a);
g=fdim(e,d);
g=(fmin(a,a))*(exp(c));
}
else{
b=(sqrt(f))+(fmin(f,d));
g=(atan(d))*(fmin(a,g));
a=(fdim(c,e))+(atan2(e,d));
d=cos(e);
}
if(islessgreater(b,f)){
g=sqrt(b);
c=(fdim(d,g))-(fdim(g,f));
}
else{
f=fdim(a,b);
b=(log(g))-(tan(f));
c=(log10(a))/(fmin(d,a));
g=fmax(a,b);
b=(pow(g,c))-(ceil(e));
}
}