#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(c);
d=(asin(f))*(cos(f));
g=(atan2(h,b))*(fmax(h,c));
d=(cos(h))*(tan(h));
b=(cos(e))/(fdim(h,e));
h=(tan(e))*(atan(e));
b=log10(d);
d=(fmax(h,e))/(cos(b));
b=log10(c);
d=fmax(e,h);
f=(fdim(e,h))-(ceil(c));
g=log10(a);
g=(asin(a))*(fdim(h,e));
}