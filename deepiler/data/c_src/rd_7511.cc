#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(exp(f))-(atan2(d,c));
c=(ceil(e))*(fmax(f,c));
c=(fdim(b,d))*(ceil(c));
while(isless(f,a)){
b=(pow(f,f))+(atan2(f,f));
c=(sqrt(c))-(fmax(c,e));
f=(log10(e))*(log(e));
a=fmax(e,d);
a=log(a);
}
while(isless(e,c)){
b=(atan2(c,b))-(atan2(a,e));
a=(atan2(e,b))-(ceil(d));
}
}