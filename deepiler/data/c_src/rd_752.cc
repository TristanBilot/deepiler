#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(f,a))+(exp(h));
a=(ceil(a))/(acos(b));
g=pow(e,d);
e=(fmin(a,h))-(fmax(a,e));
f=(fmax(g,d))*(log10(a));
b=(fmin(a,b))/(pow(h,d));
g=fdim(c,b);
h=(ceil(f))/(atan2(g,b));
b=log10(f);
if(islessgreater(d,h)){
a=(fmax(f,e))/(cos(c));
c=sin(d);
b=(pow(g,a))+(fmax(a,g));
}
else{
b=(pow(e,b))/(fdim(c,g));
f=(log(c))*(pow(d,b));
g=(atan2(h,e))*(fmin(d,f));
b=fmin(d,f);
}
}