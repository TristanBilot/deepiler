#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(asin(b))+(tan(c));
d=(asin(a))*(fmin(a,a));
f=(fmax(e,d))+(atan(f));
f=acos(d);
if(islessequal(c,b)){
d=atan2(d,b);
d=atan2(e,e);
}
else{
e=(fmin(a,b))+(fmin(e,f));
f=cos(c);
c=(tan(a))+(fmax(c,f));
}
while(islessgreater(e,c)){
e=atan2(f,a);
a=log10(d);
b=(sqrt(e))*(pow(a,b));
}
}