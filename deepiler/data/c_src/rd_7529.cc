#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(asin(d))/(pow(c,e));
e=(log(c))/(exp(c));
b=(log(c))+(atan(c));
while(isless(b,e)){
b=(exp(d))-(fdim(c,d));
e=(floor(e))-(log10(e));
}
while(islessequal(c,a)){
a=(atan2(a,b))-(fmax(e,e));
d=(exp(d))+(asin(d));
c=atan2(c,a);
}
}