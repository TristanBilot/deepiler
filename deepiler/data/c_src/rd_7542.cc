#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(f,d);
b=(fmin(e,h))-(exp(g));
g=fdim(h,e);
if(islessequal(b,a)){
d=fmax(g,g);
h=(ceil(b))*(fdim(e,c));
e=(pow(c,b))+(atan2(f,b));
h=atan2(a,e);
f=(cos(e))/(sin(c));
}
else{
b=sqrt(d);
a=(asin(c))/(fdim(d,d));
b=(fmin(f,b))*(fdim(h,a));
}
e=atan2(c,b);
g=fmax(h,f);
c=(fmin(a,d))/(fmax(b,b));
c=sin(h);
}