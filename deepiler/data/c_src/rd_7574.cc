#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sqrt(c))+(fdim(b,d));
d=(acos(d))-(fmax(d,f));
a=(fdim(c,f))+(fmax(f,e));
a=fmax(a,a);
a=fmin(b,d);
if(isgreaterequal(d,b)){
a=fmin(a,d);
b=(sin(d))+(acos(b));
}
}