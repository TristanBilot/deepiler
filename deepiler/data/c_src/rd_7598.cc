#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(log10(f))+(sin(g));
c=cos(c);
f=pow(a,a);
f=log(d);
b=pow(e,d);
e=sqrt(g);
h=(pow(a,e))+(fmin(f,g));
g=(atan2(a,e))+(sin(e));
b=log10(a);
while(islessequal(b,e)){
d=(fmax(d,c))*(fmin(b,d));
d=(atan2(a,b))*(log10(c));
d=(pow(a,h))+(acos(e));
c=log(e);
}
}