#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=asin(d);
c=atan2(c,e);
a=(ceil(f))+(sqrt(f));
while(islessgreater(g,e)){
d=(ceil(b))+(fdim(e,a));
f=fdim(c,b);
g=pow(e,h);
}
d=asin(d);
h=fmin(d,b);
}