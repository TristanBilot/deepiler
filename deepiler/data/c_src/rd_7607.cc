#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(atan(a))/(fmax(c,a));
c=(atan2(b,e))*(sqrt(d));
c=cos(d);
while(isgreaterequal(d,f)){
f=(pow(b,e))*(atan2(d,c));
c=(fmin(f,f))+(pow(c,b));
d=atan2(e,a);
c=(pow(d,e))*(pow(a,a));
e=(sqrt(a))+(fmax(b,a));
}
c=fmax(a,f);
e=log(a);
d=atan(a);
f=pow(d,d);
}