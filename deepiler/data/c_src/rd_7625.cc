#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(acos(d))+(exp(b));
d=(atan(c))*(fmin(c,c));
d=tan(c);
f=fmin(a,e);
a=(cos(f))*(fmin(d,c));
if(isless(f,b)){
f=atan2(e,c);
c=(pow(b,a))*(fmax(b,a));
}
a=(fdim(f,d))/(atan2(c,b));
e=sin(e);
f=atan2(f,c);
b=(pow(a,c))/(fdim(c,a));
a=atan2(a,a);
}