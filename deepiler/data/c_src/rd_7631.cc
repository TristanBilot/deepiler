#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(a))/(fmax(c,d));
c=(exp(e))+(floor(a));
if(isless(c,b)){
b=atan2(b,c);
a=log(d);
e=acos(a);
d=floor(e);
d=ceil(b);
}
while(isgreaterequal(d,e)){
a=(asin(c))-(asin(a));
d=fmax(d,c);
}
}