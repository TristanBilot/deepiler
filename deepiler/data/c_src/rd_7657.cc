#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(exp(b))+(atan2(c,a));
d=sqrt(a);
b=sqrt(e);
while(islessgreater(a,e)){
e=pow(f,e);
a=(atan2(a,a))*(pow(e,f));
a=pow(c,b);
d=(ceil(b))*(sin(d));
}
a=floor(c);
e=(atan2(c,c))*(asin(c));
a=(atan(a))+(cos(e));
f=pow(c,c);
}