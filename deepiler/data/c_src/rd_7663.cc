#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=acos(c);
a=(ceil(b))+(pow(d,a));
b=(log10(b))-(atan(c));
b=sin(d);
if(islessgreater(a,a)){
a=pow(b,d);
c=(ceil(c))/(cos(d));
a=(exp(a))+(pow(c,b));
}
if(isless(d,e)){
e=(ceil(b))-(pow(a,e));
d=(atan(b))-(fdim(a,b));
c=sqrt(e);
a=fdim(d,b);
a=tan(e);
}
else{
e=(fmin(a,e))-(exp(a));
d=fmax(a,a);
e=(atan2(d,d))-(atan2(b,e));
b=fdim(b,a);
}
}