#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(a);
g=atan(e);
c=fmax(a,h);
g=tan(d);
d=fmin(f,h);
a=(fdim(g,g))+(cos(c));
a=fmax(c,b);
e=(fdim(b,e))-(exp(f));
g=(fmin(b,e))*(pow(f,g));
c=pow(d,h);
while(isless(h,c)){
d=fmax(a,d);
c=atan(e);
b=pow(g,f);
h=atan2(h,b);
f=atan2(f,h);
}
}