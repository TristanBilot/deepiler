#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(atan2(g,a))*(pow(d,e));
g=floor(g);
e=exp(c);
f=exp(d);
if(islessequal(b,e)){
g=sin(g);
a=(exp(c))+(fdim(e,b));
a=(pow(d,b))+(fdim(b,c));
b=atan2(c,g);
}
else{
a=(atan2(d,e))-(log(f));
e=(acos(f))-(tan(g));
b=(sqrt(e))+(fmin(d,f));
c=(atan(d))-(log10(a));
}
e=acos(f);
d=floor(f);
}