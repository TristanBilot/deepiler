#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(d))*(pow(g,a));
d=asin(e);
d=floor(b);
b=sin(d);
if(isgreaterequal(b,c)){
b=(atan2(g,c))*(pow(e,e));
e=(exp(c))/(sqrt(d));
g=(acos(f))-(fdim(b,d));
}
if(islessequal(c,d)){
g=tan(g);
c=fdim(d,f);
f=(pow(b,c))/(atan2(e,d));
f=pow(f,c);
a=(exp(g))/(atan2(c,d));
}
}