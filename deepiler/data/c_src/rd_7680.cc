#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(exp(f))*(tan(c));
b=(asin(g))+(floor(h));
f=(pow(e,g))-(log(a));
e=(pow(f,e))/(atan(h));
d=fmin(b,g);
f=pow(d,f);
c=(pow(h,a))*(fmin(a,e));
b=sqrt(h);
while(isless(f,g)){
g=(cos(c))*(atan2(e,f));
d=acos(f);
c=fdim(d,e);
b=(pow(g,g))/(pow(c,g));
a=atan2(g,h);
}
}