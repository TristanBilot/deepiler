#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(sqrt(b))/(atan(a));
b=asin(c);
e=fmin(b,b);
d=(fmax(a,e))+(log10(e));
d=asin(f);
if(islessequal(c,b)){
b=(pow(c,b))+(ceil(g));
d=fmax(a,c);
}
else{
e=(tan(a))*(pow(b,g));
f=(ceil(g))*(floor(e));
b=floor(a);
}
if(isless(b,c)){
b=atan2(c,g);
f=(fdim(c,d))*(fdim(f,a));
b=sqrt(a);
}
else{
a=atan(f);
e=fdim(c,f);
a=atan2(b,g);
f=(log(a))+(pow(d,e));
e=fdim(c,d);
}
}