#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(b,c);
d=fmax(c,c);
b=sin(e);
d=exp(a);
c=ceil(b);
e=floor(e);
d=pow(e,c);
if(islessgreater(b,a)){
a=(ceil(d))/(cos(c));
e=pow(e,a);
}
else{
e=fdim(b,a);
a=(fmax(e,d))*(log(c));
d=ceil(d);
c=sin(c);
e=(atan2(d,b))*(floor(d));
}
}