#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(b,g);
d=(acos(f))-(asin(a));
b=(floor(f))*(pow(a,b));
d=fmax(b,a);
e=fmin(b,d);
f=pow(e,f);
a=pow(a,f);
while(islessequal(e,a)){
g=(pow(a,d))*(fmax(f,f));
g=(log(d))-(log(e));
a=(fmin(b,a))/(tan(d));
g=(acos(a))*(exp(e));
}
}