#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(cos(e))*(pow(d,b));
d=sin(a);
d=log10(a);
if(islessgreater(e,c)){
d=(pow(a,c))+(log10(a));
c=fdim(b,d);
e=ceil(e);
}
if(islessequal(e,c)){
b=pow(e,c);
a=(log10(a))*(cos(b));
e=(ceil(e))*(fmax(a,a));
}
}