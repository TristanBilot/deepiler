#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(tan(a))-(ceil(b));
c=(ceil(a))+(log(b));
d=fmax(c,d);
c=(acos(d))*(log(d));
c=log10(e);
while(isless(c,c)){
a=pow(c,a);
c=(log10(c))-(atan(b));
a=(fdim(b,a))/(asin(b));
a=(sin(c))+(cos(b));
}
d=(fmin(a,c))*(atan2(d,e));
d=pow(b,b);
d=sqrt(d);
d=(fdim(d,d))-(fdim(d,b));
}