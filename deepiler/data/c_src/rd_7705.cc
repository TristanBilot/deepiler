#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(a,d);
a=(pow(d,c))*(cos(a));
e=log(d);
d=pow(e,b);
d=fmin(c,d);
e=(ceil(e))/(log10(a));
if(isgreaterequal(e,d)){
e=pow(c,e);
c=pow(b,a);
e=log(d);
}
}