#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(b);
a=atan2(e,e);
e=(fdim(d,b))+(fmax(d,a));
c=log(e);
b=sqrt(c);
c=(log(e))*(floor(c));
b=(ceil(b))+(pow(a,c));
c=ceil(d);
b=(fdim(d,d))*(fdim(a,b));
a=(fmax(c,e))/(fmin(e,c));
e=(fmax(a,a))+(pow(d,e));
d=log(d);
}