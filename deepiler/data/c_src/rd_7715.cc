#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmin(e,a);
a=atan2(a,f);
f=atan(e);
d=(asin(f))*(fmin(d,d));
f=pow(b,d);
b=fmax(d,f);
e=(fdim(f,b))*(fmax(e,e));
while(isless(f,d)){
c=(fmax(e,a))-(fmin(f,c));
d=(atan2(f,d))/(fmax(e,b));
d=fmax(e,b);
e=(fmax(a,f))-(asin(d));
}
}