#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=tan(c);
a=fdim(b,a);
while(isless(d,a)){
d=pow(f,c);
f=(asin(f))*(log(c));
b=fmax(e,c);
}
e=atan2(d,c);
b=log(c);
a=(log10(a))-(cos(f));
b=(acos(a))*(log(d));
a=fmin(b,d);
}