#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=asin(c);
e=atan2(b,c);
h=pow(a,f);
d=pow(b,h);
if(islessequal(c,g)){
d=(sqrt(f))*(cos(h));
b=acos(h);
e=atan2(e,d);
e=(fmax(g,f))/(exp(f));
d=(fmax(e,h))+(fmin(e,a));
}
while(isgreaterequal(a,d)){
b=pow(a,d);
c=atan2(b,e);
}
}