#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(e,d);
b=pow(c,c);
d=floor(c);
a=(fmax(e,b))/(fmin(e,e));
a=fmin(d,a);
d=fmin(a,c);
b=(asin(b))/(fmax(e,b));
c=(atan2(b,b))*(cos(d));
b=(atan(a))-(acos(d));
c=(log10(c))*(cos(a));
d=(fmin(a,b))/(cos(b));
e=(fmax(e,b))-(atan(e));
c=(sqrt(b))*(atan2(d,c));
}