#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(g,f))*(acos(c));
c=tan(f);
b=(log(e))+(floor(g));
f=log(b);
g=(acos(b))-(fmax(d,a));
a=atan2(e,a);
c=fdim(b,b);
a=(fmin(e,a))*(floor(f));
if(isgreaterequal(d,f)){
d=floor(f);
e=asin(c);
g=tan(a);
c=(ceil(e))*(atan(b));
}
}