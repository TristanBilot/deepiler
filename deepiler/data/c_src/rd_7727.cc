#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(a,e);
e=(log10(e))-(fdim(b,f));
d=fdim(e,e);
a=fmin(d,e);
f=(fdim(e,e))/(fmin(a,a));
while(islessequal(c,d)){
f=(log10(e))/(floor(b));
g=pow(a,g);
d=asin(b);
c=(pow(f,d))-(fdim(b,f));
}
if(islessgreater(b,b)){
d=pow(b,c);
f=(floor(d))-(pow(c,f));
b=sin(g);
b=fmax(b,c);
e=(atan2(a,b))*(cos(g));
}
else{
d=(fdim(b,g))-(log10(e));
c=(atan2(g,c))-(atan(e));
a=(atan2(g,d))*(fmax(f,c));
}
}