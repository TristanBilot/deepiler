#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fdim(e,c))*(exp(c));
b=(atan2(a,c))*(exp(b));
c=(fdim(e,a))*(atan2(b,e));
b=cos(d);
d=atan(b);
while(islessgreater(e,e)){
e=(log(e))+(fmax(e,d));
a=fmax(c,e);
e=(exp(c))/(asin(a));
}
while(isgreaterequal(e,c)){
d=asin(b);
a=cos(c);
c=log(d);
d=acos(a);
}
}