#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,c))-(atan(e));
d=(fdim(c,b))-(log10(a));
if(islessequal(d,c)){
e=sin(c);
e=(atan2(e,c))*(tan(b));
c=(acos(b))+(tan(d));
}
else{
c=(pow(e,a))*(exp(e));
b=fmax(a,c);
}
if(islessgreater(c,e)){
d=(log10(a))-(log10(a));
a=(sqrt(d))+(fmin(a,a));
}
}