#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(b,f);
b=log(c);
if(islessequal(d,c)){
f=tan(e);
f=(pow(c,f))+(log10(b));
}
else{
a=acos(d);
e=(fmax(a,f))+(fmin(f,a));
}
while(isgreaterequal(b,b)){
e=floor(f);
c=sin(d);
c=(floor(a))-(fdim(d,d));
b=log(d);
}
}