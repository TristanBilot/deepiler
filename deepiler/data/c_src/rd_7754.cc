#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=log(a);
b=pow(a,d);
d=fmin(c,b);
d=(fdim(e,a))/(floor(b));
while(islessgreater(c,b)){
b=(fmin(b,b))-(pow(c,c));
c=(tan(b))-(pow(e,c));
a=tan(a);
}
while(islessgreater(a,a)){
d=(log(c))*(log(a));
a=ceil(c);
}
}