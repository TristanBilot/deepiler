#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(atan(e))-(atan(c));
c=log10(h);
e=(ceil(g))/(exp(a));
if(isless(b,b)){
f=fmin(g,g);
b=(fmin(a,c))+(fmin(e,e));
h=(sqrt(g))+(fmax(b,a));
}
b=log(f);
a=(sin(h))-(asin(d));
}