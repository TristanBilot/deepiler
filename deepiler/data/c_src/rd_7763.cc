#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(g);
f=fmax(e,g);
d=log10(a);
f=cos(a);
h=floor(e);
while(islessgreater(h,a)){
f=(tan(h))-(fmax(g,c));
g=acos(c);
b=(atan(c))/(fdim(b,f));
e=fmin(g,e);
}
}