#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(e,b))/(log10(e));
c=cos(g);
e=atan2(c,f);
e=fmax(f,a);
if(islessgreater(f,a)){
g=pow(b,g);
b=(fmin(c,g))*(tan(b));
}
h=(fmax(b,b))*(atan2(d,e));
f=exp(g);
a=fmin(g,h);
}