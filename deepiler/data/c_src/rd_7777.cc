#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(f);
c=tan(e);
d=exp(c);
b=log10(b);
g=(log(g))/(fdim(a,c));
while(islessequal(h,a)){
h=atan2(g,a);
d=atan2(d,g);
f=(log10(c))/(floor(h));
}
if(islessequal(a,a)){
g=(log(b))+(fmax(a,a));
c=exp(a);
b=exp(b);
}
}