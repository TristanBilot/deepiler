#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=atan(d);
f=sin(c);
c=(fdim(c,d))*(fdim(b,a));
e=(fmax(a,a))*(fmax(b,c));
f=fmax(b,a);
if(isless(b,a)){
f=(atan(a))/(log(c));
f=floor(a);
b=(pow(e,e))/(acos(e));
d=fdim(f,a);
c=fmax(a,f);
}
else{
b=fmin(d,e);
d=(atan(e))+(fdim(d,a));
c=(fdim(b,d))/(fmin(f,a));
b=fmin(b,f);
}
if(isless(e,c)){
e=(cos(b))-(acos(c));
f=(atan(c))*(cos(f));
d=tan(f);
c=(tan(b))+(pow(b,b));
c=pow(a,b);
}
else{
d=(fmin(e,a))/(pow(c,f));
f=(atan2(c,d))/(fdim(a,c));
b=(tan(b))*(fdim(c,c));
f=asin(b);
}
}