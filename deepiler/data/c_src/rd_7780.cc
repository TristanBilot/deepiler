#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=pow(d,c);
e=atan(a);
d=atan2(a,c);
b=fmax(e,d);
if(islessequal(c,d)){
d=(pow(a,a))/(log(c));
b=(ceil(e))*(log10(c));
}
e=asin(c);
c=(floor(d))+(log10(b));
b=log(e);
c=cos(c);
}