#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(e,c))*(atan(c));
e=(tan(g))/(atan(f));
f=(atan(b))-(log(b));
if(islessequal(c,f)){
g=cos(e);
f=atan2(b,b);
g=log(g);
b=fmax(g,e);
f=pow(e,g);
}
else{
g=(fmax(f,c))+(acos(g));
e=(pow(e,a))*(fdim(g,b));
d=cos(d);
d=(sin(d))/(fmin(g,d));
f=(exp(e))+(fmin(g,d));
}
b=atan(a);
e=atan2(e,b);
b=log10(b);
b=(fdim(g,b))/(log(c));
}