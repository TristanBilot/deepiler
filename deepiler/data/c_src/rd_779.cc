#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=exp(b);
e=(pow(b,a))*(fmax(c,a));
e=(sin(e))*(sin(b));
b=acos(b);
g=(fmax(e,f))*(acos(a));
while(islessgreater(d,g)){
c=(fmin(f,d))+(pow(d,c));
g=fmax(f,d);
}
while(isgreaterequal(f,f)){
f=floor(a);
f=(log(e))-(atan(g));
}
}