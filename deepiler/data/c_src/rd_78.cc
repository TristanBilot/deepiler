#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sin(c))*(exp(c));
e=(log10(b))*(atan(d));
b=(cos(g))/(floor(c));
d=fmax(g,e);
f=(sin(d))/(atan2(b,e));
c=(fmax(e,c))/(log(f));
d=atan2(c,g);
e=atan2(d,g);
a=(atan(b))-(atan2(f,b));
b=sin(a);
}