#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(acos(c))+(tan(e));
h=ceil(a);
if(islessgreater(g,g)){
g=(log10(e))-(fdim(f,f));
c=floor(g);
c=fmax(f,a);
a=(fmax(h,c))-(fmin(g,d));
b=asin(d);
}
if(isgreaterequal(h,h)){
e=(cos(b))/(exp(a));
g=(fmax(b,d))*(fmin(c,f));
h=atan2(f,h);
c=cos(e);
c=(fmax(d,h))/(fmin(e,a));
}
else{
h=atan2(e,h);
g=fmax(g,c);
a=pow(g,a);
d=pow(e,b);
a=(tan(h))/(cos(d));
}
}