#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(atan2(h,b))-(log(b));
e=(fdim(f,a))+(sin(g));
d=cos(e);
g=(pow(c,e))+(fmax(a,c));
f=log(e);
f=ceil(d);
c=(fmax(f,c))/(fmax(e,e));
g=(fdim(b,e))+(fdim(c,c));
if(islessgreater(b,g)){
h=(fmax(a,d))+(pow(e,b));
c=(fmin(c,c))*(log(c));
b=(floor(h))*(pow(a,c));
d=(tan(h))+(sin(g));
}
}