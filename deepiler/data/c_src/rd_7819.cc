#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(pow(g,a))*(cos(b));
f=fdim(a,g);
h=(exp(e))/(pow(a,f));
g=(cos(b))+(cos(g));
while(isless(h,a)){
h=tan(e);
d=(tan(a))/(log10(e));
c=(acos(a))+(atan2(h,f));
}
if(islessgreater(b,b)){
c=(atan2(c,d))*(pow(b,d));
c=ceil(g);
h=(fdim(a,g))+(fdim(h,c));
}
else{
c=(fmin(d,c))+(fmin(a,b));
a=(atan2(d,b))-(ceil(g));
b=atan2(d,e);
b=(atan2(g,b))+(fmin(g,a));
}
}