#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(c);
b=fmax(a,a);
while(islessgreater(f,c)){
b=(pow(d,e))/(fmax(d,h));
d=cos(d);
d=cos(g);
}
h=log(e);
a=fmin(a,c);
c=exp(f);
b=log10(h);
}