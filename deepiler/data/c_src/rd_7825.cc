#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=pow(b,a);
c=(fdim(b,e))+(fdim(c,b));
c=(fmax(f,f))*(fmax(f,e));
d=(acos(g))-(pow(e,d));
c=(pow(e,b))/(fdim(b,g));
c=(exp(e))*(acos(d));
b=(cos(b))+(fmax(e,d));
g=(pow(f,d))+(fmin(f,c));
}