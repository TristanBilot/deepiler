#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=acos(a);
c=(log10(f))*(log(c));
while(islessequal(b,b)){
b=asin(d);
f=atan2(a,e);
e=floor(f);
d=pow(d,b);
}
while(islessequal(d,b)){
f=atan2(e,a);
f=(fdim(f,d))/(log(c));
}
}