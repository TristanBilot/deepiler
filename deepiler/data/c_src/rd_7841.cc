#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(f,d);
f=(fdim(d,a))*(fdim(g,a));
c=sqrt(d);
g=(sin(d))/(fmax(b,b));
g=(tan(e))*(atan2(b,d));
if(islessgreater(c,c)){
e=(atan(b))*(log10(e));
e=(fmax(c,f))+(acos(c));
a=atan2(d,f);
c=fmin(c,b);
a=log(a);
}
else{
g=(acos(c))*(fdim(b,f));
f=atan(c);
f=fmin(a,b);
c=(floor(d))+(pow(c,f));
f=floor(c);
}
while(isgreaterequal(e,a)){
c=log(f);
f=log10(g);
e=(floor(e))-(fdim(f,f));
c=acos(c);
d=sqrt(d);
}
}