#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(ceil(a))/(fdim(d,b));
e=(log(d))+(asin(a));
f=(pow(e,c))/(atan2(c,b));
c=sin(f);
d=fmax(f,c);
f=(atan2(d,d))*(atan2(e,f));
b=(ceil(e))+(ceil(d));
if(islessequal(e,d)){
e=(log(b))*(fdim(e,c));
d=floor(a);
}
else{
f=atan2(c,f);
a=(log10(e))/(fmin(f,c));
a=(sqrt(f))/(sqrt(e));
c=sin(f);
e=log(d);
}
}