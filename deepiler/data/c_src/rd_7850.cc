#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(e,b))/(exp(b));
d=floor(c);
e=pow(c,d);
while(isless(a,b)){
d=asin(e);
d=sin(c);
}
while(islessequal(d,d)){
a=(sin(e))+(exp(a));
c=fmax(a,c);
a=(ceil(a))-(atan2(d,e));
d=atan(a);
a=pow(c,b);
}
}