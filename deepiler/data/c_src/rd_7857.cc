#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fdim(f,e))+(exp(c));
f=(fmin(e,f))*(fmin(d,c));
e=fmax(f,f);
c=tan(d);
c=(atan(b))+(asin(f));
if(islessequal(b,a)){
f=log10(f);
c=(atan(a))*(log(b));
e=(cos(f))*(pow(c,a));
a=sin(c);
}
a=(ceil(c))/(log(b));
e=(fmax(f,f))*(fdim(a,d));
a=log(a);
}