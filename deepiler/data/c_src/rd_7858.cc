#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(d,c))+(cos(b));
d=(atan2(a,b))+(pow(b,d));
c=fmax(e,c);
a=(atan2(d,c))-(cos(b));
e=fdim(c,c);
d=floor(d);
d=sqrt(d);
a=sin(b);
}