#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fdim(e,e))*(fmin(c,c));
c=cos(b);
if(islessequal(c,c)){
e=(cos(c))/(fmax(d,b));
e=(fdim(a,e))-(cos(b));
a=floor(b);
d=(atan2(a,c))-(log(a));
}
while(isless(e,d)){
c=atan2(a,b);
b=fmax(d,a);
a=pow(b,d);
d=(fmin(a,c))*(cos(b));
}
}