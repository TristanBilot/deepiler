#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(sqrt(a))+(exp(b));
c=fdim(a,h);
g=(fmin(h,g))*(exp(b));
e=(fdim(f,b))/(sqrt(g));
b=atan2(f,g);
if(isgreaterequal(g,c)){
h=(asin(d))-(pow(c,a));
f=(asin(e))+(exp(g));
a=atan(a);
}
else{
a=fmin(e,g);
h=fmin(g,g);
e=cos(b);
f=(atan2(f,b))/(fdim(a,e));
h=(floor(a))+(fmin(a,g));
}
if(isgreaterequal(d,d)){
d=pow(c,a);
d=atan(c);
d=fmin(e,g);
}
}