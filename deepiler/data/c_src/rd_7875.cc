#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmax(a,f))*(fdim(e,c));
b=log(a);
f=pow(b,e);
b=acos(a);
while(isless(a,b)){
b=fmin(a,f);
a=fdim(c,a);
b=fmax(a,f);
c=fmin(c,f);
}
f=(exp(b))*(fdim(a,d));
b=fdim(a,e);
f=(atan2(b,a))-(fmax(e,a));
}