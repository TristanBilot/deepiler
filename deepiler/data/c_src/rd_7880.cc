#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(cos(a))+(fmin(c,b));
a=log(e);
b=(floor(c))/(sqrt(b));
d=atan(c);
while(islessgreater(d,b)){
c=(sin(a))+(ceil(c));
b=log(d);
}
a=ceil(a);
a=sin(b);
d=(sin(d))/(fmin(c,c));
}