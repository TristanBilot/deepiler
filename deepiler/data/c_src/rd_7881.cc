#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan2(f,f))+(cos(d));
d=(fmax(a,d))/(floor(c));
b=sin(d);
a=fdim(a,a);
f=pow(a,b);
d=floor(e);
d=fdim(b,a);
f=log10(c);
c=(log10(a))/(fmin(d,b));
e=(atan(c))-(cos(d));
if(isless(b,f)){
b=(cos(f))/(fmin(b,a));
d=(atan2(b,a))+(asin(e));
e=(acos(f))*(fdim(b,b));
c=(fdim(c,e))-(fmax(f,b));
}
}