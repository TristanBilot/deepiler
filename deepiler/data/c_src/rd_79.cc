#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(exp(f))/(fmax(g,f));
b=(fmax(a,c))+(asin(g));
d=(tan(d))+(exp(d));
if(islessgreater(a,g)){
d=exp(f);
e=floor(b);
}
else{
a=(tan(c))/(pow(e,b));
c=sin(b);
}
b=(atan2(c,c))-(fdim(e,b));
c=(pow(a,a))-(fmax(b,a));
}