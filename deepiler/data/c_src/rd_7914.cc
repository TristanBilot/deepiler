#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(acos(a))+(tan(c));
d=(fmin(c,a))/(acos(b));
b=(log10(d))/(atan(e));
d=acos(f);
e=(log10(a))*(atan(c));
if(islessgreater(d,d)){
a=log(e);
e=floor(a);
f=cos(c);
}
if(islessgreater(d,c)){
c=pow(d,b);
d=acos(f);
f=fdim(f,f);
}
}