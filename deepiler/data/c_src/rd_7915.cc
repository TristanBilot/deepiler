#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(tan(e))*(floor(b));
b=fmax(d,f);
if(isgreaterequal(b,c)){
a=sin(e);
a=fmax(c,d);
}
if(islessequal(b,d)){
a=acos(f);
d=fmax(d,d);
}
else{
d=(sin(d))*(acos(b));
e=(fdim(a,c))-(fmax(a,d));
d=fmax(e,d);
f=(sqrt(b))+(pow(f,f));
}
}