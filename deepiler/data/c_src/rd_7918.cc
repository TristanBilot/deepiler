#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmax(f,g))/(cos(c));
f=cos(f);
d=tan(a);
d=fmax(f,g);
g=(acos(g))/(atan2(c,a));
d=fmax(g,f);
b=fmin(c,a);
e=log(b);
f=fmin(b,a);
d=fmax(g,f);
a=sin(g);
f=atan2(a,f);
}