#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmin(c,d);
b=(fmin(a,a))-(fmax(a,d));
b=(sin(e))/(fmax(c,c));
a=(sqrt(a))+(fdim(b,d));
c=(tan(a))-(acos(a));
if(islessequal(b,b)){
e=log(b);
c=asin(c);
b=(sqrt(c))-(log(b));
b=fmin(d,c);
}
else{
a=(pow(a,d))+(ceil(e));
a=(log10(b))/(fmin(a,d));
e=pow(e,e);
c=fmax(b,d);
}
e=(fdim(e,e))/(atan(e));
d=sqrt(d);
}