#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(atan2(a,e))*(atan(d));
e=(fmax(b,c))+(pow(a,e));
b=fmax(b,c);
e=exp(e);
c=(pow(d,d))*(cos(e));
c=log10(a);
c=fmax(a,c);
c=(atan2(c,b))*(atan2(e,e));
a=fmax(c,b);
a=ceil(a);
a=pow(a,c);
b=pow(e,a);
}