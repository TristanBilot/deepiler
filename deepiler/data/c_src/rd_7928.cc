#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(e,f))+(atan(a));
d=(fdim(f,a))/(fdim(b,a));
e=(fdim(f,f))*(atan2(f,c));
f=(fmax(e,b))/(asin(a));
if(islessequal(d,d)){
c=(cos(d))*(exp(b));
e=fdim(c,c);
e=(sqrt(a))*(pow(f,f));
}
}