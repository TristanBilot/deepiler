#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(c);
a=(ceil(d))+(fdim(b,a));
e=(ceil(c))+(log10(b));
if(isgreaterequal(e,b)){
a=(atan2(c,b))+(log(b));
c=fdim(e,c);
b=log(c);
a=pow(a,b);
e=log(b);
}
else{
e=fmax(d,b);
b=fdim(b,e);
c=(fmax(d,d))+(asin(c));
}
if(islessgreater(d,c)){
a=(atan(b))/(fmin(c,d));
a=fdim(d,c);
}
else{
a=(atan2(d,d))*(log10(b));
b=acos(c);
d=fmax(e,a);
e=atan2(b,b);
}
}