#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(pow(d,d))*(fmin(b,d));
c=(sqrt(b))-(asin(e));
c=(sqrt(c))*(sqrt(c));
while(isless(d,c)){
e=fmax(e,b);
b=(log10(e))+(atan2(f,b));
f=fmax(f,a);
d=(atan(b))*(fmax(f,e));
}
b=(acos(f))-(fmin(d,a));
a=(atan2(f,d))+(pow(b,d));
}