#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=fmin(c,a);
c=(sqrt(d))-(tan(b));
if(islessequal(d,a)){
d=exp(b);
b=(cos(b))-(pow(c,a));
a=(exp(a))+(atan(c));
}
else{
f=(exp(f))/(fmax(a,e));
d=acos(f);
f=sin(f);
d=(sqrt(f))/(pow(d,e));
}
a=fmax(d,f);
c=pow(e,c);
}