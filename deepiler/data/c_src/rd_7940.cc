#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fdim(d,h))-(exp(b));
b=pow(c,f);
d=(fmax(h,d))+(ceil(g));
while(islessequal(h,h)){
a=cos(f);
b=sin(c);
g=floor(a);
h=(cos(b))/(tan(h));
a=fmax(d,a);
}
h=(floor(b))/(acos(c));
h=(fdim(h,a))*(fmin(c,h));
}