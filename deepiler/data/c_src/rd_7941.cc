#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(a,d))/(atan2(b,b));
b=(acos(a))+(exp(b));
a=(fmin(c,e))/(fdim(b,c));
d=atan2(d,b);
if(isgreaterequal(b,b)){
c=floor(e);
d=(log10(a))+(fmax(c,c));
e=(fmin(c,a))/(exp(d));
}
a=fmin(a,e);
d=(atan2(c,b))-(log(e));
e=(sin(e))-(exp(e));
d=exp(d);
}