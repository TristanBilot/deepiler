#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(acos(a))-(ceil(e));
b=(exp(g))*(exp(c));
a=(fmax(d,a))/(fmax(c,g));
f=(pow(b,c))-(pow(b,f));
e=cos(a);
a=atan2(c,g);
b=atan2(g,a);
f=(fdim(d,a))*(log10(f));
while(isgreaterequal(g,f)){
e=(fmax(g,c))+(log10(e));
b=(sqrt(a))-(fdim(f,b));
g=ceil(f);
a=atan2(g,g);
c=(fmin(g,f))*(fdim(f,f));
}
}