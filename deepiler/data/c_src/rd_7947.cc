#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(floor(b))*(atan(b));
d=fdim(a,e);
a=ceil(e);
a=ceil(c);
if(isgreaterequal(c,b)){
a=(atan2(d,b))+(atan2(c,e));
b=(floor(d))+(log(b));
}
else{
d=log(e);
b=(asin(c))/(atan2(d,a));
d=fmin(a,d);
a=floor(d);
e=(log10(b))*(fmax(d,b));
}
if(islessequal(c,d)){
c=(fmin(b,c))-(log10(c));
e=log(d);
c=(fmin(d,c))/(sin(a));
}
}