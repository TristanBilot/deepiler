#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=cos(b);
g=pow(a,e);
e=log10(f);
if(islessequal(g,g)){
f=(sqrt(h))/(floor(g));
g=tan(a);
}
f=(pow(h,g))*(pow(b,b));
h=(pow(g,g))*(log10(b));
a=fmax(e,e);
d=(pow(f,f))-(fdim(d,d));
f=(exp(d))*(log(b));
}