#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(g,c);
g=(floor(e))*(exp(a));
a=(fmin(e,f))/(ceil(e));
while(isgreaterequal(d,a)){
c=sqrt(a);
a=(floor(f))+(fmax(e,e));
b=fmin(d,a);
e=(ceil(a))+(pow(a,d));
}
while(islessgreater(f,e)){
d=fmax(e,b);
b=(fmin(g,e))/(fmin(d,a));
}
}