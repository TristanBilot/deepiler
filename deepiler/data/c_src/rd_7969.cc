#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(f,c);
b=log10(b);
c=(fmax(g,b))+(log10(a));
g=ceil(e);
b=fmax(c,e);
a=(sqrt(a))*(sin(g));
h=(tan(a))+(atan(f));
if(isless(c,g)){
d=fmin(a,b);
g=acos(h);
e=fmax(b,e);
f=log10(h);
}
}