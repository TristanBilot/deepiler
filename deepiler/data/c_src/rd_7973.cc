#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(b,c))-(pow(b,d));
g=tan(h);
d=(fmax(g,f))/(cos(d));
g=acos(d);
e=(fmax(c,c))/(ceil(a));
d=(cos(e))*(floor(h));
if(isless(h,e)){
a=pow(d,d);
a=(atan2(h,c))*(sin(g));
e=log(b);
h=(fmax(a,g))-(sqrt(h));
}
else{
e=(cos(f))-(cos(e));
g=ceil(a);
c=sin(g);
}
}