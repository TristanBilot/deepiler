#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(pow(b,b))+(atan2(b,c));
d=(sqrt(g))*(asin(f));
g=(cos(d))/(asin(f));
f=(fmax(d,e))+(fmin(b,g));
e=pow(f,d);
if(islessgreater(a,e)){
a=log10(c);
c=asin(b);
}
else{
c=(fmax(f,d))+(atan2(b,f));
c=ceil(b);
}
}