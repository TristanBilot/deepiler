#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(e,c);
f=log10(h);
if(isless(f,e)){
a=(fmax(e,c))*(fmin(d,e));
c=pow(a,f);
}
else{
a=(exp(b))/(log(g));
e=(asin(g))+(pow(f,e));
d=fmax(b,f);
a=(floor(g))-(fmax(c,e));
}
c=(fmax(g,e))+(ceil(g));
g=(log(e))/(atan(e));
a=pow(d,b);
h=(ceil(b))/(atan(e));
f=sin(d);
}