#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=cos(d);
a=log(c);
f=(log10(e))/(atan(b));
e=log(f);
d=(pow(a,b))-(log10(c));
d=sqrt(b);
f=sin(c);
f=exp(a);
b=(pow(e,c))-(fdim(f,b));
f=(log(b))*(atan2(f,b));
d=cos(c);
d=(ceil(e))-(ceil(f));
e=(cos(b))+(sqrt(e));
}