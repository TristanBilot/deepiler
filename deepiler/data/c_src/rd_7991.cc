#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmax(d,b);
g=(atan2(b,b))/(fdim(f,b));
b=exp(d);
d=fdim(a,e);
a=(fmax(e,b))+(fmax(b,d));
d=atan2(f,d);
f=fdim(b,c);
e=(floor(b))+(pow(f,f));
if(islessequal(f,e)){
e=(pow(c,b))/(fdim(d,e));
b=exp(g);
c=(exp(b))-(log10(e));
}
}