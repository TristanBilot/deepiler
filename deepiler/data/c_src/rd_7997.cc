#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(sin(d))*(pow(b,a));
e=fmax(c,c);
e=(exp(c))-(acos(a));
while(isless(e,b)){
d=(fdim(b,e))+(fmax(d,d));
c=(log10(b))+(fmax(d,c));
b=(fmax(d,a))*(fmax(b,d));
d=cos(c);
e=(fmax(e,d))*(atan2(d,c));
}
if(isless(a,c)){
c=(atan2(a,e))-(pow(a,b));
b=(exp(c))/(fmax(d,e));
c=(exp(b))/(fmax(b,a));
e=(fmin(b,e))-(fmax(c,b));
b=log(e);
}
else{
d=atan(b);
a=atan2(a,a);
}
}