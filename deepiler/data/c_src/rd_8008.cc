#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(tan(b))/(exp(e));
b=(sin(a))+(ceil(b));
e=(pow(c,e))+(fdim(b,d));
b=(atan(b))+(pow(e,e));
d=fdim(e,d);
while(islessequal(e,a)){
a=(atan2(d,c))-(log(b));
b=(sin(a))*(fmax(e,a));
}
while(islessgreater(e,d)){
b=(log(d))/(floor(c));
e=(atan2(c,a))+(cos(d));
}
}