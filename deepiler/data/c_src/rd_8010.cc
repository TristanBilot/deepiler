#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(floor(g))-(sin(a));
h=(fmin(a,c))*(pow(b,a));
c=log(b);
e=(pow(e,g))-(asin(h));
if(islessgreater(d,g)){
f=ceil(e);
d=(acos(h))/(atan2(c,g));
}
if(isgreaterequal(a,d)){
a=(fmax(c,d))/(atan(d));
e=(asin(d))+(pow(g,h));
c=asin(f);
a=sqrt(c);
}
else{
c=ceil(f);
b=(asin(f))/(acos(b));
e=(atan(g))/(log10(a));
c=log(h);
b=atan2(c,c);
}
}