#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(d,c))/(cos(b));
b=sin(a);
if(islessequal(b,c)){
c=(fdim(e,b))/(sin(e));
d=pow(a,d);
e=(cos(c))+(acos(c));
}
b=(asin(c))*(atan2(a,c));
a=acos(d);
e=(cos(e))+(cos(c));
c=(atan2(d,a))*(sin(d));
a=(sqrt(c))/(pow(d,e));
}