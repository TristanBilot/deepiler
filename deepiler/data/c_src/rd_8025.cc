#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(exp(b))/(sqrt(a));
d=tan(b);
while(isless(d,e)){
c=(fmax(c,g))+(pow(b,b));
d=fmax(e,b);
b=pow(g,a);
c=(floor(a))*(tan(g));
e=(floor(e))-(asin(a));
}
if(isless(a,d)){
e=atan2(g,f);
g=(pow(a,a))-(ceil(d));
}
else{
e=(pow(c,e))*(fmax(f,d));
a=(fmin(e,g))*(fdim(e,d));
}
}