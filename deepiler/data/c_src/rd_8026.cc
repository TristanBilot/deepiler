#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(cos(a))*(acos(a));
d=exp(a);
c=(atan2(b,d))*(acos(c));
e=(fdim(c,a))*(fdim(e,e));
d=ceil(a);
a=(tan(d))+(atan2(b,b));
e=(floor(e))-(fdim(b,e));
c=log10(d);
c=fmin(e,d);
b=fmax(e,d);
}