#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=log(d);
f=(atan(e))/(log10(b));
b=tan(d);
f=(fmin(f,b))-(fmin(e,f));
c=log10(e);
c=(fmax(a,a))-(pow(b,c));
d=fmax(a,d);
a=(pow(a,f))/(atan2(c,a));
if(isgreaterequal(f,b)){
a=(fmin(b,f))*(tan(b));
c=(atan2(e,d))/(atan2(d,a));
d=(atan(a))*(pow(d,c));
a=(log(b))-(atan2(d,a));
d=atan(d);
}
else{
b=sqrt(b);
b=(fdim(d,e))/(fdim(f,a));
c=fdim(e,e);
f=(acos(b))+(fmax(a,f));
a=floor(c);
}
}