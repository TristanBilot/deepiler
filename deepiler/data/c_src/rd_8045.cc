#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(exp(a))+(sin(c));
d=sin(d);
while(isless(a,e)){
d=(sin(b))*(atan2(c,e));
a=log10(c);
c=(fmin(c,a))/(atan(d));
b=atan(d);
}
while(islessgreater(c,b)){
a=fmax(d,b);
b=(log(e))*(cos(e));
}
}