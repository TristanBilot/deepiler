#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(b,e))+(asin(c));
b=pow(c,c);
e=(fmin(a,d))+(floor(c));
e=(pow(b,e))/(fdim(d,b));
c=(tan(c))+(pow(b,b));
e=(pow(a,e))+(sqrt(a));
a=(exp(a))+(cos(b));
a=(fmax(c,b))+(cos(c));
if(isless(c,b)){
a=(ceil(b))/(floor(b));
a=(atan(e))/(cos(c));
}
else{
e=(atan2(c,a))*(pow(b,c));
e=(fmax(e,a))+(sqrt(b));
c=(acos(c))-(fdim(c,b));
}
}