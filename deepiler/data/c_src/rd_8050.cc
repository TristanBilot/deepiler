#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(sqrt(c))+(exp(d));
d=(pow(g,h))+(pow(b,a));
c=(tan(d))-(pow(a,e));
e=floor(b);
if(islessequal(f,h)){
c=cos(h);
e=(log10(e))/(atan2(a,h));
f=(pow(g,a))*(fdim(e,e));
}
if(islessequal(e,c)){
h=(pow(c,f))/(acos(h));
b=(atan2(f,g))/(fmin(g,e));
}
else{
h=atan2(c,a);
d=(sin(a))*(fmin(c,a));
h=(atan2(d,g))-(sin(d));
}
}