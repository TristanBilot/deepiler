#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(a,d);
b=asin(c);
a=atan2(a,d);
while(isless(c,b)){
e=pow(a,c);
e=atan(e);
}
c=tan(d);
a=(fmin(e,c))/(atan(b));
}