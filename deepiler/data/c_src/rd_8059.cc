#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=asin(a);
a=(log10(d))-(fmin(e,d));
if(isgreaterequal(b,e)){
e=ceil(d);
b=atan(c);
}
else{
e=(fdim(a,d))+(cos(c));
a=(pow(e,a))*(ceil(d));
}
while(islessequal(b,b)){
e=(fdim(b,b))/(pow(e,e));
d=atan(e);
}
}