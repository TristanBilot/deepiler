#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fmin(a,e);
b=(fmax(f,b))/(sqrt(a));
a=sqrt(c);
b=(atan2(a,a))/(sin(e));
a=(acos(c))/(pow(e,f));
f=(pow(c,e))+(atan(a));
a=floor(f);
a=(fmax(b,d))-(atan(d));
if(isless(f,e)){
f=(fmax(c,e))/(atan(d));
a=(log(e))/(floor(e));
c=(exp(f))+(fmin(c,a));
a=fdim(c,b);
}
}