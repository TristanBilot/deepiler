#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(asin(b))/(log10(c));
c=atan2(e,c);
e=fmin(e,c);
e=(atan2(e,b))/(fmin(d,c));
b=(fmax(d,b))+(atan2(d,c));
a=(fdim(a,a))*(fmax(c,b));
c=(acos(a))*(fmax(e,a));
a=(atan(a))-(log(e));
e=sin(c);
if(islessgreater(e,a)){
e=pow(c,e);
c=atan2(d,d);
a=(pow(a,e))+(asin(d));
d=(ceil(c))/(atan2(a,b));
e=(exp(b))+(cos(a));
}
else{
e=fmin(e,b);
a=sqrt(e);
a=(asin(e))/(fmax(b,d));
}
}