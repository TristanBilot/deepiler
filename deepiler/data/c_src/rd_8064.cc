#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(a,a);
a=log10(e);
a=(ceil(d))*(pow(e,d));
d=(atan2(b,c))-(atan2(e,a));
b=(fmax(c,e))*(fmax(c,e));
while(isgreaterequal(e,c)){
c=(pow(c,c))+(atan2(b,e));
c=asin(c);
d=(log10(d))-(fmin(d,c));
d=atan2(e,e);
}
}