#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(a,c);
b=(fmax(e,b))/(acos(b));
a=tan(c);
d=(asin(e))-(fmin(d,d));
e=atan(a);
while(islessequal(d,c)){
d=atan2(b,d);
a=fmax(e,d);
b=pow(c,a);
e=(atan2(c,d))+(ceil(e));
}
}