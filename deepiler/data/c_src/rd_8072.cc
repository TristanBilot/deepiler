#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=floor(c);
a=(cos(c))+(fmax(d,c));
d=fmax(e,c);
d=(log10(c))/(atan2(a,e));
d=(pow(e,b))*(cos(b));
if(islessgreater(d,e)){
b=(floor(b))*(log(b));
e=atan2(d,b);
c=fmax(e,a);
b=(atan2(b,d))+(pow(b,e));
b=(ceil(b))/(fmax(d,e));
}
a=(acos(d))-(acos(e));
e=(fmax(a,d))*(log(a));
d=ceil(d);
a=(log10(c))-(fdim(a,a));
}