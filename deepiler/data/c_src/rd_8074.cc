#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(sqrt(d))/(atan2(a,a));
d=pow(c,f);
b=tan(g);
f=acos(f);
a=(fmin(b,d))+(fmin(a,d));
c=(fmax(d,f))*(log10(f));
f=exp(c);
g=(pow(e,g))-(sqrt(e));
d=log(b);
c=log10(b);
b=sqrt(d);
}