#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(g,a);
d=atan2(a,f);
while(isless(a,f)){
g=pow(c,c);
c=(exp(d))*(atan2(d,c));
}
e=(tan(a))+(fmin(d,e));
h=(tan(a))-(sqrt(c));
c=(fdim(f,b))+(atan2(d,b));
f=pow(b,b);
}