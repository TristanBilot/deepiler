#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(a,c);
d=cos(a);
while(isless(b,d)){
c=(fmin(b,d))-(pow(a,c));
d=atan2(b,b);
a=pow(e,b);
}
while(islessgreater(c,e)){
e=floor(c);
e=(fdim(e,c))/(ceil(a));
}
}