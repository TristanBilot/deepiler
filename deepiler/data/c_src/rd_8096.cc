#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=log10(e);
b=fmax(d,a);
d=(exp(f))/(fmin(a,a));
while(islessequal(d,c)){
c=(exp(f))+(cos(c));
c=acos(e);
e=(fmax(c,b))/(pow(c,c));
b=fmax(b,b);
}
if(islessequal(a,a)){
a=(sin(f))+(atan2(d,d));
e=atan2(f,b);
}
else{
c=fmin(e,f);
b=fmin(d,c);
a=fdim(f,c);
e=(fmax(e,d))+(pow(f,a));
c=(fmin(e,f))-(log(e));
}
}