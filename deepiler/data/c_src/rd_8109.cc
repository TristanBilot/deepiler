#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log10(a))+(exp(b));
c=(fmin(e,g))-(log10(d));
while(islessequal(e,e)){
b=fmax(b,g);
d=(fdim(e,d))+(pow(e,c));
e=(atan2(g,c))*(fmax(e,d));
}
if(islessequal(b,c)){
e=pow(e,c);
e=sin(e);
}
else{
b=(exp(g))/(cos(a));
a=fdim(c,a);
}
}