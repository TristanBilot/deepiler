#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmin(c,d);
g=atan(f);
while(islessequal(g,f)){
e=fmax(g,d);
d=log10(f);
}
b=fmin(a,c);
b=pow(d,b);
c=atan(d);
d=log(e);
a=floor(c);
}