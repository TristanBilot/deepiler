#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(c);
g=log(g);
c=pow(f,c);
d=atan2(f,f);
a=fmin(a,g);
f=(exp(b))-(pow(b,h));
f=(log(h))/(pow(b,e));
b=atan2(c,d);
a=fmin(a,g);
h=pow(d,f);
while(isless(d,a)){
c=(fmin(h,h))+(log10(d));
h=exp(g);
a=(sqrt(a))-(log(h));
e=(atan2(g,d))/(atan2(d,h));
c=(tan(h))*(ceil(b));
}
}