#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=tan(b);
a=(fmin(e,e))/(fmin(d,c));
e=atan(d);
while(isless(b,a)){
e=(fmax(a,a))/(log10(b));
b=(atan(c))+(tan(c));
d=(fmax(d,d))+(fdim(d,b));
}
if(islessgreater(b,c)){
d=fmax(c,e);
f=(fmax(f,a))-(exp(c));
b=log10(c);
}
}