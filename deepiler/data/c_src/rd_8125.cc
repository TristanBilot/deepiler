#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fmin(b,a);
a=fdim(c,b);
g=(fdim(e,f))*(floor(c));
a=sqrt(a);
b=fdim(a,c);
a=sin(f);
d=atan(e);
if(isgreaterequal(d,f)){
g=fmin(a,a);
g=(ceil(d))*(exp(d));
}
}