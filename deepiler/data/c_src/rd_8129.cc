#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=pow(e,c);
f=(ceil(b))+(log(d));
b=(fmax(a,b))/(fmin(e,b));
d=acos(c);
if(islessequal(a,f)){
b=(sqrt(b))*(fmin(b,d));
c=exp(c);
e=(pow(e,d))+(fdim(b,f));
}
else{
a=(log10(c))+(fdim(a,e));
a=(floor(a))*(sin(a));
a=sin(c);
}
while(islessgreater(a,e)){
b=(log(e))/(cos(c));
f=(log(f))+(cos(e));
}
}