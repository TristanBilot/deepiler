#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(b,b);
e=fmax(b,c);
a=atan2(c,b);
c=fmax(a,a);
if(islessequal(a,c)){
a=(pow(c,b))/(acos(b));
b=exp(e);
d=(atan(a))+(cos(c));
b=pow(d,b);
}
else{
c=(atan(d))+(fdim(d,a));
a=(exp(b))+(acos(a));
a=(floor(c))/(pow(b,c));
}
while(islessequal(a,b)){
b=(atan2(c,d))*(fdim(b,e));
a=(pow(d,c))-(log10(a));
e=(pow(a,e))/(ceil(a));
}
}