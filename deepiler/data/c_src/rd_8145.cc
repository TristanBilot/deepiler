#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(h);
e=tan(g);
h=(atan2(a,g))*(asin(e));
h=pow(c,e);
h=fmax(g,c);
a=pow(d,c);
d=(exp(a))*(log10(c));
c=(fdim(g,h))-(acos(b));
}