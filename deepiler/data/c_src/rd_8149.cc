#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(d,f))+(fdim(f,c));
d=(fmax(e,b))-(pow(e,f));
e=(ceil(d))/(fmin(g,c));
c=(fmin(g,d))*(atan2(e,d));
a=atan(c);
while(islessequal(d,e)){
a=(log(a))+(acos(c));
a=(ceil(f))*(fdim(e,f));
f=(fmax(d,a))/(fdim(b,a));
}
c=(fmax(d,g))*(atan2(g,e));
e=(fdim(c,e))*(floor(f));
b=(atan2(e,d))+(sqrt(f));
c=(cos(a))/(fmax(f,a));
g=fmax(g,a);
}