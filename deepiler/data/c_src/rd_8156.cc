#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fdim(a,b);
e=(atan(a))*(sin(d));
e=fmax(e,b);
d=(exp(e))*(fmax(d,c));
e=sqrt(c);
while(isgreaterequal(b,e)){
d=atan(b);
e=(log(a))+(fdim(c,b));
a=(atan2(e,b))-(cos(c));
}
if(isgreaterequal(e,d)){
d=(fdim(c,e))-(cos(e));
d=fdim(c,e);
e=acos(d);
}
else{
e=(sin(a))*(fdim(c,c));
b=(fmin(a,a))/(floor(e));
}
}