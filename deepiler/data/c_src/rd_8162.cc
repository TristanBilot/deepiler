#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=atan2(g,a);
g=tan(d);
a=(fmin(e,f))+(fmin(f,c));
g=(fdim(g,e))/(log(g));
d=floor(b);
while(islessequal(f,a)){
f=atan2(f,d);
f=(fmax(b,d))+(fmin(c,g));
b=(sqrt(b))*(pow(e,g));
a=atan2(a,g);
}
while(islessgreater(d,f)){
e=(fdim(f,a))+(atan2(g,e));
c=(log(d))/(ceil(c));
a=log10(f);
}
}