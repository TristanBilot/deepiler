#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sqrt(a))*(log(b));
e=acos(b);
a=(cos(c))-(atan(e));
while(isgreaterequal(c,e)){
d=fmax(b,a);
d=atan2(d,d);
d=(fmax(b,b))/(pow(e,b));
e=acos(e);
}
if(isless(b,a)){
a=pow(b,e);
a=(sin(c))+(pow(a,c));
d=fmin(d,d);
b=(exp(d))+(exp(e));
b=fdim(d,b);
}
else{
a=(asin(d))*(atan(a));
e=(atan2(c,d))*(fdim(e,c));
c=cos(b);
b=floor(b);
d=(atan2(b,b))*(fmin(b,d));
}
}