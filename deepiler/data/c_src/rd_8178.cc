#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(atan2(e,e))-(fdim(c,c));
e=floor(b);
a=(pow(c,b))+(log10(a));
b=(fdim(a,c))/(sin(b));
if(isgreaterequal(a,d)){
e=(fdim(c,d))+(pow(c,b));
c=cos(b);
}
else{
b=(atan(d))/(fmin(b,a));
c=(fdim(e,e))/(atan2(e,a));
d=pow(d,d);
d=(pow(c,a))+(sqrt(d));
}
d=pow(c,a);
b=fmax(b,b);
b=sin(b);
}