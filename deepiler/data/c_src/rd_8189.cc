#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=atan2(a,e);
g=ceil(e);
e=fmax(e,d);
d=(fmax(c,f))+(log(c));
c=atan2(f,f);
while(isless(b,c)){
a=(exp(c))+(atan2(d,c));
b=asin(c);
c=(sqrt(d))+(atan2(g,c));
f=fdim(e,g);
d=(exp(c))+(fmin(g,a));
}
a=(acos(b))+(fdim(a,a));
c=(floor(e))-(pow(c,d));
c=(atan2(e,d))/(exp(a));
a=(atan(d))-(fdim(d,d));
g=asin(d);
}