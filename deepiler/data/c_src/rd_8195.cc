#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=cos(c);
a=(sin(a))-(fdim(b,a));
e=log(b);
b=(fdim(a,e))+(cos(b));
b=(sin(d))/(fmax(e,b));
while(islessgreater(e,b)){
c=exp(d);
e=log10(a);
b=sin(e);
c=(exp(e))*(log(d));
}
while(isgreaterequal(c,b)){
d=(fdim(e,d))/(acos(a));
c=atan(b);
}
}