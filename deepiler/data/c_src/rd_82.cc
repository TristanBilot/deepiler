#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(a,d))+(atan(d));
e=exp(d);
e=fmax(e,b);
c=fdim(c,a);
while(isgreaterequal(c,b)){
a=fmax(c,c);
b=(atan2(d,d))+(log10(c));
d=(fmax(a,a))+(sqrt(a));
}
while(islessequal(b,e)){
e=(fdim(c,a))*(log10(b));
d=ceil(d);
a=sin(b);
d=ceil(a);
}
}