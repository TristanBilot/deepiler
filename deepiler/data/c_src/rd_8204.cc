#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan2(f,b))/(atan2(d,e));
b=log10(d);
c=atan2(e,e);
d=fdim(f,e);
while(isgreaterequal(b,g)){
c=(atan2(g,f))/(log10(c));
e=(atan2(c,d))-(sqrt(b));
d=(log10(c))*(pow(e,a));
a=(sqrt(f))*(asin(e));
}
d=fmin(d,a);
g=(exp(b))*(atan2(b,g));
f=(exp(e))*(atan2(d,a));
e=fmax(g,g);
a=fdim(d,e);
}