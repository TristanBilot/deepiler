#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=pow(b,c);
b=(ceil(e))+(atan(c));
c=fmin(c,c);
b=tan(a);
if(isgreaterequal(b,b)){
b=fmin(b,a);
b=(acos(d))*(atan2(c,c));
e=(atan2(a,d))*(ceil(a));
b=(atan2(a,a))+(atan(a));
e=floor(c);
}
else{
c=(acos(a))*(atan2(c,e));
c=(ceil(e))*(atan2(d,b));
d=fmin(b,b);
d=fmax(d,a);
b=exp(a);
}
while(isless(d,b)){
b=(atan2(e,a))+(log(e));
e=atan2(c,a);
b=(fmin(a,d))/(pow(a,d));
}
}