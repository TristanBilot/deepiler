#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(e,b))/(tan(g));
h=tan(h);
h=fdim(c,a);
b=(pow(a,f))*(atan2(g,h));
c=(cos(a))/(sqrt(h));
d=(log(h))*(atan2(e,g));
d=(atan2(d,f))/(fmax(b,f));
a=(log10(g))/(fmin(c,a));
if(islessequal(b,b)){
d=cos(h);
a=(fmin(a,d))/(fmax(e,b));
b=fmax(h,e);
}
else{
e=ceil(h);
f=fmin(g,a);
a=fmin(g,b);
a=acos(h);
c=(sin(f))/(atan2(f,b));
}
}