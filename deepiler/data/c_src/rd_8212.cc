#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=atan2(c,d);
d=fdim(d,b);
e=exp(e);
b=(floor(b))*(exp(e));
c=fmax(d,c);
a=fmin(b,c);
d=tan(e);
c=pow(c,b);
if(isgreaterequal(b,b)){
b=sin(d);
a=(sqrt(a))/(atan2(b,c));
a=(asin(d))*(sin(a));
b=(acos(a))*(tan(b));
e=tan(c);
}
}