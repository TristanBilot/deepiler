#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=acos(e);
c=cos(e);
if(isless(e,e)){
d=(atan2(d,f))+(fmin(a,c));
f=(exp(d))-(ceil(d));
a=(asin(c))-(pow(f,f));
}
if(isless(b,f)){
b=atan2(c,f);
f=acos(c);
a=fmin(a,d);
}
}