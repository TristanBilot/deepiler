#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(c,c);
d=fmin(e,b);
c=(pow(c,d))+(fdim(b,d));
b=asin(b);
e=log10(b);
while(isless(a,c)){
e=sqrt(a);
e=(fdim(d,c))+(atan(a));
c=(atan2(a,a))+(sin(d));
}
b=log(e);
a=fdim(e,c);
}