#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(e);
d=(fmax(d,a))-(atan2(a,c));
e=(log10(d))/(acos(c));
d=(cos(d))/(fmin(e,c));
if(isless(d,d)){
a=acos(e);
b=(cos(d))-(fmin(a,b));
b=atan(c);
b=sqrt(c);
}
else{
d=pow(d,b);
c=(pow(d,e))/(asin(b));
}
a=(floor(a))*(fmax(d,b));
a=(acos(c))-(atan(b));
b=pow(b,c);
}