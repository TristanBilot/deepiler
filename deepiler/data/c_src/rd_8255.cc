#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fmin(c,f))/(exp(g));
d=pow(g,h);
e=(fmax(e,g))+(sin(e));
a=(tan(c))/(sin(e));
e=(fmax(b,e))-(tan(e));
while(islessequal(a,g)){
h=cos(a);
c=asin(g);
b=fmax(e,d);
}
if(islessequal(d,b)){
d=fdim(h,g);
d=(tan(d))+(exp(a));
e=atan(h);
}
else{
b=(fmin(e,a))-(atan2(b,e));
h=fmin(c,g);
}
}