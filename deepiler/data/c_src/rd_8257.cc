#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(c,d);
d=sqrt(f);
g=fmax(f,c);
c=fmax(d,b);
d=(pow(c,e))-(exp(a));
while(isgreaterequal(e,c)){
e=(sin(f))*(fmax(e,e));
h=acos(a);
c=pow(b,c);
a=tan(a);
}
d=sin(f);
e=log10(b);
g=pow(f,g);
b=(floor(a))*(fmin(e,d));
}