#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(e,a);
a=atan2(c,e);
if(islessgreater(a,c)){
b=(exp(a))/(pow(b,d));
b=atan2(e,a);
}
c=(fmax(b,c))/(fmax(e,a));
a=atan2(b,c);
e=exp(c);
d=(atan2(d,d))+(log(d));
}