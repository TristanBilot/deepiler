#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(a);
e=fmin(c,b);
a=cos(b);
if(islessequal(b,b)){
e=sin(a);
e=(fmax(c,c))*(fdim(d,b));
}
a=(atan2(c,d))-(atan2(a,a));
c=fmax(b,b);
a=(fmax(b,e))*(fdim(c,a));
a=pow(a,e);
}