#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=atan(b);
g=log(g);
e=(fmin(a,c))*(cos(f));
g=(acos(a))/(fdim(g,a));
f=ceil(f);
f=(fmin(g,f))-(pow(e,d));
c=acos(f);
e=sin(g);
d=(ceil(g))+(sqrt(e));
}