#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(atan(c))-(cos(a));
b=(cos(c))-(cos(f));
f=fdim(e,f);
f=(log10(f))-(atan(f));
c=pow(e,c);
if(islessgreater(a,b)){
d=(fdim(c,d))/(atan(a));
e=(log10(a))+(tan(b));
c=(atan(a))+(fdim(b,d));
f=fmax(e,a);
}
else{
f=(acos(a))*(sqrt(b));
c=(pow(d,d))+(atan(f));
e=(fmax(d,e))/(atan2(c,d));
d=fdim(c,a);
c=fmax(a,c);
}
if(isless(c,f)){
c=cos(b);
e=fmin(f,b);
d=(sin(b))/(log(f));
b=asin(f);
}
else{
c=floor(b);
c=(fdim(b,a))/(fmin(b,a));
}
}