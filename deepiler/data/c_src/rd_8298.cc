#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=exp(b);
b=(sin(c))/(pow(d,c));
b=(exp(d))*(floor(a));
if(islessequal(a,d)){
a=(fmin(b,b))/(fmax(e,a));
a=asin(b);
}
while(islessequal(e,d)){
e=sin(d);
e=floor(c);
d=fdim(c,d);
d=(sin(b))-(tan(e));
}
}