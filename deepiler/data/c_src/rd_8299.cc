#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=fdim(c,d);
a=(fdim(a,b))-(atan2(d,b));
a=fdim(f,c);
e=pow(c,e);
a=(fmin(f,f))/(fmin(f,c));
c=(fmin(e,b))*(pow(c,b));
a=fdim(e,f);
a=fmax(a,e);
e=(fmax(e,a))/(log10(d));
}