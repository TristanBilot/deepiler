#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan(e);
b=ceil(b);
b=asin(a);
while(isless(c,g)){
e=(exp(c))/(tan(a));
a=ceil(g);
f=fdim(f,g);
d=ceil(e);
}
while(islessgreater(d,b)){
d=(pow(c,f))/(log(e));
b=(floor(g))*(pow(e,c));
d=(floor(g))/(cos(f));
e=log(b);
}
}