#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(h,a);
e=atan2(e,f);
d=pow(b,a);
a=(fmax(f,h))-(asin(e));
f=(sqrt(d))/(fmin(c,f));
b=(atan2(b,g))/(fdim(d,b));
c=exp(d);
g=(asin(b))/(log10(a));
b=tan(g);
c=(atan2(h,b))+(log(g));
h=(sin(f))/(pow(b,g));
e=(sqrt(c))+(fdim(g,a));
}