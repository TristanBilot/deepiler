#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(e,g);
c=(log10(h))+(floor(d));
f=(atan(h))+(fmin(f,a));
f=log(c);
g=(log(g))*(fmax(f,c));
a=sin(e);
h=fmax(b,c);
while(isless(g,b)){
e=fdim(f,a);
c=atan(d);
a=(pow(h,g))*(sin(h));
}
}