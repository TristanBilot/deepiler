#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmax(d,b))*(pow(b,b));
a=fmax(a,e);
e=asin(a);
a=(exp(c))+(fdim(d,a));
e=(fmax(d,a))+(fmin(d,c));
while(islessequal(c,a)){
a=(pow(c,b))*(log(b));
b=cos(c);
}
while(islessequal(d,d)){
c=fmax(b,d);
a=exp(e);
a=pow(b,a);
b=fmin(c,a);
b=atan2(b,e);
}
}