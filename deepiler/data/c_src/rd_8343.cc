#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(fdim(g,a))-(fmax(g,b));
g=(fmin(f,h))*(log(a));
e=(fdim(a,b))*(tan(d));
e=(acos(a))+(cos(c));
if(islessequal(f,e)){
b=log(h);
d=asin(a);
f=pow(f,a);
a=atan2(c,e);
h=fdim(h,h);
}
else{
h=atan2(a,g);
b=(fdim(f,c))-(tan(e));
}
if(isless(h,g)){
a=(atan2(f,e))+(fdim(e,c));
c=acos(e);
f=(pow(c,d))*(sin(f));
a=(pow(f,g))/(fdim(d,f));
}
else{
f=(atan2(h,g))*(fmax(g,b));
a=fmax(c,b);
a=acos(f);
f=(tan(f))*(fdim(h,b));
}
}