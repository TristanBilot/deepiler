#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=tan(a);
d=fdim(a,e);
b=(cos(f))+(fmin(d,e));
while(isless(g,g)){
f=atan2(g,g);
d=(fdim(b,g))*(fmax(c,e));
c=fdim(b,e);
}
if(islessequal(c,f)){
f=sqrt(e);
a=(sin(c))/(asin(d));
g=atan(d);
g=(atan2(b,b))/(sqrt(c));
}
}