#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fmax(h,f))*(fdim(c,b));
a=fmax(a,a);
d=asin(a);
c=(cos(h))/(atan(g));
g=(fdim(b,a))+(sin(h));
c=fdim(b,e);
e=(fmax(h,d))*(fmin(a,d));
d=fdim(f,d);
b=fdim(h,b);
c=log(b);
e=fmin(c,b);
h=fdim(c,f);
h=(fmin(a,e))*(acos(c));
}