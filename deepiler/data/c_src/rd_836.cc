#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(sin(g))+(atan2(g,b));
b=cos(e);
d=(fmax(b,d))*(asin(h));
f=(floor(g))+(asin(b));
f=pow(c,h);
while(isless(d,f)){
e=(fdim(h,a))/(pow(h,f));
d=fmin(g,a);
e=(atan2(e,d))-(acos(d));
b=fmax(f,d);
g=fmax(e,h);
}
}