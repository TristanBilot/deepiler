#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fdim(c,d);
c=asin(c);
if(isgreaterequal(d,c)){
e=(log10(d))*(pow(a,c));
e=floor(e);
e=exp(a);
c=(atan2(b,b))/(fmin(d,b));
a=(fmin(e,e))-(fmax(c,d));
}
e=log10(e);
e=fdim(c,b);
b=fmax(e,e);
e=(sqrt(b))-(fmax(a,e));
}