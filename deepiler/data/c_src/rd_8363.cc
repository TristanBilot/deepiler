#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fdim(a,d))/(fdim(b,b));
b=(cos(b))+(log(c));
while(islessgreater(e,d)){
a=(log(d))-(fmax(c,e));
b=(fmax(c,c))*(fmax(e,a));
a=pow(e,a);
a=sin(e);
}
e=acos(d);
c=fdim(c,d);
}