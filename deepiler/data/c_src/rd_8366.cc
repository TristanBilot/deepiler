#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sqrt(g))/(atan2(b,d));
e=log(e);
b=fdim(g,f);
f=fmin(e,c);
if(isless(a,b)){
b=(pow(a,e))+(sqrt(c));
d=fmin(d,e);
}
e=(asin(d))-(log10(d));
e=(fdim(d,f))/(atan(b));
a=(pow(e,c))-(fmax(a,c));
}