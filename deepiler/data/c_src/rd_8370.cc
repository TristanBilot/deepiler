#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log(b);
d=fmax(g,c);
a=pow(a,b);
f=(log10(c))/(pow(a,e));
g=(log(d))+(log10(d));
if(isgreaterequal(c,f)){
f=log10(c);
d=(atan2(e,e))-(acos(d));
d=(fmin(e,b))*(fmax(b,e));
}
d=atan(b);
g=sin(e);
}