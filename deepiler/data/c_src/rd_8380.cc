#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(floor(d))-(fmax(b,c));
g=(sin(a))-(exp(b));
f=(acos(d))+(acos(e));
g=cos(c);
a=(asin(c))+(fdim(f,b));
c=(fdim(d,a))-(floor(a));
while(isgreaterequal(c,e)){
a=(exp(c))+(atan2(f,c));
b=atan(e);
e=log(b);
a=(fdim(b,d))+(pow(c,g));
}
}