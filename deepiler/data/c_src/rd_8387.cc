#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=sin(e);
a=log10(e);
a=acos(d);
a=asin(b);
e=(pow(a,f))*(cos(a));
b=(sqrt(f))*(sin(f));
d=ceil(d);
f=(fmax(a,f))+(tan(f));
while(isgreaterequal(e,f)){
e=(fmax(d,b))-(atan(e));
a=(fdim(d,e))+(fmax(c,a));
}
}