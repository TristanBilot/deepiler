#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(fmin(e,b))-(exp(a));
c=ceil(d);
if(islessequal(e,f)){
b=pow(d,f);
a=(fmax(a,f))/(pow(a,b));
f=(fmin(e,d))/(fmin(b,c));
e=(sin(e))+(acos(a));
a=(tan(f))*(fmin(b,d));
}
else{
c=acos(f);
c=(pow(a,e))+(fmin(b,b));
c=fmax(d,d);
b=sqrt(a);
f=(fdim(c,f))/(sin(c));
}
f=(tan(b))+(fdim(e,e));
b=fmin(c,e);
d=sqrt(c);
}