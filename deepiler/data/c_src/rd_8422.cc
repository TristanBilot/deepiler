#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,g);
h=(sin(a))*(pow(g,a));
a=(fmin(c,a))*(fmin(g,f));
g=(tan(a))+(atan2(e,b));
if(islessequal(e,c)){
a=(ceil(b))+(fmin(c,b));
h=fmin(h,a);
d=fdim(g,a);
a=floor(e);
}
g=(floor(f))+(fdim(b,f));
h=(atan2(c,e))/(fmin(a,f));
a=(sin(h))/(tan(d));
}