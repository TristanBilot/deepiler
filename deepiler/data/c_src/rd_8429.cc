#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(b,d);
d=(log(d))-(fmax(e,d));
d=fdim(c,b);
d=(log10(e))*(fmin(d,d));
c=(floor(c))-(asin(d));
e=fmax(e,c);
c=(acos(a))/(fmax(e,c));
a=(atan(a))-(fmin(c,a));
d=(fmax(b,a))/(floor(d));
}