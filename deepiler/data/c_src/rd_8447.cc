#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(e,a);
a=log(b);
e=log(e);
c=log(f);
g=atan2(g,f);
d=exp(e);
f=fmax(e,g);
h=atan2(a,f);
d=(fmin(a,g))*(cos(g));
h=(log(c))*(fdim(e,b));
h=atan2(d,c);
}