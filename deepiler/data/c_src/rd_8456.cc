#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(a,e))/(fdim(c,e));
e=(exp(a))/(sin(a));
e=(sin(a))+(atan2(b,d));
if(isless(e,a)){
a=(exp(c))+(cos(b));
c=fdim(d,b);
d=fdim(e,c);
a=atan(d);
e=(fmin(e,e))-(fmin(b,d));
}
if(islessgreater(b,a)){
b=(asin(d))/(pow(b,c));
d=(fmin(b,a))-(floor(a));
a=log10(e);
b=(pow(a,b))-(fmax(a,e));
e=pow(e,a);
}
}