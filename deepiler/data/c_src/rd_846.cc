#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=tan(b);
e=ceil(c);
if(islessgreater(a,d)){
b=(pow(d,d))*(fdim(a,d));
a=(sin(b))+(asin(b));
d=sin(a);
b=pow(e,c);
}
else{
a=(acos(d))+(floor(a));
c=(log10(b))/(fmax(d,a));
b=fmax(a,e);
a=fmax(e,d);
}
if(isgreaterequal(c,b)){
a=ceil(c);
a=(fdim(c,d))-(tan(c));
}
else{
a=(fmax(e,c))-(pow(c,a));
a=floor(e);
e=(pow(e,b))/(fmax(b,e));
c=sqrt(a);
}
}