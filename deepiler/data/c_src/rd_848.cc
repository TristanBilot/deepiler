#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(a,a))/(sin(b));
c=fmax(b,e);
c=(sqrt(e))/(fmin(d,a));
while(islessequal(d,e)){
a=(exp(c))/(log(a));
e=tan(a);
a=(fdim(e,e))-(fdim(b,e));
}
while(islessgreater(a,b)){
c=fmin(a,b);
a=atan2(c,e);
}
}