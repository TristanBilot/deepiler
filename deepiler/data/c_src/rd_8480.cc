#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log10(e);
d=atan2(e,b);
c=fmax(b,b);
f=(asin(d))+(acos(c));
d=(asin(a))*(asin(d));
b=ceil(d);
g=(pow(d,b))-(fmax(e,c));
b=asin(d);
b=(fmin(e,f))/(tan(b));
if(islessequal(c,a)){
d=log(g);
b=(fmax(g,e))-(tan(c));
f=sqrt(b);
}
}