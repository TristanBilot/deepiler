#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(log(e))+(fdim(c,f));
c=fmax(a,d);
d=fdim(b,a);
c=(atan2(a,a))/(fmax(d,a));
a=(cos(a))/(log10(b));
a=fdim(e,c);
d=exp(b);
if(islessgreater(a,c)){
a=(tan(f))-(fmax(f,c));
d=(atan2(a,f))/(pow(f,d));
e=(floor(a))+(fmin(d,b));
}
else{
c=acos(b);
f=(pow(f,f))*(sin(d));
e=(fmax(d,f))-(fmax(d,d));
b=ceil(f);
a=(fmin(b,f))*(log10(d));
}
}