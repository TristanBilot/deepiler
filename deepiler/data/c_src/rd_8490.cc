#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=log(d);
c=tan(e);
e=log10(b);
e=pow(a,b);
g=sqrt(f);
while(isless(b,b)){
d=exp(g);
d=(fmax(d,b))+(ceil(g));
}
d=(asin(e))*(tan(c));
b=fmin(b,a);
e=pow(e,b);
d=log(c);
f=(fmin(g,a))/(tan(a));
}