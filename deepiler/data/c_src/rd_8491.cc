#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(g,a);
d=(log10(h))*(tan(g));
b=(pow(g,g))-(ceil(d));
h=(atan2(a,c))+(fdim(e,h));
g=acos(h);
a=(sin(d))-(atan(h));
d=(cos(f))-(pow(b,h));
g=(fdim(g,e))+(pow(h,d));
h=(log(c))*(pow(a,c));
c=log10(b);
a=pow(b,h);
}