#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(log10(d))-(atan2(g,c));
c=(acos(a))+(fdim(d,h));
h=(acos(b))/(fdim(h,d));
g=acos(h);
while(isless(h,e)){
g=(pow(c,b))/(atan(g));
b=(atan2(c,b))-(fdim(h,e));
e=ceil(h);
b=(atan2(e,a))-(atan2(g,a));
e=(log(f))/(log10(h));
}
if(islessequal(e,h)){
d=(fmax(h,d))-(acos(d));
c=cos(e);
}
else{
b=(fmax(f,f))-(ceil(c));
h=atan2(d,h);
g=sin(c);
d=(acos(c))+(log(h));
}
}