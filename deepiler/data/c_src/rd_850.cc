#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=sqrt(g);
g=(fmin(e,b))/(pow(b,d));
if(islessgreater(b,c)){
f=(tan(a))+(exp(g));
c=(sin(g))/(sin(c));
b=(fdim(d,e))/(log10(c));
}
else{
e=fmin(d,b);
d=(asin(f))/(exp(f));
d=(pow(d,g))+(log(e));
f=(ceil(f))-(fmin(d,c));
}
e=exp(b);
e=(pow(c,a))*(fdim(c,b));
a=(log(g))+(atan2(a,c));
d=log10(e);
}