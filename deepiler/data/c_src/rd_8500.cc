#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log10(a);
g=fmin(c,g);
f=(fmin(a,b))-(floor(b));
g=floor(e);
a=(pow(b,f))/(tan(g));
b=(fmin(e,d))-(cos(f));
g=atan2(e,b);
while(islessequal(b,a)){
c=cos(b);
d=(fdim(f,b))+(ceil(b));
b=pow(f,f);
f=fmax(g,b);
g=(atan2(b,e))-(fmin(a,a));
}
}