#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(c,a);
b=(tan(e))/(pow(e,a));
while(isless(c,c)){
e=(tan(d))*(pow(d,a));
d=tan(a);
e=(asin(a))+(pow(b,d));
}
while(isgreaterequal(e,c)){
b=floor(a);
d=tan(d);
e=fmax(c,b);
}
}