#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(c,e))+(atan(d));
a=(asin(b))/(fdim(d,c));
if(islessequal(a,a)){
d=floor(b);
a=(pow(a,b))/(log10(e));
}
else{
a=exp(b);
e=(fmin(c,e))-(cos(d));
b=atan2(a,d);
b=(atan(a))/(acos(e));
d=(floor(b))*(atan2(d,e));
}
if(isless(d,b)){
c=(pow(e,b))+(fdim(c,d));
b=fmax(e,a);
a=fmin(a,a);
}
}