#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(atan(e))+(floor(d));
g=(pow(a,a))/(fdim(g,g));
g=atan2(b,a);
b=(pow(f,a))/(log(g));
e=ceil(g);
e=(fmin(g,g))/(pow(e,c));
f=(fmin(d,g))*(fdim(f,d));
e=floor(b);
g=(pow(g,b))/(pow(e,c));
f=atan2(d,e);
g=exp(f);
f=(fmin(g,b))-(pow(e,c));
g=fdim(g,d);
}