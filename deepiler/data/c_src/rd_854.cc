#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(d,g);
g=fdim(e,e);
d=(fmin(a,f))*(fmax(c,c));
g=pow(b,d);
g=(asin(d))+(asin(e));
d=fmax(a,d);
b=log(c);
c=(sqrt(d))+(atan2(d,c));
g=(cos(d))*(log(g));
b=ceil(g);
if(isless(d,b)){
d=(fmax(c,g))/(cos(e));
d=asin(e);
g=(acos(g))-(sqrt(b));
}
else{
g=acos(a);
e=sin(f);
d=(fdim(e,b))-(sin(a));
}
}