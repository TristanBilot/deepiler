#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(c,a))/(floor(b));
d=(floor(d))/(sqrt(c));
c=(sqrt(b))*(log(a));
while(isless(e,d)){
d=(fdim(a,a))-(atan2(b,e));
a=fmin(b,a);
}
if(isgreaterequal(e,b)){
b=tan(c);
c=asin(b);
a=fmax(a,a);
d=(fmin(d,a))/(exp(c));
}
}