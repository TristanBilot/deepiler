#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=floor(b);
d=(fdim(c,f))*(atan2(h,e));
e=cos(d);
while(isgreaterequal(c,b)){
h=pow(g,d);
a=exp(f);
e=tan(c);
}
while(islessequal(d,a)){
h=log(h);
f=acos(h);
g=(fmin(g,b))/(atan(e));
b=(pow(f,e))*(log(b));
g=ceil(a);
}
}