#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fdim(b,g);
g=fmax(e,a);
b=cos(e);
h=(log10(e))/(pow(g,a));
f=(pow(e,c))-(sqrt(f));
a=log10(d);
f=(fmin(a,h))+(log(c));
f=(pow(d,a))+(sin(e));
while(islessequal(b,h)){
h=(log10(f))*(asin(h));
a=(fdim(a,a))/(pow(f,h));
c=(ceil(a))+(atan(f));
}
}