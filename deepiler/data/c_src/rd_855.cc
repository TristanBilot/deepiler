#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmax(f,e))*(pow(g,d));
f=(fdim(a,e))-(fdim(f,b));
g=sin(c);
f=log10(f);
if(isless(a,d)){
d=(atan(c))/(fmin(d,a));
f=sqrt(f);
}
while(isgreaterequal(e,f)){
f=(ceil(f))/(log(e));
g=pow(b,g);
b=(atan(e))/(ceil(g));
}
}