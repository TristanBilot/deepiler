#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(b,c);
e=(log10(a))-(fmax(a,e));
a=log(b);
e=(atan2(b,d))*(atan2(a,e));
b=(floor(c))+(fmax(b,e));
e=floor(c);
while(islessequal(e,c)){
b=(tan(e))*(exp(b));
b=(fdim(b,a))*(sin(d));
b=(exp(b))/(fdim(d,c));
}
}