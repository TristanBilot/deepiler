#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(c);
h=atan(a);
a=(fmin(h,a))+(atan2(c,d));
h=(fdim(a,c))+(sin(a));
d=(ceil(a))/(fdim(g,a));
a=(pow(a,d))*(asin(c));
d=(atan2(a,g))*(cos(g));
if(islessgreater(a,h)){
e=(acos(b))+(sqrt(a));
f=(fmin(c,g))-(atan2(e,e));
b=(fdim(d,e))-(exp(c));
}
else{
f=fmin(c,f);
b=(exp(f))+(acos(e));
e=fdim(h,c);
d=fdim(f,h);
}
}