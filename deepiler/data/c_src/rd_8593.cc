#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=fmin(e,d);
f=(fmax(b,a))+(pow(d,e));
if(isgreaterequal(f,c)){
a=pow(a,d);
c=sin(d);
d=(fmin(e,c))/(fmin(f,e));
e=(asin(c))/(log10(b));
f=(exp(a))-(fdim(e,e));
}
else{
f=tan(b);
a=(log10(c))/(pow(c,c));
}
e=(fmax(f,c))/(log(e));
c=(sin(b))*(atan2(d,f));
e=(fdim(b,d))/(fmax(c,a));
}