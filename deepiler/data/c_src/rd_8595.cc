#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=pow(d,c);
a=(pow(b,e))-(exp(d));
g=floor(b);
d=(fdim(a,b))-(pow(b,h));
d=fmin(g,g);
f=fmin(d,b);
f=(atan2(b,f))/(tan(b));
if(islessequal(h,g)){
a=asin(a);
d=(log10(b))-(log10(f));
g=(fmax(h,c))+(atan2(b,a));
h=(asin(h))+(fdim(e,d));
}
}