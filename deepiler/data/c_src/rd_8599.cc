#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(d);
h=(fmax(g,h))-(fdim(b,c));
while(isless(e,e)){
h=(fdim(a,c))/(log10(h));
d=(sqrt(c))*(pow(c,g));
f=tan(e);
c=(pow(e,e))+(fmin(a,g));
}
e=(atan2(d,a))/(fmax(e,e));
b=(atan2(a,h))*(fdim(c,c));
c=tan(f);
d=(pow(b,f))*(cos(d));
g=tan(c);
}