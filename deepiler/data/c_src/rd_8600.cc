#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(fdim(d,b))/(sin(f));
e=fdim(f,g);
c=fdim(g,f);
g=sqrt(d);
while(isless(d,e)){
f=(fmin(g,f))-(tan(f));
a=(fmin(a,a))+(log10(b));
}
while(islessgreater(c,e)){
b=(pow(b,g))*(fdim(d,g));
e=(atan2(f,a))+(ceil(f));
}
}