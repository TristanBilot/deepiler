#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(exp(d))-(sin(a));
h=exp(b);
a=(fmax(h,a))*(floor(e));
a=fmin(g,h);
b=(cos(a))*(atan2(g,f));
while(islessgreater(g,c)){
f=sin(e);
g=atan(e);
b=(ceil(h))/(exp(d));
a=atan2(g,a);
a=atan2(e,g);
}
h=fmin(h,d);
e=(exp(h))+(cos(b));
e=(atan2(g,a))+(pow(b,h));
}