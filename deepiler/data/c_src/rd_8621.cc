#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(b,d))*(fmin(d,b));
a=(fdim(e,e))/(acos(e));
if(islessequal(b,e)){
a=(cos(f))+(fmax(e,d));
b=log(f);
c=exp(a);
c=sin(a);
f=(log10(f))*(asin(a));
}
e=(fmin(f,c))/(fmin(f,c));
f=(asin(c))*(tan(b));
a=(acos(d))-(atan2(e,d));
}