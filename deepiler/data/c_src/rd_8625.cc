#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(atan2(a,a))-(atan2(d,d));
f=(floor(e))-(floor(a));
b=(floor(f))-(fmin(a,a));
d=fmin(a,c);
b=(fmin(f,b))*(log10(b));
c=(pow(e,b))+(fmax(e,f));
e=log(b);
a=ceil(a);
b=atan2(a,e);
c=(exp(a))*(sin(a));
b=atan2(a,b);
e=cos(e);
c=(log10(b))-(cos(b));
}