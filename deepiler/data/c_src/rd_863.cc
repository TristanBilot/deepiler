#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fmin(b,c))+(atan2(b,b));
c=(atan2(b,e))/(ceil(b));
b=(tan(e))/(fmin(e,a));
b=fmax(e,e);
c=fmax(a,c);
c=log10(d);
a=(log(b))+(cos(c));
a=(acos(e))/(fmax(c,d));
b=fmin(c,b);
}