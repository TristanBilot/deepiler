#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(b);
d=(atan(g))/(fmax(e,a));
d=fmin(f,b);
a=ceil(d);
if(isless(e,g)){
d=tan(e);
a=pow(f,e);
g=(exp(g))*(fmax(c,e));
c=cos(a);
}
else{
c=fmin(c,f);
c=sin(b);
c=(log10(c))*(acos(d));
d=(exp(d))*(atan2(d,c));
}
g=(fdim(a,g))/(fmin(a,a));
f=(cos(g))-(tan(a));
a=fdim(b,e);
c=atan2(d,f);
c=(log10(d))-(asin(h));
}