#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,e);
a=ceil(f);
d=fmin(d,a);
while(isgreaterequal(g,h)){
h=(fmin(h,a))+(fdim(d,a));
b=log(b);
a=fmax(c,a);
e=(cos(g))-(exp(a));
d=fdim(d,e);
}
if(islessgreater(e,g)){
f=floor(h);
d=(fmax(b,a))*(exp(e));
c=ceil(a);
b=tan(a);
f=cos(h);
}
}