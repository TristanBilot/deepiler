#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=asin(a);
d=(fdim(e,b))*(ceil(c));
c=(log10(f))+(atan(e));
if(islessgreater(f,d)){
a=fmin(c,b);
c=pow(e,b);
f=(ceil(a))+(log10(c));
d=fmin(e,b);
}
else{
f=sqrt(c);
f=(atan2(d,e))*(ceil(c));
}
d=exp(b);
a=sqrt(f);
b=fdim(e,c);
}