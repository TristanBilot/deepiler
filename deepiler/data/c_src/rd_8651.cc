#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=cos(c);
c=(asin(b))+(acos(f));
b=atan2(f,b);
e=(atan(f))/(atan2(c,a));
c=(fmin(e,f))-(fmax(d,a));
if(isgreaterequal(a,d)){
c=log(b);
c=(fmax(c,f))*(ceil(c));
a=(fmax(a,d))-(log(d));
d=(atan(f))*(pow(d,e));
f=atan2(a,a);
}
while(isgreaterequal(d,e)){
d=cos(b);
a=(fmin(f,d))*(log(e));
}
}