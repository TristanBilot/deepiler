#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(c,e);
b=atan(a);
h=(cos(g))-(atan2(h,c));
g=(sqrt(g))-(acos(c));
d=(log(g))-(fmax(b,c));
h=(fmin(h,h))-(cos(e));
if(isless(h,b)){
g=(fmin(d,g))/(pow(b,f));
b=pow(e,f);
c=(ceil(c))*(sqrt(d));
g=(fmax(c,b))*(asin(d));
}
else{
e=fmax(h,f);
f=atan2(a,f);
b=(atan(e))-(tan(c));
f=(fmin(g,g))-(log(g));
}
}