#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=log(a);
d=(fdim(a,g))+(asin(e));
while(islessgreater(a,d)){
c=fmax(a,f);
f=sin(g);
f=fmax(b,f);
g=(fdim(a,a))/(atan2(c,g));
a=(fdim(e,b))+(acos(g));
}
while(isgreaterequal(e,e)){
g=sin(d);
c=fdim(b,f);
}
}