#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(c,b))/(pow(a,b));
a=(fmax(e,b))/(fdim(e,a));
while(isless(f,f)){
a=fmin(b,c);
a=sqrt(a);
a=log10(c);
}
if(isgreaterequal(a,d)){
a=(floor(b))/(pow(d,d));
b=(ceil(b))*(floor(a));
a=fmin(f,b);
f=(log(d))+(exp(f));
}
else{
b=(atan(c))*(pow(a,b));
b=(asin(e))/(ceil(d));
f=(acos(d))/(asin(b));
f=(atan2(f,b))-(fmax(a,e));
d=(atan(d))*(tan(b));
}
}