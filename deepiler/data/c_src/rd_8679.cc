#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=atan(e);
f=(atan2(c,e))/(log(f));
d=fmin(f,c);
if(islessequal(d,d)){
g=fdim(e,e);
g=(pow(b,e))/(fmax(e,a));
}
if(isless(d,g)){
a=floor(c);
a=(asin(d))-(log(g));
d=(pow(e,a))+(fmax(f,f));
}
}