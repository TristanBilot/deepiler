#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmax(e,e))*(exp(g));
f=(fmax(b,g))-(asin(a));
f=(fmax(b,e))+(fdim(a,f));
e=fmax(f,c);
a=(log10(a))-(atan(e));
f=ceil(c);
e=(atan(f))-(atan(g));
if(isgreaterequal(g,g)){
f=exp(b);
e=fmax(g,b);
b=(ceil(g))/(atan2(g,c));
d=(fdim(d,g))*(sqrt(b));
}
else{
d=(atan(b))+(fmin(e,f));
c=(pow(b,b))+(atan2(g,a));
}
}