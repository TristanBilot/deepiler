#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=atan2(c,c);
f=atan2(f,e);
b=fmax(d,a);
b=(pow(c,d))+(pow(b,f));
g=(fmin(d,a))*(acos(a));
d=fmax(e,g);
d=(cos(c))*(cos(b));
c=fdim(e,c);
if(islessequal(a,e)){
f=(atan2(f,c))*(log(a));
g=asin(d);
c=log10(c);
}
}