#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=tan(a);
e=(sin(d))-(sqrt(d));
e=fmin(c,a);
while(islessgreater(e,d)){
a=(fmax(a,d))*(cos(c));
a=floor(c);
a=(log(b))+(sqrt(b));
d=(pow(b,d))*(ceil(c));
}
while(isless(a,c)){
e=fdim(b,e);
c=fmax(b,d);
c=(fmax(a,c))/(fmax(e,b));
d=fmin(e,d);
d=sqrt(d);
}
}