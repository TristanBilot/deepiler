#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmin(a,e))*(log10(e));
e=(fmax(a,g))/(log(d));
d=(fmin(d,g))*(ceil(c));
g=fmin(g,a);
a=fmin(c,g);
c=(fmax(g,d))/(sin(g));
a=ceil(a);
while(islessequal(f,a)){
e=asin(d);
f=(fmax(f,a))*(exp(g));
}
}