#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(a,c);
a=asin(a);
while(isgreaterequal(c,d)){
a=(cos(d))*(log(a));
c=tan(e);
}
while(isgreaterequal(a,e)){
c=(asin(e))+(pow(d,e));
d=exp(c);
e=fmax(b,b);
d=(sin(a))*(fdim(a,c));
e=(pow(d,e))-(sin(c));
}
}