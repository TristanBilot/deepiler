#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=acos(d);
g=fmin(a,f);
c=(log10(a))*(ceil(f));
c=pow(b,e);
f=(sin(a))+(atan(c));
e=(ceil(e))*(fmax(d,b));
d=fmax(d,b);
a=exp(a);
if(isgreaterequal(c,f)){
a=(pow(g,g))/(log10(a));
d=exp(d);
d=(sin(a))+(fmax(c,g));
}
}