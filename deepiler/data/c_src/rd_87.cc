#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmax(c,e);
e=exp(d);
a=fmax(d,d);
d=(tan(b))-(acos(c));
e=(fmin(c,c))-(fmax(a,c));
while(isgreaterequal(a,d)){
c=fmin(a,a);
c=(fmin(c,d))-(fmax(e,c));
a=log(b);
c=(fmax(b,b))-(fdim(d,c));
e=(atan(d))*(floor(c));
}
while(islessequal(c,b)){
c=tan(a);
a=(tan(c))*(ceil(b));
}
}