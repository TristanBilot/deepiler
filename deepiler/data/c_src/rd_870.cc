#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmax(f,c);
f=(asin(e))+(tan(b));
while(isless(b,d)){
f=(ceil(b))-(tan(c));
c=(fmax(g,a))*(log10(a));
}
if(islessequal(a,f)){
e=sqrt(b);
c=(fdim(e,c))/(ceil(e));
}
else{
b=atan(d);
f=fdim(f,f);
a=log10(b);
d=(fmax(b,f))/(cos(d));
}
}