#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan2(c,c);
d=(cos(d))-(fmax(e,b));
c=log10(e);
while(islessgreater(d,c)){
b=acos(b);
a=(sqrt(c))-(atan2(e,e));
e=fmax(d,e);
b=sqrt(a);
a=(log10(d))-(atan2(a,d));
}
if(isless(d,a)){
d=pow(c,e);
d=log(d);
d=log10(d);
e=(fdim(c,b))+(acos(b));
d=(acos(b))+(log(e));
}
else{
b=(pow(d,a))*(log10(a));
d=fmin(b,e);
a=(fdim(b,a))+(fmax(d,b));
c=(atan2(e,c))+(atan2(c,e));
}
}