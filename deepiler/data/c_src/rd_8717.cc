#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(exp(a))-(tan(b));
c=log10(a);
if(isless(b,d)){
c=atan2(b,d);
b=(pow(e,e))/(fmin(d,a));
b=(fmin(c,a))/(fmax(a,c));
d=(log(e))+(ceil(a));
}
if(islessgreater(a,c)){
e=fdim(a,e);
c=fmin(a,d);
}
else{
b=(fmax(e,e))/(fmax(b,e));
c=(log(b))/(tan(a));
c=atan(e);
c=acos(c);
}
}