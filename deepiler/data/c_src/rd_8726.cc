#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=sin(c);
f=(log(c))/(pow(c,g));
while(islessequal(f,d)){
e=atan2(c,g);
a=(atan2(g,a))-(pow(g,f));
}
e=(cos(c))+(sin(a));
d=pow(d,e);
}