#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fdim(g,b))*(fdim(f,c));
e=(fdim(h,h))-(log(c));
c=(sin(e))-(atan2(a,a));
b=atan2(g,c);
d=atan(h);
if(isless(h,a)){
d=(fdim(d,g))/(asin(c));
h=(fmax(g,h))+(pow(h,e));
c=fmax(b,h);
}
else{
e=(fdim(f,c))+(tan(c));
e=exp(b);
d=(log(d))+(sqrt(c));
a=(asin(a))-(sqrt(b));
f=(tan(e))+(fmin(e,c));
}
}