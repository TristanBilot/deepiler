#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(log(g))*(sqrt(c));
a=asin(b);
b=(atan2(h,a))/(fmin(d,h));
if(isless(e,f)){
e=(sqrt(a))-(floor(a));
h=(fmax(g,f))+(sin(f));
f=(floor(d))-(floor(c));
b=(log(g))+(acos(c));
e=acos(a);
}
else{
e=fmin(e,c);
d=(fmin(a,g))-(ceil(e));
c=exp(c);
}
while(isgreaterequal(e,f)){
b=(asin(e))-(log(e));
e=log10(b);
c=(atan2(h,a))+(floor(g));
g=atan2(d,f);
}
}