#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(e,f);
b=(acos(d))+(pow(d,g));
a=(log10(h))+(atan(h));
a=(pow(a,g))/(cos(c));
g=log10(d);
if(isless(e,f)){
h=fmax(d,d);
c=(pow(h,c))/(tan(h));
e=(cos(d))*(atan2(f,h));
}
else{
h=(log(h))-(atan2(g,h));
c=fdim(e,g);
}
}