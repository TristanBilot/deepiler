#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan(b);
a=tan(e);
while(islessgreater(e,b)){
e=(atan2(c,e))/(pow(e,a));
a=(atan2(d,b))-(atan2(d,e));
d=fdim(a,a);
a=(fmax(d,c))*(cos(a));
}
e=(acos(b))-(fdim(d,c));
e=(fmax(d,d))/(fmin(a,c));
c=(exp(e))/(asin(a));
c=(fdim(b,c))/(tan(d));
}