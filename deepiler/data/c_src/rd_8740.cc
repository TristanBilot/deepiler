#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=fmax(b,f);
e=(sqrt(d))/(atan2(g,d));
a=(fmax(b,d))-(cos(g));
a=(exp(a))+(fdim(c,e));
g=exp(e);
f=atan2(b,d);
d=(fmin(c,e))/(log(e));
e=(sin(e))/(fmin(c,d));
b=(atan2(b,f))+(atan(f));
while(islessgreater(a,g)){
b=(fmax(b,b))/(fdim(d,d));
a=(fmin(b,e))*(pow(b,e));
f=(log10(f))*(cos(e));
}
}