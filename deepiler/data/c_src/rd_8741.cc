#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(exp(a))/(tan(b));
b=ceil(e);
while(islessequal(d,e)){
a=(pow(a,c))/(fmax(c,b));
e=(ceil(a))+(cos(e));
c=(fdim(c,d))/(acos(b));
}
if(islessgreater(c,c)){
a=(pow(c,b))+(fdim(b,c));
a=fmin(b,b);
}
else{
e=fdim(c,b);
c=(tan(b))*(log(c));
a=atan2(e,b);
b=cos(d);
}
}