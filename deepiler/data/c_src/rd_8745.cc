#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=asin(d);
e=(atan(g))*(fmax(a,e));
while(isgreaterequal(e,h)){
b=acos(c);
e=(pow(e,e))*(log(h));
e=(tan(c))*(atan2(d,g));
}
b=pow(h,c);
c=(floor(b))-(acos(f));
g=(fmax(c,d))*(exp(b));
c=(cos(f))-(atan(f));
}