#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(asin(b))+(fdim(d,g));
d=fdim(b,b);
c=(fmax(e,e))+(fmin(d,f));
d=(floor(a))/(fmax(f,a));
f=(ceil(d))*(atan(b));
a=(fdim(c,e))/(fdim(g,a));
c=sin(c);
e=(cos(b))+(atan2(c,d));
while(isless(f,e)){
f=atan(c);
b=pow(a,b);
}
}