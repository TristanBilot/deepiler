#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log(a))+(atan(c));
g=(fmin(h,b))*(fdim(h,a));
g=fmax(h,g);
h=(atan(b))-(exp(c));
while(isgreaterequal(d,b)){
h=asin(g);
c=(log(b))-(atan2(c,b));
d=(fmax(g,e))/(log10(b));
e=asin(d);
}
while(islessgreater(e,f)){
e=(fmin(c,h))-(floor(f));
b=(sin(a))*(fdim(e,f));
}
}