#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log(e);
b=(fdim(a,e))-(atan(e));
while(isless(e,c)){
e=(sqrt(b))/(fdim(d,d));
c=cos(c);
e=fmin(d,a);
d=atan(e);
d=sqrt(c);
}
while(isgreaterequal(a,e)){
c=exp(d);
d=log10(a);
e=(fmin(e,b))*(atan2(a,c));
d=(tan(d))+(log10(e));
}
}