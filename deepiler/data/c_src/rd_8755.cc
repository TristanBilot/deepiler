#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fmax(e,b);
c=ceil(b);
c=(ceil(c))/(floor(c));
b=(fdim(a,d))-(atan(e));
while(isless(a,a)){
d=(sin(b))/(acos(c));
e=(fmax(b,c))/(exp(e));
c=(fmax(a,b))+(tan(e));
e=sqrt(d);
c=(fmin(a,d))+(fmax(a,e));
}
b=atan2(d,d);
e=cos(b);
c=(fmax(d,b))/(fdim(a,a));
b=(fmin(e,a))-(exp(a));
}