#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,d))/(sqrt(d));
d=pow(a,c);
c=asin(c);
d=fmin(a,b);
while(isless(d,e)){
c=(pow(e,e))+(sin(b));
a=acos(a);
a=(fmax(b,a))+(fdim(a,b));
e=(atan2(a,e))*(atan2(d,d));
}
if(islessequal(c,a)){
b=ceil(c);
c=sin(e);
b=(atan2(c,a))*(fdim(c,c));
b=fmax(a,c);
a=(atan2(c,d))+(log(a));
}
}