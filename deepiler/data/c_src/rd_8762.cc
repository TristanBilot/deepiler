#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(pow(d,d))/(atan2(f,e));
b=(atan(d))+(fmax(d,e));
c=(tan(b))-(sin(d));
f=(tan(c))/(fdim(c,e));
d=fdim(e,d);
c=log(e);
c=fmin(a,c);
if(isless(b,b)){
d=(fmin(a,b))/(exp(b));
c=fmax(b,a);
}
else{
c=atan2(e,d);
a=fmax(a,e);
e=(pow(f,b))-(exp(c));
}
}