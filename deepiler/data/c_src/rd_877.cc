#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(f,a);
d=(sin(b))*(atan2(a,e));
c=(cos(e))-(floor(d));
if(islessequal(c,a)){
g=log10(d);
b=fmin(d,d);
e=(ceil(b))*(fmin(e,c));
}
else{
d=(exp(g))-(atan2(b,g));
e=(fmin(c,b))-(acos(f));
}
if(islessequal(f,a)){
a=(ceil(b))-(fmax(g,e));
c=exp(a);
g=fmax(a,c);
f=(fdim(g,d))-(exp(a));
b=(pow(d,e))-(pow(c,c));
}
else{
e=(pow(g,g))/(atan2(c,a));
a=(asin(b))/(atan2(e,d));
c=(fmax(d,f))-(tan(f));
}
}