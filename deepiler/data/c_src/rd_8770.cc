#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(c);
b=(ceil(e))+(log10(e));
a=pow(h,f);
if(isless(b,e)){
b=(log(f))+(acos(h));
e=(atan(b))/(exp(a));
h=(ceil(c))+(sqrt(h));
}
if(isless(g,d)){
c=(pow(b,h))/(sin(g));
a=(log10(a))*(asin(h));
f=fmax(a,h);
}
else{
a=floor(f);
a=asin(g);
h=(fmax(h,h))*(sqrt(d));
}
}