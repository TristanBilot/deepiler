#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(log(f))+(acos(d));
f=(pow(f,e))-(floor(f));
f=pow(c,c);
a=fmax(h,g);
while(islessequal(c,e)){
c=tan(f);
a=(fmin(f,b))+(pow(f,a));
b=fmax(c,f);
f=fmax(d,b);
e=exp(b);
}
while(isless(h,b)){
h=(fmin(e,b))*(tan(e));
c=fdim(a,g);
d=fdim(f,a);
g=fmax(d,f);
}
}