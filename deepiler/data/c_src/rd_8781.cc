#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(g,e))*(exp(d));
f=log(f);
c=floor(h);
c=fmin(a,e);
e=(log10(b))+(ceil(d));
if(isless(b,a)){
e=(fmax(c,b))*(log10(e));
d=(sin(a))/(atan2(f,f));
c=atan2(c,g);
b=(pow(a,a))+(ceil(e));
d=atan(h);
}
if(islessequal(c,b)){
b=(acos(d))-(atan(e));
d=pow(d,e);
g=pow(g,f);
d=atan(d);
}
else{
a=(pow(h,d))/(floor(a));
h=cos(a);
c=(fmax(c,c))/(asin(g));
d=(sin(b))-(atan(g));
}
}