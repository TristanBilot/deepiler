#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=sin(e);
c=log10(e);
d=sin(f);
d=fmin(b,c);
if(islessequal(a,b)){
f=(fmin(e,b))-(atan(e));
f=fmin(a,a);
b=(fmin(e,e))*(exp(b));
c=acos(b);
}
else{
e=(atan(b))-(ceil(c));
a=(fmax(d,a))-(pow(d,f));
f=fmax(b,c);
a=fmax(b,e);
}
if(isgreaterequal(a,d)){
e=tan(f);
a=(exp(a))-(fmax(f,b));
}
}