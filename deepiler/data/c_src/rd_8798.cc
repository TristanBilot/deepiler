#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fmax(d,e))+(floor(a));
c=(pow(b,c))*(fdim(d,e));
g=(atan2(h,h))+(tan(a));
while(islessequal(c,g)){
e=(pow(c,g))/(pow(a,c));
g=log(c);
h=fmin(g,h);
h=asin(g);
e=fdim(a,a);
}
e=(fdim(e,f))*(sqrt(a));
d=log(f);
a=ceil(a);
}