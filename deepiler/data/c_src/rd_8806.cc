#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=log(b);
f=(floor(b))-(atan2(e,c));
b=pow(c,b);
while(islessequal(a,d)){
c=sqrt(d);
e=fmax(a,c);
e=fmin(d,d);
c=atan2(b,e);
d=(cos(d))-(sin(c));
}
b=(cos(e))+(fmax(b,b));
a=acos(b);
}