#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=fdim(b,e);
g=(pow(a,g))*(log10(a));
a=tan(c);
e=(pow(e,b))/(fdim(d,d));
g=(log(d))*(asin(c));
b=(atan2(e,f))+(asin(g));
if(islessequal(g,d)){
a=fmin(e,a);
f=(atan(d))*(pow(d,e));
}
else{
f=(acos(c))/(log10(e));
b=pow(b,b);
c=atan(a);
g=fmax(a,f);
f=(fmax(b,g))-(acos(f));
}
}