#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=pow(g,c);
b=(sin(a))/(log10(g));
g=(tan(f))+(exp(f));
b=(fdim(c,e))*(log10(g));
g=sin(f);
if(islessgreater(d,e)){
e=(fmin(b,g))+(fmax(c,a));
g=asin(b);
d=(pow(f,d))*(fmin(d,b));
b=(fmax(a,f))-(atan2(e,e));
}
else{
g=fmin(c,g);
d=(exp(g))/(ceil(c));
}
a=(atan2(d,a))*(fmin(b,d));
g=(pow(d,f))/(pow(d,d));
a=(fdim(f,a))*(fdim(g,g));
d=fmin(g,f);
a=(log(c))*(fmin(c,g));
}