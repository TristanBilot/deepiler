#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(h,e);
h=(floor(c))+(pow(e,h));
d=fmin(f,b);
if(isless(c,b)){
f=log(d);
e=tan(b);
e=(fmin(b,h))/(fmin(d,e));
c=pow(h,b);
}
d=log10(a);
f=pow(b,e);
}