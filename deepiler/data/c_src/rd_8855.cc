#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(pow(c,e))+(pow(c,e));
a=pow(c,e);
c=fmin(e,b);
c=fmax(e,a);
a=(log10(c))/(pow(a,d));
if(isless(c,c)){
b=(fmax(d,b))+(fmax(a,b));
a=(log(d))-(tan(c));
}
else{
a=(fmax(e,e))+(log10(b));
a=pow(a,e);
d=sqrt(d);
e=(acos(d))-(pow(b,a));
}
if(islessgreater(b,c)){
e=(floor(a))/(acos(d));
b=(tan(b))*(exp(e));
}
}