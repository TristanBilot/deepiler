#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=log(d);
f=(fdim(e,b))/(pow(a,c));
if(isless(b,a)){
c=acos(b);
c=floor(d);
c=(atan(e))*(atan(e));
f=fdim(c,e);
e=(pow(f,e))*(sqrt(e));
}
else{
e=(fmin(d,f))/(atan(a));
b=fmax(a,a);
}
if(islessequal(e,a)){
e=(ceil(d))*(floor(f));
d=fmin(b,d);
e=ceil(f);
c=(exp(d))*(ceil(c));
}
else{
e=(ceil(d))*(exp(d));
b=sin(a);
f=atan2(f,a);
a=tan(f);
c=(fmax(b,b))/(pow(c,e));
}
}