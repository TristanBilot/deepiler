#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=fmin(b,e);
d=(acos(d))/(floor(g));
f=(fmin(d,a))*(fmin(d,b));
g=(acos(g))/(sin(a));
b=atan2(b,d);
while(islessequal(f,d)){
d=(atan(a))+(atan(g));
g=(exp(b))+(asin(c));
a=(acos(f))*(exp(d));
}
c=fmax(e,a);
c=(pow(c,e))/(cos(d));
e=(acos(d))+(tan(a));
}