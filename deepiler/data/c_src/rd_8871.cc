#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(asin(d))-(exp(e));
f=(atan2(f,f))*(fmax(d,f));
c=(fdim(b,d))-(asin(a));
while(isless(e,b)){
d=(fdim(c,d))+(fmax(a,f));
e=pow(d,b);
e=(atan2(f,b))-(acos(a));
e=(atan2(e,a))+(atan2(f,c));
e=pow(b,b);
}
while(isless(b,f)){
e=tan(c);
d=(fmin(c,b))-(fmin(e,a));
}
}