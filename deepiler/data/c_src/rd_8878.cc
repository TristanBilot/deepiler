#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=tan(b);
b=(fmax(c,e))+(fmax(d,b));
b=(sqrt(d))*(pow(b,d));
c=atan2(b,b);
while(islessequal(a,e)){
d=(sqrt(a))*(ceil(a));
c=cos(c);
d=fmax(e,e);
e=(acos(c))-(fmax(d,c));
}
while(isless(a,a)){
b=fdim(a,d);
d=tan(e);
b=(log(a))*(atan2(d,b));
}
}