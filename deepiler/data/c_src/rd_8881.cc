#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(d,b))-(fmax(b,c));
c=tan(c);
while(islessgreater(e,e)){
e=(pow(d,e))*(fdim(c,b));
c=(fdim(d,d))+(fmax(d,a));
b=(acos(a))/(fmin(e,e));
}
if(isgreaterequal(e,e)){
b=log10(e);
e=fmin(a,c);
d=atan2(d,b);
e=(atan(b))*(fmin(a,b));
d=sqrt(e);
}
else{
c=(asin(b))-(atan2(d,c));
d=(sqrt(e))*(floor(a));
d=(atan(e))-(log(a));
}
}