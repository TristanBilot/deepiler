#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(a,c))-(log(c));
d=fdim(a,c);
if(isless(c,b)){
b=exp(c);
d=fdim(e,e);
e=pow(c,a);
d=(pow(b,c))/(atan(c));
}
else{
a=sin(d);
c=(fdim(a,d))-(fdim(d,a));
c=pow(e,e);
c=fdim(b,b);
a=(fmax(d,c))/(log10(c));
}
if(isless(e,a)){
e=(atan(c))-(atan(e));
a=log(c);
a=tan(d);
b=(fmin(d,a))+(pow(c,d));
a=fmax(c,d);
}
}