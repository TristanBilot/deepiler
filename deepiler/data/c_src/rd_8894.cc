#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fdim(b,g);
e=log10(d);
c=(exp(a))-(fdim(d,d));
h=(fmax(d,g))-(ceil(h));
while(isgreaterequal(b,g)){
g=floor(b);
b=cos(a);
a=(exp(a))+(fdim(e,h));
b=exp(e);
h=(cos(a))-(exp(a));
}
if(isless(g,b)){
d=fmin(b,h);
g=pow(c,d);
d=(pow(b,b))/(pow(d,f));
}
}