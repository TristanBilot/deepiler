#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(sin(a))/(sin(g));
f=asin(f);
c=cos(e);
if(isgreaterequal(a,d)){
f=(fmax(f,d))+(sqrt(e));
e=sqrt(d);
}
while(islessequal(b,g)){
c=(fmin(e,b))-(acos(b));
b=(atan2(c,e))/(fdim(g,f));
a=(fmin(a,b))*(exp(b));
}
}