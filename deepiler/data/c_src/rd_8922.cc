#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(pow(c,d))*(log10(b));
b=fmax(d,a);
d=floor(c);
d=atan2(e,d);
e=atan(c);
if(islessequal(a,d)){
d=sin(a);
d=asin(a);
c=(tan(e))+(fmax(c,b));
}
else{
e=ceil(d);
e=(fmin(e,a))-(sin(c));
d=sin(e);
b=(sqrt(c))+(atan2(b,e));
a=acos(a);
}
while(islessequal(c,a)){
d=atan2(c,c);
b=(floor(c))-(sin(e));
a=(tan(b))*(atan(d));
}
}