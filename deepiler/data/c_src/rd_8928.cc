#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(a,a))/(cos(a));
d=pow(d,b);
e=fdim(c,e);
d=pow(b,e);
b=(sin(d))-(atan(d));
b=(atan2(b,d))*(pow(a,c));
b=(fmax(a,c))+(fmin(c,e));
a=(pow(c,d))*(tan(a));
b=fmin(b,d);
a=(atan2(d,b))+(fmin(a,c));
a=(sin(c))-(fmin(d,c));
}