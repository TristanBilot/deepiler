#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(log(b))-(sqrt(c));
a=(fmin(b,a))+(sqrt(b));
if(isgreaterequal(a,e)){
b=atan(d);
e=fmin(b,b);
d=log10(c);
a=(exp(d))*(asin(a));
b=(fmax(a,a))+(pow(d,a));
}
else{
d=fmin(c,c);
b=fmin(d,c);
d=atan2(b,c);
}
while(isgreaterequal(b,e)){
a=(pow(b,a))*(log(c));
d=pow(a,b);
b=(atan2(c,d))-(pow(b,b));
b=(log10(d))*(exp(d));
b=(fmax(a,e))/(pow(a,a));
}
}