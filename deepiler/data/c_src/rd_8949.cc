#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=exp(a);
d=(fmax(a,c))*(fdim(e,b));
a=(log10(c))*(asin(c));
b=(acos(b))-(sin(a));
while(isless(a,e)){
e=(atan(e))*(fmax(c,c));
e=(tan(d))*(fmin(a,c));
a=(sqrt(d))-(cos(c));
}
while(isgreaterequal(e,a)){
c=pow(e,d);
d=(sin(d))*(asin(c));
}
}