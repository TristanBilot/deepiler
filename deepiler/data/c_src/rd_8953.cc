#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fmax(d,a);
f=pow(g,e);
while(islessgreater(e,g)){
f=(tan(e))*(ceil(d));
b=(ceil(a))+(fmin(f,g));
g=(pow(c,f))*(tan(e));
}
a=(fmax(a,e))/(ceil(d));
g=(fmax(c,d))-(exp(b));
}