#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(atan2(a,d))+(fdim(e,d));
f=(ceil(b))*(fdim(f,b));
c=(atan2(c,g))+(ceil(a));
e=fmax(d,d);
b=(cos(b))*(ceil(c));
c=(pow(b,g))-(floor(b));
d=sqrt(e);
while(isgreaterequal(f,e)){
b=fmax(b,a);
b=(sin(a))-(acos(d));
g=atan(c);
}
}