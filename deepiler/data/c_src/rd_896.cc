#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmin(e,d))+(ceil(g));
c=(fmin(e,e))*(atan(e));
d=(pow(b,a))+(pow(e,a));
g=ceil(f);
while(islessequal(a,g)){
g=(atan2(c,g))-(pow(d,e));
b=(pow(d,g))*(atan2(e,b));
e=fmin(b,b);
g=(atan2(d,f))-(fmax(e,d));
g=(fdim(a,a))*(log(a));
}
g=(atan2(a,c))*(fdim(c,c));
g=(asin(d))-(fmax(d,g));
a=fdim(e,f);
g=(fdim(b,a))/(exp(f));
f=fdim(b,d);
}