#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(c,a);
f=(fmin(c,a))+(log10(e));
e=(pow(d,c))*(atan2(f,g));
b=(asin(a))/(asin(f));
h=(fmax(g,f))/(fdim(e,e));
g=(tan(h))+(log10(a));
while(isless(e,b)){
d=asin(c);
e=(fmin(h,g))*(log(d));
h=(atan2(h,a))+(pow(f,b));
h=(exp(g))-(atan2(c,d));
b=ceil(h);
}
}