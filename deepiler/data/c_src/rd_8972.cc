#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fdim(c,e);
g=fdim(b,g);
e=(ceil(d))+(fmax(f,c));
b=(fmax(f,b))+(fmin(g,f));
d=sin(f);
c=(fdim(f,f))+(tan(a));
while(isless(f,f)){
e=(cos(a))+(pow(a,d));
f=fmin(e,c);
}
}