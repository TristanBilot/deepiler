#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=pow(f,e);
g=(ceil(f))*(fdim(c,b));
c=(fmax(c,d))/(atan2(g,g));
b=atan2(c,a);
while(isgreaterequal(g,f)){
a=(fmax(a,d))+(fmax(b,f));
a=floor(a);
a=fdim(a,a);
c=(pow(f,b))+(pow(f,f));
f=(fmax(e,b))-(log10(e));
}
if(isless(f,d)){
f=(fmin(g,b))-(fmax(g,f));
a=fdim(e,c);
g=(exp(f))+(fmax(c,f));
}
}