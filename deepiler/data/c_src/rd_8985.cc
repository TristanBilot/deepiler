#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,d))/(sqrt(d));
d=atan2(d,a);
d=pow(d,a);
e=(atan2(e,b))+(tan(a));
b=(tan(a))/(fmin(b,d));
if(isless(a,e)){
d=(exp(e))*(pow(e,d));
e=(fmin(b,c))/(exp(e));
c=(exp(b))-(atan(e));
b=acos(b);
}
else{
a=fmin(a,d);
b=(sin(c))*(atan(c));
}
while(islessequal(d,a)){
b=(asin(d))+(log(b));
d=fdim(a,a);
d=fmax(a,d);
}
}