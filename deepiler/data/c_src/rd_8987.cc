#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(g,b))*(sin(a));
f=(atan(b))-(atan2(e,c));
d=acos(c);
c=fmax(a,b);
h=fdim(d,a);
h=(pow(b,h))*(asin(a));
a=cos(a);
if(isgreaterequal(a,g)){
g=atan(c);
e=log10(a);
f=atan2(e,d);
b=atan(d);
a=(atan2(d,h))*(fmax(e,g));
}
else{
a=(floor(d))/(atan2(c,d));
h=log(e);
}
}