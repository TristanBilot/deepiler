#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(pow(d,f))*(sin(c));
g=(atan2(c,c))*(log(g));
h=(sin(d))*(ceil(d));
b=exp(c);
g=(fmax(c,d))/(pow(d,h));
a=atan2(h,a);
f=fmin(g,b);
b=log(f);
while(islessgreater(c,e)){
f=(fmax(e,h))*(fmax(g,c));
h=(asin(d))+(pow(g,a));
e=(exp(h))+(pow(e,b));
e=(exp(d))/(ceil(b));
}
}