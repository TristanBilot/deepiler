#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(log(a))+(fmax(f,d));
g=(asin(b))-(log(b));
e=(fmax(f,g))-(pow(g,a));
while(isgreaterequal(f,c)){
g=(atan2(e,g))-(fmin(e,d));
f=fmax(d,d);
e=sqrt(d);
d=(log(c))*(fmax(c,c));
b=fdim(b,a);
}
while(islessgreater(f,e)){
c=(log10(e))-(asin(e));
b=log10(d);
d=asin(f);
}
}