#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(log10(b))*(exp(e));
g=(sin(f))*(fdim(c,d));
a=log(a);
d=(pow(e,e))+(cos(e));
b=sqrt(b);
if(islessequal(f,a)){
a=tan(g);
f=fmax(a,c);
}
else{
f=fdim(f,e);
b=(fmin(d,c))-(pow(f,d));
g=fmax(f,g);
a=(asin(c))+(log(c));
}
if(islessgreater(d,d)){
d=acos(d);
c=fdim(e,g);
}
}