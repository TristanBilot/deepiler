#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=(fmax(h,d))*(ceil(g));
a=fdim(h,c);
c=fmax(g,g);
h=(fmax(e,b))*(sin(g));
d=fmin(d,c);
while(isless(h,f)){
b=floor(h);
a=(fmax(f,b))-(asin(f));
e=(exp(h))/(pow(a,b));
d=(fmin(e,b))-(atan2(d,a));
c=(pow(e,c))*(fmax(f,f));
}
if(isgreaterequal(g,c)){
a=fmin(d,g);
c=exp(h);
b=(fdim(g,b))-(exp(b));
}
else{
c=ceil(f);
b=sqrt(c);
d=(fmax(a,c))*(atan2(e,d));
}
}