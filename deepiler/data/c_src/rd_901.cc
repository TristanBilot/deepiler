#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(g,f);
g=(fdim(f,e))*(fdim(d,g));
c=log10(b);
h=fmin(c,g);
f=pow(a,g);
h=log10(b);
b=fmax(d,g);
c=(pow(f,b))/(fdim(c,a));
while(islessgreater(e,c)){
d=acos(a);
b=(fmax(b,f))*(pow(e,d));
a=(fmax(a,f))+(fmin(f,f));
d=sin(a);
a=(fmax(e,d))-(sin(h));
}
}