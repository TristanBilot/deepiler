#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmax(d,a))-(fdim(f,d));
h=(atan2(b,c))+(tan(b));
while(isgreaterequal(a,g)){
e=(pow(c,b))*(log(c));
e=(acos(b))*(atan2(h,f));
c=(fmin(a,e))/(sin(a));
c=(fmax(f,g))/(atan2(c,g));
}
d=(sqrt(b))*(log(c));
e=log10(g);
f=(fmax(e,e))*(floor(d));
a=(atan(b))*(atan2(d,d));
}