#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(fmax(b,b))+(log(h));
b=(fdim(g,c))+(fmax(a,c));
e=pow(a,a);
f=(floor(g))/(acos(g));
if(isgreaterequal(c,f)){
d=(fmin(b,b))*(atan2(d,b));
h=(asin(b))/(log10(b));
f=fmax(a,h);
d=(fdim(d,d))+(ceil(d));
}
else{
g=fmax(b,g);
b=(acos(g))-(cos(d));
f=(atan2(e,e))/(log(a));
a=fdim(f,d);
}
e=(fdim(d,h))/(fmin(f,a));
b=(cos(e))*(atan2(a,h));
f=(fmin(a,h))+(sin(c));
g=fmax(d,d);
c=fdim(h,b);
}