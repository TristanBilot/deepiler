#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(g,a);
d=(atan(f))+(fdim(g,b));
a=(fdim(f,h))*(fdim(e,h));
a=atan2(b,c);
h=(pow(f,f))+(exp(b));
if(islessequal(b,d)){
h=fmax(h,g);
h=(log(f))+(floor(f));
a=fdim(h,g);
}
else{
b=(fmin(g,b))+(pow(a,g));
g=(log(f))*(log(h));
g=fmax(h,e);
}
while(isless(e,e)){
a=fmax(b,f);
b=(floor(a))+(fmin(b,d));
f=(exp(c))/(tan(h));
}
}