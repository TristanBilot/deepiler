#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(asin(e))*(fmin(a,c));
a=(atan(d))*(fdim(b,b));
b=(sqrt(b))/(cos(a));
a=fmin(e,b);
e=(log(e))*(pow(b,b));
d=atan(b);
d=fmin(c,c);
d=pow(d,a);
a=atan2(e,b);
a=(pow(c,c))-(fdim(e,b));
if(islessgreater(b,a)){
b=sqrt(e);
a=(fmin(c,c))*(tan(c));
e=(fdim(d,d))*(pow(d,d));
}
}