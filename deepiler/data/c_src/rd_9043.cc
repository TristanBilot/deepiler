#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(d,c))*(fmax(a,c));
c=(atan2(g,g))+(exp(c));
h=(log(h))*(fmin(b,a));
if(islessgreater(g,h)){
c=pow(g,f);
b=tan(d);
h=(fmax(a,a))/(atan2(b,h));
c=(floor(e))/(log(a));
}
else{
c=fmin(a,c);
e=atan(h);
b=fdim(a,a);
f=(acos(d))*(fdim(e,f));
d=asin(b);
}
if(islessequal(f,e)){
a=(atan2(g,a))*(fdim(a,a));
h=log(h);
h=(exp(g))+(floor(b));
h=(exp(h))+(floor(h));
}
else{
d=floor(h);
a=fmin(a,g);
h=asin(b);
}
}