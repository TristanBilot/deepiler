#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=tan(d);
g=pow(f,d);
b=(fdim(g,e))+(atan2(c,c));
d=log10(e);
a=(fmax(b,c))*(fdim(a,a));
f=(cos(e))/(asin(f));
b=(ceil(c))-(fmax(f,g));
g=log(c);
g=fmax(a,g);
b=(sin(d))/(fdim(c,g));
c=fdim(c,a);
}