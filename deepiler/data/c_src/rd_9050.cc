#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmax(a,d))/(log(f));
d=fmax(c,b);
a=atan(f);
b=pow(c,d);
d=(sqrt(c))*(log(c));
while(islessgreater(b,e)){
f=asin(b);
c=exp(f);
c=(log10(e))*(atan2(e,f));
a=(asin(d))-(log(a));
d=(cos(a))-(cos(d));
}
}