#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=fmax(a,d);
f=(tan(d))/(fdim(c,d));
c=pow(f,b);
f=floor(c);
if(isless(a,a)){
a=sqrt(f);
d=fmax(d,c);
b=fdim(f,d);
c=atan2(e,e);
b=fmax(a,b);
}
else{
e=(exp(a))+(fmin(d,b));
c=sin(c);
b=pow(e,c);
}
if(islessequal(d,a)){
e=(exp(a))+(ceil(a));
c=tan(c);
c=(cos(a))-(acos(d));
a=(fmin(d,a))+(fmin(e,c));
}
}