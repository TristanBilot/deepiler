#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(d,d);
g=(pow(d,e))+(ceil(a));
h=floor(a);
c=(ceil(b))-(fmin(h,b));
f=pow(e,h);
if(isless(g,g)){
g=(sqrt(f))+(floor(g));
f=fmin(a,f);
f=(ceil(d))+(acos(f));
h=(fmax(b,h))/(atan2(a,b));
}
else{
f=exp(e);
f=(atan2(c,a))-(exp(b));
h=(tan(e))/(atan2(a,a));
g=(atan2(e,g))-(atan2(c,d));
h=tan(c);
}
g=fdim(e,f);
h=fmin(e,h);
d=pow(h,d);
g=(asin(f))*(log(g));
}