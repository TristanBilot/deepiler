#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(exp(d))*(log(f));
c=atan2(e,g);
a=(fmax(d,b))-(pow(f,g));
g=sqrt(e);
b=(exp(f))+(asin(g));
if(isless(e,e)){
g=(fmin(b,e))/(acos(b));
c=atan2(f,d);
b=fmax(g,g);
d=(floor(b))-(cos(a));
b=fmax(d,d);
}
f=sin(f);
d=(pow(a,f))/(sin(e));
e=exp(g);
g=(fmin(d,a))*(fmax(a,g));
}