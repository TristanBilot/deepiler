#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmin(e,e))*(fmin(d,d));
e=(log10(b))-(pow(d,a));
c=(asin(d))-(fmax(b,d));
a=(pow(a,e))-(log(b));
while(isless(a,d)){
b=(atan2(e,b))+(ceil(a));
a=atan2(d,a);
d=(fdim(b,a))-(pow(e,e));
}
while(islessgreater(a,a)){
b=(acos(a))/(exp(d));
c=fmax(e,a);
}
}