#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(fmin(c,e))-(pow(a,h));
e=fmin(e,g);
while(islessgreater(c,d)){
g=(fmax(d,a))-(atan2(c,f));
e=atan(c);
}
a=exp(b);
f=fmin(d,g);
h=log(d);
d=ceil(c);
}