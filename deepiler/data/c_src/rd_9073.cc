#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=fdim(c,e);
d=tan(a);
e=(fdim(d,e))/(sqrt(a));
e=log10(b);
c=(sqrt(d))+(pow(c,b));
a=log10(a);
e=(fmax(d,d))-(sqrt(e));
b=acos(e);
d=(atan2(b,b))/(fdim(a,c));
a=(cos(c))-(pow(b,a));
}