#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(c,c))-(fmin(a,e));
d=tan(e);
d=(fmax(a,b))*(fdim(a,e));
c=(fdim(e,a))*(fmax(e,c));
b=tan(a);
if(isgreaterequal(d,e)){
b=fmin(e,a);
d=(log(b))*(sin(b));
}
else{
c=(fdim(a,b))/(atan2(c,b));
c=(asin(c))/(acos(b));
b=(atan2(b,a))+(atan(d));
d=(atan2(d,a))/(atan2(c,a));
c=(asin(e))+(floor(b));
}
while(islessgreater(b,c)){
e=exp(e);
a=(cos(b))*(pow(e,d));
a=(atan(b))/(fmax(e,a));
a=fmax(c,b);
}
}