#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=exp(d);
a=(cos(e))/(exp(c));
d=pow(d,e);
while(islessequal(e,a)){
d=(fmin(c,c))+(fmin(c,a));
e=log(c);
b=log(d);
a=floor(e);
a=fdim(b,b);
}
if(islessequal(b,d)){
e=fmax(a,d);
a=(atan2(e,b))*(fdim(e,c));
}
}