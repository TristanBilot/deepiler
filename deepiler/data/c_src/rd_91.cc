#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(c,f);
e=(atan2(f,f))-(cos(b));
e=(fdim(a,c))/(floor(e));
d=pow(f,b);
a=fdim(b,a);
f=(exp(a))/(cos(b));
if(isless(e,a)){
c=(fdim(a,d))*(log10(e));
e=sqrt(e);
}
else{
e=fmax(f,c);
d=(pow(a,f))-(sin(c));
f=(fmin(b,c))+(pow(d,e));
}
}