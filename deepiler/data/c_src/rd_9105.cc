#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmin(c,b);
b=fmax(a,a);
b=(log(d))*(atan(a));
c=asin(e);
if(isgreaterequal(b,b)){
c=sqrt(b);
b=acos(c);
b=(fmin(e,c))-(exp(c));
}
if(isless(c,b)){
e=(log(e))*(acos(b));
c=atan(d);
c=(exp(a))/(acos(a));
d=fdim(b,a);
}
else{
b=fmin(b,d);
a=log10(d);
a=fmax(a,e);
}
}