#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=exp(a);
c=(fmin(d,c))+(log10(a));
b=(log10(b))+(floor(b));
e=fmax(b,d);
if(islessgreater(b,b)){
c=(fmin(a,d))*(fmax(b,e));
e=asin(a);
}
e=pow(f,e);
f=(log(e))-(sin(b));
d=(acos(d))+(atan2(f,e));
b=(sin(c))*(atan(f));
}