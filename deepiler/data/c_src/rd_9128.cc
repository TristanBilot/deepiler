#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=exp(a);
b=fmax(e,c);
e=(floor(c))/(exp(f));
e=acos(d);
a=(atan(a))/(exp(f));
if(islessgreater(d,c)){
c=cos(b);
d=(sqrt(c))*(exp(c));
d=log10(c);
e=(ceil(c))+(atan(e));
}
if(islessequal(b,c)){
e=fmax(f,a);
f=fmax(a,c);
d=(atan2(e,e))*(asin(d));
f=(exp(d))+(log(c));
}
}