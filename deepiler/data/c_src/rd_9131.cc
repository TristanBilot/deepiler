#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmax(b,d))+(cos(a));
e=(pow(d,c))/(atan(b));
b=(fdim(e,a))+(log10(e));
d=log(c);
b=cos(d);
if(isless(d,e)){
c=pow(a,a);
e=atan(d);
c=atan(d);
e=fdim(b,b);
}
if(islessgreater(e,b)){
b=acos(a);
d=(cos(c))+(log10(c));
e=(fdim(e,a))/(pow(e,b));
d=(sqrt(b))/(asin(c));
b=(atan2(e,c))*(fmax(d,d));
}
else{
d=(asin(c))*(pow(a,b));
c=fdim(e,b);
c=ceil(e);
}
}