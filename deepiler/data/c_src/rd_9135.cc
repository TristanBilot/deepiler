#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan2(e,d);
c=(floor(d))/(acos(a));
e=fmax(a,a);
c=(log(d))*(fmin(b,a));
a=(fdim(d,a))-(floor(d));
b=(sqrt(a))/(atan(c));
while(isless(c,a)){
d=(pow(c,a))-(cos(e));
e=tan(a);
e=acos(d);
b=(floor(e))-(cos(e));
}
}