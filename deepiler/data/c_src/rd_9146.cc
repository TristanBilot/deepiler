#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(e,a))/(atan2(c,f));
d=sin(g);
c=sqrt(b);
d=(log(c))+(atan2(a,e));
g=(fmax(b,e))*(fmax(d,d));
if(isgreaterequal(b,d)){
e=log10(c);
d=cos(a);
g=(fmax(a,d))+(fdim(b,d));
d=(exp(d))-(pow(g,a));
c=fdim(e,c);
}
if(islessequal(d,a)){
d=(atan2(a,e))-(fdim(f,g));
g=(fmin(e,b))+(atan2(d,b));
g=atan2(d,a);
b=(exp(a))/(tan(b));
g=exp(f);
}
else{
d=atan2(b,b);
e=fdim(g,a);
}
}