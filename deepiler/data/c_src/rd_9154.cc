#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fdim(c,e))/(atan2(c,e));
f=(pow(e,a))-(atan(b));
b=sin(g);
while(islessequal(f,b)){
b=fmin(a,c);
f=(atan2(e,b))/(fdim(b,g));
d=sqrt(a);
c=acos(b);
}
a=ceil(c);
c=acos(e);
f=fdim(f,f);
}