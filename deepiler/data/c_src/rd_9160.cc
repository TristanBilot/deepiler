#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fdim(b,d);
c=sqrt(c);
a=fmin(b,b);
e=log(a);
if(isless(b,e)){
d=fmax(b,c);
c=(fmin(b,b))/(fdim(a,a));
c=floor(c);
d=atan(b);
b=(sqrt(b))-(log10(b));
}
while(isgreaterequal(b,b)){
c=fdim(a,b);
a=(floor(c))/(fmax(a,e));
b=(exp(a))+(acos(d));
e=(sin(b))/(atan2(d,e));
}
}