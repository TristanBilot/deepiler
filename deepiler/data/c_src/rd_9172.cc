#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(log(a))*(pow(f,f));
f=exp(d);
e=atan2(d,a);
a=ceil(e);
if(isless(d,e)){
f=(atan2(e,a))-(cos(a));
f=(atan2(f,a))/(pow(d,f));
f=sqrt(e);
c=pow(b,f);
b=floor(d);
}
while(islessequal(c,a)){
a=sqrt(e);
d=(fmax(a,b))+(cos(c));
c=acos(d);
f=floor(c);
}
}