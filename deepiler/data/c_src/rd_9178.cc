#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(d,b))/(log(e));
g=(fmin(a,e))*(fmax(e,f));
a=(sqrt(a))+(fdim(e,h));
a=(atan(a))+(fmax(a,a));
if(islessequal(d,a)){
g=pow(d,f);
c=tan(e);
g=sqrt(g);
d=(pow(c,b))*(sqrt(h));
h=exp(e);
}
while(islessgreater(c,e)){
b=ceil(g);
c=(exp(g))+(atan(b));
}
}