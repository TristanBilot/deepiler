#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(d);
e=atan2(e,f);
while(isless(f,b)){
g=fmax(a,a);
g=sqrt(e);
}
if(isless(e,e)){
g=atan(c);
e=cos(g);
e=pow(b,e);
c=pow(d,b);
g=sqrt(c);
}
else{
e=(exp(c))*(fmax(d,b));
e=floor(f);
c=(log10(a))*(exp(g));
b=(fdim(g,f))*(log10(b));
f=fdim(c,e);
}
}