#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(e);
a=sin(d);
h=floor(b);
while(islessequal(h,g)){
f=fmax(e,g);
b=(fmax(h,a))-(acos(c));
}
if(isless(f,e)){
f=atan2(d,b);
e=(fmax(g,h))-(atan2(b,d));
b=sin(g);
h=pow(c,e);
}
else{
f=ceil(e);
d=pow(d,a);
d=atan2(d,b);
e=(exp(c))+(fmin(g,e));
}
}