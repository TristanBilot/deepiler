#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=fmax(e,d);
a=(fmax(c,b))+(fmax(a,a));
a=(tan(d))-(fdim(d,b));
c=pow(b,d);
e=(atan(a))+(pow(a,d));
b=acos(d);
e=pow(a,c);
d=(fmax(d,a))+(log(c));
a=fmin(a,a);
a=(sqrt(b))/(atan2(c,d));
c=(atan2(d,a))/(fdim(a,c));
}