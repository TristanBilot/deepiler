#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(a,a))*(fmin(e,b));
b=pow(b,b);
while(isless(d,c)){
e=(asin(c))-(atan2(a,a));
e=(acos(c))/(acos(c));
d=ceil(b);
}
if(islessequal(a,e)){
e=(asin(e))-(atan2(a,d));
d=pow(c,a);
c=sqrt(b);
c=(fmin(b,a))+(pow(c,b));
d=(cos(d))/(fmin(a,e));
}
}