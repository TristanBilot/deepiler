#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(b,g);
a=(tan(a))/(asin(h));
h=pow(b,d);
d=pow(a,d);
e=exp(a);
while(islessgreater(b,b)){
f=atan2(b,b);
h=fmax(g,f);
b=(atan2(g,a))/(pow(a,h));
g=(tan(g))*(exp(g));
}
g=(atan2(h,a))/(atan(c));
a=(atan2(e,g))/(acos(h));
c=(log(a))-(fdim(g,e));
f=(acos(c))-(log(b));
}