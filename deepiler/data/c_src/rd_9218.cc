#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(atan2(e,b))-(atan(a));
d=atan(a);
b=fmax(b,c);
e=(sin(e))/(sin(b));
while(islessgreater(a,b)){
c=(sin(b))-(log10(e));
a=(sin(a))+(exp(b));
b=acos(c);
e=(atan2(d,e))*(fmax(e,e));
e=(atan2(e,c))+(fmax(e,e));
}
if(islessgreater(e,c)){
c=fmax(b,b);
d=log(a);
b=exp(d);
d=(log(a))/(floor(d));
}
else{
e=atan2(c,d);
b=sin(a);
b=atan2(e,a);
b=(atan2(e,e))*(asin(d));
}
}