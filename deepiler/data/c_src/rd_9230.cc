#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(atan2(c,c))-(fmax(e,a));
a=(fmin(e,h))/(tan(f));
e=(fmin(c,e))+(fdim(a,e));
d=cos(c);
f=(fmin(h,h))+(asin(a));
h=(fmin(c,a))+(sqrt(e));
e=(pow(d,b))*(fdim(a,c));
d=(fmin(c,e))*(acos(b));
if(isless(f,a)){
e=(pow(g,a))/(sin(e));
a=fdim(a,g);
}
else{
c=(fmax(e,a))+(sqrt(c));
b=(fmax(d,h))-(acos(h));
}
}