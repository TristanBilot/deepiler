#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=log(d);
g=(fdim(g,d))/(pow(f,b));
d=atan2(d,g);
f=(fmax(b,f))+(atan2(a,e));
if(isless(e,b)){
b=tan(c);
a=sin(e);
a=floor(c);
e=floor(c);
f=(atan2(f,f))+(pow(d,a));
}
a=log10(g);
e=log10(d);
c=atan2(g,a);
b=(log(a))+(fmin(c,f));
}