#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(c);
e=(log(f))*(exp(a));
c=(cos(a))/(fdim(a,a));
g=fmin(g,c);
g=(tan(f))-(fmax(b,e));
e=(floor(b))-(atan2(f,a));
f=(fmin(e,a))*(exp(c));
while(isless(e,g)){
g=(fmax(e,f))*(asin(e));
b=(asin(e))*(fmin(g,b));
a=(fdim(c,a))+(sqrt(c));
}
}