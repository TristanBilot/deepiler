#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(d,b);
a=(fmin(c,c))-(fdim(a,e));
e=sin(g);
if(islessequal(e,g)){
e=(fmax(a,b))+(sin(a));
b=(cos(d))+(exp(a));
e=fdim(c,c);
}
else{
f=(cos(e))+(sin(b));
e=(log(d))-(log(f));
}
while(islessequal(c,c)){
e=pow(g,e);
c=(fmin(c,d))-(atan(e));
b=(sqrt(e))-(pow(g,b));
}
}