#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(log(a))*(fmax(e,b));
e=sqrt(e);
b=pow(e,b);
d=fmax(a,c);
while(isless(c,f)){
b=(atan2(d,e))-(pow(d,b));
e=log10(e);
c=(fmax(f,c))+(fdim(d,c));
d=pow(a,b);
b=(ceil(f))*(tan(a));
}
while(islessequal(f,a)){
c=exp(a);
f=(sqrt(c))+(fmin(a,a));
}
}