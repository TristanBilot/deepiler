#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=fmin(e,f);
d=fmin(c,c);
a=pow(a,b);
e=(atan2(b,d))/(fmin(b,c));
d=asin(d);
f=(log10(e))*(fmin(c,f));
f=(fmin(d,b))*(acos(b));
}