#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(fmin(e,f))/(log(c));
b=log10(d);
d=atan2(c,c);
f=(log10(a))+(floor(e));
while(isless(g,a)){
b=fmax(d,b);
d=(sqrt(d))*(sqrt(e));
e=tan(c);
}
if(islessequal(b,f)){
c=fdim(c,d);
g=pow(c,f);
f=tan(c);
b=fmin(e,f);
}
}