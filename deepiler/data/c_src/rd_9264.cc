#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(tan(e))*(fmax(h,d));
b=(cos(f))/(fdim(d,a));
h=(acos(e))/(log10(f));
g=(fmin(c,g))*(cos(b));
while(isless(a,a)){
b=(log(f))+(log10(b));
e=(fmin(b,g))/(exp(d));
g=cos(b);
g=fdim(g,g);
}
a=(fmax(a,g))*(log(a));
c=(fmax(e,g))*(fmax(g,g));
e=(fmax(h,b))+(acos(f));
}