#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(b,b))-(acos(d));
c=fmax(a,g);
a=exp(b);
while(islessequal(a,e)){
e=atan2(b,d);
g=sin(c);
c=(sin(b))-(ceil(e));
}
g=fmax(f,b);
f=(ceil(c))/(floor(a));
h=(sin(b))*(pow(g,h));
h=fmin(d,b);
}