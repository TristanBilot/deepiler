#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=atan(e);
c=fdim(b,a);
b=ceil(a);
if(isgreaterequal(a,b)){
e=(floor(e))/(fdim(b,b));
c=sqrt(c);
e=(tan(a))/(exp(b));
}
else{
e=(cos(e))*(sqrt(a));
d=(acos(c))/(exp(b));
}
d=pow(d,e);
b=sqrt(e);
b=log(a);
a=(fmax(d,e))*(log10(a));
b=(exp(b))-(pow(c,c));
}