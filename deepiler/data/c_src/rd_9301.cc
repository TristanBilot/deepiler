#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(e,f);
d=ceil(d);
b=exp(g);
if(islessequal(h,f)){
a=(acos(h))-(atan2(g,d));
h=(floor(h))+(fdim(d,f));
b=(pow(h,g))*(sqrt(h));
g=(fmax(b,g))/(log10(e));
}
else{
e=cos(h);
c=ceil(b);
b=(fdim(c,f))/(fmin(g,a));
h=fmax(g,f);
}
if(isless(g,b)){
g=(atan2(g,h))+(pow(d,f));
d=fdim(g,b);
g=(pow(g,c))-(pow(d,h));
c=(pow(a,b))/(atan2(d,a));
h=(log(g))+(fmax(a,f));
}
else{
d=(pow(a,b))*(sin(b));
b=(atan2(a,e))-(pow(g,h));
e=floor(a);
a=asin(c);
b=tan(d);
}
}