#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(fmin(d,f))*(sqrt(b));
a=fmax(b,f);
a=(fdim(g,c))+(log10(f));
f=tan(a);
a=fdim(b,a);
g=(cos(e))+(sqrt(d));
a=cos(d);
c=fmin(b,g);
f=(log(g))*(fmax(c,b));
e=fmax(d,g);
f=fmax(f,e);
}