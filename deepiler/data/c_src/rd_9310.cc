#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=atan2(c,c);
e=(fdim(b,d))-(sin(b));
d=tan(d);
a=(fdim(a,d))/(sin(e));
while(islessgreater(d,e)){
e=fmin(a,e);
d=(log10(c))-(pow(a,e));
b=(atan(b))/(sin(e));
c=fmin(b,a);
c=sqrt(b);
}
if(islessgreater(c,e)){
c=(fmax(c,d))+(pow(d,a));
c=(exp(e))-(exp(e));
c=(fmax(e,c))/(fmin(d,a));
e=(acos(d))-(atan2(a,d));
d=(fdim(d,c))/(ceil(a));
}
else{
b=(fdim(a,d))-(fmin(e,c));
e=floor(c);
}
}