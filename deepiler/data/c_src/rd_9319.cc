#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(atan2(g,e))-(fmax(f,e));
e=cos(h);
e=fmax(e,b);
d=asin(b);
d=(fmin(b,e))+(atan(g));
g=(pow(e,c))*(asin(f));
b=ceil(g);
while(isless(a,d)){
h=acos(d);
d=(ceil(g))-(cos(b));
c=atan(g);
g=(fdim(f,d))+(tan(h));
e=(floor(c))/(fmax(d,g));
}
}