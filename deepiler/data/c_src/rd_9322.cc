#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(cos(e))/(fdim(e,a));
c=(tan(d))-(asin(c));
d=(log10(b))+(floor(c));
a=(log10(d))-(sqrt(a));
e=sin(e);
if(isgreaterequal(c,b)){
a=(fmax(d,b))*(fdim(c,e));
a=(sqrt(b))/(exp(c));
a=pow(d,d);
a=(exp(e))-(atan2(a,e));
c=floor(d);
}
while(isgreaterequal(c,c)){
c=(log10(c))/(asin(e));
e=(fmax(b,a))/(fmin(d,a));
}
}