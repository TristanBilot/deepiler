#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=fmax(c,d);
b=tan(a);
e=(fmax(a,e))+(log(e));
if(islessequal(b,b)){
e=acos(b);
a=(fmax(a,c))/(fmin(d,c));
d=(pow(a,e))-(fdim(c,a));
c=asin(b);
b=acos(d);
}
else{
a=(fmax(e,a))/(asin(c));
d=(atan2(a,b))*(log(b));
c=fmin(b,d);
b=(atan2(b,a))*(atan2(b,a));
}
if(isless(b,a)){
e=tan(a);
b=(pow(d,a))*(fmax(a,b));
}
else{
e=(pow(a,b))-(acos(d));
a=(asin(d))+(pow(b,e));
}
}