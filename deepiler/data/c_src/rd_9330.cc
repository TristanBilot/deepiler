#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=pow(d,a);
d=(floor(e))-(floor(a));
while(isless(c,f)){
e=fmin(c,c);
d=(asin(c))*(pow(e,a));
}
while(isgreaterequal(e,f)){
b=(fmin(d,d))*(sqrt(c));
c=atan2(c,e);
d=fdim(a,c);
}
}