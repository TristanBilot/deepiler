#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=exp(b);
c=atan2(d,a);
d=(log10(c))/(exp(e));
a=(atan2(e,e))-(cos(d));
a=sin(b);
e=acos(c);
b=(fdim(c,e))*(cos(e));
while(islessgreater(d,a)){
e=tan(b);
d=atan(e);
b=(fmax(c,a))*(atan(e));
}
}