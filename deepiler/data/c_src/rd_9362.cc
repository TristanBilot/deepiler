#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(pow(b,e))/(fmax(b,b));
b=(pow(c,d))/(pow(c,d));
e=(fmin(b,a))*(atan(a));
while(isgreaterequal(c,b)){
d=tan(c);
b=atan(e);
c=acos(d);
e=asin(d);
}
b=floor(c);
e=(fdim(d,c))*(floor(b));
e=atan(e);
e=(atan2(e,a))+(atan2(c,c));
e=(fmax(a,b))*(log(c));
}