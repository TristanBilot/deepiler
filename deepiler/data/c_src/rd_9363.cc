#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(fmin(b,d))/(sqrt(b));
e=(fdim(f,c))*(pow(c,c));
f=(tan(d))+(pow(d,d));
g=(floor(a))/(sin(f));
d=atan2(d,g);
a=(fmax(c,f))+(pow(a,f));
f=fmin(a,f);
f=sqrt(d);
g=log10(g);
while(islessgreater(a,g)){
e=(ceil(g))/(fmin(c,d));
c=fdim(e,f);
}
}