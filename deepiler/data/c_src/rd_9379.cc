#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=fdim(b,c);
a=fdim(b,a);
e=(fdim(d,d))/(fmin(d,e));
b=fmin(c,b);
c=fdim(c,d);
while(isgreaterequal(c,e)){
c=(atan2(c,a))/(pow(e,e));
a=(pow(e,a))*(fdim(c,d));
a=atan2(d,a);
a=exp(a);
d=floor(d);
}
while(islessgreater(d,c)){
e=fdim(b,c);
d=atan(a);
d=log(b);
}
}