#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(c,e))-(exp(e));
f=(atan2(a,e))/(atan(a));
c=(log10(f))/(pow(c,c));
a=(fdim(b,b))-(fmin(c,e));
if(isless(a,f)){
f=(log(a))-(sqrt(b));
b=log10(a);
c=cos(e);
c=(fdim(e,c))*(atan2(d,a));
}
else{
f=(floor(f))-(fdim(f,d));
f=fdim(d,f);
b=fmin(d,b);
c=fmax(f,b);
}
if(islessequal(e,c)){
d=(fmin(c,b))-(log(d));
b=(sqrt(f))*(fmax(f,a));
}
}