#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fdim(h,b);
f=(atan(e))*(log10(d));
f=(fmax(g,d))/(fmax(f,d));
h=(floor(b))+(atan2(g,h));
e=floor(f);
c=(acos(f))+(fmax(c,d));
g=fmin(d,f);
a=tan(e);
c=(fmin(d,d))/(fmin(d,a));
f=pow(a,c);
if(islessgreater(f,h)){
c=atan2(d,b);
f=log(f);
b=fmax(a,c);
}
else{
b=(fmin(e,e))*(floor(h));
e=fdim(c,h);
g=atan2(h,g);
a=(fmax(g,b))/(sqrt(g));
}
}