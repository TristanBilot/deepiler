#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(floor(c))+(fdim(b,d));
a=floor(b);
c=(fdim(a,a))*(fmax(e,d));
if(isgreaterequal(e,c)){
d=tan(c);
e=(log(c))*(fmin(a,d));
}
if(isless(b,a)){
d=fmin(d,a);
e=atan2(d,d);
a=cos(a);
b=fmin(d,e);
b=(pow(c,e))/(cos(e));
}
}