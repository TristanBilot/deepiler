#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(exp(d))*(atan2(c,d));
b=(ceil(c))*(atan(d));
c=floor(a);
c=fmax(a,a);
if(isless(d,c)){
e=(fdim(b,c))/(log10(b));
b=(ceil(b))+(pow(c,a));
e=exp(a);
}
c=acos(e);
c=(log10(a))+(ceil(a));
a=sin(e);
}