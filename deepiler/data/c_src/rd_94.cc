#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(fmax(b,d))-(pow(a,c));
e=exp(c);
d=(fmin(b,b))-(atan2(e,b));
c=(log10(d))-(fmin(c,c));
a=(fdim(b,b))*(fmin(a,d));
if(islessequal(b,a)){
b=(sqrt(c))*(sin(b));
a=fmin(b,b);
b=(fmin(d,b))+(floor(a));
}
e=(fmin(d,b))+(sin(e));
e=(pow(b,d))+(fmin(e,d));
c=(tan(d))*(tan(b));
b=pow(a,e);
a=pow(c,a);
}