#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(a,b);
e=(acos(a))+(log10(e));
d=fmax(b,b);
c=(fmax(b,c))-(fmin(a,d));
if(isgreaterequal(b,a)){
c=atan2(e,a);
a=(fmax(d,b))/(fmin(a,a));
a=log10(c);
c=acos(c);
}
else{
c=(pow(a,b))-(pow(a,e));
d=(atan2(d,c))-(ceil(c));
c=atan2(e,a);
d=fmax(c,d);
}
c=fmin(e,c);
b=atan2(b,b);
c=acos(c);
b=pow(c,d);
}