#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(ceil(d))+(pow(e,e));
d=(fdim(a,b))+(exp(e));
c=(fdim(d,d))/(log(c));
while(islessgreater(b,b)){
c=ceil(a);
e=(fmax(b,e))*(fdim(d,c));
c=(log10(b))/(fdim(d,d));
e=(pow(c,a))+(exp(c));
c=pow(e,d);
}
if(islessgreater(b,b)){
b=(exp(c))*(sin(b));
b=(fmin(b,a))/(sin(e));
d=(atan2(c,b))*(atan2(d,c));
b=floor(b);
c=(pow(e,b))/(exp(d));
}
else{
b=(atan(a))*(atan2(c,d));
b=log10(c);
c=sin(b);
a=atan2(e,b);
d=(pow(e,c))*(fdim(c,b));
}
}