#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(floor(b))-(atan2(c,f));
e=ceil(e);
f=log10(b);
e=fmax(b,d);
b=(atan2(f,a))+(pow(f,c));
e=asin(b);
d=exp(e);
d=(log10(d))/(pow(e,f));
e=(sqrt(c))/(sin(b));
b=(log10(a))*(ceil(e));
b=(asin(f))/(acos(e));
c=fmax(b,b);
}