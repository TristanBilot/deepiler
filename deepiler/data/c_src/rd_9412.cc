#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(pow(g,c))/(fdim(c,b));
c=pow(h,a);
a=asin(f);
h=fmin(f,a);
while(islessgreater(e,c)){
c=(asin(e))-(fdim(b,f));
g=fdim(f,b);
a=tan(d);
b=atan2(f,e);
}
while(isless(h,c)){
a=sqrt(e);
d=pow(g,g);
f=fmax(a,h);
e=(acos(h))-(sin(d));
g=(fdim(c,h))*(asin(a));
}
}