#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=floor(d);
c=(fmin(b,g))/(fmin(f,g));
f=cos(e);
while(isless(f,c)){
g=atan2(g,a);
g=(fmax(b,e))-(pow(f,f));
}
a=(log10(b))-(atan2(f,f));
d=(pow(d,a))-(pow(a,b));
a=fmax(f,e);
b=(ceil(c))/(fmin(a,c));
f=fmin(c,c);
}