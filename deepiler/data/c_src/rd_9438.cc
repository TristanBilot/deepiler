#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(c,c))*(sin(b));
d=(fdim(a,c))/(fdim(c,c));
c=(ceil(c))*(asin(d));
if(islessequal(b,a)){
e=(fmax(a,d))*(fmax(a,c));
b=fdim(e,e);
}
if(isgreaterequal(e,c)){
c=(ceil(e))+(pow(b,a));
a=(atan2(c,c))-(fmin(a,e));
d=pow(e,a);
}
else{
d=fmin(d,a);
d=atan2(d,b);
b=(log10(a))-(ceil(b));
c=(pow(e,c))/(fmax(c,d));
b=(ceil(d))*(fmax(a,a));
}
}