#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(exp(d))+(pow(a,a));
b=(pow(c,c))*(fmax(c,a));
e=(acos(d))-(fmin(d,b));
a=(sin(d))-(tan(d));
while(islessgreater(b,c)){
a=(pow(c,a))+(pow(e,c));
c=(atan2(b,e))+(floor(e));
e=fmin(d,a);
}
}