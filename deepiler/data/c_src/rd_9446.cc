#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(a);
c=(fdim(a,b))*(atan2(b,c));
a=(tan(e))+(tan(c));
while(islessgreater(e,a)){
e=(floor(d))/(fmin(d,a));
c=sin(b);
}
if(isless(b,e)){
a=fdim(e,b);
a=sqrt(a);
e=(cos(e))+(floor(e));
e=(sqrt(a))+(asin(c));
c=(atan2(d,e))/(fdim(a,d));
}
else{
b=fmax(e,b);
a=(log10(a))+(atan2(c,e));
d=(pow(b,c))*(floor(b));
d=atan2(d,e);
d=(exp(d))*(floor(c));
}
}