#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,c);
h=(asin(c))-(fmin(a,c));
while(islessequal(c,h)){
f=sin(d);
h=(fdim(c,a))*(atan(c));
a=log10(e);
d=(asin(a))/(ceil(e));
}
if(islessequal(h,a)){
a=sin(f);
a=fmin(h,b);
}
}