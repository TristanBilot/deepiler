#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fdim(d,a))-(fdim(b,e));
d=(ceil(c))*(exp(g));
d=atan2(b,d);
c=tan(c);
h=ceil(a);
while(isless(b,d)){
g=(pow(c,d))-(fmax(a,e));
b=(cos(f))*(exp(e));
c=cos(c);
h=(fmin(b,e))*(pow(h,f));
a=fmin(b,b);
}
a=pow(a,b);
c=(fdim(a,e))*(pow(d,b));
f=(fdim(d,h))-(atan2(g,e));
e=(log(b))*(pow(c,e));
g=pow(e,e);
}