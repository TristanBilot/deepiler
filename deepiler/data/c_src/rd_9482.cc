#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmin(b,b))+(fdim(e,b));
f=pow(d,a);
while(islessgreater(f,e)){
a=(pow(a,e))-(fmax(d,c));
f=(log(e))/(atan2(c,c));
d=(pow(b,b))-(pow(f,e));
e=fmax(a,d);
f=(sin(a))*(acos(d));
}
g=(log10(g))/(ceil(f));
e=(log(e))-(sin(d));
b=(log(b))*(fmax(b,a));
b=floor(d);
f=fmax(b,b);
}