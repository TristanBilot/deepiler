#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=pow(b,c);
f=(sin(d))+(fmin(f,f));
f=(ceil(b))*(pow(e,c));
b=(log(a))+(fmin(d,b));
f=(fdim(c,b))/(atan2(c,a));
if(isless(e,d)){
e=(ceil(f))/(sin(e));
d=(fdim(g,c))*(cos(c));
g=pow(b,b);
}
else{
g=(log(g))+(floor(g));
d=atan2(a,e);
f=(fdim(a,b))+(asin(e));
e=(sqrt(c))/(pow(e,c));
}
if(islessequal(c,d)){
d=(log10(b))/(pow(f,e));
f=(floor(a))*(log(c));
g=(log(e))/(sin(e));
}
else{
f=fmin(d,c);
g=(asin(b))/(acos(g));
f=log(e);
e=log10(g);
}
}