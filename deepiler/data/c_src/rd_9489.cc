#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(asin(f))*(atan(g));
c=floor(g);
b=pow(g,d);
a=(log10(b))+(atan2(a,b));
c=fmax(e,d);
c=pow(a,b);
a=(fdim(a,d))/(sin(a));
e=sin(d);
e=log(g);
b=(pow(a,c))+(ceil(b));
if(isless(c,c)){
c=(sqrt(a))/(fmax(e,c));
c=asin(c);
g=(fdim(d,a))*(log10(d));
b=atan(b);
}
else{
d=asin(a);
b=(sin(g))*(asin(d));
}
}