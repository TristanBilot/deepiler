#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=(pow(h,h))/(fmin(c,d));
e=sqrt(g);
g=(pow(b,e))-(pow(a,e));
h=pow(d,c);
b=atan2(g,g);
h=(log10(f))*(log10(f));
c=atan2(g,h);
c=fmin(h,c);
h=sqrt(c);
while(islessequal(a,g)){
c=fmin(c,f);
g=(log(d))/(floor(h));
h=pow(a,h);
}
}