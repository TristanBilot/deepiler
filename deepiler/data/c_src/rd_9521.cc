#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(g);
f=fmin(h,h);
b=(fdim(d,f))-(fmax(a,b));
g=(floor(d))*(floor(c));
h=(pow(a,f))/(ceil(c));
e=(fmin(b,a))*(cos(b));
f=fmax(d,c);
if(isless(a,g)){
f=fmin(a,c);
c=(exp(b))-(exp(e));
a=(fdim(f,g))+(pow(h,h));
d=(pow(b,h))*(sqrt(b));
}
else{
a=fmax(g,c);
h=(tan(b))+(fmin(f,a));
a=(pow(e,g))*(floor(f));
}
}