#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(asin(b))*(fmax(a,a));
e=fmax(d,b);
f=(atan2(e,a))+(atan2(d,h));
g=atan2(f,f);
b=fmax(c,f);
while(isgreaterequal(e,c)){
f=pow(h,b);
a=fmin(f,b);
h=(log10(h))/(atan2(g,d));
}
b=(fdim(e,a))-(sin(a));
g=exp(f);
e=(tan(b))-(fmin(g,b));
c=ceil(g);
}