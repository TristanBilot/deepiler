#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(pow(b,d))+(tan(b));
g=(fdim(e,c))/(fmax(c,f));
d=(sqrt(c))+(fdim(a,c));
f=floor(g);
d=(pow(a,g))-(pow(f,f));
f=acos(d);
while(isless(d,b)){
f=(asin(b))/(fmin(b,e));
e=(floor(e))-(fdim(g,b));
c=(pow(a,b))+(tan(a));
}
}