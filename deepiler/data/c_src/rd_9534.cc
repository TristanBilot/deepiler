#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=tan(c);
g=(fmax(b,e))-(atan2(f,d));
e=(pow(e,f))/(pow(e,b));
if(isless(f,c)){
b=fmax(d,d);
g=sqrt(b);
b=log(f);
f=ceil(c);
d=pow(f,d);
}
if(islessgreater(e,f)){
a=fmin(b,b);
g=(pow(d,d))/(log10(a));
e=log10(c);
a=floor(e);
}
else{
e=(atan2(g,c))+(fmin(b,g));
c=atan2(e,b);
}
}