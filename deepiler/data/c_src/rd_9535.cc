#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=sin(f);
f=(log10(a))*(pow(d,c));
e=(pow(d,f))*(floor(f));
e=(fmax(f,c))+(log(b));
b=fdim(e,e);
if(isless(b,b)){
d=(fmin(c,b))+(cos(f));
c=log10(d);
a=atan2(e,c);
}
}