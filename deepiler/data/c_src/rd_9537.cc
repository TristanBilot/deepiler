#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(b,c);
c=cos(h);
if(islessequal(b,h)){
e=(sin(a))+(fmax(f,c));
g=atan(h);
e=(acos(c))+(fmax(c,f));
}
else{
c=ceil(e);
g=cos(g);
g=(cos(h))-(atan2(a,f));
h=(fdim(g,c))*(atan(b));
c=(log10(f))+(ceil(d));
}
while(isgreaterequal(g,d)){
d=(pow(h,c))-(asin(e));
h=(fmin(b,d))*(fmax(a,g));
h=(pow(h,e))-(fmax(b,c));
a=(atan2(b,h))/(acos(b));
h=(asin(e))*(fdim(a,a));
}
}