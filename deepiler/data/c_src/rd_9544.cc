#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=pow(e,b);
a=cos(e);
a=(floor(e))*(atan2(d,a));
while(islessgreater(a,a)){
e=(tan(c))*(pow(d,b));
b=(log10(a))*(floor(a));
c=(pow(c,a))-(atan(d));
d=(floor(b))+(log10(a));
}
e=ceil(a);
c=fdim(e,d);
d=log10(b);
d=(atan2(d,d))-(fdim(b,b));
}