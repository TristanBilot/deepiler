#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(a,b);
e=(ceil(c))/(fmax(g,a));
g=(atan2(d,a))/(exp(f));
a=(sin(d))+(fmin(g,e));
e=cos(f);
b=(tan(f))-(ceil(e));
d=(sqrt(d))/(exp(c));
g=fmax(c,h);
h=(pow(d,h))*(atan2(f,a));
e=(fdim(b,h))/(floor(b));
while(isgreaterequal(f,g)){
d=floor(e);
c=(sqrt(h))*(fdim(e,f));
h=(pow(g,c))+(log10(d));
f=fmax(g,c);
}
}