#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(sin(a))/(log(a));
b=(cos(d))/(fdim(c,a));
c=fmax(b,a);
a=tan(e);
e=fdim(b,c);
d=(cos(e))/(fdim(a,a));
c=(fdim(a,c))+(fmax(e,c));
while(isgreaterequal(d,c)){
a=(atan2(e,e))-(fmin(d,c));
c=fmax(b,d);
e=(fmin(b,a))+(sin(b));
c=atan2(d,c);
}
}