#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=tan(a);
b=(acos(a))+(fmax(b,f));
f=atan2(g,g);
c=(cos(b))*(exp(a));
a=cos(e);
e=(asin(d))+(pow(c,g));
d=sin(d);
if(isless(f,g)){
f=(log10(g))+(fdim(a,f));
c=fmin(f,f);
g=(sqrt(d))*(sin(f));
a=(pow(e,c))-(fdim(b,c));
}
}