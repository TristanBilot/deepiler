#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(sqrt(d))/(pow(a,f));
d=log10(a);
f=log(b);
d=fmax(b,a);
a=atan2(b,b);
e=atan(c);
a=atan(f);
if(isgreaterequal(d,d)){
a=sqrt(b);
e=fdim(f,a);
d=(log10(f))*(tan(c));
f=(fmin(e,d))/(fmin(c,c));
}
else{
c=tan(c);
c=(fmax(c,c))/(pow(c,b));
f=fdim(f,e);
f=(ceil(a))-(atan(a));
f=fmax(b,b);
}
}