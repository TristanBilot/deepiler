#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(fmax(e,d))*(atan(c));
f=(floor(e))/(pow(d,d));
a=(fdim(c,d))*(pow(e,a));
b=fmax(d,f);
if(isless(d,f)){
f=(atan2(d,a))+(sqrt(f));
b=(fmin(d,e))+(pow(f,f));
}
if(islessequal(a,a)){
b=(sin(f))-(fmax(d,f));
d=sqrt(b);
}
else{
f=(fmax(c,e))/(fmax(b,e));
f=tan(a);
f=(exp(e))+(pow(a,e));
c=fmin(a,f);
f=(fmin(a,e))-(ceil(b));
}
}