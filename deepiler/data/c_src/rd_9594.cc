#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan2(a,b))+(fmax(b,b));
a=pow(b,e);
a=log10(b);
a=(atan2(e,d))/(fmax(d,a));
a=(tan(e))/(tan(a));
b=(fmax(b,c))-(asin(d));
c=(fmax(d,d))/(pow(d,b));
b=fmin(c,d);
d=pow(e,c);
}