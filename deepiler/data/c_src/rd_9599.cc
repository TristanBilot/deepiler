#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(pow(g,g))/(sin(c));
c=(fmax(e,b))/(floor(d));
a=fmax(b,a);
d=(atan(e))+(fmax(g,d));
while(isgreaterequal(f,c)){
c=(atan2(d,b))-(atan2(e,b));
c=atan2(b,f);
f=fdim(f,f);
g=(pow(c,f))/(fmax(g,e));
d=(fdim(f,e))/(floor(e));
}
a=(exp(f))-(ceil(a));
g=sin(e);
e=(log(c))-(pow(b,e));
g=(sin(a))*(fmin(g,b));
b=fmin(e,b);
}