#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(atan(c))+(ceil(d));
e=(fdim(c,d))+(pow(c,a));
a=pow(e,c);
if(islessequal(e,a)){
e=(fdim(b,e))-(pow(a,a));
d=(pow(e,b))/(fmax(d,d));
}
d=(exp(e))+(log(a));
c=fmax(c,d);
}