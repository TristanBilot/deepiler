#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=asin(f);
a=(atan2(f,d))/(atan2(b,a));
d=(fmin(a,e))*(log(a));
d=(tan(a))/(exp(f));
while(isless(d,e)){
d=(tan(a))-(atan(d));
a=fmin(b,b);
d=(atan(a))*(fmin(a,e));
}
}