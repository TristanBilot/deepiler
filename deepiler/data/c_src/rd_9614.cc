#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(cos(e))/(pow(d,g));
f=fmin(h,f);
g=(fmin(c,g))/(ceil(h));
a=(asin(b))+(pow(d,e));
h=(atan2(c,d))/(acos(f));
b=(fdim(a,a))+(fdim(g,d));
if(islessgreater(a,h)){
h=(fmin(g,g))-(fdim(c,b));
d=(fdim(h,d))-(fmin(b,e));
e=fmin(f,d);
}
else{
g=(sqrt(b))/(pow(a,e));
f=(pow(c,b))*(sqrt(f));
c=(ceil(c))+(asin(e));
}
}