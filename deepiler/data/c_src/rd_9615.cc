#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(fmax(f,b))*(fdim(e,g));
a=(atan2(a,d))*(sqrt(c));
e=(pow(g,d))*(sin(c));
f=fmax(c,g);
if(islessgreater(d,e)){
f=atan2(f,c);
c=(floor(d))+(log10(g));
}
else{
g=fdim(d,g);
a=fmax(a,e);
f=floor(e);
g=(pow(f,a))+(acos(a));
}
c=(fmax(f,a))/(tan(f));
c=fdim(f,c);
a=(fmax(f,c))/(pow(g,g));
b=exp(d);
}