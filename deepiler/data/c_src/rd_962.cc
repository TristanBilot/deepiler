#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmin(b,d))*(cos(b));
d=atan2(d,a);
d=fdim(c,c);
d=fdim(a,d);
a=fmax(e,b);
d=(cos(e))*(atan(e));
a=log10(e);
if(islessequal(a,e)){
d=atan2(e,d);
e=(sqrt(d))*(cos(a));
a=fmax(c,b);
}
else{
b=fmin(c,b);
c=fmin(c,d);
}
}