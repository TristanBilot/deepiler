#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=(fmax(e,d))/(atan(c));
d=(log10(b))-(floor(b));
while(isless(b,b)){
a=fmin(d,e);
d=(cos(e))-(atan2(e,d));
e=(fdim(b,b))/(fdim(c,b));
a=(fmax(d,c))*(log10(c));
}
b=(fmin(a,d))/(acos(e));
d=pow(c,b);
}