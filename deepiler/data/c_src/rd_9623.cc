#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(ceil(g))-(fmax(f,f));
d=ceil(c);
d=fmax(b,f);
f=(exp(c))-(ceil(f));
f=ceil(d);
d=(pow(b,a))+(fmax(c,b));
a=(fdim(c,d))-(log10(b));
b=(cos(e))-(acos(b));
}