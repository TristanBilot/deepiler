#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=log(a);
e=(fmin(e,a))*(atan2(e,b));
b=(ceil(c))*(exp(b));
a=cos(c);
d=fmax(e,b);
if(islessequal(c,e)){
c=(floor(b))-(log10(c));
b=log10(b);
d=(fmin(b,b))/(fmax(c,b));
e=(pow(c,c))*(fmax(d,d));
a=fmax(a,d);
}
}