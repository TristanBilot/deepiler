#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fmax(e,c))+(ceil(b));
b=(fmin(a,c))+(atan2(f,e));
f=ceil(b);
f=cos(a);
a=(exp(c))-(cos(e));
if(islessequal(f,c)){
d=fmax(a,a);
c=sin(f);
}
else{
e=fmax(b,e);
b=fdim(d,c);
}
e=(atan2(a,b))+(sin(e));
a=(sqrt(a))+(exp(a));
e=(pow(f,a))*(atan(f));
a=asin(a);
f=(log10(f))/(fdim(f,b));
}