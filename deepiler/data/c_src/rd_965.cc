#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(pow(d,d))/(fmax(c,b));
d=fmin(c,b);
c=(fdim(c,d))+(tan(e));
d=fmin(a,b);
a=(sin(d))+(log10(e));
while(islessgreater(e,e)){
b=atan2(e,d);
b=(log(e))/(pow(c,c));
}
while(islessequal(a,a)){
e=atan(c);
a=(log10(b))/(atan(a));
c=sin(c);
}
}