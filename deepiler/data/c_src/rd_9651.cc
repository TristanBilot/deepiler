#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(ceil(c))/(fmin(c,a));
a=pow(b,e);
if(islessequal(f,c)){
e=(pow(e,a))/(fmax(c,a));
c=(sqrt(b))/(log(f));
}
while(islessgreater(d,d)){
a=(asin(b))/(sin(c));
e=pow(a,c);
f=pow(d,e);
d=(asin(e))+(fdim(a,d));
f=acos(a);
}
}