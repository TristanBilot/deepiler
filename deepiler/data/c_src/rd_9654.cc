#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(atan2(g,b))-(asin(e));
c=exp(a);
d=(fmax(b,f))*(sqrt(a));
h=pow(e,d);
g=(sqrt(e))/(sin(a));
a=fdim(e,g);
d=exp(e);
h=(fdim(c,f))*(floor(e));
g=(pow(g,g))/(fdim(c,c));
}