#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=(sqrt(f))+(atan2(a,c));
e=sin(c);
g=fdim(a,c);
g=(sqrt(d))/(log10(c));
e=(log10(a))/(sqrt(f));
c=cos(d);
a=floor(g);
e=fdim(a,d);
d=(ceil(d))-(fmin(b,a));
}