#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=(pow(d,d))/(sin(c));
d=fdim(b,f);
b=(acos(d))/(tan(f));
b=fdim(e,c);
g=(pow(c,e))-(pow(b,c));
f=(pow(e,a))*(asin(f));
d=fdim(e,c);
b=exp(a);
e=pow(f,e);
g=fmax(d,e);
if(islessequal(g,c)){
e=floor(e);
f=fmin(e,e);
g=floor(e);
}
else{
b=asin(b);
e=sqrt(g);
b=(exp(e))-(atan2(c,c));
f=fmax(g,b);
d=sqrt(c);
}
}