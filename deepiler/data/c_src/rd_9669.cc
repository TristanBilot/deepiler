#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=(fdim(f,g))*(fdim(b,a));
g=fdim(g,g);
b=(fmax(b,g))*(fmin(f,g));
f=fdim(e,e);
d=(tan(c))/(atan(c));
h=(atan(d))/(sin(a));
if(isless(d,e)){
f=sqrt(a);
h=(log10(b))*(atan2(h,b));
}
}