#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
a=pow(a,c);
e=atan2(a,a);
if(islessgreater(e,a)){
d=asin(e);
a=(atan(d))/(fmax(e,d));
a=fmax(c,c);
e=(log10(c))/(pow(d,e));
b=fmax(d,e);
}
else{
b=atan2(a,e);
b=fdim(b,c);
b=(sqrt(c))/(fmax(d,d));
b=sin(d);
b=(atan2(a,a))/(fmax(b,b));
}
c=(sqrt(e))+(fdim(a,d));
d=fmin(b,d);
}