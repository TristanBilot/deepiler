#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(fmin(c,b))+(atan(a));
d=fmin(e,d);
c=(fdim(a,b))-(fdim(c,b));
f=(exp(e))*(fmax(e,b));
b=(fmin(b,d))*(asin(e));
e=(fdim(c,c))+(log10(a));
e=fdim(c,c);
d=fmin(e,a);
a=sqrt(c);
f=(tan(e))/(log(f));
d=(log10(b))-(fdim(e,c));
e=pow(e,b);
}