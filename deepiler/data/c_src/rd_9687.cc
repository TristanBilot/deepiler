#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=atan2(a,e);
e=(log10(b))+(sin(f));
f=exp(g);
c=acos(f);
c=fmin(g,b);
b=(pow(g,f))*(fmax(e,c));
c=fdim(f,f);
while(isless(e,a)){
a=(fmax(g,b))+(fdim(a,d));
f=(atan(a))/(fmin(b,b));
e=(pow(a,f))*(tan(g));
a=fmin(e,d);
d=fmax(a,f);
}
}