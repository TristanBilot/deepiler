#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(fdim(c,c))*(fdim(d,b));
d=(cos(d))-(sqrt(e));
f=acos(d);
h=(fmax(f,a))/(floor(d));
f=atan2(h,e);
while(islessgreater(g,e)){
e=ceil(g);
b=log10(b);
f=atan(d);
}
if(isgreaterequal(h,g)){
g=tan(h);
g=(asin(g))/(tan(h));
g=(atan2(g,e))+(asin(b));
g=(ceil(h))+(atan2(b,a));
}
else{
a=fmax(a,e);
c=atan2(e,a);
g=fmin(h,h);
}
}