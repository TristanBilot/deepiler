#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(fmax(b,c))/(fmin(h,h));
b=(atan2(b,g))+(acos(d));
a=asin(a);
if(islessequal(b,e)){
c=(fmax(a,a))-(fmin(a,d));
b=(log10(f))+(atan2(a,b));
}
if(islessequal(e,e)){
f=log10(a);
c=atan2(c,a);
a=(asin(f))*(fmax(g,c));
d=(cos(f))/(log10(b));
}
else{
d=(fmax(g,d))*(acos(h));
g=(asin(e))*(fmax(h,b));
}
}