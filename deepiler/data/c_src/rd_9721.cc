#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=fmin(e,d);
f=fdim(a,e);
a=exp(b);
f=tan(d);
g=acos(d);
if(islessequal(f,e)){
e=fmax(g,f);
c=(floor(a))-(pow(b,c));
a=fmax(f,f);
c=(tan(f))*(acos(b));
}
while(islessgreater(g,f)){
a=(sqrt(f))*(fmin(a,f));
c=asin(g);
f=(pow(f,c))+(fmax(g,e));
}
}