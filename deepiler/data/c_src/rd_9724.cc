#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(c,d);
g=fmax(e,d);
g=(fdim(g,c))+(fmin(b,g));
f=(pow(d,c))-(fdim(g,f));
e=(cos(b))*(fdim(e,f));
b=(asin(c))/(fdim(a,e));
a=pow(e,b);
while(isgreaterequal(g,b)){
f=(pow(d,b))/(fmin(b,f));
a=(fmax(a,a))+(log10(g));
}
}