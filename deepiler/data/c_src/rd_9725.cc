#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
c=(fmin(b,a))*(tan(b));
e=(pow(c,d))*(atan2(a,b));
f=(fmax(d,b))+(pow(a,d));
e=(acos(b))/(atan(c));
d=(exp(f))/(atan(c));
c=(atan(b))+(exp(f));
f=(ceil(d))-(sqrt(e));
d=pow(e,a);
e=pow(f,a);
a=(atan(e))/(pow(c,d));
}