#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=(asin(a))-(atan2(c,a));
c=(pow(f,f))/(atan2(e,a));
d=(tan(e))/(sqrt(b));
d=fmax(e,f);
a=(log(c))*(asin(c));
if(isless(f,b)){
d=(fmin(c,d))-(atan2(b,c));
c=(exp(e))-(asin(b));
d=(pow(c,e))-(fmax(e,b));
}
if(islessequal(c,d)){
e=fdim(b,d);
a=(exp(e))/(exp(c));
a=(atan2(f,e))+(sin(d));
b=(atan2(c,f))-(pow(c,e));
d=exp(f);
}
}