#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=ceil(b);
c=(pow(e,d))+(fmax(e,c));
e=fdim(d,d);
d=(pow(d,d))+(cos(f));
c=floor(b);
c=fmax(a,c);
d=fmin(c,c);
if(islessequal(e,f)){
a=log(e);
f=exp(b);
d=(fdim(a,c))*(fmax(d,b));
a=fmax(b,d);
}
else{
f=tan(e);
a=asin(a);
f=(pow(d,a))*(fmax(d,f));
b=(fmax(e,c))/(pow(d,b));
}
}