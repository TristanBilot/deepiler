#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fdim(d,a);
f=(atan2(a,b))*(fmin(d,a));
c=atan(b);
d=(pow(f,c))*(ceil(c));
f=exp(c);
if(isless(e,d)){
b=(pow(d,b))/(fmin(f,e));
c=(tan(f))/(pow(a,e));
}
else{
d=asin(d);
d=(log10(d))+(exp(d));
a=log10(f);
b=(pow(b,f))+(fmin(c,b));
}
if(islessequal(f,f)){
d=pow(b,e);
d=(fdim(d,d))/(fmax(f,b));
c=floor(f);
}
else{
d=(ceil(b))-(tan(b));
d=(fmax(a,e))*(log10(d));
c=atan(d);
}
}