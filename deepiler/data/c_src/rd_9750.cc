#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(atan2(c,c))-(atan2(d,b));
c=fmax(c,d);
while(islessgreater(b,a)){
b=fmax(d,a);
d=(log(a))*(ceil(c));
d=(sin(a))-(cos(a));
a=(atan(e))+(floor(c));
}
if(islessgreater(e,e)){
b=fdim(c,a);
d=(tan(a))/(atan(a));
e=(fdim(a,b))-(pow(b,b));
}
}