#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=(fmax(d,f))-(sin(c));
h=ceil(a);
g=acos(c);
e=pow(h,c);
a=(floor(h))*(fmax(h,f));
while(isless(d,d)){
h=(exp(c))*(ceil(e));
c=(acos(d))*(tan(c));
}
if(isless(d,a)){
b=(floor(g))*(sin(h));
d=(atan2(h,a))/(ceil(b));
a=(log(g))+(log10(e));
h=cos(d);
c=(log10(h))+(log10(a));
}
else{
h=(atan2(e,b))-(sqrt(g));
c=ceil(e);
f=(fmin(d,d))*(ceil(d));
c=pow(b,e);
f=tan(d);
}
}