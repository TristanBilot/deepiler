#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=(fdim(a,d))*(fmin(b,e));
d=(fdim(e,d))*(fmin(a,c));
b=(sqrt(e))/(pow(c,c));
if(islessequal(d,c)){
d=pow(d,a);
a=log10(b);
e=cos(e);
}
if(isless(c,a)){
e=(atan2(a,c))*(fmin(e,d));
b=(fmax(d,e))/(cos(c));
a=fdim(e,c);
}
else{
e=pow(b,d);
e=sqrt(c);
}
}