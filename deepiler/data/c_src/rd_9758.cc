#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(a);
a=(exp(c))*(log(a));
d=(log10(a))*(fmax(f,d));
c=fmax(h,c);
if(isgreaterequal(a,g)){
f=(tan(c))-(sin(a));
d=(fmin(c,c))*(sqrt(e));
}
else{
h=(cos(e))/(atan2(g,a));
f=(acos(h))-(sin(f));
c=asin(g);
f=(acos(b))*(fmax(f,e));
e=fmax(e,e);
}
f=(atan2(g,a))*(sqrt(b));
a=(floor(h))/(log(f));
e=(asin(c))*(sqrt(c));
b=asin(e);
}