#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
c=atan(b);
d=fdim(c,f);
d=fdim(b,e);
g=fmin(d,f);
while(islessgreater(f,d)){
a=(sqrt(c))+(atan2(d,e));
c=fdim(a,g);
d=(exp(d))-(fdim(d,b));
a=(sqrt(f))-(atan2(e,b));
}
if(islessgreater(c,a)){
f=exp(e);
d=(atan2(c,e))+(log10(c));
e=(floor(g))*(fmin(g,g));
a=(sin(f))/(fmin(a,e));
c=sqrt(f);
}
else{
g=asin(c);
a=(ceil(e))/(sqrt(f));
}
}