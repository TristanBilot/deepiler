#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=log10(b);
b=log(f);
if(islessgreater(e,c)){
e=tan(d);
b=(ceil(a))*(fdim(e,e));
}
else{
f=(pow(a,f))/(sin(g));
g=(pow(f,a))/(atan2(f,e));
a=log(c);
a=fmax(e,b);
}
while(isgreaterequal(g,a)){
g=log(b);
d=(fmax(d,c))*(fmax(d,d));
f=(sin(e))+(tan(c));
c=(cos(c))/(floor(b));
}
}