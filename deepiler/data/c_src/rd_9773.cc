#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=fdim(c,a);
f=fdim(b,f);
a=(exp(a))+(fmin(a,d));
e=(atan2(d,e))*(atan2(f,a));
c=atan2(b,c);
a=fmax(b,f);
a=pow(d,d);
d=pow(a,e);
d=fmax(d,b);
f=log10(c);
}