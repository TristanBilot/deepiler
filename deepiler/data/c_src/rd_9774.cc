#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(pow(c,e))*(ceil(e));
a=(acos(f))/(pow(f,e));
if(isgreaterequal(b,b)){
d=fmax(a,a);
b=ceil(f);
c=fmax(f,a);
}
while(isgreaterequal(e,a)){
f=(pow(a,b))*(atan(d));
a=sin(d);
f=(log(a))*(sin(a));
c=asin(f);
}
}