#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=(log10(g))-(fdim(a,d));
b=(exp(d))*(fmax(f,a));
c=(fdim(g,e))*(exp(g));
e=fmax(e,c);
a=(pow(a,a))+(atan2(e,f));
if(islessequal(g,a)){
g=(fdim(b,b))-(pow(f,e));
c=fdim(e,g);
}
else{
b=atan2(d,d);
a=(floor(f))-(sin(b));
}
a=(sin(g))*(fmin(e,c));
g=fmax(d,g);
}