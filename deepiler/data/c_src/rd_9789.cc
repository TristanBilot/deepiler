#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
b=log10(e);
c=pow(a,c);
while(islessequal(g,e)){
b=cos(a);
f=(fdim(g,c))+(tan(b));
e=(log(a))/(fmax(e,b));
c=(atan2(f,b))-(log10(d));
}
while(islessgreater(e,d)){
f=(ceil(e))/(log(a));
b=fmax(d,e);
}
}