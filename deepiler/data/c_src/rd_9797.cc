#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=fmin(c,c);
b=asin(a);
e=fmax(d,a);
b=(fmax(e,c))*(log10(d));
b=sin(b);
while(isgreaterequal(b,d)){
d=ceil(c);
e=cos(e);
c=(ceil(d))-(fmax(b,b));
e=pow(b,c);
}
}