#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
b=(atan2(b,a))+(floor(a));
f=fmin(c,a);
f=(fdim(a,d))+(fmin(e,c));
d=(sqrt(c))+(sin(b));
e=(fmax(d,e))*(pow(d,e));
e=ceil(d);
if(isgreaterequal(c,b)){
f=(atan2(e,d))*(log10(b));
a=ceil(a);
c=(asin(d))*(ceil(c));
b=log(e);
e=(atan2(b,d))/(cos(a));
}
}