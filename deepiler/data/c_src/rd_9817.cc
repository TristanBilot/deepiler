#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=atan2(a,d);
e=fmin(a,c);
a=(atan2(d,a))-(pow(b,a));
if(isless(b,e)){
e=(atan(a))-(acos(d));
e=(atan(a))*(tan(c));
b=atan2(d,e);
e=fmax(a,a);
c=fmin(a,b);
}
while(isgreaterequal(c,b)){
a=sqrt(c);
e=(fdim(a,c))/(tan(e));
a=(atan2(a,c))-(atan(a));
}
}