#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=fmin(e,d);
b=(atan2(e,b))+(fdim(a,e));
c=(sin(a))*(atan2(b,a));
while(isless(e,d)){
e=pow(c,a);
d=log(c);
d=fdim(e,b);
f=(floor(g))-(tan(a));
}
b=sin(b);
b=(exp(d))+(floor(g));
d=fmax(c,g);
a=fmin(g,d);
g=fmin(a,f);
}