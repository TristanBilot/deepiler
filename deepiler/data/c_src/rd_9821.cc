#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
f=pow(a,d);
d=(fdim(e,c))*(pow(a,f));
d=(fmax(d,d))*(fmax(f,f));
c=tan(e);
f=(atan2(f,c))-(fmax(f,f));
while(islessgreater(b,b)){
b=(atan2(b,b))/(atan2(c,a));
c=(log(d))+(log10(d));
d=log(e);
b=sqrt(e);
a=ceil(a);
}
while(islessequal(d,b)){
e=(pow(e,a))-(log10(e));
d=atan(f);
f=pow(b,a);
}
}