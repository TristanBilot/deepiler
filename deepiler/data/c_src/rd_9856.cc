#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=(log(a))+(exp(c));
f=tan(b);
a=sqrt(c);
while(isless(g,h)){
b=(fmin(f,d))/(cos(d));
g=(fdim(c,a))*(sqrt(b));
}
while(islessgreater(f,g)){
f=(atan2(a,d))-(pow(g,b));
g=(log10(g))-(fmax(c,b));
h=tan(h);
g=tan(e);
}
}