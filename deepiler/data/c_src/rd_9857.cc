#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=atan(e);
c=log10(b);
e=(sin(a))+(pow(b,c));
if(islessequal(a,d)){
c=(fmax(c,e))*(fmax(c,c));
d=(log10(a))+(atan(a));
d=(exp(d))/(fdim(d,e));
a=(log(e))*(atan2(e,e));
}
else{
a=fmin(d,c);
d=pow(d,d);
}
while(islessequal(e,e)){
e=fmax(d,d);
c=(asin(b))+(exp(d));
}
}