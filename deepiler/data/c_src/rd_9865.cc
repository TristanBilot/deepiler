#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(f);
d=log10(g);
c=fmax(a,h);
if(isless(g,c)){
f=(tan(a))-(cos(a));
a=(log10(c))+(pow(b,d));
d=fdim(e,a);
}
else{
a=fdim(c,d);
a=(fmin(b,a))/(pow(h,c));
}
if(isless(d,c)){
a=log10(h);
b=asin(g);
g=atan2(f,a);
a=(fmax(b,d))+(fdim(d,c));
g=floor(a);
}
}