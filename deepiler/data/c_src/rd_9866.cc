#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
b=(sqrt(e))-(pow(c,d));
c=fmax(d,a);
c=pow(e,c);
while(islessequal(e,c)){
d=pow(b,e);
b=log(b);
a=asin(d);
}
a=atan2(d,d);
a=asin(b);
b=sin(d);
e=floor(c);
}