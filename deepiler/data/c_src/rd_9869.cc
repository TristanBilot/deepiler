#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(f);
g=fmax(a,f);
while(isgreaterequal(f,e)){
e=(acos(g))*(sqrt(d));
b=fmin(d,a);
h=atan2(f,h);
h=(pow(g,c))+(atan(b));
b=pow(g,d);
}
f=(acos(d))-(sqrt(e));
b=(fmin(h,c))-(pow(f,a));
f=atan2(c,b);
c=fdim(e,d);
h=asin(a);
}