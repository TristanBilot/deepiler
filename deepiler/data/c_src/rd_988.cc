#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=log10(c);
b=pow(c,d);
e=(fmax(b,b))*(pow(c,e));
b=floor(b);
while(isgreaterequal(d,c)){
a=(tan(b))/(sin(d));
d=atan2(b,e);
d=(exp(d))-(sqrt(d));
e=fmax(a,b);
c=(sqrt(d))*(floor(b));
}
c=exp(e);
c=fmax(a,d);
a=(fmax(c,a))-(fmax(d,e));
}