#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(e,e);
h=(fmin(d,c))/(tan(d));
c=(pow(e,c))-(pow(a,e));
f=(pow(h,d))-(ceil(f));
g=(atan2(f,d))*(fmax(d,f));
d=(pow(c,e))/(log10(g));
g=(fmax(b,b))+(cos(e));
h=(pow(h,b))/(asin(b));
b=(fmin(d,g))*(pow(f,f));
h=ceil(a);
if(islessgreater(f,f)){
a=(atan(h))+(atan(c));
a=(fdim(d,h))-(fmin(g,a));
a=ceil(g);
}
}