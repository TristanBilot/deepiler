#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=asin(d);
f=(atan2(e,b))*(pow(d,c));
e=(fmax(e,a))/(floor(a));
if(islessequal(e,a)){
b=atan2(e,f);
b=asin(f);
a=(pow(b,a))*(atan2(f,d));
e=fdim(d,f);
}
while(isgreaterequal(e,b)){
b=fmax(e,c);
c=(atan2(d,a))-(log10(f));
}
}