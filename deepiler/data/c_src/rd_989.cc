#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(atan2(d,a))*(fmin(d,d));
e=fmin(f,b);
c=(fmax(f,b))/(sqrt(b));
while(isgreaterequal(e,f)){
f=(fdim(a,e))*(exp(a));
b=(atan2(e,a))+(exp(a));
f=(pow(d,d))-(atan(c));
e=pow(c,f);
a=atan2(d,c);
}
if(isgreaterequal(b,c)){
a=log10(f);
e=pow(a,c);
f=fmin(a,b);
}
else{
c=(asin(b))/(exp(d));
a=fmax(d,e);
e=fmax(e,a);
}
}