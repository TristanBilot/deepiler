#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
a=(ceil(c))*(sin(b));
f=fmax(b,b);
c=fdim(a,b);
a=(fmax(e,d))/(fmax(c,f));
b=(fdim(b,c))*(atan(a));
a=(pow(f,e))/(log(f));
a=(fmax(a,e))/(fmin(e,c));
d=(fmax(e,b))+(fdim(e,e));
e=sin(f);
if(isless(c,b)){
d=(fmax(f,f))-(fdim(c,d));
b=tan(c);
c=atan(f);
}
else{
d=(fmin(f,b))-(exp(c));
c=atan2(b,a);
a=(acos(b))*(asin(e));
d=acos(f);
}
}