#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
e=(fmin(c,b))*(exp(f));
d=pow(b,c);
c=fmax(a,b);
a=(acos(f))-(log(g));
g=(exp(e))/(log(b));
b=(pow(g,e))/(atan2(e,b));
e=(atan(a))+(sin(g));
while(isgreaterequal(c,f)){
g=fmax(d,f);
b=(fmin(a,e))-(sqrt(g));
g=fdim(d,c);
}
}