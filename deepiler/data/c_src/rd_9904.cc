#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(a,d);
a=acos(e);
if(islessgreater(g,c)){
b=(log10(d))*(asin(c));
g=log(a);
g=exp(e);
g=(asin(c))*(fdim(a,e));
}
else{
h=pow(a,g);
b=(exp(c))-(fdim(e,f));
}
while(islessequal(g,d)){
f=(sin(e))*(atan2(a,e));
c=(fmax(h,c))*(sqrt(e));
a=fmax(c,g);
g=(fdim(g,f))/(cos(e));
}
}