#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(cos(b))*(fmin(f,b));
c=atan(c);
d=(cos(a))*(fmax(e,d));
while(islessequal(a,f)){
c=atan2(c,f);
d=asin(c);
}
if(isgreaterequal(b,f)){
a=exp(f);
a=(asin(a))+(fdim(c,c));
}
else{
f=(fmin(d,d))+(cos(b));
e=(atan2(a,a))-(fdim(e,a));
f=ceil(b);
e=(atan(a))+(atan2(f,f));
}
}