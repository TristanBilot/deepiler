#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
d=(fdim(b,c))*(fmin(d,c));
e=pow(b,f);
if(islessequal(c,b)){
a=(log(d))/(fmin(e,e));
f=(fmax(b,f))*(atan2(e,f));
}
while(islessgreater(e,d)){
d=(floor(c))*(fmin(a,d));
a=fmax(c,e);
f=atan2(f,b);
}
}