#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
d=(fmax(e,f))+(fmax(c,a));
g=(atan2(d,b))-(pow(b,b));
b=(asin(d))-(atan2(e,e));
a=acos(f);
g=(fmax(g,b))+(log10(g));
while(isgreaterequal(d,d)){
c=atan2(b,e);
g=acos(g);
g=(fmax(b,g))/(fmin(f,b));
}
if(isgreaterequal(d,g)){
f=(fmax(e,a))-(asin(a));
a=fmax(g,e);
b=(fmin(a,e))-(atan2(g,c));
b=fmin(b,f);
}
}