#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
d=(fmax(d,b))+(log10(b));
c=log(d);
c=(floor(d))/(fdim(e,d));
b=atan2(d,b);
while(islessgreater(a,a)){
b=(atan2(a,c))*(fmin(e,b));
a=(atan(a))+(log(e));
e=(exp(e))*(asin(a));
}
e=ceil(b);
e=asin(b);
d=fdim(e,e);
}