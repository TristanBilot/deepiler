#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=(floor(e))-(exp(b));
g=(exp(c))+(fmax(b,c));
g=(cos(c))/(fdim(d,a));
f=(acos(h))-(sin(e));
c=fmin(b,c);
a=atan2(e,h);
b=(fdim(f,c))/(atan2(a,g));
g=(floor(g))+(fmin(a,d));
c=sqrt(e);
}