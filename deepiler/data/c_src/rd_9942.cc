#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
f=(exp(e))/(fdim(e,c));
f=(atan(a))+(atan(e));
f=(pow(g,g))*(fmin(b,e));
e=(atan2(b,f))/(acos(c));
b=fmax(d,g);
e=fmax(e,b);
a=(fmax(c,c))*(atan(e));
c=sqrt(g);
a=(pow(d,f))*(fdim(f,e));
a=sin(b);
e=(sqrt(d))/(sin(d));
c=(fmax(c,e))+(fmin(a,e));
}