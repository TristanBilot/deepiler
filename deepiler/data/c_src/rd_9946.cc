#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
a=fdim(f,c);
e=(pow(a,b))-(fmax(c,d));
e=(fmin(b,g))-(log10(f));
if(islessequal(a,c)){
e=fdim(d,g);
c=(sqrt(a))*(sqrt(e));
}
else{
b=(pow(d,a))*(fmax(c,f));
e=fmin(e,f);
}
b=tan(d);
g=(atan(d))*(fmin(c,g));
g=fmax(c,b);
}