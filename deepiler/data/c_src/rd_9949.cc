#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=(exp(h))+(pow(f,h));
d=(sqrt(e))*(asin(c));
if(islessequal(a,h)){
b=(atan2(g,c))+(atan2(e,f));
f=(fmax(c,h))/(fmin(f,b));
c=tan(f);
e=(log(b))-(ceil(b));
d=log(a);
}
else{
b=(tan(a))*(fmin(d,d));
f=fmin(h,f);
f=(exp(h))*(atan2(f,d));
d=(atan(c))/(ceil(a));
g=(exp(e))-(fmax(c,h));
}
if(isgreaterequal(f,d)){
h=(fmin(f,h))/(pow(g,a));
c=pow(a,c);
}
else{
e=(pow(e,b))*(pow(c,e));
g=(pow(b,e))-(floor(e));
}
}