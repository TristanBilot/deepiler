#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
e=pow(e,d);
c=fdim(b,e);
a=(fmin(b,e))*(exp(e));
while(islessgreater(d,a)){
c=(tan(c))/(tan(b));
e=fdim(a,e);
}
while(isless(e,d)){
e=(fmax(a,b))+(pow(c,e));
b=(atan2(d,d))/(atan(a));
e=log10(a);
c=pow(a,d);
d=(asin(d))-(floor(e));
}
}