#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
e=(ceil(d))/(fdim(c,e));
c=fmax(b,b);
c=exp(e);
while(islessequal(a,a)){
b=(fmin(f,b))+(atan2(d,b));
a=(log10(c))*(asin(c));
b=fdim(c,c);
}
d=(fdim(e,f))*(exp(c));
d=fdim(b,a);
}