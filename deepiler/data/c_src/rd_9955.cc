#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(a);
d=fmax(d,c);
f=(atan2(a,a))+(log(a));
h=sqrt(f);
h=log(d);
g=(log10(a))+(sin(e));
a=log(c);
g=(exp(d))/(acos(a));
if(islessequal(g,d)){
g=(sqrt(a))+(asin(a));
h=ceil(f);
f=fmin(f,e);
e=ceil(e);
d=fmax(c,h);
}
}