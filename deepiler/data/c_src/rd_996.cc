#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(tan(b))/(ceil(d));
c=tan(e);
while(islessequal(b,b)){
b=(acos(a))+(acos(c));
b=(atan2(d,b))-(log10(e));
e=(atan2(b,b))/(acos(b));
e=pow(c,b);
c=(fmin(e,d))/(pow(d,a));
}
if(islessequal(d,e)){
b=(asin(a))-(log10(b));
e=(fdim(c,d))-(ceil(e));
b=ceil(b);
}
else{
a=(pow(b,a))/(fmax(b,a));
b=floor(a);
d=(floor(a))*(atan2(c,e));
}
}