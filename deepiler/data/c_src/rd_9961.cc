#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
c=(fmin(b,a))*(fdim(a,c));
d=(log10(e))-(cos(c));
b=(atan2(e,b))/(log(e));
b=fdim(a,d);
b=acos(e);
if(islessequal(c,d)){
d=(atan2(c,c))-(fdim(e,a));
c=atan(e);
d=floor(e);
}
d=(ceil(a))*(atan2(c,a));
e=fmin(d,b);
e=fmax(a,c);
d=(log10(a))-(acos(c));
}