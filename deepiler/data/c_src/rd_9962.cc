#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=(pow(a,b))+(log10(c));
f=fdim(c,d);
h=(fdim(b,c))-(atan2(b,d));
while(isless(a,d)){
b=(log10(g))*(sqrt(b));
g=fmin(d,d);
b=fdim(d,a);
h=(fmax(a,h))-(atan(c));
}
if(isless(g,g)){
a=fmin(b,h);
e=(ceil(a))*(acos(h));
}
else{
g=fdim(c,b);
h=(fdim(d,b))+(sqrt(a));
h=(fmax(c,f))+(tan(g));
e=(exp(c))/(fmin(b,h));
g=tan(h);
}
}