#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
g=(tan(a))*(sqrt(b));
a=(fmax(f,b))+(pow(g,c));
b=fmax(f,f);
e=fmin(e,d);
while(islessequal(d,c)){
d=(atan2(g,e))+(sqrt(f));
d=floor(e);
a=fmax(e,b);
e=fmin(b,b);
}
while(isless(e,g)){
f=acos(f);
e=(sqrt(a))+(asin(e));
}
}